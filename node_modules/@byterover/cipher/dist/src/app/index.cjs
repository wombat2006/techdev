#!/usr/bin/env node
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __esm = (fn, res) => function __init() {
  return fn && (res = (0, fn[__getOwnPropNames(fn)[0]])(fn = 0)), res;
};
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// node_modules/.pnpm/tsup@8.5.0_postcss@8.5.6_tsx@4.20.3_typescript@5.8.3_yaml@2.8.0/node_modules/tsup/assets/cjs_shims.js
var getImportMetaUrl, importMetaUrl;
var init_cjs_shims = __esm({
  "node_modules/.pnpm/tsup@8.5.0_postcss@8.5.6_tsx@4.20.3_typescript@5.8.3_yaml@2.8.0/node_modules/tsup/assets/cjs_shims.js"() {
    getImportMetaUrl = () => typeof document === "undefined" ? new URL(`file:${__filename}`).href : document.currentScript && document.currentScript.src || new URL("main.js", document.baseURI).href;
    importMetaUrl = /* @__PURE__ */ getImportMetaUrl();
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js
var util, objectUtil, ZodParsedType, getParsedType;
var init_util = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/util.js"() {
    init_cjs_shims();
    (function(util2) {
      util2.assertEqual = (_) => {
      };
      function assertIs(_arg) {
      }
      util2.assertIs = assertIs;
      function assertNever(_x) {
        throw new Error();
      }
      util2.assertNever = assertNever;
      util2.arrayToEnum = (items) => {
        const obj = {};
        for (const item of items) {
          obj[item] = item;
        }
        return obj;
      };
      util2.getValidEnumValues = (obj) => {
        const validKeys = util2.objectKeys(obj).filter((k) => typeof obj[obj[k]] !== "number");
        const filtered = {};
        for (const k of validKeys) {
          filtered[k] = obj[k];
        }
        return util2.objectValues(filtered);
      };
      util2.objectValues = (obj) => {
        return util2.objectKeys(obj).map(function(e) {
          return obj[e];
        });
      };
      util2.objectKeys = typeof Object.keys === "function" ? (obj) => Object.keys(obj) : (object) => {
        const keys = [];
        for (const key in object) {
          if (Object.prototype.hasOwnProperty.call(object, key)) {
            keys.push(key);
          }
        }
        return keys;
      };
      util2.find = (arr, checker) => {
        for (const item of arr) {
          if (checker(item))
            return item;
        }
        return void 0;
      };
      util2.isInteger = typeof Number.isInteger === "function" ? (val) => Number.isInteger(val) : (val) => typeof val === "number" && Number.isFinite(val) && Math.floor(val) === val;
      function joinValues(array, separator = " | ") {
        return array.map((val) => typeof val === "string" ? `'${val}'` : val).join(separator);
      }
      util2.joinValues = joinValues;
      util2.jsonStringifyReplacer = (_, value) => {
        if (typeof value === "bigint") {
          return value.toString();
        }
        return value;
      };
    })(util || (util = {}));
    (function(objectUtil2) {
      objectUtil2.mergeShapes = (first, second) => {
        return {
          ...first,
          ...second
          // second overwrites first
        };
      };
    })(objectUtil || (objectUtil = {}));
    ZodParsedType = util.arrayToEnum([
      "string",
      "nan",
      "number",
      "integer",
      "float",
      "boolean",
      "date",
      "bigint",
      "symbol",
      "function",
      "undefined",
      "null",
      "array",
      "object",
      "unknown",
      "promise",
      "void",
      "never",
      "map",
      "set"
    ]);
    getParsedType = (data) => {
      const t = typeof data;
      switch (t) {
        case "undefined":
          return ZodParsedType.undefined;
        case "string":
          return ZodParsedType.string;
        case "number":
          return Number.isNaN(data) ? ZodParsedType.nan : ZodParsedType.number;
        case "boolean":
          return ZodParsedType.boolean;
        case "function":
          return ZodParsedType.function;
        case "bigint":
          return ZodParsedType.bigint;
        case "symbol":
          return ZodParsedType.symbol;
        case "object":
          if (Array.isArray(data)) {
            return ZodParsedType.array;
          }
          if (data === null) {
            return ZodParsedType.null;
          }
          if (data.then && typeof data.then === "function" && data.catch && typeof data.catch === "function") {
            return ZodParsedType.promise;
          }
          if (typeof Map !== "undefined" && data instanceof Map) {
            return ZodParsedType.map;
          }
          if (typeof Set !== "undefined" && data instanceof Set) {
            return ZodParsedType.set;
          }
          if (typeof Date !== "undefined" && data instanceof Date) {
            return ZodParsedType.date;
          }
          return ZodParsedType.object;
        default:
          return ZodParsedType.unknown;
      }
    };
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js
var ZodIssueCode, quotelessJson, ZodError;
var init_ZodError = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/ZodError.js"() {
    init_cjs_shims();
    init_util();
    ZodIssueCode = util.arrayToEnum([
      "invalid_type",
      "invalid_literal",
      "custom",
      "invalid_union",
      "invalid_union_discriminator",
      "invalid_enum_value",
      "unrecognized_keys",
      "invalid_arguments",
      "invalid_return_type",
      "invalid_date",
      "invalid_string",
      "too_small",
      "too_big",
      "invalid_intersection_types",
      "not_multiple_of",
      "not_finite"
    ]);
    quotelessJson = (obj) => {
      const json = JSON.stringify(obj, null, 2);
      return json.replace(/"([^"]+)":/g, "$1:");
    };
    ZodError = class _ZodError extends Error {
      get errors() {
        return this.issues;
      }
      constructor(issues) {
        super();
        this.issues = [];
        this.addIssue = (sub) => {
          this.issues = [...this.issues, sub];
        };
        this.addIssues = (subs = []) => {
          this.issues = [...this.issues, ...subs];
        };
        const actualProto = new.target.prototype;
        if (Object.setPrototypeOf) {
          Object.setPrototypeOf(this, actualProto);
        } else {
          this.__proto__ = actualProto;
        }
        this.name = "ZodError";
        this.issues = issues;
      }
      format(_mapper) {
        const mapper = _mapper || function(issue) {
          return issue.message;
        };
        const fieldErrors = { _errors: [] };
        const processError = (error) => {
          for (const issue of error.issues) {
            if (issue.code === "invalid_union") {
              issue.unionErrors.map(processError);
            } else if (issue.code === "invalid_return_type") {
              processError(issue.returnTypeError);
            } else if (issue.code === "invalid_arguments") {
              processError(issue.argumentsError);
            } else if (issue.path.length === 0) {
              fieldErrors._errors.push(mapper(issue));
            } else {
              let curr = fieldErrors;
              let i = 0;
              while (i < issue.path.length) {
                const el = issue.path[i];
                const terminal = i === issue.path.length - 1;
                if (!terminal) {
                  curr[el] = curr[el] || { _errors: [] };
                } else {
                  curr[el] = curr[el] || { _errors: [] };
                  curr[el]._errors.push(mapper(issue));
                }
                curr = curr[el];
                i++;
              }
            }
          }
        };
        processError(this);
        return fieldErrors;
      }
      static assert(value) {
        if (!(value instanceof _ZodError)) {
          throw new Error(`Not a ZodError: ${value}`);
        }
      }
      toString() {
        return this.message;
      }
      get message() {
        return JSON.stringify(this.issues, util.jsonStringifyReplacer, 2);
      }
      get isEmpty() {
        return this.issues.length === 0;
      }
      flatten(mapper = (issue) => issue.message) {
        const fieldErrors = {};
        const formErrors = [];
        for (const sub of this.issues) {
          if (sub.path.length > 0) {
            const firstEl = sub.path[0];
            fieldErrors[firstEl] = fieldErrors[firstEl] || [];
            fieldErrors[firstEl].push(mapper(sub));
          } else {
            formErrors.push(mapper(sub));
          }
        }
        return { formErrors, fieldErrors };
      }
      get formErrors() {
        return this.flatten();
      }
    };
    ZodError.create = (issues) => {
      const error = new ZodError(issues);
      return error;
    };
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js
var errorMap, en_default;
var init_en = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/locales/en.js"() {
    init_cjs_shims();
    init_ZodError();
    init_util();
    errorMap = (issue, _ctx) => {
      let message;
      switch (issue.code) {
        case ZodIssueCode.invalid_type:
          if (issue.received === ZodParsedType.undefined) {
            message = "Required";
          } else {
            message = `Expected ${issue.expected}, received ${issue.received}`;
          }
          break;
        case ZodIssueCode.invalid_literal:
          message = `Invalid literal value, expected ${JSON.stringify(issue.expected, util.jsonStringifyReplacer)}`;
          break;
        case ZodIssueCode.unrecognized_keys:
          message = `Unrecognized key(s) in object: ${util.joinValues(issue.keys, ", ")}`;
          break;
        case ZodIssueCode.invalid_union:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_union_discriminator:
          message = `Invalid discriminator value. Expected ${util.joinValues(issue.options)}`;
          break;
        case ZodIssueCode.invalid_enum_value:
          message = `Invalid enum value. Expected ${util.joinValues(issue.options)}, received '${issue.received}'`;
          break;
        case ZodIssueCode.invalid_arguments:
          message = `Invalid function arguments`;
          break;
        case ZodIssueCode.invalid_return_type:
          message = `Invalid function return type`;
          break;
        case ZodIssueCode.invalid_date:
          message = `Invalid date`;
          break;
        case ZodIssueCode.invalid_string:
          if (typeof issue.validation === "object") {
            if ("includes" in issue.validation) {
              message = `Invalid input: must include "${issue.validation.includes}"`;
              if (typeof issue.validation.position === "number") {
                message = `${message} at one or more positions greater than or equal to ${issue.validation.position}`;
              }
            } else if ("startsWith" in issue.validation) {
              message = `Invalid input: must start with "${issue.validation.startsWith}"`;
            } else if ("endsWith" in issue.validation) {
              message = `Invalid input: must end with "${issue.validation.endsWith}"`;
            } else {
              util.assertNever(issue.validation);
            }
          } else if (issue.validation !== "regex") {
            message = `Invalid ${issue.validation}`;
          } else {
            message = "Invalid";
          }
          break;
        case ZodIssueCode.too_small:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `more than`} ${issue.minimum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? "exactly" : issue.inclusive ? `at least` : `over`} ${issue.minimum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
          else if (issue.type === "bigint")
            message = `Number must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${issue.minimum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly equal to ` : issue.inclusive ? `greater than or equal to ` : `greater than `}${new Date(Number(issue.minimum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.too_big:
          if (issue.type === "array")
            message = `Array must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `less than`} ${issue.maximum} element(s)`;
          else if (issue.type === "string")
            message = `String must contain ${issue.exact ? `exactly` : issue.inclusive ? `at most` : `under`} ${issue.maximum} character(s)`;
          else if (issue.type === "number")
            message = `Number must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "bigint")
            message = `BigInt must be ${issue.exact ? `exactly` : issue.inclusive ? `less than or equal to` : `less than`} ${issue.maximum}`;
          else if (issue.type === "date")
            message = `Date must be ${issue.exact ? `exactly` : issue.inclusive ? `smaller than or equal to` : `smaller than`} ${new Date(Number(issue.maximum))}`;
          else
            message = "Invalid input";
          break;
        case ZodIssueCode.custom:
          message = `Invalid input`;
          break;
        case ZodIssueCode.invalid_intersection_types:
          message = `Intersection results could not be merged`;
          break;
        case ZodIssueCode.not_multiple_of:
          message = `Number must be a multiple of ${issue.multipleOf}`;
          break;
        case ZodIssueCode.not_finite:
          message = "Number must be finite";
          break;
        default:
          message = _ctx.defaultError;
          util.assertNever(issue);
      }
      return { message };
    };
    en_default = errorMap;
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js
function setErrorMap(map) {
  overrideErrorMap = map;
}
function getErrorMap() {
  return overrideErrorMap;
}
var overrideErrorMap;
var init_errors = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/errors.js"() {
    init_cjs_shims();
    init_en();
    overrideErrorMap = en_default;
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js
function addIssueToContext(ctx, issueData) {
  const overrideMap = getErrorMap();
  const issue = makeIssue({
    issueData,
    data: ctx.data,
    path: ctx.path,
    errorMaps: [
      ctx.common.contextualErrorMap,
      // contextual error map is first priority
      ctx.schemaErrorMap,
      // then schema-bound map if available
      overrideMap,
      // then global override map
      overrideMap === en_default ? void 0 : en_default
      // then global default map
    ].filter((x) => !!x)
  });
  ctx.common.issues.push(issue);
}
var makeIssue, EMPTY_PATH, ParseStatus, INVALID, DIRTY, OK, isAborted, isDirty, isValid, isAsync;
var init_parseUtil = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/parseUtil.js"() {
    init_cjs_shims();
    init_errors();
    init_en();
    makeIssue = (params) => {
      const { data, path: path12, errorMaps, issueData } = params;
      const fullPath = [...path12, ...issueData.path || []];
      const fullIssue = {
        ...issueData,
        path: fullPath
      };
      if (issueData.message !== void 0) {
        return {
          ...issueData,
          path: fullPath,
          message: issueData.message
        };
      }
      let errorMessage = "";
      const maps = errorMaps.filter((m) => !!m).slice().reverse();
      for (const map of maps) {
        errorMessage = map(fullIssue, { data, defaultError: errorMessage }).message;
      }
      return {
        ...issueData,
        path: fullPath,
        message: errorMessage
      };
    };
    EMPTY_PATH = [];
    ParseStatus = class _ParseStatus {
      constructor() {
        this.value = "valid";
      }
      dirty() {
        if (this.value === "valid")
          this.value = "dirty";
      }
      abort() {
        if (this.value !== "aborted")
          this.value = "aborted";
      }
      static mergeArray(status, results) {
        const arrayValue = [];
        for (const s of results) {
          if (s.status === "aborted")
            return INVALID;
          if (s.status === "dirty")
            status.dirty();
          arrayValue.push(s.value);
        }
        return { status: status.value, value: arrayValue };
      }
      static async mergeObjectAsync(status, pairs) {
        const syncPairs = [];
        for (const pair of pairs) {
          const key = await pair.key;
          const value = await pair.value;
          syncPairs.push({
            key,
            value
          });
        }
        return _ParseStatus.mergeObjectSync(status, syncPairs);
      }
      static mergeObjectSync(status, pairs) {
        const finalObject = {};
        for (const pair of pairs) {
          const { key, value } = pair;
          if (key.status === "aborted")
            return INVALID;
          if (value.status === "aborted")
            return INVALID;
          if (key.status === "dirty")
            status.dirty();
          if (value.status === "dirty")
            status.dirty();
          if (key.value !== "__proto__" && (typeof value.value !== "undefined" || pair.alwaysSet)) {
            finalObject[key.value] = value.value;
          }
        }
        return { status: status.value, value: finalObject };
      }
    };
    INVALID = Object.freeze({
      status: "aborted"
    });
    DIRTY = (value) => ({ status: "dirty", value });
    OK = (value) => ({ status: "valid", value });
    isAborted = (x) => x.status === "aborted";
    isDirty = (x) => x.status === "dirty";
    isValid = (x) => x.status === "valid";
    isAsync = (x) => typeof Promise !== "undefined" && x instanceof Promise;
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/typeAliases.js
var init_typeAliases = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/typeAliases.js"() {
    init_cjs_shims();
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js
var errorUtil;
var init_errorUtil = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/helpers/errorUtil.js"() {
    init_cjs_shims();
    (function(errorUtil2) {
      errorUtil2.errToObj = (message) => typeof message === "string" ? { message } : message || {};
      errorUtil2.toString = (message) => typeof message === "string" ? message : message?.message;
    })(errorUtil || (errorUtil = {}));
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js
function processCreateParams(params) {
  if (!params)
    return {};
  const { errorMap: errorMap2, invalid_type_error, required_error, description } = params;
  if (errorMap2 && (invalid_type_error || required_error)) {
    throw new Error(`Can't use "invalid_type_error" or "required_error" in conjunction with custom error map.`);
  }
  if (errorMap2)
    return { errorMap: errorMap2, description };
  const customMap = (iss, ctx) => {
    const { message } = params;
    if (iss.code === "invalid_enum_value") {
      return { message: message ?? ctx.defaultError };
    }
    if (typeof ctx.data === "undefined") {
      return { message: message ?? required_error ?? ctx.defaultError };
    }
    if (iss.code !== "invalid_type")
      return { message: ctx.defaultError };
    return { message: message ?? invalid_type_error ?? ctx.defaultError };
  };
  return { errorMap: customMap, description };
}
function timeRegexSource(args) {
  let secondsRegexSource = `[0-5]\\d`;
  if (args.precision) {
    secondsRegexSource = `${secondsRegexSource}\\.\\d{${args.precision}}`;
  } else if (args.precision == null) {
    secondsRegexSource = `${secondsRegexSource}(\\.\\d+)?`;
  }
  const secondsQuantifier = args.precision ? "+" : "?";
  return `([01]\\d|2[0-3]):[0-5]\\d(:${secondsRegexSource})${secondsQuantifier}`;
}
function timeRegex(args) {
  return new RegExp(`^${timeRegexSource(args)}$`);
}
function datetimeRegex(args) {
  let regex2 = `${dateRegexSource}T${timeRegexSource(args)}`;
  const opts = [];
  opts.push(args.local ? `Z?` : `Z`);
  if (args.offset)
    opts.push(`([+-]\\d{2}:?\\d{2})`);
  regex2 = `${regex2}(${opts.join("|")})`;
  return new RegExp(`^${regex2}$`);
}
function isValidIP(ip, version) {
  if ((version === "v4" || !version) && ipv4Regex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6Regex.test(ip)) {
    return true;
  }
  return false;
}
function isValidJWT(jwt, alg) {
  if (!jwtRegex.test(jwt))
    return false;
  try {
    const [header] = jwt.split(".");
    if (!header)
      return false;
    const base64 = header.replace(/-/g, "+").replace(/_/g, "/").padEnd(header.length + (4 - header.length % 4) % 4, "=");
    const decoded = JSON.parse(atob(base64));
    if (typeof decoded !== "object" || decoded === null)
      return false;
    if ("typ" in decoded && decoded?.typ !== "JWT")
      return false;
    if (!decoded.alg)
      return false;
    if (alg && decoded.alg !== alg)
      return false;
    return true;
  } catch {
    return false;
  }
}
function isValidCidr(ip, version) {
  if ((version === "v4" || !version) && ipv4CidrRegex.test(ip)) {
    return true;
  }
  if ((version === "v6" || !version) && ipv6CidrRegex.test(ip)) {
    return true;
  }
  return false;
}
function floatSafeRemainder(val, step) {
  const valDecCount = (val.toString().split(".")[1] || "").length;
  const stepDecCount = (step.toString().split(".")[1] || "").length;
  const decCount = valDecCount > stepDecCount ? valDecCount : stepDecCount;
  const valInt = Number.parseInt(val.toFixed(decCount).replace(".", ""));
  const stepInt = Number.parseInt(step.toFixed(decCount).replace(".", ""));
  return valInt % stepInt / 10 ** decCount;
}
function deepPartialify(schema) {
  if (schema instanceof ZodObject) {
    const newShape = {};
    for (const key in schema.shape) {
      const fieldSchema = schema.shape[key];
      newShape[key] = ZodOptional.create(deepPartialify(fieldSchema));
    }
    return new ZodObject({
      ...schema._def,
      shape: () => newShape
    });
  } else if (schema instanceof ZodArray) {
    return new ZodArray({
      ...schema._def,
      type: deepPartialify(schema.element)
    });
  } else if (schema instanceof ZodOptional) {
    return ZodOptional.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodNullable) {
    return ZodNullable.create(deepPartialify(schema.unwrap()));
  } else if (schema instanceof ZodTuple) {
    return ZodTuple.create(schema.items.map((item) => deepPartialify(item)));
  } else {
    return schema;
  }
}
function mergeValues(a, b) {
  const aType = getParsedType(a);
  const bType = getParsedType(b);
  if (a === b) {
    return { valid: true, data: a };
  } else if (aType === ZodParsedType.object && bType === ZodParsedType.object) {
    const bKeys = util.objectKeys(b);
    const sharedKeys = util.objectKeys(a).filter((key) => bKeys.indexOf(key) !== -1);
    const newObj = { ...a, ...b };
    for (const key of sharedKeys) {
      const sharedValue = mergeValues(a[key], b[key]);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newObj[key] = sharedValue.data;
    }
    return { valid: true, data: newObj };
  } else if (aType === ZodParsedType.array && bType === ZodParsedType.array) {
    if (a.length !== b.length) {
      return { valid: false };
    }
    const newArray = [];
    for (let index = 0; index < a.length; index++) {
      const itemA = a[index];
      const itemB = b[index];
      const sharedValue = mergeValues(itemA, itemB);
      if (!sharedValue.valid) {
        return { valid: false };
      }
      newArray.push(sharedValue.data);
    }
    return { valid: true, data: newArray };
  } else if (aType === ZodParsedType.date && bType === ZodParsedType.date && +a === +b) {
    return { valid: true, data: a };
  } else {
    return { valid: false };
  }
}
function createZodEnum(values, params) {
  return new ZodEnum({
    values,
    typeName: ZodFirstPartyTypeKind.ZodEnum,
    ...processCreateParams(params)
  });
}
function cleanParams(params, data) {
  const p = typeof params === "function" ? params(data) : typeof params === "string" ? { message: params } : params;
  const p2 = typeof p === "string" ? { message: p } : p;
  return p2;
}
function custom(check, _params = {}, fatal) {
  if (check)
    return ZodAny.create().superRefine((data, ctx) => {
      const r = check(data);
      if (r instanceof Promise) {
        return r.then((r2) => {
          if (!r2) {
            const params = cleanParams(_params, data);
            const _fatal = params.fatal ?? fatal ?? true;
            ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
          }
        });
      }
      if (!r) {
        const params = cleanParams(_params, data);
        const _fatal = params.fatal ?? fatal ?? true;
        ctx.addIssue({ code: "custom", ...params, fatal: _fatal });
      }
      return;
    });
  return ZodAny.create();
}
var ParseInputLazyPath, handleResult, ZodType, cuidRegex, cuid2Regex, ulidRegex, uuidRegex, nanoidRegex, jwtRegex, durationRegex, emailRegex, _emojiRegex, emojiRegex, ipv4Regex, ipv4CidrRegex, ipv6Regex, ipv6CidrRegex, base64Regex, base64urlRegex, dateRegexSource, dateRegex, ZodString, ZodNumber, ZodBigInt, ZodBoolean, ZodDate, ZodSymbol, ZodUndefined, ZodNull, ZodAny, ZodUnknown, ZodNever, ZodVoid, ZodArray, ZodObject, ZodUnion, getDiscriminator, ZodDiscriminatedUnion, ZodIntersection, ZodTuple, ZodRecord, ZodMap, ZodSet, ZodFunction, ZodLazy, ZodLiteral, ZodEnum, ZodNativeEnum, ZodPromise, ZodEffects, ZodOptional, ZodNullable, ZodDefault, ZodCatch, ZodNaN, BRAND, ZodBranded, ZodPipeline, ZodReadonly, late, ZodFirstPartyTypeKind, instanceOfType, stringType, numberType, nanType, bigIntType, booleanType, dateType, symbolType, undefinedType, nullType, anyType, unknownType, neverType, voidType, arrayType, objectType, strictObjectType, unionType, discriminatedUnionType, intersectionType, tupleType, recordType, mapType, setType, functionType, lazyType, literalType, enumType, nativeEnumType, promiseType, effectsType, optionalType, nullableType, preprocessType, pipelineType, ostring, onumber, oboolean, coerce, NEVER;
var init_types = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/types.js"() {
    init_cjs_shims();
    init_ZodError();
    init_errors();
    init_errorUtil();
    init_parseUtil();
    init_util();
    ParseInputLazyPath = class {
      constructor(parent, value, path12, key) {
        this._cachedPath = [];
        this.parent = parent;
        this.data = value;
        this._path = path12;
        this._key = key;
      }
      get path() {
        if (!this._cachedPath.length) {
          if (Array.isArray(this._key)) {
            this._cachedPath.push(...this._path, ...this._key);
          } else {
            this._cachedPath.push(...this._path, this._key);
          }
        }
        return this._cachedPath;
      }
    };
    handleResult = (ctx, result) => {
      if (isValid(result)) {
        return { success: true, data: result.value };
      } else {
        if (!ctx.common.issues.length) {
          throw new Error("Validation failed but no issues detected.");
        }
        return {
          success: false,
          get error() {
            if (this._error)
              return this._error;
            const error = new ZodError(ctx.common.issues);
            this._error = error;
            return this._error;
          }
        };
      }
    };
    ZodType = class {
      get description() {
        return this._def.description;
      }
      _getType(input) {
        return getParsedType(input.data);
      }
      _getOrReturnCtx(input, ctx) {
        return ctx || {
          common: input.parent.common,
          data: input.data,
          parsedType: getParsedType(input.data),
          schemaErrorMap: this._def.errorMap,
          path: input.path,
          parent: input.parent
        };
      }
      _processInputParams(input) {
        return {
          status: new ParseStatus(),
          ctx: {
            common: input.parent.common,
            data: input.data,
            parsedType: getParsedType(input.data),
            schemaErrorMap: this._def.errorMap,
            path: input.path,
            parent: input.parent
          }
        };
      }
      _parseSync(input) {
        const result = this._parse(input);
        if (isAsync(result)) {
          throw new Error("Synchronous parse encountered promise.");
        }
        return result;
      }
      _parseAsync(input) {
        const result = this._parse(input);
        return Promise.resolve(result);
      }
      parse(data, params) {
        const result = this.safeParse(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      safeParse(data, params) {
        const ctx = {
          common: {
            issues: [],
            async: params?.async ?? false,
            contextualErrorMap: params?.errorMap
          },
          path: params?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const result = this._parseSync({ data, path: ctx.path, parent: ctx });
        return handleResult(ctx, result);
      }
      "~validate"(data) {
        const ctx = {
          common: {
            issues: [],
            async: !!this["~standard"].async
          },
          path: [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        if (!this["~standard"].async) {
          try {
            const result = this._parseSync({ data, path: [], parent: ctx });
            return isValid(result) ? {
              value: result.value
            } : {
              issues: ctx.common.issues
            };
          } catch (err) {
            if (err?.message?.toLowerCase()?.includes("encountered")) {
              this["~standard"].async = true;
            }
            ctx.common = {
              issues: [],
              async: true
            };
          }
        }
        return this._parseAsync({ data, path: [], parent: ctx }).then((result) => isValid(result) ? {
          value: result.value
        } : {
          issues: ctx.common.issues
        });
      }
      async parseAsync(data, params) {
        const result = await this.safeParseAsync(data, params);
        if (result.success)
          return result.data;
        throw result.error;
      }
      async safeParseAsync(data, params) {
        const ctx = {
          common: {
            issues: [],
            contextualErrorMap: params?.errorMap,
            async: true
          },
          path: params?.path || [],
          schemaErrorMap: this._def.errorMap,
          parent: null,
          data,
          parsedType: getParsedType(data)
        };
        const maybeAsyncResult = this._parse({ data, path: ctx.path, parent: ctx });
        const result = await (isAsync(maybeAsyncResult) ? maybeAsyncResult : Promise.resolve(maybeAsyncResult));
        return handleResult(ctx, result);
      }
      refine(check, message) {
        const getIssueProperties = (val) => {
          if (typeof message === "string" || typeof message === "undefined") {
            return { message };
          } else if (typeof message === "function") {
            return message(val);
          } else {
            return message;
          }
        };
        return this._refinement((val, ctx) => {
          const result = check(val);
          const setError = () => ctx.addIssue({
            code: ZodIssueCode.custom,
            ...getIssueProperties(val)
          });
          if (typeof Promise !== "undefined" && result instanceof Promise) {
            return result.then((data) => {
              if (!data) {
                setError();
                return false;
              } else {
                return true;
              }
            });
          }
          if (!result) {
            setError();
            return false;
          } else {
            return true;
          }
        });
      }
      refinement(check, refinementData) {
        return this._refinement((val, ctx) => {
          if (!check(val)) {
            ctx.addIssue(typeof refinementData === "function" ? refinementData(val, ctx) : refinementData);
            return false;
          } else {
            return true;
          }
        });
      }
      _refinement(refinement) {
        return new ZodEffects({
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "refinement", refinement }
        });
      }
      superRefine(refinement) {
        return this._refinement(refinement);
      }
      constructor(def) {
        this.spa = this.safeParseAsync;
        this._def = def;
        this.parse = this.parse.bind(this);
        this.safeParse = this.safeParse.bind(this);
        this.parseAsync = this.parseAsync.bind(this);
        this.safeParseAsync = this.safeParseAsync.bind(this);
        this.spa = this.spa.bind(this);
        this.refine = this.refine.bind(this);
        this.refinement = this.refinement.bind(this);
        this.superRefine = this.superRefine.bind(this);
        this.optional = this.optional.bind(this);
        this.nullable = this.nullable.bind(this);
        this.nullish = this.nullish.bind(this);
        this.array = this.array.bind(this);
        this.promise = this.promise.bind(this);
        this.or = this.or.bind(this);
        this.and = this.and.bind(this);
        this.transform = this.transform.bind(this);
        this.brand = this.brand.bind(this);
        this.default = this.default.bind(this);
        this.catch = this.catch.bind(this);
        this.describe = this.describe.bind(this);
        this.pipe = this.pipe.bind(this);
        this.readonly = this.readonly.bind(this);
        this.isNullable = this.isNullable.bind(this);
        this.isOptional = this.isOptional.bind(this);
        this["~standard"] = {
          version: 1,
          vendor: "zod",
          validate: (data) => this["~validate"](data)
        };
      }
      optional() {
        return ZodOptional.create(this, this._def);
      }
      nullable() {
        return ZodNullable.create(this, this._def);
      }
      nullish() {
        return this.nullable().optional();
      }
      array() {
        return ZodArray.create(this);
      }
      promise() {
        return ZodPromise.create(this, this._def);
      }
      or(option) {
        return ZodUnion.create([this, option], this._def);
      }
      and(incoming) {
        return ZodIntersection.create(this, incoming, this._def);
      }
      transform(transform) {
        return new ZodEffects({
          ...processCreateParams(this._def),
          schema: this,
          typeName: ZodFirstPartyTypeKind.ZodEffects,
          effect: { type: "transform", transform }
        });
      }
      default(def) {
        const defaultValueFunc = typeof def === "function" ? def : () => def;
        return new ZodDefault({
          ...processCreateParams(this._def),
          innerType: this,
          defaultValue: defaultValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodDefault
        });
      }
      brand() {
        return new ZodBranded({
          typeName: ZodFirstPartyTypeKind.ZodBranded,
          type: this,
          ...processCreateParams(this._def)
        });
      }
      catch(def) {
        const catchValueFunc = typeof def === "function" ? def : () => def;
        return new ZodCatch({
          ...processCreateParams(this._def),
          innerType: this,
          catchValue: catchValueFunc,
          typeName: ZodFirstPartyTypeKind.ZodCatch
        });
      }
      describe(description) {
        const This = this.constructor;
        return new This({
          ...this._def,
          description
        });
      }
      pipe(target) {
        return ZodPipeline.create(this, target);
      }
      readonly() {
        return ZodReadonly.create(this);
      }
      isOptional() {
        return this.safeParse(void 0).success;
      }
      isNullable() {
        return this.safeParse(null).success;
      }
    };
    cuidRegex = /^c[^\s-]{8,}$/i;
    cuid2Regex = /^[0-9a-z]+$/;
    ulidRegex = /^[0-9A-HJKMNP-TV-Z]{26}$/i;
    uuidRegex = /^[0-9a-fA-F]{8}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{4}\b-[0-9a-fA-F]{12}$/i;
    nanoidRegex = /^[a-z0-9_-]{21}$/i;
    jwtRegex = /^[A-Za-z0-9-_]+\.[A-Za-z0-9-_]+\.[A-Za-z0-9-_]*$/;
    durationRegex = /^[-+]?P(?!$)(?:(?:[-+]?\d+Y)|(?:[-+]?\d+[.,]\d+Y$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:(?:[-+]?\d+W)|(?:[-+]?\d+[.,]\d+W$))?(?:(?:[-+]?\d+D)|(?:[-+]?\d+[.,]\d+D$))?(?:T(?=[\d+-])(?:(?:[-+]?\d+H)|(?:[-+]?\d+[.,]\d+H$))?(?:(?:[-+]?\d+M)|(?:[-+]?\d+[.,]\d+M$))?(?:[-+]?\d+(?:[.,]\d+)?S)?)??$/;
    emailRegex = /^(?!\.)(?!.*\.\.)([A-Z0-9_'+\-\.]*)[A-Z0-9_+-]@([A-Z0-9][A-Z0-9\-]*\.)+[A-Z]{2,}$/i;
    _emojiRegex = `^(\\p{Extended_Pictographic}|\\p{Emoji_Component})+$`;
    ipv4Regex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])$/;
    ipv4CidrRegex = /^(?:(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\.){3}(?:25[0-5]|2[0-4][0-9]|1[0-9][0-9]|[1-9][0-9]|[0-9])\/(3[0-2]|[12]?[0-9])$/;
    ipv6Regex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))$/;
    ipv6CidrRegex = /^(([0-9a-fA-F]{1,4}:){7,7}[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,7}:|([0-9a-fA-F]{1,4}:){1,6}:[0-9a-fA-F]{1,4}|([0-9a-fA-F]{1,4}:){1,5}(:[0-9a-fA-F]{1,4}){1,2}|([0-9a-fA-F]{1,4}:){1,4}(:[0-9a-fA-F]{1,4}){1,3}|([0-9a-fA-F]{1,4}:){1,3}(:[0-9a-fA-F]{1,4}){1,4}|([0-9a-fA-F]{1,4}:){1,2}(:[0-9a-fA-F]{1,4}){1,5}|[0-9a-fA-F]{1,4}:((:[0-9a-fA-F]{1,4}){1,6})|:((:[0-9a-fA-F]{1,4}){1,7}|:)|fe80:(:[0-9a-fA-F]{0,4}){0,4}%[0-9a-zA-Z]{1,}|::(ffff(:0{1,4}){0,1}:){0,1}((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])|([0-9a-fA-F]{1,4}:){1,4}:((25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9])\.){3,3}(25[0-5]|(2[0-4]|1{0,1}[0-9]){0,1}[0-9]))\/(12[0-8]|1[01][0-9]|[1-9]?[0-9])$/;
    base64Regex = /^([0-9a-zA-Z+/]{4})*(([0-9a-zA-Z+/]{2}==)|([0-9a-zA-Z+/]{3}=))?$/;
    base64urlRegex = /^([0-9a-zA-Z-_]{4})*(([0-9a-zA-Z-_]{2}(==)?)|([0-9a-zA-Z-_]{3}(=)?))?$/;
    dateRegexSource = `((\\d\\d[2468][048]|\\d\\d[13579][26]|\\d\\d0[48]|[02468][048]00|[13579][26]00)-02-29|\\d{4}-((0[13578]|1[02])-(0[1-9]|[12]\\d|3[01])|(0[469]|11)-(0[1-9]|[12]\\d|30)|(02)-(0[1-9]|1\\d|2[0-8])))`;
    dateRegex = new RegExp(`^${dateRegexSource}$`);
    ZodString = class _ZodString extends ZodType {
      _parse(input) {
        if (this._def.coerce) {
          input.data = String(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.string) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.string,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const status = new ParseStatus();
        let ctx = void 0;
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            if (input.data.length < check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            if (input.data.length > check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check.value,
                type: "string",
                inclusive: true,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "length") {
            const tooBig = input.data.length > check.value;
            const tooSmall = input.data.length < check.value;
            if (tooBig || tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              if (tooBig) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_big,
                  maximum: check.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check.message
                });
              } else if (tooSmall) {
                addIssueToContext(ctx, {
                  code: ZodIssueCode.too_small,
                  minimum: check.value,
                  type: "string",
                  inclusive: true,
                  exact: true,
                  message: check.message
                });
              }
              status.dirty();
            }
          } else if (check.kind === "email") {
            if (!emailRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "email",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "emoji") {
            if (!emojiRegex) {
              emojiRegex = new RegExp(_emojiRegex, "u");
            }
            if (!emojiRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "emoji",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "uuid") {
            if (!uuidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "uuid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "nanoid") {
            if (!nanoidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "nanoid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cuid") {
            if (!cuidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cuid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cuid2") {
            if (!cuid2Regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cuid2",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "ulid") {
            if (!ulidRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "ulid",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "url") {
            try {
              new URL(input.data);
            } catch {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "url",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "regex") {
            check.regex.lastIndex = 0;
            const testResult = check.regex.test(input.data);
            if (!testResult) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "regex",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "trim") {
            input.data = input.data.trim();
          } else if (check.kind === "includes") {
            if (!input.data.includes(check.value, check.position)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { includes: check.value, position: check.position },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "toLowerCase") {
            input.data = input.data.toLowerCase();
          } else if (check.kind === "toUpperCase") {
            input.data = input.data.toUpperCase();
          } else if (check.kind === "startsWith") {
            if (!input.data.startsWith(check.value)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { startsWith: check.value },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "endsWith") {
            if (!input.data.endsWith(check.value)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: { endsWith: check.value },
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "datetime") {
            const regex2 = datetimeRegex(check);
            if (!regex2.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "datetime",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "date") {
            const regex2 = dateRegex;
            if (!regex2.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "date",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "time") {
            const regex2 = timeRegex(check);
            if (!regex2.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_string,
                validation: "time",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "duration") {
            if (!durationRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "duration",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "ip") {
            if (!isValidIP(input.data, check.version)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "ip",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "jwt") {
            if (!isValidJWT(input.data, check.alg)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "jwt",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "cidr") {
            if (!isValidCidr(input.data, check.version)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "cidr",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "base64") {
            if (!base64Regex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "base64",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "base64url") {
            if (!base64urlRegex.test(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                validation: "base64url",
                code: ZodIssueCode.invalid_string,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      _regex(regex2, validation, message) {
        return this.refinement((data) => regex2.test(data), {
          validation,
          code: ZodIssueCode.invalid_string,
          ...errorUtil.errToObj(message)
        });
      }
      _addCheck(check) {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      email(message) {
        return this._addCheck({ kind: "email", ...errorUtil.errToObj(message) });
      }
      url(message) {
        return this._addCheck({ kind: "url", ...errorUtil.errToObj(message) });
      }
      emoji(message) {
        return this._addCheck({ kind: "emoji", ...errorUtil.errToObj(message) });
      }
      uuid(message) {
        return this._addCheck({ kind: "uuid", ...errorUtil.errToObj(message) });
      }
      nanoid(message) {
        return this._addCheck({ kind: "nanoid", ...errorUtil.errToObj(message) });
      }
      cuid(message) {
        return this._addCheck({ kind: "cuid", ...errorUtil.errToObj(message) });
      }
      cuid2(message) {
        return this._addCheck({ kind: "cuid2", ...errorUtil.errToObj(message) });
      }
      ulid(message) {
        return this._addCheck({ kind: "ulid", ...errorUtil.errToObj(message) });
      }
      base64(message) {
        return this._addCheck({ kind: "base64", ...errorUtil.errToObj(message) });
      }
      base64url(message) {
        return this._addCheck({
          kind: "base64url",
          ...errorUtil.errToObj(message)
        });
      }
      jwt(options) {
        return this._addCheck({ kind: "jwt", ...errorUtil.errToObj(options) });
      }
      ip(options) {
        return this._addCheck({ kind: "ip", ...errorUtil.errToObj(options) });
      }
      cidr(options) {
        return this._addCheck({ kind: "cidr", ...errorUtil.errToObj(options) });
      }
      datetime(options) {
        if (typeof options === "string") {
          return this._addCheck({
            kind: "datetime",
            precision: null,
            offset: false,
            local: false,
            message: options
          });
        }
        return this._addCheck({
          kind: "datetime",
          precision: typeof options?.precision === "undefined" ? null : options?.precision,
          offset: options?.offset ?? false,
          local: options?.local ?? false,
          ...errorUtil.errToObj(options?.message)
        });
      }
      date(message) {
        return this._addCheck({ kind: "date", message });
      }
      time(options) {
        if (typeof options === "string") {
          return this._addCheck({
            kind: "time",
            precision: null,
            message: options
          });
        }
        return this._addCheck({
          kind: "time",
          precision: typeof options?.precision === "undefined" ? null : options?.precision,
          ...errorUtil.errToObj(options?.message)
        });
      }
      duration(message) {
        return this._addCheck({ kind: "duration", ...errorUtil.errToObj(message) });
      }
      regex(regex2, message) {
        return this._addCheck({
          kind: "regex",
          regex: regex2,
          ...errorUtil.errToObj(message)
        });
      }
      includes(value, options) {
        return this._addCheck({
          kind: "includes",
          value,
          position: options?.position,
          ...errorUtil.errToObj(options?.message)
        });
      }
      startsWith(value, message) {
        return this._addCheck({
          kind: "startsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      endsWith(value, message) {
        return this._addCheck({
          kind: "endsWith",
          value,
          ...errorUtil.errToObj(message)
        });
      }
      min(minLength, message) {
        return this._addCheck({
          kind: "min",
          value: minLength,
          ...errorUtil.errToObj(message)
        });
      }
      max(maxLength, message) {
        return this._addCheck({
          kind: "max",
          value: maxLength,
          ...errorUtil.errToObj(message)
        });
      }
      length(len, message) {
        return this._addCheck({
          kind: "length",
          value: len,
          ...errorUtil.errToObj(message)
        });
      }
      /**
       * Equivalent to `.min(1)`
       */
      nonempty(message) {
        return this.min(1, errorUtil.errToObj(message));
      }
      trim() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "trim" }]
        });
      }
      toLowerCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toLowerCase" }]
        });
      }
      toUpperCase() {
        return new _ZodString({
          ...this._def,
          checks: [...this._def.checks, { kind: "toUpperCase" }]
        });
      }
      get isDatetime() {
        return !!this._def.checks.find((ch) => ch.kind === "datetime");
      }
      get isDate() {
        return !!this._def.checks.find((ch) => ch.kind === "date");
      }
      get isTime() {
        return !!this._def.checks.find((ch) => ch.kind === "time");
      }
      get isDuration() {
        return !!this._def.checks.find((ch) => ch.kind === "duration");
      }
      get isEmail() {
        return !!this._def.checks.find((ch) => ch.kind === "email");
      }
      get isURL() {
        return !!this._def.checks.find((ch) => ch.kind === "url");
      }
      get isEmoji() {
        return !!this._def.checks.find((ch) => ch.kind === "emoji");
      }
      get isUUID() {
        return !!this._def.checks.find((ch) => ch.kind === "uuid");
      }
      get isNANOID() {
        return !!this._def.checks.find((ch) => ch.kind === "nanoid");
      }
      get isCUID() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid");
      }
      get isCUID2() {
        return !!this._def.checks.find((ch) => ch.kind === "cuid2");
      }
      get isULID() {
        return !!this._def.checks.find((ch) => ch.kind === "ulid");
      }
      get isIP() {
        return !!this._def.checks.find((ch) => ch.kind === "ip");
      }
      get isCIDR() {
        return !!this._def.checks.find((ch) => ch.kind === "cidr");
      }
      get isBase64() {
        return !!this._def.checks.find((ch) => ch.kind === "base64");
      }
      get isBase64url() {
        return !!this._def.checks.find((ch) => ch.kind === "base64url");
      }
      get minLength() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxLength() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
    };
    ZodString.create = (params) => {
      return new ZodString({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodString,
        coerce: params?.coerce ?? false,
        ...processCreateParams(params)
      });
    };
    ZodNumber = class _ZodNumber extends ZodType {
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
        this.step = this.multipleOf;
      }
      _parse(input) {
        if (this._def.coerce) {
          input.data = Number(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.number) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.number,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        let ctx = void 0;
        const status = new ParseStatus();
        for (const check of this._def.checks) {
          if (check.kind === "int") {
            if (!util.isInteger(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.invalid_type,
                expected: "integer",
                received: "float",
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "min") {
            const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                minimum: check.value,
                type: "number",
                inclusive: check.inclusive,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                maximum: check.value,
                type: "number",
                inclusive: check.inclusive,
                exact: false,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "multipleOf") {
            if (floatSafeRemainder(input.data, check.value) !== 0) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check.value,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "finite") {
            if (!Number.isFinite(input.data)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_finite,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodNumber({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodNumber({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      int(message) {
        return this._addCheck({
          kind: "int",
          message: errorUtil.toString(message)
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: 0,
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      finite(message) {
        return this._addCheck({
          kind: "finite",
          message: errorUtil.toString(message)
        });
      }
      safe(message) {
        return this._addCheck({
          kind: "min",
          inclusive: true,
          value: Number.MIN_SAFE_INTEGER,
          message: errorUtil.toString(message)
        })._addCheck({
          kind: "max",
          inclusive: true,
          value: Number.MAX_SAFE_INTEGER,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxValue() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
      get isInt() {
        return !!this._def.checks.find((ch) => ch.kind === "int" || ch.kind === "multipleOf" && util.isInteger(ch.value));
      }
      get isFinite() {
        let max = null;
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "finite" || ch.kind === "int" || ch.kind === "multipleOf") {
            return true;
          } else if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          } else if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return Number.isFinite(min) && Number.isFinite(max);
      }
    };
    ZodNumber.create = (params) => {
      return new ZodNumber({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodNumber,
        coerce: params?.coerce || false,
        ...processCreateParams(params)
      });
    };
    ZodBigInt = class _ZodBigInt extends ZodType {
      constructor() {
        super(...arguments);
        this.min = this.gte;
        this.max = this.lte;
      }
      _parse(input) {
        if (this._def.coerce) {
          try {
            input.data = BigInt(input.data);
          } catch {
            return this._getInvalidInput(input);
          }
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.bigint) {
          return this._getInvalidInput(input);
        }
        let ctx = void 0;
        const status = new ParseStatus();
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            const tooSmall = check.inclusive ? input.data < check.value : input.data <= check.value;
            if (tooSmall) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                type: "bigint",
                minimum: check.value,
                inclusive: check.inclusive,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            const tooBig = check.inclusive ? input.data > check.value : input.data >= check.value;
            if (tooBig) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                type: "bigint",
                maximum: check.value,
                inclusive: check.inclusive,
                message: check.message
              });
              status.dirty();
            }
          } else if (check.kind === "multipleOf") {
            if (input.data % check.value !== BigInt(0)) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.not_multiple_of,
                multipleOf: check.value,
                message: check.message
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return { status: status.value, value: input.data };
      }
      _getInvalidInput(input) {
        const ctx = this._getOrReturnCtx(input);
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.bigint,
          received: ctx.parsedType
        });
        return INVALID;
      }
      gte(value, message) {
        return this.setLimit("min", value, true, errorUtil.toString(message));
      }
      gt(value, message) {
        return this.setLimit("min", value, false, errorUtil.toString(message));
      }
      lte(value, message) {
        return this.setLimit("max", value, true, errorUtil.toString(message));
      }
      lt(value, message) {
        return this.setLimit("max", value, false, errorUtil.toString(message));
      }
      setLimit(kind, value, inclusive, message) {
        return new _ZodBigInt({
          ...this._def,
          checks: [
            ...this._def.checks,
            {
              kind,
              value,
              inclusive,
              message: errorUtil.toString(message)
            }
          ]
        });
      }
      _addCheck(check) {
        return new _ZodBigInt({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      positive(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      negative(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: false,
          message: errorUtil.toString(message)
        });
      }
      nonpositive(message) {
        return this._addCheck({
          kind: "max",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      nonnegative(message) {
        return this._addCheck({
          kind: "min",
          value: BigInt(0),
          inclusive: true,
          message: errorUtil.toString(message)
        });
      }
      multipleOf(value, message) {
        return this._addCheck({
          kind: "multipleOf",
          value,
          message: errorUtil.toString(message)
        });
      }
      get minValue() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min;
      }
      get maxValue() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max;
      }
    };
    ZodBigInt.create = (params) => {
      return new ZodBigInt({
        checks: [],
        typeName: ZodFirstPartyTypeKind.ZodBigInt,
        coerce: params?.coerce ?? false,
        ...processCreateParams(params)
      });
    };
    ZodBoolean = class extends ZodType {
      _parse(input) {
        if (this._def.coerce) {
          input.data = Boolean(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.boolean) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.boolean,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodBoolean.create = (params) => {
      return new ZodBoolean({
        typeName: ZodFirstPartyTypeKind.ZodBoolean,
        coerce: params?.coerce || false,
        ...processCreateParams(params)
      });
    };
    ZodDate = class _ZodDate extends ZodType {
      _parse(input) {
        if (this._def.coerce) {
          input.data = new Date(input.data);
        }
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.date) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.date,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        if (Number.isNaN(input.data.getTime())) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_date
          });
          return INVALID;
        }
        const status = new ParseStatus();
        let ctx = void 0;
        for (const check of this._def.checks) {
          if (check.kind === "min") {
            if (input.data.getTime() < check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_small,
                message: check.message,
                inclusive: true,
                exact: false,
                minimum: check.value,
                type: "date"
              });
              status.dirty();
            }
          } else if (check.kind === "max") {
            if (input.data.getTime() > check.value) {
              ctx = this._getOrReturnCtx(input, ctx);
              addIssueToContext(ctx, {
                code: ZodIssueCode.too_big,
                message: check.message,
                inclusive: true,
                exact: false,
                maximum: check.value,
                type: "date"
              });
              status.dirty();
            }
          } else {
            util.assertNever(check);
          }
        }
        return {
          status: status.value,
          value: new Date(input.data.getTime())
        };
      }
      _addCheck(check) {
        return new _ZodDate({
          ...this._def,
          checks: [...this._def.checks, check]
        });
      }
      min(minDate, message) {
        return this._addCheck({
          kind: "min",
          value: minDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      max(maxDate, message) {
        return this._addCheck({
          kind: "max",
          value: maxDate.getTime(),
          message: errorUtil.toString(message)
        });
      }
      get minDate() {
        let min = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "min") {
            if (min === null || ch.value > min)
              min = ch.value;
          }
        }
        return min != null ? new Date(min) : null;
      }
      get maxDate() {
        let max = null;
        for (const ch of this._def.checks) {
          if (ch.kind === "max") {
            if (max === null || ch.value < max)
              max = ch.value;
          }
        }
        return max != null ? new Date(max) : null;
      }
    };
    ZodDate.create = (params) => {
      return new ZodDate({
        checks: [],
        coerce: params?.coerce || false,
        typeName: ZodFirstPartyTypeKind.ZodDate,
        ...processCreateParams(params)
      });
    };
    ZodSymbol = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.symbol) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.symbol,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodSymbol.create = (params) => {
      return new ZodSymbol({
        typeName: ZodFirstPartyTypeKind.ZodSymbol,
        ...processCreateParams(params)
      });
    };
    ZodUndefined = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.undefined,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodUndefined.create = (params) => {
      return new ZodUndefined({
        typeName: ZodFirstPartyTypeKind.ZodUndefined,
        ...processCreateParams(params)
      });
    };
    ZodNull = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.null) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.null,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodNull.create = (params) => {
      return new ZodNull({
        typeName: ZodFirstPartyTypeKind.ZodNull,
        ...processCreateParams(params)
      });
    };
    ZodAny = class extends ZodType {
      constructor() {
        super(...arguments);
        this._any = true;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodAny.create = (params) => {
      return new ZodAny({
        typeName: ZodFirstPartyTypeKind.ZodAny,
        ...processCreateParams(params)
      });
    };
    ZodUnknown = class extends ZodType {
      constructor() {
        super(...arguments);
        this._unknown = true;
      }
      _parse(input) {
        return OK(input.data);
      }
    };
    ZodUnknown.create = (params) => {
      return new ZodUnknown({
        typeName: ZodFirstPartyTypeKind.ZodUnknown,
        ...processCreateParams(params)
      });
    };
    ZodNever = class extends ZodType {
      _parse(input) {
        const ctx = this._getOrReturnCtx(input);
        addIssueToContext(ctx, {
          code: ZodIssueCode.invalid_type,
          expected: ZodParsedType.never,
          received: ctx.parsedType
        });
        return INVALID;
      }
    };
    ZodNever.create = (params) => {
      return new ZodNever({
        typeName: ZodFirstPartyTypeKind.ZodNever,
        ...processCreateParams(params)
      });
    };
    ZodVoid = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.undefined) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.void,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return OK(input.data);
      }
    };
    ZodVoid.create = (params) => {
      return new ZodVoid({
        typeName: ZodFirstPartyTypeKind.ZodVoid,
        ...processCreateParams(params)
      });
    };
    ZodArray = class _ZodArray extends ZodType {
      _parse(input) {
        const { ctx, status } = this._processInputParams(input);
        const def = this._def;
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (def.exactLength !== null) {
          const tooBig = ctx.data.length > def.exactLength.value;
          const tooSmall = ctx.data.length < def.exactLength.value;
          if (tooBig || tooSmall) {
            addIssueToContext(ctx, {
              code: tooBig ? ZodIssueCode.too_big : ZodIssueCode.too_small,
              minimum: tooSmall ? def.exactLength.value : void 0,
              maximum: tooBig ? def.exactLength.value : void 0,
              type: "array",
              inclusive: true,
              exact: true,
              message: def.exactLength.message
            });
            status.dirty();
          }
        }
        if (def.minLength !== null) {
          if (ctx.data.length < def.minLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.minLength.message
            });
            status.dirty();
          }
        }
        if (def.maxLength !== null) {
          if (ctx.data.length > def.maxLength.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxLength.value,
              type: "array",
              inclusive: true,
              exact: false,
              message: def.maxLength.message
            });
            status.dirty();
          }
        }
        if (ctx.common.async) {
          return Promise.all([...ctx.data].map((item, i) => {
            return def.type._parseAsync(new ParseInputLazyPath(ctx, item, ctx.path, i));
          })).then((result2) => {
            return ParseStatus.mergeArray(status, result2);
          });
        }
        const result = [...ctx.data].map((item, i) => {
          return def.type._parseSync(new ParseInputLazyPath(ctx, item, ctx.path, i));
        });
        return ParseStatus.mergeArray(status, result);
      }
      get element() {
        return this._def.type;
      }
      min(minLength, message) {
        return new _ZodArray({
          ...this._def,
          minLength: { value: minLength, message: errorUtil.toString(message) }
        });
      }
      max(maxLength, message) {
        return new _ZodArray({
          ...this._def,
          maxLength: { value: maxLength, message: errorUtil.toString(message) }
        });
      }
      length(len, message) {
        return new _ZodArray({
          ...this._def,
          exactLength: { value: len, message: errorUtil.toString(message) }
        });
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodArray.create = (schema, params) => {
      return new ZodArray({
        type: schema,
        minLength: null,
        maxLength: null,
        exactLength: null,
        typeName: ZodFirstPartyTypeKind.ZodArray,
        ...processCreateParams(params)
      });
    };
    ZodObject = class _ZodObject extends ZodType {
      constructor() {
        super(...arguments);
        this._cached = null;
        this.nonstrict = this.passthrough;
        this.augment = this.extend;
      }
      _getCached() {
        if (this._cached !== null)
          return this._cached;
        const shape = this._def.shape();
        const keys = util.objectKeys(shape);
        this._cached = { shape, keys };
        return this._cached;
      }
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.object) {
          const ctx2 = this._getOrReturnCtx(input);
          addIssueToContext(ctx2, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx2.parsedType
          });
          return INVALID;
        }
        const { status, ctx } = this._processInputParams(input);
        const { shape, keys: shapeKeys } = this._getCached();
        const extraKeys = [];
        if (!(this._def.catchall instanceof ZodNever && this._def.unknownKeys === "strip")) {
          for (const key in ctx.data) {
            if (!shapeKeys.includes(key)) {
              extraKeys.push(key);
            }
          }
        }
        const pairs = [];
        for (const key of shapeKeys) {
          const keyValidator = shape[key];
          const value = ctx.data[key];
          pairs.push({
            key: { status: "valid", value: key },
            value: keyValidator._parse(new ParseInputLazyPath(ctx, value, ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (this._def.catchall instanceof ZodNever) {
          const unknownKeys = this._def.unknownKeys;
          if (unknownKeys === "passthrough") {
            for (const key of extraKeys) {
              pairs.push({
                key: { status: "valid", value: key },
                value: { status: "valid", value: ctx.data[key] }
              });
            }
          } else if (unknownKeys === "strict") {
            if (extraKeys.length > 0) {
              addIssueToContext(ctx, {
                code: ZodIssueCode.unrecognized_keys,
                keys: extraKeys
              });
              status.dirty();
            }
          } else if (unknownKeys === "strip") {
          } else {
            throw new Error(`Internal ZodObject error: invalid unknownKeys value.`);
          }
        } else {
          const catchall = this._def.catchall;
          for (const key of extraKeys) {
            const value = ctx.data[key];
            pairs.push({
              key: { status: "valid", value: key },
              value: catchall._parse(
                new ParseInputLazyPath(ctx, value, ctx.path, key)
                //, ctx.child(key), value, getParsedType(value)
              ),
              alwaysSet: key in ctx.data
            });
          }
        }
        if (ctx.common.async) {
          return Promise.resolve().then(async () => {
            const syncPairs = [];
            for (const pair of pairs) {
              const key = await pair.key;
              const value = await pair.value;
              syncPairs.push({
                key,
                value,
                alwaysSet: pair.alwaysSet
              });
            }
            return syncPairs;
          }).then((syncPairs) => {
            return ParseStatus.mergeObjectSync(status, syncPairs);
          });
        } else {
          return ParseStatus.mergeObjectSync(status, pairs);
        }
      }
      get shape() {
        return this._def.shape();
      }
      strict(message) {
        errorUtil.errToObj;
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strict",
          ...message !== void 0 ? {
            errorMap: (issue, ctx) => {
              const defaultError = this._def.errorMap?.(issue, ctx).message ?? ctx.defaultError;
              if (issue.code === "unrecognized_keys")
                return {
                  message: errorUtil.errToObj(message).message ?? defaultError
                };
              return {
                message: defaultError
              };
            }
          } : {}
        });
      }
      strip() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "strip"
        });
      }
      passthrough() {
        return new _ZodObject({
          ...this._def,
          unknownKeys: "passthrough"
        });
      }
      // const AugmentFactory =
      //   <Def extends ZodObjectDef>(def: Def) =>
      //   <Augmentation extends ZodRawShape>(
      //     augmentation: Augmentation
      //   ): ZodObject<
      //     extendShape<ReturnType<Def["shape"]>, Augmentation>,
      //     Def["unknownKeys"],
      //     Def["catchall"]
      //   > => {
      //     return new ZodObject({
      //       ...def,
      //       shape: () => ({
      //         ...def.shape(),
      //         ...augmentation,
      //       }),
      //     }) as any;
      //   };
      extend(augmentation) {
        return new _ZodObject({
          ...this._def,
          shape: () => ({
            ...this._def.shape(),
            ...augmentation
          })
        });
      }
      /**
       * Prior to zod@1.0.12 there was a bug in the
       * inferred type of merged objects. Please
       * upgrade if you are experiencing issues.
       */
      merge(merging) {
        const merged = new _ZodObject({
          unknownKeys: merging._def.unknownKeys,
          catchall: merging._def.catchall,
          shape: () => ({
            ...this._def.shape(),
            ...merging._def.shape()
          }),
          typeName: ZodFirstPartyTypeKind.ZodObject
        });
        return merged;
      }
      // merge<
      //   Incoming extends AnyZodObject,
      //   Augmentation extends Incoming["shape"],
      //   NewOutput extends {
      //     [k in keyof Augmentation | keyof Output]: k extends keyof Augmentation
      //       ? Augmentation[k]["_output"]
      //       : k extends keyof Output
      //       ? Output[k]
      //       : never;
      //   },
      //   NewInput extends {
      //     [k in keyof Augmentation | keyof Input]: k extends keyof Augmentation
      //       ? Augmentation[k]["_input"]
      //       : k extends keyof Input
      //       ? Input[k]
      //       : never;
      //   }
      // >(
      //   merging: Incoming
      // ): ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"],
      //   NewOutput,
      //   NewInput
      // > {
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      setKey(key, schema) {
        return this.augment({ [key]: schema });
      }
      // merge<Incoming extends AnyZodObject>(
      //   merging: Incoming
      // ): //ZodObject<T & Incoming["_shape"], UnknownKeys, Catchall> = (merging) => {
      // ZodObject<
      //   extendShape<T, ReturnType<Incoming["_def"]["shape"]>>,
      //   Incoming["_def"]["unknownKeys"],
      //   Incoming["_def"]["catchall"]
      // > {
      //   // const mergedShape = objectUtil.mergeShapes(
      //   //   this._def.shape(),
      //   //   merging._def.shape()
      //   // );
      //   const merged: any = new ZodObject({
      //     unknownKeys: merging._def.unknownKeys,
      //     catchall: merging._def.catchall,
      //     shape: () =>
      //       objectUtil.mergeShapes(this._def.shape(), merging._def.shape()),
      //     typeName: ZodFirstPartyTypeKind.ZodObject,
      //   }) as any;
      //   return merged;
      // }
      catchall(index) {
        return new _ZodObject({
          ...this._def,
          catchall: index
        });
      }
      pick(mask) {
        const shape = {};
        for (const key of util.objectKeys(mask)) {
          if (mask[key] && this.shape[key]) {
            shape[key] = this.shape[key];
          }
        }
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      omit(mask) {
        const shape = {};
        for (const key of util.objectKeys(this.shape)) {
          if (!mask[key]) {
            shape[key] = this.shape[key];
          }
        }
        return new _ZodObject({
          ...this._def,
          shape: () => shape
        });
      }
      /**
       * @deprecated
       */
      deepPartial() {
        return deepPartialify(this);
      }
      partial(mask) {
        const newShape = {};
        for (const key of util.objectKeys(this.shape)) {
          const fieldSchema = this.shape[key];
          if (mask && !mask[key]) {
            newShape[key] = fieldSchema;
          } else {
            newShape[key] = fieldSchema.optional();
          }
        }
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      required(mask) {
        const newShape = {};
        for (const key of util.objectKeys(this.shape)) {
          if (mask && !mask[key]) {
            newShape[key] = this.shape[key];
          } else {
            const fieldSchema = this.shape[key];
            let newField = fieldSchema;
            while (newField instanceof ZodOptional) {
              newField = newField._def.innerType;
            }
            newShape[key] = newField;
          }
        }
        return new _ZodObject({
          ...this._def,
          shape: () => newShape
        });
      }
      keyof() {
        return createZodEnum(util.objectKeys(this.shape));
      }
    };
    ZodObject.create = (shape, params) => {
      return new ZodObject({
        shape: () => shape,
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.strictCreate = (shape, params) => {
      return new ZodObject({
        shape: () => shape,
        unknownKeys: "strict",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodObject.lazycreate = (shape, params) => {
      return new ZodObject({
        shape,
        unknownKeys: "strip",
        catchall: ZodNever.create(),
        typeName: ZodFirstPartyTypeKind.ZodObject,
        ...processCreateParams(params)
      });
    };
    ZodUnion = class extends ZodType {
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const options = this._def.options;
        function handleResults(results) {
          for (const result of results) {
            if (result.result.status === "valid") {
              return result.result;
            }
          }
          for (const result of results) {
            if (result.result.status === "dirty") {
              ctx.common.issues.push(...result.ctx.common.issues);
              return result.result;
            }
          }
          const unionErrors = results.map((result) => new ZodError(result.ctx.common.issues));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
        if (ctx.common.async) {
          return Promise.all(options.map(async (option) => {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            return {
              result: await option._parseAsync({
                data: ctx.data,
                path: ctx.path,
                parent: childCtx
              }),
              ctx: childCtx
            };
          })).then(handleResults);
        } else {
          let dirty = void 0;
          const issues = [];
          for (const option of options) {
            const childCtx = {
              ...ctx,
              common: {
                ...ctx.common,
                issues: []
              },
              parent: null
            };
            const result = option._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: childCtx
            });
            if (result.status === "valid") {
              return result;
            } else if (result.status === "dirty" && !dirty) {
              dirty = { result, ctx: childCtx };
            }
            if (childCtx.common.issues.length) {
              issues.push(childCtx.common.issues);
            }
          }
          if (dirty) {
            ctx.common.issues.push(...dirty.ctx.common.issues);
            return dirty.result;
          }
          const unionErrors = issues.map((issues2) => new ZodError(issues2));
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union,
            unionErrors
          });
          return INVALID;
        }
      }
      get options() {
        return this._def.options;
      }
    };
    ZodUnion.create = (types2, params) => {
      return new ZodUnion({
        options: types2,
        typeName: ZodFirstPartyTypeKind.ZodUnion,
        ...processCreateParams(params)
      });
    };
    getDiscriminator = (type) => {
      if (type instanceof ZodLazy) {
        return getDiscriminator(type.schema);
      } else if (type instanceof ZodEffects) {
        return getDiscriminator(type.innerType());
      } else if (type instanceof ZodLiteral) {
        return [type.value];
      } else if (type instanceof ZodEnum) {
        return type.options;
      } else if (type instanceof ZodNativeEnum) {
        return util.objectValues(type.enum);
      } else if (type instanceof ZodDefault) {
        return getDiscriminator(type._def.innerType);
      } else if (type instanceof ZodUndefined) {
        return [void 0];
      } else if (type instanceof ZodNull) {
        return [null];
      } else if (type instanceof ZodOptional) {
        return [void 0, ...getDiscriminator(type.unwrap())];
      } else if (type instanceof ZodNullable) {
        return [null, ...getDiscriminator(type.unwrap())];
      } else if (type instanceof ZodBranded) {
        return getDiscriminator(type.unwrap());
      } else if (type instanceof ZodReadonly) {
        return getDiscriminator(type.unwrap());
      } else if (type instanceof ZodCatch) {
        return getDiscriminator(type._def.innerType);
      } else {
        return [];
      }
    };
    ZodDiscriminatedUnion = class _ZodDiscriminatedUnion extends ZodType {
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const discriminator = this.discriminator;
        const discriminatorValue = ctx.data[discriminator];
        const option = this.optionsMap.get(discriminatorValue);
        if (!option) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_union_discriminator,
            options: Array.from(this.optionsMap.keys()),
            path: [discriminator]
          });
          return INVALID;
        }
        if (ctx.common.async) {
          return option._parseAsync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        } else {
          return option._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
        }
      }
      get discriminator() {
        return this._def.discriminator;
      }
      get options() {
        return this._def.options;
      }
      get optionsMap() {
        return this._def.optionsMap;
      }
      /**
       * The constructor of the discriminated union schema. Its behaviour is very similar to that of the normal z.union() constructor.
       * However, it only allows a union of objects, all of which need to share a discriminator property. This property must
       * have a different value for each object in the union.
       * @param discriminator the name of the discriminator property
       * @param types an array of object schemas
       * @param params
       */
      static create(discriminator, options, params) {
        const optionsMap = /* @__PURE__ */ new Map();
        for (const type of options) {
          const discriminatorValues = getDiscriminator(type.shape[discriminator]);
          if (!discriminatorValues.length) {
            throw new Error(`A discriminator value for key \`${discriminator}\` could not be extracted from all schema options`);
          }
          for (const value of discriminatorValues) {
            if (optionsMap.has(value)) {
              throw new Error(`Discriminator property ${String(discriminator)} has duplicate value ${String(value)}`);
            }
            optionsMap.set(value, type);
          }
        }
        return new _ZodDiscriminatedUnion({
          typeName: ZodFirstPartyTypeKind.ZodDiscriminatedUnion,
          discriminator,
          options,
          optionsMap,
          ...processCreateParams(params)
        });
      }
    };
    ZodIntersection = class extends ZodType {
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        const handleParsed = (parsedLeft, parsedRight) => {
          if (isAborted(parsedLeft) || isAborted(parsedRight)) {
            return INVALID;
          }
          const merged = mergeValues(parsedLeft.value, parsedRight.value);
          if (!merged.valid) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.invalid_intersection_types
            });
            return INVALID;
          }
          if (isDirty(parsedLeft) || isDirty(parsedRight)) {
            status.dirty();
          }
          return { status: status.value, value: merged.data };
        };
        if (ctx.common.async) {
          return Promise.all([
            this._def.left._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            }),
            this._def.right._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            })
          ]).then(([left, right]) => handleParsed(left, right));
        } else {
          return handleParsed(this._def.left._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }), this._def.right._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          }));
        }
      }
    };
    ZodIntersection.create = (left, right, params) => {
      return new ZodIntersection({
        left,
        right,
        typeName: ZodFirstPartyTypeKind.ZodIntersection,
        ...processCreateParams(params)
      });
    };
    ZodTuple = class _ZodTuple extends ZodType {
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.array) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.array,
            received: ctx.parsedType
          });
          return INVALID;
        }
        if (ctx.data.length < this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_small,
            minimum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          return INVALID;
        }
        const rest = this._def.rest;
        if (!rest && ctx.data.length > this._def.items.length) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.too_big,
            maximum: this._def.items.length,
            inclusive: true,
            exact: false,
            type: "array"
          });
          status.dirty();
        }
        const items = [...ctx.data].map((item, itemIndex) => {
          const schema = this._def.items[itemIndex] || this._def.rest;
          if (!schema)
            return null;
          return schema._parse(new ParseInputLazyPath(ctx, item, ctx.path, itemIndex));
        }).filter((x) => !!x);
        if (ctx.common.async) {
          return Promise.all(items).then((results) => {
            return ParseStatus.mergeArray(status, results);
          });
        } else {
          return ParseStatus.mergeArray(status, items);
        }
      }
      get items() {
        return this._def.items;
      }
      rest(rest) {
        return new _ZodTuple({
          ...this._def,
          rest
        });
      }
    };
    ZodTuple.create = (schemas, params) => {
      if (!Array.isArray(schemas)) {
        throw new Error("You must pass an array of schemas to z.tuple([ ... ])");
      }
      return new ZodTuple({
        items: schemas,
        typeName: ZodFirstPartyTypeKind.ZodTuple,
        rest: null,
        ...processCreateParams(params)
      });
    };
    ZodRecord = class _ZodRecord extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.object) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.object,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const pairs = [];
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        for (const key in ctx.data) {
          pairs.push({
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, key)),
            value: valueType._parse(new ParseInputLazyPath(ctx, ctx.data[key], ctx.path, key)),
            alwaysSet: key in ctx.data
          });
        }
        if (ctx.common.async) {
          return ParseStatus.mergeObjectAsync(status, pairs);
        } else {
          return ParseStatus.mergeObjectSync(status, pairs);
        }
      }
      get element() {
        return this._def.valueType;
      }
      static create(first, second, third) {
        if (second instanceof ZodType) {
          return new _ZodRecord({
            keyType: first,
            valueType: second,
            typeName: ZodFirstPartyTypeKind.ZodRecord,
            ...processCreateParams(third)
          });
        }
        return new _ZodRecord({
          keyType: ZodString.create(),
          valueType: first,
          typeName: ZodFirstPartyTypeKind.ZodRecord,
          ...processCreateParams(second)
        });
      }
    };
    ZodMap = class extends ZodType {
      get keySchema() {
        return this._def.keyType;
      }
      get valueSchema() {
        return this._def.valueType;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.map) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.map,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const keyType = this._def.keyType;
        const valueType = this._def.valueType;
        const pairs = [...ctx.data.entries()].map(([key, value], index) => {
          return {
            key: keyType._parse(new ParseInputLazyPath(ctx, key, ctx.path, [index, "key"])),
            value: valueType._parse(new ParseInputLazyPath(ctx, value, ctx.path, [index, "value"]))
          };
        });
        if (ctx.common.async) {
          const finalMap = /* @__PURE__ */ new Map();
          return Promise.resolve().then(async () => {
            for (const pair of pairs) {
              const key = await pair.key;
              const value = await pair.value;
              if (key.status === "aborted" || value.status === "aborted") {
                return INVALID;
              }
              if (key.status === "dirty" || value.status === "dirty") {
                status.dirty();
              }
              finalMap.set(key.value, value.value);
            }
            return { status: status.value, value: finalMap };
          });
        } else {
          const finalMap = /* @__PURE__ */ new Map();
          for (const pair of pairs) {
            const key = pair.key;
            const value = pair.value;
            if (key.status === "aborted" || value.status === "aborted") {
              return INVALID;
            }
            if (key.status === "dirty" || value.status === "dirty") {
              status.dirty();
            }
            finalMap.set(key.value, value.value);
          }
          return { status: status.value, value: finalMap };
        }
      }
    };
    ZodMap.create = (keyType, valueType, params) => {
      return new ZodMap({
        valueType,
        keyType,
        typeName: ZodFirstPartyTypeKind.ZodMap,
        ...processCreateParams(params)
      });
    };
    ZodSet = class _ZodSet extends ZodType {
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.set) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.set,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const def = this._def;
        if (def.minSize !== null) {
          if (ctx.data.size < def.minSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_small,
              minimum: def.minSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.minSize.message
            });
            status.dirty();
          }
        }
        if (def.maxSize !== null) {
          if (ctx.data.size > def.maxSize.value) {
            addIssueToContext(ctx, {
              code: ZodIssueCode.too_big,
              maximum: def.maxSize.value,
              type: "set",
              inclusive: true,
              exact: false,
              message: def.maxSize.message
            });
            status.dirty();
          }
        }
        const valueType = this._def.valueType;
        function finalizeSet(elements2) {
          const parsedSet = /* @__PURE__ */ new Set();
          for (const element of elements2) {
            if (element.status === "aborted")
              return INVALID;
            if (element.status === "dirty")
              status.dirty();
            parsedSet.add(element.value);
          }
          return { status: status.value, value: parsedSet };
        }
        const elements = [...ctx.data.values()].map((item, i) => valueType._parse(new ParseInputLazyPath(ctx, item, ctx.path, i)));
        if (ctx.common.async) {
          return Promise.all(elements).then((elements2) => finalizeSet(elements2));
        } else {
          return finalizeSet(elements);
        }
      }
      min(minSize, message) {
        return new _ZodSet({
          ...this._def,
          minSize: { value: minSize, message: errorUtil.toString(message) }
        });
      }
      max(maxSize, message) {
        return new _ZodSet({
          ...this._def,
          maxSize: { value: maxSize, message: errorUtil.toString(message) }
        });
      }
      size(size, message) {
        return this.min(size, message).max(size, message);
      }
      nonempty(message) {
        return this.min(1, message);
      }
    };
    ZodSet.create = (valueType, params) => {
      return new ZodSet({
        valueType,
        minSize: null,
        maxSize: null,
        typeName: ZodFirstPartyTypeKind.ZodSet,
        ...processCreateParams(params)
      });
    };
    ZodFunction = class _ZodFunction extends ZodType {
      constructor() {
        super(...arguments);
        this.validate = this.implement;
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.function) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.function,
            received: ctx.parsedType
          });
          return INVALID;
        }
        function makeArgsIssue(args, error) {
          return makeIssue({
            data: args,
            path: ctx.path,
            errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_arguments,
              argumentsError: error
            }
          });
        }
        function makeReturnsIssue(returns, error) {
          return makeIssue({
            data: returns,
            path: ctx.path,
            errorMaps: [ctx.common.contextualErrorMap, ctx.schemaErrorMap, getErrorMap(), en_default].filter((x) => !!x),
            issueData: {
              code: ZodIssueCode.invalid_return_type,
              returnTypeError: error
            }
          });
        }
        const params = { errorMap: ctx.common.contextualErrorMap };
        const fn = ctx.data;
        if (this._def.returns instanceof ZodPromise) {
          const me = this;
          return OK(async function(...args) {
            const error = new ZodError([]);
            const parsedArgs = await me._def.args.parseAsync(args, params).catch((e) => {
              error.addIssue(makeArgsIssue(args, e));
              throw error;
            });
            const result = await Reflect.apply(fn, this, parsedArgs);
            const parsedReturns = await me._def.returns._def.type.parseAsync(result, params).catch((e) => {
              error.addIssue(makeReturnsIssue(result, e));
              throw error;
            });
            return parsedReturns;
          });
        } else {
          const me = this;
          return OK(function(...args) {
            const parsedArgs = me._def.args.safeParse(args, params);
            if (!parsedArgs.success) {
              throw new ZodError([makeArgsIssue(args, parsedArgs.error)]);
            }
            const result = Reflect.apply(fn, this, parsedArgs.data);
            const parsedReturns = me._def.returns.safeParse(result, params);
            if (!parsedReturns.success) {
              throw new ZodError([makeReturnsIssue(result, parsedReturns.error)]);
            }
            return parsedReturns.data;
          });
        }
      }
      parameters() {
        return this._def.args;
      }
      returnType() {
        return this._def.returns;
      }
      args(...items) {
        return new _ZodFunction({
          ...this._def,
          args: ZodTuple.create(items).rest(ZodUnknown.create())
        });
      }
      returns(returnType) {
        return new _ZodFunction({
          ...this._def,
          returns: returnType
        });
      }
      implement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      strictImplement(func) {
        const validatedFunc = this.parse(func);
        return validatedFunc;
      }
      static create(args, returns, params) {
        return new _ZodFunction({
          args: args ? args : ZodTuple.create([]).rest(ZodUnknown.create()),
          returns: returns || ZodUnknown.create(),
          typeName: ZodFirstPartyTypeKind.ZodFunction,
          ...processCreateParams(params)
        });
      }
    };
    ZodLazy = class extends ZodType {
      get schema() {
        return this._def.getter();
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const lazySchema = this._def.getter();
        return lazySchema._parse({ data: ctx.data, path: ctx.path, parent: ctx });
      }
    };
    ZodLazy.create = (getter, params) => {
      return new ZodLazy({
        getter,
        typeName: ZodFirstPartyTypeKind.ZodLazy,
        ...processCreateParams(params)
      });
    };
    ZodLiteral = class extends ZodType {
      _parse(input) {
        if (input.data !== this._def.value) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_literal,
            expected: this._def.value
          });
          return INVALID;
        }
        return { status: "valid", value: input.data };
      }
      get value() {
        return this._def.value;
      }
    };
    ZodLiteral.create = (value, params) => {
      return new ZodLiteral({
        value,
        typeName: ZodFirstPartyTypeKind.ZodLiteral,
        ...processCreateParams(params)
      });
    };
    ZodEnum = class _ZodEnum extends ZodType {
      _parse(input) {
        if (typeof input.data !== "string") {
          const ctx = this._getOrReturnCtx(input);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!this._cache) {
          this._cache = new Set(this._def.values);
        }
        if (!this._cache.has(input.data)) {
          const ctx = this._getOrReturnCtx(input);
          const expectedValues = this._def.values;
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input.data);
      }
      get options() {
        return this._def.values;
      }
      get enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Values() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      get Enum() {
        const enumValues = {};
        for (const val of this._def.values) {
          enumValues[val] = val;
        }
        return enumValues;
      }
      extract(values, newDef = this._def) {
        return _ZodEnum.create(values, {
          ...this._def,
          ...newDef
        });
      }
      exclude(values, newDef = this._def) {
        return _ZodEnum.create(this.options.filter((opt) => !values.includes(opt)), {
          ...this._def,
          ...newDef
        });
      }
    };
    ZodEnum.create = createZodEnum;
    ZodNativeEnum = class extends ZodType {
      _parse(input) {
        const nativeEnumValues = util.getValidEnumValues(this._def.values);
        const ctx = this._getOrReturnCtx(input);
        if (ctx.parsedType !== ZodParsedType.string && ctx.parsedType !== ZodParsedType.number) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            expected: util.joinValues(expectedValues),
            received: ctx.parsedType,
            code: ZodIssueCode.invalid_type
          });
          return INVALID;
        }
        if (!this._cache) {
          this._cache = new Set(util.getValidEnumValues(this._def.values));
        }
        if (!this._cache.has(input.data)) {
          const expectedValues = util.objectValues(nativeEnumValues);
          addIssueToContext(ctx, {
            received: ctx.data,
            code: ZodIssueCode.invalid_enum_value,
            options: expectedValues
          });
          return INVALID;
        }
        return OK(input.data);
      }
      get enum() {
        return this._def.values;
      }
    };
    ZodNativeEnum.create = (values, params) => {
      return new ZodNativeEnum({
        values,
        typeName: ZodFirstPartyTypeKind.ZodNativeEnum,
        ...processCreateParams(params)
      });
    };
    ZodPromise = class extends ZodType {
      unwrap() {
        return this._def.type;
      }
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        if (ctx.parsedType !== ZodParsedType.promise && ctx.common.async === false) {
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.promise,
            received: ctx.parsedType
          });
          return INVALID;
        }
        const promisified = ctx.parsedType === ZodParsedType.promise ? ctx.data : Promise.resolve(ctx.data);
        return OK(promisified.then((data) => {
          return this._def.type.parseAsync(data, {
            path: ctx.path,
            errorMap: ctx.common.contextualErrorMap
          });
        }));
      }
    };
    ZodPromise.create = (schema, params) => {
      return new ZodPromise({
        type: schema,
        typeName: ZodFirstPartyTypeKind.ZodPromise,
        ...processCreateParams(params)
      });
    };
    ZodEffects = class extends ZodType {
      innerType() {
        return this._def.schema;
      }
      sourceType() {
        return this._def.schema._def.typeName === ZodFirstPartyTypeKind.ZodEffects ? this._def.schema.sourceType() : this._def.schema;
      }
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        const effect = this._def.effect || null;
        const checkCtx = {
          addIssue: (arg) => {
            addIssueToContext(ctx, arg);
            if (arg.fatal) {
              status.abort();
            } else {
              status.dirty();
            }
          },
          get path() {
            return ctx.path;
          }
        };
        checkCtx.addIssue = checkCtx.addIssue.bind(checkCtx);
        if (effect.type === "preprocess") {
          const processed = effect.transform(ctx.data, checkCtx);
          if (ctx.common.async) {
            return Promise.resolve(processed).then(async (processed2) => {
              if (status.value === "aborted")
                return INVALID;
              const result = await this._def.schema._parseAsync({
                data: processed2,
                path: ctx.path,
                parent: ctx
              });
              if (result.status === "aborted")
                return INVALID;
              if (result.status === "dirty")
                return DIRTY(result.value);
              if (status.value === "dirty")
                return DIRTY(result.value);
              return result;
            });
          } else {
            if (status.value === "aborted")
              return INVALID;
            const result = this._def.schema._parseSync({
              data: processed,
              path: ctx.path,
              parent: ctx
            });
            if (result.status === "aborted")
              return INVALID;
            if (result.status === "dirty")
              return DIRTY(result.value);
            if (status.value === "dirty")
              return DIRTY(result.value);
            return result;
          }
        }
        if (effect.type === "refinement") {
          const executeRefinement = (acc) => {
            const result = effect.refinement(acc, checkCtx);
            if (ctx.common.async) {
              return Promise.resolve(result);
            }
            if (result instanceof Promise) {
              throw new Error("Async refinement encountered during synchronous parse operation. Use .parseAsync instead.");
            }
            return acc;
          };
          if (ctx.common.async === false) {
            const inner = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inner.status === "aborted")
              return INVALID;
            if (inner.status === "dirty")
              status.dirty();
            executeRefinement(inner.value);
            return { status: status.value, value: inner.value };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((inner) => {
              if (inner.status === "aborted")
                return INVALID;
              if (inner.status === "dirty")
                status.dirty();
              return executeRefinement(inner.value).then(() => {
                return { status: status.value, value: inner.value };
              });
            });
          }
        }
        if (effect.type === "transform") {
          if (ctx.common.async === false) {
            const base = this._def.schema._parseSync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (!isValid(base))
              return INVALID;
            const result = effect.transform(base.value, checkCtx);
            if (result instanceof Promise) {
              throw new Error(`Asynchronous transform encountered during synchronous parse operation. Use .parseAsync instead.`);
            }
            return { status: status.value, value: result };
          } else {
            return this._def.schema._parseAsync({ data: ctx.data, path: ctx.path, parent: ctx }).then((base) => {
              if (!isValid(base))
                return INVALID;
              return Promise.resolve(effect.transform(base.value, checkCtx)).then((result) => ({
                status: status.value,
                value: result
              }));
            });
          }
        }
        util.assertNever(effect);
      }
    };
    ZodEffects.create = (schema, effect, params) => {
      return new ZodEffects({
        schema,
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        effect,
        ...processCreateParams(params)
      });
    };
    ZodEffects.createWithPreprocess = (preprocess, schema, params) => {
      return new ZodEffects({
        schema,
        effect: { type: "preprocess", transform: preprocess },
        typeName: ZodFirstPartyTypeKind.ZodEffects,
        ...processCreateParams(params)
      });
    };
    ZodOptional = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType === ZodParsedType.undefined) {
          return OK(void 0);
        }
        return this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodOptional.create = (type, params) => {
      return new ZodOptional({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodOptional,
        ...processCreateParams(params)
      });
    };
    ZodNullable = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType === ZodParsedType.null) {
          return OK(null);
        }
        return this._def.innerType._parse(input);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodNullable.create = (type, params) => {
      return new ZodNullable({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodNullable,
        ...processCreateParams(params)
      });
    };
    ZodDefault = class extends ZodType {
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        let data = ctx.data;
        if (ctx.parsedType === ZodParsedType.undefined) {
          data = this._def.defaultValue();
        }
        return this._def.innerType._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      removeDefault() {
        return this._def.innerType;
      }
    };
    ZodDefault.create = (type, params) => {
      return new ZodDefault({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodDefault,
        defaultValue: typeof params.default === "function" ? params.default : () => params.default,
        ...processCreateParams(params)
      });
    };
    ZodCatch = class extends ZodType {
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const newCtx = {
          ...ctx,
          common: {
            ...ctx.common,
            issues: []
          }
        };
        const result = this._def.innerType._parse({
          data: newCtx.data,
          path: newCtx.path,
          parent: {
            ...newCtx
          }
        });
        if (isAsync(result)) {
          return result.then((result2) => {
            return {
              status: "valid",
              value: result2.status === "valid" ? result2.value : this._def.catchValue({
                get error() {
                  return new ZodError(newCtx.common.issues);
                },
                input: newCtx.data
              })
            };
          });
        } else {
          return {
            status: "valid",
            value: result.status === "valid" ? result.value : this._def.catchValue({
              get error() {
                return new ZodError(newCtx.common.issues);
              },
              input: newCtx.data
            })
          };
        }
      }
      removeCatch() {
        return this._def.innerType;
      }
    };
    ZodCatch.create = (type, params) => {
      return new ZodCatch({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodCatch,
        catchValue: typeof params.catch === "function" ? params.catch : () => params.catch,
        ...processCreateParams(params)
      });
    };
    ZodNaN = class extends ZodType {
      _parse(input) {
        const parsedType = this._getType(input);
        if (parsedType !== ZodParsedType.nan) {
          const ctx = this._getOrReturnCtx(input);
          addIssueToContext(ctx, {
            code: ZodIssueCode.invalid_type,
            expected: ZodParsedType.nan,
            received: ctx.parsedType
          });
          return INVALID;
        }
        return { status: "valid", value: input.data };
      }
    };
    ZodNaN.create = (params) => {
      return new ZodNaN({
        typeName: ZodFirstPartyTypeKind.ZodNaN,
        ...processCreateParams(params)
      });
    };
    BRAND = Symbol("zod_brand");
    ZodBranded = class extends ZodType {
      _parse(input) {
        const { ctx } = this._processInputParams(input);
        const data = ctx.data;
        return this._def.type._parse({
          data,
          path: ctx.path,
          parent: ctx
        });
      }
      unwrap() {
        return this._def.type;
      }
    };
    ZodPipeline = class _ZodPipeline extends ZodType {
      _parse(input) {
        const { status, ctx } = this._processInputParams(input);
        if (ctx.common.async) {
          const handleAsync = async () => {
            const inResult = await this._def.in._parseAsync({
              data: ctx.data,
              path: ctx.path,
              parent: ctx
            });
            if (inResult.status === "aborted")
              return INVALID;
            if (inResult.status === "dirty") {
              status.dirty();
              return DIRTY(inResult.value);
            } else {
              return this._def.out._parseAsync({
                data: inResult.value,
                path: ctx.path,
                parent: ctx
              });
            }
          };
          return handleAsync();
        } else {
          const inResult = this._def.in._parseSync({
            data: ctx.data,
            path: ctx.path,
            parent: ctx
          });
          if (inResult.status === "aborted")
            return INVALID;
          if (inResult.status === "dirty") {
            status.dirty();
            return {
              status: "dirty",
              value: inResult.value
            };
          } else {
            return this._def.out._parseSync({
              data: inResult.value,
              path: ctx.path,
              parent: ctx
            });
          }
        }
      }
      static create(a, b) {
        return new _ZodPipeline({
          in: a,
          out: b,
          typeName: ZodFirstPartyTypeKind.ZodPipeline
        });
      }
    };
    ZodReadonly = class extends ZodType {
      _parse(input) {
        const result = this._def.innerType._parse(input);
        const freeze = (data) => {
          if (isValid(data)) {
            data.value = Object.freeze(data.value);
          }
          return data;
        };
        return isAsync(result) ? result.then((data) => freeze(data)) : freeze(result);
      }
      unwrap() {
        return this._def.innerType;
      }
    };
    ZodReadonly.create = (type, params) => {
      return new ZodReadonly({
        innerType: type,
        typeName: ZodFirstPartyTypeKind.ZodReadonly,
        ...processCreateParams(params)
      });
    };
    late = {
      object: ZodObject.lazycreate
    };
    (function(ZodFirstPartyTypeKind2) {
      ZodFirstPartyTypeKind2["ZodString"] = "ZodString";
      ZodFirstPartyTypeKind2["ZodNumber"] = "ZodNumber";
      ZodFirstPartyTypeKind2["ZodNaN"] = "ZodNaN";
      ZodFirstPartyTypeKind2["ZodBigInt"] = "ZodBigInt";
      ZodFirstPartyTypeKind2["ZodBoolean"] = "ZodBoolean";
      ZodFirstPartyTypeKind2["ZodDate"] = "ZodDate";
      ZodFirstPartyTypeKind2["ZodSymbol"] = "ZodSymbol";
      ZodFirstPartyTypeKind2["ZodUndefined"] = "ZodUndefined";
      ZodFirstPartyTypeKind2["ZodNull"] = "ZodNull";
      ZodFirstPartyTypeKind2["ZodAny"] = "ZodAny";
      ZodFirstPartyTypeKind2["ZodUnknown"] = "ZodUnknown";
      ZodFirstPartyTypeKind2["ZodNever"] = "ZodNever";
      ZodFirstPartyTypeKind2["ZodVoid"] = "ZodVoid";
      ZodFirstPartyTypeKind2["ZodArray"] = "ZodArray";
      ZodFirstPartyTypeKind2["ZodObject"] = "ZodObject";
      ZodFirstPartyTypeKind2["ZodUnion"] = "ZodUnion";
      ZodFirstPartyTypeKind2["ZodDiscriminatedUnion"] = "ZodDiscriminatedUnion";
      ZodFirstPartyTypeKind2["ZodIntersection"] = "ZodIntersection";
      ZodFirstPartyTypeKind2["ZodTuple"] = "ZodTuple";
      ZodFirstPartyTypeKind2["ZodRecord"] = "ZodRecord";
      ZodFirstPartyTypeKind2["ZodMap"] = "ZodMap";
      ZodFirstPartyTypeKind2["ZodSet"] = "ZodSet";
      ZodFirstPartyTypeKind2["ZodFunction"] = "ZodFunction";
      ZodFirstPartyTypeKind2["ZodLazy"] = "ZodLazy";
      ZodFirstPartyTypeKind2["ZodLiteral"] = "ZodLiteral";
      ZodFirstPartyTypeKind2["ZodEnum"] = "ZodEnum";
      ZodFirstPartyTypeKind2["ZodEffects"] = "ZodEffects";
      ZodFirstPartyTypeKind2["ZodNativeEnum"] = "ZodNativeEnum";
      ZodFirstPartyTypeKind2["ZodOptional"] = "ZodOptional";
      ZodFirstPartyTypeKind2["ZodNullable"] = "ZodNullable";
      ZodFirstPartyTypeKind2["ZodDefault"] = "ZodDefault";
      ZodFirstPartyTypeKind2["ZodCatch"] = "ZodCatch";
      ZodFirstPartyTypeKind2["ZodPromise"] = "ZodPromise";
      ZodFirstPartyTypeKind2["ZodBranded"] = "ZodBranded";
      ZodFirstPartyTypeKind2["ZodPipeline"] = "ZodPipeline";
      ZodFirstPartyTypeKind2["ZodReadonly"] = "ZodReadonly";
    })(ZodFirstPartyTypeKind || (ZodFirstPartyTypeKind = {}));
    instanceOfType = (cls, params = {
      message: `Input not instance of ${cls.name}`
    }) => custom((data) => data instanceof cls, params);
    stringType = ZodString.create;
    numberType = ZodNumber.create;
    nanType = ZodNaN.create;
    bigIntType = ZodBigInt.create;
    booleanType = ZodBoolean.create;
    dateType = ZodDate.create;
    symbolType = ZodSymbol.create;
    undefinedType = ZodUndefined.create;
    nullType = ZodNull.create;
    anyType = ZodAny.create;
    unknownType = ZodUnknown.create;
    neverType = ZodNever.create;
    voidType = ZodVoid.create;
    arrayType = ZodArray.create;
    objectType = ZodObject.create;
    strictObjectType = ZodObject.strictCreate;
    unionType = ZodUnion.create;
    discriminatedUnionType = ZodDiscriminatedUnion.create;
    intersectionType = ZodIntersection.create;
    tupleType = ZodTuple.create;
    recordType = ZodRecord.create;
    mapType = ZodMap.create;
    setType = ZodSet.create;
    functionType = ZodFunction.create;
    lazyType = ZodLazy.create;
    literalType = ZodLiteral.create;
    enumType = ZodEnum.create;
    nativeEnumType = ZodNativeEnum.create;
    promiseType = ZodPromise.create;
    effectsType = ZodEffects.create;
    optionalType = ZodOptional.create;
    nullableType = ZodNullable.create;
    preprocessType = ZodEffects.createWithPreprocess;
    pipelineType = ZodPipeline.create;
    ostring = () => stringType().optional();
    onumber = () => numberType().optional();
    oboolean = () => booleanType().optional();
    coerce = {
      string: (arg) => ZodString.create({ ...arg, coerce: true }),
      number: (arg) => ZodNumber.create({ ...arg, coerce: true }),
      boolean: (arg) => ZodBoolean.create({
        ...arg,
        coerce: true
      }),
      bigint: (arg) => ZodBigInt.create({ ...arg, coerce: true }),
      date: (arg) => ZodDate.create({ ...arg, coerce: true })
    };
    NEVER = INVALID;
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js
var external_exports = {};
__export(external_exports, {
  BRAND: () => BRAND,
  DIRTY: () => DIRTY,
  EMPTY_PATH: () => EMPTY_PATH,
  INVALID: () => INVALID,
  NEVER: () => NEVER,
  OK: () => OK,
  ParseStatus: () => ParseStatus,
  Schema: () => ZodType,
  ZodAny: () => ZodAny,
  ZodArray: () => ZodArray,
  ZodBigInt: () => ZodBigInt,
  ZodBoolean: () => ZodBoolean,
  ZodBranded: () => ZodBranded,
  ZodCatch: () => ZodCatch,
  ZodDate: () => ZodDate,
  ZodDefault: () => ZodDefault,
  ZodDiscriminatedUnion: () => ZodDiscriminatedUnion,
  ZodEffects: () => ZodEffects,
  ZodEnum: () => ZodEnum,
  ZodError: () => ZodError,
  ZodFirstPartyTypeKind: () => ZodFirstPartyTypeKind,
  ZodFunction: () => ZodFunction,
  ZodIntersection: () => ZodIntersection,
  ZodIssueCode: () => ZodIssueCode,
  ZodLazy: () => ZodLazy,
  ZodLiteral: () => ZodLiteral,
  ZodMap: () => ZodMap,
  ZodNaN: () => ZodNaN,
  ZodNativeEnum: () => ZodNativeEnum,
  ZodNever: () => ZodNever,
  ZodNull: () => ZodNull,
  ZodNullable: () => ZodNullable,
  ZodNumber: () => ZodNumber,
  ZodObject: () => ZodObject,
  ZodOptional: () => ZodOptional,
  ZodParsedType: () => ZodParsedType,
  ZodPipeline: () => ZodPipeline,
  ZodPromise: () => ZodPromise,
  ZodReadonly: () => ZodReadonly,
  ZodRecord: () => ZodRecord,
  ZodSchema: () => ZodType,
  ZodSet: () => ZodSet,
  ZodString: () => ZodString,
  ZodSymbol: () => ZodSymbol,
  ZodTransformer: () => ZodEffects,
  ZodTuple: () => ZodTuple,
  ZodType: () => ZodType,
  ZodUndefined: () => ZodUndefined,
  ZodUnion: () => ZodUnion,
  ZodUnknown: () => ZodUnknown,
  ZodVoid: () => ZodVoid,
  addIssueToContext: () => addIssueToContext,
  any: () => anyType,
  array: () => arrayType,
  bigint: () => bigIntType,
  boolean: () => booleanType,
  coerce: () => coerce,
  custom: () => custom,
  date: () => dateType,
  datetimeRegex: () => datetimeRegex,
  defaultErrorMap: () => en_default,
  discriminatedUnion: () => discriminatedUnionType,
  effect: () => effectsType,
  enum: () => enumType,
  function: () => functionType,
  getErrorMap: () => getErrorMap,
  getParsedType: () => getParsedType,
  instanceof: () => instanceOfType,
  intersection: () => intersectionType,
  isAborted: () => isAborted,
  isAsync: () => isAsync,
  isDirty: () => isDirty,
  isValid: () => isValid,
  late: () => late,
  lazy: () => lazyType,
  literal: () => literalType,
  makeIssue: () => makeIssue,
  map: () => mapType,
  nan: () => nanType,
  nativeEnum: () => nativeEnumType,
  never: () => neverType,
  null: () => nullType,
  nullable: () => nullableType,
  number: () => numberType,
  object: () => objectType,
  objectUtil: () => objectUtil,
  oboolean: () => oboolean,
  onumber: () => onumber,
  optional: () => optionalType,
  ostring: () => ostring,
  pipeline: () => pipelineType,
  preprocess: () => preprocessType,
  promise: () => promiseType,
  quotelessJson: () => quotelessJson,
  record: () => recordType,
  set: () => setType,
  setErrorMap: () => setErrorMap,
  strictObject: () => strictObjectType,
  string: () => stringType,
  symbol: () => symbolType,
  transformer: () => effectsType,
  tuple: () => tupleType,
  undefined: () => undefinedType,
  union: () => unionType,
  unknown: () => unknownType,
  util: () => util,
  void: () => voidType
});
var init_external = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/v3/external.js"() {
    init_cjs_shims();
    init_errors();
    init_parseUtil();
    init_typeAliases();
    init_util();
    init_types();
    init_ZodError();
  }
});

// node_modules/.pnpm/zod@3.25.76/node_modules/zod/index.js
var init_zod = __esm({
  "node_modules/.pnpm/zod@3.25.76/node_modules/zod/index.js"() {
    init_cjs_shims();
    init_external();
    init_external();
  }
});

// src/core/env.ts
var env_exports = {};
__export(env_exports, {
  env: () => env,
  validateEnv: () => validateEnv
});
var import_dotenv, isMcpMode, envSchema, env, validateEnv;
var init_env = __esm({
  "src/core/env.ts"() {
    init_cjs_shims();
    import_dotenv = require("dotenv");
    init_zod();
    isMcpMode = process.argv.includes("--mode") && process.argv[process.argv.indexOf("--mode") + 1] === "mcp";
    if (isMcpMode) {
      (0, import_dotenv.config)({ override: false });
    } else {
      (0, import_dotenv.config)();
    }
    envSchema = external_exports.object({
      NODE_ENV: external_exports.enum(["development", "production", "test"]).default("development"),
      CIPHER_LOG_LEVEL: external_exports.enum(["error", "warn", "info", "debug", "silly"]).default("info"),
      REDACT_SECRETS: external_exports.boolean().default(true),
      OPENAI_API_KEY: external_exports.string().optional(),
      ANTHROPIC_API_KEY: external_exports.string().optional(),
      OPENROUTER_API_KEY: external_exports.string().optional(),
      QWEN_API_KEY: external_exports.string().optional(),
      OPENAI_BASE_URL: external_exports.string().optional(),
      OLLAMA_BASE_URL: external_exports.string().optional(),
      LMSTUDIO_BASE_URL: external_exports.string().optional(),
      OPENAI_ORG_ID: external_exports.string().optional(),
      // Embedding Configuration
      EMBEDDING_PROVIDER: external_exports.string().optional(),
      EMBEDDING_MODEL: external_exports.string().optional(),
      EMBEDDING_TIMEOUT: external_exports.number().optional(),
      EMBEDDING_MAX_RETRIES: external_exports.number().optional(),
      EMBEDDING_DIMENSIONS: external_exports.number().optional(),
      DISABLE_EMBEDDINGS: external_exports.boolean().default(false),
      EMBEDDING_DISABLED: external_exports.boolean().default(false),
      GEMINI_API_KEY: external_exports.string().optional(),
      GEMINI_BASE_URL: external_exports.string().optional(),
      // Storage Configuration
      STORAGE_CACHE_TYPE: external_exports.enum(["in-memory", "redis"]).default("in-memory"),
      STORAGE_CACHE_HOST: external_exports.string().optional(),
      STORAGE_CACHE_PORT: external_exports.number().optional(),
      STORAGE_CACHE_USERNAME: external_exports.string().optional(),
      STORAGE_CACHE_PASSWORD: external_exports.string().optional(),
      STORAGE_CACHE_DATABASE: external_exports.number().optional(),
      STORAGE_DATABASE_TYPE: external_exports.enum(["in-memory", "sqlite", "postgres"]).default("in-memory"),
      STORAGE_DATABASE_PATH: external_exports.string().optional(),
      STORAGE_DATABASE_NAME: external_exports.string().optional(),
      // PostgreSQL Configuration
      CIPHER_PG_URL: external_exports.string().optional(),
      STORAGE_DATABASE_HOST: external_exports.string().optional(),
      STORAGE_DATABASE_PORT: external_exports.number().optional(),
      STORAGE_DATABASE_USER: external_exports.string().optional(),
      STORAGE_DATABASE_PASSWORD: external_exports.string().optional(),
      STORAGE_DATABASE_SSL: external_exports.boolean().default(false),
      // Vector Storage Configuration
      VECTOR_STORE_TYPE: external_exports.enum(["qdrant", "milvus", "chroma", "pinecone", "in-memory", "faiss"]).default("in-memory"),
      VECTOR_STORE_HOST: external_exports.string().optional(),
      VECTOR_STORE_PORT: external_exports.number().optional(),
      VECTOR_STORE_URL: external_exports.string().optional(),
      VECTOR_STORE_API_KEY: external_exports.string().optional(),
      VECTOR_STORE_USERNAME: external_exports.string().optional(),
      VECTOR_STORE_PASSWORD: external_exports.string().optional(),
      VECTOR_STORE_COLLECTION: external_exports.string().default("knowledge_memory"),
      VECTOR_STORE_DIMENSION: external_exports.number().default(1536),
      VECTOR_STORE_DISTANCE: external_exports.enum(["Cosine", "Euclidean", "Dot", "Manhattan"]).default("Cosine"),
      VECTOR_STORE_ON_DISK: external_exports.boolean().default(false),
      VECTOR_STORE_MAX_VECTORS: external_exports.number().default(1e4),
      // Pinecone-specific Configuration
      PINECONE_PROVIDER: external_exports.string().default("aws"),
      PINECONE_REGION: external_exports.string().default("us-east-1"),
      // PGVECTOR -specific Configuration
      PGVECTOR_INDEXTYPE: external_exports.enum(["hnsw", "ivfflat"]).default("hnsw"),
      PGVECTOR_INDEXMETRIC: external_exports.enum(["vector_l2_ops", "vector_ip_ops"]).default("vector_l2_ops"),
      PINECONE_NAMESPACE: external_exports.string().default("default"),
      // Faiss-specific Configuration
      FAISS_BASE_STORAGE_PATH: external_exports.string().optional(),
      // Knowledge Graph Configuration
      KNOWLEDGE_GRAPH_ENABLED: external_exports.boolean().default(false),
      KNOWLEDGE_GRAPH_TYPE: external_exports.enum(["neo4j", "in-memory"]).default("in-memory"),
      KNOWLEDGE_GRAPH_HOST: external_exports.string().optional(),
      KNOWLEDGE_GRAPH_PORT: external_exports.number().optional(),
      KNOWLEDGE_GRAPH_URI: external_exports.string().optional(),
      KNOWLEDGE_GRAPH_USERNAME: external_exports.string().optional(),
      KNOWLEDGE_GRAPH_PASSWORD: external_exports.string().optional(),
      KNOWLEDGE_GRAPH_DATABASE: external_exports.string().default("neo4j"),
      // Memory Search Configuration
      SEARCH_MEMORY_TYPE: external_exports.enum(["knowledge", "reflection", "both"]).default("knowledge"),
      // Reflection Memory Configuration
      REFLECTION_VECTOR_STORE_COLLECTION: external_exports.string().default("reflection_memory"),
      DISABLE_REFLECTION_MEMORY: external_exports.boolean().default(true),
      // Event Persistence Configuration
      EVENT_PERSISTENCE_ENABLED: external_exports.boolean().default(false),
      EVENT_PERSISTENCE_PATH: external_exports.string().optional(),
      // Lazy Loading Configuration
      ENABLE_LAZY_LOADING: external_exports.string().optional(),
      LAZY_LOADING_ENABLED: external_exports.string().optional(),
      SKIP_HEAVY_SERVICES: external_exports.string().optional(),
      LAZY_EMBEDDING: external_exports.string().optional(),
      LAZY_VECTOR_STORE: external_exports.string().optional(),
      LAZY_MEMORY_OPERATIONS: external_exports.string().optional(),
      DISABLE_BACKGROUND_PRELOAD: external_exports.string().optional(),
      LAZY_INIT_TIMEOUT: external_exports.string().optional(),
      BACKGROUND_PRELOAD_DELAY: external_exports.string().optional(),
      // Workspace Memory Configuration
      USE_WORKSPACE_MEMORY: external_exports.boolean().default(false),
      WORKSPACE_SEARCH_THRESHOLD: external_exports.number().default(0.4),
      DISABLE_DEFAULT_MEMORY: external_exports.boolean().default(false),
      WORKSPACE_VECTOR_STORE_TYPE: external_exports.enum(["qdrant", "milvus", "chroma", "pinecone", "pgvector", "in-memory"]).optional(),
      WORKSPACE_VECTOR_STORE_HOST: external_exports.string().optional(),
      WORKSPACE_VECTOR_STORE_PORT: external_exports.number().optional(),
      WORKSPACE_VECTOR_STORE_URL: external_exports.string().optional(),
      WORKSPACE_VECTOR_STORE_API_KEY: external_exports.string().optional(),
      WORKSPACE_VECTOR_STORE_USERNAME: external_exports.string().optional(),
      WORKSPACE_VECTOR_STORE_PASSWORD: external_exports.string().optional(),
      WORKSPACE_VECTOR_STORE_COLLECTION: external_exports.string().default("workspace_memory"),
      WORKSPACE_VECTOR_STORE_DIMENSION: external_exports.number().optional(),
      WORKSPACE_VECTOR_STORE_DISTANCE: external_exports.enum(["Cosine", "Euclidean", "Dot", "Manhattan"]).optional(),
      WORKSPACE_VECTOR_STORE_ON_DISK: external_exports.boolean().optional(),
      WORKSPACE_VECTOR_STORE_MAX_VECTORS: external_exports.number().optional(),
      // Redis-specific Configuration
      WORKSPACE_REDIS_DATABASE: external_exports.number().optional(),
      // Pinecone-specific Configuration
      WORKSPACE_PINECONE_PROVIDER: external_exports.string().optional(),
      WORKSPACE_PINECONE_REGION: external_exports.string().optional(),
      WORKSPACE_PINECONE_NAMESPACE: external_exports.string().optional(),
      // Query Refinement Configuration
      ENABLE_QUERY_REFINEMENT: external_exports.boolean().default(true),
      // Cross-Tool Memory Sharing Configuration
      CIPHER_USER_ID: external_exports.string().optional(),
      CIPHER_PROJECT_NAME: external_exports.string().optional(),
      CIPHER_WORKSPACE_MODE: external_exports.enum(["shared", "isolated"]).default("isolated"),
      // MCP Aggregator Configuration
      USE_ASK_CIPHER: external_exports.boolean().default(false)
    });
    env = new Proxy({}, {
      get(target, prop) {
        switch (prop) {
          case "NODE_ENV":
            return process.env.NODE_ENV || "development";
          case "CIPHER_LOG_LEVEL":
            return process.env.CIPHER_LOG_LEVEL || "info";
          case "REDACT_SECRETS":
            return process.env.REDACT_SECRETS === "false" ? false : true;
          case "OPENAI_API_KEY":
            return process.env.OPENAI_API_KEY;
          case "ANTHROPIC_API_KEY":
            return process.env.ANTHROPIC_API_KEY;
          case "OPENROUTER_API_KEY":
            return process.env.OPENROUTER_API_KEY;
          case "QWEN_API_KEY":
            return process.env.QWEN_API_KEY;
          case "OPENAI_BASE_URL":
            return process.env.OPENAI_BASE_URL;
          case "OLLAMA_BASE_URL":
            return process.env.OLLAMA_BASE_URL;
          case "OPENAI_ORG_ID":
            return process.env.OPENAI_ORG_ID;
          // Embedding Configuration
          case "EMBEDDING_PROVIDER":
            return process.env.EMBEDDING_PROVIDER;
          case "EMBEDDING_MODEL":
            return process.env.EMBEDDING_MODEL;
          case "EMBEDDING_TIMEOUT":
            return process.env.EMBEDDING_TIMEOUT ? parseInt(process.env.EMBEDDING_TIMEOUT, 10) : void 0;
          case "EMBEDDING_MAX_RETRIES":
            return process.env.EMBEDDING_MAX_RETRIES ? parseInt(process.env.EMBEDDING_MAX_RETRIES, 10) : void 0;
          case "EMBEDDING_DIMENSIONS":
            return process.env.EMBEDDING_DIMENSIONS ? parseInt(process.env.EMBEDDING_DIMENSIONS, 10) : void 0;
          case "DISABLE_EMBEDDINGS":
            return process.env.DISABLE_EMBEDDINGS === "true";
          case "EMBEDDING_DISABLED":
            return process.env.EMBEDDING_DISABLED === "true";
          case "GEMINI_API_KEY":
            return process.env.GEMINI_API_KEY;
          case "GEMINI_BASE_URL":
            return process.env.GEMINI_BASE_URL;
          // Storage Configuration
          case "STORAGE_CACHE_TYPE":
            return process.env.STORAGE_CACHE_TYPE || "in-memory";
          case "STORAGE_CACHE_HOST":
            return process.env.STORAGE_CACHE_HOST;
          case "STORAGE_CACHE_PORT":
            return process.env.STORAGE_CACHE_PORT ? parseInt(process.env.STORAGE_CACHE_PORT, 10) : void 0;
          case "STORAGE_CACHE_USERNAME":
            return process.env.STORAGE_CACHE_USERNAME;
          case "STORAGE_CACHE_PASSWORD":
            return process.env.STORAGE_CACHE_PASSWORD;
          case "STORAGE_CACHE_DATABASE":
            return process.env.STORAGE_CACHE_DATABASE ? parseInt(process.env.STORAGE_CACHE_DATABASE, 10) : void 0;
          case "STORAGE_DATABASE_TYPE":
            return process.env.STORAGE_DATABASE_TYPE || "in-memory";
          case "STORAGE_DATABASE_PATH":
            return process.env.STORAGE_DATABASE_PATH;
          case "STORAGE_DATABASE_NAME":
            return process.env.STORAGE_DATABASE_NAME;
          // PostgreSQL Configuration
          case "CIPHER_PG_URL":
            return process.env.CIPHER_PG_URL;
          case "STORAGE_DATABASE_HOST":
            return process.env.STORAGE_DATABASE_HOST;
          case "STORAGE_DATABASE_PORT":
            return process.env.STORAGE_DATABASE_PORT ? parseInt(process.env.STORAGE_DATABASE_PORT, 10) : void 0;
          case "STORAGE_DATABASE_USER":
            return process.env.STORAGE_DATABASE_USER;
          case "STORAGE_DATABASE_PASSWORD":
            return process.env.STORAGE_DATABASE_PASSWORD;
          case "STORAGE_DATABASE_SSL":
            return process.env.STORAGE_DATABASE_SSL === "true";
          // Vector Storage Configuration
          case "VECTOR_STORE_TYPE":
            return process.env.VECTOR_STORE_TYPE || "in-memory";
          case "VECTOR_STORE_HOST":
            return process.env.VECTOR_STORE_HOST;
          case "VECTOR_STORE_PORT":
            return process.env.VECTOR_STORE_PORT ? parseInt(process.env.VECTOR_STORE_PORT, 10) : void 0;
          case "VECTOR_STORE_URL":
            return process.env.VECTOR_STORE_URL;
          case "VECTOR_STORE_API_KEY":
            return process.env.VECTOR_STORE_API_KEY;
          case "VECTOR_STORE_USERNAME":
            return process.env.VECTOR_STORE_USERNAME;
          case "VECTOR_STORE_PASSWORD":
            return process.env.VECTOR_STORE_PASSWORD;
          case "VECTOR_STORE_COLLECTION":
            return process.env.VECTOR_STORE_COLLECTION || "knowledge_memory";
          case "VECTOR_STORE_DIMENSION":
            return process.env.VECTOR_STORE_DIMENSION ? parseInt(process.env.VECTOR_STORE_DIMENSION, 10) : 1536;
          case "VECTOR_STORE_DISTANCE":
            return process.env.VECTOR_STORE_DISTANCE || "Cosine";
          case "VECTOR_STORE_ON_DISK":
            return process.env.VECTOR_STORE_ON_DISK === "true";
          case "VECTOR_STORE_MAX_VECTORS":
            return process.env.VECTOR_STORE_MAX_VECTORS ? parseInt(process.env.VECTOR_STORE_MAX_VECTORS, 10) : 1e4;
          // Faiss-specific Configuration
          case "FAISS_BASE_STORAGE_PATH":
            return process.env.FAISS_BASE_STORAGE_PATH;
          // Knowledge Graph Configuration
          case "KNOWLEDGE_GRAPH_ENABLED":
            return process.env.KNOWLEDGE_GRAPH_ENABLED === "true";
          case "KNOWLEDGE_GRAPH_TYPE":
            return process.env.KNOWLEDGE_GRAPH_TYPE || "in-memory";
          case "KNOWLEDGE_GRAPH_HOST":
            return process.env.KNOWLEDGE_GRAPH_HOST;
          case "KNOWLEDGE_GRAPH_PORT":
            return process.env.KNOWLEDGE_GRAPH_PORT ? parseInt(process.env.KNOWLEDGE_GRAPH_PORT, 10) : void 0;
          case "KNOWLEDGE_GRAPH_URI":
            return process.env.KNOWLEDGE_GRAPH_URI;
          case "KNOWLEDGE_GRAPH_USERNAME":
            return process.env.KNOWLEDGE_GRAPH_USERNAME;
          case "KNOWLEDGE_GRAPH_PASSWORD":
            return process.env.KNOWLEDGE_GRAPH_PASSWORD;
          case "KNOWLEDGE_GRAPH_DATABASE":
            return process.env.KNOWLEDGE_GRAPH_DATABASE || "neo4j";
          // Memory Search Configuration
          case "SEARCH_MEMORY_TYPE":
            return process.env.SEARCH_MEMORY_TYPE || "knowledge";
          // Reflection Memory Configuration
          case "REFLECTION_VECTOR_STORE_COLLECTION": {
            const value = process.env.REFLECTION_VECTOR_STORE_COLLECTION || "reflection_memory";
            if (value === "true") return true;
            if (value === "false") return false;
            return value;
          }
          case "DISABLE_REFLECTION_MEMORY":
            return process.env.DISABLE_REFLECTION_MEMORY === "true";
          // Event Persistence Configuration
          case "EVENT_PERSISTENCE_ENABLED":
            return process.env.EVENT_PERSISTENCE_ENABLED === "true";
          case "EVENT_PERSISTENCE_PATH":
            return process.env.EVENT_PERSISTENCE_PATH;
          // Lazy Loading Configuration
          case "ENABLE_LAZY_LOADING":
            return process.env.ENABLE_LAZY_LOADING;
          case "LAZY_LOADING_ENABLED":
            return process.env.LAZY_LOADING_ENABLED;
          case "SKIP_HEAVY_SERVICES":
            return process.env.SKIP_HEAVY_SERVICES;
          case "LAZY_EMBEDDING":
            return process.env.LAZY_EMBEDDING;
          case "LAZY_VECTOR_STORE":
            return process.env.LAZY_VECTOR_STORE;
          case "LAZY_MEMORY_OPERATIONS":
            return process.env.LAZY_MEMORY_OPERATIONS;
          case "DISABLE_BACKGROUND_PRELOAD":
            return process.env.DISABLE_BACKGROUND_PRELOAD;
          case "LAZY_INIT_TIMEOUT":
            return process.env.LAZY_INIT_TIMEOUT;
          case "BACKGROUND_PRELOAD_DELAY":
            return process.env.BACKGROUND_PRELOAD_DELAY;
          // Workspace Memory Configuration
          case "USE_WORKSPACE_MEMORY":
            return process.env.USE_WORKSPACE_MEMORY === "true";
          case "WORKSPACE_SEARCH_THRESHOLD":
            return process.env.WORKSPACE_SEARCH_THRESHOLD ? parseFloat(process.env.WORKSPACE_SEARCH_THRESHOLD) : 0.4;
          case "DISABLE_DEFAULT_MEMORY":
            return process.env.DISABLE_DEFAULT_MEMORY === "true";
          case "WORKSPACE_VECTOR_STORE_TYPE":
            return process.env.WORKSPACE_VECTOR_STORE_TYPE;
          case "WORKSPACE_VECTOR_STORE_HOST":
            return process.env.WORKSPACE_VECTOR_STORE_HOST;
          case "WORKSPACE_VECTOR_STORE_PORT":
            return process.env.WORKSPACE_VECTOR_STORE_PORT ? parseInt(process.env.WORKSPACE_VECTOR_STORE_PORT, 10) : void 0;
          case "WORKSPACE_VECTOR_STORE_URL":
            return process.env.WORKSPACE_VECTOR_STORE_URL;
          case "WORKSPACE_VECTOR_STORE_API_KEY":
            return process.env.WORKSPACE_VECTOR_STORE_API_KEY;
          case "WORKSPACE_VECTOR_STORE_USERNAME":
            return process.env.WORKSPACE_VECTOR_STORE_USERNAME;
          case "WORKSPACE_VECTOR_STORE_PASSWORD":
            return process.env.WORKSPACE_VECTOR_STORE_PASSWORD;
          case "WORKSPACE_VECTOR_STORE_COLLECTION":
            return process.env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory";
          case "WORKSPACE_VECTOR_STORE_DIMENSION":
            return process.env.WORKSPACE_VECTOR_STORE_DIMENSION ? parseInt(process.env.WORKSPACE_VECTOR_STORE_DIMENSION, 10) : process.env.VECTOR_STORE_DIMENSION ? parseInt(process.env.VECTOR_STORE_DIMENSION, 10) : 1536;
          case "WORKSPACE_VECTOR_STORE_DISTANCE":
            return process.env.WORKSPACE_VECTOR_STORE_DISTANCE || process.env.VECTOR_STORE_DISTANCE || "Cosine";
          case "WORKSPACE_VECTOR_STORE_ON_DISK":
            return process.env.WORKSPACE_VECTOR_STORE_ON_DISK !== void 0 ? process.env.WORKSPACE_VECTOR_STORE_ON_DISK === "true" : process.env.VECTOR_STORE_ON_DISK !== void 0 ? process.env.VECTOR_STORE_ON_DISK === "true" : false;
          case "WORKSPACE_VECTOR_STORE_MAX_VECTORS":
            return process.env.WORKSPACE_VECTOR_STORE_MAX_VECTORS ? parseInt(process.env.WORKSPACE_VECTOR_STORE_MAX_VECTORS, 10) : process.env.VECTOR_STORE_MAX_VECTORS ? parseInt(process.env.VECTOR_STORE_MAX_VECTORS, 10) : 1e4;
          // Pinecone-specific Configuration
          case "WORKSPACE_PINECONE_PROVIDER":
            return process.env.WORKSPACE_PINECONE_PROVIDER;
          case "WORKSPACE_PINECONE_REGION":
            return process.env.WORKSPACE_PINECONE_REGION;
          case "WORKSPACE_PINECONE_NAMESPACE":
            return process.env.WORKSPACE_PINECONE_NAMESPACE;
          // Cross-Tool Memory Sharing Configuration
          case "CIPHER_USER_ID":
            return process.env.CIPHER_USER_ID;
          case "CIPHER_PROJECT_NAME":
            return process.env.CIPHER_PROJECT_NAME;
          case "CIPHER_WORKSPACE_MODE":
            return process.env.CIPHER_WORKSPACE_MODE || "isolated";
          case "USE_ASK_CIPHER":
            return process.env.USE_ASK_CIPHER === "true";
          default:
            return process.env[prop];
        }
      }
    });
    validateEnv = () => {
      const embeddingsDisabled = process.env.DISABLE_EMBEDDINGS === "true" || process.env.EMBEDDING_DISABLED === "true";
      if (!embeddingsDisabled) {
        const hasOpenAI = !!process.env.OPENAI_API_KEY;
        const hasGemini = !!process.env.GEMINI_API_KEY;
        const hasOpenRouter = !!process.env.OPENROUTER_API_KEY;
        const hasOllama = !!process.env.OLLAMA_BASE_URL;
        const hasAnyEmbeddingProvider = hasOpenAI || hasGemini || hasOpenRouter || hasOllama;
        if (!hasAnyEmbeddingProvider) {
          const errorMsg = "No embedding provider configured. Set one of: OPENAI_API_KEY, GEMINI_API_KEY, OPENROUTER_API_KEY, OLLAMA_BASE_URL, or set DISABLE_EMBEDDINGS=true to run without memory capabilities";
          if (isMcpMode) {
            process.stderr.write(`[CIPHER-MCP] WARNING: ${errorMsg}
`);
          } else {
            console.warn(errorMsg);
          }
        }
      } else {
        const infoMsg = "Embeddings are disabled - Cipher will run without memory capabilities";
        if (isMcpMode) {
          process.stderr.write(`[CIPHER-MCP] INFO: ${infoMsg}
`);
        } else {
          console.info(infoMsg);
        }
      }
      const envToValidate = {
        NODE_ENV: process.env.NODE_ENV,
        CIPHER_LOG_LEVEL: process.env.CIPHER_LOG_LEVEL,
        REDACT_SECRETS: process.env.REDACT_SECRETS === "false" ? false : true,
        OPENAI_API_KEY: process.env.OPENAI_API_KEY,
        ANTHROPIC_API_KEY: process.env.ANTHROPIC_API_KEY,
        OPENROUTER_API_KEY: process.env.OPENROUTER_API_KEY,
        QWEN_API_KEY: process.env.QWEN_API_KEY,
        OPENAI_BASE_URL: process.env.OPENAI_BASE_URL,
        OLLAMA_BASE_URL: process.env.OLLAMA_BASE_URL,
        OPENAI_ORG_ID: process.env.OPENAI_ORG_ID,
        // Embedding Configuration
        EMBEDDING_PROVIDER: process.env.EMBEDDING_PROVIDER,
        EMBEDDING_MODEL: process.env.EMBEDDING_MODEL,
        EMBEDDING_TIMEOUT: process.env.EMBEDDING_TIMEOUT ? parseInt(process.env.EMBEDDING_TIMEOUT, 10) : void 0,
        EMBEDDING_MAX_RETRIES: process.env.EMBEDDING_MAX_RETRIES ? parseInt(process.env.EMBEDDING_MAX_RETRIES, 10) : void 0,
        EMBEDDING_DIMENSIONS: process.env.EMBEDDING_DIMENSIONS ? parseInt(process.env.EMBEDDING_DIMENSIONS, 10) : void 0,
        DISABLE_EMBEDDINGS: process.env.DISABLE_EMBEDDINGS === "true",
        EMBEDDING_DISABLED: process.env.EMBEDDING_DISABLED === "true",
        GEMINI_API_KEY: process.env.GEMINI_API_KEY,
        GEMINI_BASE_URL: process.env.GEMINI_BASE_URL,
        // Storage Configuration
        STORAGE_CACHE_TYPE: process.env.STORAGE_CACHE_TYPE || "in-memory",
        STORAGE_CACHE_HOST: process.env.STORAGE_CACHE_HOST,
        STORAGE_CACHE_PORT: process.env.STORAGE_CACHE_PORT ? parseInt(process.env.STORAGE_CACHE_PORT, 10) : void 0,
        STORAGE_CACHE_USERNAME: process.env.STORAGE_CACHE_USERNAME,
        STORAGE_CACHE_PASSWORD: process.env.STORAGE_CACHE_PASSWORD,
        STORAGE_CACHE_DATABASE: process.env.STORAGE_CACHE_DATABASE ? parseInt(process.env.STORAGE_CACHE_DATABASE, 10) : void 0,
        STORAGE_DATABASE_TYPE: process.env.STORAGE_DATABASE_TYPE || "in-memory",
        STORAGE_DATABASE_PATH: process.env.STORAGE_DATABASE_PATH,
        STORAGE_DATABASE_NAME: process.env.STORAGE_DATABASE_NAME,
        // PostgreSQL Configuration
        CIPHER_PG_URL: process.env.CIPHER_PG_URL,
        STORAGE_DATABASE_HOST: process.env.STORAGE_DATABASE_HOST,
        STORAGE_DATABASE_PORT: process.env.STORAGE_DATABASE_PORT ? parseInt(process.env.STORAGE_DATABASE_PORT, 10) : void 0,
        STORAGE_DATABASE_USER: process.env.STORAGE_DATABASE_USER,
        STORAGE_DATABASE_PASSWORD: process.env.STORAGE_DATABASE_PASSWORD,
        STORAGE_DATABASE_SSL: process.env.STORAGE_DATABASE_SSL === "true",
        // Vector Storage Configuration
        VECTOR_STORE_TYPE: process.env.VECTOR_STORE_TYPE || "in-memory",
        VECTOR_STORE_HOST: process.env.VECTOR_STORE_HOST,
        VECTOR_STORE_PORT: process.env.VECTOR_STORE_PORT ? parseInt(process.env.VECTOR_STORE_PORT, 10) : void 0,
        VECTOR_STORE_URL: process.env.VECTOR_STORE_URL,
        VECTOR_STORE_API_KEY: process.env.VECTOR_STORE_API_KEY,
        VECTOR_STORE_USERNAME: process.env.VECTOR_STORE_USERNAME,
        VECTOR_STORE_PASSWORD: process.env.VECTOR_STORE_PASSWORD,
        VECTOR_STORE_COLLECTION: process.env.VECTOR_STORE_COLLECTION || "knowledge_memory",
        VECTOR_STORE_DIMENSION: process.env.VECTOR_STORE_DIMENSION ? parseInt(process.env.VECTOR_STORE_DIMENSION, 10) : 1536,
        VECTOR_STORE_DISTANCE: process.env.VECTOR_STORE_DISTANCE || "Cosine",
        VECTOR_STORE_ON_DISK: process.env.VECTOR_STORE_ON_DISK === "true",
        VECTOR_STORE_MAX_VECTORS: process.env.VECTOR_STORE_MAX_VECTORS ? parseInt(process.env.VECTOR_STORE_MAX_VECTORS, 10) : 1e4,
        // Faiss-specific Configuration
        FAISS_BASE_STORAGE_PATH: process.env.FAISS_BASE_STORAGE_PATH,
        // Knowledge Graph Configuration
        KNOWLEDGE_GRAPH_ENABLED: process.env.KNOWLEDGE_GRAPH_ENABLED === "true",
        KNOWLEDGE_GRAPH_TYPE: process.env.KNOWLEDGE_GRAPH_TYPE || "in-memory",
        KNOWLEDGE_GRAPH_HOST: process.env.KNOWLEDGE_GRAPH_HOST,
        KNOWLEDGE_GRAPH_PORT: process.env.KNOWLEDGE_GRAPH_PORT ? parseInt(process.env.KNOWLEDGE_GRAPH_PORT, 10) : void 0,
        KNOWLEDGE_GRAPH_URI: process.env.KNOWLEDGE_GRAPH_URI,
        KNOWLEDGE_GRAPH_USERNAME: process.env.KNOWLEDGE_GRAPH_USERNAME,
        KNOWLEDGE_GRAPH_PASSWORD: process.env.KNOWLEDGE_GRAPH_PASSWORD,
        KNOWLEDGE_GRAPH_DATABASE: process.env.KNOWLEDGE_GRAPH_DATABASE || "neo4j",
        // Memory Search Configuration
        SEARCH_MEMORY_TYPE: process.env.SEARCH_MEMORY_TYPE || "knowledge",
        // Reflection Memory Configuration
        REFLECTION_VECTOR_STORE_COLLECTION: process.env.REFLECTION_VECTOR_STORE_COLLECTION || "reflection_memory",
        DISABLE_REFLECTION_MEMORY: process.env.DISABLE_REFLECTION_MEMORY === "true",
        // Workspace Memory Configuration
        USE_WORKSPACE_MEMORY: process.env.USE_WORKSPACE_MEMORY === "true",
        WORKSPACE_SEARCH_THRESHOLD: process.env.WORKSPACE_SEARCH_THRESHOLD ? parseFloat(process.env.WORKSPACE_SEARCH_THRESHOLD) : 0.4,
        DISABLE_DEFAULT_MEMORY: process.env.DISABLE_DEFAULT_MEMORY === "true",
        WORKSPACE_VECTOR_STORE_TYPE: process.env.WORKSPACE_VECTOR_STORE_TYPE,
        WORKSPACE_VECTOR_STORE_HOST: process.env.WORKSPACE_VECTOR_STORE_HOST,
        WORKSPACE_VECTOR_STORE_PORT: process.env.WORKSPACE_VECTOR_STORE_PORT ? parseInt(process.env.WORKSPACE_VECTOR_STORE_PORT, 10) : void 0,
        WORKSPACE_VECTOR_STORE_URL: process.env.WORKSPACE_VECTOR_STORE_URL,
        WORKSPACE_VECTOR_STORE_API_KEY: process.env.WORKSPACE_VECTOR_STORE_API_KEY,
        WORKSPACE_VECTOR_STORE_USERNAME: process.env.WORKSPACE_VECTOR_STORE_USERNAME,
        WORKSPACE_VECTOR_STORE_PASSWORD: process.env.WORKSPACE_VECTOR_STORE_PASSWORD,
        WORKSPACE_VECTOR_STORE_COLLECTION: process.env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory",
        WORKSPACE_VECTOR_STORE_DIMENSION: process.env.WORKSPACE_VECTOR_STORE_DIMENSION ? parseInt(process.env.WORKSPACE_VECTOR_STORE_DIMENSION, 10) : 1536,
        WORKSPACE_VECTOR_STORE_DISTANCE: process.env.WORKSPACE_VECTOR_STORE_DISTANCE || "Cosine",
        WORKSPACE_VECTOR_STORE_ON_DISK: process.env.WORKSPACE_VECTOR_STORE_ON_DISK === "true",
        WORKSPACE_VECTOR_STORE_MAX_VECTORS: process.env.WORKSPACE_VECTOR_STORE_MAX_VECTORS ? parseInt(process.env.WORKSPACE_VECTOR_STORE_MAX_VECTORS, 10) : 1e4,
        // Cross-Tool Memory Sharing Configuration
        CIPHER_USER_ID: process.env.CIPHER_USER_ID,
        CIPHER_PROJECT_NAME: process.env.CIPHER_PROJECT_NAME,
        CIPHER_WORKSPACE_MODE: process.env.CIPHER_WORKSPACE_MODE || "isolated"
      };
      const result = envSchema.safeParse(envToValidate);
      if (!result.success) {
        const errorMsg = `Environment validation failed: ${JSON.stringify(result.error.issues)}`;
        if (isMcpMode) {
          process.stderr.write(`[CIPHER-MCP] ERROR: ${errorMsg}
`);
        } else {
          console.error("Environment validation failed:", result.error.issues);
        }
        return false;
      }
      return result.success;
    };
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/error.js
var require_error = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/error.js"(exports2) {
    init_cjs_shims();
    var CommanderError2 = class extends Error {
      /**
       * Constructs the CommanderError class
       * @param {number} exitCode suggested exit code which could be used with process.exit
       * @param {string} code an id string representing the error
       * @param {string} message human-readable description of the error
       * @constructor
       */
      constructor(exitCode, code, message) {
        super(message);
        Error.captureStackTrace(this, this.constructor);
        this.name = this.constructor.name;
        this.code = code;
        this.exitCode = exitCode;
        this.nestedError = void 0;
      }
    };
    var InvalidArgumentError2 = class extends CommanderError2 {
      /**
       * Constructs the InvalidArgumentError class
       * @param {string} [message] explanation of why argument is invalid
       * @constructor
       */
      constructor(message) {
        super(1, "commander.invalidArgument", message);
        Error.captureStackTrace(this, this.constructor);
        this.name = this.constructor.name;
      }
    };
    exports2.CommanderError = CommanderError2;
    exports2.InvalidArgumentError = InvalidArgumentError2;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/argument.js
var require_argument = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/argument.js"(exports2) {
    init_cjs_shims();
    var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var Argument2 = class {
      /**
       * Initialize a new command argument with the given name and description.
       * The default is that the argument is required, and you can explicitly
       * indicate this with <> around the name. Put [] around the name for an optional argument.
       *
       * @param {string} name
       * @param {string} [description]
       */
      constructor(name, description) {
        this.description = description || "";
        this.variadic = false;
        this.parseArg = void 0;
        this.defaultValue = void 0;
        this.defaultValueDescription = void 0;
        this.argChoices = void 0;
        switch (name[0]) {
          case "<":
            this.required = true;
            this._name = name.slice(1, -1);
            break;
          case "[":
            this.required = false;
            this._name = name.slice(1, -1);
            break;
          default:
            this.required = true;
            this._name = name;
            break;
        }
        if (this._name.length > 3 && this._name.slice(-3) === "...") {
          this.variadic = true;
          this._name = this._name.slice(0, -3);
        }
      }
      /**
       * Return argument name.
       *
       * @return {string}
       */
      name() {
        return this._name;
      }
      /**
       * @api private
       */
      _concatValue(value, previous) {
        if (previous === this.defaultValue || !Array.isArray(previous)) {
          return [value];
        }
        return previous.concat(value);
      }
      /**
       * Set the default value, and optionally supply the description to be displayed in the help.
       *
       * @param {*} value
       * @param {string} [description]
       * @return {Argument}
       */
      default(value, description) {
        this.defaultValue = value;
        this.defaultValueDescription = description;
        return this;
      }
      /**
       * Set the custom handler for processing CLI command arguments into argument values.
       *
       * @param {Function} [fn]
       * @return {Argument}
       */
      argParser(fn) {
        this.parseArg = fn;
        return this;
      }
      /**
       * Only allow argument value to be one of choices.
       *
       * @param {string[]} values
       * @return {Argument}
       */
      choices(values) {
        this.argChoices = values.slice();
        this.parseArg = (arg, previous) => {
          if (!this.argChoices.includes(arg)) {
            throw new InvalidArgumentError2(`Allowed choices are ${this.argChoices.join(", ")}.`);
          }
          if (this.variadic) {
            return this._concatValue(arg, previous);
          }
          return arg;
        };
        return this;
      }
      /**
       * Make argument required.
       */
      argRequired() {
        this.required = true;
        return this;
      }
      /**
       * Make argument optional.
       */
      argOptional() {
        this.required = false;
        return this;
      }
    };
    function humanReadableArgName(arg) {
      const nameOutput = arg.name() + (arg.variadic === true ? "..." : "");
      return arg.required ? "<" + nameOutput + ">" : "[" + nameOutput + "]";
    }
    exports2.Argument = Argument2;
    exports2.humanReadableArgName = humanReadableArgName;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/help.js
var require_help = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/help.js"(exports2) {
    init_cjs_shims();
    var { humanReadableArgName } = require_argument();
    var Help2 = class {
      constructor() {
        this.helpWidth = void 0;
        this.sortSubcommands = false;
        this.sortOptions = false;
        this.showGlobalOptions = false;
      }
      /**
       * Get an array of the visible subcommands. Includes a placeholder for the implicit help command, if there is one.
       *
       * @param {Command} cmd
       * @returns {Command[]}
       */
      visibleCommands(cmd) {
        const visibleCommands = cmd.commands.filter((cmd2) => !cmd2._hidden);
        if (cmd._hasImplicitHelpCommand()) {
          const [, helpName, helpArgs] = cmd._helpCommandnameAndArgs.match(/([^ ]+) *(.*)/);
          const helpCommand = cmd.createCommand(helpName).helpOption(false);
          helpCommand.description(cmd._helpCommandDescription);
          if (helpArgs) helpCommand.arguments(helpArgs);
          visibleCommands.push(helpCommand);
        }
        if (this.sortSubcommands) {
          visibleCommands.sort((a, b) => {
            return a.name().localeCompare(b.name());
          });
        }
        return visibleCommands;
      }
      /**
       * Compare options for sort.
       *
       * @param {Option} a
       * @param {Option} b
       * @returns number
       */
      compareOptions(a, b) {
        const getSortKey = (option) => {
          return option.short ? option.short.replace(/^-/, "") : option.long.replace(/^--/, "");
        };
        return getSortKey(a).localeCompare(getSortKey(b));
      }
      /**
       * Get an array of the visible options. Includes a placeholder for the implicit help option, if there is one.
       *
       * @param {Command} cmd
       * @returns {Option[]}
       */
      visibleOptions(cmd) {
        const visibleOptions = cmd.options.filter((option) => !option.hidden);
        const showShortHelpFlag = cmd._hasHelpOption && cmd._helpShortFlag && !cmd._findOption(cmd._helpShortFlag);
        const showLongHelpFlag = cmd._hasHelpOption && !cmd._findOption(cmd._helpLongFlag);
        if (showShortHelpFlag || showLongHelpFlag) {
          let helpOption;
          if (!showShortHelpFlag) {
            helpOption = cmd.createOption(cmd._helpLongFlag, cmd._helpDescription);
          } else if (!showLongHelpFlag) {
            helpOption = cmd.createOption(cmd._helpShortFlag, cmd._helpDescription);
          } else {
            helpOption = cmd.createOption(cmd._helpFlags, cmd._helpDescription);
          }
          visibleOptions.push(helpOption);
        }
        if (this.sortOptions) {
          visibleOptions.sort(this.compareOptions);
        }
        return visibleOptions;
      }
      /**
       * Get an array of the visible global options. (Not including help.)
       *
       * @param {Command} cmd
       * @returns {Option[]}
       */
      visibleGlobalOptions(cmd) {
        if (!this.showGlobalOptions) return [];
        const globalOptions = [];
        for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
          const visibleOptions = ancestorCmd.options.filter((option) => !option.hidden);
          globalOptions.push(...visibleOptions);
        }
        if (this.sortOptions) {
          globalOptions.sort(this.compareOptions);
        }
        return globalOptions;
      }
      /**
       * Get an array of the arguments if any have a description.
       *
       * @param {Command} cmd
       * @returns {Argument[]}
       */
      visibleArguments(cmd) {
        if (cmd._argsDescription) {
          cmd.registeredArguments.forEach((argument) => {
            argument.description = argument.description || cmd._argsDescription[argument.name()] || "";
          });
        }
        if (cmd.registeredArguments.find((argument) => argument.description)) {
          return cmd.registeredArguments;
        }
        return [];
      }
      /**
       * Get the command term to show in the list of subcommands.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      subcommandTerm(cmd) {
        const args = cmd.registeredArguments.map((arg) => humanReadableArgName(arg)).join(" ");
        return cmd._name + (cmd._aliases[0] ? "|" + cmd._aliases[0] : "") + (cmd.options.length ? " [options]" : "") + // simplistic check for non-help option
        (args ? " " + args : "");
      }
      /**
       * Get the option term to show in the list of options.
       *
       * @param {Option} option
       * @returns {string}
       */
      optionTerm(option) {
        return option.flags;
      }
      /**
       * Get the argument term to show in the list of arguments.
       *
       * @param {Argument} argument
       * @returns {string}
       */
      argumentTerm(argument) {
        return argument.name();
      }
      /**
       * Get the longest command term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestSubcommandTermLength(cmd, helper) {
        return helper.visibleCommands(cmd).reduce((max, command) => {
          return Math.max(max, helper.subcommandTerm(command).length);
        }, 0);
      }
      /**
       * Get the longest option term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestOptionTermLength(cmd, helper) {
        return helper.visibleOptions(cmd).reduce((max, option) => {
          return Math.max(max, helper.optionTerm(option).length);
        }, 0);
      }
      /**
       * Get the longest global option term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestGlobalOptionTermLength(cmd, helper) {
        return helper.visibleGlobalOptions(cmd).reduce((max, option) => {
          return Math.max(max, helper.optionTerm(option).length);
        }, 0);
      }
      /**
       * Get the longest argument term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      longestArgumentTermLength(cmd, helper) {
        return helper.visibleArguments(cmd).reduce((max, argument) => {
          return Math.max(max, helper.argumentTerm(argument).length);
        }, 0);
      }
      /**
       * Get the command usage to be displayed at the top of the built-in help.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      commandUsage(cmd) {
        let cmdName = cmd._name;
        if (cmd._aliases[0]) {
          cmdName = cmdName + "|" + cmd._aliases[0];
        }
        let ancestorCmdNames = "";
        for (let ancestorCmd = cmd.parent; ancestorCmd; ancestorCmd = ancestorCmd.parent) {
          ancestorCmdNames = ancestorCmd.name() + " " + ancestorCmdNames;
        }
        return ancestorCmdNames + cmdName + " " + cmd.usage();
      }
      /**
       * Get the description for the command.
       *
       * @param {Command} cmd
       * @returns {string}
       */
      commandDescription(cmd) {
        return cmd.description();
      }
      /**
       * Get the subcommand summary to show in the list of subcommands.
       * (Fallback to description for backwards compatibility.)
       *
       * @param {Command} cmd
       * @returns {string}
       */
      subcommandDescription(cmd) {
        return cmd.summary() || cmd.description();
      }
      /**
       * Get the option description to show in the list of options.
       *
       * @param {Option} option
       * @return {string}
       */
      optionDescription(option) {
        const extraInfo = [];
        if (option.argChoices) {
          extraInfo.push(
            // use stringify to match the display of the default value
            `choices: ${option.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
          );
        }
        if (option.defaultValue !== void 0) {
          const showDefault = option.required || option.optional || option.isBoolean() && typeof option.defaultValue === "boolean";
          if (showDefault) {
            extraInfo.push(`default: ${option.defaultValueDescription || JSON.stringify(option.defaultValue)}`);
          }
        }
        if (option.presetArg !== void 0 && option.optional) {
          extraInfo.push(`preset: ${JSON.stringify(option.presetArg)}`);
        }
        if (option.envVar !== void 0) {
          extraInfo.push(`env: ${option.envVar}`);
        }
        if (extraInfo.length > 0) {
          return `${option.description} (${extraInfo.join(", ")})`;
        }
        return option.description;
      }
      /**
       * Get the argument description to show in the list of arguments.
       *
       * @param {Argument} argument
       * @return {string}
       */
      argumentDescription(argument) {
        const extraInfo = [];
        if (argument.argChoices) {
          extraInfo.push(
            // use stringify to match the display of the default value
            `choices: ${argument.argChoices.map((choice) => JSON.stringify(choice)).join(", ")}`
          );
        }
        if (argument.defaultValue !== void 0) {
          extraInfo.push(`default: ${argument.defaultValueDescription || JSON.stringify(argument.defaultValue)}`);
        }
        if (extraInfo.length > 0) {
          const extraDescripton = `(${extraInfo.join(", ")})`;
          if (argument.description) {
            return `${argument.description} ${extraDescripton}`;
          }
          return extraDescripton;
        }
        return argument.description;
      }
      /**
       * Generate the built-in help text.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {string}
       */
      formatHelp(cmd, helper) {
        const termWidth = helper.padWidth(cmd, helper);
        const helpWidth = helper.helpWidth || 80;
        const itemIndentWidth = 2;
        const itemSeparatorWidth = 2;
        function formatItem(term, description) {
          if (description) {
            const fullText = `${term.padEnd(termWidth + itemSeparatorWidth)}${description}`;
            return helper.wrap(fullText, helpWidth - itemIndentWidth, termWidth + itemSeparatorWidth);
          }
          return term;
        }
        function formatList(textArray) {
          return textArray.join("\n").replace(/^/gm, " ".repeat(itemIndentWidth));
        }
        let output = [`Usage: ${helper.commandUsage(cmd)}`, ""];
        const commandDescription = helper.commandDescription(cmd);
        if (commandDescription.length > 0) {
          output = output.concat([helper.wrap(commandDescription, helpWidth, 0), ""]);
        }
        const argumentList = helper.visibleArguments(cmd).map((argument) => {
          return formatItem(helper.argumentTerm(argument), helper.argumentDescription(argument));
        });
        if (argumentList.length > 0) {
          output = output.concat(["Arguments:", formatList(argumentList), ""]);
        }
        const optionList = helper.visibleOptions(cmd).map((option) => {
          return formatItem(helper.optionTerm(option), helper.optionDescription(option));
        });
        if (optionList.length > 0) {
          output = output.concat(["Options:", formatList(optionList), ""]);
        }
        if (this.showGlobalOptions) {
          const globalOptionList = helper.visibleGlobalOptions(cmd).map((option) => {
            return formatItem(helper.optionTerm(option), helper.optionDescription(option));
          });
          if (globalOptionList.length > 0) {
            output = output.concat(["Global Options:", formatList(globalOptionList), ""]);
          }
        }
        const commandList = helper.visibleCommands(cmd).map((cmd2) => {
          return formatItem(helper.subcommandTerm(cmd2), helper.subcommandDescription(cmd2));
        });
        if (commandList.length > 0) {
          output = output.concat(["Commands:", formatList(commandList), ""]);
        }
        return output.join("\n");
      }
      /**
       * Calculate the pad width from the maximum term length.
       *
       * @param {Command} cmd
       * @param {Help} helper
       * @returns {number}
       */
      padWidth(cmd, helper) {
        return Math.max(
          helper.longestOptionTermLength(cmd, helper),
          helper.longestGlobalOptionTermLength(cmd, helper),
          helper.longestSubcommandTermLength(cmd, helper),
          helper.longestArgumentTermLength(cmd, helper)
        );
      }
      /**
       * Wrap the given string to width characters per line, with lines after the first indented.
       * Do not wrap if insufficient room for wrapping (minColumnWidth), or string is manually formatted.
       *
       * @param {string} str
       * @param {number} width
       * @param {number} indent
       * @param {number} [minColumnWidth=40]
       * @return {string}
       *
       */
      wrap(str, width, indent, minColumnWidth = 40) {
        const indents = " \\f\\t\\v\xA0\u1680\u2000-\u200A\u202F\u205F\u3000\uFEFF";
        const manualIndent = new RegExp(`[\\n][${indents}]+`);
        if (str.match(manualIndent)) return str;
        const columnWidth = width - indent;
        if (columnWidth < minColumnWidth) return str;
        const leadingStr = str.slice(0, indent);
        const columnText = str.slice(indent).replace("\r\n", "\n");
        const indentString = " ".repeat(indent);
        const zeroWidthSpace = "\u200B";
        const breaks = `\\s${zeroWidthSpace}`;
        const regex2 = new RegExp(`
|.{1,${columnWidth - 1}}([${breaks}]|$)|[^${breaks}]+?([${breaks}]|$)`, "g");
        const lines = columnText.match(regex2) || [];
        return leadingStr + lines.map((line, i) => {
          if (line === "\n") return "";
          return (i > 0 ? indentString : "") + line.trimEnd();
        }).join("\n");
      }
    };
    exports2.Help = Help2;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/option.js
var require_option = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/option.js"(exports2) {
    init_cjs_shims();
    var { InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var Option2 = class {
      /**
       * Initialize a new `Option` with the given `flags` and `description`.
       *
       * @param {string} flags
       * @param {string} [description]
       */
      constructor(flags, description) {
        this.flags = flags;
        this.description = description || "";
        this.required = flags.includes("<");
        this.optional = flags.includes("[");
        this.variadic = /\w\.\.\.[>\]]$/.test(flags);
        this.mandatory = false;
        const optionFlags = splitOptionFlags(flags);
        this.short = optionFlags.shortFlag;
        this.long = optionFlags.longFlag;
        this.negate = false;
        if (this.long) {
          this.negate = this.long.startsWith("--no-");
        }
        this.defaultValue = void 0;
        this.defaultValueDescription = void 0;
        this.presetArg = void 0;
        this.envVar = void 0;
        this.parseArg = void 0;
        this.hidden = false;
        this.argChoices = void 0;
        this.conflictsWith = [];
        this.implied = void 0;
      }
      /**
       * Set the default value, and optionally supply the description to be displayed in the help.
       *
       * @param {*} value
       * @param {string} [description]
       * @return {Option}
       */
      default(value, description) {
        this.defaultValue = value;
        this.defaultValueDescription = description;
        return this;
      }
      /**
       * Preset to use when option used without option-argument, especially optional but also boolean and negated.
       * The custom processing (parseArg) is called.
       *
       * @example
       * new Option('--color').default('GREYSCALE').preset('RGB');
       * new Option('--donate [amount]').preset('20').argParser(parseFloat);
       *
       * @param {*} arg
       * @return {Option}
       */
      preset(arg) {
        this.presetArg = arg;
        return this;
      }
      /**
       * Add option name(s) that conflict with this option.
       * An error will be displayed if conflicting options are found during parsing.
       *
       * @example
       * new Option('--rgb').conflicts('cmyk');
       * new Option('--js').conflicts(['ts', 'jsx']);
       *
       * @param {string | string[]} names
       * @return {Option}
       */
      conflicts(names) {
        this.conflictsWith = this.conflictsWith.concat(names);
        return this;
      }
      /**
       * Specify implied option values for when this option is set and the implied options are not.
       *
       * The custom processing (parseArg) is not called on the implied values.
       *
       * @example
       * program
       *   .addOption(new Option('--log', 'write logging information to file'))
       *   .addOption(new Option('--trace', 'log extra details').implies({ log: 'trace.txt' }));
       *
       * @param {Object} impliedOptionValues
       * @return {Option}
       */
      implies(impliedOptionValues) {
        let newImplied = impliedOptionValues;
        if (typeof impliedOptionValues === "string") {
          newImplied = { [impliedOptionValues]: true };
        }
        this.implied = Object.assign(this.implied || {}, newImplied);
        return this;
      }
      /**
       * Set environment variable to check for option value.
       *
       * An environment variable is only used if when processed the current option value is
       * undefined, or the source of the current value is 'default' or 'config' or 'env'.
       *
       * @param {string} name
       * @return {Option}
       */
      env(name) {
        this.envVar = name;
        return this;
      }
      /**
       * Set the custom handler for processing CLI option arguments into option values.
       *
       * @param {Function} [fn]
       * @return {Option}
       */
      argParser(fn) {
        this.parseArg = fn;
        return this;
      }
      /**
       * Whether the option is mandatory and must have a value after parsing.
       *
       * @param {boolean} [mandatory=true]
       * @return {Option}
       */
      makeOptionMandatory(mandatory = true) {
        this.mandatory = !!mandatory;
        return this;
      }
      /**
       * Hide option in help.
       *
       * @param {boolean} [hide=true]
       * @return {Option}
       */
      hideHelp(hide = true) {
        this.hidden = !!hide;
        return this;
      }
      /**
       * @api private
       */
      _concatValue(value, previous) {
        if (previous === this.defaultValue || !Array.isArray(previous)) {
          return [value];
        }
        return previous.concat(value);
      }
      /**
       * Only allow option value to be one of choices.
       *
       * @param {string[]} values
       * @return {Option}
       */
      choices(values) {
        this.argChoices = values.slice();
        this.parseArg = (arg, previous) => {
          if (!this.argChoices.includes(arg)) {
            throw new InvalidArgumentError2(`Allowed choices are ${this.argChoices.join(", ")}.`);
          }
          if (this.variadic) {
            return this._concatValue(arg, previous);
          }
          return arg;
        };
        return this;
      }
      /**
       * Return option name.
       *
       * @return {string}
       */
      name() {
        if (this.long) {
          return this.long.replace(/^--/, "");
        }
        return this.short.replace(/^-/, "");
      }
      /**
       * Return option name, in a camelcase format that can be used
       * as a object attribute key.
       *
       * @return {string}
       * @api private
       */
      attributeName() {
        return camelcase(this.name().replace(/^no-/, ""));
      }
      /**
       * Check if `arg` matches the short or long flag.
       *
       * @param {string} arg
       * @return {boolean}
       * @api private
       */
      is(arg) {
        return this.short === arg || this.long === arg;
      }
      /**
       * Return whether a boolean option.
       *
       * Options are one of boolean, negated, required argument, or optional argument.
       *
       * @return {boolean}
       * @api private
       */
      isBoolean() {
        return !this.required && !this.optional && !this.negate;
      }
    };
    var DualOptions = class {
      /**
       * @param {Option[]} options
       */
      constructor(options) {
        this.positiveOptions = /* @__PURE__ */ new Map();
        this.negativeOptions = /* @__PURE__ */ new Map();
        this.dualOptions = /* @__PURE__ */ new Set();
        options.forEach((option) => {
          if (option.negate) {
            this.negativeOptions.set(option.attributeName(), option);
          } else {
            this.positiveOptions.set(option.attributeName(), option);
          }
        });
        this.negativeOptions.forEach((value, key) => {
          if (this.positiveOptions.has(key)) {
            this.dualOptions.add(key);
          }
        });
      }
      /**
       * Did the value come from the option, and not from possible matching dual option?
       *
       * @param {*} value
       * @param {Option} option
       * @returns {boolean}
       */
      valueFromOption(value, option) {
        const optionKey = option.attributeName();
        if (!this.dualOptions.has(optionKey)) return true;
        const preset = this.negativeOptions.get(optionKey).presetArg;
        const negativeValue = preset !== void 0 ? preset : false;
        return option.negate === (negativeValue === value);
      }
    };
    function camelcase(str) {
      return str.split("-").reduce((str2, word) => {
        return str2 + word[0].toUpperCase() + word.slice(1);
      });
    }
    function splitOptionFlags(flags) {
      let shortFlag;
      let longFlag;
      const flagParts = flags.split(/[ |,]+/);
      if (flagParts.length > 1 && !/^[[<]/.test(flagParts[1])) shortFlag = flagParts.shift();
      longFlag = flagParts.shift();
      if (!shortFlag && /^-[^-]$/.test(longFlag)) {
        shortFlag = longFlag;
        longFlag = void 0;
      }
      return { shortFlag, longFlag };
    }
    exports2.Option = Option2;
    exports2.splitOptionFlags = splitOptionFlags;
    exports2.DualOptions = DualOptions;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/suggestSimilar.js
var require_suggestSimilar = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/suggestSimilar.js"(exports2) {
    init_cjs_shims();
    var maxDistance = 3;
    function editDistance(a, b) {
      if (Math.abs(a.length - b.length) > maxDistance) return Math.max(a.length, b.length);
      const d = [];
      for (let i = 0; i <= a.length; i++) {
        d[i] = [i];
      }
      for (let j = 0; j <= b.length; j++) {
        d[0][j] = j;
      }
      for (let j = 1; j <= b.length; j++) {
        for (let i = 1; i <= a.length; i++) {
          let cost = 1;
          if (a[i - 1] === b[j - 1]) {
            cost = 0;
          } else {
            cost = 1;
          }
          d[i][j] = Math.min(
            d[i - 1][j] + 1,
            // deletion
            d[i][j - 1] + 1,
            // insertion
            d[i - 1][j - 1] + cost
            // substitution
          );
          if (i > 1 && j > 1 && a[i - 1] === b[j - 2] && a[i - 2] === b[j - 1]) {
            d[i][j] = Math.min(d[i][j], d[i - 2][j - 2] + 1);
          }
        }
      }
      return d[a.length][b.length];
    }
    function suggestSimilar(word, candidates) {
      if (!candidates || candidates.length === 0) return "";
      candidates = Array.from(new Set(candidates));
      const searchingOptions = word.startsWith("--");
      if (searchingOptions) {
        word = word.slice(2);
        candidates = candidates.map((candidate) => candidate.slice(2));
      }
      let similar = [];
      let bestDistance = maxDistance;
      const minSimilarity = 0.4;
      candidates.forEach((candidate) => {
        if (candidate.length <= 1) return;
        const distance = editDistance(word, candidate);
        const length = Math.max(word.length, candidate.length);
        const similarity = (length - distance) / length;
        if (similarity > minSimilarity) {
          if (distance < bestDistance) {
            bestDistance = distance;
            similar = [candidate];
          } else if (distance === bestDistance) {
            similar.push(candidate);
          }
        }
      });
      similar.sort((a, b) => a.localeCompare(b));
      if (searchingOptions) {
        similar = similar.map((candidate) => `--${candidate}`);
      }
      if (similar.length > 1) {
        return `
(Did you mean one of ${similar.join(", ")}?)`;
      }
      if (similar.length === 1) {
        return `
(Did you mean ${similar[0]}?)`;
      }
      return "";
    }
    exports2.suggestSimilar = suggestSimilar;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/command.js
var require_command = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/lib/command.js"(exports2) {
    init_cjs_shims();
    var EventEmitter4 = require("events").EventEmitter;
    var childProcess = require("child_process");
    var path12 = require("path");
    var fs10 = require("fs");
    var process4 = require("process");
    var { Argument: Argument2, humanReadableArgName } = require_argument();
    var { CommanderError: CommanderError2 } = require_error();
    var { Help: Help2 } = require_help();
    var { Option: Option2, splitOptionFlags, DualOptions } = require_option();
    var { suggestSimilar } = require_suggestSimilar();
    var Command2 = class _Command extends EventEmitter4 {
      /**
       * Initialize a new `Command`.
       *
       * @param {string} [name]
       */
      constructor(name) {
        super();
        this.commands = [];
        this.options = [];
        this.parent = null;
        this._allowUnknownOption = false;
        this._allowExcessArguments = true;
        this.registeredArguments = [];
        this._args = this.registeredArguments;
        this.args = [];
        this.rawArgs = [];
        this.processedArgs = [];
        this._scriptPath = null;
        this._name = name || "";
        this._optionValues = {};
        this._optionValueSources = {};
        this._storeOptionsAsProperties = false;
        this._actionHandler = null;
        this._executableHandler = false;
        this._executableFile = null;
        this._executableDir = null;
        this._defaultCommandName = null;
        this._exitCallback = null;
        this._aliases = [];
        this._combineFlagAndOptionalValue = true;
        this._description = "";
        this._summary = "";
        this._argsDescription = void 0;
        this._enablePositionalOptions = false;
        this._passThroughOptions = false;
        this._lifeCycleHooks = {};
        this._showHelpAfterError = false;
        this._showSuggestionAfterError = true;
        this._outputConfiguration = {
          writeOut: (str) => process4.stdout.write(str),
          writeErr: (str) => process4.stderr.write(str),
          getOutHelpWidth: () => process4.stdout.isTTY ? process4.stdout.columns : void 0,
          getErrHelpWidth: () => process4.stderr.isTTY ? process4.stderr.columns : void 0,
          outputError: (str, write) => write(str)
        };
        this._hidden = false;
        this._hasHelpOption = true;
        this._helpFlags = "-h, --help";
        this._helpDescription = "display help for command";
        this._helpShortFlag = "-h";
        this._helpLongFlag = "--help";
        this._addImplicitHelpCommand = void 0;
        this._helpCommandName = "help";
        this._helpCommandnameAndArgs = "help [command]";
        this._helpCommandDescription = "display help for command";
        this._helpConfiguration = {};
      }
      /**
       * Copy settings that are useful to have in common across root command and subcommands.
       *
       * (Used internally when adding a command using `.command()` so subcommands inherit parent settings.)
       *
       * @param {Command} sourceCommand
       * @return {Command} `this` command for chaining
       */
      copyInheritedSettings(sourceCommand) {
        this._outputConfiguration = sourceCommand._outputConfiguration;
        this._hasHelpOption = sourceCommand._hasHelpOption;
        this._helpFlags = sourceCommand._helpFlags;
        this._helpDescription = sourceCommand._helpDescription;
        this._helpShortFlag = sourceCommand._helpShortFlag;
        this._helpLongFlag = sourceCommand._helpLongFlag;
        this._helpCommandName = sourceCommand._helpCommandName;
        this._helpCommandnameAndArgs = sourceCommand._helpCommandnameAndArgs;
        this._helpCommandDescription = sourceCommand._helpCommandDescription;
        this._helpConfiguration = sourceCommand._helpConfiguration;
        this._exitCallback = sourceCommand._exitCallback;
        this._storeOptionsAsProperties = sourceCommand._storeOptionsAsProperties;
        this._combineFlagAndOptionalValue = sourceCommand._combineFlagAndOptionalValue;
        this._allowExcessArguments = sourceCommand._allowExcessArguments;
        this._enablePositionalOptions = sourceCommand._enablePositionalOptions;
        this._showHelpAfterError = sourceCommand._showHelpAfterError;
        this._showSuggestionAfterError = sourceCommand._showSuggestionAfterError;
        return this;
      }
      /**
       * @returns {Command[]}
       * @api private
       */
      _getCommandAndAncestors() {
        const result = [];
        for (let command = this; command; command = command.parent) {
          result.push(command);
        }
        return result;
      }
      /**
       * Define a command.
       *
       * There are two styles of command: pay attention to where to put the description.
       *
       * @example
       * // Command implemented using action handler (description is supplied separately to `.command`)
       * program
       *   .command('clone <source> [destination]')
       *   .description('clone a repository into a newly created directory')
       *   .action((source, destination) => {
       *     console.log('clone command called');
       *   });
       *
       * // Command implemented using separate executable file (description is second parameter to `.command`)
       * program
       *   .command('start <service>', 'start named service')
       *   .command('stop [service]', 'stop named service, or all if no name supplied');
       *
       * @param {string} nameAndArgs - command name and arguments, args are `<required>` or `[optional]` and last may also be `variadic...`
       * @param {Object|string} [actionOptsOrExecDesc] - configuration options (for action), or description (for executable)
       * @param {Object} [execOpts] - configuration options (for executable)
       * @return {Command} returns new command for action handler, or `this` for executable command
       */
      command(nameAndArgs, actionOptsOrExecDesc, execOpts) {
        let desc = actionOptsOrExecDesc;
        let opts = execOpts;
        if (typeof desc === "object" && desc !== null) {
          opts = desc;
          desc = null;
        }
        opts = opts || {};
        const [, name, args] = nameAndArgs.match(/([^ ]+) *(.*)/);
        const cmd = this.createCommand(name);
        if (desc) {
          cmd.description(desc);
          cmd._executableHandler = true;
        }
        if (opts.isDefault) this._defaultCommandName = cmd._name;
        cmd._hidden = !!(opts.noHelp || opts.hidden);
        cmd._executableFile = opts.executableFile || null;
        if (args) cmd.arguments(args);
        this.commands.push(cmd);
        cmd.parent = this;
        cmd.copyInheritedSettings(this);
        if (desc) return this;
        return cmd;
      }
      /**
       * Factory routine to create a new unattached command.
       *
       * See .command() for creating an attached subcommand, which uses this routine to
       * create the command. You can override createCommand to customise subcommands.
       *
       * @param {string} [name]
       * @return {Command} new command
       */
      createCommand(name) {
        return new _Command(name);
      }
      /**
       * You can customise the help with a subclass of Help by overriding createHelp,
       * or by overriding Help properties using configureHelp().
       *
       * @return {Help}
       */
      createHelp() {
        return Object.assign(new Help2(), this.configureHelp());
      }
      /**
       * You can customise the help by overriding Help properties using configureHelp(),
       * or with a subclass of Help by overriding createHelp().
       *
       * @param {Object} [configuration] - configuration options
       * @return {Command|Object} `this` command for chaining, or stored configuration
       */
      configureHelp(configuration) {
        if (configuration === void 0) return this._helpConfiguration;
        this._helpConfiguration = configuration;
        return this;
      }
      /**
       * The default output goes to stdout and stderr. You can customise this for special
       * applications. You can also customise the display of errors by overriding outputError.
       *
       * The configuration properties are all functions:
       *
       *     // functions to change where being written, stdout and stderr
       *     writeOut(str)
       *     writeErr(str)
       *     // matching functions to specify width for wrapping help
       *     getOutHelpWidth()
       *     getErrHelpWidth()
       *     // functions based on what is being written out
       *     outputError(str, write) // used for displaying errors, and not used for displaying help
       *
       * @param {Object} [configuration] - configuration options
       * @return {Command|Object} `this` command for chaining, or stored configuration
       */
      configureOutput(configuration) {
        if (configuration === void 0) return this._outputConfiguration;
        Object.assign(this._outputConfiguration, configuration);
        return this;
      }
      /**
       * Display the help or a custom message after an error occurs.
       *
       * @param {boolean|string} [displayHelp]
       * @return {Command} `this` command for chaining
       */
      showHelpAfterError(displayHelp = true) {
        if (typeof displayHelp !== "string") displayHelp = !!displayHelp;
        this._showHelpAfterError = displayHelp;
        return this;
      }
      /**
       * Display suggestion of similar commands for unknown commands, or options for unknown options.
       *
       * @param {boolean} [displaySuggestion]
       * @return {Command} `this` command for chaining
       */
      showSuggestionAfterError(displaySuggestion = true) {
        this._showSuggestionAfterError = !!displaySuggestion;
        return this;
      }
      /**
       * Add a prepared subcommand.
       *
       * See .command() for creating an attached subcommand which inherits settings from its parent.
       *
       * @param {Command} cmd - new subcommand
       * @param {Object} [opts] - configuration options
       * @return {Command} `this` command for chaining
       */
      addCommand(cmd, opts) {
        if (!cmd._name) {
          throw new Error(`Command passed to .addCommand() must have a name
- specify the name in Command constructor or using .name()`);
        }
        opts = opts || {};
        if (opts.isDefault) this._defaultCommandName = cmd._name;
        if (opts.noHelp || opts.hidden) cmd._hidden = true;
        this.commands.push(cmd);
        cmd.parent = this;
        return this;
      }
      /**
       * Factory routine to create a new unattached argument.
       *
       * See .argument() for creating an attached argument, which uses this routine to
       * create the argument. You can override createArgument to return a custom argument.
       *
       * @param {string} name
       * @param {string} [description]
       * @return {Argument} new argument
       */
      createArgument(name, description) {
        return new Argument2(name, description);
      }
      /**
       * Define argument syntax for command.
       *
       * The default is that the argument is required, and you can explicitly
       * indicate this with <> around the name. Put [] around the name for an optional argument.
       *
       * @example
       * program.argument('<input-file>');
       * program.argument('[output-file]');
       *
       * @param {string} name
       * @param {string} [description]
       * @param {Function|*} [fn] - custom argument processing function
       * @param {*} [defaultValue]
       * @return {Command} `this` command for chaining
       */
      argument(name, description, fn, defaultValue) {
        const argument = this.createArgument(name, description);
        if (typeof fn === "function") {
          argument.default(defaultValue).argParser(fn);
        } else {
          argument.default(fn);
        }
        this.addArgument(argument);
        return this;
      }
      /**
       * Define argument syntax for command, adding multiple at once (without descriptions).
       *
       * See also .argument().
       *
       * @example
       * program.arguments('<cmd> [env]');
       *
       * @param {string} names
       * @return {Command} `this` command for chaining
       */
      arguments(names) {
        names.trim().split(/ +/).forEach((detail) => {
          this.argument(detail);
        });
        return this;
      }
      /**
       * Define argument syntax for command, adding a prepared argument.
       *
       * @param {Argument} argument
       * @return {Command} `this` command for chaining
       */
      addArgument(argument) {
        const previousArgument = this.registeredArguments.slice(-1)[0];
        if (previousArgument && previousArgument.variadic) {
          throw new Error(`only the last argument can be variadic '${previousArgument.name()}'`);
        }
        if (argument.required && argument.defaultValue !== void 0 && argument.parseArg === void 0) {
          throw new Error(`a default value for a required argument is never used: '${argument.name()}'`);
        }
        this.registeredArguments.push(argument);
        return this;
      }
      /**
       * Override default decision whether to add implicit help command.
       *
       *    addHelpCommand() // force on
       *    addHelpCommand(false); // force off
       *    addHelpCommand('help [cmd]', 'display help for [cmd]'); // force on with custom details
       *
       * @return {Command} `this` command for chaining
       */
      addHelpCommand(enableOrNameAndArgs, description) {
        if (enableOrNameAndArgs === false) {
          this._addImplicitHelpCommand = false;
        } else {
          this._addImplicitHelpCommand = true;
          if (typeof enableOrNameAndArgs === "string") {
            this._helpCommandName = enableOrNameAndArgs.split(" ")[0];
            this._helpCommandnameAndArgs = enableOrNameAndArgs;
          }
          this._helpCommandDescription = description || this._helpCommandDescription;
        }
        return this;
      }
      /**
       * @return {boolean}
       * @api private
       */
      _hasImplicitHelpCommand() {
        if (this._addImplicitHelpCommand === void 0) {
          return this.commands.length && !this._actionHandler && !this._findCommand("help");
        }
        return this._addImplicitHelpCommand;
      }
      /**
       * Add hook for life cycle event.
       *
       * @param {string} event
       * @param {Function} listener
       * @return {Command} `this` command for chaining
       */
      hook(event, listener) {
        const allowedValues = ["preSubcommand", "preAction", "postAction"];
        if (!allowedValues.includes(event)) {
          throw new Error(`Unexpected value for event passed to hook : '${event}'.
Expecting one of '${allowedValues.join("', '")}'`);
        }
        if (this._lifeCycleHooks[event]) {
          this._lifeCycleHooks[event].push(listener);
        } else {
          this._lifeCycleHooks[event] = [listener];
        }
        return this;
      }
      /**
       * Register callback to use as replacement for calling process.exit.
       *
       * @param {Function} [fn] optional callback which will be passed a CommanderError, defaults to throwing
       * @return {Command} `this` command for chaining
       */
      exitOverride(fn) {
        if (fn) {
          this._exitCallback = fn;
        } else {
          this._exitCallback = (err) => {
            if (err.code !== "commander.executeSubCommandAsync") {
              throw err;
            } else {
            }
          };
        }
        return this;
      }
      /**
       * Call process.exit, and _exitCallback if defined.
       *
       * @param {number} exitCode exit code for using with process.exit
       * @param {string} code an id string representing the error
       * @param {string} message human-readable description of the error
       * @return never
       * @api private
       */
      _exit(exitCode, code, message) {
        if (this._exitCallback) {
          this._exitCallback(new CommanderError2(exitCode, code, message));
        }
        process4.exit(exitCode);
      }
      /**
       * Register callback `fn` for the command.
       *
       * @example
       * program
       *   .command('serve')
       *   .description('start service')
       *   .action(function() {
       *      // do work here
       *   });
       *
       * @param {Function} fn
       * @return {Command} `this` command for chaining
       */
      action(fn) {
        const listener = (args) => {
          const expectedArgsCount = this.registeredArguments.length;
          const actionArgs = args.slice(0, expectedArgsCount);
          if (this._storeOptionsAsProperties) {
            actionArgs[expectedArgsCount] = this;
          } else {
            actionArgs[expectedArgsCount] = this.opts();
          }
          actionArgs.push(this);
          return fn.apply(this, actionArgs);
        };
        this._actionHandler = listener;
        return this;
      }
      /**
       * Factory routine to create a new unattached option.
       *
       * See .option() for creating an attached option, which uses this routine to
       * create the option. You can override createOption to return a custom option.
       *
       * @param {string} flags
       * @param {string} [description]
       * @return {Option} new option
       */
      createOption(flags, description) {
        return new Option2(flags, description);
      }
      /**
       * Wrap parseArgs to catch 'commander.invalidArgument'.
       *
       * @param {Option | Argument} target
       * @param {string} value
       * @param {*} previous
       * @param {string} invalidArgumentMessage
       * @api private
       */
      _callParseArg(target, value, previous, invalidArgumentMessage) {
        try {
          return target.parseArg(value, previous);
        } catch (err) {
          if (err.code === "commander.invalidArgument") {
            const message = `${invalidArgumentMessage} ${err.message}`;
            this.error(message, { exitCode: err.exitCode, code: err.code });
          }
          throw err;
        }
      }
      /**
       * Add an option.
       *
       * @param {Option} option
       * @return {Command} `this` command for chaining
       */
      addOption(option) {
        const oname = option.name();
        const name = option.attributeName();
        if (option.negate) {
          const positiveLongFlag = option.long.replace(/^--no-/, "--");
          if (!this._findOption(positiveLongFlag)) {
            this.setOptionValueWithSource(name, option.defaultValue === void 0 ? true : option.defaultValue, "default");
          }
        } else if (option.defaultValue !== void 0) {
          this.setOptionValueWithSource(name, option.defaultValue, "default");
        }
        this.options.push(option);
        const handleOptionValue = (val, invalidValueMessage, valueSource) => {
          if (val == null && option.presetArg !== void 0) {
            val = option.presetArg;
          }
          const oldValue = this.getOptionValue(name);
          if (val !== null && option.parseArg) {
            val = this._callParseArg(option, val, oldValue, invalidValueMessage);
          } else if (val !== null && option.variadic) {
            val = option._concatValue(val, oldValue);
          }
          if (val == null) {
            if (option.negate) {
              val = false;
            } else if (option.isBoolean() || option.optional) {
              val = true;
            } else {
              val = "";
            }
          }
          this.setOptionValueWithSource(name, val, valueSource);
        };
        this.on("option:" + oname, (val) => {
          const invalidValueMessage = `error: option '${option.flags}' argument '${val}' is invalid.`;
          handleOptionValue(val, invalidValueMessage, "cli");
        });
        if (option.envVar) {
          this.on("optionEnv:" + oname, (val) => {
            const invalidValueMessage = `error: option '${option.flags}' value '${val}' from env '${option.envVar}' is invalid.`;
            handleOptionValue(val, invalidValueMessage, "env");
          });
        }
        return this;
      }
      /**
       * Internal implementation shared by .option() and .requiredOption()
       *
       * @api private
       */
      _optionEx(config2, flags, description, fn, defaultValue) {
        if (typeof flags === "object" && flags instanceof Option2) {
          throw new Error("To add an Option object use addOption() instead of option() or requiredOption()");
        }
        const option = this.createOption(flags, description);
        option.makeOptionMandatory(!!config2.mandatory);
        if (typeof fn === "function") {
          option.default(defaultValue).argParser(fn);
        } else if (fn instanceof RegExp) {
          const regex2 = fn;
          fn = (val, def) => {
            const m = regex2.exec(val);
            return m ? m[0] : def;
          };
          option.default(defaultValue).argParser(fn);
        } else {
          option.default(fn);
        }
        return this.addOption(option);
      }
      /**
       * Define option with `flags`, `description`, and optional argument parsing function or `defaultValue` or both.
       *
       * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space. A required
       * option-argument is indicated by `<>` and an optional option-argument by `[]`.
       *
       * See the README for more details, and see also addOption() and requiredOption().
       *
       * @example
       * program
       *     .option('-p, --pepper', 'add pepper')
       *     .option('-p, --pizza-type <TYPE>', 'type of pizza') // required option-argument
       *     .option('-c, --cheese [CHEESE]', 'add extra cheese', 'mozzarella') // optional option-argument with default
       *     .option('-t, --tip <VALUE>', 'add tip to purchase cost', parseFloat) // custom parse function
       *
       * @param {string} flags
       * @param {string} [description]
       * @param {Function|*} [parseArg] - custom option processing function or default value
       * @param {*} [defaultValue]
       * @return {Command} `this` command for chaining
       */
      option(flags, description, parseArg, defaultValue) {
        return this._optionEx({}, flags, description, parseArg, defaultValue);
      }
      /**
      * Add a required option which must have a value after parsing. This usually means
      * the option must be specified on the command line. (Otherwise the same as .option().)
      *
      * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space.
      *
      * @param {string} flags
      * @param {string} [description]
      * @param {Function|*} [parseArg] - custom option processing function or default value
      * @param {*} [defaultValue]
      * @return {Command} `this` command for chaining
      */
      requiredOption(flags, description, parseArg, defaultValue) {
        return this._optionEx({ mandatory: true }, flags, description, parseArg, defaultValue);
      }
      /**
       * Alter parsing of short flags with optional values.
       *
       * @example
       * // for `.option('-f,--flag [value]'):
       * program.combineFlagAndOptionalValue(true);  // `-f80` is treated like `--flag=80`, this is the default behaviour
       * program.combineFlagAndOptionalValue(false) // `-fb` is treated like `-f -b`
       *
       * @param {Boolean} [combine=true] - if `true` or omitted, an optional value can be specified directly after the flag.
       */
      combineFlagAndOptionalValue(combine = true) {
        this._combineFlagAndOptionalValue = !!combine;
        return this;
      }
      /**
       * Allow unknown options on the command line.
       *
       * @param {Boolean} [allowUnknown=true] - if `true` or omitted, no error will be thrown
       * for unknown options.
       */
      allowUnknownOption(allowUnknown = true) {
        this._allowUnknownOption = !!allowUnknown;
        return this;
      }
      /**
       * Allow excess command-arguments on the command line. Pass false to make excess arguments an error.
       *
       * @param {Boolean} [allowExcess=true] - if `true` or omitted, no error will be thrown
       * for excess arguments.
       */
      allowExcessArguments(allowExcess = true) {
        this._allowExcessArguments = !!allowExcess;
        return this;
      }
      /**
       * Enable positional options. Positional means global options are specified before subcommands which lets
       * subcommands reuse the same option names, and also enables subcommands to turn on passThroughOptions.
       * The default behaviour is non-positional and global options may appear anywhere on the command line.
       *
       * @param {Boolean} [positional=true]
       */
      enablePositionalOptions(positional = true) {
        this._enablePositionalOptions = !!positional;
        return this;
      }
      /**
       * Pass through options that come after command-arguments rather than treat them as command-options,
       * so actual command-options come before command-arguments. Turning this on for a subcommand requires
       * positional options to have been enabled on the program (parent commands).
       * The default behaviour is non-positional and options may appear before or after command-arguments.
       *
       * @param {Boolean} [passThrough=true]
       * for unknown options.
       */
      passThroughOptions(passThrough = true) {
        this._passThroughOptions = !!passThrough;
        if (!!this.parent && passThrough && !this.parent._enablePositionalOptions) {
          throw new Error("passThroughOptions can not be used without turning on enablePositionalOptions for parent command(s)");
        }
        return this;
      }
      /**
        * Whether to store option values as properties on command object,
        * or store separately (specify false). In both cases the option values can be accessed using .opts().
        *
        * @param {boolean} [storeAsProperties=true]
        * @return {Command} `this` command for chaining
        */
      storeOptionsAsProperties(storeAsProperties = true) {
        if (this.options.length) {
          throw new Error("call .storeOptionsAsProperties() before adding options");
        }
        this._storeOptionsAsProperties = !!storeAsProperties;
        return this;
      }
      /**
       * Retrieve option value.
       *
       * @param {string} key
       * @return {Object} value
       */
      getOptionValue(key) {
        if (this._storeOptionsAsProperties) {
          return this[key];
        }
        return this._optionValues[key];
      }
      /**
       * Store option value.
       *
       * @param {string} key
       * @param {Object} value
       * @return {Command} `this` command for chaining
       */
      setOptionValue(key, value) {
        return this.setOptionValueWithSource(key, value, void 0);
      }
      /**
        * Store option value and where the value came from.
        *
        * @param {string} key
        * @param {Object} value
        * @param {string} source - expected values are default/config/env/cli/implied
        * @return {Command} `this` command for chaining
        */
      setOptionValueWithSource(key, value, source) {
        if (this._storeOptionsAsProperties) {
          this[key] = value;
        } else {
          this._optionValues[key] = value;
        }
        this._optionValueSources[key] = source;
        return this;
      }
      /**
        * Get source of option value.
        * Expected values are default | config | env | cli | implied
        *
        * @param {string} key
        * @return {string}
        */
      getOptionValueSource(key) {
        return this._optionValueSources[key];
      }
      /**
        * Get source of option value. See also .optsWithGlobals().
        * Expected values are default | config | env | cli | implied
        *
        * @param {string} key
        * @return {string}
        */
      getOptionValueSourceWithGlobals(key) {
        let source;
        this._getCommandAndAncestors().forEach((cmd) => {
          if (cmd.getOptionValueSource(key) !== void 0) {
            source = cmd.getOptionValueSource(key);
          }
        });
        return source;
      }
      /**
       * Get user arguments from implied or explicit arguments.
       * Side-effects: set _scriptPath if args included script. Used for default program name, and subcommand searches.
       *
       * @api private
       */
      _prepareUserArgs(argv, parseOptions) {
        if (argv !== void 0 && !Array.isArray(argv)) {
          throw new Error("first parameter to parse must be array or undefined");
        }
        parseOptions = parseOptions || {};
        if (argv === void 0) {
          argv = process4.argv;
          if (process4.versions && process4.versions.electron) {
            parseOptions.from = "electron";
          }
        }
        this.rawArgs = argv.slice();
        let userArgs;
        switch (parseOptions.from) {
          case void 0:
          case "node":
            this._scriptPath = argv[1];
            userArgs = argv.slice(2);
            break;
          case "electron":
            if (process4.defaultApp) {
              this._scriptPath = argv[1];
              userArgs = argv.slice(2);
            } else {
              userArgs = argv.slice(1);
            }
            break;
          case "user":
            userArgs = argv.slice(0);
            break;
          default:
            throw new Error(`unexpected parse option { from: '${parseOptions.from}' }`);
        }
        if (!this._name && this._scriptPath) this.nameFromFilename(this._scriptPath);
        this._name = this._name || "program";
        return userArgs;
      }
      /**
       * Parse `argv`, setting options and invoking commands when defined.
       *
       * The default expectation is that the arguments are from node and have the application as argv[0]
       * and the script being run in argv[1], with user parameters after that.
       *
       * @example
       * program.parse(process.argv);
       * program.parse(); // implicitly use process.argv and auto-detect node vs electron conventions
       * program.parse(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
       *
       * @param {string[]} [argv] - optional, defaults to process.argv
       * @param {Object} [parseOptions] - optionally specify style of options with from: node/user/electron
       * @param {string} [parseOptions.from] - where the args are from: 'node', 'user', 'electron'
       * @return {Command} `this` command for chaining
       */
      parse(argv, parseOptions) {
        const userArgs = this._prepareUserArgs(argv, parseOptions);
        this._parseCommand([], userArgs);
        return this;
      }
      /**
       * Parse `argv`, setting options and invoking commands when defined.
       *
       * Use parseAsync instead of parse if any of your action handlers are async. Returns a Promise.
       *
       * The default expectation is that the arguments are from node and have the application as argv[0]
       * and the script being run in argv[1], with user parameters after that.
       *
       * @example
       * await program.parseAsync(process.argv);
       * await program.parseAsync(); // implicitly use process.argv and auto-detect node vs electron conventions
       * await program.parseAsync(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
       *
       * @param {string[]} [argv]
       * @param {Object} [parseOptions]
       * @param {string} parseOptions.from - where the args are from: 'node', 'user', 'electron'
       * @return {Promise}
       */
      async parseAsync(argv, parseOptions) {
        const userArgs = this._prepareUserArgs(argv, parseOptions);
        await this._parseCommand([], userArgs);
        return this;
      }
      /**
       * Execute a sub-command executable.
       *
       * @api private
       */
      _executeSubCommand(subcommand, args) {
        args = args.slice();
        let launchWithNode = false;
        const sourceExt = [".js", ".ts", ".tsx", ".mjs", ".cjs"];
        function findFile(baseDir, baseName) {
          const localBin = path12.resolve(baseDir, baseName);
          if (fs10.existsSync(localBin)) return localBin;
          if (sourceExt.includes(path12.extname(baseName))) return void 0;
          const foundExt = sourceExt.find((ext) => fs10.existsSync(`${localBin}${ext}`));
          if (foundExt) return `${localBin}${foundExt}`;
          return void 0;
        }
        this._checkForMissingMandatoryOptions();
        this._checkForConflictingOptions();
        let executableFile = subcommand._executableFile || `${this._name}-${subcommand._name}`;
        let executableDir = this._executableDir || "";
        if (this._scriptPath) {
          let resolvedScriptPath;
          try {
            resolvedScriptPath = fs10.realpathSync(this._scriptPath);
          } catch (err) {
            resolvedScriptPath = this._scriptPath;
          }
          executableDir = path12.resolve(path12.dirname(resolvedScriptPath), executableDir);
        }
        if (executableDir) {
          let localFile = findFile(executableDir, executableFile);
          if (!localFile && !subcommand._executableFile && this._scriptPath) {
            const legacyName = path12.basename(this._scriptPath, path12.extname(this._scriptPath));
            if (legacyName !== this._name) {
              localFile = findFile(executableDir, `${legacyName}-${subcommand._name}`);
            }
          }
          executableFile = localFile || executableFile;
        }
        launchWithNode = sourceExt.includes(path12.extname(executableFile));
        let proc;
        if (process4.platform !== "win32") {
          if (launchWithNode) {
            args.unshift(executableFile);
            args = incrementNodeInspectorPort(process4.execArgv).concat(args);
            proc = childProcess.spawn(process4.argv[0], args, { stdio: "inherit" });
          } else {
            proc = childProcess.spawn(executableFile, args, { stdio: "inherit" });
          }
        } else {
          args.unshift(executableFile);
          args = incrementNodeInspectorPort(process4.execArgv).concat(args);
          proc = childProcess.spawn(process4.execPath, args, { stdio: "inherit" });
        }
        if (!proc.killed) {
          const signals = ["SIGUSR1", "SIGUSR2", "SIGTERM", "SIGINT", "SIGHUP"];
          signals.forEach((signal) => {
            process4.on(signal, () => {
              if (proc.killed === false && proc.exitCode === null) {
                proc.kill(signal);
              }
            });
          });
        }
        const exitCallback = this._exitCallback;
        if (!exitCallback) {
          proc.on("close", process4.exit.bind(process4));
        } else {
          proc.on("close", () => {
            exitCallback(new CommanderError2(process4.exitCode || 0, "commander.executeSubCommandAsync", "(close)"));
          });
        }
        proc.on("error", (err) => {
          if (err.code === "ENOENT") {
            const executableDirMessage = executableDir ? `searched for local subcommand relative to directory '${executableDir}'` : "no directory for search for local subcommand, use .executableDir() to supply a custom directory";
            const executableMissing = `'${executableFile}' does not exist
 - if '${subcommand._name}' is not meant to be an executable command, remove description parameter from '.command()' and use '.description()' instead
 - if the default executable name is not suitable, use the executableFile option to supply a custom name or path
 - ${executableDirMessage}`;
            throw new Error(executableMissing);
          } else if (err.code === "EACCES") {
            throw new Error(`'${executableFile}' not executable`);
          }
          if (!exitCallback) {
            process4.exit(1);
          } else {
            const wrappedError = new CommanderError2(1, "commander.executeSubCommandAsync", "(error)");
            wrappedError.nestedError = err;
            exitCallback(wrappedError);
          }
        });
        this.runningCommand = proc;
      }
      /**
       * @api private
       */
      _dispatchSubcommand(commandName, operands, unknown) {
        const subCommand = this._findCommand(commandName);
        if (!subCommand) this.help({ error: true });
        let promiseChain;
        promiseChain = this._chainOrCallSubCommandHook(promiseChain, subCommand, "preSubcommand");
        promiseChain = this._chainOrCall(promiseChain, () => {
          if (subCommand._executableHandler) {
            this._executeSubCommand(subCommand, operands.concat(unknown));
          } else {
            return subCommand._parseCommand(operands, unknown);
          }
        });
        return promiseChain;
      }
      /**
       * Invoke help directly if possible, or dispatch if necessary.
       * e.g. help foo
       *
       * @api private
       */
      _dispatchHelpCommand(subcommandName) {
        if (!subcommandName) {
          this.help();
        }
        const subCommand = this._findCommand(subcommandName);
        if (subCommand && !subCommand._executableHandler) {
          subCommand.help();
        }
        return this._dispatchSubcommand(subcommandName, [], [
          this._helpLongFlag || this._helpShortFlag
        ]);
      }
      /**
       * Check this.args against expected this.registeredArguments.
       *
       * @api private
       */
      _checkNumberOfArguments() {
        this.registeredArguments.forEach((arg, i) => {
          if (arg.required && this.args[i] == null) {
            this.missingArgument(arg.name());
          }
        });
        if (this.registeredArguments.length > 0 && this.registeredArguments[this.registeredArguments.length - 1].variadic) {
          return;
        }
        if (this.args.length > this.registeredArguments.length) {
          this._excessArguments(this.args);
        }
      }
      /**
       * Process this.args using this.registeredArguments and save as this.processedArgs!
       *
       * @api private
       */
      _processArguments() {
        const myParseArg = (argument, value, previous) => {
          let parsedValue = value;
          if (value !== null && argument.parseArg) {
            const invalidValueMessage = `error: command-argument value '${value}' is invalid for argument '${argument.name()}'.`;
            parsedValue = this._callParseArg(argument, value, previous, invalidValueMessage);
          }
          return parsedValue;
        };
        this._checkNumberOfArguments();
        const processedArgs = [];
        this.registeredArguments.forEach((declaredArg, index) => {
          let value = declaredArg.defaultValue;
          if (declaredArg.variadic) {
            if (index < this.args.length) {
              value = this.args.slice(index);
              if (declaredArg.parseArg) {
                value = value.reduce((processed, v) => {
                  return myParseArg(declaredArg, v, processed);
                }, declaredArg.defaultValue);
              }
            } else if (value === void 0) {
              value = [];
            }
          } else if (index < this.args.length) {
            value = this.args[index];
            if (declaredArg.parseArg) {
              value = myParseArg(declaredArg, value, declaredArg.defaultValue);
            }
          }
          processedArgs[index] = value;
        });
        this.processedArgs = processedArgs;
      }
      /**
       * Once we have a promise we chain, but call synchronously until then.
       *
       * @param {Promise|undefined} promise
       * @param {Function} fn
       * @return {Promise|undefined}
       * @api private
       */
      _chainOrCall(promise, fn) {
        if (promise && promise.then && typeof promise.then === "function") {
          return promise.then(() => fn());
        }
        return fn();
      }
      /**
       *
       * @param {Promise|undefined} promise
       * @param {string} event
       * @return {Promise|undefined}
       * @api private
       */
      _chainOrCallHooks(promise, event) {
        let result = promise;
        const hooks = [];
        this._getCommandAndAncestors().reverse().filter((cmd) => cmd._lifeCycleHooks[event] !== void 0).forEach((hookedCommand) => {
          hookedCommand._lifeCycleHooks[event].forEach((callback) => {
            hooks.push({ hookedCommand, callback });
          });
        });
        if (event === "postAction") {
          hooks.reverse();
        }
        hooks.forEach((hookDetail) => {
          result = this._chainOrCall(result, () => {
            return hookDetail.callback(hookDetail.hookedCommand, this);
          });
        });
        return result;
      }
      /**
       *
       * @param {Promise|undefined} promise
       * @param {Command} subCommand
       * @param {string} event
       * @return {Promise|undefined}
       * @api private
       */
      _chainOrCallSubCommandHook(promise, subCommand, event) {
        let result = promise;
        if (this._lifeCycleHooks[event] !== void 0) {
          this._lifeCycleHooks[event].forEach((hook) => {
            result = this._chainOrCall(result, () => {
              return hook(this, subCommand);
            });
          });
        }
        return result;
      }
      /**
       * Process arguments in context of this command.
       * Returns action result, in case it is a promise.
       *
       * @api private
       */
      _parseCommand(operands, unknown) {
        const parsed = this.parseOptions(unknown);
        this._parseOptionsEnv();
        this._parseOptionsImplied();
        operands = operands.concat(parsed.operands);
        unknown = parsed.unknown;
        this.args = operands.concat(unknown);
        if (operands && this._findCommand(operands[0])) {
          return this._dispatchSubcommand(operands[0], operands.slice(1), unknown);
        }
        if (this._hasImplicitHelpCommand() && operands[0] === this._helpCommandName) {
          return this._dispatchHelpCommand(operands[1]);
        }
        if (this._defaultCommandName) {
          outputHelpIfRequested(this, unknown);
          return this._dispatchSubcommand(this._defaultCommandName, operands, unknown);
        }
        if (this.commands.length && this.args.length === 0 && !this._actionHandler && !this._defaultCommandName) {
          this.help({ error: true });
        }
        outputHelpIfRequested(this, parsed.unknown);
        this._checkForMissingMandatoryOptions();
        this._checkForConflictingOptions();
        const checkForUnknownOptions = () => {
          if (parsed.unknown.length > 0) {
            this.unknownOption(parsed.unknown[0]);
          }
        };
        const commandEvent = `command:${this.name()}`;
        if (this._actionHandler) {
          checkForUnknownOptions();
          this._processArguments();
          let promiseChain;
          promiseChain = this._chainOrCallHooks(promiseChain, "preAction");
          promiseChain = this._chainOrCall(promiseChain, () => this._actionHandler(this.processedArgs));
          if (this.parent) {
            promiseChain = this._chainOrCall(promiseChain, () => {
              this.parent.emit(commandEvent, operands, unknown);
            });
          }
          promiseChain = this._chainOrCallHooks(promiseChain, "postAction");
          return promiseChain;
        }
        if (this.parent && this.parent.listenerCount(commandEvent)) {
          checkForUnknownOptions();
          this._processArguments();
          this.parent.emit(commandEvent, operands, unknown);
        } else if (operands.length) {
          if (this._findCommand("*")) {
            return this._dispatchSubcommand("*", operands, unknown);
          }
          if (this.listenerCount("command:*")) {
            this.emit("command:*", operands, unknown);
          } else if (this.commands.length) {
            this.unknownCommand();
          } else {
            checkForUnknownOptions();
            this._processArguments();
          }
        } else if (this.commands.length) {
          checkForUnknownOptions();
          this.help({ error: true });
        } else {
          checkForUnknownOptions();
          this._processArguments();
        }
      }
      /**
       * Find matching command.
       *
       * @api private
       */
      _findCommand(name) {
        if (!name) return void 0;
        return this.commands.find((cmd) => cmd._name === name || cmd._aliases.includes(name));
      }
      /**
       * Return an option matching `arg` if any.
       *
       * @param {string} arg
       * @return {Option}
       * @api private
       */
      _findOption(arg) {
        return this.options.find((option) => option.is(arg));
      }
      /**
       * Display an error message if a mandatory option does not have a value.
       * Called after checking for help flags in leaf subcommand.
       *
       * @api private
       */
      _checkForMissingMandatoryOptions() {
        this._getCommandAndAncestors().forEach((cmd) => {
          cmd.options.forEach((anOption) => {
            if (anOption.mandatory && cmd.getOptionValue(anOption.attributeName()) === void 0) {
              cmd.missingMandatoryOptionValue(anOption);
            }
          });
        });
      }
      /**
       * Display an error message if conflicting options are used together in this.
       *
       * @api private
       */
      _checkForConflictingLocalOptions() {
        const definedNonDefaultOptions = this.options.filter(
          (option) => {
            const optionKey = option.attributeName();
            if (this.getOptionValue(optionKey) === void 0) {
              return false;
            }
            return this.getOptionValueSource(optionKey) !== "default";
          }
        );
        const optionsWithConflicting = definedNonDefaultOptions.filter(
          (option) => option.conflictsWith.length > 0
        );
        optionsWithConflicting.forEach((option) => {
          const conflictingAndDefined = definedNonDefaultOptions.find(
            (defined) => option.conflictsWith.includes(defined.attributeName())
          );
          if (conflictingAndDefined) {
            this._conflictingOption(option, conflictingAndDefined);
          }
        });
      }
      /**
       * Display an error message if conflicting options are used together.
       * Called after checking for help flags in leaf subcommand.
       *
       * @api private
       */
      _checkForConflictingOptions() {
        this._getCommandAndAncestors().forEach((cmd) => {
          cmd._checkForConflictingLocalOptions();
        });
      }
      /**
       * Parse options from `argv` removing known options,
       * and return argv split into operands and unknown arguments.
       *
       * Examples:
       *
       *     argv => operands, unknown
       *     --known kkk op => [op], []
       *     op --known kkk => [op], []
       *     sub --unknown uuu op => [sub], [--unknown uuu op]
       *     sub -- --unknown uuu op => [sub --unknown uuu op], []
       *
       * @param {String[]} argv
       * @return {{operands: String[], unknown: String[]}}
       */
      parseOptions(argv) {
        const operands = [];
        const unknown = [];
        let dest = operands;
        const args = argv.slice();
        function maybeOption(arg) {
          return arg.length > 1 && arg[0] === "-";
        }
        let activeVariadicOption = null;
        while (args.length) {
          const arg = args.shift();
          if (arg === "--") {
            if (dest === unknown) dest.push(arg);
            dest.push(...args);
            break;
          }
          if (activeVariadicOption && !maybeOption(arg)) {
            this.emit(`option:${activeVariadicOption.name()}`, arg);
            continue;
          }
          activeVariadicOption = null;
          if (maybeOption(arg)) {
            const option = this._findOption(arg);
            if (option) {
              if (option.required) {
                const value = args.shift();
                if (value === void 0) this.optionMissingArgument(option);
                this.emit(`option:${option.name()}`, value);
              } else if (option.optional) {
                let value = null;
                if (args.length > 0 && !maybeOption(args[0])) {
                  value = args.shift();
                }
                this.emit(`option:${option.name()}`, value);
              } else {
                this.emit(`option:${option.name()}`);
              }
              activeVariadicOption = option.variadic ? option : null;
              continue;
            }
          }
          if (arg.length > 2 && arg[0] === "-" && arg[1] !== "-") {
            const option = this._findOption(`-${arg[1]}`);
            if (option) {
              if (option.required || option.optional && this._combineFlagAndOptionalValue) {
                this.emit(`option:${option.name()}`, arg.slice(2));
              } else {
                this.emit(`option:${option.name()}`);
                args.unshift(`-${arg.slice(2)}`);
              }
              continue;
            }
          }
          if (/^--[^=]+=/.test(arg)) {
            const index = arg.indexOf("=");
            const option = this._findOption(arg.slice(0, index));
            if (option && (option.required || option.optional)) {
              this.emit(`option:${option.name()}`, arg.slice(index + 1));
              continue;
            }
          }
          if (maybeOption(arg)) {
            dest = unknown;
          }
          if ((this._enablePositionalOptions || this._passThroughOptions) && operands.length === 0 && unknown.length === 0) {
            if (this._findCommand(arg)) {
              operands.push(arg);
              if (args.length > 0) unknown.push(...args);
              break;
            } else if (arg === this._helpCommandName && this._hasImplicitHelpCommand()) {
              operands.push(arg);
              if (args.length > 0) operands.push(...args);
              break;
            } else if (this._defaultCommandName) {
              unknown.push(arg);
              if (args.length > 0) unknown.push(...args);
              break;
            }
          }
          if (this._passThroughOptions) {
            dest.push(arg);
            if (args.length > 0) dest.push(...args);
            break;
          }
          dest.push(arg);
        }
        return { operands, unknown };
      }
      /**
       * Return an object containing local option values as key-value pairs.
       *
       * @return {Object}
       */
      opts() {
        if (this._storeOptionsAsProperties) {
          const result = {};
          const len = this.options.length;
          for (let i = 0; i < len; i++) {
            const key = this.options[i].attributeName();
            result[key] = key === this._versionOptionName ? this._version : this[key];
          }
          return result;
        }
        return this._optionValues;
      }
      /**
       * Return an object containing merged local and global option values as key-value pairs.
       *
       * @return {Object}
       */
      optsWithGlobals() {
        return this._getCommandAndAncestors().reduce(
          (combinedOptions, cmd) => Object.assign(combinedOptions, cmd.opts()),
          {}
        );
      }
      /**
       * Display error message and exit (or call exitOverride).
       *
       * @param {string} message
       * @param {Object} [errorOptions]
       * @param {string} [errorOptions.code] - an id string representing the error
       * @param {number} [errorOptions.exitCode] - used with process.exit
       */
      error(message, errorOptions) {
        this._outputConfiguration.outputError(`${message}
`, this._outputConfiguration.writeErr);
        if (typeof this._showHelpAfterError === "string") {
          this._outputConfiguration.writeErr(`${this._showHelpAfterError}
`);
        } else if (this._showHelpAfterError) {
          this._outputConfiguration.writeErr("\n");
          this.outputHelp({ error: true });
        }
        const config2 = errorOptions || {};
        const exitCode = config2.exitCode || 1;
        const code = config2.code || "commander.error";
        this._exit(exitCode, code, message);
      }
      /**
       * Apply any option related environment variables, if option does
       * not have a value from cli or client code.
       *
       * @api private
       */
      _parseOptionsEnv() {
        this.options.forEach((option) => {
          if (option.envVar && option.envVar in process4.env) {
            const optionKey = option.attributeName();
            if (this.getOptionValue(optionKey) === void 0 || ["default", "config", "env"].includes(this.getOptionValueSource(optionKey))) {
              if (option.required || option.optional) {
                this.emit(`optionEnv:${option.name()}`, process4.env[option.envVar]);
              } else {
                this.emit(`optionEnv:${option.name()}`);
              }
            }
          }
        });
      }
      /**
       * Apply any implied option values, if option is undefined or default value.
       *
       * @api private
       */
      _parseOptionsImplied() {
        const dualHelper = new DualOptions(this.options);
        const hasCustomOptionValue = (optionKey) => {
          return this.getOptionValue(optionKey) !== void 0 && !["default", "implied"].includes(this.getOptionValueSource(optionKey));
        };
        this.options.filter((option) => option.implied !== void 0 && hasCustomOptionValue(option.attributeName()) && dualHelper.valueFromOption(this.getOptionValue(option.attributeName()), option)).forEach((option) => {
          Object.keys(option.implied).filter((impliedKey) => !hasCustomOptionValue(impliedKey)).forEach((impliedKey) => {
            this.setOptionValueWithSource(impliedKey, option.implied[impliedKey], "implied");
          });
        });
      }
      /**
       * Argument `name` is missing.
       *
       * @param {string} name
       * @api private
       */
      missingArgument(name) {
        const message = `error: missing required argument '${name}'`;
        this.error(message, { code: "commander.missingArgument" });
      }
      /**
       * `Option` is missing an argument.
       *
       * @param {Option} option
       * @api private
       */
      optionMissingArgument(option) {
        const message = `error: option '${option.flags}' argument missing`;
        this.error(message, { code: "commander.optionMissingArgument" });
      }
      /**
       * `Option` does not have a value, and is a mandatory option.
       *
       * @param {Option} option
       * @api private
       */
      missingMandatoryOptionValue(option) {
        const message = `error: required option '${option.flags}' not specified`;
        this.error(message, { code: "commander.missingMandatoryOptionValue" });
      }
      /**
       * `Option` conflicts with another option.
       *
       * @param {Option} option
       * @param {Option} conflictingOption
       * @api private
       */
      _conflictingOption(option, conflictingOption) {
        const findBestOptionFromValue = (option2) => {
          const optionKey = option2.attributeName();
          const optionValue = this.getOptionValue(optionKey);
          const negativeOption = this.options.find((target) => target.negate && optionKey === target.attributeName());
          const positiveOption = this.options.find((target) => !target.negate && optionKey === target.attributeName());
          if (negativeOption && (negativeOption.presetArg === void 0 && optionValue === false || negativeOption.presetArg !== void 0 && optionValue === negativeOption.presetArg)) {
            return negativeOption;
          }
          return positiveOption || option2;
        };
        const getErrorMessage = (option2) => {
          const bestOption = findBestOptionFromValue(option2);
          const optionKey = bestOption.attributeName();
          const source = this.getOptionValueSource(optionKey);
          if (source === "env") {
            return `environment variable '${bestOption.envVar}'`;
          }
          return `option '${bestOption.flags}'`;
        };
        const message = `error: ${getErrorMessage(option)} cannot be used with ${getErrorMessage(conflictingOption)}`;
        this.error(message, { code: "commander.conflictingOption" });
      }
      /**
       * Unknown option `flag`.
       *
       * @param {string} flag
       * @api private
       */
      unknownOption(flag) {
        if (this._allowUnknownOption) return;
        let suggestion = "";
        if (flag.startsWith("--") && this._showSuggestionAfterError) {
          let candidateFlags = [];
          let command = this;
          do {
            const moreFlags = command.createHelp().visibleOptions(command).filter((option) => option.long).map((option) => option.long);
            candidateFlags = candidateFlags.concat(moreFlags);
            command = command.parent;
          } while (command && !command._enablePositionalOptions);
          suggestion = suggestSimilar(flag, candidateFlags);
        }
        const message = `error: unknown option '${flag}'${suggestion}`;
        this.error(message, { code: "commander.unknownOption" });
      }
      /**
       * Excess arguments, more than expected.
       *
       * @param {string[]} receivedArgs
       * @api private
       */
      _excessArguments(receivedArgs) {
        if (this._allowExcessArguments) return;
        const expected = this.registeredArguments.length;
        const s = expected === 1 ? "" : "s";
        const forSubcommand = this.parent ? ` for '${this.name()}'` : "";
        const message = `error: too many arguments${forSubcommand}. Expected ${expected} argument${s} but got ${receivedArgs.length}.`;
        this.error(message, { code: "commander.excessArguments" });
      }
      /**
       * Unknown command.
       *
       * @api private
       */
      unknownCommand() {
        const unknownName = this.args[0];
        let suggestion = "";
        if (this._showSuggestionAfterError) {
          const candidateNames = [];
          this.createHelp().visibleCommands(this).forEach((command) => {
            candidateNames.push(command.name());
            if (command.alias()) candidateNames.push(command.alias());
          });
          suggestion = suggestSimilar(unknownName, candidateNames);
        }
        const message = `error: unknown command '${unknownName}'${suggestion}`;
        this.error(message, { code: "commander.unknownCommand" });
      }
      /**
       * Get or set the program version.
       *
       * This method auto-registers the "-V, --version" option which will print the version number.
       *
       * You can optionally supply the flags and description to override the defaults.
       *
       * @param {string} [str]
       * @param {string} [flags]
       * @param {string} [description]
       * @return {this | string | undefined} `this` command for chaining, or version string if no arguments
       */
      version(str, flags, description) {
        if (str === void 0) return this._version;
        this._version = str;
        flags = flags || "-V, --version";
        description = description || "output the version number";
        const versionOption = this.createOption(flags, description);
        this._versionOptionName = versionOption.attributeName();
        this.options.push(versionOption);
        this.on("option:" + versionOption.name(), () => {
          this._outputConfiguration.writeOut(`${str}
`);
          this._exit(0, "commander.version", str);
        });
        return this;
      }
      /**
       * Set the description.
       *
       * @param {string} [str]
       * @param {Object} [argsDescription]
       * @return {string|Command}
       */
      description(str, argsDescription) {
        if (str === void 0 && argsDescription === void 0) return this._description;
        this._description = str;
        if (argsDescription) {
          this._argsDescription = argsDescription;
        }
        return this;
      }
      /**
       * Set the summary. Used when listed as subcommand of parent.
       *
       * @param {string} [str]
       * @return {string|Command}
       */
      summary(str) {
        if (str === void 0) return this._summary;
        this._summary = str;
        return this;
      }
      /**
       * Set an alias for the command.
       *
       * You may call more than once to add multiple aliases. Only the first alias is shown in the auto-generated help.
       *
       * @param {string} [alias]
       * @return {string|Command}
       */
      alias(alias) {
        if (alias === void 0) return this._aliases[0];
        let command = this;
        if (this.commands.length !== 0 && this.commands[this.commands.length - 1]._executableHandler) {
          command = this.commands[this.commands.length - 1];
        }
        if (alias === command._name) throw new Error("Command alias can't be the same as its name");
        command._aliases.push(alias);
        return this;
      }
      /**
       * Set aliases for the command.
       *
       * Only the first alias is shown in the auto-generated help.
       *
       * @param {string[]} [aliases]
       * @return {string[]|Command}
       */
      aliases(aliases) {
        if (aliases === void 0) return this._aliases;
        aliases.forEach((alias) => this.alias(alias));
        return this;
      }
      /**
       * Set / get the command usage `str`.
       *
       * @param {string} [str]
       * @return {String|Command}
       */
      usage(str) {
        if (str === void 0) {
          if (this._usage) return this._usage;
          const args = this.registeredArguments.map((arg) => {
            return humanReadableArgName(arg);
          });
          return [].concat(
            this.options.length || this._hasHelpOption ? "[options]" : [],
            this.commands.length ? "[command]" : [],
            this.registeredArguments.length ? args : []
          ).join(" ");
        }
        this._usage = str;
        return this;
      }
      /**
       * Get or set the name of the command.
       *
       * @param {string} [str]
       * @return {string|Command}
       */
      name(str) {
        if (str === void 0) return this._name;
        this._name = str;
        return this;
      }
      /**
       * Set the name of the command from script filename, such as process.argv[1],
       * or require.main.filename, or __filename.
       *
       * (Used internally and public although not documented in README.)
       *
       * @example
       * program.nameFromFilename(require.main.filename);
       *
       * @param {string} filename
       * @return {Command}
       */
      nameFromFilename(filename) {
        this._name = path12.basename(filename, path12.extname(filename));
        return this;
      }
      /**
       * Get or set the directory for searching for executable subcommands of this command.
       *
       * @example
       * program.executableDir(__dirname);
       * // or
       * program.executableDir('subcommands');
       *
       * @param {string} [path]
       * @return {string|null|Command}
       */
      executableDir(path13) {
        if (path13 === void 0) return this._executableDir;
        this._executableDir = path13;
        return this;
      }
      /**
       * Return program help documentation.
       *
       * @param {{ error: boolean }} [contextOptions] - pass {error:true} to wrap for stderr instead of stdout
       * @return {string}
       */
      helpInformation(contextOptions) {
        const helper = this.createHelp();
        if (helper.helpWidth === void 0) {
          helper.helpWidth = contextOptions && contextOptions.error ? this._outputConfiguration.getErrHelpWidth() : this._outputConfiguration.getOutHelpWidth();
        }
        return helper.formatHelp(this, helper);
      }
      /**
       * @api private
       */
      _getHelpContext(contextOptions) {
        contextOptions = contextOptions || {};
        const context = { error: !!contextOptions.error };
        let write;
        if (context.error) {
          write = (arg) => this._outputConfiguration.writeErr(arg);
        } else {
          write = (arg) => this._outputConfiguration.writeOut(arg);
        }
        context.write = contextOptions.write || write;
        context.command = this;
        return context;
      }
      /**
       * Output help information for this command.
       *
       * Outputs built-in help, and custom text added using `.addHelpText()`.
       *
       * @param {{ error: boolean } | Function} [contextOptions] - pass {error:true} to write to stderr instead of stdout
       */
      outputHelp(contextOptions) {
        let deprecatedCallback;
        if (typeof contextOptions === "function") {
          deprecatedCallback = contextOptions;
          contextOptions = void 0;
        }
        const context = this._getHelpContext(contextOptions);
        this._getCommandAndAncestors().reverse().forEach((command) => command.emit("beforeAllHelp", context));
        this.emit("beforeHelp", context);
        let helpInformation = this.helpInformation(context);
        if (deprecatedCallback) {
          helpInformation = deprecatedCallback(helpInformation);
          if (typeof helpInformation !== "string" && !Buffer.isBuffer(helpInformation)) {
            throw new Error("outputHelp callback must return a string or a Buffer");
          }
        }
        context.write(helpInformation);
        if (this._helpLongFlag) {
          this.emit(this._helpLongFlag);
        }
        this.emit("afterHelp", context);
        this._getCommandAndAncestors().forEach((command) => command.emit("afterAllHelp", context));
      }
      /**
       * You can pass in flags and a description to override the help
       * flags and help description for your command. Pass in false to
       * disable the built-in help option.
       *
       * @param {string | boolean} [flags]
       * @param {string} [description]
       * @return {Command} `this` command for chaining
       */
      helpOption(flags, description) {
        if (typeof flags === "boolean") {
          this._hasHelpOption = flags;
          return this;
        }
        this._helpFlags = flags || this._helpFlags;
        this._helpDescription = description || this._helpDescription;
        const helpFlags = splitOptionFlags(this._helpFlags);
        this._helpShortFlag = helpFlags.shortFlag;
        this._helpLongFlag = helpFlags.longFlag;
        return this;
      }
      /**
       * Output help information and exit.
       *
       * Outputs built-in help, and custom text added using `.addHelpText()`.
       *
       * @param {{ error: boolean }} [contextOptions] - pass {error:true} to write to stderr instead of stdout
       */
      help(contextOptions) {
        this.outputHelp(contextOptions);
        let exitCode = process4.exitCode || 0;
        if (exitCode === 0 && contextOptions && typeof contextOptions !== "function" && contextOptions.error) {
          exitCode = 1;
        }
        this._exit(exitCode, "commander.help", "(outputHelp)");
      }
      /**
       * Add additional text to be displayed with the built-in help.
       *
       * Position is 'before' or 'after' to affect just this command,
       * and 'beforeAll' or 'afterAll' to affect this command and all its subcommands.
       *
       * @param {string} position - before or after built-in help
       * @param {string | Function} text - string to add, or a function returning a string
       * @return {Command} `this` command for chaining
       */
      addHelpText(position, text) {
        const allowedValues = ["beforeAll", "before", "after", "afterAll"];
        if (!allowedValues.includes(position)) {
          throw new Error(`Unexpected value for position to addHelpText.
Expecting one of '${allowedValues.join("', '")}'`);
        }
        const helpEvent = `${position}Help`;
        this.on(helpEvent, (context) => {
          let helpStr;
          if (typeof text === "function") {
            helpStr = text({ error: context.error, command: context.command });
          } else {
            helpStr = text;
          }
          if (helpStr) {
            context.write(`${helpStr}
`);
          }
        });
        return this;
      }
    };
    function outputHelpIfRequested(cmd, args) {
      const helpOption = cmd._hasHelpOption && args.find((arg) => arg === cmd._helpLongFlag || arg === cmd._helpShortFlag);
      if (helpOption) {
        cmd.outputHelp();
        cmd._exit(0, "commander.helpDisplayed", "(outputHelp)");
      }
    }
    function incrementNodeInspectorPort(args) {
      return args.map((arg) => {
        if (!arg.startsWith("--inspect")) {
          return arg;
        }
        let debugOption;
        let debugHost = "127.0.0.1";
        let debugPort = "9229";
        let match;
        if ((match = arg.match(/^(--inspect(-brk)?)$/)) !== null) {
          debugOption = match[1];
        } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+)$/)) !== null) {
          debugOption = match[1];
          if (/^\d+$/.test(match[3])) {
            debugPort = match[3];
          } else {
            debugHost = match[3];
          }
        } else if ((match = arg.match(/^(--inspect(-brk|-port)?)=([^:]+):(\d+)$/)) !== null) {
          debugOption = match[1];
          debugHost = match[3];
          debugPort = match[4];
        }
        if (debugOption && debugPort !== "0") {
          return `${debugOption}=${debugHost}:${parseInt(debugPort) + 1}`;
        }
        return arg;
      });
    }
    exports2.Command = Command2;
  }
});

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/index.js
var require_commander = __commonJS({
  "node_modules/.pnpm/commander@11.1.0/node_modules/commander/index.js"(exports2, module2) {
    init_cjs_shims();
    var { Argument: Argument2 } = require_argument();
    var { Command: Command2 } = require_command();
    var { CommanderError: CommanderError2, InvalidArgumentError: InvalidArgumentError2 } = require_error();
    var { Help: Help2 } = require_help();
    var { Option: Option2 } = require_option();
    exports2 = module2.exports = new Command2();
    exports2.program = exports2;
    exports2.Command = Command2;
    exports2.Option = Option2;
    exports2.Argument = Argument2;
    exports2.Help = Help2;
    exports2.CommanderError = CommanderError2;
    exports2.InvalidArgumentError = InvalidArgumentError2;
    exports2.InvalidOptionArgumentError = InvalidArgumentError2;
  }
});

// node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/vendor/ansi-styles/index.js
function assembleStyles() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles, "codes", {
    value: codes,
    enumerable: false
  });
  styles.color.close = "\x1B[39m";
  styles.bgColor.close = "\x1B[49m";
  styles.color.ansi = wrapAnsi16();
  styles.color.ansi256 = wrapAnsi256();
  styles.color.ansi16m = wrapAnsi16m();
  styles.bgColor.ansi = wrapAnsi16(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi256 = wrapAnsi256(ANSI_BACKGROUND_OFFSET);
  styles.bgColor.ansi16m = wrapAnsi16m(ANSI_BACKGROUND_OFFSET);
  Object.defineProperties(styles, {
    rgbToAnsi256: {
      value(red, green, blue) {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value(hex) {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer >> 16 & 255,
          integer >> 8 & 255,
          integer & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: (hex) => styles.rgbToAnsi256(...styles.hexToRgb(hex)),
      enumerable: false
    },
    ansi256ToAnsi: {
      value(code) {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: (red, green, blue) => styles.ansi256ToAnsi(styles.rgbToAnsi256(red, green, blue)),
      enumerable: false
    },
    hexToAnsi: {
      value: (hex) => styles.ansi256ToAnsi(styles.hexToAnsi256(hex)),
      enumerable: false
    }
  });
  return styles;
}
var ANSI_BACKGROUND_OFFSET, wrapAnsi16, wrapAnsi256, wrapAnsi16m, styles, modifierNames, foregroundColorNames, backgroundColorNames, colorNames, ansiStyles, ansi_styles_default;
var init_ansi_styles = __esm({
  "node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/vendor/ansi-styles/index.js"() {
    init_cjs_shims();
    ANSI_BACKGROUND_OFFSET = 10;
    wrapAnsi16 = (offset = 0) => (code) => `\x1B[${code + offset}m`;
    wrapAnsi256 = (offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`;
    wrapAnsi16m = (offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`;
    styles = {
      modifier: {
        reset: [0, 0],
        // 21 isn't widely supported and 22 does the same thing
        bold: [1, 22],
        dim: [2, 22],
        italic: [3, 23],
        underline: [4, 24],
        overline: [53, 55],
        inverse: [7, 27],
        hidden: [8, 28],
        strikethrough: [9, 29]
      },
      color: {
        black: [30, 39],
        red: [31, 39],
        green: [32, 39],
        yellow: [33, 39],
        blue: [34, 39],
        magenta: [35, 39],
        cyan: [36, 39],
        white: [37, 39],
        // Bright color
        blackBright: [90, 39],
        gray: [90, 39],
        // Alias of `blackBright`
        grey: [90, 39],
        // Alias of `blackBright`
        redBright: [91, 39],
        greenBright: [92, 39],
        yellowBright: [93, 39],
        blueBright: [94, 39],
        magentaBright: [95, 39],
        cyanBright: [96, 39],
        whiteBright: [97, 39]
      },
      bgColor: {
        bgBlack: [40, 49],
        bgRed: [41, 49],
        bgGreen: [42, 49],
        bgYellow: [43, 49],
        bgBlue: [44, 49],
        bgMagenta: [45, 49],
        bgCyan: [46, 49],
        bgWhite: [47, 49],
        // Bright color
        bgBlackBright: [100, 49],
        bgGray: [100, 49],
        // Alias of `bgBlackBright`
        bgGrey: [100, 49],
        // Alias of `bgBlackBright`
        bgRedBright: [101, 49],
        bgGreenBright: [102, 49],
        bgYellowBright: [103, 49],
        bgBlueBright: [104, 49],
        bgMagentaBright: [105, 49],
        bgCyanBright: [106, 49],
        bgWhiteBright: [107, 49]
      }
    };
    modifierNames = Object.keys(styles.modifier);
    foregroundColorNames = Object.keys(styles.color);
    backgroundColorNames = Object.keys(styles.bgColor);
    colorNames = [...foregroundColorNames, ...backgroundColorNames];
    ansiStyles = assembleStyles();
    ansi_styles_default = ansiStyles;
  }
});

// node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/vendor/supports-color/index.js
function hasFlag(flag, argv = globalThis.Deno ? globalThis.Deno.args : import_node_process.default.argv) {
  const prefix = flag.startsWith("-") ? "" : flag.length === 1 ? "-" : "--";
  const position = argv.indexOf(prefix + flag);
  const terminatorPosition = argv.indexOf("--");
  return position !== -1 && (terminatorPosition === -1 || position < terminatorPosition);
}
function envForceColor() {
  if ("FORCE_COLOR" in env2) {
    if (env2.FORCE_COLOR === "true") {
      return 1;
    }
    if (env2.FORCE_COLOR === "false") {
      return 0;
    }
    return env2.FORCE_COLOR.length === 0 ? 1 : Math.min(Number.parseInt(env2.FORCE_COLOR, 10), 3);
  }
}
function translateLevel(level) {
  if (level === 0) {
    return false;
  }
  return {
    level,
    hasBasic: true,
    has256: level >= 2,
    has16m: level >= 3
  };
}
function _supportsColor(haveStream, { streamIsTTY, sniffFlags = true } = {}) {
  const noFlagForceColor = envForceColor();
  if (noFlagForceColor !== void 0) {
    flagForceColor = noFlagForceColor;
  }
  const forceColor = sniffFlags ? flagForceColor : noFlagForceColor;
  if (forceColor === 0) {
    return 0;
  }
  if (sniffFlags) {
    if (hasFlag("color=16m") || hasFlag("color=full") || hasFlag("color=truecolor")) {
      return 3;
    }
    if (hasFlag("color=256")) {
      return 2;
    }
  }
  if ("TF_BUILD" in env2 && "AGENT_NAME" in env2) {
    return 1;
  }
  if (haveStream && !streamIsTTY && forceColor === void 0) {
    return 0;
  }
  const min = forceColor || 0;
  if (env2.TERM === "dumb") {
    return min;
  }
  if (import_node_process.default.platform === "win32") {
    const osRelease = import_node_os.default.release().split(".");
    if (Number(osRelease[0]) >= 10 && Number(osRelease[2]) >= 10586) {
      return Number(osRelease[2]) >= 14931 ? 3 : 2;
    }
    return 1;
  }
  if ("CI" in env2) {
    if (["GITHUB_ACTIONS", "GITEA_ACTIONS", "CIRCLECI"].some((key) => key in env2)) {
      return 3;
    }
    if (["TRAVIS", "APPVEYOR", "GITLAB_CI", "BUILDKITE", "DRONE"].some((sign) => sign in env2) || env2.CI_NAME === "codeship") {
      return 1;
    }
    return min;
  }
  if ("TEAMCITY_VERSION" in env2) {
    return /^(9\.(0*[1-9]\d*)\.|\d{2,}\.)/.test(env2.TEAMCITY_VERSION) ? 1 : 0;
  }
  if (env2.COLORTERM === "truecolor") {
    return 3;
  }
  if (env2.TERM === "xterm-kitty") {
    return 3;
  }
  if ("TERM_PROGRAM" in env2) {
    const version = Number.parseInt((env2.TERM_PROGRAM_VERSION || "").split(".")[0], 10);
    switch (env2.TERM_PROGRAM) {
      case "iTerm.app": {
        return version >= 3 ? 3 : 2;
      }
      case "Apple_Terminal": {
        return 2;
      }
    }
  }
  if (/-256(color)?$/i.test(env2.TERM)) {
    return 2;
  }
  if (/^screen|^xterm|^vt100|^vt220|^rxvt|color|ansi|cygwin|linux/i.test(env2.TERM)) {
    return 1;
  }
  if ("COLORTERM" in env2) {
    return 1;
  }
  return min;
}
function createSupportsColor(stream, options = {}) {
  const level = _supportsColor(stream, {
    streamIsTTY: stream && stream.isTTY,
    ...options
  });
  return translateLevel(level);
}
var import_node_process, import_node_os, import_node_tty, env2, flagForceColor, supportsColor, supports_color_default;
var init_supports_color = __esm({
  "node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/vendor/supports-color/index.js"() {
    init_cjs_shims();
    import_node_process = __toESM(require("process"), 1);
    import_node_os = __toESM(require("os"), 1);
    import_node_tty = __toESM(require("tty"), 1);
    ({ env: env2 } = import_node_process.default);
    if (hasFlag("no-color") || hasFlag("no-colors") || hasFlag("color=false") || hasFlag("color=never")) {
      flagForceColor = 0;
    } else if (hasFlag("color") || hasFlag("colors") || hasFlag("color=true") || hasFlag("color=always")) {
      flagForceColor = 1;
    }
    supportsColor = {
      stdout: createSupportsColor({ isTTY: import_node_tty.default.isatty(1) }),
      stderr: createSupportsColor({ isTTY: import_node_tty.default.isatty(2) })
    };
    supports_color_default = supportsColor;
  }
});

// node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/utilities.js
function stringReplaceAll(string, substring, replacer) {
  let index = string.indexOf(substring);
  if (index === -1) {
    return string;
  }
  const substringLength = substring.length;
  let endIndex = 0;
  let returnValue = "";
  do {
    returnValue += string.slice(endIndex, index) + substring + replacer;
    endIndex = index + substringLength;
    index = string.indexOf(substring, endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
function stringEncaseCRLFWithFirstIndex(string, prefix, postfix, index) {
  let endIndex = 0;
  let returnValue = "";
  do {
    const gotCR = string[index - 1] === "\r";
    returnValue += string.slice(endIndex, gotCR ? index - 1 : index) + prefix + (gotCR ? "\r\n" : "\n") + postfix;
    endIndex = index + 1;
    index = string.indexOf("\n", endIndex);
  } while (index !== -1);
  returnValue += string.slice(endIndex);
  return returnValue;
}
var init_utilities = __esm({
  "node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/utilities.js"() {
    init_cjs_shims();
  }
});

// node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/index.js
function createChalk(options) {
  return chalkFactory(options);
}
var stdoutColor, stderrColor, GENERATOR, STYLER, IS_EMPTY, levelMapping, styles2, applyOptions, chalkFactory, getModelAnsi, usedModels, proto, createStyler, createBuilder, applyStyle, chalk, chalkStderr, source_default;
var init_source = __esm({
  "node_modules/.pnpm/chalk@5.4.1/node_modules/chalk/source/index.js"() {
    init_cjs_shims();
    init_ansi_styles();
    init_supports_color();
    init_utilities();
    ({ stdout: stdoutColor, stderr: stderrColor } = supports_color_default);
    GENERATOR = Symbol("GENERATOR");
    STYLER = Symbol("STYLER");
    IS_EMPTY = Symbol("IS_EMPTY");
    levelMapping = [
      "ansi",
      "ansi",
      "ansi256",
      "ansi16m"
    ];
    styles2 = /* @__PURE__ */ Object.create(null);
    applyOptions = (object, options = {}) => {
      if (options.level && !(Number.isInteger(options.level) && options.level >= 0 && options.level <= 3)) {
        throw new Error("The `level` option should be an integer from 0 to 3");
      }
      const colorLevel = stdoutColor ? stdoutColor.level : 0;
      object.level = options.level === void 0 ? colorLevel : options.level;
    };
    chalkFactory = (options) => {
      const chalk2 = (...strings) => strings.join(" ");
      applyOptions(chalk2, options);
      Object.setPrototypeOf(chalk2, createChalk.prototype);
      return chalk2;
    };
    Object.setPrototypeOf(createChalk.prototype, Function.prototype);
    for (const [styleName, style] of Object.entries(ansi_styles_default)) {
      styles2[styleName] = {
        get() {
          const builder = createBuilder(this, createStyler(style.open, style.close, this[STYLER]), this[IS_EMPTY]);
          Object.defineProperty(this, styleName, { value: builder });
          return builder;
        }
      };
    }
    styles2.visible = {
      get() {
        const builder = createBuilder(this, this[STYLER], true);
        Object.defineProperty(this, "visible", { value: builder });
        return builder;
      }
    };
    getModelAnsi = (model, level, type, ...arguments_) => {
      if (model === "rgb") {
        if (level === "ansi16m") {
          return ansi_styles_default[type].ansi16m(...arguments_);
        }
        if (level === "ansi256") {
          return ansi_styles_default[type].ansi256(ansi_styles_default.rgbToAnsi256(...arguments_));
        }
        return ansi_styles_default[type].ansi(ansi_styles_default.rgbToAnsi(...arguments_));
      }
      if (model === "hex") {
        return getModelAnsi("rgb", level, type, ...ansi_styles_default.hexToRgb(...arguments_));
      }
      return ansi_styles_default[type][model](...arguments_);
    };
    usedModels = ["rgb", "hex", "ansi256"];
    for (const model of usedModels) {
      styles2[model] = {
        get() {
          const { level } = this;
          return function(...arguments_) {
            const styler = createStyler(getModelAnsi(model, levelMapping[level], "color", ...arguments_), ansi_styles_default.color.close, this[STYLER]);
            return createBuilder(this, styler, this[IS_EMPTY]);
          };
        }
      };
      const bgModel = "bg" + model[0].toUpperCase() + model.slice(1);
      styles2[bgModel] = {
        get() {
          const { level } = this;
          return function(...arguments_) {
            const styler = createStyler(getModelAnsi(model, levelMapping[level], "bgColor", ...arguments_), ansi_styles_default.bgColor.close, this[STYLER]);
            return createBuilder(this, styler, this[IS_EMPTY]);
          };
        }
      };
    }
    proto = Object.defineProperties(() => {
    }, {
      ...styles2,
      level: {
        enumerable: true,
        get() {
          return this[GENERATOR].level;
        },
        set(level) {
          this[GENERATOR].level = level;
        }
      }
    });
    createStyler = (open, close, parent) => {
      let openAll;
      let closeAll;
      if (parent === void 0) {
        openAll = open;
        closeAll = close;
      } else {
        openAll = parent.openAll + open;
        closeAll = close + parent.closeAll;
      }
      return {
        open,
        close,
        openAll,
        closeAll,
        parent
      };
    };
    createBuilder = (self, _styler, _isEmpty) => {
      const builder = (...arguments_) => applyStyle(builder, arguments_.length === 1 ? "" + arguments_[0] : arguments_.join(" "));
      Object.setPrototypeOf(builder, proto);
      builder[GENERATOR] = self;
      builder[STYLER] = _styler;
      builder[IS_EMPTY] = _isEmpty;
      return builder;
    };
    applyStyle = (self, string) => {
      if (self.level <= 0 || !string) {
        return self[IS_EMPTY] ? "" : string;
      }
      let styler = self[STYLER];
      if (styler === void 0) {
        return string;
      }
      const { openAll, closeAll } = styler;
      if (string.includes("\x1B")) {
        while (styler !== void 0) {
          string = stringReplaceAll(string, styler.close, styler.open);
          styler = styler.parent;
        }
      }
      const lfIndex = string.indexOf("\n");
      if (lfIndex !== -1) {
        string = stringEncaseCRLFWithFirstIndex(string, closeAll, openAll, lfIndex);
      }
      return openAll + string + closeAll;
    };
    Object.defineProperties(createChalk.prototype, styles2);
    chalk = createChalk();
    chalkStderr = createChalk({ level: stderrColor ? stderrColor.level : 0 });
    source_default = chalk;
  }
});

// node_modules/.pnpm/ansi-regex@6.1.0/node_modules/ansi-regex/index.js
function ansiRegex({ onlyFirst = false } = {}) {
  const ST = "(?:\\u0007|\\u001B\\u005C|\\u009C)";
  const pattern = [
    `[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?${ST})`,
    "(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]))"
  ].join("|");
  return new RegExp(pattern, onlyFirst ? void 0 : "g");
}
var init_ansi_regex = __esm({
  "node_modules/.pnpm/ansi-regex@6.1.0/node_modules/ansi-regex/index.js"() {
    init_cjs_shims();
  }
});

// node_modules/.pnpm/strip-ansi@7.1.0/node_modules/strip-ansi/index.js
function stripAnsi(string) {
  if (typeof string !== "string") {
    throw new TypeError(`Expected a \`string\`, got \`${typeof string}\``);
  }
  return string.replace(regex, "");
}
var regex;
var init_strip_ansi = __esm({
  "node_modules/.pnpm/strip-ansi@7.1.0/node_modules/strip-ansi/index.js"() {
    init_cjs_shims();
    init_ansi_regex();
    regex = ansiRegex();
  }
});

// node_modules/.pnpm/get-east-asian-width@1.3.0/node_modules/get-east-asian-width/lookup.js
function isAmbiguous(x) {
  return x === 161 || x === 164 || x === 167 || x === 168 || x === 170 || x === 173 || x === 174 || x >= 176 && x <= 180 || x >= 182 && x <= 186 || x >= 188 && x <= 191 || x === 198 || x === 208 || x === 215 || x === 216 || x >= 222 && x <= 225 || x === 230 || x >= 232 && x <= 234 || x === 236 || x === 237 || x === 240 || x === 242 || x === 243 || x >= 247 && x <= 250 || x === 252 || x === 254 || x === 257 || x === 273 || x === 275 || x === 283 || x === 294 || x === 295 || x === 299 || x >= 305 && x <= 307 || x === 312 || x >= 319 && x <= 322 || x === 324 || x >= 328 && x <= 331 || x === 333 || x === 338 || x === 339 || x === 358 || x === 359 || x === 363 || x === 462 || x === 464 || x === 466 || x === 468 || x === 470 || x === 472 || x === 474 || x === 476 || x === 593 || x === 609 || x === 708 || x === 711 || x >= 713 && x <= 715 || x === 717 || x === 720 || x >= 728 && x <= 731 || x === 733 || x === 735 || x >= 768 && x <= 879 || x >= 913 && x <= 929 || x >= 931 && x <= 937 || x >= 945 && x <= 961 || x >= 963 && x <= 969 || x === 1025 || x >= 1040 && x <= 1103 || x === 1105 || x === 8208 || x >= 8211 && x <= 8214 || x === 8216 || x === 8217 || x === 8220 || x === 8221 || x >= 8224 && x <= 8226 || x >= 8228 && x <= 8231 || x === 8240 || x === 8242 || x === 8243 || x === 8245 || x === 8251 || x === 8254 || x === 8308 || x === 8319 || x >= 8321 && x <= 8324 || x === 8364 || x === 8451 || x === 8453 || x === 8457 || x === 8467 || x === 8470 || x === 8481 || x === 8482 || x === 8486 || x === 8491 || x === 8531 || x === 8532 || x >= 8539 && x <= 8542 || x >= 8544 && x <= 8555 || x >= 8560 && x <= 8569 || x === 8585 || x >= 8592 && x <= 8601 || x === 8632 || x === 8633 || x === 8658 || x === 8660 || x === 8679 || x === 8704 || x === 8706 || x === 8707 || x === 8711 || x === 8712 || x === 8715 || x === 8719 || x === 8721 || x === 8725 || x === 8730 || x >= 8733 && x <= 8736 || x === 8739 || x === 8741 || x >= 8743 && x <= 8748 || x === 8750 || x >= 8756 && x <= 8759 || x === 8764 || x === 8765 || x === 8776 || x === 8780 || x === 8786 || x === 8800 || x === 8801 || x >= 8804 && x <= 8807 || x === 8810 || x === 8811 || x === 8814 || x === 8815 || x === 8834 || x === 8835 || x === 8838 || x === 8839 || x === 8853 || x === 8857 || x === 8869 || x === 8895 || x === 8978 || x >= 9312 && x <= 9449 || x >= 9451 && x <= 9547 || x >= 9552 && x <= 9587 || x >= 9600 && x <= 9615 || x >= 9618 && x <= 9621 || x === 9632 || x === 9633 || x >= 9635 && x <= 9641 || x === 9650 || x === 9651 || x === 9654 || x === 9655 || x === 9660 || x === 9661 || x === 9664 || x === 9665 || x >= 9670 && x <= 9672 || x === 9675 || x >= 9678 && x <= 9681 || x >= 9698 && x <= 9701 || x === 9711 || x === 9733 || x === 9734 || x === 9737 || x === 9742 || x === 9743 || x === 9756 || x === 9758 || x === 9792 || x === 9794 || x === 9824 || x === 9825 || x >= 9827 && x <= 9829 || x >= 9831 && x <= 9834 || x === 9836 || x === 9837 || x === 9839 || x === 9886 || x === 9887 || x === 9919 || x >= 9926 && x <= 9933 || x >= 9935 && x <= 9939 || x >= 9941 && x <= 9953 || x === 9955 || x === 9960 || x === 9961 || x >= 9963 && x <= 9969 || x === 9972 || x >= 9974 && x <= 9977 || x === 9979 || x === 9980 || x === 9982 || x === 9983 || x === 10045 || x >= 10102 && x <= 10111 || x >= 11094 && x <= 11097 || x >= 12872 && x <= 12879 || x >= 57344 && x <= 63743 || x >= 65024 && x <= 65039 || x === 65533 || x >= 127232 && x <= 127242 || x >= 127248 && x <= 127277 || x >= 127280 && x <= 127337 || x >= 127344 && x <= 127373 || x === 127375 || x === 127376 || x >= 127387 && x <= 127404 || x >= 917760 && x <= 917999 || x >= 983040 && x <= 1048573 || x >= 1048576 && x <= 1114109;
}
function isFullWidth(x) {
  return x === 12288 || x >= 65281 && x <= 65376 || x >= 65504 && x <= 65510;
}
function isWide(x) {
  return x >= 4352 && x <= 4447 || x === 8986 || x === 8987 || x === 9001 || x === 9002 || x >= 9193 && x <= 9196 || x === 9200 || x === 9203 || x === 9725 || x === 9726 || x === 9748 || x === 9749 || x >= 9776 && x <= 9783 || x >= 9800 && x <= 9811 || x === 9855 || x >= 9866 && x <= 9871 || x === 9875 || x === 9889 || x === 9898 || x === 9899 || x === 9917 || x === 9918 || x === 9924 || x === 9925 || x === 9934 || x === 9940 || x === 9962 || x === 9970 || x === 9971 || x === 9973 || x === 9978 || x === 9981 || x === 9989 || x === 9994 || x === 9995 || x === 10024 || x === 10060 || x === 10062 || x >= 10067 && x <= 10069 || x === 10071 || x >= 10133 && x <= 10135 || x === 10160 || x === 10175 || x === 11035 || x === 11036 || x === 11088 || x === 11093 || x >= 11904 && x <= 11929 || x >= 11931 && x <= 12019 || x >= 12032 && x <= 12245 || x >= 12272 && x <= 12287 || x >= 12289 && x <= 12350 || x >= 12353 && x <= 12438 || x >= 12441 && x <= 12543 || x >= 12549 && x <= 12591 || x >= 12593 && x <= 12686 || x >= 12688 && x <= 12773 || x >= 12783 && x <= 12830 || x >= 12832 && x <= 12871 || x >= 12880 && x <= 42124 || x >= 42128 && x <= 42182 || x >= 43360 && x <= 43388 || x >= 44032 && x <= 55203 || x >= 63744 && x <= 64255 || x >= 65040 && x <= 65049 || x >= 65072 && x <= 65106 || x >= 65108 && x <= 65126 || x >= 65128 && x <= 65131 || x >= 94176 && x <= 94180 || x === 94192 || x === 94193 || x >= 94208 && x <= 100343 || x >= 100352 && x <= 101589 || x >= 101631 && x <= 101640 || x >= 110576 && x <= 110579 || x >= 110581 && x <= 110587 || x === 110589 || x === 110590 || x >= 110592 && x <= 110882 || x === 110898 || x >= 110928 && x <= 110930 || x === 110933 || x >= 110948 && x <= 110951 || x >= 110960 && x <= 111355 || x >= 119552 && x <= 119638 || x >= 119648 && x <= 119670 || x === 126980 || x === 127183 || x === 127374 || x >= 127377 && x <= 127386 || x >= 127488 && x <= 127490 || x >= 127504 && x <= 127547 || x >= 127552 && x <= 127560 || x === 127568 || x === 127569 || x >= 127584 && x <= 127589 || x >= 127744 && x <= 127776 || x >= 127789 && x <= 127797 || x >= 127799 && x <= 127868 || x >= 127870 && x <= 127891 || x >= 127904 && x <= 127946 || x >= 127951 && x <= 127955 || x >= 127968 && x <= 127984 || x === 127988 || x >= 127992 && x <= 128062 || x === 128064 || x >= 128066 && x <= 128252 || x >= 128255 && x <= 128317 || x >= 128331 && x <= 128334 || x >= 128336 && x <= 128359 || x === 128378 || x === 128405 || x === 128406 || x === 128420 || x >= 128507 && x <= 128591 || x >= 128640 && x <= 128709 || x === 128716 || x >= 128720 && x <= 128722 || x >= 128725 && x <= 128727 || x >= 128732 && x <= 128735 || x === 128747 || x === 128748 || x >= 128756 && x <= 128764 || x >= 128992 && x <= 129003 || x === 129008 || x >= 129292 && x <= 129338 || x >= 129340 && x <= 129349 || x >= 129351 && x <= 129535 || x >= 129648 && x <= 129660 || x >= 129664 && x <= 129673 || x >= 129679 && x <= 129734 || x >= 129742 && x <= 129756 || x >= 129759 && x <= 129769 || x >= 129776 && x <= 129784 || x >= 131072 && x <= 196605 || x >= 196608 && x <= 262141;
}
var init_lookup = __esm({
  "node_modules/.pnpm/get-east-asian-width@1.3.0/node_modules/get-east-asian-width/lookup.js"() {
    init_cjs_shims();
  }
});

// node_modules/.pnpm/get-east-asian-width@1.3.0/node_modules/get-east-asian-width/index.js
function validate(codePoint) {
  if (!Number.isSafeInteger(codePoint)) {
    throw new TypeError(`Expected a code point, got \`${typeof codePoint}\`.`);
  }
}
function eastAsianWidth(codePoint, { ambiguousAsWide = false } = {}) {
  validate(codePoint);
  if (isFullWidth(codePoint) || isWide(codePoint) || ambiguousAsWide && isAmbiguous(codePoint)) {
    return 2;
  }
  return 1;
}
var init_get_east_asian_width = __esm({
  "node_modules/.pnpm/get-east-asian-width@1.3.0/node_modules/get-east-asian-width/index.js"() {
    init_cjs_shims();
    init_lookup();
  }
});

// node_modules/.pnpm/emoji-regex@10.4.0/node_modules/emoji-regex/index.mjs
var emoji_regex_default;
var init_emoji_regex = __esm({
  "node_modules/.pnpm/emoji-regex@10.4.0/node_modules/emoji-regex/index.mjs"() {
    init_cjs_shims();
    emoji_regex_default = () => {
      return /[#*0-9]\uFE0F?\u20E3|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26AA\u26B0\u26B1\u26BD\u26BE\u26C4\u26C8\u26CF\u26D1\u26E9\u26F0-\u26F5\u26F7\u26F8\u26FA\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2757\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B55\u3030\u303D\u3297\u3299]\uFE0F?|[\u261D\u270C\u270D](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\u270A\u270B](?:\uD83C[\uDFFB-\uDFFF])?|[\u23E9-\u23EC\u23F0\u23F3\u25FD\u2693\u26A1\u26AB\u26C5\u26CE\u26D4\u26EA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2795-\u2797\u27B0\u27BF\u2B50]|\u26D3\uFE0F?(?:\u200D\uD83D\uDCA5)?|\u26F9(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\u2764\uFE0F?(?:\u200D(?:\uD83D\uDD25|\uD83E\uDE79))?|\uD83C(?:[\uDC04\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]\uFE0F?|[\uDF85\uDFC2\uDFC7](?:\uD83C[\uDFFB-\uDFFF])?|[\uDFC4\uDFCA](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDFCB\uDFCC](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF43\uDF45-\uDF4A\uDF4C-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uDDE6\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF]|\uDDE7\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF]|\uDDE8\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF7\uDDFA-\uDDFF]|\uDDE9\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF]|\uDDEA\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA]|\uDDEB\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7]|\uDDEC\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE]|\uDDED\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA]|\uDDEE\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9]|\uDDEF\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5]|\uDDF0\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF]|\uDDF1\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE]|\uDDF2\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF]|\uDDF3\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF]|\uDDF4\uD83C\uDDF2|\uDDF5\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE]|\uDDF6\uD83C\uDDE6|\uDDF7\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC]|\uDDF8\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF]|\uDDF9\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF]|\uDDFA\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF]|\uDDFB\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA]|\uDDFC\uD83C[\uDDEB\uDDF8]|\uDDFD\uD83C\uDDF0|\uDDFE\uD83C[\uDDEA\uDDF9]|\uDDFF\uD83C[\uDDE6\uDDF2\uDDFC]|\uDF44(?:\u200D\uD83D\uDFEB)?|\uDF4B(?:\u200D\uD83D\uDFE9)?|\uDFC3(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDFF3\uFE0F?(?:\u200D(?:\u26A7\uFE0F?|\uD83C\uDF08))?|\uDFF4(?:\u200D\u2620\uFE0F?|\uDB40\uDC67\uDB40\uDC62\uDB40(?:\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDC73\uDB40\uDC63\uDB40\uDC74|\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F)?)|\uD83D(?:[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3]\uFE0F?|[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC](?:\uD83C[\uDFFB-\uDFFF])?|[\uDC6E\uDC70\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4\uDEB5](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD74\uDD90](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC25\uDC27-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE41\uDE43\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED7\uDEDC-\uDEDF\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB\uDFF0]|\uDC08(?:\u200D\u2B1B)?|\uDC15(?:\u200D\uD83E\uDDBA)?|\uDC26(?:\u200D(?:\u2B1B|\uD83D\uDD25))?|\uDC3B(?:\u200D\u2744\uFE0F?)?|\uDC41\uFE0F?(?:\u200D\uD83D\uDDE8\uFE0F?)?|\uDC68(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDC68\uDC69]\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE])))?))?|\uDC69(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?[\uDC68\uDC69]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?|\uDC69\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?))|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFE])))?))?|\uDC6F(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDD75(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDE2E(?:\u200D\uD83D\uDCA8)?|\uDE35(?:\u200D\uD83D\uDCAB)?|\uDE36(?:\u200D\uD83C\uDF2B\uFE0F?)?|\uDE42(?:\u200D[\u2194\u2195]\uFE0F?)?|\uDEB6(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?)|\uD83E(?:[\uDD0C\uDD0F\uDD18-\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5\uDEC3-\uDEC5\uDEF0\uDEF2-\uDEF8](?:\uD83C[\uDFFB-\uDFFF])?|[\uDD26\uDD35\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD\uDDCF\uDDD4\uDDD6-\uDDDD](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDDDE\uDDDF](?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD0D\uDD0E\uDD10-\uDD17\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCC\uDDD0\uDDE0-\uDDFF\uDE70-\uDE7C\uDE80-\uDE89\uDE8F-\uDEC2\uDEC6\uDECE-\uDEDC\uDEDF-\uDEE9]|\uDD3C(?:\u200D[\u2640\u2642]\uFE0F?|\uD83C[\uDFFB-\uDFFF])?|\uDDCE(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDDD1(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1|\uDDD1\u200D\uD83E\uDDD2(?:\u200D\uD83E\uDDD2)?|\uDDD2(?:\u200D\uD83E\uDDD2)?))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF])))?))?|\uDEF1(?:\uD83C(?:\uDFFB(?:\u200D\uD83E\uDEF2\uD83C[\uDFFC-\uDFFF])?|\uDFFC(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFD-\uDFFF])?|\uDFFD(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])?|\uDFFE(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFD\uDFFF])?|\uDFFF(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFE])?))?)/g;
    };
  }
});

// node_modules/.pnpm/string-width@7.2.0/node_modules/string-width/index.js
function stringWidth(string, options = {}) {
  if (typeof string !== "string" || string.length === 0) {
    return 0;
  }
  const {
    ambiguousIsNarrow = true,
    countAnsiEscapeCodes = false
  } = options;
  if (!countAnsiEscapeCodes) {
    string = stripAnsi(string);
  }
  if (string.length === 0) {
    return 0;
  }
  let width = 0;
  const eastAsianWidthOptions = { ambiguousAsWide: !ambiguousIsNarrow };
  for (const { segment: character } of segmenter.segment(string)) {
    const codePoint = character.codePointAt(0);
    if (codePoint <= 31 || codePoint >= 127 && codePoint <= 159) {
      continue;
    }
    if (codePoint >= 8203 && codePoint <= 8207 || codePoint === 65279) {
      continue;
    }
    if (codePoint >= 768 && codePoint <= 879 || codePoint >= 6832 && codePoint <= 6911 || codePoint >= 7616 && codePoint <= 7679 || codePoint >= 8400 && codePoint <= 8447 || codePoint >= 65056 && codePoint <= 65071) {
      continue;
    }
    if (codePoint >= 55296 && codePoint <= 57343) {
      continue;
    }
    if (codePoint >= 65024 && codePoint <= 65039) {
      continue;
    }
    if (defaultIgnorableCodePointRegex.test(character)) {
      continue;
    }
    if (emoji_regex_default().test(character)) {
      width += 2;
      continue;
    }
    width += eastAsianWidth(codePoint, eastAsianWidthOptions);
  }
  return width;
}
var segmenter, defaultIgnorableCodePointRegex;
var init_string_width = __esm({
  "node_modules/.pnpm/string-width@7.2.0/node_modules/string-width/index.js"() {
    init_cjs_shims();
    init_strip_ansi();
    init_get_east_asian_width();
    init_emoji_regex();
    segmenter = new Intl.Segmenter();
    defaultIgnorableCodePointRegex = new RegExp("^\\p{Default_Ignorable_Code_Point}$", "u");
  }
});

// node_modules/.pnpm/widest-line@5.0.0/node_modules/widest-line/index.js
function widestLine(string) {
  let lineWidth = 0;
  for (const line of string.split("\n")) {
    lineWidth = Math.max(lineWidth, stringWidth(line));
  }
  return lineWidth;
}
var init_widest_line = __esm({
  "node_modules/.pnpm/widest-line@5.0.0/node_modules/widest-line/index.js"() {
    init_cjs_shims();
    init_string_width();
  }
});

// node_modules/.pnpm/cli-boxes@3.0.0/node_modules/cli-boxes/boxes.json
var require_boxes = __commonJS({
  "node_modules/.pnpm/cli-boxes@3.0.0/node_modules/cli-boxes/boxes.json"(exports2, module2) {
    module2.exports = {
      single: {
        topLeft: "\u250C",
        top: "\u2500",
        topRight: "\u2510",
        right: "\u2502",
        bottomRight: "\u2518",
        bottom: "\u2500",
        bottomLeft: "\u2514",
        left: "\u2502"
      },
      double: {
        topLeft: "\u2554",
        top: "\u2550",
        topRight: "\u2557",
        right: "\u2551",
        bottomRight: "\u255D",
        bottom: "\u2550",
        bottomLeft: "\u255A",
        left: "\u2551"
      },
      round: {
        topLeft: "\u256D",
        top: "\u2500",
        topRight: "\u256E",
        right: "\u2502",
        bottomRight: "\u256F",
        bottom: "\u2500",
        bottomLeft: "\u2570",
        left: "\u2502"
      },
      bold: {
        topLeft: "\u250F",
        top: "\u2501",
        topRight: "\u2513",
        right: "\u2503",
        bottomRight: "\u251B",
        bottom: "\u2501",
        bottomLeft: "\u2517",
        left: "\u2503"
      },
      singleDouble: {
        topLeft: "\u2553",
        top: "\u2500",
        topRight: "\u2556",
        right: "\u2551",
        bottomRight: "\u255C",
        bottom: "\u2500",
        bottomLeft: "\u2559",
        left: "\u2551"
      },
      doubleSingle: {
        topLeft: "\u2552",
        top: "\u2550",
        topRight: "\u2555",
        right: "\u2502",
        bottomRight: "\u255B",
        bottom: "\u2550",
        bottomLeft: "\u2558",
        left: "\u2502"
      },
      classic: {
        topLeft: "+",
        top: "-",
        topRight: "+",
        right: "|",
        bottomRight: "+",
        bottom: "-",
        bottomLeft: "+",
        left: "|"
      },
      arrow: {
        topLeft: "\u2198",
        top: "\u2193",
        topRight: "\u2199",
        right: "\u2190",
        bottomRight: "\u2196",
        bottom: "\u2191",
        bottomLeft: "\u2197",
        left: "\u2192"
      }
    };
  }
});

// node_modules/.pnpm/cli-boxes@3.0.0/node_modules/cli-boxes/index.js
var require_cli_boxes = __commonJS({
  "node_modules/.pnpm/cli-boxes@3.0.0/node_modules/cli-boxes/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var cliBoxes2 = require_boxes();
    module2.exports = cliBoxes2;
    module2.exports.default = cliBoxes2;
  }
});

// node_modules/.pnpm/camelcase@8.0.0/node_modules/camelcase/index.js
function camelCase(input, options) {
  if (!(typeof input === "string" || Array.isArray(input))) {
    throw new TypeError("Expected the input to be `string | string[]`");
  }
  options = {
    pascalCase: false,
    preserveConsecutiveUppercase: false,
    ...options
  };
  if (Array.isArray(input)) {
    input = input.map((x) => x.trim()).filter((x) => x.length).join("-");
  } else {
    input = input.trim();
  }
  if (input.length === 0) {
    return "";
  }
  const toLowerCase = options.locale === false ? (string) => string.toLowerCase() : (string) => string.toLocaleLowerCase(options.locale);
  const toUpperCase = options.locale === false ? (string) => string.toUpperCase() : (string) => string.toLocaleUpperCase(options.locale);
  if (input.length === 1) {
    if (SEPARATORS.test(input)) {
      return "";
    }
    return options.pascalCase ? toUpperCase(input) : toLowerCase(input);
  }
  const hasUpperCase = input !== toLowerCase(input);
  if (hasUpperCase) {
    input = preserveCamelCase(input, toLowerCase, toUpperCase, options.preserveConsecutiveUppercase);
  }
  input = input.replace(LEADING_SEPARATORS, "");
  input = options.preserveConsecutiveUppercase ? preserveConsecutiveUppercase(input, toLowerCase) : toLowerCase(input);
  if (options.pascalCase) {
    input = toUpperCase(input.charAt(0)) + input.slice(1);
  }
  return postProcess(input, toUpperCase);
}
var UPPERCASE, LOWERCASE, LEADING_CAPITAL, IDENTIFIER, SEPARATORS, LEADING_SEPARATORS, SEPARATORS_AND_IDENTIFIER, NUMBERS_AND_IDENTIFIER, preserveCamelCase, preserveConsecutiveUppercase, postProcess;
var init_camelcase = __esm({
  "node_modules/.pnpm/camelcase@8.0.0/node_modules/camelcase/index.js"() {
    init_cjs_shims();
    UPPERCASE = /[\p{Lu}]/u;
    LOWERCASE = /[\p{Ll}]/u;
    LEADING_CAPITAL = /^[\p{Lu}](?![\p{Lu}])/gu;
    IDENTIFIER = /([\p{Alpha}\p{N}_]|$)/u;
    SEPARATORS = /[_.\- ]+/;
    LEADING_SEPARATORS = new RegExp("^" + SEPARATORS.source);
    SEPARATORS_AND_IDENTIFIER = new RegExp(SEPARATORS.source + IDENTIFIER.source, "gu");
    NUMBERS_AND_IDENTIFIER = new RegExp("\\d+" + IDENTIFIER.source, "gu");
    preserveCamelCase = (string, toLowerCase, toUpperCase, preserveConsecutiveUppercase2) => {
      let isLastCharLower = false;
      let isLastCharUpper = false;
      let isLastLastCharUpper = false;
      let isLastLastCharPreserved = false;
      for (let index = 0; index < string.length; index++) {
        const character = string[index];
        isLastLastCharPreserved = index > 2 ? string[index - 3] === "-" : true;
        if (isLastCharLower && UPPERCASE.test(character)) {
          string = string.slice(0, index) + "-" + string.slice(index);
          isLastCharLower = false;
          isLastLastCharUpper = isLastCharUpper;
          isLastCharUpper = true;
          index++;
        } else if (isLastCharUpper && isLastLastCharUpper && LOWERCASE.test(character) && (!isLastLastCharPreserved || preserveConsecutiveUppercase2)) {
          string = string.slice(0, index - 1) + "-" + string.slice(index - 1);
          isLastLastCharUpper = isLastCharUpper;
          isLastCharUpper = false;
          isLastCharLower = true;
        } else {
          isLastCharLower = toLowerCase(character) === character && toUpperCase(character) !== character;
          isLastLastCharUpper = isLastCharUpper;
          isLastCharUpper = toUpperCase(character) === character && toLowerCase(character) !== character;
        }
      }
      return string;
    };
    preserveConsecutiveUppercase = (input, toLowerCase) => {
      LEADING_CAPITAL.lastIndex = 0;
      return input.replaceAll(LEADING_CAPITAL, (match) => toLowerCase(match));
    };
    postProcess = (input, toUpperCase) => {
      SEPARATORS_AND_IDENTIFIER.lastIndex = 0;
      NUMBERS_AND_IDENTIFIER.lastIndex = 0;
      return input.replaceAll(NUMBERS_AND_IDENTIFIER, (match, pattern, offset) => ["_", "-"].includes(input.charAt(offset + match.length)) ? match : toUpperCase(match)).replaceAll(SEPARATORS_AND_IDENTIFIER, (_, identifier) => toUpperCase(identifier));
    };
  }
});

// node_modules/.pnpm/ansi-regex@5.0.1/node_modules/ansi-regex/index.js
var require_ansi_regex = __commonJS({
  "node_modules/.pnpm/ansi-regex@5.0.1/node_modules/ansi-regex/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    module2.exports = ({ onlyFirst = false } = {}) => {
      const pattern = [
        "[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?\\u0007)",
        "(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-ntqry=><~]))"
      ].join("|");
      return new RegExp(pattern, onlyFirst ? void 0 : "g");
    };
  }
});

// node_modules/.pnpm/strip-ansi@6.0.1/node_modules/strip-ansi/index.js
var require_strip_ansi = __commonJS({
  "node_modules/.pnpm/strip-ansi@6.0.1/node_modules/strip-ansi/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var ansiRegex2 = require_ansi_regex();
    module2.exports = (string) => typeof string === "string" ? string.replace(ansiRegex2(), "") : string;
  }
});

// node_modules/.pnpm/is-fullwidth-code-point@3.0.0/node_modules/is-fullwidth-code-point/index.js
var require_is_fullwidth_code_point = __commonJS({
  "node_modules/.pnpm/is-fullwidth-code-point@3.0.0/node_modules/is-fullwidth-code-point/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var isFullwidthCodePoint = (codePoint) => {
      if (Number.isNaN(codePoint)) {
        return false;
      }
      if (codePoint >= 4352 && (codePoint <= 4447 || // Hangul Jamo
      codePoint === 9001 || // LEFT-POINTING ANGLE BRACKET
      codePoint === 9002 || // RIGHT-POINTING ANGLE BRACKET
      // CJK Radicals Supplement .. Enclosed CJK Letters and Months
      11904 <= codePoint && codePoint <= 12871 && codePoint !== 12351 || // Enclosed CJK Letters and Months .. CJK Unified Ideographs Extension A
      12880 <= codePoint && codePoint <= 19903 || // CJK Unified Ideographs .. Yi Radicals
      19968 <= codePoint && codePoint <= 42182 || // Hangul Jamo Extended-A
      43360 <= codePoint && codePoint <= 43388 || // Hangul Syllables
      44032 <= codePoint && codePoint <= 55203 || // CJK Compatibility Ideographs
      63744 <= codePoint && codePoint <= 64255 || // Vertical Forms
      65040 <= codePoint && codePoint <= 65049 || // CJK Compatibility Forms .. Small Form Variants
      65072 <= codePoint && codePoint <= 65131 || // Halfwidth and Fullwidth Forms
      65281 <= codePoint && codePoint <= 65376 || 65504 <= codePoint && codePoint <= 65510 || // Kana Supplement
      110592 <= codePoint && codePoint <= 110593 || // Enclosed Ideographic Supplement
      127488 <= codePoint && codePoint <= 127569 || // CJK Unified Ideographs Extension B .. Tertiary Ideographic Plane
      131072 <= codePoint && codePoint <= 262141)) {
        return true;
      }
      return false;
    };
    module2.exports = isFullwidthCodePoint;
    module2.exports.default = isFullwidthCodePoint;
  }
});

// node_modules/.pnpm/emoji-regex@8.0.0/node_modules/emoji-regex/index.js
var require_emoji_regex = __commonJS({
  "node_modules/.pnpm/emoji-regex@8.0.0/node_modules/emoji-regex/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    module2.exports = function() {
      return /\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62(?:\uDB40\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDB40\uDC73\uDB40\uDC63\uDB40\uDC74|\uDB40\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F|\uD83D\uDC68(?:\uD83C\uDFFC\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68\uD83C\uDFFB|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFE])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFE\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFD])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFC])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83D\uDC68|(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D[\uDC66\uDC67])|[\u2695\u2696\u2708]\uFE0F|\uD83D[\uDC66\uDC67]|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|(?:\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708])\uFE0F|\uD83C\uDFFB\u200D(?:\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C[\uDFFB-\uDFFF])|(?:\uD83E\uDDD1\uD83C\uDFFB\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFC\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)\uD83C\uDFFB|\uD83E\uDDD1(?:\uD83C\uDFFF\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1(?:\uD83C[\uDFFB-\uDFFF])|\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1)|(?:\uD83E\uDDD1\uD83C\uDFFE\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFF\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB-\uDFFE])|(?:\uD83E\uDDD1\uD83C\uDFFC\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFD\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)(?:\uD83C[\uDFFB\uDFFC])|\uD83D\uDC69(?:\uD83C\uDFFE\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFC\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFB\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFC-\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D(?:\uD83D[\uDC68\uDC69])|\uD83D[\uDC68\uDC69])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD]))|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|(?:\uD83E\uDDD1\uD83C\uDFFD\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFE\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)(?:\uD83C[\uDFFB-\uDFFD])|\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D[\uDC66\uDC67])|(?:\uD83D\uDC41\uFE0F\u200D\uD83D\uDDE8|\uD83D\uDC69(?:\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708]|\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\u200D[\u2695\u2696\u2708])|(?:(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)\uFE0F|\uD83D\uDC6F|\uD83E[\uDD3C\uDDDE\uDDDF])\u200D[\u2640\u2642]|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uD83C[\uDFFB-\uDFFF])\u200D[\u2640\u2642]|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD6-\uDDDD])(?:(?:\uD83C[\uDFFB-\uDFFF])\u200D[\u2640\u2642]|\u200D[\u2640\u2642])|\uD83C\uDFF4\u200D\u2620)\uFE0F|\uD83D\uDC69\u200D\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|\uD83C\uDFF3\uFE0F\u200D\uD83C\uDF08|\uD83D\uDC15\u200D\uD83E\uDDBA|\uD83D\uDC69\u200D\uD83D\uDC66|\uD83D\uDC69\u200D\uD83D\uDC67|\uD83C\uDDFD\uD83C\uDDF0|\uD83C\uDDF4\uD83C\uDDF2|\uD83C\uDDF6\uD83C\uDDE6|[#\*0-9]\uFE0F\u20E3|\uD83C\uDDE7(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF])|\uD83C\uDDF9(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF])|\uD83C\uDDEA(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA])|\uD83E\uDDD1(?:\uD83C[\uDFFB-\uDFFF])|\uD83C\uDDF7(?:\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC])|\uD83D\uDC69(?:\uD83C[\uDFFB-\uDFFF])|\uD83C\uDDF2(?:\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF])|\uD83C\uDDE6(?:\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF])|\uD83C\uDDF0(?:\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF])|\uD83C\uDDED(?:\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA])|\uD83C\uDDE9(?:\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF])|\uD83C\uDDFE(?:\uD83C[\uDDEA\uDDF9])|\uD83C\uDDEC(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE])|\uD83C\uDDF8(?:\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF])|\uD83C\uDDEB(?:\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7])|\uD83C\uDDF5(?:\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE])|\uD83C\uDDFB(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA])|\uD83C\uDDF3(?:\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF])|\uD83C\uDDE8(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF5\uDDF7\uDDFA-\uDDFF])|\uD83C\uDDF1(?:\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE])|\uD83C\uDDFF(?:\uD83C[\uDDE6\uDDF2\uDDFC])|\uD83C\uDDFC(?:\uD83C[\uDDEB\uDDF8])|\uD83C\uDDFA(?:\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF])|\uD83C\uDDEE(?:\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9])|\uD83C\uDDEF(?:\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5])|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD6-\uDDDD])(?:\uD83C[\uDFFB-\uDFFF])|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uD83C[\uDFFB-\uDFFF])|(?:[\u261D\u270A-\u270D]|\uD83C[\uDF85\uDFC2\uDFC7]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC70\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDCAA\uDD74\uDD7A\uDD90\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC]|\uD83E[\uDD0F\uDD18-\uDD1C\uDD1E\uDD1F\uDD30-\uDD36\uDDB5\uDDB6\uDDBB\uDDD2-\uDDD5])(?:\uD83C[\uDFFB-\uDFFF])|(?:[\u231A\u231B\u23E9-\u23EC\u23F0\u23F3\u25FD\u25FE\u2614\u2615\u2648-\u2653\u267F\u2693\u26A1\u26AA\u26AB\u26BD\u26BE\u26C4\u26C5\u26CE\u26D4\u26EA\u26F2\u26F3\u26F5\u26FA\u26FD\u2705\u270A\u270B\u2728\u274C\u274E\u2753-\u2755\u2757\u2795-\u2797\u27B0\u27BF\u2B1B\u2B1C\u2B50\u2B55]|\uD83C[\uDC04\uDCCF\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF7C\uDF7E-\uDF93\uDFA0-\uDFCA\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF4\uDFF8-\uDFFF]|\uD83D[\uDC00-\uDC3E\uDC40\uDC42-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDD7A\uDD95\uDD96\uDDA4\uDDFB-\uDE4F\uDE80-\uDEC5\uDECC\uDED0-\uDED2\uDED5\uDEEB\uDEEC\uDEF4-\uDEFA\uDFE0-\uDFEB]|\uD83E[\uDD0D-\uDD3A\uDD3C-\uDD45\uDD47-\uDD71\uDD73-\uDD76\uDD7A-\uDDA2\uDDA5-\uDDAA\uDDAE-\uDDCA\uDDCD-\uDDFF\uDE70-\uDE73\uDE78-\uDE7A\uDE80-\uDE82\uDE90-\uDE95])|(?:[#\*0-9\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23E9-\u23F3\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB-\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u261D\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692-\u2697\u2699\u269B\u269C\u26A0\u26A1\u26AA\u26AB\u26B0\u26B1\u26BD\u26BE\u26C4\u26C5\u26C8\u26CE\u26CF\u26D1\u26D3\u26D4\u26E9\u26EA\u26F0-\u26F5\u26F7-\u26FA\u26FD\u2702\u2705\u2708-\u270D\u270F\u2712\u2714\u2716\u271D\u2721\u2728\u2733\u2734\u2744\u2747\u274C\u274E\u2753-\u2755\u2757\u2763\u2764\u2795-\u2797\u27A1\u27B0\u27BF\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B50\u2B55\u3030\u303D\u3297\u3299]|\uD83C[\uDC04\uDCCF\uDD70\uDD71\uDD7E\uDD7F\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE02\uDE1A\uDE2F\uDE32-\uDE3A\uDE50\uDE51\uDF00-\uDF21\uDF24-\uDF93\uDF96\uDF97\uDF99-\uDF9B\uDF9E-\uDFF0\uDFF3-\uDFF5\uDFF7-\uDFFF]|\uD83D[\uDC00-\uDCFD\uDCFF-\uDD3D\uDD49-\uDD4E\uDD50-\uDD67\uDD6F\uDD70\uDD73-\uDD7A\uDD87\uDD8A-\uDD8D\uDD90\uDD95\uDD96\uDDA4\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA-\uDE4F\uDE80-\uDEC5\uDECB-\uDED2\uDED5\uDEE0-\uDEE5\uDEE9\uDEEB\uDEEC\uDEF0\uDEF3-\uDEFA\uDFE0-\uDFEB]|\uD83E[\uDD0D-\uDD3A\uDD3C-\uDD45\uDD47-\uDD71\uDD73-\uDD76\uDD7A-\uDDA2\uDDA5-\uDDAA\uDDAE-\uDDCA\uDDCD-\uDDFF\uDE70-\uDE73\uDE78-\uDE7A\uDE80-\uDE82\uDE90-\uDE95])\uFE0F|(?:[\u261D\u26F9\u270A-\u270D]|\uD83C[\uDF85\uDFC2-\uDFC4\uDFC7\uDFCA-\uDFCC]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66-\uDC78\uDC7C\uDC81-\uDC83\uDC85-\uDC87\uDC8F\uDC91\uDCAA\uDD74\uDD75\uDD7A\uDD90\uDD95\uDD96\uDE45-\uDE47\uDE4B-\uDE4F\uDEA3\uDEB4-\uDEB6\uDEC0\uDECC]|\uD83E[\uDD0F\uDD18-\uDD1F\uDD26\uDD30-\uDD39\uDD3C-\uDD3E\uDDB5\uDDB6\uDDB8\uDDB9\uDDBB\uDDCD-\uDDCF\uDDD1-\uDDDD])/g;
    };
  }
});

// node_modules/.pnpm/string-width@4.2.3/node_modules/string-width/index.js
var require_string_width = __commonJS({
  "node_modules/.pnpm/string-width@4.2.3/node_modules/string-width/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var stripAnsi2 = require_strip_ansi();
    var isFullwidthCodePoint = require_is_fullwidth_code_point();
    var emojiRegex2 = require_emoji_regex();
    var stringWidth2 = (string) => {
      if (typeof string !== "string" || string.length === 0) {
        return 0;
      }
      string = stripAnsi2(string);
      if (string.length === 0) {
        return 0;
      }
      string = string.replace(emojiRegex2(), "  ");
      let width = 0;
      for (let i = 0; i < string.length; i++) {
        const code = string.codePointAt(i);
        if (code <= 31 || code >= 127 && code <= 159) {
          continue;
        }
        if (code >= 768 && code <= 879) {
          continue;
        }
        if (code > 65535) {
          i++;
        }
        width += isFullwidthCodePoint(code) ? 2 : 1;
      }
      return width;
    };
    module2.exports = stringWidth2;
    module2.exports.default = stringWidth2;
  }
});

// node_modules/.pnpm/ansi-align@3.0.1/node_modules/ansi-align/index.js
var require_ansi_align = __commonJS({
  "node_modules/.pnpm/ansi-align@3.0.1/node_modules/ansi-align/index.js"(exports2, module2) {
    "use strict";
    init_cjs_shims();
    var stringWidth2 = require_string_width();
    function ansiAlign2(text, opts) {
      if (!text) return text;
      opts = opts || {};
      const align = opts.align || "center";
      if (align === "left") return text;
      const split = opts.split || "\n";
      const pad = opts.pad || " ";
      const widthDiffFn = align !== "right" ? halfDiff : fullDiff;
      let returnString = false;
      if (!Array.isArray(text)) {
        returnString = true;
        text = String(text).split(split);
      }
      let width;
      let maxWidth = 0;
      text = text.map(function(str) {
        str = String(str);
        width = stringWidth2(str);
        maxWidth = Math.max(width, maxWidth);
        return {
          str,
          width
        };
      }).map(function(obj) {
        return new Array(widthDiffFn(maxWidth, obj.width) + 1).join(pad) + obj.str;
      });
      return returnString ? text.join(split) : text;
    }
    ansiAlign2.left = function left(text) {
      return ansiAlign2(text, { align: "left" });
    };
    ansiAlign2.center = function center(text) {
      return ansiAlign2(text, { align: "center" });
    };
    ansiAlign2.right = function right(text) {
      return ansiAlign2(text, { align: "right" });
    };
    module2.exports = ansiAlign2;
    function halfDiff(maxWidth, curWidth) {
      return Math.floor((maxWidth - curWidth) / 2);
    }
    function fullDiff(maxWidth, curWidth) {
      return maxWidth - curWidth;
    }
  }
});

// node_modules/.pnpm/ansi-styles@6.2.1/node_modules/ansi-styles/index.js
function assembleStyles2() {
  const codes = /* @__PURE__ */ new Map();
  for (const [groupName, group] of Object.entries(styles3)) {
    for (const [styleName, style] of Object.entries(group)) {
      styles3[styleName] = {
        open: `\x1B[${style[0]}m`,
        close: `\x1B[${style[1]}m`
      };
      group[styleName] = styles3[styleName];
      codes.set(style[0], style[1]);
    }
    Object.defineProperty(styles3, groupName, {
      value: group,
      enumerable: false
    });
  }
  Object.defineProperty(styles3, "codes", {
    value: codes,
    enumerable: false
  });
  styles3.color.close = "\x1B[39m";
  styles3.bgColor.close = "\x1B[49m";
  styles3.color.ansi = wrapAnsi162();
  styles3.color.ansi256 = wrapAnsi2562();
  styles3.color.ansi16m = wrapAnsi16m2();
  styles3.bgColor.ansi = wrapAnsi162(ANSI_BACKGROUND_OFFSET2);
  styles3.bgColor.ansi256 = wrapAnsi2562(ANSI_BACKGROUND_OFFSET2);
  styles3.bgColor.ansi16m = wrapAnsi16m2(ANSI_BACKGROUND_OFFSET2);
  Object.defineProperties(styles3, {
    rgbToAnsi256: {
      value: (red, green, blue) => {
        if (red === green && green === blue) {
          if (red < 8) {
            return 16;
          }
          if (red > 248) {
            return 231;
          }
          return Math.round((red - 8) / 247 * 24) + 232;
        }
        return 16 + 36 * Math.round(red / 255 * 5) + 6 * Math.round(green / 255 * 5) + Math.round(blue / 255 * 5);
      },
      enumerable: false
    },
    hexToRgb: {
      value: (hex) => {
        const matches = /[a-f\d]{6}|[a-f\d]{3}/i.exec(hex.toString(16));
        if (!matches) {
          return [0, 0, 0];
        }
        let [colorString] = matches;
        if (colorString.length === 3) {
          colorString = [...colorString].map((character) => character + character).join("");
        }
        const integer = Number.parseInt(colorString, 16);
        return [
          /* eslint-disable no-bitwise */
          integer >> 16 & 255,
          integer >> 8 & 255,
          integer & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: false
    },
    hexToAnsi256: {
      value: (hex) => styles3.rgbToAnsi256(...styles3.hexToRgb(hex)),
      enumerable: false
    },
    ansi256ToAnsi: {
      value: (code) => {
        if (code < 8) {
          return 30 + code;
        }
        if (code < 16) {
          return 90 + (code - 8);
        }
        let red;
        let green;
        let blue;
        if (code >= 232) {
          red = ((code - 232) * 10 + 8) / 255;
          green = red;
          blue = red;
        } else {
          code -= 16;
          const remainder = code % 36;
          red = Math.floor(code / 36) / 5;
          green = Math.floor(remainder / 6) / 5;
          blue = remainder % 6 / 5;
        }
        const value = Math.max(red, green, blue) * 2;
        if (value === 0) {
          return 30;
        }
        let result = 30 + (Math.round(blue) << 2 | Math.round(green) << 1 | Math.round(red));
        if (value === 2) {
          result += 60;
        }
        return result;
      },
      enumerable: false
    },
    rgbToAnsi: {
      value: (red, green, blue) => styles3.ansi256ToAnsi(styles3.rgbToAnsi256(red, green, blue)),
      enumerable: false
    },
    hexToAnsi: {
      value: (hex) => styles3.ansi256ToAnsi(styles3.hexToAnsi256(hex)),
      enumerable: false
    }
  });
  return styles3;
}
var ANSI_BACKGROUND_OFFSET2, wrapAnsi162, wrapAnsi2562, wrapAnsi16m2, styles3, modifierNames2, foregroundColorNames2, backgroundColorNames2, colorNames2, ansiStyles2, ansi_styles_default2;
var init_ansi_styles2 = __esm({
  "node_modules/.pnpm/ansi-styles@6.2.1/node_modules/ansi-styles/index.js"() {
    init_cjs_shims();
    ANSI_BACKGROUND_OFFSET2 = 10;
    wrapAnsi162 = (offset = 0) => (code) => `\x1B[${code + offset}m`;
    wrapAnsi2562 = (offset = 0) => (code) => `\x1B[${38 + offset};5;${code}m`;
    wrapAnsi16m2 = (offset = 0) => (red, green, blue) => `\x1B[${38 + offset};2;${red};${green};${blue}m`;
    styles3 = {
      modifier: {
        reset: [0, 0],
        // 21 isn't widely supported and 22 does the same thing
        bold: [1, 22],
        dim: [2, 22],
        italic: [3, 23],
        underline: [4, 24],
        overline: [53, 55],
        inverse: [7, 27],
        hidden: [8, 28],
        strikethrough: [9, 29]
      },
      color: {
        black: [30, 39],
        red: [31, 39],
        green: [32, 39],
        yellow: [33, 39],
        blue: [34, 39],
        magenta: [35, 39],
        cyan: [36, 39],
        white: [37, 39],
        // Bright color
        blackBright: [90, 39],
        gray: [90, 39],
        // Alias of `blackBright`
        grey: [90, 39],
        // Alias of `blackBright`
        redBright: [91, 39],
        greenBright: [92, 39],
        yellowBright: [93, 39],
        blueBright: [94, 39],
        magentaBright: [95, 39],
        cyanBright: [96, 39],
        whiteBright: [97, 39]
      },
      bgColor: {
        bgBlack: [40, 49],
        bgRed: [41, 49],
        bgGreen: [42, 49],
        bgYellow: [43, 49],
        bgBlue: [44, 49],
        bgMagenta: [45, 49],
        bgCyan: [46, 49],
        bgWhite: [47, 49],
        // Bright color
        bgBlackBright: [100, 49],
        bgGray: [100, 49],
        // Alias of `bgBlackBright`
        bgGrey: [100, 49],
        // Alias of `bgBlackBright`
        bgRedBright: [101, 49],
        bgGreenBright: [102, 49],
        bgYellowBright: [103, 49],
        bgBlueBright: [104, 49],
        bgMagentaBright: [105, 49],
        bgCyanBright: [106, 49],
        bgWhiteBright: [107, 49]
      }
    };
    modifierNames2 = Object.keys(styles3.modifier);
    foregroundColorNames2 = Object.keys(styles3.color);
    backgroundColorNames2 = Object.keys(styles3.bgColor);
    colorNames2 = [...foregroundColorNames2, ...backgroundColorNames2];
    ansiStyles2 = assembleStyles2();
    ansi_styles_default2 = ansiStyles2;
  }
});

// node_modules/.pnpm/wrap-ansi@9.0.0/node_modules/wrap-ansi/index.js
function wrapAnsi(string, columns, options) {
  return String(string).normalize().replaceAll("\r\n", "\n").split("\n").map((line) => exec(line, columns, options)).join("\n");
}
var ESCAPES, END_CODE, ANSI_ESCAPE_BELL, ANSI_CSI, ANSI_OSC, ANSI_SGR_TERMINATOR, ANSI_ESCAPE_LINK, wrapAnsiCode, wrapAnsiHyperlink, wordLengths, wrapWord, stringVisibleTrimSpacesRight, exec;
var init_wrap_ansi = __esm({
  "node_modules/.pnpm/wrap-ansi@9.0.0/node_modules/wrap-ansi/index.js"() {
    init_cjs_shims();
    init_string_width();
    init_strip_ansi();
    init_ansi_styles2();
    ESCAPES = /* @__PURE__ */ new Set([
      "\x1B",
      "\x9B"
    ]);
    END_CODE = 39;
    ANSI_ESCAPE_BELL = "\x07";
    ANSI_CSI = "[";
    ANSI_OSC = "]";
    ANSI_SGR_TERMINATOR = "m";
    ANSI_ESCAPE_LINK = `${ANSI_OSC}8;;`;
    wrapAnsiCode = (code) => `${ESCAPES.values().next().value}${ANSI_CSI}${code}${ANSI_SGR_TERMINATOR}`;
    wrapAnsiHyperlink = (url) => `${ESCAPES.values().next().value}${ANSI_ESCAPE_LINK}${url}${ANSI_ESCAPE_BELL}`;
    wordLengths = (string) => string.split(" ").map((character) => stringWidth(character));
    wrapWord = (rows, word, columns) => {
      const characters = [...word];
      let isInsideEscape = false;
      let isInsideLinkEscape = false;
      let visible = stringWidth(stripAnsi(rows.at(-1)));
      for (const [index, character] of characters.entries()) {
        const characterLength = stringWidth(character);
        if (visible + characterLength <= columns) {
          rows[rows.length - 1] += character;
        } else {
          rows.push(character);
          visible = 0;
        }
        if (ESCAPES.has(character)) {
          isInsideEscape = true;
          const ansiEscapeLinkCandidate = characters.slice(index + 1, index + 1 + ANSI_ESCAPE_LINK.length).join("");
          isInsideLinkEscape = ansiEscapeLinkCandidate === ANSI_ESCAPE_LINK;
        }
        if (isInsideEscape) {
          if (isInsideLinkEscape) {
            if (character === ANSI_ESCAPE_BELL) {
              isInsideEscape = false;
              isInsideLinkEscape = false;
            }
          } else if (character === ANSI_SGR_TERMINATOR) {
            isInsideEscape = false;
          }
          continue;
        }
        visible += characterLength;
        if (visible === columns && index < characters.length - 1) {
          rows.push("");
          visible = 0;
        }
      }
      if (!visible && rows.at(-1).length > 0 && rows.length > 1) {
        rows[rows.length - 2] += rows.pop();
      }
    };
    stringVisibleTrimSpacesRight = (string) => {
      const words = string.split(" ");
      let last = words.length;
      while (last > 0) {
        if (stringWidth(words[last - 1]) > 0) {
          break;
        }
        last--;
      }
      if (last === words.length) {
        return string;
      }
      return words.slice(0, last).join(" ") + words.slice(last).join("");
    };
    exec = (string, columns, options = {}) => {
      if (options.trim !== false && string.trim() === "") {
        return "";
      }
      let returnValue = "";
      let escapeCode;
      let escapeUrl;
      const lengths = wordLengths(string);
      let rows = [""];
      for (const [index, word] of string.split(" ").entries()) {
        if (options.trim !== false) {
          rows[rows.length - 1] = rows.at(-1).trimStart();
        }
        let rowLength = stringWidth(rows.at(-1));
        if (index !== 0) {
          if (rowLength >= columns && (options.wordWrap === false || options.trim === false)) {
            rows.push("");
            rowLength = 0;
          }
          if (rowLength > 0 || options.trim === false) {
            rows[rows.length - 1] += " ";
            rowLength++;
          }
        }
        if (options.hard && lengths[index] > columns) {
          const remainingColumns = columns - rowLength;
          const breaksStartingThisLine = 1 + Math.floor((lengths[index] - remainingColumns - 1) / columns);
          const breaksStartingNextLine = Math.floor((lengths[index] - 1) / columns);
          if (breaksStartingNextLine < breaksStartingThisLine) {
            rows.push("");
          }
          wrapWord(rows, word, columns);
          continue;
        }
        if (rowLength + lengths[index] > columns && rowLength > 0 && lengths[index] > 0) {
          if (options.wordWrap === false && rowLength < columns) {
            wrapWord(rows, word, columns);
            continue;
          }
          rows.push("");
        }
        if (rowLength + lengths[index] > columns && options.wordWrap === false) {
          wrapWord(rows, word, columns);
          continue;
        }
        rows[rows.length - 1] += word;
      }
      if (options.trim !== false) {
        rows = rows.map((row) => stringVisibleTrimSpacesRight(row));
      }
      const preString = rows.join("\n");
      const pre = [...preString];
      let preStringIndex = 0;
      for (const [index, character] of pre.entries()) {
        returnValue += character;
        if (ESCAPES.has(character)) {
          const { groups } = new RegExp(`(?:\\${ANSI_CSI}(?<code>\\d+)m|\\${ANSI_ESCAPE_LINK}(?<uri>.*)${ANSI_ESCAPE_BELL})`).exec(preString.slice(preStringIndex)) || { groups: {} };
          if (groups.code !== void 0) {
            const code2 = Number.parseFloat(groups.code);
            escapeCode = code2 === END_CODE ? void 0 : code2;
          } else if (groups.uri !== void 0) {
            escapeUrl = groups.uri.length === 0 ? void 0 : groups.uri;
          }
        }
        const code = ansi_styles_default2.codes.get(Number(escapeCode));
        if (pre[index + 1] === "\n") {
          if (escapeUrl) {
            returnValue += wrapAnsiHyperlink("");
          }
          if (escapeCode && code) {
            returnValue += wrapAnsiCode(code);
          }
        } else if (character === "\n") {
          if (escapeCode && code) {
            returnValue += wrapAnsiCode(escapeCode);
          }
          if (escapeUrl) {
            returnValue += wrapAnsiHyperlink(escapeUrl);
          }
        }
        preStringIndex += character.length;
      }
      return returnValue;
    };
  }
});

// node_modules/.pnpm/boxen@8.0.1/node_modules/boxen/index.js
function boxen(text, options) {
  options = {
    padding: 0,
    borderStyle: "single",
    dimBorder: false,
    textAlignment: "left",
    float: "left",
    titleAlignment: "left",
    ...options
  };
  if (options.align) {
    options.textAlignment = options.align;
  }
  if (options.borderColor && !isColorValid(options.borderColor)) {
    throw new Error(`${options.borderColor} is not a valid borderColor`);
  }
  if (options.backgroundColor && !isColorValid(options.backgroundColor)) {
    throw new Error(`${options.backgroundColor} is not a valid backgroundColor`);
  }
  options.padding = getObject(options.padding);
  options.margin = getObject(options.margin);
  options = determineDimensions(text, options);
  text = makeContentText(text, options);
  return boxContent(text, options.width, options);
}
var import_node_process2, import_cli_boxes, import_ansi_align, import_cli_boxes2, NEWLINE, PAD, NONE, terminalColumns, getObject, getBorderWidth, getBorderChars, makeTitle, makeContentText, boxContent, sanitizeOptions, formatTitle, determineDimensions, isHex, isColorValid, getColorFunction, getBGColorFunction;
var init_boxen = __esm({
  "node_modules/.pnpm/boxen@8.0.1/node_modules/boxen/index.js"() {
    init_cjs_shims();
    import_node_process2 = __toESM(require("process"), 1);
    init_string_width();
    init_source();
    init_widest_line();
    import_cli_boxes = __toESM(require_cli_boxes(), 1);
    init_camelcase();
    import_ansi_align = __toESM(require_ansi_align(), 1);
    init_wrap_ansi();
    import_cli_boxes2 = __toESM(require_cli_boxes(), 1);
    NEWLINE = "\n";
    PAD = " ";
    NONE = "none";
    terminalColumns = () => {
      const { env: env3, stdout, stderr } = import_node_process2.default;
      if (stdout?.columns) {
        return stdout.columns;
      }
      if (stderr?.columns) {
        return stderr.columns;
      }
      if (env3.COLUMNS) {
        return Number.parseInt(env3.COLUMNS, 10);
      }
      return 80;
    };
    getObject = (detail) => typeof detail === "number" ? {
      top: detail,
      right: detail * 3,
      bottom: detail,
      left: detail * 3
    } : {
      top: 0,
      right: 0,
      bottom: 0,
      left: 0,
      ...detail
    };
    getBorderWidth = (borderStyle) => borderStyle === NONE ? 0 : 2;
    getBorderChars = (borderStyle) => {
      const sides = [
        "topLeft",
        "topRight",
        "bottomRight",
        "bottomLeft",
        "left",
        "right",
        "top",
        "bottom"
      ];
      let characters;
      if (borderStyle === NONE) {
        borderStyle = {};
        for (const side of sides) {
          borderStyle[side] = "";
        }
      }
      if (typeof borderStyle === "string") {
        characters = import_cli_boxes.default[borderStyle];
        if (!characters) {
          throw new TypeError(`Invalid border style: ${borderStyle}`);
        }
      } else {
        if (typeof borderStyle?.vertical === "string") {
          borderStyle.left = borderStyle.vertical;
          borderStyle.right = borderStyle.vertical;
        }
        if (typeof borderStyle?.horizontal === "string") {
          borderStyle.top = borderStyle.horizontal;
          borderStyle.bottom = borderStyle.horizontal;
        }
        for (const side of sides) {
          if (borderStyle[side] === null || typeof borderStyle[side] !== "string") {
            throw new TypeError(`Invalid border style: ${side}`);
          }
        }
        characters = borderStyle;
      }
      return characters;
    };
    makeTitle = (text, horizontal, alignment) => {
      let title = "";
      const textWidth = stringWidth(text);
      switch (alignment) {
        case "left": {
          title = text + horizontal.slice(textWidth);
          break;
        }
        case "right": {
          title = horizontal.slice(textWidth) + text;
          break;
        }
        default: {
          horizontal = horizontal.slice(textWidth);
          if (horizontal.length % 2 === 1) {
            horizontal = horizontal.slice(Math.floor(horizontal.length / 2));
            title = horizontal.slice(1) + text + horizontal;
          } else {
            horizontal = horizontal.slice(horizontal.length / 2);
            title = horizontal + text + horizontal;
          }
          break;
        }
      }
      return title;
    };
    makeContentText = (text, { padding, width, textAlignment, height }) => {
      text = (0, import_ansi_align.default)(text, { align: textAlignment });
      let lines = text.split(NEWLINE);
      const textWidth = widestLine(text);
      const max = width - padding.left - padding.right;
      if (textWidth > max) {
        const newLines = [];
        for (const line of lines) {
          const createdLines = wrapAnsi(line, max, { hard: true });
          const alignedLines = (0, import_ansi_align.default)(createdLines, { align: textAlignment });
          const alignedLinesArray = alignedLines.split("\n");
          const longestLength = Math.max(...alignedLinesArray.map((s) => stringWidth(s)));
          for (const alignedLine of alignedLinesArray) {
            let paddedLine;
            switch (textAlignment) {
              case "center": {
                paddedLine = PAD.repeat((max - longestLength) / 2) + alignedLine;
                break;
              }
              case "right": {
                paddedLine = PAD.repeat(max - longestLength) + alignedLine;
                break;
              }
              default: {
                paddedLine = alignedLine;
                break;
              }
            }
            newLines.push(paddedLine);
          }
        }
        lines = newLines;
      }
      if (textAlignment === "center" && textWidth < max) {
        lines = lines.map((line) => PAD.repeat((max - textWidth) / 2) + line);
      } else if (textAlignment === "right" && textWidth < max) {
        lines = lines.map((line) => PAD.repeat(max - textWidth) + line);
      }
      const paddingLeft = PAD.repeat(padding.left);
      const paddingRight = PAD.repeat(padding.right);
      lines = lines.map((line) => {
        const newLine = paddingLeft + line + paddingRight;
        return newLine + PAD.repeat(width - stringWidth(newLine));
      });
      if (padding.top > 0) {
        lines = [...Array.from({ length: padding.top }).fill(PAD.repeat(width)), ...lines];
      }
      if (padding.bottom > 0) {
        lines = [...lines, ...Array.from({ length: padding.bottom }).fill(PAD.repeat(width))];
      }
      if (height && lines.length > height) {
        lines = lines.slice(0, height);
      } else if (height && lines.length < height) {
        lines = [...lines, ...Array.from({ length: height - lines.length }).fill(PAD.repeat(width))];
      }
      return lines.join(NEWLINE);
    };
    boxContent = (content, contentWidth, options) => {
      const colorizeBorder = (border) => {
        const newBorder = options.borderColor ? getColorFunction(options.borderColor)(border) : border;
        return options.dimBorder ? source_default.dim(newBorder) : newBorder;
      };
      const colorizeContent = (content2) => options.backgroundColor ? getBGColorFunction(options.backgroundColor)(content2) : content2;
      const chars = getBorderChars(options.borderStyle);
      const columns = terminalColumns();
      let marginLeft = PAD.repeat(options.margin.left);
      if (options.float === "center") {
        const marginWidth = Math.max((columns - contentWidth - getBorderWidth(options.borderStyle)) / 2, 0);
        marginLeft = PAD.repeat(marginWidth);
      } else if (options.float === "right") {
        const marginWidth = Math.max(columns - contentWidth - options.margin.right - getBorderWidth(options.borderStyle), 0);
        marginLeft = PAD.repeat(marginWidth);
      }
      let result = "";
      if (options.margin.top) {
        result += NEWLINE.repeat(options.margin.top);
      }
      if (options.borderStyle !== NONE || options.title) {
        result += colorizeBorder(marginLeft + chars.topLeft + (options.title ? makeTitle(options.title, chars.top.repeat(contentWidth), options.titleAlignment) : chars.top.repeat(contentWidth)) + chars.topRight) + NEWLINE;
      }
      const lines = content.split(NEWLINE);
      result += lines.map((line) => marginLeft + colorizeBorder(chars.left) + colorizeContent(line) + colorizeBorder(chars.right)).join(NEWLINE);
      if (options.borderStyle !== NONE) {
        result += NEWLINE + colorizeBorder(marginLeft + chars.bottomLeft + chars.bottom.repeat(contentWidth) + chars.bottomRight);
      }
      if (options.margin.bottom) {
        result += NEWLINE.repeat(options.margin.bottom);
      }
      return result;
    };
    sanitizeOptions = (options) => {
      if (options.fullscreen && import_node_process2.default?.stdout) {
        let newDimensions = [import_node_process2.default.stdout.columns, import_node_process2.default.stdout.rows];
        if (typeof options.fullscreen === "function") {
          newDimensions = options.fullscreen(...newDimensions);
        }
        options.width ||= newDimensions[0];
        options.height ||= newDimensions[1];
      }
      options.width &&= Math.max(1, options.width - getBorderWidth(options.borderStyle));
      options.height &&= Math.max(1, options.height - getBorderWidth(options.borderStyle));
      return options;
    };
    formatTitle = (title, borderStyle) => borderStyle === NONE ? title : ` ${title} `;
    determineDimensions = (text, options) => {
      options = sanitizeOptions(options);
      const widthOverride = options.width !== void 0;
      const columns = terminalColumns();
      const borderWidth = getBorderWidth(options.borderStyle);
      const maxWidth = columns - options.margin.left - options.margin.right - borderWidth;
      const widest = widestLine(wrapAnsi(text, columns - borderWidth, { hard: true, trim: false })) + options.padding.left + options.padding.right;
      if (options.title && widthOverride) {
        options.title = options.title.slice(0, Math.max(0, options.width - 2));
        options.title &&= formatTitle(options.title, options.borderStyle);
      } else if (options.title) {
        options.title = options.title.slice(0, Math.max(0, maxWidth - 2));
        if (options.title) {
          options.title = formatTitle(options.title, options.borderStyle);
          if (stringWidth(options.title) > widest) {
            options.width = stringWidth(options.title);
          }
        }
      }
      options.width ||= widest;
      if (!widthOverride) {
        if (options.margin.left && options.margin.right && options.width > maxWidth) {
          const spaceForMargins = columns - options.width - borderWidth;
          const multiplier = spaceForMargins / (options.margin.left + options.margin.right);
          options.margin.left = Math.max(0, Math.floor(options.margin.left * multiplier));
          options.margin.right = Math.max(0, Math.floor(options.margin.right * multiplier));
        }
        options.width = Math.min(options.width, columns - borderWidth - options.margin.left - options.margin.right);
      }
      if (options.width - (options.padding.left + options.padding.right) <= 0) {
        options.padding.left = 0;
        options.padding.right = 0;
      }
      if (options.height && options.height - (options.padding.top + options.padding.bottom) <= 0) {
        options.padding.top = 0;
        options.padding.bottom = 0;
      }
      return options;
    };
    isHex = (color) => color.match(/^#(?:[0-f]{3}){1,2}$/i);
    isColorValid = (color) => typeof color === "string" && (source_default[color] ?? isHex(color));
    getColorFunction = (color) => isHex(color) ? source_default.hex(color) : source_default[color];
    getBGColorFunction = (color) => isHex(color) ? source_default.bgHex(color) : source_default[camelCase(["bg", color])];
  }
});

// src/core/logger/logger.ts
var import_winston, import_fs, import_path, logLevels, SENSITIVE_KEYS, MASK_REGEX, redactSensitiveData, levelColorMap, maskFormat, consoleFormat, fileFormat, getDefaultLogLevel, Logger, logger, createLogger;
var init_logger = __esm({
  "src/core/logger/logger.ts"() {
    init_cjs_shims();
    import_winston = __toESM(require("winston"), 1);
    init_source();
    init_boxen();
    import_fs = __toESM(require("fs"), 1);
    import_path = __toESM(require("path"), 1);
    init_env();
    logLevels = {
      error: 0,
      // Highest priority
      warn: 1,
      info: 2,
      http: 3,
      verbose: 4,
      debug: 5,
      silly: 6
      // Lowest priority
    };
    SENSITIVE_KEYS = ["apiKey", "password", "secret", "token", "auth", "key", "credential"];
    MASK_REGEX = new RegExp(
      `(${SENSITIVE_KEYS.join("|")})(["']?\\s*[:=]\\s*)(["'])?.*?\\3`,
      "gi"
    );
    redactSensitiveData = (message) => {
      const shouldRedact = env.REDACT_SECRETS !== false;
      if (!shouldRedact) return message;
      return message.replace(MASK_REGEX, (match, key, separator, quote) => {
        const quoteMark = quote || "";
        return `${key}${separator}${quoteMark}***REDACTED***${quoteMark}`;
      });
    };
    levelColorMap = {
      error: source_default.red,
      warn: source_default.yellow,
      info: source_default.blue,
      http: source_default.cyan,
      verbose: source_default.magenta,
      debug: source_default.gray,
      silly: source_default.gray.dim
    };
    maskFormat = import_winston.default.format((info) => {
      if (typeof info.message === "string") {
        info.message = redactSensitiveData(info.message);
      }
      return info;
    });
    consoleFormat = import_winston.default.format.printf(({ level, message, timestamp, color }) => {
      const colorize = levelColorMap[level] || source_default.white;
      let formattedMessage = message;
      if (color && source_default[color]) {
        formattedMessage = source_default[color](message);
      }
      return `${source_default.dim(timestamp)} ${colorize(level.toUpperCase())}: ${formattedMessage}`;
    });
    fileFormat = import_winston.default.format.printf(({ level, message, timestamp }) => {
      return `${timestamp} [${level.toUpperCase()}]: ${message}`;
    });
    getDefaultLogLevel = () => {
      const envLevel = env.CIPHER_LOG_LEVEL;
      if (envLevel && Object.keys(logLevels).includes(envLevel.toLowerCase())) {
        return envLevel.toLowerCase();
      }
      return "info";
    };
    Logger = class _Logger {
      logger;
      isSilent = false;
      constructor(options = {}) {
        const level = options.level || getDefaultLogLevel();
        this.isSilent = options.silent || false;
        const errorFormat = import_winston.default.format((info) => {
          if (info instanceof Error) {
            return Object.assign({}, info, {
              message: info.message,
              stack: info.stack
            });
          }
          if (info.error instanceof Error) {
            info.message += `
${info.error.stack}`;
          }
          return info;
        });
        this.logger = import_winston.default.createLogger({
          levels: logLevels,
          level,
          format: import_winston.default.format.combine(
            errorFormat(),
            import_winston.default.format.timestamp({ format: "YYYY-MM-DD HH:mm:ss" }),
            maskFormat()
          ),
          transports: this.createTransports(options.file),
          silent: this.isSilent
        });
        import_winston.default.addColors({
          error: "red",
          warn: "yellow",
          info: "blue",
          http: "cyan",
          verbose: "magenta",
          debug: "gray",
          silly: "gray"
        });
      }
      createTransports(filePath) {
        const transports = [];
        if (filePath) {
          import_fs.default.mkdirSync(import_path.default.dirname(filePath), { recursive: true });
          transports.push(
            new import_winston.default.transports.File({
              filename: filePath,
              format: import_winston.default.format.combine(
                import_winston.default.format.timestamp({ format: "YYYY-MM-DD HH:mm:ss" }),
                maskFormat(),
                fileFormat
              )
            })
          );
        } else {
          transports.push(
            new import_winston.default.transports.Console({
              format: import_winston.default.format.combine(
                import_winston.default.format.timestamp({ format: "HH:mm:ss" }),
                maskFormat(),
                consoleFormat
              ),
              stderrLevels: Object.keys(logLevels)
              // Redirect all log levels to stderr
            })
          );
        }
        return transports;
      }
      // ===== Core Logging Methods =====
      error(message, meta, color) {
        this.logger.error(message, { ...meta, color });
      }
      warn(message, meta, color) {
        this.logger.warn(message, { ...meta, color });
      }
      info(message, meta, color) {
        this.logger.info(message, { ...meta, color });
      }
      http(message, meta, color) {
        this.logger.http(message, { ...meta, color });
      }
      verbose(message, meta, color) {
        this.logger.verbose(message, { ...meta, color });
      }
      debug(message, meta, color) {
        this.logger.debug(message, { ...meta, color });
      }
      silly(message, meta, color) {
        this.logger.silly(message, { ...meta, color });
      }
      // ===== Specialized Display Features =====
      displayAIResponse(response) {
        if (this.isSilent) return;
        const content = typeof response === "string" ? response : response?.content || JSON.stringify(response, null, 2);
        console.log(
          boxen(source_default.white(content), {
            padding: 1,
            borderColor: "yellow",
            title: "\u{1F916} AI Response",
            titleAlignment: "center"
          })
        );
      }
      toolCall(toolName, args) {
        if (this.isSilent) return;
        const argsString = typeof args === "string" ? args : JSON.stringify(args, null, 2);
        console.log(
          boxen(
            `${source_default.cyan("Tool Call")}: ${source_default.yellow(toolName)}
${source_default.dim("Arguments")}:
${source_default.white(argsString)}`,
            {
              padding: 1,
              borderColor: "blue",
              title: "\u{1F527} Tool Call",
              titleAlignment: "center"
            }
          )
        );
      }
      toolResult(result) {
        if (this.isSilent) return;
        const resultString = typeof result === "string" ? result : JSON.stringify(result, null, 2);
        console.log(
          boxen(source_default.green(resultString), {
            padding: 1,
            borderColor: "green",
            title: "\u2705 Tool Result",
            titleAlignment: "center"
          })
        );
      }
      displayBox(title, content, borderColor = "white") {
        if (this.isSilent) return;
        console.log(
          boxen(content, {
            padding: 1,
            borderColor,
            title,
            titleAlignment: "center"
          })
        );
      }
      // ===== Runtime Configuration Management =====
      setLevel(level) {
        if (Object.keys(logLevels).includes(level.toLowerCase())) {
          this.logger.level = level.toLowerCase();
          if (!this.isSilent) {
            console.log(`Log level set to: ${level}`);
          }
        } else {
          this.error(`Invalid log level: ${level}. Valid levels: ${Object.keys(logLevels).join(", ")}`);
        }
      }
      getLevel() {
        return this.logger.level;
      }
      setSilent(silent) {
        this.isSilent = silent;
        this.logger.silent = silent;
      }
      redirectToFile(filePath) {
        try {
          import_fs.default.mkdirSync(import_path.default.dirname(filePath), { recursive: true });
          this.logger.clear();
          this.logger.add(
            new import_winston.default.transports.File({
              filename: filePath,
              format: import_winston.default.format.combine(
                import_winston.default.format.timestamp({ format: "YYYY-MM-DD HH:mm:ss" }),
                maskFormat(),
                fileFormat
              )
            })
          );
          this.isSilent = true;
        } catch (error) {
          this.error(`Failed to redirect logger to file: ${error}`);
        }
      }
      redirectToConsole() {
        try {
          this.logger.clear();
          this.logger.add(
            new import_winston.default.transports.Console({
              format: import_winston.default.format.combine(
                import_winston.default.format.timestamp({ format: "HH:mm:ss" }),
                maskFormat(),
                consoleFormat
              ),
              stderrLevels: Object.keys(logLevels)
              // Redirect all log levels to stderr
            })
          );
          this.isSilent = false;
          console.log("Logger redirected to console");
        } catch (error) {
          this.error(`Failed to redirect logger to console: ${error}`);
        }
      }
      // ===== Utility Methods =====
      createChild(options = {}) {
        const childOptions = {
          level: options.level || this.getLevel(),
          silent: options.silent !== void 0 ? options.silent : this.isSilent
        };
        if (options.file !== void 0) {
          childOptions.file = options.file;
        }
        return new _Logger(childOptions);
      }
      // Get logger instance for advanced usage
      getWinstonLogger() {
        return this.logger;
      }
    };
    logger = new Logger();
    createLogger = (options = {}) => {
      return new Logger(options);
    };
  }
});

// src/core/logger/index.ts
var init_logger2 = __esm({
  "src/core/logger/index.ts"() {
    init_cjs_shims();
    init_logger();
  }
});

// src/core/mcp/constants.ts
var DEFAULT_TIMEOUT_MS, TRANSPORT_TYPES, CONNECTION_MODES, ERROR_MESSAGES, LOG_PREFIXES, ENV_VARS;
var init_constants = __esm({
  "src/core/mcp/constants.ts"() {
    init_cjs_shims();
    DEFAULT_TIMEOUT_MS = 6e4;
    TRANSPORT_TYPES = {
      STDIO: "stdio",
      SSE: "sse",
      STREAMABLE_HTTP: "streamable-http"
    };
    CONNECTION_MODES = {
      /**
       * Strict mode requires the server to successfully connect.
       * If connection fails, an error will be thrown.
       */
      STRICT: "strict",
      /**
       * Lenient mode allows the server to fail connecting.
       * If connection fails, a warning will be logged but no error will be thrown.
       */
      LENIENT: "lenient"
    };
    ERROR_MESSAGES = {
      // Connection errors
      CONNECTION_FAILED: "Failed to connect to MCP server",
      DISCONNECTION_FAILED: "Failed to disconnect from MCP server",
      NOT_CONNECTED: "Client not connected. Please call connect() first",
      // Tool execution errors
      TOOL_EXECUTION_FAILED: "Tool execution failed",
      NO_CLIENT_FOR_TOOL: "No client found for tool",
      // Prompt and resource errors
      NO_CLIENT_FOR_PROMPT: "No client found for prompt",
      NO_CLIENT_FOR_RESOURCE: "No client found for resource",
      PROMPT_NOT_FOUND: "Prompt not found",
      RESOURCE_NOT_FOUND: "Resource not found",
      // Server configuration errors
      INVALID_CONFIG: "Invalid server configuration",
      UNSUPPORTED_SERVER_TYPE: "Unsupported server type",
      // Client registry errors
      CLIENT_ALREADY_REGISTERED: "Client already registered",
      MISSING_REQUIRED_SERVERS: "Failed to connect to required strict servers"
    };
    LOG_PREFIXES = {
      CONNECT: "MCP Connection:",
      TOOL: "MCP Tool:",
      PROMPT: "MCP Prompt:",
      RESOURCE: "MCP Resource:",
      MANAGER: "MCP Manager:"
    };
    ENV_VARS = {
      /**
       * Environment variable to set the global timeout for all MCP operations.
       */
      GLOBAL_TIMEOUT: "MCP_GLOBAL_TIMEOUT",
      /**
       * Environment variable to set the default connection mode.
       */
      DEFAULT_CONNECTION_MODE: "MCP_DEFAULT_CONNECTION_MODE"
    };
  }
});

// src/core/mcp/client.ts
var import_client, import_stdio, import_sse, import_streamableHttp, path2, fs2, import_url, MCPClient;
var init_client = __esm({
  "src/core/mcp/client.ts"() {
    init_cjs_shims();
    import_client = require("@modelcontextprotocol/sdk/client/index.js");
    import_stdio = require("@modelcontextprotocol/sdk/client/stdio.js");
    import_sse = require("@modelcontextprotocol/sdk/client/sse.js");
    import_streamableHttp = require("@modelcontextprotocol/sdk/client/streamableHttp.js");
    path2 = __toESM(require("path"), 1);
    fs2 = __toESM(require("fs"), 1);
    import_url = require("url");
    init_constants();
    init_logger2();
    MCPClient = class {
      client = null;
      transport = null;
      connected = false;
      serverConfig = null;
      serverName = "";
      logger;
      connectionPromise = null;
      quietMode = false;
      // Server process information (for stdio connections)
      serverInfo = {
        spawned: false,
        pid: null,
        command: null,
        originalArgs: null,
        resolvedArgs: null,
        env: null,
        alias: null
      };
      constructor() {
        this.logger = createLogger({ level: "info" });
      }
      /**
       * Enable quiet mode to reduce logging verbosity (useful for CLI mode)
       */
      setQuietMode(quiet) {
        this.quietMode = quiet;
      }
      /**
       * Connect to an MCP server using the provided configuration.
       */
      async connect(config2, serverName) {
        if (this.connected && this.client) {
          this.logger.warn(`${LOG_PREFIXES.CONNECT} Already connected to ${serverName}`);
          return this.client;
        }
        if (this.connectionPromise) {
          return this.connectionPromise;
        }
        this.connectionPromise = this._performConnection(config2, serverName);
        try {
          const client = await this.connectionPromise;
          this.connectionPromise = null;
          return client;
        } catch (error) {
          this.connectionPromise = null;
          throw error;
        }
      }
      /**
       * Internal method to perform the actual connection.
       */
      async _performConnection(config2, serverName) {
        this.serverConfig = config2;
        this.serverName = serverName;
        if (!this.quietMode) {
          this.logger.info(`${LOG_PREFIXES.CONNECT} Connecting to ${serverName} (${config2.type})`, {
            serverName,
            transportType: config2.type
          });
        }
        try {
          this.transport = await this._createTransport(config2);
          this.client = new import_client.Client(
            {
              name: `cipher-mcp-client-${serverName}`,
              version: "1.0.0"
            },
            {
              capabilities: {
                tools: {},
                prompts: {},
                resources: {}
              }
            }
          );
          const timeout = this._getOperationTimeout(config2);
          await this._connectWithTimeout(this.transport, timeout);
          this.connected = true;
          if (!this.quietMode) {
            this.logger.info(`${LOG_PREFIXES.CONNECT} Successfully connected to ${serverName}`, {
              serverName,
              timeout
            });
          }
          return this.client;
        } catch (error) {
          await this._cleanup();
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(
            `${LOG_PREFIXES.CONNECT} ${ERROR_MESSAGES.CONNECTION_FAILED}: ${serverName}`,
            { serverName, error: errorMessage, transportType: config2.type }
          );
          throw new Error(`${ERROR_MESSAGES.CONNECTION_FAILED}: ${errorMessage}`);
        }
      }
      /**
       * Create transport based on server configuration.
       */
      async _createTransport(config2) {
        switch (config2.type) {
          case TRANSPORT_TYPES.STDIO:
            return this._createStdioTransport(config2);
          case TRANSPORT_TYPES.SSE:
            return this._createSseTransport(config2);
          case TRANSPORT_TYPES.STREAMABLE_HTTP:
            return this._createStreamableHttpTransport(config2);
          default:
            throw new Error(`${ERROR_MESSAGES.UNSUPPORTED_SERVER_TYPE}: ${config2.type}`);
        }
      }
      /**
       * Create stdio transport.
       */
      async _createStdioTransport(config2) {
        const resolvedCommand = this._resolveCommand(config2.command);
        const resolvedArgs = this._resolveArgs(config2.args || []);
        const env3 = this._mergeEnvironment(config2.env || {});
        this.serverInfo = {
          spawned: true,
          pid: null,
          // Will be set after spawn
          command: resolvedCommand,
          originalArgs: config2.args || [],
          resolvedArgs,
          env: env3,
          alias: this.serverName
        };
        this.logger.debug(`${LOG_PREFIXES.CONNECT} Creating stdio transport`, {
          command: resolvedCommand,
          args: resolvedArgs,
          serverName: this.serverName
        });
        const transport = new import_stdio.StdioClientTransport({
          command: resolvedCommand,
          args: resolvedArgs,
          env: env3
        });
        return transport;
      }
      /**
       * Create SSE transport.
       */
      async _createSseTransport(config2) {
        this.logger.debug(`${LOG_PREFIXES.CONNECT} Creating SSE transport`, {
          url: config2.url,
          serverName: this.serverName
        });
        const transportOptions = config2.headers ? { requestInit: { headers: config2.headers } } : void 0;
        const transport = new import_sse.SSEClientTransport(new URL(config2.url), transportOptions);
        return transport;
      }
      /**
       * Create streamable HTTP transport.
       */
      async _createStreamableHttpTransport(config2) {
        this.logger.debug(`${LOG_PREFIXES.CONNECT} Creating streamable HTTP transport`, {
          url: config2.url,
          serverName: this.serverName
        });
        const transportOptions = config2.headers ? { requestInit: { headers: config2.headers } } : void 0;
        const transport = new import_streamableHttp.StreamableHTTPClientTransport(new URL(config2.url), transportOptions);
        return transport;
      }
      /**
       * Connect client to transport with timeout.
       */
      async _connectWithTimeout(transport, timeout) {
        if (!this.client) {
          throw new Error("Client not initialized");
        }
        return new Promise((resolve3, reject) => {
          const timeoutId = setTimeout(() => {
            reject(new Error(`Connection timeout after ${timeout}ms`));
          }, timeout);
          this.client.connect(transport).then(() => {
            clearTimeout(timeoutId);
            resolve3();
          }).catch((error) => {
            clearTimeout(timeoutId);
            reject(error);
          });
        });
      }
      /**
       * Disconnect from the MCP server.
       */
      async disconnect() {
        if (!this.connected) {
          this.logger.warn(`${LOG_PREFIXES.CONNECT} Already disconnected from ${this.serverName}`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES.CONNECT} Disconnecting from ${this.serverName}`);
        try {
          await this._cleanup();
          this.logger.info(`${LOG_PREFIXES.CONNECT} Successfully disconnected from ${this.serverName}`);
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(
            `${LOG_PREFIXES.CONNECT} ${ERROR_MESSAGES.DISCONNECTION_FAILED}: ${this.serverName}`,
            { serverName: this.serverName, error: errorMessage }
          );
          throw new Error(`${ERROR_MESSAGES.DISCONNECTION_FAILED}: ${errorMessage}`);
        }
      }
      /**
       * Call a tool with the given name and arguments.
       */
      async callTool(name, args) {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        this.logger.info(`${LOG_PREFIXES.TOOL} Calling tool: ${name}`, {
          toolName: name,
          serverName: this.serverName,
          timeout
        });
        try {
          const result = await this._executeWithTimeout(
            () => this.client.callTool({ name, arguments: args }),
            timeout,
            `Tool execution timeout: ${name}`
          );
          this.logger.info(`${LOG_PREFIXES.TOOL} Tool executed successfully: ${name}`, {
            toolName: name,
            serverName: this.serverName
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.TOOL} ${ERROR_MESSAGES.TOOL_EXECUTION_FAILED}: ${name}`, {
            toolName: name,
            serverName: this.serverName,
            error: errorMessage
          });
          throw new Error(`${ERROR_MESSAGES.TOOL_EXECUTION_FAILED}: ${errorMessage}`);
        }
      }
      /**
       * Get all tools provided by this client.
       */
      async getTools() {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        try {
          const result = await this._executeWithTimeout(
            () => this.client.listTools(),
            timeout,
            "List tools timeout"
          );
          const toolSet = {};
          result.tools.forEach((tool) => {
            toolSet[tool.name] = {
              description: tool.description || "",
              parameters: tool.inputSchema
            };
          });
          this.logger.debug(`${LOG_PREFIXES.TOOL} Retrieved ${Object.keys(toolSet).length} tools`, {
            serverName: this.serverName,
            toolCount: Object.keys(toolSet).length
          });
          return toolSet;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.TOOL} Failed to list tools`, {
            serverName: this.serverName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * List all prompts provided by this client.
       */
      async listPrompts() {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        try {
          const result = await this._executeWithTimeout(
            () => this.client.listPrompts(),
            timeout,
            "List prompts timeout"
          );
          const promptNames = result.prompts.map((prompt) => prompt.name);
          this.logger.debug(`${LOG_PREFIXES.PROMPT} Retrieved ${promptNames.length} prompts`, {
            serverName: this.serverName,
            promptCount: promptNames.length
          });
          return promptNames;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const isCapabilityError = errorMessage.includes("not implemented") || errorMessage.includes("not supported") || errorMessage.includes("Method not found") || errorMessage.includes("prompts") === false;
          if (isCapabilityError) {
            this.logger.debug(
              `${LOG_PREFIXES.PROMPT} Prompts not supported by server (this is normal)`,
              {
                serverName: this.serverName,
                reason: "Server does not implement prompt capability"
              }
            );
            return [];
          }
          this.logger.error(`${LOG_PREFIXES.PROMPT} Failed to list prompts`, {
            serverName: this.serverName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * Get a prompt by name.
       */
      async getPrompt(name, args) {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        this.logger.info(`${LOG_PREFIXES.PROMPT} Getting prompt: ${name}`, {
          promptName: name,
          serverName: this.serverName
        });
        try {
          const result = await this._executeWithTimeout(
            () => this.client.getPrompt({ name, arguments: args }),
            timeout,
            `Get prompt timeout: ${name}`
          );
          this.logger.info(`${LOG_PREFIXES.PROMPT} Retrieved prompt successfully: ${name}`, {
            promptName: name,
            serverName: this.serverName
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.PROMPT} Failed to get prompt: ${name}`, {
            promptName: name,
            serverName: this.serverName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * List all resources provided by this client.
       */
      async listResources() {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        try {
          const result = await this._executeWithTimeout(
            () => this.client.listResources(),
            timeout,
            "List resources timeout"
          );
          const resourceUris = result.resources.map((resource) => resource.uri);
          this.logger.debug(`${LOG_PREFIXES.RESOURCE} Retrieved ${resourceUris.length} resources`, {
            serverName: this.serverName,
            resourceCount: resourceUris.length
          });
          return resourceUris;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const isCapabilityError = errorMessage.includes("not implemented") || errorMessage.includes("not supported") || errorMessage.includes("Method not found") || errorMessage.includes("resources") === false;
          if (isCapabilityError) {
            this.logger.debug(
              `${LOG_PREFIXES.RESOURCE} Resources not supported by server (this is normal)`,
              {
                serverName: this.serverName,
                reason: "Server does not implement resource capability"
              }
            );
            return [];
          }
          this.logger.error(`${LOG_PREFIXES.RESOURCE} Failed to list resources`, {
            serverName: this.serverName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * Read a resource by URI.
       */
      async readResource(uri) {
        this._ensureConnected();
        const timeout = this._getOperationTimeout();
        this.logger.info(`${LOG_PREFIXES.RESOURCE} Reading resource: ${uri}`, {
          resourceUri: uri,
          serverName: this.serverName
        });
        try {
          const result = await this._executeWithTimeout(
            () => this.client.readResource({ uri }),
            timeout,
            `Read resource timeout: ${uri}`
          );
          this.logger.info(`${LOG_PREFIXES.RESOURCE} Read resource successfully: ${uri}`, {
            resourceUri: uri,
            serverName: this.serverName
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.RESOURCE} Failed to read resource: ${uri}`, {
            resourceUri: uri,
            serverName: this.serverName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * Get the connection status of the client.
       */
      getConnectionStatus() {
        return this.connected;
      }
      /**
       * Get the underlying MCP client instance.
       */
      getClient() {
        return this.client;
      }
      /**
       * Get information about the connected server.
       */
      getServerInfo() {
        return { ...this.serverInfo };
      }
      /**
       * Get the client instance once connected.
       */
      async getConnectedClient() {
        if (this.connected && this.client) {
          return this.client;
        }
        if (this.connectionPromise) {
          return this.connectionPromise;
        }
        throw new Error(ERROR_MESSAGES.NOT_CONNECTED);
      }
      // ======================================================
      // Private Utility Methods
      // ======================================================
      /**
       * Ensure the client is connected before performing operations.
       */
      _ensureConnected() {
        if (!this.connected || !this.client) {
          throw new Error(ERROR_MESSAGES.NOT_CONNECTED);
        }
      }
      /**
       * Get the operation timeout from configuration or default.
       */
      _getOperationTimeout(config2) {
        if (config2?.timeout) {
          return config2.timeout;
        }
        if (this.serverConfig?.timeout) {
          return this.serverConfig.timeout;
        }
        const envTimeout = process.env[ENV_VARS.GLOBAL_TIMEOUT];
        if (envTimeout) {
          const parsed = parseInt(envTimeout, 10);
          if (!isNaN(parsed) && parsed > 0) {
            return parsed;
          }
        }
        return DEFAULT_TIMEOUT_MS;
      }
      /**
       * Execute a function with timeout.
       */
      async _executeWithTimeout(operation, timeout, timeoutMessage) {
        return new Promise((resolve3, reject) => {
          const timeoutId = setTimeout(() => {
            reject(new Error(timeoutMessage));
          }, timeout);
          operation().then((result) => {
            clearTimeout(timeoutId);
            resolve3(result);
          }).catch((error) => {
            clearTimeout(timeoutId);
            reject(error);
          });
        });
      }
      /**
       * Resolve command path, handling bundled scripts and relative paths.
       */
      _resolveCommand(command) {
        if (path2.isAbsolute(command)) {
          return command;
        }
        const cwdPath = path2.resolve(process.cwd(), command);
        if (fs2.existsSync(cwdPath)) {
          return cwdPath;
        }
        try {
          const moduleDir = path2.dirname((0, import_url.fileURLToPath)(importMetaUrl));
          const bundledPath = path2.resolve(moduleDir, "../../../", command);
          if (fs2.existsSync(bundledPath)) {
            return bundledPath;
          }
        } catch {
        }
        return command;
      }
      /**
       * Resolve arguments, performing any necessary path resolution.
       */
      _resolveArgs(args) {
        return args.map((arg) => {
          if (arg.includes("/") || arg.includes("\\")) {
            const resolved = this._resolveCommand(arg);
            return resolved;
          }
          return arg;
        });
      }
      /**
       * Merge environment variables with current process environment.
       */
      _mergeEnvironment(configEnv) {
        const processEnv = Object.fromEntries(
          Object.entries(process.env).filter(([_, value]) => value !== void 0)
        );
        return {
          ...processEnv,
          ...configEnv
        };
      }
      /**
       * Clean up resources and reset state.
       */
      async _cleanup() {
        this.connected = false;
        if (this.client) {
          try {
            await this.client.close();
          } catch (_error) {
            this.logger.warn(`${LOG_PREFIXES.CONNECT} Error during client cleanup`, {
              error: _error instanceof Error ? _error.message : String(_error)
            });
          }
          this.client = null;
        }
        if (this.transport) {
          try {
            await this.transport.close();
          } catch (_error) {
            this.logger.warn(`${LOG_PREFIXES.CONNECT} Error during transport cleanup`, {
              error: _error instanceof Error ? _error.message : String(_error)
            });
          }
          this.transport = null;
        }
        this.serverInfo = {
          spawned: false,
          pid: null,
          command: null,
          originalArgs: null,
          resolvedArgs: null,
          env: null,
          alias: null
        };
        this.serverConfig = null;
        this.serverName = "";
      }
    };
  }
});

// src/core/events/event-types.ts
var ServiceEvents, SessionEvents;
var init_event_types = __esm({
  "src/core/events/event-types.ts"() {
    init_cjs_shims();
    ServiceEvents = {
      CIPHER_STARTED: "cipher:started",
      CIPHER_STOPPED: "cipher:stopped",
      CIPHER_ERROR: "cipher:error",
      SERVICE_STARTED: "cipher:serviceStarted",
      SERVICE_ERROR: "cipher:serviceError",
      ALL_SERVICES_READY: "cipher:allServicesReady",
      TOOL_REGISTERED: "cipher:toolRegistered",
      TOOL_UNREGISTERED: "cipher:toolUnregistered",
      TOOL_ERROR: "cipher:toolError",
      MCP_CLIENT_CONNECTED: "cipher:mcpClientConnected",
      MCP_CLIENT_DISCONNECTED: "cipher:mcpClientDisconnected",
      MCP_CLIENT_ERROR: "cipher:mcpClientError",
      MEMORY_OPERATION_STARTED: "cipher:memoryOperationStarted",
      MEMORY_OPERATION_COMPLETED: "cipher:memoryOperationCompleted",
      MEMORY_OPERATION_FAILED: "cipher:memoryOperationFailed",
      VECTOR_STORE_CONNECTED: "cipher:vectorStoreConnected",
      VECTOR_STORE_DISCONNECTED: "cipher:vectorStoreDisconnected",
      VECTOR_STORE_ERROR: "cipher:vectorStoreError",
      LLM_PROVIDER_REGISTERED: "cipher:llmProviderRegistered",
      LLM_PROVIDER_ERROR: "cipher:llmProviderError"
    };
    SessionEvents = {
      SESSION_CREATED: "session:created",
      SESSION_ACTIVATED: "session:activated",
      SESSION_DEACTIVATED: "session:deactivated",
      SESSION_EXPIRED: "session:expired",
      SESSION_DELETED: "session:deleted",
      TOOL_EXECUTION_STARTED: "tool:executionStarted",
      TOOL_EXECUTION_COMPLETED: "tool:executionCompleted",
      TOOL_EXECUTION_FAILED: "tool:executionFailed",
      LLM_THINKING: "llm:thinking",
      LLM_RESPONSE_STARTED: "llm:responseStarted",
      LLM_RESPONSE_CHUNK: "llm:responseChunk",
      LLM_RESPONSE_COMPLETED: "llm:responseCompleted",
      LLM_RESPONSE_ERROR: "llm:responseError",
      MEMORY_STORED: "memory:stored",
      MEMORY_RETRIEVED: "memory:retrieved",
      MEMORY_SEARCHED: "memory:searched",
      CONVERSATION_MESSAGE_ADDED: "conversation:messageAdded",
      CONVERSATION_MESSAGE_UPDATED: "conversation:messageUpdated",
      CONVERSATION_CLEARED: "conversation:cleared",
      CONTEXT_UPDATED: "context:updated",
      CONTEXT_TRUNCATED: "context:truncated"
    };
  }
});

// src/core/mcp/manager.ts
var MCPManager;
var init_manager = __esm({
  "src/core/mcp/manager.ts"() {
    init_cjs_shims();
    init_constants();
    init_client();
    init_logger2();
    init_event_types();
    MCPManager = class {
      clients = /* @__PURE__ */ new Map();
      failedConnections = {};
      logger;
      eventManager;
      quietMode = false;
      // O(1) lookup caches
      toolCache = /* @__PURE__ */ new Map();
      toolClientMap = /* @__PURE__ */ new Map();
      // toolName -> clientName
      promptCache = /* @__PURE__ */ new Map();
      promptClientMap = /* @__PURE__ */ new Map();
      // promptName -> clientName
      resourceCache = /* @__PURE__ */ new Map();
      resourceClientMap = /* @__PURE__ */ new Map();
      // resourceUri -> clientName
      // Cache configuration
      cacheTimeout = 5 * 60 * 1e3;
      // 5 minutes
      maxCacheSize = 1e3;
      constructor() {
        this.logger = createLogger({ level: "info" });
        this.logger.info(`${LOG_PREFIXES.MANAGER} MCPManager initialized`);
      }
      /**
       * Set the event manager for emitting connection lifecycle events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      /**
       * Enable quiet mode to reduce logging verbosity (useful for CLI mode)
       */
      setQuietMode(quiet) {
        this.quietMode = quiet;
      }
      /**
       * Register a client with the manager.
       */
      registerClient(name, client) {
        if (this.clients.has(name)) {
          this.logger.warn(
            `${LOG_PREFIXES.MANAGER} ${ERROR_MESSAGES.CLIENT_ALREADY_REGISTERED}: ${name}`,
            { clientName: name }
          );
          return;
        }
        this.clients.set(name, {
          client,
          config: {},
          // Will be set during connection
          connected: false,
          lastSeen: Date.now(),
          failureCount: 0
        });
        this.logger.info(`${LOG_PREFIXES.MANAGER} Registered client: ${name}`, {
          clientName: name,
          totalClients: this.clients.size
        });
      }
      /**
       * Get all available tools from all connected clients.
       */
      async getAllTools() {
        const allTools = {};
        const errors = [];
        const toolPromises = Array.from(this.clients.entries()).map(async ([name, entry]) => {
          if (!entry.connected) {
            return;
          }
          try {
            const tools = await entry.client.getTools();
            Object.entries(tools).forEach(([toolName, toolDef]) => {
              const finalToolName = allTools[toolName] ? `${name}.${toolName}` : toolName;
              allTools[finalToolName] = toolDef;
              this.toolClientMap.set(finalToolName, name);
              this.toolCache.set(finalToolName, {
                data: toolDef,
                timestamp: Date.now(),
                clientName: name
              });
            });
            this.logger.debug(
              `${LOG_PREFIXES.MANAGER} Retrieved ${Object.keys(tools).length} tools from ${name}`,
              { clientName: name, toolCount: Object.keys(tools).length }
            );
          } catch (error) {
            const errorMessage = error instanceof Error ? error.message : String(error);
            errors.push(`${name}: ${errorMessage}`);
            this._updateClientFailure(name);
            this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to get tools from ${name}`, {
              clientName: name,
              error: errorMessage
            });
          }
        });
        await Promise.allSettled(toolPromises);
        if (errors.length > 0 && Object.keys(allTools).length === 0) {
          throw new Error(`Failed to retrieve tools from all clients: ${errors.join("; ")}`);
        }
        this._cleanupCache(this.toolCache);
        this.logger.debug(
          `${LOG_PREFIXES.MANAGER} Retrieved ${Object.keys(allTools).length} total tools`,
          { toolCount: Object.keys(allTools).length, clientCount: this.clients.size }
        );
        return allTools;
      }
      /**
       * Get the client that provides a specific tool.
       */
      getToolClient(toolName) {
        const clientName = this.toolClientMap.get(toolName);
        if (!clientName) {
          return void 0;
        }
        const entry = this.clients.get(clientName);
        return entry?.connected ? entry.client : void 0;
      }
      /**
       * Execute a tool with the given name and arguments.
       */
      async executeTool(toolName, args) {
        const client = this.getToolClient(toolName);
        if (!client) {
          await this._refreshToolCache();
          const refreshedClient = this.getToolClient(toolName);
          if (!refreshedClient) {
            throw new Error(`${ERROR_MESSAGES.NO_CLIENT_FOR_TOOL}: ${toolName}`);
          }
          return refreshedClient.callTool(toolName, args);
        }
        this.logger.info(`${LOG_PREFIXES.MANAGER} Executing tool: ${toolName}`, {
          toolName,
          hasArgs: !!args
        });
        try {
          const result = await client.callTool(toolName, args);
          this.logger.info(`${LOG_PREFIXES.MANAGER} Tool executed successfully: ${toolName}`, {
            toolName
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.MANAGER} Tool execution failed: ${toolName}`, {
            toolName,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * List all available prompts from all connected clients.
       */
      async listAllPrompts() {
        const allPrompts = /* @__PURE__ */ new Set();
        const errors = [];
        const promptPromises = Array.from(this.clients.entries()).map(async ([name, entry]) => {
          if (!entry.connected) {
            return;
          }
          try {
            const prompts = await entry.client.listPrompts();
            prompts.forEach((promptName) => {
              const finalPromptName = allPrompts.has(promptName) ? `${name}.${promptName}` : promptName;
              allPrompts.add(finalPromptName);
              this.promptClientMap.set(finalPromptName, name);
            });
            if (prompts.length > 0) {
              this.logger.debug(
                `${LOG_PREFIXES.MANAGER} Retrieved ${prompts.length} prompts from ${name}`,
                { clientName: name, promptCount: prompts.length }
              );
            }
          } catch (error) {
            const errorMessage = error instanceof Error ? error.message : String(error);
            errors.push(`${name}: ${errorMessage}`);
            this._updateClientFailure(name);
            this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to list prompts from ${name}`, {
              clientName: name,
              error: errorMessage
            });
          }
        });
        await Promise.allSettled(promptPromises);
        const promptList = Array.from(allPrompts);
        if (!this.quietMode) {
          this.logger.info(`${LOG_PREFIXES.MANAGER} Retrieved ${promptList.length} total prompts`, {
            promptCount: promptList.length,
            clientCount: this.clients.size
          });
        }
        return promptList;
      }
      /**
       * Get the client that provides a specific prompt.
       */
      getPromptClient(promptName) {
        const clientName = this.promptClientMap.get(promptName);
        if (!clientName) {
          return void 0;
        }
        const entry = this.clients.get(clientName);
        return entry?.connected ? entry.client : void 0;
      }
      /**
       * Get a prompt by name.
       */
      async getPrompt(name, args) {
        const client = this.getPromptClient(name);
        if (!client) {
          await this._refreshPromptCache();
          const refreshedClient = this.getPromptClient(name);
          if (!refreshedClient) {
            throw new Error(`${ERROR_MESSAGES.NO_CLIENT_FOR_PROMPT}: ${name}`);
          }
          return refreshedClient.getPrompt(name, args);
        }
        this.logger.info(`${LOG_PREFIXES.MANAGER} Getting prompt: ${name}`, {
          promptName: name,
          hasArgs: !!args
        });
        try {
          const result = await client.getPrompt(name, args);
          this.logger.info(`${LOG_PREFIXES.MANAGER} Prompt retrieved successfully: ${name}`, {
            promptName: name
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to get prompt: ${name}`, {
            promptName: name,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * List all available resources from all connected clients.
       */
      async listAllResources() {
        const allResources = /* @__PURE__ */ new Set();
        const errors = [];
        const resourcePromises = Array.from(this.clients.entries()).map(async ([name, entry]) => {
          if (!entry.connected) {
            return;
          }
          try {
            const resources = await entry.client.listResources();
            resources.forEach((resourceUri) => {
              allResources.add(resourceUri);
              this.resourceClientMap.set(resourceUri, name);
            });
            if (resources.length > 0) {
              this.logger.debug(
                `${LOG_PREFIXES.MANAGER} Retrieved ${resources.length} resources from ${name}`,
                { clientName: name, resourceCount: resources.length }
              );
            }
          } catch (error) {
            const errorMessage = error instanceof Error ? error.message : String(error);
            errors.push(`${name}: ${errorMessage}`);
            this._updateClientFailure(name);
            this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to list resources from ${name}`, {
              clientName: name,
              error: errorMessage
            });
          }
        });
        await Promise.allSettled(resourcePromises);
        const resourceList = Array.from(allResources);
        if (!this.quietMode) {
          this.logger.info(`${LOG_PREFIXES.MANAGER} Retrieved ${resourceList.length} total resources`, {
            resourceCount: resourceList.length,
            clientCount: this.clients.size
          });
        }
        return resourceList;
      }
      /**
       * Get the client that provides a specific resource.
       */
      getResourceClient(resourceUri) {
        const clientName = this.resourceClientMap.get(resourceUri);
        if (!clientName) {
          return void 0;
        }
        const entry = this.clients.get(clientName);
        return entry?.connected ? entry.client : void 0;
      }
      /**
       * Read a resource by URI.
       */
      async readResource(uri) {
        const client = this.getResourceClient(uri);
        if (!client) {
          await this._refreshResourceCache();
          const refreshedClient = this.getResourceClient(uri);
          if (!refreshedClient) {
            throw new Error(`${ERROR_MESSAGES.NO_CLIENT_FOR_RESOURCE}: ${uri}`);
          }
          return refreshedClient.readResource(uri);
        }
        this.logger.info(`${LOG_PREFIXES.MANAGER} Reading resource: ${uri}`, { resourceUri: uri });
        try {
          const result = await client.readResource(uri);
          this.logger.info(`${LOG_PREFIXES.MANAGER} Resource read successfully: ${uri}`, {
            resourceUri: uri
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to read resource: ${uri}`, {
            resourceUri: uri,
            error: errorMessage
          });
          throw error;
        }
      }
      /**
       * Initialize clients from server configurations.
       */
      async initializeFromConfig(serverConfigs) {
        const enabledServers = Object.entries(serverConfigs).filter(
          ([, config2]) => config2.enabled !== false
        );
        if (!this.quietMode) {
          this.logger.info(
            `${LOG_PREFIXES.MANAGER} Initializing ${enabledServers.length} servers (${Object.keys(serverConfigs).length - enabledServers.length} disabled)`,
            {
              enabledServerCount: enabledServers.length,
              totalServerCount: Object.keys(serverConfigs).length,
              disabledServerCount: Object.keys(serverConfigs).length - enabledServers.length
            }
          );
        }
        const strictServers = [];
        const connectionPromises = [];
        for (const [name, config2] of Object.entries(serverConfigs)) {
          if (config2.enabled === false) {
            this.logger.info(`${LOG_PREFIXES.MANAGER} Skipping disabled server: ${name}`, {
              serverName: name
            });
            continue;
          }
          if (config2.connectionMode === CONNECTION_MODES.STRICT) {
            strictServers.push(name);
          }
          const connectionPromise = this.connectServer(name, config2);
          connectionPromises.push(connectionPromise);
        }
        const results = await Promise.allSettled(connectionPromises);
        const failedStrictServers = strictServers.filter(
          (name) => this.failedConnections[name] !== void 0
        );
        if (failedStrictServers.length > 0) {
          const errorMessage = `${ERROR_MESSAGES.MISSING_REQUIRED_SERVERS}: ${failedStrictServers.join(", ")}`;
          this.logger.error(`${LOG_PREFIXES.MANAGER} ${errorMessage}`, {
            failedServers: failedStrictServers
          });
          throw new Error(errorMessage);
        }
        const successCount = results.filter((result) => result.status === "fulfilled").length;
        const failureCount = results.filter((result) => result.status === "rejected").length;
        if (!this.quietMode) {
          this.logger.info(`${LOG_PREFIXES.MANAGER} Initialization complete`, {
            successCount,
            failureCount,
            totalCount: Object.keys(serverConfigs).length,
            strictServerCount: strictServers.length
          });
        }
      }
      /**
       * Connect to a new MCP server.
       */
      async connectServer(name, config2) {
        if (!this.quietMode) {
          this.logger.info(`${LOG_PREFIXES.MANAGER} Connecting to server: ${name}`, {
            serverName: name,
            transportType: config2.type,
            connectionMode: config2.connectionMode
          });
        }
        try {
          if (!this.clients.has(name)) {
            const client = new MCPClient();
            if (this.quietMode) {
              client.setQuietMode(true);
            }
            this.registerClient(name, client);
          }
          const entry = this.clients.get(name);
          entry.config = config2;
          await entry.client.connect(config2, name);
          entry.connected = true;
          entry.lastSeen = Date.now();
          entry.failureCount = 0;
          delete this.failedConnections[name];
          if (this.eventManager) {
            this.eventManager.emitServiceEvent(ServiceEvents.MCP_CLIENT_CONNECTED, {
              clientId: name,
              serverName: name,
              timestamp: Date.now()
            });
          }
          if (!this.quietMode) {
            this.logger.info(`${LOG_PREFIXES.MANAGER} Successfully connected to server: ${name}`, {
              serverName: name
            });
          }
          await this._refreshAllCaches();
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          this.failedConnections[name] = errorMessage;
          this._updateClientFailure(name);
          if (this.eventManager) {
            this.eventManager.emitServiceEvent(ServiceEvents.MCP_CLIENT_ERROR, {
              clientId: name,
              serverName: name,
              error: errorMessage,
              timestamp: Date.now()
            });
          }
          this.logger.error(`${LOG_PREFIXES.MANAGER} Failed to connect to server: ${name}`, {
            serverName: name,
            error: errorMessage,
            connectionMode: config2.connectionMode
          });
          if (config2.connectionMode === CONNECTION_MODES.STRICT) {
            throw error;
          }
        }
      }
      /**
       * Get all registered clients.
       */
      getClients() {
        const clientMap = /* @__PURE__ */ new Map();
        for (const [name, entry] of this.clients.entries()) {
          clientMap.set(name, entry.client);
        }
        return clientMap;
      }
      /**
       * Get errors from failed connections.
       */
      getFailedConnections() {
        return { ...this.failedConnections };
      }
      /**
       * Disconnect and remove a specific client.
       */
      async removeClient(name) {
        const entry = this.clients.get(name);
        if (!entry) {
          this.logger.warn(`${LOG_PREFIXES.MANAGER} Client not found for removal: ${name}`, {
            clientName: name
          });
          return;
        }
        this.logger.info(`${LOG_PREFIXES.MANAGER} Removing client: ${name}`, { clientName: name });
        try {
          if (entry.connected) {
            await entry.client.disconnect();
          }
        } catch (error) {
          this.logger.warn(
            `${LOG_PREFIXES.MANAGER} Error disconnecting client during removal: ${name}`,
            { clientName: name, error: error instanceof Error ? error.message : String(error) }
          );
        }
        if (this.eventManager) {
          this.eventManager.emitServiceEvent(ServiceEvents.MCP_CLIENT_DISCONNECTED, {
            clientId: name,
            serverName: name,
            reason: "Client removed",
            timestamp: Date.now()
          });
        }
        this.clients.delete(name);
        this._removeClientFromCaches(name);
        delete this.failedConnections[name];
        this.logger.info(`${LOG_PREFIXES.MANAGER} Client removed successfully: ${name}`, {
          clientName: name,
          remainingClients: this.clients.size
        });
      }
      /**
       * Disconnect all clients and clear caches.
       */
      async disconnectAll() {
        this.logger.info(`${LOG_PREFIXES.MANAGER} Disconnecting all clients`, {
          clientCount: this.clients.size
        });
        const disconnectPromises = Array.from(this.clients.entries()).map(async ([name, entry]) => {
          try {
            if (entry.connected) {
              await entry.client.disconnect();
            }
          } catch (error) {
            this.logger.warn(`${LOG_PREFIXES.MANAGER} Error disconnecting client: ${name}`, {
              clientName: name,
              error: error instanceof Error ? error.message : String(error)
            });
          }
        });
        await Promise.allSettled(disconnectPromises);
        this.clients.clear();
        this.failedConnections = {};
        this._clearAllCaches();
        this.logger.info(`${LOG_PREFIXES.MANAGER} All clients disconnected and caches cleared`);
      }
      // ======================================================
      // Private Cache Management Methods
      // ======================================================
      /**
       * Refresh tool cache from all connected clients.
       */
      async _refreshToolCache() {
        this.toolCache.clear();
        this.toolClientMap.clear();
        await this.getAllTools();
      }
      /**
       * Refresh prompt cache from all connected clients.
       */
      async _refreshPromptCache() {
        this.promptCache.clear();
        this.promptClientMap.clear();
        await this.listAllPrompts();
      }
      /**
       * Refresh resource cache from all connected clients.
       */
      async _refreshResourceCache() {
        this.resourceCache.clear();
        this.resourceClientMap.clear();
        await this.listAllResources();
      }
      /**
       * Refresh all caches.
       */
      async _refreshAllCaches() {
        await Promise.allSettled([
          this._refreshToolCache(),
          this._refreshPromptCache(),
          this._refreshResourceCache()
        ]);
      }
      /**
       * Clean up expired cache entries.
       */
      _cleanupCache(cache) {
        if (cache.size <= this.maxCacheSize) {
          return;
        }
        const now = Date.now();
        const entriesToRemove = [];
        for (const [key, entry] of cache.entries()) {
          if (now - entry.timestamp > this.cacheTimeout) {
            entriesToRemove.push(key);
          }
        }
        entriesToRemove.forEach((key) => cache.delete(key));
        if (cache.size > this.maxCacheSize) {
          const sortedEntries = Array.from(cache.entries()).sort(
            ([, a], [, b]) => a.timestamp - b.timestamp
          );
          const toRemove = sortedEntries.slice(0, cache.size - this.maxCacheSize);
          toRemove.forEach(([key]) => cache.delete(key));
        }
      }
      /**
       * Remove a client from all caches.
       */
      _removeClientFromCaches(clientName) {
        for (const [toolName, entry] of this.toolCache.entries()) {
          if (entry.clientName === clientName) {
            this.toolCache.delete(toolName);
            this.toolClientMap.delete(toolName);
          }
        }
        for (const [promptName, clientMapName] of this.promptClientMap.entries()) {
          if (clientMapName === clientName) {
            this.promptClientMap.delete(promptName);
          }
        }
        for (const [resourceUri, clientMapName] of this.resourceClientMap.entries()) {
          if (clientMapName === clientName) {
            this.resourceClientMap.delete(resourceUri);
          }
        }
      }
      /**
       * Clear all caches.
       */
      _clearAllCaches() {
        this.toolCache.clear();
        this.toolClientMap.clear();
        this.promptCache.clear();
        this.promptClientMap.clear();
        this.resourceCache.clear();
        this.resourceClientMap.clear();
      }
      /**
       * Update client failure count and connected status.
       */
      _updateClientFailure(clientName) {
        const entry = this.clients.get(clientName);
        if (entry) {
          entry.failureCount++;
          entry.connected = false;
          entry.lastSeen = Date.now();
        }
      }
    };
  }
});

// src/core/brain/systemPrompt/interfaces.ts
var ProviderType;
var init_interfaces = __esm({
  "src/core/brain/systemPrompt/interfaces.ts"() {
    init_cjs_shims();
    ProviderType = /* @__PURE__ */ ((ProviderType2) => {
      ProviderType2["STATIC"] = "static";
      ProviderType2["DYNAMIC"] = "dynamic";
      ProviderType2["FILE_BASED"] = "file-based";
      return ProviderType2;
    })(ProviderType || {});
  }
});

// src/core/brain/systemPrompt/config-manager.ts
var import_fs2, import_path2, SystemPromptConfigManager;
var init_config_manager = __esm({
  "src/core/brain/systemPrompt/config-manager.ts"() {
    init_cjs_shims();
    import_fs2 = require("fs");
    import_path2 = __toESM(require("path"), 1);
    init_interfaces();
    SystemPromptConfigManager = class {
      config = null;
      baseDir = process.cwd();
      /**
       * Load configuration from an object
       */
      loadFromObject(config2, options = {}) {
        this.baseDir = options.baseDir || process.cwd();
        if (options.validate !== false) {
          this.validateConfig(config2);
        }
        this.config = this.processConfiguration(config2, options);
      }
      /**
       * Load configuration from a JSON file
       */
      async loadFromFile(filePath, options = {}) {
        const fullPath = import_path2.default.isAbsolute(filePath) ? filePath : import_path2.default.resolve(options.baseDir || process.cwd(), filePath);
        this.baseDir = import_path2.default.dirname(fullPath);
        try {
          const fileContent = await import_fs2.promises.readFile(fullPath, "utf8");
          const rawConfig = JSON.parse(fileContent);
          if (options.validate !== false) {
            this.validateConfig(rawConfig);
          }
          this.config = this.processConfiguration(rawConfig, options);
        } catch (error) {
          throw new Error(
            `Failed to load configuration from ${filePath}: ${error instanceof Error ? error.message : "Unknown error"}`
          );
        }
      }
      /**
       * Get the current configuration
       */
      getConfig() {
        if (!this.config) {
          throw new Error("Configuration not loaded. Call loadFromObject() or loadFromFile() first.");
        }
        return this.config;
      }
      /**
       * Get providers sorted by priority (highest first)
       */
      getProviders() {
        const config2 = this.getConfig();
        return [...config2.providers].sort((a, b) => b.priority - a.priority);
      }
      /**
       * Get enabled providers sorted by priority
       */
      getEnabledProviders() {
        return this.getProviders().filter((provider) => provider.enabled);
      }
      /**
       * Get a specific provider by name
       */
      getProvider(name) {
        const config2 = this.getConfig();
        return config2.providers.find((provider) => provider.name === name);
      }
      /**
       * Check if configuration is loaded
       */
      isLoaded() {
        return this.config !== null;
      }
      /**
       * Get configuration settings
       */
      getSettings() {
        const config2 = this.getConfig();
        return config2.settings;
      }
      /**
       * Create a default configuration
       */
      static createDefault() {
        return {
          providers: [
            {
              name: "built-in-instructions",
              type: "static" /* STATIC */,
              priority: 0,
              enabled: true,
              config: {
                content: "# Built-in System Instructions\nThis section contains built-in tool instructions and agent behavior guidelines."
              }
            }
          ],
          settings: {
            maxGenerationTime: 5e3,
            failOnProviderError: false,
            contentSeparator: "\n\n"
          }
        };
      }
      /**
       * Validate configuration structure and content
       */
      validateConfig(config2) {
        if (!config2 || typeof config2 !== "object") {
          throw new Error("Configuration must be an object");
        }
        if (!Array.isArray(config2.providers)) {
          throw new Error('Configuration must have a "providers" array');
        }
        config2.providers.forEach((provider, index) => {
          this.validateProvider(provider, index);
        });
        if (!config2.settings || typeof config2.settings !== "object") {
          throw new Error('Configuration must have a "settings" object');
        }
        this.validateSettings(config2.settings);
      }
      /**
       * Validate a single provider configuration
       */
      validateProvider(provider, index) {
        const prefix = `Provider at index ${index}`;
        if (!provider || typeof provider !== "object") {
          throw new Error(`${prefix} must be an object`);
        }
        if (typeof provider.name !== "string" || !provider.name.trim()) {
          throw new Error(`${prefix} must have a non-empty "name" string`);
        }
        if (typeof provider.type !== "string" || !Object.values(ProviderType).includes(provider.type)) {
          throw new Error(
            `${prefix} must have a valid "type" (${Object.values(ProviderType).join(", ")})`
          );
        }
        if (typeof provider.priority !== "number") {
          throw new Error(`${prefix} must have a numeric "priority"`);
        }
        if (typeof provider.enabled !== "boolean") {
          throw new Error(`${prefix} must have a boolean "enabled" field`);
        }
        if (provider.config !== void 0 && (typeof provider.config !== "object" || provider.config === null)) {
          throw new Error(`${prefix} "config" must be an object if provided`);
        }
      }
      /**
       * Validate settings configuration
       */
      validateSettings(settings) {
        if (typeof settings.maxGenerationTime !== "number" || settings.maxGenerationTime <= 0) {
          throw new Error('Settings "maxGenerationTime" must be a positive number');
        }
        if (typeof settings.failOnProviderError !== "boolean") {
          throw new Error('Settings "failOnProviderError" must be a boolean');
        }
        if (typeof settings.contentSeparator !== "string") {
          throw new Error('Settings "contentSeparator" must be a string');
        }
      }
      /**
       * Process configuration with environment variable replacement and path resolution
       */
      processConfiguration(config2, options) {
        const processed = JSON.parse(JSON.stringify(config2));
        if (options.envVariables) {
          this.replaceEnvironmentVariables(processed, options.envVariables);
        }
        if (Array.isArray(processed.providers)) {
          processed.providers.forEach((provider) => {
            if (provider.type === "file-based" /* FILE_BASED */ && provider.config?.filePath) {
              if (!import_path2.default.isAbsolute(provider.config.filePath)) {
                provider.config.filePath = import_path2.default.resolve(this.baseDir, provider.config.filePath);
              }
            }
          });
        }
        return processed;
      }
      /**
       * Replace environment variables in configuration
       */
      replaceEnvironmentVariables(obj, envVars) {
        for (const key in obj) {
          if (typeof obj[key] === "string") {
            obj[key] = obj[key].replace(/\$\{([^}]+)\}/g, (match, varName) => {
              return envVars[varName] || match;
            });
          } else if (typeof obj[key] === "object" && obj[key] !== null) {
            this.replaceEnvironmentVariables(obj[key], envVars);
          }
        }
      }
    };
  }
});

// src/core/brain/systemPrompt/providers/base-provider.ts
var BasePromptProvider;
var init_base_provider = __esm({
  "src/core/brain/systemPrompt/providers/base-provider.ts"() {
    init_cjs_shims();
    BasePromptProvider = class {
      id;
      name;
      type;
      priority;
      enabled;
      config = {};
      initialized = false;
      constructor(id, name, type, priority, enabled = true) {
        this.id = id;
        this.name = name;
        this.type = type;
        this.priority = priority;
        this.enabled = enabled;
      }
      /**
       * Default validation - can be overridden by concrete providers
       */
      validateConfig(config2) {
        return typeof config2 === "object" && config2 !== null;
      }
      /**
       * Initialize the provider with configuration
       */
      async initialize(config2) {
        if (!this.validateConfig(config2)) {
          throw new Error(`Invalid configuration for provider ${this.id}`);
        }
        this.config = { ...config2 };
        this.initialized = true;
      }
      /**
       * Default cleanup - can be overridden by concrete providers
       */
      async destroy() {
        this.config = {};
        this.initialized = false;
      }
      /**
       * Check if provider is initialized
       */
      ensureInitialized() {
        if (!this.initialized) {
          throw new Error(`Provider ${this.id} is not initialized`);
        }
      }
      /**
       * Check if provider is enabled and initialized
       */
      canGenerate() {
        return this.enabled && this.initialized;
      }
    };
  }
});

// src/core/brain/systemPrompt/providers/static-provider.ts
var StaticPromptProvider;
var init_static_provider = __esm({
  "src/core/brain/systemPrompt/providers/static-provider.ts"() {
    init_cjs_shims();
    init_interfaces();
    init_base_provider();
    StaticPromptProvider = class extends BasePromptProvider {
      content = "";
      variables = {};
      constructor(id, name, priority, enabled = true) {
        super(id, name, "static" /* STATIC */, priority, enabled);
      }
      validateConfig(config2) {
        if (!super.validateConfig(config2)) {
          return false;
        }
        const typedConfig = config2;
        if (typeof typedConfig.content !== "string") {
          return false;
        }
        if (typedConfig.variables !== void 0) {
          if (typeof typedConfig.variables !== "object" || typedConfig.variables === null) {
            return false;
          }
          for (const [key, value] of Object.entries(typedConfig.variables)) {
            if (typeof key !== "string" || typeof value !== "string") {
              return false;
            }
          }
        }
        return true;
      }
      async initialize(config2) {
        await super.initialize(config2);
        const typedConfig = config2;
        this.content = typedConfig.content;
        this.variables = typedConfig.variables || {};
      }
      async generateContent(_context) {
        this.ensureInitialized();
        if (!this.canGenerate()) {
          return "";
        }
        let result = this.content;
        for (const [key, value] of Object.entries(this.variables)) {
          const placeholder = `{{${key}}}`;
          result = result.replace(new RegExp(placeholder, "g"), value);
        }
        return result;
      }
      async destroy() {
        await super.destroy();
        this.content = "";
        this.variables = {};
      }
    };
  }
});

// src/core/brain/systemPrompt/providers/dynamic-provider.ts
var dynamic_provider_exports = {};
__export(dynamic_provider_exports, {
  DynamicPromptProvider: () => DynamicPromptProvider
});
var DynamicPromptProvider;
var init_dynamic_provider = __esm({
  "src/core/brain/systemPrompt/providers/dynamic-provider.ts"() {
    init_cjs_shims();
    init_interfaces();
    init_base_provider();
    DynamicPromptProvider = class _DynamicPromptProvider extends BasePromptProvider {
      generatorName = "";
      generatorConfig = {};
      template = "";
      generatorFunction;
      // Cached content for this provider
      cachedContent = null;
      // Static registry of generator functions
      static generators = /* @__PURE__ */ new Map();
      constructor(id, name, priority, enabled = true) {
        super(id, name, "dynamic" /* DYNAMIC */, priority, enabled);
      }
      /**
       * Register a dynamic content generator function
       */
      static registerGenerator(name, generator) {
        _DynamicPromptProvider.generators.set(name, generator);
      }
      /**
       * Get all registered generator names
       */
      static getRegisteredGenerators() {
        return Array.from(_DynamicPromptProvider.generators.keys());
      }
      /**
       * Check if a generator is registered
       */
      static isGeneratorRegistered(name) {
        return _DynamicPromptProvider.generators.has(name);
      }
      validateConfig(config2) {
        if (!super.validateConfig(config2)) {
          return false;
        }
        const typedConfig = config2;
        if (typeof typedConfig.generator !== "string" || !typedConfig.generator.trim()) {
          return false;
        }
        if (!_DynamicPromptProvider.isGeneratorRegistered(typedConfig.generator)) {
          return false;
        }
        if (typedConfig.generatorConfig !== void 0) {
          if (typeof typedConfig.generatorConfig !== "object" || typedConfig.generatorConfig === null) {
            return false;
          }
        }
        if (typedConfig.template !== void 0) {
          if (typeof typedConfig.template !== "string") {
            return false;
          }
        }
        return true;
      }
      async initialize(config2) {
        await super.initialize(config2);
        const typedConfig = config2;
        this.generatorName = typedConfig.generator;
        this.generatorConfig = typedConfig.generatorConfig || {};
        this.template = typedConfig.template || "{{content}}";
        const generatorFunction = _DynamicPromptProvider.generators.get(this.generatorName);
        if (!generatorFunction) {
          throw new Error(`Generator function '${this.generatorName}' not found`);
        }
        this.generatorFunction = generatorFunction;
      }
      async generateContent(context) {
        this.ensureInitialized();
        if (!this.canGenerate() || !this.generatorFunction) {
          return "";
        }
        if (this.cachedContent !== null) {
          return this.cachedContent;
        }
        try {
          const dynamicContent = await this.generatorFunction(context, this.generatorConfig);
          const result = this.template.replace("{{content}}", dynamicContent);
          return result;
        } catch (error) {
          throw new Error(
            `Failed to generate dynamic content: ${error instanceof Error ? error.message : "Unknown error"}`
          );
        }
      }
      /**
       * Set the cached content for this provider
       */
      setCachedContent(content) {
        this.cachedContent = content;
      }
      /**
       * Get the cached content for this provider
       */
      getCachedContent() {
        return this.cachedContent;
      }
      /**
       * Clear the cached content (for cache invalidation)
       */
      clearCachedContent() {
        this.cachedContent = null;
      }
      async destroy() {
        await super.destroy();
        this.generatorName = "";
        this.generatorConfig = {};
        this.template = "";
        this.generatorFunction = void 0;
        this.cachedContent = null;
      }
    };
  }
});

// src/core/brain/systemPrompt/providers/file-provider.ts
var import_fs3, import_path3, FilePromptProvider;
var init_file_provider = __esm({
  "src/core/brain/systemPrompt/providers/file-provider.ts"() {
    init_cjs_shims();
    import_fs3 = require("fs");
    import_path3 = __toESM(require("path"), 1);
    init_interfaces();
    init_base_provider();
    FilePromptProvider = class extends BasePromptProvider {
      filePath = "";
      baseDir = "";
      watchForChanges = false;
      encoding = "utf8";
      variables = {};
      cachedContent = "";
      lastModified = /* @__PURE__ */ new Date(0);
      constructor(id, name, priority, enabled = true) {
        super(id, name, "file-based" /* FILE_BASED */, priority, enabled);
      }
      validateConfig(config2) {
        if (!super.validateConfig(config2)) {
          return false;
        }
        const typedConfig = config2;
        if (typeof typedConfig.filePath !== "string" || !typedConfig.filePath.trim()) {
          return false;
        }
        if (typedConfig.baseDir !== void 0) {
          if (typeof typedConfig.baseDir !== "string") {
            return false;
          }
        }
        if (typedConfig.watchForChanges !== void 0) {
          if (typeof typedConfig.watchForChanges !== "boolean") {
            return false;
          }
        }
        if (typedConfig.encoding !== void 0) {
          if (typeof typedConfig.encoding !== "string") {
            return false;
          }
        }
        if (typedConfig.variables !== void 0) {
          if (typeof typedConfig.variables !== "object" || typedConfig.variables === null) {
            return false;
          }
          for (const [key, value] of Object.entries(typedConfig.variables)) {
            if (typeof key !== "string" || typeof value !== "string") {
              return false;
            }
          }
        }
        if (typedConfig.summarize !== void 0) {
          if (typeof typedConfig.summarize !== "boolean") {
            return false;
          }
        }
        return true;
      }
      async initialize(config2) {
        await super.initialize(config2);
        const typedConfig = config2;
        this.filePath = typedConfig.filePath;
        this.baseDir = typedConfig.baseDir || process.cwd();
        this.watchForChanges = typedConfig.watchForChanges || false;
        this.encoding = typedConfig.encoding || "utf8";
        this.variables = typedConfig.variables || {};
        await this.loadFileContent();
      }
      async generateContent(context) {
        this.ensureInitialized();
        if (!this.canGenerate()) {
          return "";
        }
        if (this.watchForChanges) {
          await this.checkAndReloadFile();
        }
        let result = this.cachedContent;
        for (const [key, value] of Object.entries(this.variables)) {
          const placeholder = `{{${key}}}`;
          result = result.replace(new RegExp(placeholder, "g"), value);
        }
        const summarize = this.config?.summarize === true || context?.summarize === true;
        if (summarize) {
          const llmService = context?.metadata?.llmService;
          if (!llmService) {
            return "[LLM service unavailable: cannot summarize file]";
          }
          if (this._cachedSummary && this._cachedSummarySource === result) {
            return this._cachedSummary;
          }
          const prompt = `Summarize the following project document for the system prompt. Focus on key guidelines, rules, and standards. Keep the summary concise, maximum 4-5 sentences.

${result}

Summary:`;
          const summary = await llmService.directGenerate(prompt);
          this._cachedSummary = summary;
          this._cachedSummarySource = result;
          return summary;
        }
        return result;
      }
      async destroy() {
        await super.destroy();
        this.filePath = "";
        this.baseDir = "";
        this.watchForChanges = false;
        this.encoding = "utf8";
        this.variables = {};
        this.cachedContent = "";
        this.lastModified = /* @__PURE__ */ new Date(0);
      }
      /**
       * Get the full file path (resolves relative paths)
       */
      getFullPath() {
        if (import_path3.default.isAbsolute(this.filePath)) {
          return this.filePath;
        }
        return import_path3.default.resolve(this.baseDir, this.filePath);
      }
      /**
       * Load file content and cache it
       */
      async loadFileContent() {
        try {
          const fullPath = this.getFullPath();
          const stats = await import_fs3.promises.stat(fullPath);
          this.lastModified = stats.mtime;
          this.cachedContent = await import_fs3.promises.readFile(fullPath, "utf8");
        } catch (error) {
          throw new Error(
            `Failed to load file ${this.filePath}: ${error instanceof Error ? error.message : "Unknown error"}`
          );
        }
      }
      /**
       * Check if file has been modified and reload if necessary
       */
      async checkAndReloadFile() {
        try {
          const fullPath = this.getFullPath();
          const stats = await import_fs3.promises.stat(fullPath);
          if (stats.mtime > this.lastModified) {
            await this.loadFileContent();
          }
        } catch (error) {
          console.warn(
            `Failed to check file ${this.filePath}: ${error instanceof Error ? error.message : "Unknown error"}`
          );
        }
      }
    };
  }
});

// src/core/brain/systemPrompt/registry.ts
var DefaultProviderRegistry, providerRegistry;
var init_registry = __esm({
  "src/core/brain/systemPrompt/registry.ts"() {
    init_cjs_shims();
    init_interfaces();
    init_static_provider();
    init_dynamic_provider();
    init_file_provider();
    DefaultProviderRegistry = class {
      generators = /* @__PURE__ */ new Map();
      constructor() {
        this.registerBuiltInProviders();
      }
      /**
       * Register a provider generator function
       */
      register(type, generator) {
        if (!type || typeof type !== "string") {
          throw new Error("Provider type must be a non-empty string");
        }
        if (typeof generator !== "function") {
          throw new Error("Generator must be a function");
        }
        this.generators.set(type, generator);
      }
      /**
       * Create a provider instance
       */
      async create(config2) {
        if (!this.isRegistered(config2.type)) {
          throw new Error(`Provider type '${config2.type}' is not registered`);
        }
        const generator = this.generators.get(config2.type);
        try {
          const provider = await generator(config2);
          return provider;
        } catch (error) {
          throw new Error(
            `Failed to create provider '${config2.name}': ${error instanceof Error ? error.message : "Unknown error"}`
          );
        }
      }
      /**
       * Get all registered provider types
       */
      getRegisteredTypes() {
        return Array.from(this.generators.keys());
      }
      /**
       * Check if a provider type is registered
       */
      isRegistered(type) {
        return this.generators.has(type);
      }
      /**
       * Unregister a provider type
       */
      unregister(type) {
        return this.generators.delete(type);
      }
      /**
       * Clear all registered providers
       */
      clear() {
        this.generators.clear();
      }
      /**
       * Register built-in provider types
       */
      registerBuiltInProviders() {
        this.register("static" /* STATIC */, async (config2) => {
          const provider = new StaticPromptProvider(
            config2.name,
            config2.name,
            config2.priority,
            config2.enabled
          );
          if (config2.config) {
            await provider.initialize(config2.config);
          }
          return provider;
        });
        this.register("dynamic" /* DYNAMIC */, async (config2) => {
          const provider = new DynamicPromptProvider(
            config2.name,
            config2.name,
            config2.priority,
            config2.enabled
          );
          if (config2.config) {
            await provider.initialize(config2.config);
          }
          return provider;
        });
        this.register("file-based" /* FILE_BASED */, async (config2) => {
          const provider = new FilePromptProvider(
            config2.name,
            config2.name,
            config2.priority,
            config2.enabled
          );
          if (config2.config) {
            await provider.initialize(config2.config);
          }
          return provider;
        });
      }
    };
    providerRegistry = new DefaultProviderRegistry();
  }
});

// src/core/brain/llm/messages/history/database.ts
var MESSAGE_LIMIT, STORAGE_KEY_PREFIX, DatabaseHistoryProvider;
var init_database = __esm({
  "src/core/brain/llm/messages/history/database.ts"() {
    init_cjs_shims();
    init_logger2();
    MESSAGE_LIMIT = 1e3;
    STORAGE_KEY_PREFIX = "messages:";
    DatabaseHistoryProvider = class {
      storageManager;
      constructor(storageManager) {
        this.storageManager = storageManager;
        const backends = this.storageManager.getBackends();
        if (!backends || typeof backends.database?.get !== "function" || typeof backends.database?.set !== "function") {
          throw new Error(
            "DatabaseHistoryProvider: StorageManager database backend is not compatible."
          );
        }
      }
      getKey(sessionId) {
        return `${STORAGE_KEY_PREFIX}${sessionId}`;
      }
      /**
       * Retrieve up to the most recent `limit` messages for a session, in chronological order (oldest to newest).
       */
      async getHistory(sessionId, limit = MESSAGE_LIMIT) {
        const key = this.getKey(sessionId);
        try {
          const backends = this.storageManager.getBackends();
          const messages = await backends?.database.get(key) || [];
          const result = messages.slice(-limit);
          return result;
        } catch (err) {
          logger.error(
            `DatabaseHistoryProvider.getHistory failed for session ${sessionId}: ${err.message}`
          );
          return [];
        }
      }
      /**
       * Save a message to the session's history, enforcing message limit and order.
       */
      async saveMessage(sessionId, message) {
        const key = this.getKey(sessionId);
        try {
          const backends = this.storageManager.getBackends();
          const messages = await backends?.database.get(key) || [];
          messages.push(message);
          const trimmed = messages.slice(-MESSAGE_LIMIT);
          await backends?.database.set(key, trimmed);
        } catch (err) {
          logger.error(
            `DatabaseHistoryProvider.saveMessage failed for session ${sessionId}: ${err.message}`
          );
        }
      }
      /**
       * Clear all conversation history for a session.
       */
      async clearHistory(sessionId) {
        const key = this.getKey(sessionId);
        try {
          const backends = this.storageManager.getBackends();
          await backends?.database.delete(key);
          logger.info(`DatabaseHistoryProvider: Cleared conversation history for session ${sessionId}`);
        } catch (err) {
          logger.error(
            `DatabaseHistoryProvider.clearHistory failed for session ${sessionId}: ${err.message}`
          );
        }
      }
    };
  }
});

// src/core/brain/llm/messages/history/multi-backend.ts
var MultiBackendHistoryProvider;
var init_multi_backend = __esm({
  "src/core/brain/llm/messages/history/multi-backend.ts"() {
    init_cjs_shims();
    MultiBackendHistoryProvider = class {
      constructor(primary, backup, wal, flushIntervalMs = 5e3) {
        this.primary = primary;
        this.backup = backup;
        this.wal = wal;
        this.flushIntervalMs = flushIntervalMs;
        this.startWALFlushWorker();
      }
      walFlushInterval = null;
      async getHistory(sessionId, limit) {
        try {
          return await this.primary.getHistory(sessionId, limit);
        } catch (err) {
          console.error("[MultiBackend] Primary getHistory failed, falling back to backup:", err);
          return await this.backup.getHistory(sessionId, limit);
        }
      }
      async saveMessage(sessionId, message) {
        try {
          await this.primary.saveMessage(sessionId, message);
        } catch (err) {
          console.error("[MultiBackend] Primary saveMessage failed:", err);
          throw err;
        }
        try {
          await this.wal.saveMessage(sessionId, message);
        } catch (err) {
          console.error("[MultiBackend] WAL saveMessage failed:", err);
          throw err;
        }
      }
      async clearHistory(sessionId) {
        try {
          await this.primary.clearHistory(sessionId);
        } catch (err) {
          console.error("[MultiBackend] Primary clearHistory failed:", err);
        }
        try {
          await this.backup.clearHistory(sessionId);
        } catch (err) {
          console.error("[MultiBackend] Backup clearHistory failed:", err);
        }
        try {
          await this.wal.clearHistory(sessionId);
        } catch (err) {
          console.error("[MultiBackend] WAL clearHistory failed:", err);
        }
      }
      startWALFlushWorker() {
        if (this.walFlushInterval) return;
        this.walFlushInterval = setInterval(async () => {
          try {
            const pending = await this.wal.getPendingEntries();
            for (const { sessionId, message } of pending) {
              try {
                await this.backup.saveMessage(sessionId, message);
                await this.wal.markFlushed(sessionId, message);
              } catch (err) {
                console.error("[MultiBackend] Backup saveMessage failed during WAL flush:", err);
              }
            }
          } catch (err) {
            console.error("[MultiBackend] WAL flush worker error:", err);
          }
        }, this.flushIntervalMs);
      }
      async disconnect() {
        if (this.walFlushInterval) {
          clearInterval(this.walFlushInterval);
          this.walFlushInterval = null;
        }
        if (typeof this.primary.disconnect === "function") {
          await this.primary.disconnect();
        }
        if (typeof this.backup.disconnect === "function") {
          await this.backup.disconnect();
        }
        if (typeof this.wal.disconnect === "function") {
          await this.wal.disconnect();
        }
      }
    };
  }
});

// src/core/brain/llm/messages/history/wal.ts
var WALHistoryProvider;
var init_wal = __esm({
  "src/core/brain/llm/messages/history/wal.ts"() {
    init_cjs_shims();
    WALHistoryProvider = class {
      log = [];
      async getHistory(sessionId, _limit) {
        return this.log.filter((e) => e.sessionId === sessionId).map((e) => e.message);
      }
      async saveMessage(sessionId, message) {
        this.log.push({ sessionId, message, flushed: false });
      }
      async clearHistory(sessionId) {
        this.log = this.log.filter((e) => e.sessionId !== sessionId);
      }
      async getPendingEntries() {
        return this.log.filter((e) => !e.flushed).map((e) => ({ sessionId: e.sessionId, message: e.message }));
      }
      async markFlushed(sessionId, message) {
        const entry = this.log.find(
          (e) => e.sessionId === sessionId && e.message === message && !e.flushed
        );
        if (entry) entry.flushed = true;
      }
      async disconnect() {
      }
    };
  }
});

// src/core/brain/llm/messages/history/factory.ts
var factory_exports = {};
__export(factory_exports, {
  createDatabaseHistoryProvider: () => createDatabaseHistoryProvider,
  createMultiBackendHistoryProvider: () => createMultiBackendHistoryProvider
});
function createDatabaseHistoryProvider(storage) {
  return new DatabaseHistoryProvider(storage);
}
function createMultiBackendHistoryProvider(primary, backup, wal, flushIntervalMs = 5e3) {
  if (!wal) wal = new WALHistoryProvider();
  return new MultiBackendHistoryProvider(primary, backup, wal, flushIntervalMs);
}
var init_factory = __esm({
  "src/core/brain/llm/messages/history/factory.ts"() {
    init_cjs_shims();
    init_database();
    init_multi_backend();
    init_wal();
  }
});

// src/core/brain/systemPrompt/built-in-generators.ts
function evaluateCondition(condition, context) {
  if (typeof condition === "string") {
    return !!context[condition];
  }
  if (typeof condition === "object" && condition !== null) {
    const { field, operator, value } = condition;
    const fieldValue = context[field];
    switch (operator) {
      case "exists":
        return fieldValue !== void 0 && fieldValue !== null && fieldValue !== "";
      case "equals":
        return fieldValue === value;
      case "contains":
        return typeof fieldValue === "string" && fieldValue.includes(value);
      case "gt":
        return typeof fieldValue === "number" && fieldValue > value;
      case "lt":
        return typeof fieldValue === "number" && fieldValue < value;
      default:
        return false;
    }
  }
  return false;
}
async function fetchSessionHistory(context, config2) {
  const sessionId = context.sessionId;
  const historyConfig = config2.history || "all";
  const metadata = context.metadata || {};
  const storageManager = metadata.storageManager;
  if (!sessionId || !storageManager) return [];
  const { createDatabaseHistoryProvider: createDatabaseHistoryProvider2 } = await Promise.resolve().then(() => (init_factory(), factory_exports));
  const historyProvider = createDatabaseHistoryProvider2(storageManager);
  let limit = 1e3;
  if (typeof historyConfig === "number") limit = historyConfig;
  if (typeof historyConfig === "string" && historyConfig !== "all")
    limit = parseInt(historyConfig, 10) || 1e3;
  const messages = await historyProvider.getHistory(sessionId, limit);
  return messages.map((m) => {
    if (typeof m.content === "string") return m.content;
    if (Array.isArray(m.content)) {
      return m.content.map((seg) => seg.type === "text" ? seg.text : "[Image]").join(" ");
    }
    return "";
  });
}
function getLLMService(context) {
  return context.metadata?.llmService;
}
async function registerBuiltInGenerators() {
  const { DynamicPromptProvider: DynamicPromptProvider2 } = await Promise.resolve().then(() => (init_dynamic_provider(), dynamic_provider_exports));
  DynamicPromptProvider2.registerGenerator("timestamp", timestampGenerator);
  DynamicPromptProvider2.registerGenerator("session-context", sessionContextGenerator);
  DynamicPromptProvider2.registerGenerator("memory-context", memoryContextGenerator);
  DynamicPromptProvider2.registerGenerator("environment", environmentGenerator);
  DynamicPromptProvider2.registerGenerator("conditional", conditionalGenerator);
  DynamicPromptProvider2.registerGenerator("summary", summaryGenerator);
  DynamicPromptProvider2.registerGenerator("rules", rulesGenerator);
  DynamicPromptProvider2.registerGenerator("error-detection", errorDetectionGenerator);
}
var timestampGenerator, sessionContextGenerator, memoryContextGenerator, environmentGenerator, conditionalGenerator, summaryGenerator, rulesGenerator, errorDetectionGenerator;
var init_built_in_generators = __esm({
  "src/core/brain/systemPrompt/built-in-generators.ts"() {
    init_cjs_shims();
    timestampGenerator = async (context, config2) => {
      const format = config2.format || "iso";
      const includeTimezone = config2.includeTimezone || false;
      switch (format) {
        case "iso":
          return context.timestamp.toISOString();
        case "locale": {
          const options = {
            year: "numeric",
            month: "long",
            day: "numeric",
            hour: "2-digit",
            minute: "2-digit",
            ...includeTimezone && { timeZoneName: "short" }
          };
          return context.timestamp.toLocaleString("en-US", options);
        }
        case "date-only":
          return context.timestamp.toISOString().split("T")[0];
        case "time-only":
          return context.timestamp.toISOString().split("T")[1].split(".")[0];
        default:
          return context.timestamp.toString();
      }
    };
    sessionContextGenerator = async (context, config2) => {
      const includeFields = config2.includeFields || ["sessionId", "userId"];
      const format = config2.format || "list";
      const contextInfo = [];
      if (includeFields.includes("sessionId") && context.sessionId) {
        contextInfo.push(`Session ID: ${context.sessionId}`);
      }
      if (includeFields.includes("userId") && context.userId) {
        contextInfo.push(`User ID: ${context.userId}`);
      }
      if (includeFields.includes("timestamp")) {
        contextInfo.push(`Timestamp: ${context.timestamp.toISOString()}`);
      }
      if (contextInfo.length === 0) {
        return "";
      }
      switch (format) {
        case "list": {
          return contextInfo.join("\n");
        }
        case "inline": {
          return contextInfo.join(", ");
        }
        case "json": {
          const obj = {};
          if (includeFields.includes("sessionId") && context.sessionId)
            obj.sessionId = context.sessionId;
          if (includeFields.includes("userId") && context.userId) obj.userId = context.userId;
          if (includeFields.includes("timestamp")) obj.timestamp = context.timestamp.toISOString();
          return JSON.stringify(obj, null, 2);
        }
        default: {
          return contextInfo.join("\n");
        }
      }
    };
    memoryContextGenerator = async (context, config2) => {
      if (!context.memoryContext) {
        return config2.emptyMessage || "No memory context available";
      }
      const format = config2.format || "summary";
      const maxItems = config2.maxItems || 5;
      switch (format) {
        case "summary": {
          const itemCount = Object.keys(context.memoryContext).length;
          return `Memory context contains ${itemCount} items`;
        }
        case "list": {
          const items = Object.entries(context.memoryContext).slice(0, maxItems);
          return items.map(([key, value]) => `${key}: ${JSON.stringify(value)}`).join("\n");
        }
        case "json": {
          return JSON.stringify(context.memoryContext, null, 2);
        }
        default: {
          return "Memory context is available";
        }
      }
    };
    environmentGenerator = async (context, config2) => {
      const environment = config2.environment || "production";
      const customMessages = config2.messages || {};
      const defaultMessages = {
        development: "Development mode: Enhanced logging and debugging features are available.",
        staging: "Staging environment: Please verify all changes before promoting to production.",
        production: "Production environment: Exercise caution with all operations.",
        testing: "Testing mode: Automated testing features are enabled."
      };
      return customMessages[environment] || defaultMessages[environment] || `Environment: ${environment}`;
    };
    conditionalGenerator = async (context, config2) => {
      const conditions = config2.conditions || [];
      for (const condition of conditions) {
        if (evaluateCondition(condition.if, context)) {
          return condition.then || "";
        }
      }
      return config2.else || "";
    };
    summaryGenerator = async (context, config2) => {
      const llmService = getLLMService(context);
      if (!llmService) return "[LLM service unavailable: cannot generate summary]";
      const history = await fetchSessionHistory(context, config2);
      if (!history.length) return "[No session history to summarize]";
      const prompt = `Summarize the following conversation in a concise paragraph (maximum 4-5 sentences) for the system prompt.

${history.join("\n")}

Summary:`;
      return await llmService.directGenerate(prompt);
    };
    rulesGenerator = async (context, config2) => {
      const llmService = getLLMService(context);
      if (!llmService) return "[LLM service unavailable: cannot extract rules]";
      const history = await fetchSessionHistory(context, config2);
      if (!history.length) return "[No session history to extract rules from]";
      const prompt = `Extract any rules, requirements, or project specifications set by the user in the following conversation. List them clearly and concisely for the system prompt (maximum 4-5 sentences).

${history.join("\n")}

Rules/Specs:`;
      return await llmService.directGenerate(prompt);
    };
    errorDetectionGenerator = async (context, config2) => {
      const llmService = getLLMService(context);
      if (!llmService) return "[LLM service unavailable: cannot detect errors]";
      const history = await fetchSessionHistory(context, config2);
      if (!history.length) return "[No session history to detect errors from]";
      const prompt = `Identify any errors, bugs, or issues discussed or emphasized by the user in the following conversation. List them concisely for the system prompt (maximum 4-5 sentences).

${history.join("\n")}

Errors/Bugs:`;
      return await llmService.directGenerate(prompt);
    };
  }
});

// src/core/brain/systemPrompt/enhanced-manager.ts
var EnhancedPromptManager;
var init_enhanced_manager = __esm({
  "src/core/brain/systemPrompt/enhanced-manager.ts"() {
    init_cjs_shims();
    init_config_manager();
    init_registry();
    init_built_in_generators();
    EnhancedPromptManager = class {
      configManager;
      providers = /* @__PURE__ */ new Map();
      dynamicAndFileProviderConfigs = /* @__PURE__ */ new Map();
      // Store configs for runtime
      defaultContext;
      initialized = false;
      llmService = void 0;
      // Store reference to llmService
      constructor(options = {}) {
        this.configManager = new SystemPromptConfigManager();
        this.defaultContext = options.defaultContext || {};
        if (options.config) {
          this.configManager.loadFromObject(options.config);
        }
        if (options.registerBuiltInGenerators !== false) {
          this.initializeBuiltInGenerators();
        }
      }
      /**
       * Set the LLM service reference for use in provider context
       */
      setLLMService(llmService) {
        this.llmService = llmService;
      }
      /**
       * Initialize the manager with configuration
       */
      async initialize(config2) {
        if (config2) {
          this.configManager.loadFromObject(config2);
        }
        if (!this.configManager.isLoaded()) {
          const defaultConfig = SystemPromptConfigManager.createDefault();
          this.configManager.loadFromObject(defaultConfig);
        }
        await this.createProviders();
        this.initialized = true;
      }
      /**
       * Load configuration from file
       */
      async loadConfigFromFile(filePath) {
        await this.configManager.loadFromFile(filePath);
        if (this.initialized) {
          await this.destroyProviders();
          await this.createProviders();
        }
      }
      /**
       * Generate complete system prompt
       */
      async generateSystemPrompt(runtimeContext = {}) {
        this.ensureInitialized();
        const startTime = Date.now();
        const context = this.buildContext(runtimeContext);
        const settings = this.configManager.getSettings();
        const enabledProviders = this.getEnabledProviders();
        const useCache = runtimeContext.useCache === true;
        const providerResults = [];
        const errors = [];
        let success = true;
        const allProviders = Array.from(this.providers.values()).sort(
          (a, b) => b.priority - a.priority
        );
        const promises = allProviders.map(async (provider) => {
          if (!provider.enabled) {
            providerResults.push({
              providerId: provider.id,
              content: "",
              generationTimeMs: 0,
              success: false,
              error: new Error("Provider is disabled")
            });
            return;
          }
          const providerStartTime = Date.now();
          try {
            if (useCache && typeof provider.getCachedContent === "function") {
              const cached = provider.getCachedContent();
              if (cached !== null && cached !== void 0) {
                providerResults.push({
                  providerId: provider.id,
                  content: cached,
                  generationTimeMs: 0,
                  success: true
                });
                return;
              }
            }
            const content2 = await this.executeWithTimeout(
              () => provider.generateContent(context),
              settings.maxGenerationTime
            );
            providerResults.push({
              providerId: provider.id,
              content: content2,
              generationTimeMs: Date.now() - providerStartTime,
              success: true
            });
          } catch (error) {
            const err = error instanceof Error ? error : new Error("Unknown error");
            providerResults.push({
              providerId: provider.id,
              content: "",
              generationTimeMs: Date.now() - providerStartTime,
              success: false,
              error: err
            });
            errors.push(err);
            if (settings.failOnProviderError) {
              success = false;
            }
          }
        });
        await Promise.all(promises);
        providerResults.sort((a, b) => {
          const providerA = enabledProviders.find((p) => p.id === a.providerId);
          const providerB = enabledProviders.find((p) => p.id === b.providerId);
          return (providerB?.priority || 0) - (providerA?.priority || 0);
        });
        const successfulResults = providerResults.filter((r) => r.success && r.content.trim());
        const content = successfulResults.map((r) => r.content).join(settings.contentSeparator);
        return {
          content,
          providerResults,
          generationTimeMs: Date.now() - startTime,
          success: success && successfulResults.length > 0,
          errors
        };
      }
      /**
       * Get current configuration
       */
      getConfig() {
        return this.configManager.getConfig();
      }
      /**
       * Get all providers (enabled and disabled)
       */
      getProviders() {
        return Array.from(this.providers.values());
      }
      /**
       * Get enabled providers sorted by priority
       */
      getEnabledProviders() {
        return Array.from(this.providers.values()).filter((provider) => provider.enabled).sort((a, b) => b.priority - a.priority);
      }
      /**
       * Get a specific provider by ID
       */
      getProvider(id) {
        return this.providers.get(id);
      }
      /**
       * Enable or disable a provider
       */
      setProviderEnabled(id, enabled) {
        const provider = this.providers.get(id);
        if (provider) {
          provider.enabled = enabled;
        }
      }
      /**
       * Check if manager is initialized
       */
      isInitialized() {
        return this.initialized;
      }
      /**
       * Get performance statistics
       */
      async getPerformanceStats() {
        const providers = this.getProviders();
        const enabledProviders = this.getEnabledProviders();
        const testResult = await this.generateSystemPrompt({
          timestamp: /* @__PURE__ */ new Date(),
          sessionId: "perf-test"
        });
        return {
          totalProviders: providers.length,
          enabledProviders: enabledProviders.length,
          averageGenerationTime: Math.max(testResult.generationTimeMs, 1),
          // Ensure at least 1ms
          lastGenerationResult: testResult
        };
      }
      /**
       * Destroy the manager and clean up resources
       */
      async destroy() {
        await this.destroyProviders();
        this.initialized = false;
      }
      /**
       * Initialize built-in generators
       */
      async initializeBuiltInGenerators() {
        await registerBuiltInGenerators();
      }
      /**
       * Create provider instances from configuration
       * Only instantiate static providers at startup.
       * Store dynamic and file-based provider configs for runtime activation.
       */
      async createProviders() {
        const providerConfigs = this.configManager.getProviders();
        for (const config2 of providerConfigs) {
          if (config2.type === "static" && config2.enabled) {
            try {
              const provider = await providerRegistry.create(config2);
              this.providers.set(provider.id, provider);
            } catch (error) {
              console.warn(`Failed to create provider '${config2.name}':`, error);
            }
          } else if (config2.type === "dynamic" || config2.type === "file-based") {
            this.dynamicAndFileProviderConfigs.set(config2.name, config2);
          }
        }
      }
      /**
       * Add or update a dynamic or file-based provider at runtime.
       * If a provider with the same name exists, replace it.
       * Triggers prompt rebuild.
       */
      async addOrUpdateProvider(config2) {
        if (config2.type === "static") {
          throw new Error("Static providers cannot be added/updated at runtime.");
        }
        if (this.providers.has(config2.name)) {
          await this.removeProvider(config2.name);
        }
        try {
          const provider = await providerRegistry.create(config2);
          this.providers.set(provider.id, provider);
          this.dynamicAndFileProviderConfigs.set(config2.name, config2);
        } catch (error) {
          console.warn(`Failed to add/update provider '${config2.name}':`, error);
        }
      }
      /**
       * Remove a dynamic or file-based provider at runtime.
       * Triggers prompt rebuild.
       */
      async removeProvider(name) {
        const provider = this.providers.get(name);
        if (provider) {
          await provider.destroy();
          this.providers.delete(name);
          this.dynamicAndFileProviderConfigs.delete(name);
        }
      }
      /**
       * List all providers and their status.
       */
      listProviders() {
        return Array.from(this.providers.values()).map((p) => ({
          id: p.id,
          type: p.type,
          enabled: p.enabled
        }));
      }
      /**
       * Destroy all provider instances
       */
      async destroyProviders() {
        const destroyPromises = Array.from(this.providers.values()).map(async (provider) => {
          try {
            await provider.destroy();
          } catch (error) {
            console.warn(`Failed to destroy provider '${provider.id}':`, error);
          }
        });
        await Promise.all(destroyPromises);
        this.providers.clear();
      }
      /**
       * Build context by merging default and runtime context
       */
      buildContext(runtimeContext) {
        const base = {
          timestamp: /* @__PURE__ */ new Date(),
          ...this.defaultContext,
          ...runtimeContext
        };
        return {
          ...base,
          metadata: {
            ...base.metadata || {},
            llmService: this.llmService
          }
        };
      }
      /**
       * Execute a function with timeout
       */
      async executeWithTimeout(fn, timeoutMs) {
        return new Promise((resolve3, reject) => {
          const timeout = setTimeout(() => {
            reject(new Error(`Operation timed out after ${timeoutMs}ms`));
          }, timeoutMs);
          fn().then(resolve3).catch(reject).finally(() => clearTimeout(timeout));
        });
      }
      /**
       * Ensure manager is initialized
       */
      ensureInitialized() {
        if (!this.initialized) {
          throw new Error("EnhancedPromptManager is not initialized. Call initialize() first.");
        }
      }
    };
  }
});

// src/core/mcp/config.ts
var StdioServerConfigSchema, SseServerConfigSchema, StreamableHttpServerConfigSchema, AggregatorConfigSchema, McpServerConfigSchema, ExtendedMcpServerConfigSchema, ServerConfigsSchema;
var init_config = __esm({
  "src/core/mcp/config.ts"() {
    init_cjs_shims();
    init_zod();
    StdioServerConfigSchema = external_exports.object({
      type: external_exports.literal("stdio"),
      command: external_exports.string().describe("Command to launch the MCP server (e.g., 'node', 'python', 'uvx')"),
      args: external_exports.array(external_exports.string()).describe("Arguments to pass to the command (e.g., ['server.js', '--port=3000'])"),
      env: external_exports.record(external_exports.string()).default({}).describe(
        "Environment variables to set for the server process (e.g., API keys, configuration)"
      ),
      enabled: external_exports.boolean().default(true).describe(
        "Whether this server is enabled. Disabled servers will be skipped during initialization"
      ),
      timeout: external_exports.number().int().positive().default(3e4).describe("Maximum time in milliseconds to wait for server startup (30 seconds default)"),
      connectionMode: external_exports.enum(["strict", "lenient"]).default("lenient").describe(
        'How to handle connection failures: "strict" fails immediately, "lenient" continues with warnings'
      )
    }).strict();
    SseServerConfigSchema = external_exports.object({
      type: external_exports.literal("sse"),
      url: external_exports.string().url().describe(
        'Complete URL of the Server-Sent Events endpoint (e.g., "https://api.example.com/sse")'
      ),
      headers: external_exports.record(external_exports.string()).default({}).describe(
        'HTTP headers for authentication or configuration (e.g., {"Authorization": "Bearer token"})'
      ),
      enabled: external_exports.boolean().default(true).describe(
        "Whether this server is enabled. Disabled servers will be skipped during initialization"
      ),
      timeout: external_exports.number().int().positive().default(3e4).describe("Maximum time in milliseconds to establish SSE connection (30 seconds default)"),
      connectionMode: external_exports.enum(["strict", "lenient"]).default("lenient").describe(
        'How to handle connection failures: "strict" fails immediately, "lenient" continues with warnings'
      )
    }).strict();
    StreamableHttpServerConfigSchema = external_exports.object({
      type: external_exports.literal("streamable-http"),
      url: external_exports.string().url().describe('Base URL of the streamable HTTP MCP server (e.g., "https://mcp.semgrep.ai/mcp")'),
      headers: external_exports.record(external_exports.string()).default({}).describe(
        'HTTP headers sent with every request (e.g., {"Authorization": "Bearer token", "User-Agent": "MyApp"})'
      ),
      enabled: external_exports.boolean().default(true).describe(
        "Whether this server is enabled. Disabled servers will be skipped during initialization"
      ),
      timeout: external_exports.number().int().positive().default(3e4).describe("Maximum time in milliseconds to wait for HTTP responses (30 seconds default)"),
      connectionMode: external_exports.enum(["strict", "lenient"]).default("lenient").describe(
        'How to handle connection failures: "strict" fails immediately, "lenient" continues with warnings'
      )
    }).strict();
    AggregatorConfigSchema = external_exports.object({
      type: external_exports.literal("aggregator"),
      servers: external_exports.record(
        external_exports.discriminatedUnion("type", [
          StdioServerConfigSchema,
          SseServerConfigSchema,
          StreamableHttpServerConfigSchema
        ])
      ).describe("MCP servers to aggregate from (server name -> configuration)"),
      conflictResolution: external_exports.enum(["prefix", "first-wins", "error"]).default("prefix").describe(
        'Strategy for handling tool name conflicts: "prefix" adds server name, "first-wins" keeps first, "error" throws'
      ),
      autoDiscovery: external_exports.boolean().default(false).describe("Whether to auto-discover new servers in the network"),
      port: external_exports.number().int().positive().default(3e3).describe("Port for the aggregator server to listen on"),
      host: external_exports.string().default("localhost").describe("Host for the aggregator server to bind to"),
      timeout: external_exports.number().int().positive().default(6e4).describe("Maximum time in milliseconds for server operations (60 seconds default)"),
      connectionMode: external_exports.enum(["strict", "lenient"]).default("lenient").describe(
        'How to handle connection failures: "strict" fails immediately, "lenient" continues with warnings'
      )
    }).strict();
    McpServerConfigSchema = external_exports.discriminatedUnion(
      "type",
      [StdioServerConfigSchema, SseServerConfigSchema, StreamableHttpServerConfigSchema],
      {
        errorMap: (issue, ctx) => {
          if (issue.code === external_exports.ZodIssueCode.invalid_union_discriminator) {
            return {
              message: `Invalid server type. Expected 'stdio', 'sse', or 'streamable-http'.`
            };
          }
          return { message: ctx.defaultError };
        }
      }
    ).describe(
      "MCP server configuration - choose stdio for local processes, sse for real-time streams, or streamable-http for streaming HTTP servers"
    );
    ExtendedMcpServerConfigSchema = external_exports.discriminatedUnion(
      "type",
      [
        StdioServerConfigSchema,
        SseServerConfigSchema,
        StreamableHttpServerConfigSchema,
        AggregatorConfigSchema
      ],
      {
        errorMap: (issue, ctx) => {
          if (issue.code === external_exports.ZodIssueCode.invalid_union_discriminator) {
            return {
              message: `Invalid configuration type. Expected transport types: 'stdio', 'sse', 'streamable-http' or operational mode: 'aggregator'.`
            };
          }
          return { message: ctx.defaultError };
        }
      }
    ).describe(
      "MCP configuration - client transport types (stdio, sse, streamable-http) or server mode (aggregator)"
    );
    ServerConfigsSchema = external_exports.record(McpServerConfigSchema).describe("Named collection of MCP server configurations (server name -> configuration)");
  }
});

// src/core/mcp/validation.ts
function validateMcpServerConfig(serverName, serverConfig, existingServerNames = []) {
  const errors = [];
  const warnings = [];
  if (!serverName || typeof serverName !== "string" || serverName.trim() === "") {
    errors.push({
      type: "schema_validation",
      message: "Server name must be a non-empty string",
      field: "serverName",
      suggestedAction: "Provide a valid server name"
    });
  }
  try {
    McpServerConfigSchema.parse(serverConfig);
  } catch (error) {
    if (error instanceof ZodError) {
      for (const issue of error.errors) {
        errors.push({
          type: "schema_validation",
          message: `Invalid server configuration: ${issue.message}`,
          field: issue.path.join("."),
          suggestedAction: "Check the server configuration format and required fields"
        });
      }
    } else {
      errors.push({
        type: "schema_validation",
        message: `Invalid server configuration: ${error instanceof Error ? error.message : "Unknown validation error"}`,
        suggestedAction: "Check the server configuration format and required fields"
      });
    }
  }
  if (errors.length === 0) {
    const duplicateName = existingServerNames.find(
      (name) => name.toLowerCase() === serverName.toLowerCase() && name !== serverName
    );
    if (duplicateName) {
      warnings.push(
        `Server name '${serverName}' is similar to existing server '${duplicateName}' (case difference only)`
      );
    }
    if (serverConfig.type === "stdio") {
      if (!serverConfig.command || serverConfig.command.trim() === "") {
        errors.push({
          type: "schema_validation",
          message: "Stdio server requires a non-empty command",
          field: "command",
          suggestedAction: "Provide a valid command to execute"
        });
      }
    } else if (serverConfig.type === "sse" || serverConfig.type === "streamable-http") {
      const url = serverConfig.url;
      if (!url) {
        errors.push({
          type: "schema_validation",
          message: "URL is required for streamable-http/sse server types",
          field: "url",
          suggestedAction: "Provide a non-empty url string"
        });
      } else {
        try {
          new URL(url);
        } catch {
          errors.push({
            type: "schema_validation",
            message: `Invalid URL format: ${url}`,
            field: "url",
            suggestedAction: "Provide a valid URL with protocol (http:// or https://)"
          });
        }
      }
    }
  }
  let validatedConfig;
  if (errors.length === 0) {
    try {
      validatedConfig = McpServerConfigSchema.parse(serverConfig);
    } catch (schemaError) {
      if (schemaError instanceof ZodError) {
        for (const issue of schemaError.errors) {
          errors.push({
            type: "schema_validation",
            message: `Schema parsing failed: ${issue.message}`,
            field: issue.path.join(".")
          });
        }
      }
    }
  }
  return {
    isValid: errors.length === 0,
    errors,
    warnings,
    config: validatedConfig
  };
}
var init_validation = __esm({
  "src/core/mcp/validation.ts"() {
    init_cjs_shims();
    init_zod();
    init_config();
  }
});

// src/core/brain/memAgent/state-manager.ts
var MemAgentStateManager;
var init_state_manager = __esm({
  "src/core/brain/memAgent/state-manager.ts"() {
    init_cjs_shims();
    init_logger2();
    init_validation();
    MemAgentStateManager = class {
      runtimeConfig;
      baselineConfig;
      sessionOverrides = /* @__PURE__ */ new Map();
      constructor(staticConfig) {
        this.baselineConfig = structuredClone(staticConfig);
        this.runtimeConfig = structuredClone(staticConfig);
      }
      addMcpServer(serverName, serverConfig) {
        logger.debug(`Adding/updating MCP server: ${serverName}`);
        if (!this.runtimeConfig.mcpServers) {
          this.runtimeConfig.mcpServers = {};
        }
        const existingServerNames = Object.keys(this.runtimeConfig.mcpServers);
        const validation = validateMcpServerConfig(serverName, serverConfig, existingServerNames);
        if (!validation.isValid) {
          logger.warn("MCP server configuration validation failed", {
            serverName,
            errors: validation.errors.map((e) => e.message),
            warnings: validation.warnings
          });
          return validation;
        }
        if (validation.warnings.length > 0) {
          logger.warn("MCP server configuration warnings", {
            serverName,
            warnings: validation.warnings
          });
        }
        const isUpdate = this.runtimeConfig.mcpServers && serverName in this.runtimeConfig.mcpServers;
        this.runtimeConfig.mcpServers[serverName] = validation.config;
        logger.info(`MCP server '${serverName}' ${isUpdate ? "updated" : "added"} successfully`);
        return validation;
      }
      removeMcpServer(serverName) {
        logger.debug(`Removing MCP server: ${serverName}`);
        if (this.runtimeConfig.mcpServers && serverName in this.runtimeConfig.mcpServers) {
          delete this.runtimeConfig.mcpServers[serverName];
          logger.info(`MCP server '${serverName}' removed successfully`);
        } else {
          logger.warn(`MCP server '${serverName}' not found for removal`);
        }
      }
      getRuntimeConfig(sessionId) {
        if (!sessionId) {
          return structuredClone(this.runtimeConfig);
        }
        const override = this.sessionOverrides.get(sessionId);
        if (!override) {
          return structuredClone(this.runtimeConfig);
        }
        const result = {
          ...this.runtimeConfig,
          llm: { ...this.runtimeConfig.llm, ...override.llm }
        };
        if (override.evalLlm && this.runtimeConfig.evalLlm) {
          result.evalLlm = { ...this.runtimeConfig.evalLlm, ...override.evalLlm };
        } else if (override.evalLlm) {
          result.evalLlm = override.evalLlm;
        } else {
          result.evalLlm = this.runtimeConfig.evalLlm;
        }
        return result;
      }
      getLLMConfig(sessionId) {
        const config2 = this.getRuntimeConfig(sessionId);
        return {
          ...config2.llm,
          maxIterations: config2.llm.maxIterations ?? 10
          // Provide default value
        };
      }
      /**
       * Get evaluation LLM configuration with fallback to main LLM config
       * Used for evaluation tasks that typically require non-thinking models
       */
      getEvalLLMConfig(sessionId) {
        const config2 = this.getRuntimeConfig(sessionId);
        if (config2.evalLlm) {
          try {
            const evalConfig = {
              ...config2.evalLlm,
              maxIterations: config2.evalLlm.maxIterations ?? 5
              // Default for eval tasks
            };
            if (evalConfig.provider && evalConfig.model) {
              logger.debug("Using configured evalLlm for evaluation tasks", {
                provider: evalConfig.provider,
                model: evalConfig.model
              });
              return evalConfig;
            }
          } catch (error) {
            logger.warn("Invalid evalLlm configuration, falling back to main LLM", {
              error: error instanceof Error ? error.message : String(error),
              sessionId
            });
          }
        }
        const fallbackConfig = {
          ...config2.llm,
          maxIterations: 5
          // Use lower iterations for eval tasks
        };
        logger.debug("Using fallback LLM configuration for evaluation tasks", {
          provider: fallbackConfig.provider,
          model: fallbackConfig.model,
          sessionId
        });
        return fallbackConfig;
      }
      /**
       * Update LLM configuration globally or for a specific session
       */
      updateLLMConfig(newConfig, sessionId) {
        if (sessionId) {
          const existingOverride = this.sessionOverrides.get(sessionId) || {};
          this.sessionOverrides.set(sessionId, {
            ...existingOverride,
            llm: { ...existingOverride.llm, ...newConfig }
          });
          logger.info(`Updated LLM config for session ${sessionId}`, { newConfig });
        } else {
          this.runtimeConfig.llm = { ...this.runtimeConfig.llm, ...newConfig };
          logger.info("Updated global LLM config", { newConfig });
        }
      }
      /**
       * Remove session-specific LLM configuration override
       */
      clearSessionLLMOverride(sessionId) {
        const override = this.sessionOverrides.get(sessionId);
        if (override) {
          if (override.llm) {
            delete override.llm;
            if (Object.keys(override).length === 0) {
              this.sessionOverrides.delete(sessionId);
            }
            logger.info(`Cleared LLM config override for session ${sessionId}`);
          }
        }
      }
      /**
       * Get all active session overrides (for debugging/inspection)
       */
      getSessionOverrides() {
        return new Map(this.sessionOverrides);
      }
    };
  }
});

// src/core/brain/llm/messages/utils.ts
function getImageData(imagePart) {
  const { image } = imagePart;
  if (typeof image === "string") {
    return image;
  } else if (image instanceof Buffer) {
    return image.toString("base64");
  } else if (image instanceof Uint8Array) {
    return Buffer.from(image).toString("base64");
  } else if (image instanceof ArrayBuffer) {
    return Buffer.from(new Uint8Array(image)).toString("base64");
  } else if (image instanceof URL) {
    return image.toString();
  }
  logger.warn("Unexpected image data type in getImageData", { image });
  return "";
}
var init_utils = __esm({
  "src/core/brain/llm/messages/utils.ts"() {
    init_cjs_shims();
    init_logger2();
  }
});

// src/core/brain/llm/tokenizer/types.ts
var TokenizerConfigSchema;
var init_types2 = __esm({
  "src/core/brain/llm/tokenizer/types.ts"() {
    init_cjs_shims();
    init_zod();
    TokenizerConfigSchema = external_exports.object({
      provider: external_exports.enum(["openai", "anthropic", "google", "default"]),
      model: external_exports.string().optional(),
      fallbackToApproximation: external_exports.boolean().default(true),
      hybridTracking: external_exports.boolean().default(true)
    });
  }
});

// src/core/brain/llm/tokenizer/utils.ts
function extractTextFromMessage(message) {
  if (typeof message.content === "string") {
    return message.content;
  }
  if (Array.isArray(message.content)) {
    return message.content.filter((part) => part.type === "text").map((part) => part.text).join(" ");
  }
  return "";
}
function estimateTokensFromText(text) {
  if (!text) return 0;
  const words = text.split(/\s+/).filter((word) => word.length > 0);
  const avgCharsPerToken = 4;
  let adjustment = 1;
  if (text.includes("{") && text.includes("}") && text.includes(";")) {
    adjustment = 0.8;
  }
  const avgWordLength = words.reduce((sum, word) => sum + word.length, 0) / (words.length || 1);
  if (avgWordLength < 3) {
    adjustment = 0.7;
  }
  return Math.ceil(text.length / avgCharsPerToken * adjustment);
}
function formatTokenCount(count) {
  const estimated = count.estimated ? " (estimated)" : "";
  const model = count.model ? ` [${count.model}]` : "";
  return `${count.total} tokens${estimated}${model}`;
}
function createFallbackTokenCount(text, provider, model = "unknown") {
  const estimated = estimateTokensFromText(text);
  return {
    total: estimated,
    characters: text.length,
    estimated: true,
    provider,
    model
  };
}
function logTokenCount(operation, count, context) {
  logger.info(`Token counting: ${operation}`, {
    ...count,
    formatted: formatTokenCount(count),
    ...context
  });
}
var init_utils2 = __esm({
  "src/core/brain/llm/tokenizer/utils.ts"() {
    init_cjs_shims();
    init_logger2();
  }
});

// src/core/brain/llm/tokenizer/providers/openai.ts
var OpenAITokenizer;
var init_openai = __esm({
  "src/core/brain/llm/tokenizer/providers/openai.ts"() {
    init_cjs_shims();
    init_utils2();
    init_logger2();
    OpenAITokenizer = class {
      provider = "openai";
      model;
      config;
      tokenLimits;
      tiktoken = null;
      encoding = null;
      tiktokencountflag = false;
      approximationcountflag = false;
      // Token density calibration for hybrid tracking
      densityHistory = [];
      maxHistorySize = 100;
      constructor(config2) {
        this.config = config2;
        this.model = config2.model ?? "gpt-3.5-turbo";
        this.tokenLimits = this.getTokenLimitsForModel(config2.model || "gpt-3.5-turbo");
        this.initializeTiktoken();
      }
      async initializeTiktoken() {
        try {
          const tiktokenModule = await import("tiktoken").catch(() => null);
          if (!tiktokenModule) {
            logger.warn("tiktoken not available, using approximation");
            return;
          }
          this.tiktoken = tiktokenModule;
          const modelName = this.model || "gpt-3.5-turbo";
          try {
            if (this.isO1Model(modelName)) {
              this.encoding = this.tiktoken.encoding_for_model("gpt-4o");
            } else if (this.isQwenModel(modelName)) {
              this.encoding = this.tiktoken.encoding_for_model("gpt-4");
            } else {
              this.encoding = this.tiktoken.encoding_for_model(modelName);
            }
          } catch (encodingError) {
            logger.debug("Failed to get encoding for specific model, trying fallback", {
              model: modelName,
              error: encodingError.message
            });
            this.encoding = this.tiktoken.encoding_for_model("gpt-4");
          }
          logger.debug("OpenAI tokenizer initialized with tiktoken", { model: modelName });
        } catch (error) {
          logger.warn("Failed to initialize tiktoken, falling back to approximation", {
            error: error.message
          });
          this.tiktoken = null;
          this.encoding = null;
        }
      }
      isO1Model(model) {
        return model.startsWith("o1-");
      }
      isQwenModel(model) {
        return model.startsWith("qwen") || model.includes("qwen");
      }
      getTokenLimitsForModel(model) {
        const limits = {
          "gpt-3.5-turbo": { maxTokens: 4096, contextWindow: 16385 },
          "gpt-3.5-turbo-16k": { maxTokens: 4096, contextWindow: 16385 },
          "gpt-4": { maxTokens: 4096, contextWindow: 8192 },
          "gpt-4-32k": { maxTokens: 4096, contextWindow: 32768 },
          "gpt-4-turbo": { maxTokens: 4096, contextWindow: 128e3 },
          "gpt-4o": { maxTokens: 4096, contextWindow: 128e3 },
          "gpt-4o-mini": { maxTokens: 16384, contextWindow: 128e3 },
          "o1-preview": { maxTokens: 32768, contextWindow: 128e3 },
          "o1-mini": { maxTokens: 65536, contextWindow: 128e3 }
        };
        return limits[model] || { maxTokens: 4096, contextWindow: 8192 };
      }
      async countTokens(text) {
        if (!text) {
          return {
            total: 0,
            characters: 0,
            estimated: false,
            provider: this.provider,
            model: this.model
          };
        }
        try {
          if (this.encoding) {
            const tokens = this.encoding.encode(text);
            const count = {
              total: tokens.length,
              characters: text.length,
              estimated: false,
              provider: this.provider,
              model: this.model
            };
            if (this.config.hybridTracking) {
              this.updateDensityHistory(text.length, tokens.length);
            }
            if (!this.tiktokencountflag) {
              logTokenCount("tiktoken count", count);
              this.tiktokencountflag = true;
            }
            return count;
          } else {
            const estimated = this.estimateTokens(text);
            const count = createFallbackTokenCount(text, this.provider, this.model);
            count.total = estimated;
            if (!this.approximationcountflag) {
              logTokenCount("openai approximation", count);
              this.approximationcountflag = true;
            }
            return count;
          }
        } catch (error) {
          logger.warn("Error in OpenAI token counting, falling back to approximation", {
            error: error.message
          });
          const estimated = this.estimateTokens(text);
          const count = createFallbackTokenCount(text, this.provider, this.model);
          count.total = estimated;
          return count;
        }
      }
      async countMessages(messages) {
        let totalTokens = 0;
        let totalCharacters = 0;
        let isEstimated = false;
        for (const message of messages) {
          const roleTokens = await this.countTokens(`${message.role}: `);
          const contentTokens = await this.countTokens(message.content);
          totalTokens += roleTokens.total + contentTokens.total + 3;
          totalCharacters += roleTokens.characters + contentTokens.characters;
          if (roleTokens.estimated || contentTokens.estimated) {
            isEstimated = true;
          }
        }
        totalTokens += 3;
        return {
          total: totalTokens,
          characters: totalCharacters,
          estimated: isEstimated,
          provider: this.provider,
          model: this.model
        };
      }
      getMaxTokens() {
        return this.tokenLimits.maxTokens;
      }
      getContextWindow() {
        return this.tokenLimits.contextWindow;
      }
      estimateTokens(text) {
        if (this.config.hybridTracking && this.densityHistory.length > 0) {
          const avgDensity = this.densityHistory.reduce((a, b) => a + b, 0) / this.densityHistory.length;
          return Math.ceil(text.length * avgDensity);
        }
        return estimateTokensFromText(text);
      }
      isWithinLimit(tokenCount) {
        return tokenCount <= this.tokenLimits.contextWindow;
      }
      getRemainingTokens(currentCount) {
        return Math.max(0, this.tokenLimits.contextWindow - currentCount);
      }
      updateDensityHistory(characters, tokens) {
        if (characters > 0 && tokens > 0) {
          const density = tokens / characters;
          this.densityHistory.push(density);
          if (this.densityHistory.length > this.maxHistorySize) {
            this.densityHistory.shift();
          }
        }
      }
      /**
       * Get current calibrated token density
       */
      getTokenDensity() {
        if (this.densityHistory.length === 0) {
          return 0.25;
        }
        return this.densityHistory.reduce((a, b) => a + b, 0) / this.densityHistory.length;
      }
      /**
       * Reset density calibration
       */
      resetCalibration() {
        this.densityHistory = [];
      }
    };
  }
});

// src/core/brain/llm/tokenizer/providers/anthropic.ts
var AnthropicTokenizer;
var init_anthropic = __esm({
  "src/core/brain/llm/tokenizer/providers/anthropic.ts"() {
    init_cjs_shims();
    init_utils2();
    init_logger2();
    AnthropicTokenizer = class {
      provider = "anthropic";
      model;
      config;
      tokenLimits;
      tokencountflag = false;
      // Calibrated token density for this provider
      avgTokenDensity = 0.25;
      // 4 chars per token
      constructor(config2) {
        this.config = config2;
        this.model = config2.model ?? "claude-default";
        this.tokenLimits = this.getTokenLimitsForModel(config2.model || "claude-3-sonnet");
        logger.debug("Anthropic tokenizer initialized", { model: this.model });
      }
      getTokenLimitsForModel(model) {
        const limits = {
          "claude-3-opus": { maxTokens: 4096, contextWindow: 2e5 },
          "claude-3-sonnet": { maxTokens: 4096, contextWindow: 2e5 },
          "claude-3-haiku": { maxTokens: 4096, contextWindow: 2e5 },
          "claude-3-5-sonnet": { maxTokens: 8192, contextWindow: 2e5 },
          "claude-2.1": { maxTokens: 4096, contextWindow: 2e5 },
          "claude-2.0": { maxTokens: 4096, contextWindow: 1e5 },
          "claude-instant-1.2": { maxTokens: 4096, contextWindow: 1e5 }
        };
        return limits[model] || { maxTokens: 4096, contextWindow: 2e5 };
      }
      async countTokens(text) {
        if (!text) {
          return {
            total: 0,
            characters: 0,
            estimated: true,
            provider: this.provider,
            model: this.model
          };
        }
        const tokenCount = this.estimateTokensWithCalibration(text);
        const count = {
          total: tokenCount,
          characters: text.length,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
        if (!this.tokencountflag) {
          logTokenCount("anthropic", count);
          this.tokencountflag = true;
        }
        return count;
      }
      async countMessages(messages) {
        let totalTokens = 0;
        let totalCharacters = 0;
        for (const message of messages) {
          const roleText = `${message.role}: `;
          const roleTokens = await this.countTokens(roleText);
          const contentTokens = await this.countTokens(message.content);
          totalTokens += roleTokens.total + contentTokens.total + 2;
          totalCharacters += roleTokens.characters + contentTokens.characters;
        }
        totalTokens += 5;
        return {
          total: totalTokens,
          characters: totalCharacters,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
      }
      getMaxTokens() {
        return this.tokenLimits.maxTokens;
      }
      getContextWindow() {
        return this.tokenLimits.contextWindow;
      }
      estimateTokens(text) {
        return this.estimateTokensWithCalibration(text);
      }
      estimateTokensWithCalibration(text) {
        if (!text) return 0;
        const baseEstimate = estimateTokensFromText(text);
        let adjustment = 1;
        const codePatterns = /[{}()[\];=><]/g;
        const codeMatches = text.match(codePatterns)?.length || 0;
        const codeRatio = codeMatches / text.length;
        if (codeRatio < 0.01) {
          adjustment = 0.95;
        } else if (codeRatio > 0.05) {
          adjustment = 1.05;
        }
        const charBasedEstimate = Math.ceil(text.length * this.avgTokenDensity * adjustment);
        return Math.max(baseEstimate, charBasedEstimate);
      }
      isWithinLimit(tokenCount) {
        return tokenCount <= this.tokenLimits.contextWindow;
      }
      getRemainingTokens(currentCount) {
        return Math.max(0, this.tokenLimits.contextWindow - currentCount);
      }
      /**
       * Update token density based on actual API responses
       * This can be called when actual token usage is known from API responses
       */
      calibrateDensity(text, actualTokens) {
        if (text && actualTokens > 0) {
          const actualDensity = actualTokens / text.length;
          const alpha = 0.1;
          this.avgTokenDensity = alpha * actualDensity + (1 - alpha) * this.avgTokenDensity;
          logger.debug("Anthropic tokenizer density calibrated", {
            actualDensity,
            newAvgDensity: this.avgTokenDensity,
            textLength: text.length,
            actualTokens
          });
        }
      }
      /**
       * Get current calibrated token density
       */
      getTokenDensity() {
        return this.avgTokenDensity;
      }
      /**
       * Reset density to default
       */
      resetCalibration() {
        this.avgTokenDensity = 0.25;
      }
    };
  }
});

// src/core/brain/llm/tokenizer/providers/google.ts
var GoogleTokenizer;
var init_google = __esm({
  "src/core/brain/llm/tokenizer/providers/google.ts"() {
    init_cjs_shims();
    init_utils2();
    init_logger2();
    GoogleTokenizer = class {
      provider = "google";
      model;
      config;
      tokenLimits;
      tokencountflag = false;
      // Calibrated token density for Google models
      avgTokenDensity = 0.28;
      // Slightly higher than other providers
      constructor(config2) {
        this.config = config2;
        this.model = config2.model ?? "gemini-default";
        this.tokenLimits = this.getTokenLimitsForModel(config2.model || "gemini-pro");
        logger.debug("Google tokenizer initialized", { model: this.model });
      }
      getTokenLimitsForModel(model) {
        const limits = {
          "gemini-pro": { maxTokens: 8192, contextWindow: 32760 },
          "gemini-pro-vision": { maxTokens: 4096, contextWindow: 16384 },
          "gemini-ultra": { maxTokens: 8192, contextWindow: 32760 },
          "gemini-1.5-pro": { maxTokens: 8192, contextWindow: 1e6 },
          // 1M token context
          "gemini-1.5-flash": { maxTokens: 8192, contextWindow: 1e6 }
        };
        return limits[model] || { maxTokens: 8192, contextWindow: 32760 };
      }
      async countTokens(text) {
        if (!text) {
          return {
            total: 0,
            characters: 0,
            estimated: true,
            provider: this.provider,
            model: this.model
          };
        }
        const tokenCount = this.estimateTokensWithCalibration(text);
        const count = {
          total: tokenCount,
          characters: text.length,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
        if (!this.tokencountflag) {
          logTokenCount("google", count);
          this.tokencountflag = true;
        }
        return count;
      }
      async countMessages(messages) {
        let totalTokens = 0;
        let totalCharacters = 0;
        for (const message of messages) {
          const roleMapping = this.mapRoleForGoogle(message.role);
          const roleText = `${roleMapping}: `;
          const roleTokens = await this.countTokens(roleText);
          const contentTokens = await this.countTokens(message.content);
          totalTokens += roleTokens.total + contentTokens.total + 3;
          totalCharacters += roleTokens.characters + contentTokens.characters;
        }
        totalTokens += 4;
        return {
          total: totalTokens,
          characters: totalCharacters,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
      }
      mapRoleForGoogle(role) {
        const roleMap = {
          user: "user",
          assistant: "model",
          system: "user",
          // Google doesn't have explicit system role
          tool: "function"
        };
        return roleMap[role] || role;
      }
      getMaxTokens() {
        return this.tokenLimits.maxTokens;
      }
      getContextWindow() {
        return this.tokenLimits.contextWindow;
      }
      estimateTokens(text) {
        return this.estimateTokensWithCalibration(text);
      }
      estimateTokensWithCalibration(text) {
        if (!text) return 0;
        const baseEstimate = estimateTokensFromText(text);
        let adjustment = 1.1;
        const structuredPatterns = /[{}"':,\]<>]/g;
        const structuredMatches = text.match(structuredPatterns)?.length || 0;
        const structuredRatio = structuredMatches / text.length;
        if (structuredRatio > 0.1) {
          adjustment = 1.15;
        }
        const charBasedEstimate = Math.ceil(text.length * this.avgTokenDensity * adjustment);
        return Math.max(baseEstimate, charBasedEstimate);
      }
      isWithinLimit(tokenCount) {
        return tokenCount <= this.tokenLimits.contextWindow;
      }
      getRemainingTokens(currentCount) {
        return Math.max(0, this.tokenLimits.contextWindow - currentCount);
      }
      /**
       * Update token density based on actual API responses
       */
      calibrateDensity(text, actualTokens) {
        if (text && actualTokens > 0) {
          const actualDensity = actualTokens / text.length;
          const alpha = 0.1;
          this.avgTokenDensity = alpha * actualDensity + (1 - alpha) * this.avgTokenDensity;
          logger.debug("Google tokenizer density calibrated", {
            actualDensity,
            newAvgDensity: this.avgTokenDensity,
            textLength: text.length,
            actualTokens
          });
        }
      }
      /**
       * Get current calibrated token density
       */
      getTokenDensity() {
        return this.avgTokenDensity;
      }
      /**
       * Reset density to default
       */
      resetCalibration() {
        this.avgTokenDensity = 0.28;
      }
      /**
       * Check if model supports very large context windows
       */
      isLargeContextModel() {
        return this.model?.includes("1.5") || false;
      }
    };
  }
});

// src/core/brain/llm/tokenizer/providers/default.ts
var DefaultTokenizer;
var init_default = __esm({
  "src/core/brain/llm/tokenizer/providers/default.ts"() {
    init_cjs_shims();
    init_utils2();
    init_logger2();
    DefaultTokenizer = class {
      provider = "default";
      model;
      config;
      tokenLimits;
      tokencountflag = false;
      // Conservative token density
      avgTokenDensity = 0.3;
      // ~3.3 chars per token (conservative)
      constructor(config2) {
        this.config = config2;
        this.model = config2.model ?? "default-model";
        this.tokenLimits = this.getDefaultTokenLimits();
        logger.debug("Default tokenizer initialized", { model: this.model });
      }
      getDefaultTokenLimits() {
        return {
          maxTokens: 4096,
          contextWindow: 8192
        };
      }
      async countTokens(text) {
        if (!text) {
          return {
            total: 0,
            characters: 0,
            estimated: true,
            provider: this.provider,
            model: this.model
          };
        }
        const tokenCount = this.estimateTokensConservatively(text);
        const count = {
          total: tokenCount,
          characters: text.length,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
        if (!this.tokencountflag) {
          logTokenCount("default", count);
          this.tokencountflag = true;
        }
        return count;
      }
      async countMessages(messages) {
        let totalTokens = 0;
        let totalCharacters = 0;
        for (const message of messages) {
          const roleText = `${message.role}: `;
          const roleTokens = await this.countTokens(roleText);
          const contentTokens = await this.countTokens(message.content);
          totalTokens += roleTokens.total + contentTokens.total + 5;
          totalCharacters += roleTokens.characters + contentTokens.characters;
        }
        totalTokens += 10;
        return {
          total: totalTokens,
          characters: totalCharacters,
          estimated: true,
          provider: this.provider,
          model: this.model
        };
      }
      getMaxTokens() {
        return this.tokenLimits.maxTokens;
      }
      getContextWindow() {
        return this.tokenLimits.contextWindow;
      }
      estimateTokens(text) {
        return this.estimateTokensConservatively(text);
      }
      estimateTokensConservatively(text) {
        if (!text) return 0;
        const baseEstimate = estimateTokensFromText(text);
        const conservativeMultiplier = 1.25;
        const charBasedEstimate = Math.ceil(text.length * this.avgTokenDensity);
        const finalEstimate = Math.max(
          Math.ceil(baseEstimate * conservativeMultiplier),
          charBasedEstimate
        );
        return finalEstimate;
      }
      isWithinLimit(tokenCount) {
        return tokenCount <= this.tokenLimits.contextWindow;
      }
      getRemainingTokens(currentCount) {
        return Math.max(0, this.tokenLimits.contextWindow - currentCount);
      }
      /**
       * Update token limits for better model support
       */
      updateTokenLimits(limits) {
        this.tokenLimits = {
          ...this.tokenLimits,
          ...limits
        };
        logger.debug("Default tokenizer limits updated", this.tokenLimits);
      }
      /**
       * Update token density based on observed behavior
       */
      calibrateDensity(text, actualTokens) {
        if (text && actualTokens > 0) {
          const actualDensity = actualTokens / text.length;
          const alpha = 0.05;
          this.avgTokenDensity = alpha * actualDensity + (1 - alpha) * this.avgTokenDensity;
          this.avgTokenDensity = Math.max(this.avgTokenDensity, 0.2);
          logger.debug("Default tokenizer density calibrated", {
            actualDensity,
            newAvgDensity: this.avgTokenDensity,
            textLength: text.length,
            actualTokens
          });
        }
      }
      /**
       * Get current calibrated token density
       */
      getTokenDensity() {
        return this.avgTokenDensity;
      }
      /**
       * Reset density to default
       */
      resetCalibration() {
        this.avgTokenDensity = 0.3;
      }
      /**
       * Set model-specific limits if known
       */
      setModelLimits(model, limits) {
        this.tokenLimits = limits;
        logger.debug("Default tokenizer configured for specific model", { model, limits });
      }
    };
  }
});

// src/core/brain/llm/tokenizer/cache.ts
var import_crypto, TokenizerCache, getTokenizerCache;
var init_cache = __esm({
  "src/core/brain/llm/tokenizer/cache.ts"() {
    init_cjs_shims();
    init_logger2();
    import_crypto = __toESM(require("crypto"), 1);
    TokenizerCache = class _TokenizerCache {
      static instance = null;
      cache = /* @__PURE__ */ new Map();
      maxCacheSize = 50;
      // Prevent memory bloat
      cacheHits = /* @__PURE__ */ new Map();
      cacheAccess = /* @__PURE__ */ new Map();
      // Track access times for LRU
      constructor() {
      }
      /**
       * Get the singleton instance of TokenizerCache
       */
      static getInstance() {
        if (!_TokenizerCache.instance) {
          _TokenizerCache.instance = new _TokenizerCache();
        }
        return _TokenizerCache.instance;
      }
      /**
       * Generate a unique cache key based on tokenizer configuration
       */
      generateCacheKey(config2) {
        const configString = JSON.stringify({
          provider: config2.provider,
          model: config2.model || "default",
          fallbackToApproximation: config2.fallbackToApproximation,
          hybridTracking: config2.hybridTracking
        });
        return import_crypto.default.createHash("md5").update(configString).digest("hex").substring(0, 16);
      }
      /**
       * Get a tokenizer from cache or create new one
       */
      get(config2, factory) {
        const cacheKey = this.generateCacheKey(config2);
        if (this.cache.has(cacheKey)) {
          this.cacheHits.set(cacheKey, (this.cacheHits.get(cacheKey) || 0) + 1);
          this.cacheAccess.set(cacheKey, Date.now());
          const tokenizer2 = this.cache.get(cacheKey);
          logger.debug("TokenizerCache: Cache hit", {
            cacheKey,
            provider: config2.provider,
            model: config2.model,
            totalHits: this.cacheHits.get(cacheKey),
            cacheSize: this.cache.size
          });
          return tokenizer2;
        }
        logger.debug("TokenizerCache: Cache miss, creating new tokenizer", {
          cacheKey,
          provider: config2.provider,
          model: config2.model,
          cacheSize: this.cache.size
        });
        if (this.cache.size >= this.maxCacheSize) {
          this.evictLeastRecentlyUsed();
        }
        const tokenizer = factory(config2);
        this.cache.set(cacheKey, tokenizer);
        this.cacheHits.set(cacheKey, 1);
        this.cacheAccess.set(cacheKey, Date.now());
        logger.debug("TokenizerCache: Tokenizer cached", {
          cacheKey,
          provider: config2.provider,
          model: config2.model,
          cacheSize: this.cache.size
        });
        return tokenizer;
      }
      /**
       * Evict the least recently used tokenizer from cache
       */
      evictLeastRecentlyUsed() {
        let oldestKey = null;
        let oldestTime = Date.now();
        for (const [key, accessTime] of this.cacheAccess) {
          if (accessTime < oldestTime) {
            oldestTime = accessTime;
            oldestKey = key;
          }
        }
        if (oldestKey) {
          logger.debug("TokenizerCache: Evicting LRU tokenizer", {
            evictedKey: oldestKey,
            cacheSize: this.cache.size
          });
          this.cache.delete(oldestKey);
          this.cacheHits.delete(oldestKey);
          this.cacheAccess.delete(oldestKey);
        }
      }
      /**
       * Clear specific tokenizer from cache
       */
      invalidate(config2) {
        const cacheKey = this.generateCacheKey(config2);
        const removed = this.cache.delete(cacheKey);
        if (removed) {
          this.cacheHits.delete(cacheKey);
          this.cacheAccess.delete(cacheKey);
          logger.debug("TokenizerCache: Tokenizer invalidated", {
            cacheKey,
            provider: config2.provider,
            model: config2.model
          });
        }
        return removed;
      }
      /**
       * Clear all cached tokenizers
       */
      clear() {
        const size = this.cache.size;
        this.cache.clear();
        this.cacheHits.clear();
        this.cacheAccess.clear();
        logger.debug("TokenizerCache: All tokenizers cleared", {
          previousSize: size
        });
      }
      /**
       * Get cache statistics for monitoring
       */
      getStats() {
        const totalHits = Array.from(this.cacheHits.values()).reduce((sum, hits) => sum + hits, 0);
        const totalRequests = totalHits + (this.cache.size > 0 ? this.cache.size : 1);
        const entries = Array.from(this.cache.keys()).map((key) => ({
          key,
          hits: this.cacheHits.get(key) || 0,
          lastAccess: this.cacheAccess.get(key) || 0
        }));
        return {
          cacheSize: this.cache.size,
          maxCacheSize: this.maxCacheSize,
          totalHits,
          hitRate: totalHits / totalRequests,
          entries
        };
      }
      /**
       * Check if a tokenizer is cached
       */
      has(config2) {
        const cacheKey = this.generateCacheKey(config2);
        return this.cache.has(cacheKey);
      }
      /**
       * Get current cache size
       */
      size() {
        return this.cache.size;
      }
    };
    getTokenizerCache = () => {
      return TokenizerCache.getInstance();
    };
  }
});

// src/core/brain/llm/tokenizer/factory.ts
function createTokenizer(config2) {
  const validatedConfig = TokenizerConfigSchema.parse(config2);
  const cache = getTokenizerCache();
  return cache.get(validatedConfig, (config3) => {
    logger.debug("Creating tokenizer", {
      provider: config3.provider,
      model: config3.model
    });
    switch (config3.provider) {
      case "openai":
        return new OpenAITokenizer(config3);
      case "anthropic":
        return new AnthropicTokenizer(config3);
      case "google":
        return new GoogleTokenizer(config3);
      case "default":
      default:
        logger.warn("Using default tokenizer, token counting may be less accurate");
        return new DefaultTokenizer(config3);
    }
  });
}
function getTokenizerConfigForModel(model) {
  if (model.startsWith("gpt-") || model.startsWith("o1-") || model.includes("openai")) {
    return {
      provider: "openai",
      model,
      fallbackToApproximation: true,
      hybridTracking: true
    };
  }
  if (model.startsWith("claude-") || model.includes("anthropic")) {
    return {
      provider: "anthropic",
      model,
      fallbackToApproximation: true,
      hybridTracking: true
    };
  }
  if (model.startsWith("gemini-") || model.includes("google")) {
    return {
      provider: "google",
      model,
      fallbackToApproximation: true,
      hybridTracking: true
    };
  }
  if (model.startsWith("qwen") || model.includes("qwen")) {
    return {
      provider: "openai",
      // Qwen uses OpenAI-compatible tokenization
      model,
      fallbackToApproximation: true,
      hybridTracking: true
    };
  }
  if (model.includes("lmstudio") || model.includes("llama") || model.includes("mistral")) {
    return {
      provider: "openai",
      // LM Studio uses OpenAI-compatible tokenization
      model,
      fallbackToApproximation: true,
      hybridTracking: true
    };
  }
  return {
    provider: "default",
    model,
    fallbackToApproximation: true,
    hybridTracking: false
  };
}
var init_factory2 = __esm({
  "src/core/brain/llm/tokenizer/factory.ts"() {
    init_cjs_shims();
    init_types2();
    init_openai();
    init_anthropic();
    init_google();
    init_default();
    init_logger2();
    init_cache();
  }
});

// src/core/brain/llm/tokenizer/index.ts
var init_tokenizer = __esm({
  "src/core/brain/llm/tokenizer/index.ts"() {
    init_cjs_shims();
    init_types2();
    init_factory2();
    init_utils2();
    init_cache();
    init_openai();
    init_anthropic();
    init_google();
    init_default();
  }
});

// src/core/brain/llm/compression/types.ts
var CompressionConfigSchema;
var init_types3 = __esm({
  "src/core/brain/llm/compression/types.ts"() {
    init_cjs_shims();
    init_zod();
    CompressionConfigSchema = external_exports.object({
      strategy: external_exports.enum(["middle-removal", "oldest-removal", "hybrid"]),
      maxTokens: external_exports.number().positive(),
      warningThreshold: external_exports.number().min(0).max(1).default(0.8),
      compressionThreshold: external_exports.number().min(0).max(1).default(0.9),
      preserveStart: external_exports.number().min(1).default(4),
      preserveEnd: external_exports.number().min(1).default(5),
      minMessagesToKeep: external_exports.number().min(1).default(4)
    });
  }
});

// src/core/brain/llm/compression/utils.ts
function getMessageTokenCount(message) {
  if (message.tokenCount !== void 0) {
    return message.tokenCount;
  }
  const text = extractTextFromMessage(message);
  const approxToken = Math.ceil(text.length / 4);
  if (approxToken > 1e4) {
    console.warn("Token estimation unusually high:", {
      textLength: text.length,
      approxToken,
      message
    });
  }
  return approxToken;
}
function calculateTotalTokens(messages) {
  return messages.reduce((total, message) => total + getMessageTokenCount(message), 0);
}
function assignMessagePriorities(messages) {
  return messages.map((message, index) => {
    if (message.priority !== void 0) {
      return message;
    }
    let priority = "normal" /* NORMAL */;
    const tokenCount = getMessageTokenCount(message);
    const textContent = typeof message.content === "string" ? message.content : "";
    if (message.role === "system") {
      priority = "critical" /* CRITICAL */;
    } else if (message.role === "tool") {
      priority = "high" /* HIGH */;
    } else if (index < 2 || index >= messages.length - 2) {
      priority = "high" /* HIGH */;
    } else if (tokenCount > 800) {
      priority = "high" /* HIGH */;
    } else if (tokenCount < 20 && !textContent.includes("?")) {
      priority = "low" /* LOW */;
    } else if (message.function_call || message.tool_calls || message.name) {
      priority = "high" /* HIGH */;
    }
    return {
      ...message,
      priority,
      timestamp: message.timestamp || Date.now()
    };
  });
}
function getPreservableMessages(messages) {
  return messages.filter(
    (message) => message.preserveInCompression === true || message.priority === "critical" /* CRITICAL */ || message.role === "system"
  );
}
function getRemovableMessages(messages) {
  const removable = [];
  const debugInfo = [];
  for (const message of messages) {
    if (message.preserveInCompression !== true && message.priority !== "critical" /* CRITICAL */ && message.role !== "system") {
      removable.push(message);
      debugInfo.push({
        messageId: message.messageId,
        role: message.role,
        priority: message.priority,
        preserveInCompression: message.preserveInCompression,
        removable: true
      });
    } else {
      let reason = "";
      if (message.preserveInCompression === true) reason += "preserveInCompression=true; ";
      if (message.priority === "critical" /* CRITICAL */) reason += "priority=CRITICAL; ";
      if (message.role === "system") reason += "role=system; ";
      debugInfo.push({
        messageId: message.messageId,
        role: message.role,
        priority: message.priority,
        preserveInCompression: message.preserveInCompression,
        removable: false,
        reason
      });
    }
  }
  logger.debug("Removable message debug info:", debugInfo);
  return removable;
}
function sortMessagesByRemovalPriority(messages) {
  return [...messages].sort((a, b) => {
    const priorityOrder = {
      ["low" /* LOW */]: 0,
      ["normal" /* NORMAL */]: 1,
      ["high" /* HIGH */]: 2,
      ["critical" /* CRITICAL */]: 3
    };
    const aPriority = priorityOrder[a.priority || "normal" /* NORMAL */];
    const bPriority = priorityOrder[b.priority || "normal" /* NORMAL */];
    if (aPriority !== bPriority) {
      return aPriority - bPriority;
    }
    const aTime = a.timestamp || 0;
    const bTime = b.timestamp || 0;
    return aTime - bTime;
  });
}
function createCompressionResult(originalMessages, compressedMessages, removedMessages, strategy) {
  const originalTokenCount = calculateTotalTokens(originalMessages);
  const compressedTokenCount = calculateTotalTokens(compressedMessages);
  let compressionRatio = 1;
  if (originalTokenCount > 0) {
    compressionRatio = compressedTokenCount / originalTokenCount;
    if (compressionRatio > 1) {
      compressionRatio = 1;
    }
  }
  return {
    compressedMessages,
    removedMessages,
    originalTokenCount,
    compressedTokenCount,
    compressionRatio,
    strategy,
    timestamp: Date.now()
  };
}
function validateCompressionResult(result, minMessagesToKeep) {
  if (result.compressedMessages.length < minMessagesToKeep) {
    logger.warn("Compression removed too many messages", {
      remaining: result.compressedMessages.length,
      minimum: minMessagesToKeep
    });
    return false;
  }
  const allMessages = result.compressedMessages.concat(result.removedMessages);
  const systemMessages = result.compressedMessages.filter((m) => m.role === "system");
  const originalSystemMessages = allMessages.filter((m) => m.role === "system");
  if (systemMessages.length !== originalSystemMessages.length) {
    logger.warn("Compression removed system messages");
    return false;
  }
  const criticalMessages = result.compressedMessages.filter(
    (m) => m.priority === "critical" /* CRITICAL */
  );
  const originalCriticalMessages = allMessages.filter((m) => m.priority === "critical" /* CRITICAL */);
  if (criticalMessages.length !== originalCriticalMessages.length) {
    logger.warn("Compression removed critical messages");
    return false;
  }
  if (result.compressionRatio >= 1) {
    logger.warn("Compression achieved no token reduction", {
      ratio: result.compressionRatio
    });
    return false;
  }
  return true;
}
function ensureMessageIds(messages) {
  return messages.map((message, index) => ({
    ...message,
    messageId: message.messageId || `msg_${Date.now()}_${index}`
  }));
}
function logCompressionOperation(operation, result, context) {
  logger.debug(`Compression: ${operation}`, {
    strategy: result.strategy,
    originalTokens: result.originalTokenCount,
    compressedTokens: result.compressedTokenCount,
    ratio: result.compressionRatio,
    messagesRemoved: result.removedMessages.length,
    messagesRemaining: result.compressedMessages.length,
    ...context
  });
}
function calculateCompressionEfficiency(result) {
  const tokenReduction = 1 - result.compressionRatio;
  const messagePreservation = result.compressedMessages.length / (result.compressedMessages.length + result.removedMessages.length);
  return tokenReduction * 0.6 + messagePreservation * 0.4;
}
var init_utils3 = __esm({
  "src/core/brain/llm/compression/utils.ts"() {
    init_cjs_shims();
    init_types3();
    init_utils2();
    init_logger2();
  }
});

// src/core/brain/llm/compression/strategies/middle-removal.ts
var MiddleRemovalStrategy;
var init_middle_removal = __esm({
  "src/core/brain/llm/compression/strategies/middle-removal.ts"() {
    init_cjs_shims();
    init_types3();
    init_utils3();
    init_logger2();
    MiddleRemovalStrategy = class {
      name = "middle-removal";
      config;
      constructor(config2) {
        this.config = config2;
        logger.debug("MiddleRemovalStrategy initialized", config2);
      }
      async compress(messages, currentTokenCount, targetTokenCount) {
        const startTime = Date.now();
        const originalMessages = [...messages];
        let processedMessages = ensureMessageIds(messages);
        processedMessages = assignMessagePriorities(processedMessages);
        logger.debug("Starting middle removal compression", {
          messageCount: processedMessages.length,
          currentTokens: currentTokenCount,
          targetTokens: targetTokenCount,
          tokensToRemove: currentTokenCount - targetTokenCount
        });
        const minMessagesToKeep = this.config.minMessagesToKeep;
        const preserveStart = Math.min(this.config.preserveStart, processedMessages.length);
        const preserveEnd = Math.min(this.config.preserveEnd, processedMessages.length);
        const preservableMessages = getPreservableMessages(processedMessages);
        const removableMessages = getRemovableMessages(processedMessages);
        if (removableMessages.length === 0) {
          logger.warn("No removable messages found for compression");
          return createCompressionResult(originalMessages, processedMessages, [], this.name);
        }
        const sortedRemovable = removableMessages.sort((a, b) => {
          const aIndex = processedMessages.indexOf(a);
          const bIndex = processedMessages.indexOf(b);
          return aIndex - bIndex;
        });
        const startMessages = sortedRemovable.slice(0, preserveStart);
        const endMessages = sortedRemovable.slice(-preserveEnd);
        const middleMessages = sortedRemovable.slice(
          preserveStart,
          sortedRemovable.length - preserveEnd
        );
        let compressedMessages = [...preservableMessages, ...startMessages, ...endMessages];
        let removedMessages = [];
        let currentTokens = calculateTotalTokens(compressedMessages);
        for (const message of middleMessages) {
          if (compressedMessages.length <= minMessagesToKeep) {
            break;
          }
          if (currentTokens <= targetTokenCount) {
            break;
          }
          const messageIndex = compressedMessages.findIndex((m) => m.messageId === message.messageId);
          if (messageIndex !== -1) {
            compressedMessages.splice(messageIndex, 1);
          }
          removedMessages.push(message);
          currentTokens = calculateTotalTokens(compressedMessages);
          logger.debug("Removed middle message", {
            messageId: message.messageId,
            tokensAfterRemoval: currentTokens,
            targetTokens: targetTokenCount,
            messagesRemaining: compressedMessages.length
          });
        }
        if (compressedMessages.length < minMessagesToKeep) {
          const restoreCount = minMessagesToKeep - compressedMessages.length;
          const toRestore = removedMessages.slice(-restoreCount);
          compressedMessages.push(...toRestore);
          removedMessages = removedMessages.slice(0, -restoreCount);
          logger.warn("Restored messages to meet minMessagesToKeep", {
            restoreCount,
            minMessagesToKeep,
            messagesRestored: toRestore.map((m) => m.messageId)
          });
        }
        compressedMessages = compressedMessages.sort((a, b) => {
          const aIndex = processedMessages.indexOf(a);
          const bIndex = processedMessages.indexOf(b);
          return aIndex - bIndex;
        });
        logger.debug("Compression summary before validate", {
          messagesRemaining: compressedMessages.length,
          minMessagesToKeep,
          messagesRemoved: removedMessages.length,
          finalTokenCount: calculateTotalTokens(compressedMessages)
        });
        const result = createCompressionResult(
          originalMessages,
          compressedMessages,
          removedMessages,
          this.name
        );
        if (!validateCompressionResult(result, minMessagesToKeep)) {
          logger.warn("Middle removal compression validation failed, returning original messages");
          return createCompressionResult(originalMessages, originalMessages, [], this.name);
        }
        const duration = Date.now() - startTime;
        logCompressionOperation("middle removal completed", result, { duration });
        return result;
      }
      shouldCompress(currentTokenCount) {
        const threshold = this.config.maxTokens * this.config.compressionThreshold;
        return currentTokenCount >= threshold;
      }
      getCompressionLevel(currentTokenCount) {
        const warningThreshold = this.config.maxTokens * this.config.warningThreshold;
        const compressionThreshold = this.config.maxTokens * this.config.compressionThreshold;
        if (currentTokenCount < warningThreshold) {
          return 0 /* NONE */;
        } else if (currentTokenCount < compressionThreshold) {
          return 1 /* WARNING */;
        } else if (currentTokenCount < this.config.maxTokens * 0.95) {
          return 2 /* SOFT */;
        } else if (currentTokenCount < this.config.maxTokens) {
          return 3 /* HARD */;
        } else {
          return 4 /* EMERGENCY */;
        }
      }
      validateCompression(result) {
        return validateCompressionResult(result, this.config.minMessagesToKeep);
      }
      /**
       * Get strategy-specific statistics
       */
      getStrategyStats() {
        return {
          name: this.name,
          preserveStart: this.config.preserveStart,
          preserveEnd: this.config.preserveEnd,
          compressionType: "middle-removal",
          bestFor: "maintaining conversation flow and recent context"
        };
      }
    };
  }
});

// src/core/brain/llm/compression/strategies/oldest-removal.ts
var OldestRemovalStrategy;
var init_oldest_removal = __esm({
  "src/core/brain/llm/compression/strategies/oldest-removal.ts"() {
    init_cjs_shims();
    init_types3();
    init_utils3();
    init_logger2();
    OldestRemovalStrategy = class {
      name = "oldest-removal";
      config;
      constructor(config2) {
        this.config = config2;
        logger.debug("OldestRemovalStrategy initialized", config2);
      }
      async compress(messages, currentTokenCount, targetTokenCount) {
        const startTime = Date.now();
        const originalMessages = [...messages];
        let processedMessages = ensureMessageIds(messages);
        processedMessages = assignMessagePriorities(processedMessages);
        logger.debug("Starting oldest removal compression", {
          messageCount: processedMessages.length,
          currentTokens: currentTokenCount,
          targetTokens: targetTokenCount,
          tokensToRemove: currentTokenCount - targetTokenCount
        });
        const minMessagesToKeep = this.config.minMessagesToKeep;
        const preserveStart = Math.min(this.config.preserveStart, processedMessages.length);
        const preserveEnd = Math.min(this.config.preserveEnd, processedMessages.length);
        const preservableMessages = getPreservableMessages(processedMessages);
        const removableMessages = getRemovableMessages(processedMessages);
        if (removableMessages.length === 0) {
          logger.warn("No removable messages found for compression");
          return createCompressionResult(originalMessages, processedMessages, [], this.name);
        }
        const sortedForRemoval = sortMessagesByRemovalPriority(removableMessages).sort(
          (a, b) => (a.timestamp || 0) - (b.timestamp || 0)
        );
        logger.debug("Oldest removal strategy analysis", {
          totalRemovable: removableMessages.length,
          preserveStart,
          preserveEnd,
          sortedForRemovalCount: sortedForRemoval.length
        });
        let compressedMessages = [...preservableMessages, ...sortedForRemoval];
        let removedMessages = [];
        while (calculateTotalTokens(compressedMessages) > targetTokenCount && compressedMessages.length > minMessagesToKeep) {
          let oldestIndex = -1;
          let oldestTimestamp = Infinity;
          for (let i = 0; i < compressedMessages.length; i++) {
            const message = compressedMessages[i];
            if (!message) continue;
            if (preservableMessages.includes(message)) continue;
            const timestamp = message.timestamp || 0;
            if (timestamp < oldestTimestamp) {
              oldestTimestamp = timestamp;
              oldestIndex = i;
            }
          }
          if (oldestIndex >= 0) {
            const removedMessage = compressedMessages.splice(oldestIndex, 1)[0];
            if (removedMessage) {
              removedMessages.push(removedMessage);
              logger.debug("Removed oldest message", {
                messageId: removedMessage.messageId,
                timestamp: removedMessage.timestamp,
                priority: removedMessage.priority,
                remainingMessages: compressedMessages.length,
                currentTokens: calculateTotalTokens(compressedMessages)
              });
            }
          } else {
            break;
          }
        }
        compressedMessages = compressedMessages.sort((a, b) => {
          const aIndex = processedMessages.indexOf(a);
          const bIndex = processedMessages.indexOf(b);
          return aIndex - bIndex;
        });
        logger.debug("Compression summary before validate", {
          messagesRemaining: compressedMessages.length,
          minMessagesToKeep,
          messagesRemoved: removedMessages.length,
          finalTokenCount: calculateTotalTokens(compressedMessages)
        });
        const result = createCompressionResult(
          originalMessages,
          compressedMessages,
          removedMessages,
          this.name
        );
        if (!validateCompressionResult(result, minMessagesToKeep)) {
          logger.warn("Oldest removal compression validation failed, returning original messages");
          return createCompressionResult(originalMessages, originalMessages, [], this.name);
        }
        const duration = Date.now() - startTime;
        logCompressionOperation("oldest removal completed", result, { duration });
        return result;
      }
      shouldCompress(currentTokenCount) {
        const threshold = this.config.maxTokens * this.config.compressionThreshold;
        return currentTokenCount >= threshold;
      }
      getCompressionLevel(currentTokenCount) {
        const warningThreshold = this.config.maxTokens * this.config.warningThreshold;
        const compressionThreshold = this.config.maxTokens * this.config.compressionThreshold;
        if (currentTokenCount < warningThreshold) {
          return 0 /* NONE */;
        } else if (currentTokenCount < compressionThreshold) {
          return 1 /* WARNING */;
        } else if (currentTokenCount < this.config.maxTokens * 0.95) {
          return 2 /* SOFT */;
        } else if (currentTokenCount < this.config.maxTokens) {
          return 3 /* HARD */;
        } else {
          return 4 /* EMERGENCY */;
        }
      }
      validateCompression(result) {
        return validateCompressionResult(result, this.config.minMessagesToKeep);
      }
      getStrategyStats() {
        return {
          name: this.name,
          preserveStart: this.config.preserveStart,
          preserveEnd: this.config.preserveEnd,
          minMessagesToKeep: this.config.minMessagesToKeep,
          compressionType: "oldest-removal",
          bestFor: "long conversations where recent context is most important"
        };
      }
    };
  }
});

// src/core/brain/llm/compression/strategies/hybrid.ts
var HybridStrategy;
var init_hybrid = __esm({
  "src/core/brain/llm/compression/strategies/hybrid.ts"() {
    init_cjs_shims();
    init_types3();
    init_utils3();
    init_middle_removal();
    init_oldest_removal();
    init_logger2();
    HybridStrategy = class {
      name = "hybrid";
      config;
      middleRemovalStrategy;
      oldestRemovalStrategy;
      constructor(config2) {
        this.config = config2;
        this.middleRemovalStrategy = new MiddleRemovalStrategy(config2);
        this.oldestRemovalStrategy = new OldestRemovalStrategy(config2);
        logger.debug("HybridStrategy initialized", config2);
      }
      async compress(messages, currentTokenCount, targetTokenCount) {
        const startTime = Date.now();
        let processedMessages = ensureMessageIds(messages);
        processedMessages = assignMessagePriorities(processedMessages);
        logger.debug("Starting hybrid compression", {
          messageCount: processedMessages.length,
          currentTokens: currentTokenCount,
          targetTokens: targetTokenCount,
          tokensToRemove: currentTokenCount - targetTokenCount
        });
        const conversationAnalysis = this.analyzeConversation(
          processedMessages,
          currentTokenCount,
          targetTokenCount
        );
        logger.debug("Conversation analysis for hybrid strategy", conversationAnalysis);
        let result;
        if (conversationAnalysis.recommendedStrategy === "middle-removal") {
          logger.debug("Hybrid strategy choosing middle-removal");
          result = await this.middleRemovalStrategy.compress(
            messages,
            currentTokenCount,
            targetTokenCount
          );
        } else if (conversationAnalysis.recommendedStrategy === "oldest-removal") {
          logger.debug("Hybrid strategy choosing oldest-removal");
          result = await this.oldestRemovalStrategy.compress(
            messages,
            currentTokenCount,
            targetTokenCount
          );
        } else {
          logger.debug("Hybrid strategy using adaptive approach");
          result = await this.adaptiveCompress(messages, currentTokenCount, targetTokenCount);
        }
        result.strategy = this.name;
        const duration = Date.now() - startTime;
        logCompressionOperation("hybrid compression completed", result, {
          duration,
          analysis: conversationAnalysis,
          chosenStrategy: conversationAnalysis.recommendedStrategy || "adaptive"
        });
        return result;
      }
      async adaptiveCompress(messages, currentTokenCount, targetTokenCount) {
        const middleResult = await this.middleRemovalStrategy.compress(
          messages,
          currentTokenCount,
          targetTokenCount
        );
        const oldestResult = await this.oldestRemovalStrategy.compress(
          messages,
          currentTokenCount,
          targetTokenCount
        );
        const middleEfficiency = calculateCompressionEfficiency(middleResult);
        const oldestEfficiency = calculateCompressionEfficiency(oldestResult);
        logger.debug("Comparing compression strategies", {
          middleEfficiency,
          oldestEfficiency,
          middleTokens: middleResult.compressedTokenCount,
          oldestTokens: oldestResult.compressedTokenCount
        });
        if (middleEfficiency >= oldestEfficiency) {
          return middleResult;
        } else {
          return oldestResult;
        }
      }
      analyzeConversation(messages, currentTokenCount, targetTokenCount) {
        const totalMessages = messages.length;
        const avgMessageLength = currentTokenCount / totalMessages;
        const compressionRatio = targetTokenCount / currentTokenCount;
        const recentMessages = messages.slice(-5);
        const recentTokens = calculateTotalTokens(recentMessages);
        const recentRatio = recentTokens / currentTokenCount;
        const hasLongMessages = messages.some((m) => (m.tokenCount || 0) > 300);
        const hasSystemMessages = messages.some((m) => m.role === "system");
        const hasToolMessages = messages.some((m) => m.role === "tool");
        const compressionSeverity = this.getCompressionSeverity(compressionRatio);
        let recommendedStrategy = null;
        let confidence = 0;
        if (compressionSeverity === "light" && totalMessages < 20) {
          recommendedStrategy = "middle-removal";
          confidence = 0.8;
        } else if (compressionSeverity === "heavy" && totalMessages > 30) {
          recommendedStrategy = "oldest-removal";
          confidence = 0.9;
        } else if (recentRatio > 0.4) {
          recommendedStrategy = "middle-removal";
          confidence = 0.7;
        } else if (hasLongMessages && compressionSeverity !== "light") {
          recommendedStrategy = "oldest-removal";
          confidence = 0.6;
        } else if (hasToolMessages || hasSystemMessages) {
          recommendedStrategy = "middle-removal";
          confidence = 0.7;
        }
        if (confidence < 0.6) {
          recommendedStrategy = null;
        }
        return {
          totalMessages,
          avgMessageLength,
          compressionRatio,
          recentRatio,
          compressionSeverity,
          hasLongMessages,
          hasSystemMessages,
          hasToolMessages,
          recommendedStrategy,
          confidence
        };
      }
      getCompressionSeverity(compressionRatio) {
        if (compressionRatio > 0.8) return "light";
        if (compressionRatio > 0.6) return "moderate";
        return "heavy";
      }
      shouldCompress(currentTokenCount) {
        const threshold = this.config.maxTokens * this.config.compressionThreshold;
        return currentTokenCount >= threshold;
      }
      getCompressionLevel(currentTokenCount) {
        const warningThreshold = this.config.maxTokens * this.config.warningThreshold;
        const compressionThreshold = this.config.maxTokens * this.config.compressionThreshold;
        if (currentTokenCount < warningThreshold) {
          return 0 /* NONE */;
        } else if (currentTokenCount < compressionThreshold) {
          return 1 /* WARNING */;
        } else if (currentTokenCount < this.config.maxTokens * 0.95) {
          return 2 /* SOFT */;
        } else if (currentTokenCount < this.config.maxTokens) {
          return 3 /* HARD */;
        } else {
          return 4 /* EMERGENCY */;
        }
      }
      validateCompression(result) {
        return validateCompressionResult(result, this.config.minMessagesToKeep);
      }
      /**
       * Get strategy-specific statistics
       */
      getStrategyStats() {
        return {
          name: this.name,
          compressionType: "hybrid",
          strategies: ["middle-removal", "oldest-removal"],
          bestFor: "adaptive compression based on conversation characteristics",
          decisionFactors: [
            "conversation length",
            "compression severity",
            "recent context weight",
            "message types",
            "token distribution"
          ]
        };
      }
    };
  }
});

// src/core/brain/memory/service-cache.ts
function getServiceCache() {
  return ServiceCache.getInstance();
}
function createServiceKey(type, config2) {
  if (!config2) {
    return type;
  }
  const normalizedConfig = normalizeConfigForCache(config2);
  const configStr = JSON.stringify(normalizedConfig);
  const hash = Array.from(configStr).reduce((hash2, char) => {
    return (hash2 << 5) - hash2 + char.charCodeAt(0);
  }, 0);
  const cacheKey = `${type}:${Math.abs(hash)}`;
  return cacheKey;
}
function normalizeConfigForCache(config2) {
  if (!config2 || typeof config2 !== "object") {
    return config2;
  }
  const normalized = {};
  for (const [key, value] of Object.entries(config2)) {
    if (value === void 0 || value === null) {
      normalized[key] = "";
    } else if (typeof value === "boolean") {
      normalized[key] = value;
    } else if (typeof value === "string") {
      normalized[key] = value.trim().toLowerCase();
    } else if (typeof value !== "object") {
      normalized[key] = value;
    } else {
      normalized[key] = normalizeConfigForCache(value);
    }
  }
  return normalized;
}
var ServiceCache;
var init_service_cache = __esm({
  "src/core/brain/memory/service-cache.ts"() {
    init_cjs_shims();
    init_logger2();
    ServiceCache = class _ServiceCache {
      static instance = null;
      cache = /* @__PURE__ */ new Map();
      initPromises = /* @__PURE__ */ new Map();
      static getInstance() {
        if (!_ServiceCache.instance) {
          _ServiceCache.instance = new _ServiceCache();
        }
        return _ServiceCache.instance;
      }
      /**
       * Get or create a cached service
       */
      async getOrCreate(key, factory, options = {}) {
        if (this.cache.has(key)) {
          logger.debug(`ServiceCache: Cache hit for ${key}`);
          return this.cache.get(key);
        }
        if (this.initPromises.has(key)) {
          logger.debug(`ServiceCache: Waiting for in-progress creation of ${key}`);
          return await this.initPromises.get(key);
        }
        logger.debug(`ServiceCache: Cache miss, creating ${key}`, {
          cacheSize: this.cache.size,
          pendingPromises: this.initPromises.size,
          existingKeys: Array.from(this.cache.keys()).slice(0, 5)
          // Show first 5 keys for debugging
        });
        const promise = factory();
        this.initPromises.set(key, promise);
        try {
          const instance = await promise;
          this.cache.set(key, instance);
          this.initPromises.delete(key);
          if (options.ttl) {
            setTimeout(() => {
              this.cache.delete(key);
              logger.debug(`ServiceCache: Expired ${key} after ${options.ttl}ms`);
            }, options.ttl);
          }
          logger.debug(`ServiceCache: Cached ${key}`);
          return instance;
        } catch (error) {
          this.initPromises.delete(key);
          throw error;
        }
      }
      /**
       * Check if a service is cached
       */
      has(key) {
        return this.cache.has(key);
      }
      /**
       * Clear the cache
       */
      clear() {
        this.cache.clear();
        this.initPromises.clear();
        logger.debug("ServiceCache: Cache cleared");
      }
      /**
       * Get cache statistics
       */
      getStats() {
        return {
          cacheSize: this.cache.size,
          pendingPromises: this.initPromises.size,
          cachedKeys: Array.from(this.cache.keys())
        };
      }
    };
  }
});

// src/core/brain/llm/compression/factory.ts
async function createCompressionStrategy(config2) {
  const validatedConfig = CompressionConfigSchema.parse(config2);
  const serviceCache = getServiceCache();
  const cacheKey = createServiceKey("compressionStrategy", validatedConfig);
  return await serviceCache.getOrCreate(cacheKey, async () => {
    logger.debug("Creating compression strategy", {
      strategy: validatedConfig.strategy,
      maxTokens: validatedConfig.maxTokens
    });
    switch (validatedConfig.strategy) {
      case "middle-removal":
        return new MiddleRemovalStrategy(validatedConfig);
      case "oldest-removal":
        return new OldestRemovalStrategy(validatedConfig);
      case "hybrid":
        return new HybridStrategy(validatedConfig);
      default:
        logger.warn("Unknown compression strategy, falling back to middle-removal", {
          strategy: validatedConfig.strategy
        });
        return new MiddleRemovalStrategy(validatedConfig);
    }
  });
}
function getCompressionConfigForProvider(provider, model, contextWindow) {
  const baseConfig = {
    strategy: "hybrid",
    maxTokens: contextWindow || 8192,
    warningThreshold: 0.8,
    compressionThreshold: 0.9,
    preserveStart: 4,
    preserveEnd: 5,
    minMessagesToKeep: 4
  };
  switch (provider) {
    case "openai":
      if (model?.startsWith("gpt-4")) {
        return {
          ...baseConfig,
          strategy: "hybrid",
          preserveStart: 6,
          preserveEnd: 6,
          warningThreshold: 0.85
        };
      }
      if (model?.startsWith("o1-")) {
        return {
          ...baseConfig,
          strategy: "middle-removal",
          preserveStart: 8,
          preserveEnd: 8,
          warningThreshold: 0.9,
          compressionThreshold: 0.95
        };
      }
      break;
    case "anthropic":
      return {
        ...baseConfig,
        strategy: "oldest-removal",
        preserveStart: 5,
        preserveEnd: 7,
        warningThreshold: 0.85,
        compressionThreshold: 0.92
      };
    case "lmstudio":
    case "ollama":
      return {
        ...baseConfig,
        strategy: "hybrid",
        warningThreshold: 0.7,
        compressionThreshold: 0.8,
        preserveStart: 3,
        preserveEnd: 4,
        minMessagesToKeep: 3
      };
    case "google":
      if (model?.includes("1.5")) {
        return {
          ...baseConfig,
          strategy: "middle-removal",
          warningThreshold: 0.9,
          compressionThreshold: 0.95,
          preserveStart: 10,
          preserveEnd: 10
        };
      }
      break;
  }
  return baseConfig;
}
var init_factory3 = __esm({
  "src/core/brain/llm/compression/factory.ts"() {
    init_cjs_shims();
    init_types3();
    init_middle_removal();
    init_oldest_removal();
    init_hybrid();
    init_logger2();
    init_service_cache();
  }
});

// src/core/brain/llm/compression/index.ts
var init_compression = __esm({
  "src/core/brain/llm/compression/index.ts"() {
    init_cjs_shims();
    init_types3();
    init_factory3();
    init_utils3();
    init_middle_removal();
    init_oldest_removal();
    init_hybrid();
  }
});

// src/core/brain/llm/messages/manager.ts
var ContextManager;
var init_manager2 = __esm({
  "src/core/brain/llm/messages/manager.ts"() {
    init_cjs_shims();
    init_logger2();
    init_utils();
    init_tokenizer();
    init_compression();
    init_utils3();
    ContextManager = class {
      promptManager;
      formatter;
      historyProvider;
      sessionId;
      messages = [];
      // Token-aware compression components
      tokenizer;
      compressionStrategy;
      enableCompression = false;
      // Token tracking
      currentTokenCount = 0;
      compressionHistory = [];
      lastCompressionCheck = 0;
      // Configuration
      compressionConfig = {
        checkInterval: 5e3,
        // Check every 5 seconds
        maxCompressionHistory: 10
      };
      fallbackToMemory = false;
      constructor(formatter, promptManager, historyProvider, sessionId) {
        if (!formatter) {
          throw new Error("formatter is required");
        }
        this.formatter = formatter;
        this.promptManager = promptManager;
        this.historyProvider = historyProvider;
        this.sessionId = sessionId;
        logger.debug("ContextManager initialized with formatter", { formatter });
      }
      // Public API Methods
      async getSystemPrompt() {
        const result = await this.promptManager.generateSystemPrompt();
        logger.debug(`[SystemPrompt] Built complete system prompt (${result.content.length} chars)`);
        return result.content;
      }
      async addMessage(message) {
        this.validateMessage(message);
        await this.storeMessage(message);
        logger.debug(`Adding message to context: ${JSON.stringify(message, null, 2)}`);
        logger.debug(`Total messages in context: ${this.messages.length}`);
        if (this.enableCompression) {
          await this.updateTokenCount();
          await this.checkAndCompress();
        }
      }
      async restoreHistory() {
        if (this.historyProvider && this.sessionId) {
          try {
            this.messages = await this.historyProvider.getHistory(this.sessionId);
          } catch (err) {
            logger.error(`Failed to restore history from provider: ${err}`);
            this.fallbackToMemory = true;
          }
        }
      }
      async addUserMessage(textContent, imageData) {
        if (typeof textContent !== "string" || textContent.trim() === "") {
          throw new Error("Content must be a non-empty string.");
        }
        const messageParts = this.buildUserMessageContent(
          textContent,
          imageData
        );
        logger.debug(`Adding user message: ${JSON.stringify(messageParts, null, 2)}`);
        await this.addMessage({ role: "user", content: messageParts });
      }
      async addAssistantMessage(content, toolCalls) {
        if (content === null && (!toolCalls || toolCalls.length === 0)) {
          throw new Error("Must provide content or toolCalls.");
        }
        await this.addMessage({
          role: "assistant",
          content,
          ...toolCalls && toolCalls.length > 0 && { toolCalls }
        });
      }
      async addToolResult(toolCallId, name, result) {
        if (!toolCallId || !name) {
          throw new Error("addToolResult: toolCallId and name are required.");
        }
        const content = this.formatToolResultContent(result);
        await this.addMessage({ role: "tool", content, toolCallId, name });
      }
      async getFormattedMessage(_message) {
        try {
          return this.getAllFormattedMessages();
        } catch (error) {
          logger.error("Failed to get formatted messages", { error });
          throw new Error(
            `Failed to format message: ${error instanceof Error ? error.message : String(error)}`
          );
        }
      }
      async getAllFormattedMessages(includeSystemMessage = true) {
        try {
          const formattedMessages = [];
          const prompt = await this.getSystemPrompt();
          if (includeSystemMessage && prompt) {
            formattedMessages.push({ role: "system", content: prompt });
          }
          const validatedMessages = this.validateAndRepairMessageFlow(this.messages);
          for (const msg of validatedMessages) {
            const formatted = this.formatter.format(msg, null);
            if (Array.isArray(formatted)) {
              formattedMessages.push(...formatted);
            } else if (formatted && formatted !== null) {
              formattedMessages.push(formatted);
            }
          }
          logger.debug(
            `Formatted ${formattedMessages.length} messages from history of ${validatedMessages.length} messages`
          );
          return formattedMessages;
        } catch (error) {
          logger.error("Failed to format all messages", { error });
          throw new Error(
            `Failed to format messages: ${error instanceof Error ? error.message : String(error)}`
          );
        }
      }
      async processLLMStreamResponse(response) {
        if (this.formatter.parseStreamResponse) {
          const msgs = await this.formatter.parseStreamResponse(response) ?? [];
          await this.processMessages(msgs);
        } else {
          await this.processLLMResponse(response);
        }
      }
      async processLLMResponse(response) {
        const msgs = this.formatter.parseResponse(response) ?? [];
        await this.processMessages(msgs);
      }
      // Message retrieval methods
      getRawMessages() {
        return this.messages;
      }
      /**
       * @deprecated Use getRawMessagesAsync() for persistent storage support
       */
      getRawMessagesSync() {
        return this.messages;
      }
      async getRawMessagesAsync() {
        if (this.shouldUsePersistentStorage()) {
          try {
            return await this.historyProvider.getHistory(this.sessionId);
          } catch (err) {
            logger.error(
              `History provider failed in getRawMessages, falling back to in-memory: ${err}`
            );
            this.fallbackToMemory = true;
            return this.messages;
          }
        } else {
          return this.messages;
        }
      }
      async restoreHistoryPersistent() {
        if (this.shouldUsePersistentStorage()) {
          try {
            const history = await this.historyProvider.getHistory(this.sessionId);
            this.messages = history;
            logger.debug(`ContextManager: Restored ${history.length} messages from persistent storage`);
          } catch (err) {
            logger.error(
              `ContextManager: Failed to restore history, falling back to in-memory: ${err}`
            );
            this.fallbackToMemory = true;
          }
        }
      }
      // Compression configuration and management
      async configureCompression(provider, model, contextWindow) {
        try {
          this.tokenizer = this.createTokenizer(model, provider);
          this.compressionStrategy = await this.createCompressionStrategy(
            provider,
            model,
            contextWindow
          );
          this.enableCompression = true;
          await this.updateTokenCount();
          logger.debug("Token-aware compression configured", {
            provider,
            model,
            contextWindow,
            tokenizerProvider: this.tokenizer.provider,
            compressionStrategy: this.compressionStrategy.name,
            currentTokens: this.currentTokenCount
          });
        } catch (error) {
          logger.error("Failed to configure compression", { error, provider, model });
          this.enableCompression = false;
        }
      }
      getCompressionLevel() {
        if (!this.compressionStrategy) {
          return 0 /* NONE */;
        }
        return this.compressionStrategy.getCompressionLevel(this.currentTokenCount);
      }
      getTokenStats() {
        const maxTokens = this.compressionStrategy?.config.maxTokens || 0;
        return {
          currentTokens: this.currentTokenCount,
          maxTokens,
          utilization: maxTokens > 0 ? this.currentTokenCount / maxTokens : 0,
          compressionLevel: this.getCompressionLevel(),
          compressionHistory: this.compressionHistory.length
        };
      }
      async forceCompression() {
        if (!this.enableCompression || !this.compressionStrategy) {
          throw new Error("Compression not configured");
        }
        await this.performCompression();
        return this.compressionHistory[this.compressionHistory.length - 1] || null;
      }
      // Private helper methods
      validateMessage(message) {
        if (!message.role) {
          throw new Error("Role is required for a message");
        }
        switch (message.role) {
          case "user":
            this.validateUserMessage(message);
            break;
          case "assistant":
            this.validateAssistantMessage(message);
            break;
          case "tool":
            this.validateToolMessage(message);
            break;
          case "system":
            this.validateSystemMessage(message);
            break;
          default:
            throw new Error(`Unknown message role: ${message.role}`);
        }
      }
      validateUserMessage(message) {
        const hasValidArrayContent = Array.isArray(message.content) && message.content.length > 0;
        const hasValidStringContent = typeof message.content === "string" && message.content.trim() !== "";
        if (!hasValidArrayContent && !hasValidStringContent) {
          throw new Error(
            "User message content should be a non-empty string or a non-empty array of parts."
          );
        }
      }
      validateAssistantMessage(message) {
        if (message.content === null && (!message.toolCalls || message.toolCalls.length === 0)) {
          throw new Error("Assistant message must have content or toolCalls.");
        }
        if (message.toolCalls && !this.isValidToolCalls(message.toolCalls)) {
          throw new Error("Invalid toolCalls structure in assistant message.");
        }
      }
      validateToolMessage(message) {
        if (!message.toolCallId || !message.name || message.content === null) {
          throw new Error("Tool message missing required fields (toolCallId, name, content).");
        }
      }
      validateSystemMessage(message) {
        if (typeof message.content !== "string" || message.content.trim() === "") {
          throw new Error("System message content must be a non-empty string.");
        }
      }
      isValidToolCalls(toolCalls) {
        return Array.isArray(toolCalls) && !toolCalls.some((tc) => !tc.id || !tc.function?.name || !tc.function?.arguments);
      }
      async storeMessage(message) {
        if (this.shouldUsePersistentStorage()) {
          try {
            await this.historyProvider.saveMessage(this.sessionId, message);
            this.messages.push(message);
          } catch (err) {
            logger.error(`ContextManager: History provider failed, falling back to in-memory: ${err}`);
            this.fallbackToMemory = true;
            this.messages.push(message);
          }
        } else {
          this.messages.push(message);
        }
      }
      shouldUsePersistentStorage() {
        const hasHistoryProvider = !!this.historyProvider;
        const hasSessionId = !!this.sessionId;
        const notInFallback = !this.fallbackToMemory;
        const shouldUse = hasHistoryProvider && hasSessionId && notInFallback;
        return shouldUse;
      }
      buildUserMessageContent(textContent, imageData) {
        return imageData ? [
          { type: "text", text: textContent },
          {
            type: "image",
            image: imageData.image,
            mimeType: imageData.mimeType || "image/jpeg"
          }
        ] : [{ type: "text", text: textContent }];
      }
      formatToolResultContent(result) {
        if (result && typeof result === "object" && "image" in result) {
          const imagePart = result;
          return [
            {
              type: "image",
              image: getImageData(imagePart),
              mimeType: imagePart.mimeType || "image/jpeg"
            }
          ];
        }
        if (typeof result === "string") {
          return result;
        }
        if (Array.isArray(result)) {
          return result;
        }
        return JSON.stringify(result ?? "");
      }
      async processMessages(msgs) {
        for (const msg of msgs) {
          try {
            await this.addMessage(msg);
          } catch (error) {
            logger.error("Failed to process LLM response message", { error });
          }
        }
      }
      createTokenizer(model, provider) {
        const tokenizerConfig = getTokenizerConfigForModel(model || `${provider}-default`);
        return createTokenizer(tokenizerConfig);
      }
      async createCompressionStrategy(provider, model, contextWindow) {
        const compressionConfig = getCompressionConfigForProvider(provider, model, contextWindow);
        return createCompressionStrategy(compressionConfig);
      }
      // Token management methods
      async updateTokenCount() {
        if (!this.tokenizer || this.messages.length === 0) {
          this.currentTokenCount = 0;
          logger.debug("[TokenAware] Token count reset (no messages or tokenizer)");
          return;
        }
        try {
          const messageTokenCounts = await this.calculateMessageTokens();
          this.currentTokenCount = messageTokenCounts.reduce((sum, count) => sum + count, 0);
          logger.info(
            `[TokenAware] Token count updated: ${this.currentTokenCount} tokens for ${this.messages.length} messages`
          );
        } catch (error) {
          logger.error("[TokenAware] Failed to update token count", { error });
        }
      }
      async calculateMessageTokens() {
        return Promise.all(
          this.messages.map(async (message, _index) => {
            const textContent = this.extractTextFromMessage(message);
            const tokenCount = await this.tokenizer.countTokens(textContent);
            if ("tokenCount" in message) {
              message.tokenCount = tokenCount.total;
            }
            return tokenCount.total;
          })
        );
      }
      extractTextFromMessage(message) {
        if (typeof message.content === "string") {
          return message.content;
        }
        if (Array.isArray(message.content)) {
          return message.content.filter((part) => part.type === "text").map((part) => part.text).join(" ");
        }
        return "";
      }
      // Compression methods
      async checkAndCompress() {
        if (!this.shouldCheckCompression()) {
          return;
        }
        this.lastCompressionCheck = Date.now();
        const utilization = this.calculateUtilization();
        this.logCompressionWarningIfNeeded(utilization);
        if (this.compressionStrategy.shouldCompress(this.currentTokenCount)) {
          logger.info(
            `[TokenAware] Compression threshold reached (${Math.round(utilization * 100)}%), starting compression...`
          );
          await this.performCompression();
        }
      }
      shouldCheckCompression() {
        if (!this.enableCompression || !this.compressionStrategy || !this.tokenizer) {
          return false;
        }
        const now = Date.now();
        return now - this.lastCompressionCheck >= this.compressionConfig.checkInterval;
      }
      calculateUtilization() {
        return this.currentTokenCount / (this.compressionStrategy.config.maxTokens || 1);
      }
      logCompressionWarningIfNeeded(utilization) {
        const config2 = this.compressionStrategy.config;
        if (utilization >= config2.warningThreshold && utilization < config2.compressionThreshold) {
          logger.warn(
            `[TokenAware] Token usage warning: ${Math.round(utilization * 100)}% of context window (${this.currentTokenCount}/${config2.maxTokens})`
          );
        }
      }
      async performCompression() {
        if (!this.compressionStrategy || !this.tokenizer) {
          return;
        }
        try {
          const enhancedMessages = this.enhanceMessagesForCompression();
          const prioritizedMessages = assignMessagePriorities(enhancedMessages);
          const targetTokenCount = this.calculateTargetTokenCount();
          const compressionResult = await this.compressionStrategy.compress(
            prioritizedMessages,
            this.currentTokenCount,
            targetTokenCount
          );
          if (!this.compressionStrategy.validateCompression(compressionResult)) {
            logger.warn("[TokenAware] Compression validation failed, keeping original messages");
            return;
          }
          await this.applyCompressionResult(compressionResult);
        } catch (error) {
          logger.error("[TokenAware] Compression failed", { error });
        }
      }
      enhanceMessagesForCompression() {
        return this.messages.map((message, index) => ({
          ...message,
          messageId: `msg_${Date.now()}_${index}`,
          timestamp: Date.now() - (this.messages.length - index) * 1e3,
          tokenCount: this.extractTextFromMessage(message).length / 4
          // Rough estimate
        }));
      }
      calculateTargetTokenCount() {
        return Math.floor(this.compressionStrategy.config.maxTokens * 0.8);
      }
      async applyCompressionResult(compressionResult) {
        this.messages = compressionResult.compressedMessages.map((msg) => {
          const {
            // messageId,
            // timestamp,
            // tokenCount,
            // priority,
            // preserveInCompression,
            ...internalMessage
          } = msg;
          return internalMessage;
        });
        await this.updateTokenCount();
        this.updateCompressionHistory(compressionResult);
        logger.info(
          `[TokenAware] Compression completed: ${compressionResult.originalTokenCount} \u2192 ${compressionResult.compressedTokenCount} tokens`
        );
      }
      updateCompressionHistory(compressionResult) {
        this.compressionHistory.push(compressionResult);
        if (this.compressionHistory.length > this.compressionConfig.maxCompressionHistory) {
          this.compressionHistory.shift();
        }
      }
      /**
       * Validates and repairs message flow to ensure OpenAI compatibility.
       * OpenAI requires that tool messages always follow assistant messages with tool_calls.
       * This method removes orphaned tool messages and warns about inconsistencies.
       */
      validateAndRepairMessageFlow(messages) {
        const repairedMessages = [];
        let lastAssistantWithToolCalls = null;
        let orphanedToolMessages = 0;
        for (let i = 0; i < messages.length; i++) {
          const message = messages[i];
          if (!message) {
            continue;
          }
          switch (message.role) {
            case "system":
            case "user":
              repairedMessages.push(message);
              lastAssistantWithToolCalls = null;
              break;
            case "assistant":
              repairedMessages.push(message);
              if (message.toolCalls && message.toolCalls.length > 0) {
                lastAssistantWithToolCalls = message;
              } else {
                lastAssistantWithToolCalls = null;
              }
              break;
            case "tool":
              if (lastAssistantWithToolCalls && this.isValidToolResponse(message, lastAssistantWithToolCalls)) {
                repairedMessages.push(message);
              } else {
                orphanedToolMessages++;
              }
              break;
            default:
              repairedMessages.push(message);
              break;
          }
        }
        return repairedMessages;
      }
      /**
       * Validates that a tool message is a valid response to the given assistant message.
       */
      isValidToolResponse(toolMessage, assistantMessage) {
        if (!assistantMessage.toolCalls || assistantMessage.toolCalls.length === 0) {
          return false;
        }
        const matchingToolCall = assistantMessage.toolCalls.find(
          (tc) => tc.id === toolMessage.toolCallId
        );
        return !!matchingToolCall;
      }
    };
  }
});

// src/core/brain/llm/messages/types.ts
var init_types4 = __esm({
  "src/core/brain/llm/messages/types.ts"() {
    init_cjs_shims();
  }
});

// src/core/brain/llm/messages/index.ts
var init_messages = __esm({
  "src/core/brain/llm/messages/index.ts"() {
    init_cjs_shims();
    init_manager2();
    init_types4();
  }
});

// src/core/brain/llm/services/types.ts
var init_types5 = __esm({
  "src/core/brain/llm/services/types.ts"() {
    init_cjs_shims();
  }
});

// src/core/brain/llm/utils/tool-result-formatter.ts
function formatToolResult(toolName, result) {
  if (!result) {
    return source_default.gray("(no result)");
  }
  if (typeof result === "string") {
    return result;
  }
  try {
    if (toolName.includes("memory_search") || toolName.includes("search_memory")) {
      return formatMemorySearchResult(result);
    }
    if (toolName.includes("search_graph") || toolName.includes("query_graph") || toolName.includes("get_neighbors")) {
      console.log("Knowledge graph result");
      return formatKnowledgeGraphResult(result);
    }
    if (toolName.includes("reasoning") || toolName.includes("extract_reasoning") || toolName.includes("evaluate_reasoning")) {
      return formatReasoningResult(result);
    }
    if (toolName.includes("extract_and_operate") || toolName.includes("memory_operation")) {
      return formatMemoryOperationResult(result);
    }
    if (toolName.includes("add_node") || toolName.includes("add_edge") || toolName.includes("update_node") || toolName.includes("delete_node")) {
      return formatKnowledgeGraphModificationResult(result);
    }
    if (toolName.includes("read_file") || toolName.includes("write_file") || toolName.includes("list_files")) {
      return formatMCPFileResult(toolName, result);
    }
    return formatGenericResult(result);
  } catch (error) {
    logger.debug("Error formatting tool result, falling back to JSON", { error, toolName });
    if (result && result.error) {
      return source_default.red(`\u274C Error: ${result.error}`);
    }
    return JSON.stringify(result, null, 2);
  }
}
function formatMemorySearchResult(result) {
  if (!result.success) {
    return source_default.red(`\u274C Search failed: ${result.error || "Unknown error"}`);
  }
  const output = [];
  output.push(source_default.blue.bold(`\u{1F50D} Memory Search Results`));
  output.push(source_default.gray(`Query: "${result.query}"`));
  output.push(
    source_default.gray(`Found ${result.results.length} result(s) in ${result.metadata.searchTime}ms`)
  );
  output.push("");
  if (result.results.length === 0) {
    output.push(source_default.yellow("No results found"));
  } else {
    result.results.forEach((item, index) => {
      output.push(
        source_default.cyan(
          `${index + 1}. ${source_default.bold(item.text.substring(0, 80))}${item.text.length > 80 ? "..." : ""}`
        )
      );
      output.push(source_default.gray(`   \u{1F4CA} Similarity: ${(item.similarity * 100).toFixed(1)}%`));
      if (item.confidence) {
        output.push(source_default.gray(`   \u{1F3AF} Confidence: ${(item.confidence * 100).toFixed(1)}%`));
      }
      if (item.domain) {
        output.push(source_default.gray(`   \u{1F3F7}\uFE0F  Domain: ${item.domain}`));
      }
      output.push(source_default.gray(`   \u{1F3F7}\uFE0F  Tags: ${item.tags.join(", ")}`));
      output.push(source_default.gray(`   \u{1F4C5} ${new Date(item.timestamp).toLocaleString()}`));
      output.push("");
    });
    output.push(source_default.gray(`\u{1F4CA} Summary:`));
    output.push(
      source_default.gray(`   \u2022 Max similarity: ${(result.metadata.maxSimilarity * 100).toFixed(1)}%`)
    );
    output.push(
      source_default.gray(`   \u2022 Min similarity: ${(result.metadata.minSimilarity * 100).toFixed(1)}%`)
    );
    output.push(
      source_default.gray(
        `   \u2022 Average similarity: ${(result.metadata.averageSimilarity * 100).toFixed(1)}%`
      )
    );
    if (result.metadata.usedFallback) {
      output.push(source_default.yellow(`   \u26A0\uFE0F  Used fallback search`));
    }
  }
  return output.join("\n");
}
function formatKnowledgeGraphResult(result) {
  if (!result.success) {
    console.log("Knowledge graph result", result.error);
    return source_default.red(`\u274C Graph query failed: ${result.error || "Unknown error"}`);
  }
  const output = [];
  output.push(source_default.blue.bold(`\u{1F578}\uFE0F  Knowledge Graph Results`));
  if (result.message) {
    output.push(source_default.gray(result.message));
  }
  if (result.executionTime) {
    output.push(source_default.gray(`Execution time: ${result.executionTime}ms`));
  }
  output.push("");
  if (result.nodes && result.nodes.length > 0) {
    output.push(source_default.cyan.bold(`\u{1F4CD} Nodes (${result.nodes.length}):`));
    result.nodes.slice(0, 10).forEach((node, index) => {
      output.push(
        source_default.cyan(
          `  ${index + 1}. ${source_default.bold(node.name)} ${node.type ? source_default.gray(`(${node.type})`) : ""}`
        )
      );
      if (node.score) {
        output.push(source_default.gray(`     \u{1F4CA} Score: ${(node.score * 100).toFixed(1)}%`));
      }
      if (node.matchReason) {
        output.push(source_default.gray(`     \u{1F3AF} Match: ${node.matchReason}`));
      }
      if (node.properties && Object.keys(node.properties).length > 0) {
        const propStr = Object.entries(node.properties).slice(0, 3).map(([k, v]) => `${k}: ${v}`).join(", ");
        output.push(source_default.gray(`     \u{1F3F7}\uFE0F  ${propStr}`));
      }
    });
    if (result.nodes.length > 10) {
      output.push(source_default.gray(`  ... and ${result.nodes.length - 10} more`));
    }
    output.push("");
  }
  if (result.edges && result.edges.length > 0) {
    output.push(source_default.magenta.bold(`\u{1F517} Relationships (${result.edges.length}):`));
    result.edges.slice(0, 10).forEach((edge, index) => {
      output.push(
        source_default.magenta(
          `  ${index + 1}. ${source_default.bold(edge.source)} ${source_default.gray("\u2192")} ${source_default.bold(edge.target)}`
        )
      );
      output.push(source_default.gray(`     \u{1F3F7}\uFE0F  Type: ${edge.type}`));
      if (edge.score) {
        output.push(source_default.gray(`     \u{1F4CA} Score: ${(edge.score * 100).toFixed(1)}%`));
      }
    });
    if (result.edges.length > 10) {
      output.push(source_default.gray(`  ... and ${result.edges.length - 10} more`));
    }
    output.push("");
  }
  if (result.related && result.related.length > 0) {
    output.push(source_default.yellow.bold(`\u{1F517} Related Entities (${result.related.length}):`));
    result.related.slice(0, 5).forEach((rel, index) => {
      output.push(
        source_default.yellow(
          `  ${index + 1}. ${source_default.bold(rel.entity.name)} ${source_default.gray(`(distance: ${rel.distance})`)}`
        )
      );
      output.push(source_default.gray(`     \u{1F517} via ${rel.relationship.type}`));
    });
    if (result.related.length > 5) {
      output.push(source_default.gray(`  ... and ${result.related.length - 5} more`));
    }
    output.push("");
  }
  if (result.totalCount) {
    output.push(source_default.gray(`\u{1F4CA} Total results: ${result.totalCount}`));
  }
  return output.join("\n");
}
function formatReasoningResult(result) {
  if (!result.success) {
    return source_default.red(`\u274C Reasoning analysis failed: ${result.error || "Unknown error"}`);
  }
  const output = [];
  output.push(source_default.blue.bold(`\u{1F9E0} Reasoning Analysis`));
  output.push("");
  if (result.steps && result.steps.length > 0) {
    output.push(source_default.cyan.bold(`\u{1F4AD} Reasoning Steps (${result.steps.length}):`));
    result.steps.forEach((step, index) => {
      const typeIcon = getStepTypeIcon(step.type);
      output.push(source_default.cyan(`  ${index + 1}. ${typeIcon} ${source_default.bold(step.type)}`));
      output.push(
        source_default.gray(
          `     ${step.content.substring(0, 100)}${step.content.length > 100 ? "..." : ""}`
        )
      );
    });
    output.push("");
  }
  if (result.evaluation) {
    output.push(source_default.yellow.bold(`\u{1F4CA} Quality Evaluation:`));
    output.push(
      source_default.yellow(`  Quality Score: ${(result.evaluation.qualityScore * 100).toFixed(1)}%`)
    );
    if (result.evaluation.issues && result.evaluation.issues.length > 0) {
      output.push(source_default.red(`  Issues Found (${result.evaluation.issues.length}):`));
      result.evaluation.issues.forEach((issue, index) => {
        const severityColor = issue.severity === "high" ? source_default.red : issue.severity === "medium" ? source_default.yellow : source_default.gray;
        output.push(severityColor(`    ${index + 1}. ${issue.description}`));
      });
    }
    if (result.evaluation.suggestions && result.evaluation.suggestions.length > 0) {
      output.push(source_default.green(`  Suggestions (${result.evaluation.suggestions.length}):`));
      result.evaluation.suggestions.forEach((suggestion, index) => {
        output.push(source_default.green(`    ${index + 1}. ${suggestion}`));
      });
    }
    output.push("");
  }
  if (result.patterns && result.patterns.length > 0) {
    output.push(source_default.magenta.bold(`\u{1F50D} Similar Patterns (${result.patterns.length}):`));
    result.patterns.slice(0, 3).forEach((pattern, index) => {
      output.push(
        source_default.magenta(
          `  ${index + 1}. ${source_default.bold(pattern.context)} ${source_default.gray(`(${(pattern.similarity * 100).toFixed(1)}% match)`)}`
        )
      );
      output.push(source_default.gray(`     ${pattern.steps.length} steps`));
    });
    if (result.patterns.length > 3) {
      output.push(source_default.gray(`  ... and ${result.patterns.length - 3} more`));
    }
    output.push("");
  }
  return output.join("\n");
}
function formatMemoryOperationResult(result) {
  if (!result.success) {
    return source_default.red(`\u274C Memory operation failed: ${result.error || "Unknown error"}`);
  }
  const output = [];
  output.push(source_default.blue.bold(`\u{1F4BE} Memory Operation Results`));
  output.push("");
  if (result.operations) {
    const ops = result.operations;
    const summary = [];
    if (ops.added > 0) summary.push(source_default.green(`${ops.added} added`));
    if (ops.updated > 0) summary.push(source_default.yellow(`${ops.updated} updated`));
    if (ops.deleted > 0) summary.push(source_default.red(`${ops.deleted} deleted`));
    if (ops.unchanged > 0) summary.push(source_default.gray(`${ops.unchanged} unchanged`));
    if (summary.length > 0) {
      output.push(source_default.cyan(`\u{1F4CA} Summary: ${summary.join(", ")}`));
      output.push("");
    }
  }
  if (result.memories && result.memories.length > 0) {
    output.push(source_default.cyan.bold(`\u{1F4DD} Memory Entries (${result.memories.length}):`));
    result.memories.slice(0, 5).forEach((memory, index) => {
      const eventIcon = getMemoryEventIcon(memory.event);
      output.push(
        source_default.cyan(
          `  ${index + 1}. ${eventIcon} ${source_default.bold(memory.event)} - ${memory.text.substring(0, 60)}${memory.text.length > 60 ? "..." : ""}`
        )
      );
      if (memory.confidence) {
        output.push(source_default.gray(`     \u{1F3AF} Confidence: ${(memory.confidence * 100).toFixed(1)}%`));
      }
      if (memory.tags && memory.tags.length > 0) {
        output.push(source_default.gray(`     \u{1F3F7}\uFE0F  Tags: ${memory.tags.join(", ")}`));
      }
      if (memory.reasoning) {
        output.push(
          source_default.gray(
            `     \u{1F4AD} Reasoning: ${memory.reasoning.substring(0, 80)}${memory.reasoning.length > 80 ? "..." : ""}`
          )
        );
      }
    });
    if (result.memories.length > 5) {
      output.push(source_default.gray(`  ... and ${result.memories.length - 5} more`));
    }
    output.push("");
  }
  return output.join("\n");
}
function formatKnowledgeGraphModificationResult(result) {
  if (!result.success) {
    return source_default.red(`\u274C Graph modification failed: ${result.error || "Unknown error"}`);
  }
  const output = [];
  output.push(source_default.blue.bold(`\u{1F578}\uFE0F  Knowledge Graph Updated`));
  if (result.message) {
    output.push(source_default.gray(result.message));
  }
  output.push("");
  if (result.node) {
    output.push(
      source_default.green(
        `\u2705 Node: ${source_default.bold(result.node.name)} ${result.node.type ? source_default.gray(`(${result.node.type})`) : ""}`
      )
    );
    if (result.node.properties && Object.keys(result.node.properties).length > 0) {
      const propStr = Object.entries(result.node.properties).slice(0, 3).map(([k, v]) => `${k}: ${v}`).join(", ");
      output.push(source_default.gray(`   \u{1F3F7}\uFE0F  Properties: ${propStr}`));
    }
  }
  if (result.edge) {
    output.push(
      source_default.green(
        `\u2705 Relationship: ${source_default.bold(result.edge.source)} ${source_default.gray("\u2192")} ${source_default.bold(result.edge.target)}`
      )
    );
    output.push(source_default.gray(`   \u{1F3F7}\uFE0F  Type: ${result.edge.type}`));
    if (result.edge.properties && Object.keys(result.edge.properties).length > 0) {
      const propStr = Object.entries(result.edge.properties).slice(0, 3).map(([k, v]) => `${k}: ${v}`).join(", ");
      output.push(source_default.gray(`   \u{1F3F7}\uFE0F  Properties: ${propStr}`));
    }
  }
  if (result.statistics) {
    output.push("");
    output.push(source_default.gray(`\u{1F4CA} Graph Statistics:`));
    if (result.statistics.totalNodes) {
      output.push(source_default.gray(`   \u2022 Total nodes: ${result.statistics.totalNodes}`));
    }
    if (result.statistics.totalEdges) {
      output.push(source_default.gray(`   \u2022 Total edges: ${result.statistics.totalEdges}`));
    }
  }
  return output.join("\n");
}
function formatMCPFileResult(toolName, result) {
  if (result.error) {
    return source_default.red(`\u274C ${toolName} failed: ${result.error}`);
  }
  if (result.content) {
    const output = [];
    output.push(source_default.green(`\u2705 ${toolName} completed successfully`));
    if (toolName.includes("read_file") && Array.isArray(result.content)) {
      const textContent = result.content.find((item) => item.type === "text");
      if (textContent && textContent.text) {
        const lines = textContent.text.split("\n").length;
        const chars = textContent.text.length;
        output.push(source_default.gray(`\u{1F4C4} File read: ${lines} lines, ${chars} characters`));
      }
    } else if (toolName.includes("list_files") && Array.isArray(result.content)) {
      const files = result.content.filter((item) => item.type === "file").length;
      const dirs = result.content.filter((item) => item.type === "directory").length;
      output.push(source_default.gray(`\u{1F4C1} Listed: ${files} files, ${dirs} directories`));
    }
    return output.join("\n");
  }
  return source_default.green(`\u2705 ${toolName} completed successfully`);
}
function formatGenericResult(result) {
  if (result.success === false || result.error) {
    return source_default.red(`\u274C ${result.error || result.message || "Operation failed"}`);
  }
  const hasData = result.data || result.content && !result.error;
  const isExplicitSuccess = result.success === true;
  if (isExplicitSuccess || hasData) {
    const output = [];
    output.push(source_default.green(`\u2705 ${result.message || "Operation completed successfully"}`));
    if (result.data) {
      if (typeof result.data === "object" && result.data !== null) {
        const keys = Object.keys(result.data);
        if (keys.length > 0) {
          output.push("");
          output.push(source_default.gray("\u{1F4C4} Data:"));
          keys.slice(0, 5).forEach((key) => {
            const value = result.data[key];
            const displayValue = typeof value === "string" ? value : JSON.stringify(value);
            output.push(
              source_default.gray(
                `   \u2022 ${key}: ${displayValue.substring(0, 50)}${displayValue.length > 50 ? "..." : ""}`
              )
            );
          });
          if (keys.length > 5) {
            output.push(source_default.gray(`   ... and ${keys.length - 5} more fields`));
          }
        }
      }
    }
    return output.join("\n");
  }
  return source_default.green(`\u2705 ${result.message || "Operation completed successfully"}`);
}
function getStepTypeIcon(type) {
  switch (type.toLowerCase()) {
    case "thought":
      return "\u{1F4AD}";
    case "action":
      return "\u26A1";
    case "observation":
      return "\u{1F441}\uFE0F";
    case "decision":
      return "\u{1F3AF}";
    case "conclusion":
      return "\u{1F393}";
    case "reflection":
      return "\u{1FA9E}";
    default:
      return "\u2022";
  }
}
function getMemoryEventIcon(event) {
  switch (event.toUpperCase()) {
    case "ADD":
      return "\u2795";
    case "UPDATE":
      return "\u270F\uFE0F";
    case "DELETE":
      return "\u{1F5D1}\uFE0F";
    case "NONE":
      return "\u26AA";
    default:
      return "\u2022";
  }
}
var init_tool_result_formatter = __esm({
  "src/core/brain/llm/utils/tool-result-formatter.ts"() {
    init_cjs_shims();
    init_source();
    init_logger2();
  }
});

// src/core/brain/llm/services/openrouter.ts
var import_uuid, OpenRouterService;
var init_openrouter = __esm({
  "src/core/brain/llm/services/openrouter.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid = require("uuid");
    OpenRouterService = class {
      client;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      eventManager;
      constructor(client, model, mcpManager, contextManager, maxIterations = 5, unifiedToolManager) {
        this.client = client;
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("openrouter");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForOpenRouter(rawTools);
        }
        logger.silly(`Formatted tools for OpenRouter: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput);
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`OpenRouter tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`Error in OpenRouter service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(`Error processing request: ${errorMessage}`);
          return `Error processing request: ${errorMessage}`;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("OpenRouterService: Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const response = await this.client.chat.completions.create({
            model: this.model,
            messages
            // No tools for direct calls - this is for simple text generation
          });
          const responseText = response.choices[0]?.message?.content || "";
          logger.debug("OpenRouterService: Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("OpenRouterService: Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "openrouter",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in OpenRouter response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            const response = await this.client.chat.completions.create({
              model: this.model,
              messages: formattedMessages,
              tools: attempts === 1 ? tools : [],
              // Only offer tools on first attempt
              tool_choice: attempts === 1 ? "auto" : "none"
              // Disable tool choice on retry
            });
            logger.silly("OPENROUTER CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const message = response.choices[0]?.message;
            if (!message) {
              throw new Error("Received empty message from OpenRouter API");
            }
            return { message };
          } catch (error) {
            const apiError = error;
            logger.error(
              `Error in OpenRouter API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400 && apiError.error?.code === "context_length_exceeded") {
              logger.warn(
                `Context length exceeded. ContextManager compression might not be sufficient. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`Failed to get response from OpenRouter after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      formatToolsForOpenRouter(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/ollama.ts
var import_uuid2, OllamaService;
var init_ollama = __esm({
  "src/core/brain/llm/services/ollama.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid2 = require("uuid");
    OllamaService = class {
      openai;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      eventManager;
      constructor(openai, model, mcpManager, contextManager, maxIterations = 5, unifiedToolManager) {
        this.openai = openai;
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
      }
      /**
       * Set the event manager for emitting LLM response events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid2.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("openai");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForOpenAI(rawTools);
        }
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput);
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`Tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                logger.error(`Error executing tool ${toolName}:`, error);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: error instanceof Error ? error.message : String(error)
                });
              }
            }
          }
          throw new Error(`Maximum iterations (${this.maxIterations}) exceeded`);
        } catch (error) {
          logger.error("Error in Ollama service:", error);
          throw error;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("OllamaService: Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const response = await this.openai.chat.completions.create({
            model: this.model,
            messages
            // No tools for direct calls - this is for simple text generation
          });
          const responseText = response.choices[0]?.message?.content || "";
          logger.debug("OllamaService: Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("OllamaService: Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "ollama",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in Ollama response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            const response = await this.openai.chat.completions.create({
              model: this.model,
              messages: formattedMessages,
              tools: attempts === 1 ? tools : [],
              // Only offer tools on first attempt
              tool_choice: attempts === 1 ? "auto" : "none"
              // Disable tool choice on retry
            });
            logger.silly("OLLAMA CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const message = response.choices[0]?.message;
            if (!message) {
              throw new Error("Received empty message from Ollama API");
            }
            return { message };
          } catch (error) {
            const apiError = error;
            logger.error(
              `Error in Ollama API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400) {
              logger.warn(
                `Ollama API error - check if model '${this.model}' is available locally. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            if (apiError.code === "ECONNREFUSED" || apiError.code === "ENOTFOUND") {
              logger.error(
                "Failed to connect to Ollama server. Please ensure Ollama is running and accessible."
              );
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`Failed to get response from Ollama after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      formatToolsForOpenAI(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/lmstudio.ts
var import_uuid3, LMStudioService;
var init_lmstudio = __esm({
  "src/core/brain/llm/services/lmstudio.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid3 = require("uuid");
    LMStudioService = class {
      openai;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      eventManager;
      constructor(openai, model, mcpManager, contextManager, maxIterations = 5, unifiedToolManager) {
        this.openai = openai;
        this.model = model;
        this.mcpManager = mcpManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
        this.unifiedToolManager = unifiedToolManager;
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid3.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("openai");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForLMStudio(rawTools);
        }
        logger.silly(`Formatted tools for LM Studio: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput);
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`LM Studio tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`Error in LM Studio service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(`Error processing request: ${errorMessage}`);
          return `Error processing request: ${errorMessage}`;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("LMStudioService: Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const response = await this.openai.chat.completions.create({
            model: this.model,
            messages
            // No tools for direct calls - this is for simple text generation
          });
          const responseText = response.choices[0]?.message?.content || "";
          logger.debug("LMStudioService: Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("LMStudioService: Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "lmstudio",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in LM Studio response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            const response = await this.openai.chat.completions.create({
              model: this.model,
              messages: formattedMessages,
              tools: attempts === 1 ? tools : [],
              // Only offer tools on first attempt
              tool_choice: attempts === 1 ? "auto" : "none"
              // Disable tool choice on retry
            });
            logger.silly("LM STUDIO CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const message = response.choices[0]?.message;
            if (!message) {
              throw new Error("Received empty message from LM Studio API");
            }
            return { message };
          } catch (error) {
            const apiError = error;
            logger.error(
              `Error in LM Studio API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400) {
              logger.warn(
                `LM Studio API error - check if model '${this.model}' is loaded. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            if (apiError.code === "ECONNREFUSED" || apiError.code === "ENOTFOUND") {
              logger.error(
                "Failed to connect to LM Studio server. Please ensure LM Studio is running and accessible."
              );
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`Failed to get response from LM Studio after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      formatToolsForLMStudio(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/messages/formatters/aws.ts
var BedrockAnthropicMessageFormatter, BedrockLlamaMessageFormatter, BedrockTitanMessageFormatter, BedrockDeepSeekMessageFormatter, BedrockAI21MessageFormatter;
var init_aws = __esm({
  "src/core/brain/llm/messages/formatters/aws.ts"() {
    init_cjs_shims();
    BedrockAnthropicMessageFormatter = class {
      format(message, _systemPrompt = null, _tools) {
        const role = message.role;
        let contentArr = [];
        if (role === "user" || role === "assistant") {
          if (typeof message.content === "string") {
            if (message.content.trim()) {
              contentArr.push({ type: "text", text: message.content });
            }
          } else if (Array.isArray(message.content)) {
            for (const c of message.content) {
              if (typeof c === "string") {
                const textContent = c;
                if (textContent.trim()) {
                  contentArr.push({ type: "text", text: textContent });
                }
              } else if (c && typeof c === "object" && "type" in c) {
                if (c.type === "image" && "image" in c) {
                  contentArr.push({ type: "image", image: c.image, mimeType: c.mimeType });
                } else if (c.type === "text" && "text" in c && typeof c.text === "string") {
                  if (c.text.trim()) {
                    contentArr.push({ type: "text", text: c.text });
                  }
                }
              }
            }
          }
        } else if (role === "tool") {
          return [];
        } else if (role === "system") {
          return [];
        }
        if (contentArr.length === 0) {
          return [];
        }
        return [{ role, content: contentArr }];
      }
      parseResponse(response) {
        const internal = [];
        if (!response || !response.content) return internal;
        for (const block of response.content) {
          if (block.type === "text") {
            internal.push({ role: "assistant", content: block.text });
          } else if (block.type === "tool_use") {
            internal.push({
              role: "assistant",
              content: "",
              toolCalls: [
                {
                  id: block.id,
                  type: "function",
                  function: {
                    name: block.name,
                    arguments: JSON.stringify(block.input)
                  }
                }
              ]
            });
          }
        }
        return internal;
      }
      formatTools(_tools) {
        if (!_tools || typeof _tools !== "object") return [];
        return Object.entries(_tools).map(([toolName, tool]) => ({
          name: toolName,
          description: tool.description,
          input_schema: tool.parameters
        }));
      }
    };
    BedrockLlamaMessageFormatter = class {
      format(message, _systemPrompt = null) {
        let prompt = "<|begin_of_text|>";
        if (_systemPrompt) {
          prompt += `<|start_header_id|>system<|end_header_id|> ${_systemPrompt} <|eot_id|>`;
        }
        const role = message.role === "assistant" ? "assistant" : "user";
        const content = Array.isArray(message.content) ? message.content.filter((c) => c.type === "text").map((c) => c.text).join("") : message.content;
        prompt += `<|start_header_id|>${role}<|end_header_id|> ${content} <|eot_id|>`;
        prompt += "<|start_header_id|>assistant<|end_header_id|>";
        return [
          {
            prompt,
            max_gen_len: 512,
            temperature: 0.5,
            top_p: 0.9
          }
        ];
      }
      parseResponse(response) {
        return [{ role: "assistant", content: response.generation }];
      }
      formatTools(_tools) {
        return [];
      }
    };
    BedrockTitanMessageFormatter = class {
      format(message, systemPrompt = null) {
        let inputText = "";
        if (systemPrompt) {
          inputText += `${systemPrompt}

`;
        }
        const role = message.role === "assistant" ? "Bot" : "User";
        const content = Array.isArray(message.content) ? message.content.filter((c) => c.type === "text").map((c) => c.text).join("") : message.content;
        inputText += `${role}: ${content}
`;
        inputText += "Bot:";
        return [
          {
            inputText,
            textGenerationConfig: {
              maxTokenCount: 512,
              temperature: 0.7,
              topP: 0.9,
              stopSequences: []
            }
          }
        ];
      }
      parseResponse(response) {
        const textContent = response.results.map((result) => result.outputText).join("");
        return [{ role: "assistant", content: textContent }];
      }
      formatTools(_tools) {
        return [];
      }
    };
    BedrockDeepSeekMessageFormatter = class {
      format(message, systemPrompt = null) {
        let prompt = "<\uFF5Cbegin of sentence\uFF5C>";
        let firstUserMessage = true;
        const content = Array.isArray(message.content) ? message.content.filter((c) => c.type === "text").map((c) => c.text).join("") : message.content;
        if (message.role === "user") {
          let userContent = content;
          if (firstUserMessage && systemPrompt) {
            userContent = `${systemPrompt}

${content}`;
            firstUserMessage = false;
          }
          prompt += " " + userContent;
        } else if (message.role === "assistant") {
          prompt += " " + content;
        }
        prompt += " <think>\n";
        return [
          {
            prompt,
            temperature: 0.5,
            top_p: 0.9,
            max_tokens: 512
          }
        ];
      }
      parseResponse(response) {
        const textContent = response.choices.map((choice) => choice.text).join("");
        return [{ role: "assistant", content: textContent }];
      }
      formatTools(_tools) {
        return [];
      }
    };
    BedrockAI21MessageFormatter = class {
      format(message, systemPrompt = null) {
        const ai21Messages = [];
        if (systemPrompt) {
          ai21Messages.push({ role: "system", content: systemPrompt });
        }
        const content = Array.isArray(message.content) ? message.content.filter((c) => c.type === "text").map((c) => c.text).join("") : message.content;
        ai21Messages.push({
          role: message.role,
          content: content || ""
        });
        return [
          {
            messages: ai21Messages,
            temperature: 1,
            top_p: 1,
            max_tokens: 4096,
            frequency_penalty: 0,
            presence_penalty: 0
          }
        ];
      }
      parseResponse(response) {
        const textContent = response.choices.map((choice) => choice.message.content).join("");
        return [{ role: "assistant", content: textContent }];
      }
      formatTools(_tools) {
        return [];
      }
    };
  }
});

// src/core/brain/llm/services/aws.ts
var import_client_bedrock_runtime, import_util4, import_uuid4, AwsService;
var init_aws2 = __esm({
  "src/core/brain/llm/services/aws.ts"() {
    init_cjs_shims();
    import_client_bedrock_runtime = require("@aws-sdk/client-bedrock-runtime");
    init_logger2();
    init_tool_result_formatter();
    import_util4 = require("util");
    init_aws();
    init_event_types();
    import_uuid4 = require("uuid");
    AwsService = class {
      client;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      modelFamily;
      inferenceProfileArn;
      formatter;
      // Should be IMessageFormatter, but use any for now if needed
      eventManager;
      constructor(model, mcpManager, contextManager, unifiedToolManager, maxIterations = 10, awsConfig) {
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
        this.modelFamily = this.detectModelFamily(model);
        this.inferenceProfileArn = awsConfig?.inferenceProfileArn || process.env.AWS_BEDROCK_INFERENCE_PROFILE_ARN;
        switch (this.modelFamily) {
          case "anthropic" /* ANTHROPIC */:
            this.formatter = new BedrockAnthropicMessageFormatter();
            break;
          case "meta" /* META_LLAMA */:
            this.formatter = new BedrockLlamaMessageFormatter();
            break;
          case "amazon.titan" /* AMAZON_TITAN */:
            this.formatter = new BedrockTitanMessageFormatter();
            break;
          case "deepseek" /* DEEPSEEK */:
            this.formatter = new BedrockDeepSeekMessageFormatter();
            break;
          case "ai21" /* AI21_LABS */:
            this.formatter = new BedrockAI21MessageFormatter();
            break;
          default:
            this.formatter = new BedrockAnthropicMessageFormatter();
        }
        const clientConfig = {
          region: awsConfig?.region || process.env.AWS_DEFAULT_REGION || "us-east-1"
        };
        const accessKeyId = awsConfig?.accessKeyId || process.env.AWS_ACCESS_KEY_ID;
        const secretAccessKey = awsConfig?.secretAccessKey || process.env.AWS_SECRET_ACCESS_KEY;
        const sessionToken = awsConfig?.sessionToken || process.env.AWS_SESSION_TOKEN;
        if (accessKeyId && secretAccessKey) {
          const credentials = {
            accessKeyId,
            secretAccessKey
          };
          if (sessionToken) {
            credentials.sessionToken = sessionToken;
          }
          clientConfig.credentials = credentials;
        }
        this.client = new import_client_bedrock_runtime.BedrockRuntimeClient(clientConfig);
        logger.info(
          `AWS Bedrock service initialized with model: ${model} (family: ${this.modelFamily}) in region: ${clientConfig.region}`
        );
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      detectModelFamily(modelId) {
        const id = modelId.toLowerCase();
        logger.info(`Detecting model family for: ${id}`);
        if (id.startsWith("anthropic.") || id.startsWith("us.anthropic.")) {
          logger.info(`Detected ANTHROPIC model family`);
          return "anthropic" /* ANTHROPIC */;
        }
        if (id.startsWith("meta.") || id.startsWith("us.meta.")) {
          logger.info(`Detected META_LLAMA model family`);
          return "meta" /* META_LLAMA */;
        }
        if (id.startsWith("amazon.titan")) {
          logger.debug(`Detected AMAZON_TITAN model family`);
          return "amazon.titan" /* AMAZON_TITAN */;
        }
        if (id.startsWith("amazon.nova")) {
          logger.debug(`Detected AMAZON_NOVA model family`);
          return "amazon.nova" /* AMAZON_NOVA */;
        }
        if (id.startsWith("ai21.") || id.startsWith("us.ai21.")) {
          logger.debug(`Detected AI21_LABS model family`);
          return "ai21" /* AI21_LABS */;
        }
        if (id.startsWith("cohere.") || id.startsWith("us.cohere.")) {
          logger.debug(`Detected COHERE model family`);
          return "cohere" /* COHERE */;
        }
        if (id.startsWith("deepseek") || id.startsWith("us.deepseek")) {
          logger.debug(`Detected DEEPSEEK model family`);
          return "deepseek" /* DEEPSEEK */;
        }
        if (id.startsWith("luma.")) {
          logger.debug(`Detected LUMA_AI model family`);
          return "luma" /* LUMA_AI */;
        }
        if (id.startsWith("mistral.") || id.startsWith("us.mistral.")) {
          logger.debug(`Detected MISTRAL_AI model family`);
          return "mistral" /* MISTRAL_AI */;
        }
        if (id.startsWith("stability.")) {
          logger.debug(`Detected STABILITY_AI model family`);
          return "stability" /* STABILITY_AI */;
        }
        if (id.startsWith("twelvelabs.")) {
          logger.debug(`Detected TWELVELABS model family`);
          return "twelvelabs" /* TWELVELABS */;
        }
        if (id.startsWith("writer.")) {
          logger.debug(`Detected WRITER model family`);
          return "writer" /* WRITER */;
        }
        logger.warn(`Unknown model family for ${modelId}, defaulting to Anthropic format`);
        return "anthropic" /* ANTHROPIC */;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid4.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("aws");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForBedrock(rawTools);
        }
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const response = await this.getAIResponse(formattedTools);
            const { textContent, toolCalls } = this.parseResponse(response);
            if (!toolCalls || toolCalls.length === 0) {
              await this.contextManager.addAssistantMessage(textContent);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: textContent
                });
              }
              return textContent;
            }
            if (textContent && textContent.trim()) {
              logger.info(`\u{1F4AD} ${textContent.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(textContent, toolCalls);
            if (toolCalls.length > 0) {
              for (const toolCall of toolCalls) {
                logger.debug(`Tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
                logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
                const toolName = toolCall.function.name;
                const args = JSON.parse(toolCall.function.arguments);
                try {
                  let toolResult;
                  if (this.unifiedToolManager) {
                    toolResult = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                  } else {
                    const toolExecutionResult = await this.mcpManager.executeTool(toolName, args);
                    toolResult = toolExecutionResult.content;
                  }
                  const formattedResult = formatToolResult(toolName, toolResult);
                  await this.contextManager.addToolResult(toolCall.id, toolName, formattedResult);
                  logger.debug(`Tool result: ${formattedResult}`);
                } catch (error) {
                  const errorMessage = `Tool execution failed: ${error instanceof Error ? error.message : "Unknown error"}`;
                  await this.contextManager.addToolResult(toolCall.id, toolName, errorMessage);
                  logger.error(`Tool execution error: ${errorMessage}`);
                }
              }
            }
          }
          throw new Error(`Maximum iterations (${this.maxIterations}) reached without completion`);
        } catch (error) {
          logger.error("AWS Bedrock generation error:", error);
          logger.error("Error details:", {
            message: error instanceof Error ? error.message : String(error),
            stack: error instanceof Error ? error.stack : void 0,
            iterationCount,
            modelFamily: this.modelFamily,
            model: this.model
          });
          throw error;
        }
      }
      async directGenerate(userInput, systemPrompt) {
        let messages;
        if (this.modelFamily === "anthropic" /* ANTHROPIC */) {
          messages = await this.contextManager.getAllFormattedMessages(false);
          if (!messages || messages.length === 0) {
            messages = [
              {
                role: "user",
                content: [{ type: "text", text: userInput }]
              }
            ];
          }
        } else {
          messages = await this.contextManager.getAllFormattedMessages();
        }
        let request;
        const formattedMessage = this.formatter.format(messages[0], systemPrompt)[0];
        if (this.modelFamily === "anthropic" /* ANTHROPIC */) {
          request = {
            messages: [formattedMessage],
            anthropic_version: "bedrock-2023-05-31",
            max_tokens: 4096,
            temperature: 0.7,
            ...systemPrompt ? { system: systemPrompt } : {}
          };
        } else {
          request = formattedMessage;
        }
        const commandParams = {
          modelId: this.model,
          contentType: "application/json",
          accept: "application/json",
          body: JSON.stringify(request)
        };
        if (this.inferenceProfileArn) {
          commandParams.inferenceConfig = { profileId: this.inferenceProfileArn };
        }
        const command = new import_client_bedrock_runtime.InvokeModelCommand(commandParams);
        const response = await this.client.send(command);
        const responseBody = JSON.parse(new import_util4.TextDecoder().decode(response.body));
        const { textContent } = this.parseResponse(responseBody);
        return textContent;
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          const combinedTools = await this.unifiedToolManager.getAllTools();
          const toolSet = {};
          for (const [toolName, toolInfo] of Object.entries(combinedTools)) {
            toolSet[toolName] = {
              description: toolInfo.description,
              parameters: toolInfo.parameters
            };
          }
          return toolSet;
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "aws",
          model: this.model
        };
      }
      async getAIResponse(formattedTools) {
        logger.info("Getting formatted messages from context manager");
        let messages;
        if (this.modelFamily === "anthropic" /* ANTHROPIC */) {
          messages = await this.contextManager.getAllFormattedMessages(false);
        } else {
          messages = await this.contextManager.getAllFormattedMessages();
        }
        logger.info(`Got ${messages.length} messages from context`);
        logger.info("Getting system prompt from context manager");
        const systemPrompt = await this.contextManager.getSystemPrompt();
        logger.info(`Got system prompt: ${systemPrompt ? "yes" : "no"}`);
        logger.info(`Building request for model family: ${this.modelFamily}`);
        let request;
        if (this.modelFamily === "anthropic" /* ANTHROPIC */) {
          const formattedMessages = messages.map((msg) => this.formatter.format(msg, systemPrompt)).flat().filter((msg) => msg !== null && msg !== void 0);
          request = {
            messages: formattedMessages,
            anthropic_version: "bedrock-2023-05-31",
            max_tokens: 4096,
            temperature: 0.7,
            ...systemPrompt ? { system: systemPrompt } : {}
          };
          if (formattedTools && formattedTools.length > 0) {
            request.tools = formattedTools;
            request.tool_choice = { type: "auto" };
          }
        } else {
          request = this.formatter.format(messages[0], systemPrompt)[0];
        }
        logger.info("Creating InvokeModelCommand");
        const commandParams = {
          modelId: this.model,
          contentType: "application/json",
          accept: "application/json",
          body: JSON.stringify(request)
        };
        if (this.inferenceProfileArn) {
          commandParams.inferenceConfig = { profileId: this.inferenceProfileArn };
        }
        const command = new import_client_bedrock_runtime.InvokeModelCommand(commandParams);
        logger.info("Sending command to Bedrock client");
        const response = await this.client.send(command);
        logger.info("Got response from Bedrock, parsing...");
        const parsedResponse = JSON.parse(new import_util4.TextDecoder().decode(response.body));
        logger.info("Response parsed successfully");
        return parsedResponse;
      }
      parseResponse(response) {
        switch (this.modelFamily) {
          case "anthropic" /* ANTHROPIC */:
            return this.parseAnthropicResponse(response);
          case "meta" /* META_LLAMA */:
            return this.parseLlamaResponse(response);
          case "amazon.titan" /* AMAZON_TITAN */:
            return this.parseTitanResponse(response);
          case "ai21" /* AI21_LABS */:
            return this.parseAI21Response(response);
          case "deepseek" /* DEEPSEEK */:
            return this.parseDeepSeekResponse(response);
          default:
            return this.parseAnthropicResponse(response);
        }
      }
      parseAnthropicResponse(response) {
        const toolUseBlocks = response.content.filter((block) => block.type === "tool_use");
        const textContent = response.content.filter((block) => block.type === "text").map((block) => block.text).join("");
        const toolCalls = toolUseBlocks.map((block) => ({
          id: block.id,
          type: "function",
          function: {
            name: block.name,
            arguments: JSON.stringify(block.input)
          }
        }));
        return { textContent, toolCalls };
      }
      parseLlamaResponse(response) {
        return {
          textContent: response.generation,
          toolCalls: []
          // Llama models don't support tool calling
        };
      }
      parseTitanResponse(response) {
        const textContent = response.results.map((result) => result.outputText).join("");
        return {
          textContent,
          toolCalls: []
          // Titan models don't support tool calling
        };
      }
      parseAI21Response(response) {
        const textContent = response.choices.map((choice) => choice.message.content).join("");
        return {
          textContent,
          toolCalls: []
          // AI21 models don't support tool calling in this implementation
        };
      }
      parseDeepSeekResponse(response) {
        const textContent = response.choices.map((choice) => choice.text).join("");
        return {
          textContent,
          toolCalls: []
          // DeepSeek models don't support tool calling
        };
      }
      formatToolsForBedrock(rawTools) {
        if (!rawTools || typeof rawTools !== "object") return [];
        return Object.entries(rawTools).map(([toolName, tool]) => ({
          name: toolName,
          description: tool.description,
          input_schema: tool.parameters
        }));
      }
    };
  }
});

// src/core/brain/llm/services/azure.ts
var import_openai3, import_uuid5, AzureService;
var init_azure = __esm({
  "src/core/brain/llm/services/azure.ts"() {
    init_cjs_shims();
    import_openai3 = require("@azure/openai");
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid5 = require("uuid");
    AzureService = class {
      client;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      deploymentName;
      eventManager;
      constructor(model, mcpManager, contextManager, unifiedToolManager, maxIterations = 5, azureConfig) {
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
        const endpoint = azureConfig?.endpoint || process.env.AZURE_OPENAI_ENDPOINT;
        const apiKey = process.env.AZURE_OPENAI_API_KEY;
        if (!endpoint) {
          throw new Error(
            "Azure OpenAI endpoint is required. Provide via config.azure.endpoint or AZURE_OPENAI_ENDPOINT environment variable"
          );
        }
        if (!apiKey) {
          throw new Error("AZURE_OPENAI_API_KEY environment variable is required");
        }
        this.deploymentName = azureConfig?.deploymentName || model;
        this.client = new import_openai3.OpenAIClient(endpoint, new import_openai3.AzureKeyCredential(apiKey));
        logger.info(
          `Azure OpenAI service initialized with deployment: ${this.deploymentName}, endpoint: ${endpoint}`
        );
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid5.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("azure");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForAzure(rawTools);
        }
        logger.silly(`Formatted tools: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput);
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`Tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`Error in Azure OpenAI service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(`Error processing request: ${errorMessage}`);
          return `Error processing request: ${errorMessage}`;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("AzureService: Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const response = await this.client.getChatCompletions(this.deploymentName, messages, {
            temperature: 0.7,
            maxTokens: 4096,
            topP: 1
            // No tools for direct calls - this is for simple text generation
          });
          const choice = response.choices[0];
          if (!choice) {
            throw new Error("No choices returned from Azure OpenAI");
          }
          const responseText = choice.message?.content || "";
          logger.debug("AzureService: Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("AzureService: Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "azure",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            logger.debug(`Sending ${formattedMessages.length} formatted messages to Azure OpenAI:`, {
              messages: formattedMessages.map((msg, idx) => ({
                index: idx,
                role: msg.role,
                hasContent: !!msg.content,
                hasToolCalls: !!msg.tool_calls,
                toolCallId: msg.toolCallId || msg.tool_call_id,
                name: msg.name
              }))
            });
            const requestOptions = {
              temperature: 0.7,
              maxTokens: 4096,
              topP: 1
            };
            if (attempts === 1 && tools.length > 0) {
              requestOptions.tools = tools;
              requestOptions.toolChoice = "auto";
            }
            const response = await this.client.getChatCompletions(
              this.deploymentName,
              formattedMessages,
              requestOptions
            );
            logger.silly("AZURE OPENAI CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const choice = response.choices[0];
            if (!choice) {
              throw new Error("Received empty message from Azure OpenAI API");
            }
            const message = choice.message;
            const normalizedMessage = {
              ...message,
              tool_calls: message.toolCalls || message.tool_calls || (message.functionCall ? [
                {
                  id: `call_${Date.now()}`,
                  type: "function",
                  function: {
                    name: message.functionCall.name,
                    arguments: message.functionCall.arguments
                  }
                }
              ] : void 0)
            };
            return { message: normalizedMessage };
          } catch (error) {
            const apiError = error;
            logger.error(
              `Error in Azure OpenAI API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400) {
              if (apiError.message?.includes("context_length_exceeded") || apiError.message?.includes("maximum context length")) {
                logger.warn(
                  `Context length exceeded in Azure OpenAI. ContextManager compression might not be sufficient. Error details: ${JSON.stringify(apiError)}`
                );
              }
              if (apiError.message?.includes("tool_call_id")) {
                logger.warn(
                  `Azure OpenAI tool_call_id error. This indicates message format issues. Error details: ${JSON.stringify(apiError)}`
                );
              }
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`Failed to get response from Azure OpenAI after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      formatToolsForAzure(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/index.ts
var init_services = __esm({
  "src/core/brain/llm/services/index.ts"() {
    init_cjs_shims();
    init_types5();
    init_openrouter();
    init_ollama();
    init_lmstudio();
    init_aws2();
    init_azure();
  }
});

// src/core/brain/llm/config.ts
var AwsConfigSchema, AzureConfigSchema, LLMConfigSchema;
var init_config2 = __esm({
  "src/core/brain/llm/config.ts"() {
    init_cjs_shims();
    init_zod();
    AwsConfigSchema = external_exports.object({
      region: external_exports.string().optional().describe("AWS region (defaults to us-east-1 or AWS_DEFAULT_REGION)"),
      accessKeyId: external_exports.string().optional().describe("AWS Access Key ID (can use AWS_ACCESS_KEY_ID env var)"),
      secretAccessKey: external_exports.string().optional().describe("AWS Secret Access Key (can use AWS_SECRET_ACCESS_KEY env var)"),
      sessionToken: external_exports.string().optional().describe("AWS Session Token (can use AWS_SESSION_TOKEN env var)"),
      inferenceProfileArn: external_exports.string().optional().describe(
        "ARN of the AWS Bedrock provisioned throughput (inference profile) for the selected model"
      )
    });
    AzureConfigSchema = external_exports.object({
      endpoint: external_exports.string().url().describe("Azure OpenAI endpoint URL (required for Azure)"),
      deploymentName: external_exports.string().optional().describe("Azure deployment name (defaults to model name if not provided)")
    });
    LLMConfigSchema = external_exports.object({
      provider: external_exports.string().nonempty().describe(
        "The LLM provider (e.g., 'openai', 'anthropic', 'openrouter', 'ollama', 'lmstudio', 'qwen', 'aws', 'azure', 'gemini')"
      ),
      model: external_exports.string().nonempty().describe("The specific model name for the selected provider"),
      apiKey: external_exports.string().optional().describe(
        "API key for the LLM provider (can also be set via environment variables using $VAR syntax). Not required for Ollama, LM Studio, or AWS (if using IAM roles)."
      ),
      maxIterations: external_exports.number().int().positive().optional().default(50).describe(
        "Maximum number of iterations for agentic loops or chained LLM calls, defaults to 50"
      ),
      baseURL: external_exports.string().url().optional().describe(
        "Base URL for the LLM provider (e.g., https://api.openai.com/v1, https://openrouter.ai/api/v1, http://localhost:1234/v1). \nSupported for OpenAI, OpenRouter, Ollama, LM Studio, and Qwen providers."
      ),
      qwenOptions: external_exports.object({
        enableThinking: external_exports.boolean().optional(),
        thinkingBudget: external_exports.number().int().positive().optional(),
        temperature: external_exports.number().min(0).max(2).optional(),
        top_p: external_exports.number().min(0).max(1).optional()
      }).optional().describe("Qwen-specific options for advanced configuration"),
      aws: AwsConfigSchema.optional().describe("AWS-specific configuration options"),
      azure: AzureConfigSchema.optional().describe("Azure-specific configuration options")
    }).strict().superRefine((data, ctx) => {
      const providerLower = data.provider?.toLowerCase();
      const supportedProvidersList = [
        "openai",
        "anthropic",
        "openrouter",
        "ollama",
        "lmstudio",
        // Added LM Studio as a supported provider
        "qwen",
        "aws",
        "azure",
        "gemini"
      ];
      if (!supportedProvidersList.includes(providerLower)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          path: ["provider"],
          message: `Provider '${data.provider}' is not supported. Supported: ${supportedProvidersList.join(", ")}`
        });
      }
      if (providerLower === "aws") {
        if (!data.aws) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["aws"],
            message: "AWS configuration object is required when using AWS provider"
          });
        }
      } else if (providerLower === "azure") {
        if (!data.azure) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["azure"],
            message: "Azure configuration object is required when using Azure provider"
          });
        } else if (!data.azure.endpoint) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["azure", "endpoint"],
            message: "Azure endpoint is required when using Azure provider"
          });
        }
        if (!data.apiKey || data.apiKey.trim().length === 0) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["apiKey"],
            message: "API key is required for Azure OpenAI provider"
          });
        }
      } else if (providerLower !== "ollama" && providerLower !== "aws" && providerLower !== "lmstudio") {
        if (!data.apiKey || data.apiKey.trim().length === 0) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            path: ["apiKey"],
            message: `API key is required for provider '${data.provider}'. Only Ollama, LM Studio, and AWS (with IAM roles) don't require an API key.`
          });
        }
      }
    });
  }
});

// src/core/brain/llm/errors.ts
var init_errors2 = __esm({
  "src/core/brain/llm/errors.ts"() {
    init_cjs_shims();
  }
});

// src/core/brain/llm/index.ts
var init_llm = __esm({
  "src/core/brain/llm/index.ts"() {
    init_cjs_shims();
    init_messages();
    init_services();
    init_config2();
    init_errors2();
  }
});

// src/core/brain/llm/messages/formatters/openai.ts
var OpenAIMessageFormatter;
var init_openai2 = __esm({
  "src/core/brain/llm/messages/formatters/openai.ts"() {
    init_cjs_shims();
    init_utils();
    OpenAIMessageFormatter = class {
      /**
       * Format the message into the specific structure of target LLM API.
       * This method maintains compatibility with the interface but is more efficient for single messages.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns The formatted message array.
       */
      format(message, systemPrompt = null) {
        const result = this.formatSingle(message, systemPrompt);
        return Array.isArray(result) ? result : [result];
      }
      /**
       * Format a single message more efficiently without always creating an array.
       * Use this when you know you're processing a single message and want optimal performance.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns A single formatted message object or array if system prompt is included.
       */
      formatSingle(message, systemPrompt = null) {
        if (systemPrompt) {
          const systemMessage = {
            role: "system",
            content: systemPrompt
          };
          const formattedMessage = this.formatMessageOnly(message);
          return [systemMessage, formattedMessage];
        }
        return this.formatMessageOnly(message);
      }
      /**
       * Format multiple messages efficiently.
       *
       * @param messages - Array of messages to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns Array of formatted messages.
       */
      formatMultiple(messages, systemPrompt = null) {
        const formatted = [];
        if (systemPrompt) {
          formatted.push({
            role: "system",
            content: systemPrompt
          });
        }
        for (const message of messages) {
          formatted.push(this.formatMessageOnly(message));
        }
        return formatted;
      }
      /**
       * Format a single message without any system prompt handling.
       * This is the core formatting logic extracted for reuse.
       *
       * @param message - The message to format.
       * @returns The formatted message object.
       */
      formatMessageOnly(message) {
        switch (message.role) {
          case "system":
            return {
              role: "system",
              content: message.content
            };
          case "user":
            return {
              role: "user",
              content: this.formatUserContent(message.content)
            };
          case "assistant":
            if (message.toolCalls && message.toolCalls.length > 0) {
              return {
                role: "assistant",
                content: message.content,
                tool_calls: message.toolCalls
              };
            } else {
              return {
                role: "assistant",
                content: message.content
              };
            }
          case "tool":
            return {
              role: "tool",
              content: message.content,
              tool_call_id: message.toolCallId,
              name: message.name
            };
          default:
            throw new Error(`Unsupported message role: ${message.role}`);
        }
      }
      parseResponse(response) {
        const internal = [];
        if (!response.choices || !Array.isArray(response.choices)) return internal;
        for (const choice of response.choices) {
          const msg = choice.message;
          if (!msg || !msg.role) continue;
          const role = msg.role;
          if (role === "assistant") {
            const content = msg.content ?? null;
            if (msg.tool_calls && Array.isArray(msg.tool_calls) && msg.tool_calls.length > 0) {
              const calls = msg.tool_calls.map((call) => ({
                id: call.id,
                type: "function",
                function: {
                  name: call.function.name,
                  arguments: call.function.arguments
                }
              }));
              internal.push({ role: "assistant", content, toolCalls: calls });
            } else {
              internal.push({ role: "assistant", content });
            }
          } else if (role === "tool") {
            internal.push({
              role: "tool",
              content: msg.content,
              toolCallId: msg.tool_call_id,
              name: msg.name
            });
          } else if (role === "user" || role === "system") {
            if (msg.content) {
              internal.push({ role, content: msg.content });
            }
          }
        }
        return internal;
      }
      /**
       * Format the user content into the specific structure of OpenAI API.
       *
       * @param content - The user content to format.
       * @returns The formatted user content.
       */
      formatUserContent(content) {
        if (!Array.isArray(content)) {
          return content;
        }
        return content.map((part) => {
          if (part.type === "text") {
            return { type: "text", text: part.text };
          }
          if (part.type === "image") {
            const raw = getImageData(part);
            const url = raw.startsWith("http://") || raw.startsWith("https://") || raw.startsWith("data:") ? raw : `data:${part.mimeType || "application/octet-stream"};base64,${raw}`;
            return { type: "image_url", image_url: { url } };
          }
          return null;
        }).filter(Boolean);
      }
    };
  }
});

// src/core/brain/llm/messages/formatters/anthropic.ts
var AnthropicMessageFormatter;
var init_anthropic2 = __esm({
  "src/core/brain/llm/messages/formatters/anthropic.ts"() {
    init_cjs_shims();
    init_logger2();
    init_utils();
    AnthropicMessageFormatter = class {
      /**
       * Format the message into the specific structure of target LLM API.
       * This method maintains compatibility with the interface but is more efficient for single messages.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns The formatted message array.
       */
      format(message, systemPrompt = null) {
        const result = this.formatSingle(message, systemPrompt);
        return Array.isArray(result) ? result : [result];
      }
      /**
       * Format a single message more efficiently without always creating an array.
       * Use this when you know you're processing a single message and want optimal performance.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns A single formatted message object or array if system prompt is included.
       */
      formatSingle(message, systemPrompt = null) {
        if (systemPrompt) {
          logger.debug(
            "System prompt provided for Anthropic formatter - will be handled separately by the service"
          );
        }
        return this.formatMessageOnly(message);
      }
      /**
       * Format multiple messages efficiently.
       *
       * @param messages - Array of messages to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns Array of formatted messages.
       */
      formatMultiple(messages, systemPrompt = null) {
        if (systemPrompt) {
          logger.debug(
            "System prompt provided for Anthropic formatter - will be handled separately by the service"
          );
        }
        return messages.map((message) => this.formatMessageOnly(message));
      }
      /**
       * Format a single message without any system prompt handling.
       * This is the core formatting logic extracted for reuse.
       *
       * @param message - The message to format.
       * @returns The formatted message object.
       */
      formatMessageOnly(message) {
        switch (message.role) {
          case "system":
            logger.warn(
              "System message encountered in Anthropic formatter - should be handled at API level"
            );
            return null;
          case "user":
            if (message.toolCallId) {
              return {
                role: "user",
                content: [
                  {
                    type: "tool_result",
                    tool_use_id: message.toolCallId,
                    content: message.content
                  }
                ]
              };
            }
            return {
              role: "user",
              content: this.formatUserContent(message.content)
            };
          case "assistant":
            if (message.toolCalls && message.toolCalls.length > 0) {
              const contentArray = [];
              if (message.content) {
                contentArray.push({
                  type: "text",
                  text: message.content
                });
              }
              for (const toolCall of message.toolCalls) {
                contentArray.push({
                  type: "tool_use",
                  id: toolCall.id,
                  name: toolCall.function.name,
                  input: JSON.parse(toolCall.function.arguments)
                });
              }
              return {
                role: "assistant",
                content: contentArray
              };
            } else {
              return {
                role: "assistant",
                content: message.content
              };
            }
          case "tool":
            return {
              role: "user",
              content: [
                {
                  type: "tool_result",
                  tool_use_id: message.toolCallId,
                  content: message.content
                }
              ]
            };
          default:
            throw new Error(`Unsupported message role: ${message.role}`);
        }
      }
      parseResponse(response) {
        const internal = [];
        if (!response || !Array.isArray(response.content)) {
          return internal;
        }
        let combinedText = null;
        const calls = [];
        for (const block of response.content) {
          if (block.type === "text") {
            combinedText = (combinedText ?? "") + block.text;
          } else if (block.type === "tool_use") {
            calls.push({
              id: block.id,
              type: "function",
              function: {
                name: block.name,
                arguments: JSON.stringify(block.input)
              }
            });
          }
        }
        const assistantMessage = {
          role: "assistant",
          content: combinedText
        };
        if (calls.length > 0) {
          assistantMessage.toolCalls = calls;
        }
        internal.push(assistantMessage);
        return internal;
      }
      /**
       * Format the user content into the specific structure of Anthropic API.
       *
       * @param content - The user content to format.
       * @returns The formatted user content.
       */
      formatUserContent(content) {
        if (!Array.isArray(content)) {
          return content;
        }
        return content.map((part) => {
          if (part.type === "text") {
            return { type: "text", text: part.text };
          }
          if (part.type === "image") {
            const raw = getImageData(part);
            let source;
            if (raw.startsWith("http://") || raw.startsWith("https://")) {
              source = { type: "url", url: raw };
            } else if (raw.startsWith("data:")) {
              const [meta, b64] = raw.split(",", 2);
              const mediaTypeMatch = meta?.match(/data:(.*);base64/);
              const media_type = mediaTypeMatch && mediaTypeMatch[1] || part.mimeType || "application/octet-stream";
              source = { type: "base64", media_type, data: b64 };
            } else {
              source = { type: "base64", media_type: part.mimeType, data: raw };
            }
            return { type: "image", source };
          }
          return null;
        }).filter(Boolean);
      }
    };
  }
});

// src/core/brain/llm/messages/formatters/azure.ts
var AzureMessageFormatter;
var init_azure2 = __esm({
  "src/core/brain/llm/messages/formatters/azure.ts"() {
    init_cjs_shims();
    init_utils();
    AzureMessageFormatter = class {
      /**
       * Format the message into the specific structure of Azure OpenAI API.
       * This method maintains compatibility with the interface but is more efficient for single messages.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns The formatted message array.
       */
      format(message, systemPrompt = null) {
        const result = this.formatSingle(message, systemPrompt);
        return Array.isArray(result) ? result : [result];
      }
      /**
       * Format a single message more efficiently without always creating an array.
       * Use this when you know you're processing a single message and want optimal performance.
       *
       * @param message - The message to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns A single formatted message object or array if system prompt is included.
       */
      formatSingle(message, systemPrompt = null) {
        if (systemPrompt) {
          const systemMessage = {
            role: "system",
            content: systemPrompt
          };
          const formattedMessage = this.formatMessageOnly(message);
          return [systemMessage, formattedMessage];
        }
        return this.formatMessageOnly(message);
      }
      /**
       * Format multiple messages efficiently.
       *
       * @param messages - Array of messages to format.
       * @param systemPrompt - The system prompt to include (optional).
       * @returns Array of formatted messages.
       */
      formatMultiple(messages, systemPrompt = null) {
        const formatted = [];
        if (systemPrompt) {
          formatted.push({
            role: "system",
            content: systemPrompt
          });
        }
        for (const message of messages) {
          formatted.push(this.formatMessageOnly(message));
        }
        return formatted;
      }
      /**
       * Format a single message without any system prompt handling.
       * This is the core formatting logic extracted for reuse.
       *
       * Handles Azure OpenAI specific field naming requirements.
       *
       * @param message - The message to format.
       * @returns The formatted message object.
       */
      formatMessageOnly(message) {
        switch (message.role) {
          case "system":
            return {
              role: "system",
              content: message.content
            };
          case "user":
            return {
              role: "user",
              content: this.formatUserContent(message.content)
            };
          case "assistant":
            if (message.toolCalls && message.toolCalls.length > 0) {
              return {
                role: "assistant",
                content: message.content,
                tool_calls: message.toolCalls
              };
            } else {
              return {
                role: "assistant",
                content: message.content
              };
            }
          case "tool":
            return {
              role: "tool",
              content: message.content,
              toolCallId: message.toolCallId,
              // Use camelCase for Azure
              name: message.name
            };
          default:
            throw new Error(`Unsupported message role: ${message.role}`);
        }
      }
      parseResponse(response) {
        const internal = [];
        if (!response.choices || !Array.isArray(response.choices)) return internal;
        for (const choice of response.choices) {
          const msg = choice.message;
          if (!msg || !msg.role) continue;
          const role = msg.role;
          if (role === "assistant") {
            const content = msg.content ?? null;
            const toolCalls = msg.tool_calls || msg.toolCalls || (msg.functionCall ? [
              {
                id: `call_${Date.now()}`,
                type: "function",
                function: {
                  name: msg.functionCall.name,
                  arguments: msg.functionCall.arguments
                }
              }
            ] : null);
            if (toolCalls && Array.isArray(toolCalls) && toolCalls.length > 0) {
              const calls = toolCalls.map((call) => ({
                id: call.id,
                type: "function",
                function: {
                  name: call.function.name,
                  arguments: call.function.arguments
                }
              }));
              internal.push({ role: "assistant", content, toolCalls: calls });
            } else {
              internal.push({ role: "assistant", content });
            }
          } else if (role === "tool") {
            internal.push({
              role: "tool",
              content: msg.content,
              // Handle both Azure (toolCallId) and OpenAI (tool_call_id) formats
              toolCallId: msg.toolCallId || msg.tool_call_id,
              name: msg.name
            });
          } else if (role === "user" || role === "system") {
            if (msg.content) {
              internal.push({ role, content: msg.content });
            }
          }
        }
        return internal;
      }
      /**
       * Format the user content into the specific structure of Azure OpenAI API.
       *
       * @param content - The user content to format.
       * @returns The formatted user content.
       */
      formatUserContent(content) {
        if (!Array.isArray(content)) {
          return content;
        }
        return content.map((part) => {
          if (part.type === "text") {
            return { type: "text", text: part.text };
          }
          if (part.type === "image") {
            const raw = getImageData(part);
            const url = raw.startsWith("http://") || raw.startsWith("https://") || raw.startsWith("data:") ? raw : `data:${part.mimeType || "application/octet-stream"};base64,${raw}`;
            return { type: "image_url", image_url: { url } };
          }
          return null;
        }).filter(Boolean);
      }
    };
  }
});

// src/core/brain/llm/messages/factory.ts
function getFormatter(provider) {
  const normalizedProvider = provider.toLowerCase();
  let formatter;
  switch (normalizedProvider) {
    case "openai":
    case "openrouter":
    case "ollama":
    case "lmstudio":
    case "qwen":
    case "gemini":
      formatter = new OpenAIMessageFormatter();
      break;
    case "azure":
      formatter = new AzureMessageFormatter();
      break;
    case "anthropic":
    case "aws":
      formatter = new AnthropicMessageFormatter();
      break;
    default:
      throw new Error(
        `Unsupported provider: ${provider}. Supported providers: openai, anthropic, openrouter, ollama, lmstudio, qwen, aws, azure, gemini`
      );
  }
  return formatter;
}
function createContextManager(config2, promptManager, historyProvider, sessionId) {
  try {
    LLMConfigSchema.parse(config2);
  } catch (error) {
    logger.error("Invalid LLM configuration provided to createContextManager", {
      config: config2,
      error: error instanceof Error ? error.message : String(error),
      validationIssues: error instanceof Error && "issues" in error ? error.issues : void 0
    });
    throw new Error(
      `Invalid LLM configuration: ${error instanceof Error ? error.message : String(error)}`
    );
  }
  const { provider, model } = config2;
  try {
    const formatter = getFormatter(provider);
    logger.debug("Created context manager", {
      provider: provider.toLowerCase(),
      model: model.toLowerCase(),
      formatterType: formatter.constructor.name
    });
    return new ContextManager(formatter, promptManager, historyProvider, sessionId);
  } catch (error) {
    logger.error("Failed to create context manager", {
      provider,
      model,
      error: error instanceof Error ? error.message : String(error)
    });
    throw error;
  }
}
var init_factory4 = __esm({
  "src/core/brain/llm/messages/factory.ts"() {
    init_cjs_shims();
    init_config2();
    init_openai2();
    init_anthropic2();
    init_azure2();
    init_manager2();
    init_logger2();
  }
});

// src/core/brain/llm/services/openai.ts
var import_uuid6, OpenAIService;
var init_openai3 = __esm({
  "src/core/brain/llm/services/openai.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid6 = require("uuid");
    OpenAIService = class {
      openai;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      eventManager;
      constructor(openai, model, mcpManager, contextManager, maxIterations = 5, unifiedToolManager) {
        this.openai = openai;
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
      }
      /**
       * Set the event manager for emitting LLM response events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid6.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("openai");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForOpenAI(rawTools);
        }
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput);
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`Tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                logger.error(`Error executing tool ${toolName}:`, error);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: error instanceof Error ? error.message : String(error)
                });
              }
            }
          }
          throw new Error(`Maximum iterations (${this.maxIterations}) exceeded`);
        } catch (error) {
          logger.error("Error in OpenAI service:", error);
          throw error;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("OpenAIService: Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const response = await this.openai.chat.completions.create({
            model: this.model,
            messages
            // No tools for direct calls - this is for simple text generation
          });
          const responseText = response.choices[0]?.message?.content || "";
          logger.debug("OpenAIService: Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("OpenAIService: Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "openai",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            logger.debug(`Sending ${formattedMessages.length} formatted messages to OpenAI:`, {
              messages: formattedMessages.map((msg, idx) => ({
                index: idx,
                role: msg.role,
                hasContent: !!msg.content,
                hasToolCalls: !!msg.tool_calls,
                toolCallId: msg.tool_call_id,
                name: msg.name
              }))
            });
            const response = await this.openai.chat.completions.create({
              model: this.model,
              messages: formattedMessages,
              tools: attempts === 1 ? tools : [],
              // Only offer tools on first attempt
              tool_choice: attempts === 1 ? "auto" : "none"
              // Disable tool choice on retry
            });
            logger.silly("OPENAI CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const message = response.choices[0]?.message;
            if (!message) {
              throw new Error("Received empty message from OpenAI API");
            }
            return { message };
          } catch (error) {
            const apiError = error;
            logger.error(
              `Error in OpenAI API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400 && apiError.error?.code === "context_length_exceeded") {
              logger.warn(
                `Context length exceeded. ContextManager compression might not be sufficient. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`Failed to get response from OpenAI after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      formatToolsForOpenAI(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/anthropic.ts
var import_uuid7, AnthropicService;
var init_anthropic3 = __esm({
  "src/core/brain/llm/services/anthropic.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid7 = require("uuid");
    AnthropicService = class {
      anthropic;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      eventManager;
      constructor(anthropic, model, mcpManager, contextManager, maxIterations = 5, unifiedToolManager) {
        this.anthropic = anthropic;
        this.model = model;
        this.mcpManager = mcpManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
        this.unifiedToolManager = unifiedToolManager;
      }
      /**
       * Set the event manager for emitting LLM response events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData, _stream) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid7.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("anthropic");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForAnthropic(rawTools);
        }
        logger.silly(`Formatted tools: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { response } = await this.getAIResponseWithRetries(formattedTools, userInput);
            let textContent = "";
            const toolUses = [];
            for (const content of response.content) {
              if (content.type === "text") {
                textContent += content.text;
              } else if (content.type === "tool_use") {
                toolUses.push(content);
              }
            }
            if (toolUses.length === 0) {
              await this.contextManager.addAssistantMessage(textContent);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: textContent
                });
              }
              return textContent;
            }
            if (textContent && textContent.trim()) {
              logger.info(`\u{1F4AD} ${textContent.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            const formattedToolCalls = toolUses.map((toolUse) => ({
              id: toolUse.id,
              type: "function",
              function: {
                name: toolUse.name,
                arguments: JSON.stringify(toolUse.input)
              }
            }));
            await this.contextManager.addAssistantMessage(textContent, formattedToolCalls);
            for (const toolUse of toolUses) {
              logger.debug(`Tool call initiated: ${JSON.stringify(toolUse, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolUse.name}`);
              const toolName = toolUse.name;
              const args = toolUse.input;
              const toolUseId = toolUse.id;
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolUseId, toolName, result);
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolUseId, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          if (this.eventManager && sessionId) {
            this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_ERROR, {
              sessionId,
              messageId,
              model: this.model,
              error: errorMessage,
              timestamp: Date.now()
            });
          }
          logger.error(`Error in Anthropic service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(`Error processing request: ${errorMessage}`);
          return `Error processing request: ${errorMessage}`;
        }
      }
      /**
       * Direct generate method that bypasses conversation context
       * Used for internal tool operations that shouldn't pollute conversation history
       * @param userInput - The input to generate a response for
       * @param systemPrompt - Optional system prompt to use
       * @returns Promise<string> - The generated response
       */
      async directGenerate(_userInput, systemPrompt) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug("AnthropicService: Direct generate call (bypassing conversation context)", {
          inputLength: _userInput.length,
          hasSystemPrompt: !!systemPrompt
        });
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const messages = [
              {
                role: "user",
                content: _userInput
              }
            ];
            const response = await this.anthropic.messages.create({
              model: this.model,
              messages,
              ...systemPrompt && { system: systemPrompt },
              max_tokens: 4096
              // No tools for direct calls - this is for simple text generation
            });
            let textContent = "";
            for (const content of response.content) {
              if (content.type === "text") {
                textContent += content.text;
              }
            }
            logger.debug("AnthropicService: Direct generate completed", {
              responseLength: textContent.length
            });
            return textContent;
          } catch (error) {
            const apiError = error;
            const errorStatus = apiError.status || apiError.error?.status;
            const errorType = apiError.error?.type || "unknown_error";
            const errorMessage = apiError.message || apiError.error?.message || "Unknown error";
            logger.error(
              `Error in Anthropic direct generate call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${errorMessage}`,
              {
                status: errorStatus,
                type: errorType,
                attempt: attempts,
                maxAttempts: MAX_ATTEMPTS,
                inputLength: _userInput.length
              }
            );
            const isRetryable = this.isRetryableError(errorStatus, errorType);
            if (attempts >= MAX_ATTEMPTS || !isRetryable) {
              if (!isRetryable) {
                logger.error(`Non-retryable error in direct generate: ${errorType} (${errorStatus})`);
              } else {
                logger.error(`Failed direct generate after ${MAX_ATTEMPTS} attempts.`);
              }
              throw new Error(`Direct generate failed: ${errorMessage}`);
            }
            const delay = this.calculateRetryDelay(attempts, errorStatus, errorType);
            logger.info(
              `Retrying direct generate in ${delay}ms... (Attempt ${attempts + 1}/${MAX_ATTEMPTS})`
            );
            await new Promise((resolve3) => setTimeout(resolve3, delay));
          }
        }
        throw new Error("Direct generate failed after maximum retry attempts");
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "anthropic",
          model: this.model
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, _userInput) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getAllFormattedMessages();
            const systemMessage = formattedMessages.find((msg) => msg.role === "system");
            const nonSystemMessages = formattedMessages.filter((msg) => msg.role !== "system");
            const response = await this.anthropic.messages.create({
              model: this.model,
              messages: nonSystemMessages,
              ...systemMessage && { system: systemMessage.content },
              tools: attempts === 1 ? tools : [],
              // Only offer tools on first attempt
              max_tokens: 4096
            });
            logger.silly("ANTHROPIC MESSAGE RESPONSE: ", JSON.stringify(response, null, 2));
            if (!response || !response.content) {
              throw new Error("Received empty response from Anthropic API");
            }
            return { response };
          } catch (error) {
            const apiError = error;
            const errorStatus = apiError.status || apiError.error?.status;
            const errorType = apiError.error?.type || "unknown_error";
            const errorMessage = apiError.message || apiError.error?.message || "Unknown error";
            logger.error(
              `Error in Anthropic API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${errorMessage}`,
              {
                status: errorStatus,
                type: errorType,
                attempt: attempts,
                maxAttempts: MAX_ATTEMPTS
              }
            );
            if (errorType === "invalid_request_error" && errorMessage.includes("maximum context length")) {
              logger.warn(
                `Context length exceeded. ContextManager compression might not be sufficient. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            const isRetryable = this.isRetryableError(errorStatus, errorType);
            if (attempts >= MAX_ATTEMPTS || !isRetryable) {
              if (!isRetryable) {
                logger.error(`Non-retryable error encountered: ${errorType} (${errorStatus})`);
              } else {
                logger.error(`Failed to get response from Anthropic after ${MAX_ATTEMPTS} attempts.`);
              }
              throw error;
            }
            const delay = this.calculateRetryDelay(attempts, errorStatus, errorType);
            logger.info(`Retrying in ${delay}ms... (Attempt ${attempts + 1}/${MAX_ATTEMPTS})`);
            await new Promise((resolve3) => setTimeout(resolve3, delay));
          }
        }
        throw new Error("Failed to get response after maximum retry attempts");
      }
      /**
       * Determines if an error is retryable based on status code and error type
       */
      isRetryableError(status, errorType) {
        if (status === 400 || status === 401 || status === 403 || status === 404) {
          return false;
        }
        if (errorType === "invalid_request_error" || errorType === "authentication_error") {
          return false;
        }
        return status >= 429 || status >= 500 || errorType === "overloaded_error" || !status;
      }
      /**
       * Calculates retry delay with exponential backoff and jitter
       */
      calculateRetryDelay(attempt, status, errorType) {
        let baseDelay = 1e3;
        if (status === 529 || errorType === "overloaded_error") {
          baseDelay = 3e3;
        }
        const exponentialDelay = Math.pow(2, attempt - 1) * baseDelay;
        const jitter = 0.5 + Math.random();
        const finalDelay = Math.min(exponentialDelay * jitter, 3e4);
        return Math.round(finalDelay);
      }
      formatToolsForAnthropic(tools) {
        return Object.entries(tools).map(([toolName, tool]) => {
          const input_schema = {
            type: "object",
            properties: {},
            required: []
          };
          if (tool.parameters) {
            const jsonSchemaParams = tool.parameters;
            if (jsonSchemaParams.type === "object" && jsonSchemaParams.properties) {
              input_schema.properties = jsonSchemaParams.properties;
              if (Array.isArray(jsonSchemaParams.required)) {
                input_schema.required = jsonSchemaParams.required;
              }
            } else {
              logger.warn(`Unexpected parameters format for tool ${toolName}:`, jsonSchemaParams);
            }
          } else {
            logger.debug(`Tool ${toolName} has no defined parameters.`);
          }
          return {
            name: toolName,
            description: tool.description,
            input_schema
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/qwen.ts
var import_uuid8, QwenService;
var init_qwen = __esm({
  "src/core/brain/llm/services/qwen.ts"() {
    init_cjs_shims();
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid8 = require("uuid");
    QwenService = class {
      openai;
      model;
      mcpManager;
      unifiedToolManager;
      contextManager;
      maxIterations;
      qwenOptions;
      eventManager;
      constructor(openai, model, mcpManager, contextManager, maxIterations = 5, qwenOptions = {}, unifiedToolManager) {
        this.openai = openai;
        this.model = model;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.contextManager = contextManager;
        this.maxIterations = maxIterations;
        this.qwenOptions = qwenOptions;
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData, stream) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid8.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.model,
            timestamp: startTime
          });
        }
        let formattedTools = [];
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("qwen") || [];
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForOpenAI(rawTools) || [];
        }
        logger.silly(`[Qwen] Formatted tools: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const { message } = await this.getAIResponseWithRetries(formattedTools, userInput, stream);
            if (!message.tool_calls || !Array.isArray(message.tool_calls) || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.model,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`[Qwen] \u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`[Qwen] Tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`[Qwen] \u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`[Qwen] Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`[Qwen] \u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`[Qwen] Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`[Qwen] Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`[Qwen] Error in Qwen service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(
            `[Qwen] Error processing request: ${errorMessage}`
          );
          return `[Qwen] Error processing request: ${errorMessage}`;
        }
      }
      async directGenerate(userInput, systemPrompt) {
        try {
          logger.debug("[QwenService] Direct generate call (bypassing conversation context)", {
            inputLength: userInput.length,
            hasSystemPrompt: !!systemPrompt
          });
          const messages = [];
          if (systemPrompt) {
            messages.push({
              role: "system",
              content: systemPrompt
            });
          }
          messages.push({
            role: "user",
            content: userInput
          });
          const apiOptions = {};
          apiOptions.enable_thinking = this.qwenOptions.enableThinking ?? false;
          if (this.qwenOptions.thinkingBudget !== void 0) {
            apiOptions.thinking_budget = this.qwenOptions.thinkingBudget;
          }
          if (this.qwenOptions.temperature !== void 0) {
            apiOptions.temperature = this.qwenOptions.temperature;
          }
          if (this.qwenOptions.top_p !== void 0) {
            apiOptions.top_p = this.qwenOptions.top_p;
          }
          const response = await this.openai.chat.completions.create({
            model: this.model,
            messages,
            ...apiOptions
          });
          const responseText = response.choices[0]?.message?.content || "";
          logger.debug("[QwenService] Direct generate completed", {
            responseLength: responseText.length
          });
          return responseText;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("[QwenService] Direct generate failed", {
            error: errorMessage,
            inputLength: userInput.length
          });
          throw new Error(`[QwenService] Direct generate failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        if (this.unifiedToolManager) {
          return await this.unifiedToolManager.getAllTools();
        }
        return this.mcpManager.getAllTools();
      }
      getConfig() {
        return {
          provider: "qwen",
          model: this.model
        };
      }
      async getAIResponseWithRetries(tools, userInput, stream) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`[Qwen] Tools in response: ${tools?.length || 0}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            logger.debug(`[Qwen] Sending ${formattedMessages.length} formatted messages to Qwen:`, {
              messages: formattedMessages.map((msg, idx) => ({
                index: idx,
                role: msg.role,
                hasContent: !!msg.content,
                hasToolCalls: !!msg.tool_calls,
                toolCallId: msg.tool_call_id,
                name: msg.name
              }))
            });
            const apiOptions = {};
            apiOptions.enable_thinking = this.qwenOptions.enableThinking ?? false;
            if (this.qwenOptions.thinkingBudget !== void 0) {
              apiOptions.thinking_budget = this.qwenOptions.thinkingBudget;
            }
            if (this.qwenOptions.temperature !== void 0) {
              apiOptions.temperature = this.qwenOptions.temperature;
            }
            if (this.qwenOptions.top_p !== void 0) {
              apiOptions.top_p = this.qwenOptions.top_p;
            }
            logger.debug("[Qwen] QwenOptions being sent to API:", {
              qwenOptions: this.qwenOptions,
              apiOptions,
              enableThinking: this.qwenOptions.enableThinking,
              enable_thinking: apiOptions.enable_thinking
            });
            const requestBody = {
              model: this.model,
              messages: formattedMessages,
              tools: attempts === 1 ? tools || [] : [],
              tool_choice: attempts === 1 ? "auto" : "none",
              ...apiOptions
            };
            if (stream !== void 0) {
              requestBody.stream = stream;
            }
            const response = await this.openai.chat.completions.create(requestBody);
            logger.silly("[Qwen] QWEN CHAT COMPLETION RESPONSE: ", JSON.stringify(response, null, 2));
            const message = response.choices[0]?.message;
            if (!message) {
              throw new Error("[Qwen] Received empty message from Qwen API");
            }
            return { message };
          } catch (error) {
            const apiError = error;
            logger.error(
              `[Qwen] Error in Qwen API call (Attempt ${attempts}/${MAX_ATTEMPTS}): ${apiError.message || JSON.stringify(apiError, null, 2)}`,
              { status: apiError.status, headers: apiError.headers }
            );
            if (apiError.status === 400 && apiError.error?.code === "context_length_exceeded") {
              logger.warn(
                `[Qwen] Context length exceeded. ContextManager compression might not be sufficient. Error details: ${JSON.stringify(apiError.error)}`
              );
            }
            if (attempts >= MAX_ATTEMPTS) {
              logger.error(`[Qwen] Failed to get response from Qwen after ${MAX_ATTEMPTS} attempts.`);
              throw error;
            }
            await new Promise((resolve3) => setTimeout(resolve3, 500 * attempts));
          }
        }
        throw new Error("[Qwen] Failed to get response after maximum retry attempts");
      }
      formatToolsForOpenAI(tools) {
        if (!tools || typeof tools !== "object") {
          return [];
        }
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/gemini.ts
var import_generative_ai, import_uuid9, GeminiService;
var init_gemini = __esm({
  "src/core/brain/llm/services/gemini.ts"() {
    init_cjs_shims();
    import_generative_ai = require("@google/generative-ai");
    init_logger2();
    init_tool_result_formatter();
    init_event_types();
    import_uuid9 = require("uuid");
    GeminiService = class {
      genAI;
      model;
      modelName;
      mcpManager;
      contextManager;
      maxIterations;
      unifiedToolManager = null;
      eventManager;
      constructor(apiKey, modelName, mcpManager, contextManager, maxIterations = 50, unifiedToolManager) {
        try {
          this.genAI = new import_generative_ai.GoogleGenerativeAI(apiKey);
          this.modelName = modelName;
          this.mcpManager = mcpManager;
          this.contextManager = contextManager;
          this.maxIterations = maxIterations;
          this.unifiedToolManager = unifiedToolManager || null;
          this.model = this.genAI.getGenerativeModel({ model: modelName });
          logger.debug("Gemini service initialized successfully", {
            model: modelName,
            hasUnifiedToolManager: !!unifiedToolManager
          });
        } catch (error) {
          logger.error("Failed to initialize Gemini service", {
            error: error instanceof Error ? error.message : String(error),
            model: modelName
          });
          throw error;
        }
      }
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async generate(userInput, imageData, _stream) {
        await this.contextManager.addUserMessage(userInput, imageData);
        const messageId = (0, import_uuid9.v4)();
        const startTime = Date.now();
        const sessionId = this.contextManager?.sessionId;
        if (this.eventManager && sessionId) {
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_STARTED, {
            sessionId,
            messageId,
            model: this.modelName,
            timestamp: startTime
          });
        }
        let formattedTools;
        if (this.unifiedToolManager) {
          formattedTools = await this.unifiedToolManager.getToolsForProvider("gemini");
        } else {
          const rawTools = await this.mcpManager.getAllTools();
          formattedTools = this.formatToolsForGemini(rawTools);
        }
        logger.silly(`Formatted tools for Gemini: ${JSON.stringify(formattedTools, null, 2)}`);
        let iterationCount = 0;
        let toolsUsedInThisConversation = false;
        try {
          while (iterationCount < this.maxIterations) {
            iterationCount++;
            const conversationHistory = await this.contextManager.getAllFormattedMessages();
            const hasToolCallsInHistory = conversationHistory.some(
              (msg) => msg.tool_calls && msg.tool_calls.length > 0
            );
            if (hasToolCallsInHistory) {
              toolsUsedInThisConversation = true;
            }
            const { message } = await this.getAIResponseWithRetries(
              formattedTools,
              userInput,
              toolsUsedInThisConversation
            );
            if (!message.tool_calls || message.tool_calls.length === 0) {
              const responseText = message.content || "";
              await this.contextManager.addAssistantMessage(responseText);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_RESPONSE_COMPLETED, {
                  sessionId,
                  messageId,
                  model: this.modelName,
                  duration: Date.now() - startTime,
                  timestamp: Date.now(),
                  response: responseText
                });
              }
              return responseText;
            }
            if (message.content && message.content.trim()) {
              logger.info(`\u{1F4AD} ${message.content.trim()}`);
              if (this.eventManager && sessionId) {
                this.eventManager.emitSessionEvent(sessionId, SessionEvents.LLM_THINKING, {
                  sessionId,
                  messageId,
                  timestamp: Date.now()
                });
              }
            }
            await this.contextManager.addAssistantMessage(message.content, message.tool_calls);
            for (const toolCall of message.tool_calls) {
              logger.debug(`Gemini tool call initiated: ${JSON.stringify(toolCall, null, 2)}`);
              logger.info(`\u{1F527} Using tool: ${toolCall.function.name}`);
              const toolName = toolCall.function.name;
              let args = {};
              try {
                args = JSON.parse(toolCall.function.arguments);
              } catch (e) {
                logger.error(`Error parsing arguments for ${toolName}:`, e);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: `Failed to parse arguments: ${e}`
                });
                continue;
              }
              try {
                let result;
                if (this.unifiedToolManager) {
                  result = await this.unifiedToolManager.executeTool(toolName, args, sessionId);
                } else {
                  result = await this.mcpManager.executeTool(toolName, args);
                }
                const formattedResult = formatToolResult(toolName, result);
                logger.info(`\u{1F4CB} Tool Result:
${formattedResult}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, result);
                toolsUsedInThisConversation = true;
              } catch (error) {
                const errorMessage = error instanceof Error ? error.message : String(error);
                logger.error(`Tool execution error for ${toolName}: ${errorMessage}`);
                await this.contextManager.addToolResult(toolCall.id, toolName, {
                  error: errorMessage
                });
              }
            }
          }
          logger.warn(`Reached maximum iterations (${this.maxIterations}) for task.`);
          const finalResponse = "Task completed but reached maximum tool call iterations.";
          await this.contextManager.addAssistantMessage(finalResponse);
          return finalResponse;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`Error in Gemini service API call: ${errorMessage}`, { error });
          await this.contextManager.addAssistantMessage(`Error processing request: ${errorMessage}`);
          return `Error processing request: ${errorMessage}`;
        }
      }
      async directGenerate(prompt) {
        try {
          logger.debug("Gemini service direct generation", {
            model: this.modelName,
            promptLength: prompt.length
          });
          const result = await this.model.generateContent(prompt);
          const response = result.response.text();
          logger.debug("Gemini service direct response generated", {
            responseLength: response.length
          });
          return response;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("Error in Gemini service direct generation", {
            error: errorMessage,
            model: this.modelName
          });
          throw new Error(`Gemini direct generation failed: ${errorMessage}`);
        }
      }
      async getAllTools() {
        const mcpTools = await this.mcpManager.getAllTools();
        const internalTools = this.unifiedToolManager ? await this.unifiedToolManager.getAllTools() : {};
        return {
          ...mcpTools,
          ...internalTools
        };
      }
      getConfig() {
        return {
          provider: "gemini",
          model: this.modelName
        };
      }
      // Helper methods
      async getAIResponseWithRetries(tools, userInput, toolsUsedInHistory) {
        let attempts = 0;
        const MAX_ATTEMPTS = 3;
        logger.debug(`Tools in Gemini response: ${tools.length}`);
        while (attempts < MAX_ATTEMPTS) {
          attempts++;
          try {
            const formattedMessages = await this.contextManager.getFormattedMessage({
              role: "user",
              content: userInput
            });
            logger.debug(`Sending ${formattedMessages.length} formatted messages to Gemini:`, {
              messages: formattedMessages.map((msg, idx) => ({
                index: idx,
                role: msg.role,
                hasContent: !!msg.content,
                hasToolCalls: !!msg.tool_calls,
                toolCallId: msg.tool_call_id,
                name: msg.name
              }))
            });
            const prompt = this.convertMessagesToPrompt(formattedMessages);
            let finalPrompt = prompt;
            if (attempts === 1 && tools.length > 0 && !toolsUsedInHistory) {
              finalPrompt = this.addToolsToPrompt(prompt, tools);
            } else if (toolsUsedInHistory) {
              finalPrompt = `${prompt}

IMPORTANT: Tools have already been used in this conversation. Please provide a final response based on the tool results that are already available. Do NOT make any additional tool calls.

CONVERSATION STYLE:
While you are primarily a programming assistant, you can engage in general conversation topics when appropriate. You don't need to restrict yourself to only programming discussions. Feel free to discuss various topics including sports, general knowledge, and other subjects when the user asks about them.`;
            }
            const result = await this.model.generateContent(finalPrompt);
            const response = result.response;
            const rawText = response.text();
            const parsedResponse = this.parseGeminiResponse(rawText);
            logger.silly("GEMINI GENERATE CONTENT RESPONSE: ", JSON.stringify(parsedResponse, null, 2));
            const message = parsedResponse;
            if (!message) {
              throw new Error("Received empty message from Gemini API");
            }
            return { message };
          } catch (error) {
            const errorMessage = error instanceof Error ? error.message : String(error);
            logger.error(`Gemini API call attempt ${attempts} failed: ${errorMessage}`, { error });
            if (attempts === MAX_ATTEMPTS) {
              throw new Error(`Gemini API call failed after ${MAX_ATTEMPTS} attempts: ${errorMessage}`);
            }
            await new Promise((resolve3) => setTimeout(resolve3, 1e3 * attempts));
          }
        }
        throw new Error("Gemini API call failed after all retry attempts");
      }
      convertMessagesToPrompt(messages) {
        logger.debug("convertMessagesToPrompt called with:", {
          messagesType: typeof messages,
          isArray: Array.isArray(messages),
          length: messages?.length,
          messages
        });
        if (!Array.isArray(messages) || messages.length === 0) {
          logger.debug("convertMessagesToPrompt returning empty string - no valid messages");
          return "";
        }
        const result = messages.filter((msg) => msg && (msg.content || msg.role)).map((msg) => {
          let content = "";
          if (typeof msg.content === "string") {
            content = msg.content;
          } else if (typeof msg.content === "object" && msg.content !== null) {
            content = JSON.stringify(msg.content);
          } else if (typeof msg === "string") {
            return msg;
          } else {
            content = String(msg.content || "");
          }
          if (msg.role === "system") {
            return `System: ${content}`;
          } else if (msg.role === "user") {
            return `User: ${content}`;
          } else if (msg.role === "assistant") {
            return `Assistant: ${content}`;
          } else {
            return content;
          }
        }).filter((line) => line.trim().length > 0).join("\n\n");
        logger.debug("convertMessagesToPrompt result:", {
          resultType: typeof result,
          resultLength: result?.length || 0,
          result
        });
        return result || "";
      }
      addToolsToPrompt(prompt, tools) {
        const toolDescriptions = tools.map((tool) => {
          const func = tool.function;
          const params = func.parameters ? JSON.stringify(func.parameters, null, 2) : "{}";
          return `Tool: ${func.name}
Description: ${func.description}
Parameters: ${params}`;
        }).join("\n\n");
        return `${prompt}

IMPORTANT: You have access to the following tools. If the user's request requires using any of these tools, you MUST respond with a tool call in the exact format shown below.

Available tools:
${toolDescriptions}

TOOL CALLING FORMAT:
When you need to use a tool, respond with ONLY the tool call in this exact format:

\`\`\`tool_code
{
  "tool": "tool_name",
  "arguments": {
    "param1": "value1",
    "param2": "value2"
  }
}
\`\`\`

CRITICAL RULES:
1. If the user asks to search for information, use the appropriate search tool ONCE
2. If the user asks to store or save information, use the appropriate storage tool ONCE
3. After using a tool, provide a final response based on the tool results - DO NOT make additional tool calls
4. Only respond with a tool call if you need to use a tool
5. If you don't need to use any tools, provide a direct response
6. NEVER include both a tool call and regular text in the same response
7. NEVER make multiple tool calls for the same request - one tool call maximum per user request

CONVERSATION STYLE:
While you are primarily a programming assistant, you can engage in general conversation topics when appropriate. You don't need to restrict yourself to only programming discussions. Feel free to discuss various topics including sports, general knowledge, and other subjects when the user asks about them.

For the current request, determine if you need to use any tools and respond accordingly. If you've already used a tool in a previous response, provide a final answer based on those results.`;
      }
      parseGeminiResponse(text) {
        const toolCallPattern = /```tool_code\s*\n?([^`]*)\n?```/gi;
        const toolCalls = [];
        let match;
        while ((match = toolCallPattern.exec(text)) !== null) {
          try {
            if (match[1]) {
              const toolCallData = JSON.parse(match[1].trim());
              toolCalls.push({
                id: `gemini_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`,
                type: "function",
                function: {
                  name: toolCallData.tool,
                  arguments: JSON.stringify(toolCallData.arguments || {})
                }
              });
            }
          } catch (e) {
            logger.warn("Failed to parse tool call from Gemini response", {
              toolCallText: match[1],
              error: e instanceof Error ? e.message : String(e)
            });
          }
        }
        let textContent = "";
        if (toolCalls.length > 0) {
          const firstToolCallIndex = text.search(/```tool_code/);
          if (firstToolCallIndex > 0) {
            textContent = text.substring(0, firstToolCallIndex).trim();
          }
        } else {
          textContent = this.cleanToolMetadata(text);
        }
        if (textContent.trim().length > 20 && toolCalls.length > 0) {
          logger.debug(
            "Gemini response contains both text and tool calls - prioritizing text to prevent redundant calls",
            {
              textContentLength: textContent.length,
              toolCallCount: toolCalls.length,
              textPreview: textContent.substring(0, 100)
            }
          );
          return {
            content: textContent,
            tool_calls: void 0
          };
        }
        logger.debug("Parsed Gemini response:", {
          hasToolCalls: toolCalls.length > 0,
          toolCallCount: toolCalls.length,
          textContentLength: textContent.length,
          toolCalls: toolCalls.map((tc) => tc.function.name)
        });
        return {
          content: textContent,
          tool_calls: toolCalls.length > 0 ? toolCalls : void 0
        };
      }
      /**
       * Clean tool-related metadata from Gemini responses
       * Removes code blocks containing tool instructions that shouldn't be shown to users
       */
      cleanToolMetadata(text) {
        if (!text) return text;
        const toolCodePattern = /```tool_code\s*\n?[^`]*\n?```\s*/gi;
        let cleaned = text.replace(toolCodePattern, "");
        const toolCommentPattern = /^\s*#\s*(No tools needed|Tool:|Query:).*$/gm;
        cleaned = cleaned.replace(toolCommentPattern, "");
        cleaned = cleaned.replace(/\n\s*\n\s*\n/g, "\n\n");
        cleaned = cleaned.trim();
        return cleaned;
      }
      formatToolsForGemini(tools) {
        return Object.entries(tools).map(([name, tool]) => {
          return {
            type: "function",
            function: {
              name,
              description: tool.description,
              parameters: tool.parameters
            }
          };
        });
      }
    };
  }
});

// src/core/brain/llm/services/factory.ts
function extractApiKey(config2) {
  const provider = config2.provider.toLowerCase();
  if (provider === "ollama" || provider === "lmstudio" || provider === "aws" || provider === "azure") {
    return "not-required";
  }
  let apiKey = config2.apiKey || "";
  if (!apiKey) {
    const errorMsg = `Error: API key for ${provider} not found`;
    logger.error(errorMsg);
    logger.error(`Please set your ${provider} API key in the config file or .env file`);
    throw new Error(errorMsg);
  }
  logger.debug("Verified API key");
  return apiKey;
}
function getOpenAICompatibleBaseURL(llmConfig) {
  if (llmConfig.baseURL) {
    let baseUrl = llmConfig.baseURL.replace(/\/$/, "");
    const provider2 = llmConfig.provider.toLowerCase();
    if (provider2 === "ollama" && !baseUrl.endsWith("/v1") && !baseUrl.endsWith("/api")) {
      baseUrl = baseUrl + "/v1";
    }
    return baseUrl;
  }
  const provider = llmConfig.provider.toLowerCase();
  if (provider === "openrouter") {
    return "https://openrouter.ai/api/v1";
  }
  if (provider === "ollama") {
    let baseUrl = env.OLLAMA_BASE_URL || "http://localhost:11434/v1";
    if (!baseUrl.endsWith("/v1") && !baseUrl.endsWith("/api")) {
      baseUrl = baseUrl.replace(/\/$/, "") + "/v1";
    }
    return baseUrl;
  }
  if (provider === "lmstudio") {
    return env.LMSTUDIO_BASE_URL || "http://localhost:1234/v1";
  }
  if (provider === "openai" && env.OPENAI_BASE_URL) {
    return env.OPENAI_BASE_URL.replace(/\/$/, "");
  }
  return "";
}
function _createLLMService(config2, mcpManager, contextManager, unifiedToolManager) {
  const apiKey = extractApiKey(config2);
  switch (config2.provider.toLowerCase()) {
    case "openai": {
      const baseURL = getOpenAICompatibleBaseURL(config2);
      const OpenAIClass = require("openai");
      const openai = new OpenAIClass({ apiKey, ...baseURL ? { baseURL } : {} });
      return new OpenAIService(
        openai,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        unifiedToolManager
      );
    }
    case "openrouter": {
      const baseURL = getOpenAICompatibleBaseURL(config2);
      const OpenAIClass = require("openai");
      const openai = new OpenAIClass({
        apiKey,
        baseURL,
        defaultHeaders: {
          "HTTP-Referer": "https://github.com/byterover/cipher",
          "X-Title": "Cipher Memory Agent"
        }
      });
      return new OpenRouterService(
        openai,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        unifiedToolManager
      );
    }
    case "lmstudio": {
      const baseURL = getOpenAICompatibleBaseURL(config2);
      const OpenAIClass = require("openai");
      const openai = new OpenAIClass({
        apiKey: "lm-studio",
        // LM Studio uses "lm-studio" as the API key
        baseURL
      });
      return new LMStudioService(
        openai,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        unifiedToolManager
      );
    }
    case "anthropic": {
      const AnthropicClass = require("@anthropic-ai/sdk");
      const anthropic = new AnthropicClass({ apiKey });
      return new AnthropicService(
        anthropic,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        unifiedToolManager
      );
    }
    case "ollama": {
      const baseURL = getOpenAICompatibleBaseURL(config2);
      const OpenAIClass = require("openai");
      const openai = new OpenAIClass({
        apiKey: "not-required",
        // Ollama doesn't require an API key
        baseURL
      });
      return new OllamaService(
        openai,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        unifiedToolManager
      );
    }
    case "aws": {
      return new AwsService(
        config2.model,
        mcpManager,
        contextManager,
        unifiedToolManager,
        config2.maxIterations,
        config2.aws
      );
    }
    case "azure": {
      return new AzureService(
        config2.model,
        mcpManager,
        contextManager,
        unifiedToolManager,
        config2.maxIterations,
        config2.azure
      );
    }
    case "qwen": {
      const baseURL = config2.baseURL || "https://dashscope-intl.aliyuncs.com/compatible-mode/v1";
      const OpenAIClass = require("openai");
      const openai = new OpenAIClass({ apiKey, baseURL });
      const qwenOptions = {
        ...config2.qwenOptions?.enableThinking !== void 0 && {
          enableThinking: config2.qwenOptions.enableThinking
        },
        ...config2.qwenOptions?.thinkingBudget !== void 0 && {
          thinkingBudget: config2.qwenOptions.thinkingBudget
        },
        ...config2.qwenOptions?.temperature !== void 0 && {
          temperature: config2.qwenOptions.temperature
        },
        ...config2.qwenOptions?.top_p !== void 0 && { top_p: config2.qwenOptions.top_p }
      };
      return new QwenService(
        openai,
        config2.model,
        mcpManager,
        contextManager,
        config2.maxIterations,
        qwenOptions,
        unifiedToolManager
      );
    }
    case "gemini": {
      logger.debug("Creating Gemini service", { model: config2.model, hasApiKey: !!apiKey });
      try {
        return new GeminiService(
          apiKey,
          config2.model,
          mcpManager,
          contextManager,
          config2.maxIterations,
          unifiedToolManager
        );
      } catch (error) {
        logger.error("Failed to create Gemini service", {
          error: error instanceof Error ? error.message : String(error),
          model: config2.model
        });
        throw error;
      }
    }
    default:
      throw new Error(`Unsupported LLM provider: ${config2.provider}`);
  }
}
function createLLMService(config2, mcpManager, contextManager, unifiedToolManager, eventManager) {
  const service = _createLLMService(config2, mcpManager, contextManager, unifiedToolManager);
  if (eventManager && typeof service.setEventManager === "function") {
    service.setEventManager(eventManager);
  }
  configureCompressionForService(config2, contextManager);
  return service;
}
async function configureCompressionForService(config2, contextManager) {
  try {
    const provider = config2.provider.toLowerCase();
    const model = config2.model;
    const contextWindow = getDefaultContextWindow(provider, model);
    setImmediate(async () => {
      try {
        await contextManager.configureCompression(provider, model, contextWindow);
        logger.debug("Token-aware compression configured for LLM service", {
          provider,
          model,
          contextWindow
        });
      } catch (error) {
        logger.warn("Failed to configure compression for LLM service", {
          error: error.message,
          provider,
          model
        });
      }
    });
  } catch (error) {
    logger.error("Error in compression configuration", { error });
  }
}
function getDefaultContextWindow(provider, model) {
  const defaults = {
    openai: {
      "gpt-3.5-turbo": 16385,
      "gpt-4": 8192,
      "gpt-4-32k": 32768,
      "gpt-4-turbo": 128e3,
      "gpt-4o": 128e3,
      "gpt-4o-mini": 128e3,
      "o1-preview": 128e3,
      "o1-mini": 128e3,
      default: 8192
    },
    anthropic: {
      "claude-3-opus": 2e5,
      "claude-3-sonnet": 2e5,
      "claude-3-haiku": 2e5,
      "claude-3-5-sonnet": 2e5,
      "claude-2.1": 2e5,
      "claude-2.0": 1e5,
      "claude-instant-1.2": 1e5,
      default: 2e5
    },
    gemini: {
      "gemini-pro": 32760,
      "gemini-pro-vision": 16384,
      "gemini-ultra": 32760,
      "gemini-1.5-pro": 1e6,
      "gemini-1.5-flash": 1e6,
      "gemini-1.5-pro-latest": 2e6,
      "gemini-1.5-flash-latest": 1e6,
      "gemini-2.0-flash": 1e6,
      "gemini-2.0-flash-exp": 1e6,
      "gemini-2.5-pro": 2e6,
      "gemini-2.5-flash": 1e6,
      "gemini-2.5-flash-lite": 1e6,
      default: 1e6
    },
    ollama: {
      default: 8192
      // Conservative default for local models
    },
    openrouter: {
      default: 8192
      // Varies by model, conservative default
    }
  };
  const providerDefaults = defaults[provider];
  if (!providerDefaults) {
    return 8192;
  }
  return providerDefaults[model || "default"] || providerDefaults.default || 8192;
}
var init_factory5 = __esm({
  "src/core/brain/llm/services/factory.ts"() {
    init_cjs_shims();
    init_env();
    init_logger2();
    init_openai3();
    init_anthropic3();
    init_openrouter();
    init_ollama();
    init_qwen();
    init_aws2();
    init_azure();
    init_gemini();
    init_lmstudio();
  }
});

// src/core/brain/reasoning/content-detector.ts
var DEFAULT_OPTIONS, ReasoningContentDetector;
var init_content_detector = __esm({
  "src/core/brain/reasoning/content-detector.ts"() {
    init_cjs_shims();
    init_logger2();
    init_factory4();
    init_factory5();
    DEFAULT_OPTIONS = {
      confidenceThreshold: 0.7,
      maxPatterns: 10,
      enableDetailedAnalysis: false
    };
    ReasoningContentDetector = class {
      llmService;
      promptManager;
      mcpManager;
      unifiedToolManager;
      options;
      evalLlmConfig;
      constructor(promptManager, mcpManager, unifiedToolManager, evalLlmConfig, options) {
        this.promptManager = promptManager;
        this.mcpManager = mcpManager;
        this.unifiedToolManager = unifiedToolManager;
        this.options = { ...DEFAULT_OPTIONS, ...options };
        this.evalLlmConfig = evalLlmConfig;
      }
      /**
       * Initialize the LLM service for reasoning analysis
       */
      async initializeLLMService() {
        if (this.llmService) return;
        try {
          const evalConfig = {
            ...this.evalLlmConfig,
            maxIterations: this.evalLlmConfig.maxIterations ?? 3
            // Default for eval tasks
          };
          const contextManager = createContextManager(
            evalConfig,
            this.promptManager,
            void 0,
            void 0
          );
          this.llmService = createLLMService(
            evalConfig,
            this.mcpManager,
            contextManager,
            this.unifiedToolManager
          );
          logger.debug("ReasoningContentDetector: LLM service initialized for analysis", {
            provider: evalConfig.provider,
            model: evalConfig.model
          });
        } catch (error) {
          logger.warn("ReasoningContentDetector: Failed to initialize LLM service for analysis", {
            error: error instanceof Error ? error.message : String(error)
          });
        }
      }
      /**
       * Detect if user input contains reasoning content
       */
      async detectReasoningContent(userInput, context) {
        try {
          logger.debug("ReasoningContentDetector: Analyzing user input for reasoning content", {
            inputLength: userInput.length
          });
          if (!userInput || userInput.trim().length < 3) {
            return {
              containsReasoning: false,
              confidence: 0,
              detectedPatterns: [],
              explanation: "Input too short for reasoning analysis"
            };
          }
          await this.initializeLLMService();
          if (this.llmService) {
            const result = await this.detectReasoningByLLM(userInput, context);
            logger.debug("ReasoningContentDetector: Detection completed", {
              containsReasoning: result.containsReasoning,
              confidence: result.confidence,
              patternCount: result.detectedPatterns.length
            });
            return result;
          } else {
            logger.warn(
              "ReasoningContentDetector: LLM service not available, cannot detect reasoning content"
            );
            return {
              containsReasoning: false,
              confidence: 0,
              detectedPatterns: [],
              explanation: "LLM service not available for reasoning analysis"
            };
          }
        } catch (error) {
          logger.error("ReasoningContentDetector: Error during reasoning detection", {
            error: error instanceof Error ? error.message : String(error)
          });
          return {
            containsReasoning: false,
            confidence: 0,
            detectedPatterns: [],
            explanation: "Error during reasoning detection"
          };
        }
      }
      // Keyword analysis removed - only LLM analysis is used
      /**
       * Detect reasoning content using LLM analysis
       */
      async detectReasoningByLLM(userInput, context) {
        if (!this.llmService) {
          throw new Error("LLM service not available for reasoning analysis");
        }
        try {
          const analysisPrompt = `Analyze the following user input to determine if it contains reasoning content that would benefit from reflection memory tools.

User Input: "${userInput}"

Context: ${context?.taskType ? `Task type: ${context.taskType}` : "General conversation"}

Please analyze this input and respond with a JSON object containing:
- "containsReasoning": boolean (true if the input contains reasoning patterns, thought processes, problem-solving steps, or analytical content)
- "confidence": number (0.0 to 1.0, how confident you are in this assessment)
- "detectedPatterns": array of strings (specific reasoning patterns you identified)
- "explanation": string (brief explanation of your reasoning)

Consider these factors:
1. Explicit reasoning markers (because, therefore, let me think, etc.)
2. Problem-solving language (debug, analyze, figure out, etc.)
3. Decision-making patterns (consider, evaluate, compare, etc.)
4. Technical reasoning (optimize, design, architecture, etc.)
5. Learning and reflection content (learn, understand, reflect, etc.)

Respond only with the JSON object, no additional text.`;
          const response = await this.llmService.directGenerate(analysisPrompt);
          try {
            let cleanedResponse = response.trim();
            logger.debug("ReasoningContentDetector: Raw LLM response", {
              rawResponse: response.substring(0, 300),
              responseLength: response.length
            });
            if (cleanedResponse.startsWith("```json")) {
              cleanedResponse = cleanedResponse.replace(/^```json\s*/, "");
            }
            if (cleanedResponse.startsWith("```")) {
              cleanedResponse = cleanedResponse.replace(/^```\s*/, "");
            }
            if (cleanedResponse.endsWith("```")) {
              cleanedResponse = cleanedResponse.replace(/\s*```$/, "");
            }
            const jsonMatch = cleanedResponse.match(/\{[\s\S]*\}/);
            if (jsonMatch) {
              cleanedResponse = jsonMatch[0];
            }
            logger.debug("ReasoningContentDetector: Cleaned response for JSON parsing", {
              cleanedResponse: cleanedResponse.substring(0, 300),
              cleanedLength: cleanedResponse.length
            });
            const result = JSON.parse(cleanedResponse);
            return {
              containsReasoning: result.containsReasoning || false,
              confidence: Math.max(0, Math.min(1, result.confidence || 0)),
              detectedPatterns: Array.isArray(result.detectedPatterns) ? result.detectedPatterns.slice(0, this.options.maxPatterns) : [],
              explanation: result.explanation || "LLM analysis"
            };
          } catch (parseError) {
            logger.warn("ReasoningContentDetector: Failed to parse LLM response", {
              response: response.substring(0, 200),
              error: parseError instanceof Error ? parseError.message : String(parseError)
            });
            return {
              containsReasoning: false,
              confidence: 0,
              detectedPatterns: [],
              explanation: "Failed to parse LLM response for reasoning detection"
            };
          }
        } catch (error) {
          logger.warn("ReasoningContentDetector: LLM analysis failed", {
            error: error instanceof Error ? error.message : String(error)
          });
          return {
            containsReasoning: false,
            confidence: 0,
            detectedPatterns: [],
            explanation: "LLM analysis failed for reasoning detection"
          };
        }
      }
      // Combined analysis removed - only LLM analysis is used
      /**
       * Update detection options
       */
      updateOptions(newOptions) {
        this.options = { ...this.options, ...newOptions };
        logger.debug("ReasoningContentDetector: Updated options", { newOptions });
      }
      /**
       * Get current detection options
       */
      getOptions() {
        return { ...this.options };
      }
    };
  }
});

// src/core/brain/reasoning/search-context-manager.ts
var DEFAULT_OPTIONS2, SearchContextManager;
var init_search_context_manager = __esm({
  "src/core/brain/reasoning/search-context-manager.ts"() {
    init_cjs_shims();
    init_logger2();
    DEFAULT_OPTIONS2 = {
      maxPrimaryResults: 5,
      maxSecondaryResults: 10,
      relevanceThreshold: 0.6,
      enableDeduplication: true,
      enableSummarization: true,
      sortByRelevance: true
    };
    SearchContextManager = class {
      options;
      recentSearches = /* @__PURE__ */ new Map();
      constructor(options) {
        this.options = { ...DEFAULT_OPTIONS2, ...options };
      }
      /**
       * Process multiple search tool results and return well-sorted context
       */
      async processSearchResults(searchResults, query2) {
        try {
          logger.debug("SearchContextManager: Processing search results", {
            graphResults: searchResults.graph?.length || 0,
            memoryResults: searchResults.memory?.length || 0,
            reasoningResults: searchResults.reasoning_patterns?.length || 0
          });
          const normalizedResults = this.normalizeSearchResults(searchResults);
          const deduplicatedResults = this.options.enableDeduplication ? this.deduplicateResults(normalizedResults) : normalizedResults;
          const sortedResults = this.options.sortByRelevance ? this.sortByRelevance(deduplicatedResults) : deduplicatedResults;
          const filteredResults = this.filterByRelevance(sortedResults);
          const { primaryResults, secondaryResults } = this.splitResults(filteredResults);
          const summary = this.options.enableSummarization ? await this.generateSummary(primaryResults, query2) : this.generateBasicSummary(primaryResults);
          if (query2) {
            this.cacheSearchResults(query2, [...primaryResults, ...secondaryResults]);
          }
          const context = {
            primaryResults,
            secondaryResults,
            summary,
            totalResults: primaryResults.length + secondaryResults.length,
            sourcesUsed: this.getSourcesUsed([...primaryResults, ...secondaryResults])
          };
          logger.debug("SearchContextManager: Context processing completed", {
            primaryCount: primaryResults.length,
            secondaryCount: secondaryResults.length,
            totalResults: context.totalResults,
            sourcesUsed: context.sourcesUsed
          });
          return context;
        } catch (error) {
          logger.error("SearchContextManager: Error processing search results", {
            error: error instanceof Error ? error.message : String(error)
          });
          return {
            primaryResults: [],
            secondaryResults: [],
            summary: "Error processing search results",
            totalResults: 0,
            sourcesUsed: []
          };
        }
      }
      /**
       * Normalize search results from different sources
       */
      normalizeSearchResults(searchResults) {
        const normalized = [];
        if (searchResults.graph && Array.isArray(searchResults.graph)) {
          for (const result of searchResults.graph) {
            if (result && typeof result === "object") {
              const content = this.extractContent(result, "graph");
              if (content && content !== "Invalid result object" && content !== "No content available") {
                const metadata = this.extractMetadata(result, "graph");
                const timestamp = this.extractTimestamp(result, "graph");
                normalized.push({
                  source: "graph",
                  content,
                  relevance: this.extractRelevance(result, "graph"),
                  ...metadata && { metadata },
                  ...timestamp && { timestamp }
                });
              }
            }
          }
        }
        if (searchResults.memory && Array.isArray(searchResults.memory)) {
          for (const result of searchResults.memory) {
            if (result && typeof result === "object") {
              const content = this.extractContent(result, "memory");
              if (content && content !== "Invalid result object" && content !== "No content available") {
                const metadata = this.extractMetadata(result, "memory");
                const timestamp = this.extractTimestamp(result, "memory");
                normalized.push({
                  source: "memory",
                  content,
                  relevance: this.extractRelevance(result, "memory"),
                  ...metadata && { metadata },
                  ...timestamp && { timestamp }
                });
              }
            }
          }
        }
        if (searchResults.reasoning_patterns && Array.isArray(searchResults.reasoning_patterns)) {
          for (const result of searchResults.reasoning_patterns) {
            if (result && typeof result === "object") {
              const content = this.extractContent(result, "reasoning_patterns");
              if (content && content !== "Invalid result object" && content !== "No content available") {
                const metadata = this.extractMetadata(result, "reasoning_patterns");
                const timestamp = this.extractTimestamp(result, "reasoning_patterns");
                normalized.push({
                  source: "reasoning_patterns",
                  content,
                  relevance: this.extractRelevance(result, "reasoning_patterns"),
                  ...metadata && { metadata },
                  ...timestamp && { timestamp }
                });
              }
            }
          }
        }
        return normalized;
      }
      /**
       * Extract content from search result based on source
       */
      extractContent(result, source) {
        if (!result || typeof result !== "object") {
          return "Invalid result object";
        }
        let content;
        switch (source) {
          case "graph":
            content = result.content || result.text || result.description || result.name;
            break;
          case "memory":
            content = result.content || result.text || result.fact || result.memory;
            break;
          case "reasoning_patterns":
            content = result.content || result.text || result.pattern || result.reasoning;
            break;
          default:
            content = result.content || result.text;
            break;
        }
        if (!content) {
          try {
            content = JSON.stringify(result);
          } catch {
            content = String(result);
          }
        }
        return content || "No content available";
      }
      /**
       * Extract relevance score from search result
       */
      extractRelevance(result, _source) {
        const relevance = result.relevance || result.score || result.similarity || result.confidence || 0.5;
        return Math.max(0, Math.min(1, relevance));
      }
      /**
       * Extract metadata from search result
       */
      extractMetadata(result, source) {
        const metadata = result.metadata || result.meta || {};
        switch (source) {
          case "graph":
            return {
              ...metadata,
              nodeType: result.type || result.nodeType,
              relationships: result.relationships || result.connections
            };
          case "memory":
            return {
              ...metadata,
              memoryType: result.memoryType || result.type,
              sessionId: result.sessionId,
              extractedAt: result.extractedAt
            };
          case "reasoning_patterns":
            return {
              ...metadata,
              patternType: result.patternType || result.type,
              qualityScore: result.qualityScore,
              stepCount: result.stepCount
            };
          default:
            return metadata;
        }
      }
      /**
       * Extract timestamp from search result
       */
      extractTimestamp(result, _source) {
        return result.timestamp || result.createdAt || result.updatedAt || result.date;
      }
      /**
       * Deduplicate search results based on content similarity
       */
      deduplicateResults(results) {
        const unique = [];
        const seen = /* @__PURE__ */ new Set();
        for (const result of results) {
          const normalizedContent = result.content.toLowerCase().trim().replace(/\s+/g, " ");
          const contentHash = `${result.source}:${normalizedContent.substring(0, 100)}`;
          if (!seen.has(contentHash)) {
            seen.add(contentHash);
            unique.push(result);
          } else {
            const existingIndex = unique.findIndex((r) => {
              const existingNormalized = r.content.toLowerCase().trim().replace(/\s+/g, " ");
              const existingHash = `${r.source}:${existingNormalized.substring(0, 100)}`;
              return existingHash === contentHash;
            });
            if (existingIndex >= 0 && unique[existingIndex] && result.relevance > unique[existingIndex].relevance) {
              unique[existingIndex] = result;
            }
          }
        }
        return unique;
      }
      /**
       * Sort results by relevance score
       */
      sortByRelevance(results) {
        return [...results].sort((a, b) => b.relevance - a.relevance);
      }
      /**
       * Filter results by relevance threshold
       */
      filterByRelevance(results) {
        return results.filter((result) => result.relevance >= this.options.relevanceThreshold);
      }
      /**
       * Split results into primary and secondary based on relevance and limits
       */
      splitResults(results) {
        const primaryResults = results.slice(0, this.options.maxPrimaryResults);
        const secondaryResults = results.slice(
          this.options.maxPrimaryResults,
          this.options.maxPrimaryResults + this.options.maxSecondaryResults
        );
        return { primaryResults, secondaryResults };
      }
      /**
       * Generate a summary of the primary search results
       */
      async generateSummary(primaryResults, _query) {
        if (primaryResults.length === 0) {
          return "No relevant search results found.";
        }
        const resultCounts = this.countResultsBySource(primaryResults);
        const topResults = primaryResults.slice(0, 3);
        let summary = `Found ${primaryResults.length} relevant results`;
        if ((resultCounts.graph || 0) > 0)
          summary += ` (${resultCounts.graph || 0} from knowledge graph`;
        if ((resultCounts.memory || 0) > 0)
          summary += `${(resultCounts.graph || 0) > 0 ? ", " : " ("}${resultCounts.memory || 0} from memory`;
        if ((resultCounts.reasoning_patterns || 0) > 0)
          summary += `${(resultCounts.graph || 0) > 0 || (resultCounts.memory || 0) > 0 ? ", " : " ("}${resultCounts.reasoning_patterns || 0} reasoning patterns`;
        summary += ")";
        if (topResults.length > 0) {
          summary += ". Top results include: ";
          summary += topResults.map(
            (result, index) => `${index + 1}) ${result.content.substring(0, 100)}${result.content.length > 100 ? "..." : ""}`
          ).join("; ");
        }
        return summary;
      }
      /**
       * Generate a basic summary without LLM processing
       */
      generateBasicSummary(primaryResults) {
        if (primaryResults.length === 0) {
          return "No relevant search results found.";
        }
        const resultCounts = this.countResultsBySource(primaryResults);
        let summary = `Found ${primaryResults.length} relevant results`;
        const sources = [];
        if ((resultCounts.graph || 0) > 0)
          sources.push(`${resultCounts.graph || 0} from knowledge graph`);
        if ((resultCounts.memory || 0) > 0) sources.push(`${resultCounts.memory || 0} from memory`);
        if ((resultCounts.reasoning_patterns || 0) > 0)
          sources.push(`${resultCounts.reasoning_patterns || 0} reasoning patterns`);
        if (sources.length > 0) {
          summary += ` (${sources.join(", ")})`;
        }
        return summary;
      }
      /**
       * Count results by source
       */
      countResultsBySource(results) {
        return results.reduce(
          (counts, result) => {
            counts[result.source] = (counts[result.source] || 0) + 1;
            return counts;
          },
          {}
        );
      }
      /**
       * Get list of sources used in results
       */
      getSourcesUsed(results) {
        const sources = /* @__PURE__ */ new Set();
        for (const result of results) {
          sources.add(result.source);
        }
        return Array.from(sources);
      }
      /**
       * Cache search results for future reference
       */
      cacheSearchResults(_query, results) {
        const now = Date.now();
        this.recentSearches.set(_query, { timestamp: now, results });
        const oneHourAgo = now - 36e5;
        for (const [key, value] of this.recentSearches.entries()) {
          if (value.timestamp < oneHourAgo) {
            this.recentSearches.delete(key);
          }
        }
      }
      /**
       * Get cached search results for a query
       */
      getCachedResults(query2) {
        const cached = this.recentSearches.get(query2);
        if (cached && Date.now() - cached.timestamp < 3e5) {
          return cached.results;
        }
        return null;
      }
      /**
       * Update search context options
       */
      updateOptions(newOptions) {
        this.options = { ...this.options, ...newOptions };
        logger.debug("SearchContextManager: Updated options", { newOptions });
      }
      /**
       * Get current search context options
       */
      getOptions() {
        return { ...this.options };
      }
      /**
       * Clear search cache
       */
      clearCache() {
        this.recentSearches.clear();
        logger.debug("SearchContextManager: Cache cleared");
      }
    };
  }
});

// src/core/storage/config.ts
var BaseBackendSchema, InMemoryBackendSchema, RedisBackendSchema, SqliteBackendSchema, PostgresBackendSchema, BackendConfigSchema, StorageSchema;
var init_config3 = __esm({
  "src/core/storage/config.ts"() {
    init_cjs_shims();
    init_zod();
    BaseBackendSchema = external_exports.object({
      /** Maximum number of concurrent connections to the backend */
      maxConnections: external_exports.number().int().positive().optional().describe("Maximum connections"),
      /** Time in milliseconds before an idle connection is closed */
      idleTimeoutMillis: external_exports.number().int().positive().optional().describe("Idle timeout in milliseconds"),
      /** Time in milliseconds to wait for a connection to be established */
      connectionTimeoutMillis: external_exports.number().int().positive().optional().describe("Connection timeout in milliseconds"),
      /** Backend-specific options that vary by implementation */
      options: external_exports.record(external_exports.any()).optional().describe("Backend-specific options")
    });
    InMemoryBackendSchema = BaseBackendSchema.extend({
      type: external_exports.literal("in-memory")
    }).strict();
    RedisBackendSchema = BaseBackendSchema.extend({
      type: external_exports.literal("redis"),
      /** Redis connection URL (redis://...) - overrides individual params if provided */
      url: external_exports.string().optional().describe("Redis connection URL (redis://...)"),
      /** Redis server hostname */
      host: external_exports.string().optional().describe("Redis host"),
      /** Redis server port (default: 6379) */
      port: external_exports.number().int().positive().optional().describe("Redis port"),
      /** Redis authentication username */
      username: external_exports.string().optional().describe("Redis username"),
      /** Redis authentication password */
      password: external_exports.string().optional().describe("Redis password"),
      /** Redis database number (0-15, default: 0) */
      database: external_exports.number().int().nonnegative().optional().describe("Redis database number")
    }).strict();
    SqliteBackendSchema = BaseBackendSchema.extend({
      type: external_exports.literal("sqlite"),
      /**
       * SQLite database file path.
       * If not provided, will auto-detect using the path resolver.
       */
      path: external_exports.string().optional().describe(
        "SQLite database file path (optional, will auto-detect using path resolver if not provided)"
      ),
      /** Database filename (default: cipher.db) */
      database: external_exports.string().optional().describe("Database filename (default: cipher.db)")
    }).strict();
    PostgresBackendSchema = BaseBackendSchema.extend({
      type: external_exports.literal("postgres"),
      /** PostgreSQL connection URL (postgresql://...) - overrides individual params if provided */
      url: external_exports.string().optional().describe("PostgreSQL connection URL (postgresql://...)"),
      /** PostgreSQL server hostname */
      host: external_exports.string().optional().describe("PostgreSQL host"),
      /** PostgreSQL server port (default: 5432) */
      port: external_exports.number().int().positive().optional().describe("PostgreSQL port"),
      /** Database name */
      database: external_exports.string().optional().describe("Database name"),
      /** Username for authentication */
      user: external_exports.string().optional().describe("Username"),
      /** Password for authentication */
      password: external_exports.string().optional().describe("Password"),
      /** Enable SSL connection (default: false) */
      ssl: external_exports.boolean().optional().describe("Enable SSL connection"),
      /** Connection pool settings */
      pool: external_exports.object({
        /** Minimum number of connections in pool */
        min: external_exports.number().int().nonnegative().optional().describe("Minimum pool size"),
        /** Maximum number of connections in pool */
        max: external_exports.number().int().positive().optional().describe("Maximum pool size"),
        /** Connection idle timeout in ms */
        idleTimeoutMillis: external_exports.number().int().positive().optional().describe("Connection idle timeout"),
        /** Connection acquire timeout in ms */
        acquireTimeoutMillis: external_exports.number().int().positive().optional().describe("Connection acquire timeout")
      }).optional().describe("Connection pool settings")
    }).strict();
    BackendConfigSchema = external_exports.discriminatedUnion(
      "type",
      [InMemoryBackendSchema, RedisBackendSchema, SqliteBackendSchema, PostgresBackendSchema],
      {
        errorMap: (issue, ctx) => {
          if (issue.code === external_exports.ZodIssueCode.invalid_union_discriminator) {
            return {
              message: `Invalid backend type. Expected 'in-memory', 'redis', 'sqlite', or 'postgres'.`
            };
          }
          return { message: ctx.defaultError };
        }
      }
    ).describe("Backend configuration for storage system").superRefine((data, ctx) => {
      if (data.type === "redis") {
        if (!data.url && !data.host) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Redis backend requires either 'url' or 'host' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "postgres") {
        if (!data.url && (!data.host || !data.database)) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "PostgreSQL backend requires either 'url' or both 'host' and 'database' to be specified",
            path: ["url"]
          });
        }
      }
    });
    StorageSchema = external_exports.object({
      /** Cache backend for fast, ephemeral storage (Redis, In-Memory) */
      cache: BackendConfigSchema.describe("Cache backend configuration (fast, ephemeral)"),
      /** Database backend for persistent, reliable storage (SQLite, PostgreSQL) */
      database: BackendConfigSchema.describe("Database backend configuration (persistent, reliable)")
    }).strict().describe("Storage configuration with cache and database backends");
  }
});

// src/core/storage/constants.ts
var LOG_PREFIXES2, ERROR_MESSAGES2, TIMEOUTS, HEALTH_CHECK, BACKEND_TYPES;
var init_constants2 = __esm({
  "src/core/storage/constants.ts"() {
    init_cjs_shims();
    LOG_PREFIXES2 = {
      MANAGER: "[StorageManager]",
      CACHE: "[StorageManager:Cache]",
      DATABASE: "[StorageManager:Database]",
      HEALTH: "[StorageManager:Health]",
      FACTORY: "[StorageFactory]",
      BACKEND: "[StorageBackend]"
    };
    ERROR_MESSAGES2 = {
      // Connection errors
      CACHE_CONNECTION_FAILED: "Failed to connect to cache backend",
      DATABASE_CONNECTION_FAILED: "Failed to connect to database backend",
      ALREADY_CONNECTED: "Storage manager is already connected",
      NOT_CONNECTED: "Storage manager is not connected",
      // Backend errors
      BACKEND_NOT_FOUND: "Storage backend not found",
      INVALID_BACKEND_TYPE: "Invalid backend type specified",
      MODULE_LOAD_FAILED: "Failed to load backend module",
      // Operation errors
      HEALTH_CHECK_FAILED: "Health check failed",
      OPERATION_TIMEOUT: "Storage operation timed out",
      SERIALIZATION_ERROR: "Failed to serialize/deserialize data",
      // Configuration errors
      INVALID_CONFIG: "Invalid storage configuration",
      MISSING_REQUIRED_CONFIG: "Missing required configuration"
    };
    TIMEOUTS = {
      CONNECTION: 1e4,
      // 10 seconds
      HEALTH_CHECK: 5e3,
      // 5 seconds
      OPERATION: 3e4,
      // 30 seconds
      SHUTDOWN: 5e3
      // 5 seconds
    };
    HEALTH_CHECK = {
      KEY: "storage_manager_health_check",
      VALUE: "ok",
      TTL_SECONDS: 10
    };
    BACKEND_TYPES = {
      // Cache backends
      REDIS: "redis",
      MEMCACHED: "memcached",
      IN_MEMORY: "in-memory",
      // Database backends
      SQLITE: "sqlite",
      POSTGRES: "postgres",
      MYSQL: "mysql"
    };
  }
});

// src/core/storage/backend/types.ts
var StorageError, StorageConnectionError;
var init_types6 = __esm({
  "src/core/storage/backend/types.ts"() {
    init_cjs_shims();
    StorageError = class extends Error {
      constructor(message, operation, cause) {
        super(message);
        this.operation = operation;
        this.cause = cause;
        this.name = "StorageError";
        this.cause = cause;
      }
    };
    StorageConnectionError = class extends StorageError {
      constructor(message, backendType, cause) {
        super(message, "connection", cause);
        this.message = message;
        this.backendType = backendType;
        this.cause = cause;
        this.name = "StorageConnectionError";
      }
    };
  }
});

// src/core/storage/backend/in-memory.ts
var in_memory_exports = {};
__export(in_memory_exports, {
  InMemoryBackend: () => InMemoryBackend
});
var InMemoryBackend;
var init_in_memory = __esm({
  "src/core/storage/backend/in-memory.ts"() {
    init_cjs_shims();
    init_types6();
    init_constants2();
    init_logger2();
    InMemoryBackend = class {
      // Storage maps
      store = /* @__PURE__ */ new Map();
      lists = /* @__PURE__ */ new Map();
      // Connection state
      connected = false;
      // Cleanup interval
      cleanupInterval;
      cleanupIntervalMs = 6e4;
      // 1 minute
      // Logger
      logger;
      // Statistics
      stats = {
        hits: 0,
        misses: 0,
        sets: 0,
        deletes: 0,
        expirations: 0
      };
      constructor() {
        this.logger = createLogger({ level: process.env.LOG_LEVEL || "info" });
      }
      // Connection Management
      /**
       * Connect to the in-memory backend
       *
       * For in-memory backend, this just sets the connected flag and
       * starts the cleanup interval for expired entries.
       */
      async connect() {
        if (this.connected) {
          this.logger.debug("InMemoryBackend already connected");
          return;
        }
        this.connected = true;
        this.startCleanupInterval();
        this.logger.info("InMemoryBackend connected", {
          cleanupInterval: `${this.cleanupIntervalMs}ms`
        });
      }
      /**
       * Disconnect from the in-memory backend
       *
       * Clears all data and stops the cleanup interval.
       */
      async disconnect() {
        if (!this.connected) {
          return;
        }
        this.stopCleanupInterval();
        this.clear();
        this.connected = false;
        this.logger.info("InMemoryBackend disconnected", {
          stats: this.stats
        });
      }
      /**
       * Check if backend is connected
       */
      isConnected() {
        return this.connected;
      }
      /**
       * Get backend type identifier
       */
      getBackendType() {
        return BACKEND_TYPES.IN_MEMORY;
      }
      // Basic Operations (shared by both interfaces)
      /**
       * Get a value by key
       *
       * Checks expiration and removes expired entries.
       */
      async get(key) {
        this.checkConnection();
        const entry = this.store.get(key);
        if (!entry) {
          this.stats.misses++;
          return void 0;
        }
        if (entry.expires && Date.now() > entry.expires) {
          this.store.delete(key);
          this.stats.expirations++;
          this.stats.misses++;
          this.logger.debug("Entry expired", { key });
          return void 0;
        }
        this.stats.hits++;
        return this.cloneValue(entry.value);
      }
      /**
       * Set a value with optional TTL (for CacheBackend)
       */
      async set(key, value, ttlSeconds) {
        this.checkConnection();
        const entry = {
          value: this.cloneValue(value)
        };
        if (ttlSeconds && ttlSeconds > 0) {
          entry.expires = Date.now() + ttlSeconds * 1e3;
        }
        this.store.set(key, entry);
        this.stats.sets++;
        this.logger.debug("Value set", {
          key,
          ttl: ttlSeconds,
          expires: entry.expires
        });
      }
      /**
       * Delete a value by key
       */
      async delete(key) {
        this.checkConnection();
        const deleted = this.store.delete(key);
        this.lists.delete(key);
        if (deleted) {
          this.stats.deletes++;
        }
        this.logger.debug("Value deleted", { key, deleted });
      }
      // DatabaseBackend specific methods
      /**
       * List all keys matching a prefix
       */
      async list(prefix) {
        this.checkConnection();
        const keys = [];
        const now = Date.now();
        for (const [key, entry] of Array.from(this.store.entries())) {
          if (entry.expires && now > entry.expires) {
            continue;
          }
          if (key.startsWith(prefix)) {
            keys.push(key);
          }
        }
        for (const key of Array.from(this.lists.keys())) {
          if (key.startsWith(prefix) && !keys.includes(key)) {
            keys.push(key);
          }
        }
        this.logger.debug("Listed keys", { prefix, count: keys.length });
        return keys;
      }
      /**
       * Append an item to a list
       */
      async append(key, item) {
        this.checkConnection();
        let list = this.lists.get(key);
        if (!list) {
          list = [];
          this.lists.set(key, list);
        }
        list.push(this.cloneValue(item));
        this.logger.debug("Item appended to list", {
          key,
          listSize: list.length
        });
      }
      /**
       * Get a range of items from a list
       */
      async getRange(key, start, count) {
        this.checkConnection();
        const list = this.lists.get(key);
        if (!list) {
          return [];
        }
        if (start < 0) {
          start = Math.max(0, list.length + start);
        }
        const end = Math.min(start + count, list.length);
        const items = list.slice(start, end);
        this.logger.debug("Retrieved range from list", {
          key,
          start,
          count,
          returned: items.length,
          totalSize: list.length
        });
        return items.map((item) => this.cloneValue(item));
      }
      // Helper Methods
      /**
       * Check if backend is connected
       * @throws {StorageError} If not connected
       */
      checkConnection() {
        if (!this.connected) {
          throw new StorageError(
            ERROR_MESSAGES2.NOT_CONNECTED,
            "operation",
            new Error("InMemoryBackend is not connected")
          );
        }
      }
      /**
       * Clone a value to prevent reference issues
       *
       * Uses JSON serialization for deep cloning.
       * This also ensures consistency with network-based backends
       * that serialize data.
       */
      cloneValue(value) {
        try {
          return JSON.parse(JSON.stringify(value));
        } catch (error) {
          throw new StorageError(ERROR_MESSAGES2.SERIALIZATION_ERROR, "clone", error);
        }
      }
      /**
       * Clear all stored data
       */
      clear() {
        this.store.clear();
        this.lists.clear();
        this.logger.debug("All data cleared");
      }
      /**
       * Start the cleanup interval for expired entries
       */
      startCleanupInterval() {
        if (this.cleanupInterval) {
          return;
        }
        this.cleanupInterval = setInterval(() => {
          this.cleanupExpired();
        }, this.cleanupIntervalMs);
        if (this.cleanupInterval.unref) {
          this.cleanupInterval.unref();
        }
      }
      /**
       * Stop the cleanup interval
       */
      stopCleanupInterval() {
        if (this.cleanupInterval) {
          clearInterval(this.cleanupInterval);
          this.cleanupInterval = void 0;
        }
      }
      /**
       * Remove expired entries from the store
       */
      cleanupExpired() {
        const now = Date.now();
        let cleaned = 0;
        for (const [key, entry] of Array.from(this.store.entries())) {
          if (entry.expires && now > entry.expires) {
            this.store.delete(key);
            cleaned++;
            this.stats.expirations++;
          }
        }
        if (cleaned > 0) {
          this.logger.debug("Cleaned up expired entries", {
            cleaned,
            remaining: this.store.size
          });
        }
      }
      // Additional utility methods
      /**
       * Get storage statistics
       */
      getStats() {
        return { ...this.stats };
      }
      /**
       * Get current storage size
       */
      getSize() {
        return {
          keys: this.store.size,
          lists: this.lists.size,
          total: this.store.size + this.lists.size
        };
      }
      /**
       * Manually trigger cleanup of expired entries
       */
      async cleanup() {
        const before = this.store.size;
        this.cleanupExpired();
        const after = this.store.size;
        return before - after;
      }
    };
  }
});

// src/core/storage/backend/redis-backend.ts
var redis_backend_exports = {};
__export(redis_backend_exports, {
  RedisBackend: () => RedisBackend
});
var import_ioredis, RedisBackend;
var init_redis_backend = __esm({
  "src/core/storage/backend/redis-backend.ts"() {
    init_cjs_shims();
    import_ioredis = require("ioredis");
    RedisBackend = class {
      /**
       * Creates a new Redis backend instance
       *
       * @param config - Redis configuration options
       */
      constructor(config2) {
        this.config = config2;
      }
      /** The Redis client instance */
      redis = null;
      /** Connection status flag */
      connected = false;
      /**
       * Establishes connection to Redis server
       *
       * Sets up connection with retry logic, error handling, and event listeners.
       * Configuration supports both individual options and connection URL.
       *
       * @throws {StorageConnectionError} If connection fails after retries
       */
      async connect() {
        if (this.connected) return;
        this.redis = new import_ioredis.Redis({
          // Host and port configuration
          ...this.config.host && { host: this.config.host },
          ...this.config.port && { port: this.config.port },
          ...this.config.username && { username: this.config.username },
          ...this.config.password && { password: this.config.password },
          // Database selection (default: 0)
          db: this.config.database || 0,
          // Force IPv4 to avoid IPv6 issues
          family: 4,
          // Timeout configuration
          ...this.config.connectionTimeoutMillis && {
            connectTimeout: this.config.connectionTimeoutMillis
          },
          ...this.config.connectionTimeoutMillis && {
            commandTimeout: this.config.connectionTimeoutMillis
          },
          // Retry configuration
          maxRetriesPerRequest: 3,
          // Lazy connect for explicit connection control
          lazyConnect: true,
          // Merge any additional options from config
          ...this.config.options
        });
        let connectionError = null;
        this.redis.on("error", (err) => {
          connectionError = err;
        });
        await new Promise((resolve3, reject) => {
          const onReady = () => {
            this.connected = true;
            cleanup();
            resolve3();
          };
          const onError = (err) => {
            cleanup();
            reject(new Error("Redis connection failed: " + err.message));
          };
          const cleanup = () => {
            this.redis?.off("ready", onReady);
            this.redis?.off("error", onError);
          };
          this.redis?.once("ready", onReady);
          this.redis?.once("error", onError);
          const timeoutMs = this.config.connectionTimeoutMillis || 1e4;
          setTimeout(() => {
            cleanup();
            if (connectionError) {
              reject(new Error("Redis connection failed: " + connectionError.message));
            } else {
              reject(new Error("Redis connection failed: timeout"));
            }
          }, timeoutMs);
          this.redis?.connect().catch(onError);
        });
      }
      /**
       * Gracefully disconnects from Redis
       *
       * Ensures all pending commands are completed before closing the connection.
       */
      async disconnect() {
        if (this.redis) {
          await this.redis.quit();
          this.redis = null;
        }
        this.connected = false;
      }
      /**
       * Checks if the Redis connection is active and ready
       *
       * @returns true if connected and Redis status is 'ready'
       */
      isConnected() {
        return this.connected && this.redis?.status === "ready";
      }
      /**
       * Returns the backend type identifier
       *
       * @returns 'redis'
       */
      getBackendType() {
        return "redis";
      }
      // Core operations
      /**
       * Retrieves and deserializes a value from Redis
       *
       * @template T - The expected type of the cached value
       * @param key - The cache key
       * @returns The cached value or undefined if not found
       * @throws {StorageError} If deserialization fails
       */
      async get(key) {
        this.checkConnection();
        const value = await this.redis.get(key);
        return value ? JSON.parse(value) : void 0;
      }
      /**
       * Serializes and stores a value in Redis with optional TTL
       *
       * @template T - The type of the value to cache
       * @param key - The cache key
       * @param value - The value to cache
       * @param ttlSeconds - Optional TTL in seconds
       */
      async set(key, value, ttlSeconds) {
        this.checkConnection();
        const serialized = JSON.stringify(value);
        if (ttlSeconds) {
          await this.redis.setex(key, ttlSeconds, serialized);
        } else {
          await this.redis.set(key, serialized);
        }
      }
      /**
       * Deletes a key from Redis
       *
       * @param key - The cache key to delete
       */
      async delete(key) {
        this.checkConnection();
        await this.redis.del(key);
      }
      /**
       * Batch retrieval of multiple keys
       *
       * More efficient than multiple individual get() calls.
       *
       * @template T - The expected type of the cached values
       * @param keys - Array of cache keys
       * @returns Array of values (undefined for missing keys)
       */
      async mget(keys) {
        this.checkConnection();
        if (keys.length === 0) return [];
        const values = await this.redis.mget(...keys);
        return values.map((value) => value ? JSON.parse(value) : void 0);
      }
      /**
       * Batch storage of multiple key-value pairs
       *
       * Uses pipeline for atomic batch operation.
       * Note: Does not support TTL for batch operations.
       *
       * @template T - The type of values to cache
       * @param entries - Array of [key, value] tuples
       */
      async mset(entries) {
        this.checkConnection();
        if (entries.length === 0) return;
        const pipeline = this.redis.pipeline();
        for (const [key, value] of entries) {
          pipeline.set(key, JSON.stringify(value));
        }
        await pipeline.exec();
      }
      /**
       * Checks if a key exists in Redis
       *
       * @param key - The cache key to check
       * @returns true if key exists, false otherwise
       */
      async exists(key) {
        this.checkConnection();
        const result = await this.redis.exists(key);
        return result === 1;
      }
      /**
       * Sets expiration time for an existing key
       *
       * @param key - The cache key
       * @param ttlSeconds - TTL in seconds
       */
      async expire(key, ttlSeconds) {
        this.checkConnection();
        await this.redis.expire(key, ttlSeconds);
      }
      // Cache-specific operations
      /**
       * Atomically increments a numeric value
       *
       * Creates the key with value 0 if it doesn't exist.
       *
       * @param key - The cache key
       * @param by - Increment amount (default: 1)
       * @returns The new value after increment
       */
      async increment(key, by = 1) {
        this.checkConnection();
        return await this.redis.incrby(key, by);
      }
      /**
       * Atomically decrements a numeric value
       *
       * @param key - The cache key
       * @param by - Decrement amount (default: 1)
       * @returns The new value after decrement
       */
      async decrement(key, by = 1) {
        this.checkConnection();
        return await this.redis.decrby(key, by);
      }
      // List operations (for compatibility with DatabaseBackend patterns)
      /**
       * Appends an item to a Redis list
       *
       * Creates the list if it doesn't exist.
       * Items are added to the end of the list (RPUSH).
       *
       * @template T - The type of list items
       * @param key - The list key
       * @param item - The item to append
       */
      async append(key, item) {
        this.checkConnection();
        await this.redis.rpush(key, JSON.stringify(item));
      }
      /**
       * Retrieves a range of items from a Redis list
       *
       * @template T - The type of list items
       * @param key - The list key
       * @param start - Starting index (0-based)
       * @param count - Number of items to retrieve
       * @returns Array of items in the specified range
       */
      async getRange(key, start, count) {
        this.checkConnection();
        const items = await this.redis.lrange(key, start, start + count - 1);
        return items.map((item) => JSON.parse(item));
      }
      /**
       * Lists all keys matching a pattern
       *
       * Warning: KEYS command can be slow on large databases.
       * Consider using SCAN for production use.
       *
       * @param prefix - Key prefix to search for
       * @returns Array of matching keys
       */
      async list(prefix) {
        this.checkConnection();
        return await this.redis.keys(`${prefix}*`);
      }
      /**
       * Validates connection status before operations
       *
       * @throws {Error} If not connected or Redis is not ready
       * @private
       */
      checkConnection() {
        if (!this.connected || !this.redis || this.redis.status !== "ready") {
          throw new Error("RedisBackend not connected");
        }
      }
      /**
       * Flushes the current Redis database
       *
       * Warning: This will delete ALL data in the current database.
       * Use with caution, typically only in development/testing.
       */
      async flushdb() {
        this.checkConnection();
        await this.redis.flushdb();
      }
      /**
       * Retrieves Redis server information
       *
       * Useful for monitoring and debugging.
       *
       * @returns Raw Redis INFO output
       */
      async info() {
        this.checkConnection();
        return await this.redis.info();
      }
    };
  }
});

// src/core/storage/backend/sqlite.ts
var sqlite_exports = {};
__export(sqlite_exports, {
  SqliteBackend: () => SqliteBackend
});
var import_better_sqlite3, import_promises, import_path4, import_fs4, SqliteBackend;
var init_sqlite = __esm({
  "src/core/storage/backend/sqlite.ts"() {
    init_cjs_shims();
    import_better_sqlite3 = __toESM(require("better-sqlite3"), 1);
    import_promises = require("fs/promises");
    import_path4 = require("path");
    import_fs4 = require("fs");
    init_types6();
    init_constants2();
    init_logger2();
    SqliteBackend = class {
      constructor(config2) {
        this.config = config2;
        this.logger = createLogger({ level: process.env.LOG_LEVEL || "info" });
        const dbPath = config2.path || "./data";
        const dbName = config2.database || "cipher.db";
        this.dbPath = (0, import_path4.join)(dbPath, dbName);
        this.logger.debug("SQLite backend initialized", {
          path: this.dbPath,
          config: { ...config2, path: dbPath, database: dbName }
        });
      }
      logger;
      connected = false;
      db;
      dbPath;
      // Prepared statements for performance
      statements = {};
      async connect() {
        if (this.connected) {
          return;
        }
        try {
          const dir = (0, import_path4.dirname)(this.dbPath);
          this.logger.debug("Ensuring database directory exists", { dir, dbPath: this.dbPath });
          if (!(0, import_fs4.existsSync)(dir)) {
            await (0, import_promises.mkdir)(dir, { recursive: true });
            this.logger.debug("Created database directory", { dir });
          }
          this.logger.debug("Opening SQLite database", { path: this.dbPath });
          this.db = new import_better_sqlite3.default(this.dbPath);
          if (!this.db) {
            throw new Error("Failed to create SQLite database connection - database instance is null");
          }
          this.logger.debug("Testing database connection");
          this.db.pragma("journal_mode = WAL");
          this.db.pragma("synchronous = NORMAL");
          this.db.pragma("foreign_keys = ON");
          this.logger.debug("Creating database tables");
          this.createTables();
          this.logger.debug("Preparing SQL statements");
          this.prepareStatements();
          this.connected = true;
          this.logger.info("SQLite backend connected successfully", {
            path: this.dbPath,
            dbSize: this.getDbInfo()
          });
        } catch (error) {
          const errorDetails = {
            path: this.dbPath,
            directory: (0, import_path4.dirname)(this.dbPath),
            directoryExists: (0, import_fs4.existsSync)((0, import_path4.dirname)(this.dbPath)),
            error: error instanceof Error ? error.message : String(error),
            stack: error instanceof Error ? error.stack : void 0,
            nodeVersion: process.version,
            platform: process.platform,
            arch: process.arch
          };
          console.log("Failed to connect to SQLite database", errorDetails);
          throw new StorageConnectionError(
            `Failed to connect to SQLite database: ${error instanceof Error ? error.message : String(error)}`,
            BACKEND_TYPES.SQLITE,
            error
          );
        }
      }
      async disconnect() {
        if (!this.connected || !this.db) {
          return;
        }
        try {
          Object.values(this.statements).forEach((_stmt) => {
            try {
            } catch (error) {
              this.logger.warn("Error finalizing statement", { error });
            }
          });
          this.statements = {};
          this.db.close();
          this.db = void 0;
          this.connected = false;
          this.logger.info("SQLite backend disconnected");
        } catch (error) {
          this.logger.error("Error disconnecting from SQLite database", {
            error: error instanceof Error ? error.message : String(error)
          });
          throw error;
        }
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return BACKEND_TYPES.SQLITE;
      }
      // DatabaseBackend implementation
      async get(key) {
        this.checkConnection();
        try {
          const row = this.statements.get.get(key);
          if (!row) {
            return void 0;
          }
          return JSON.parse(row.value);
        } catch (error) {
          this.logger.error("Error getting value from SQLite", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to get value from SQLite", "get", error);
        }
      }
      async set(key, value) {
        this.checkConnection();
        try {
          const serialized = JSON.stringify(value);
          const now = Date.now();
          this.statements.set.run(key, serialized, now, now);
        } catch (error) {
          this.logger.error("Error setting value in SQLite", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to set value in SQLite", "set", error);
        }
      }
      async delete(key) {
        this.checkConnection();
        try {
          const transaction = this.db.transaction(() => {
            this.statements.delete.run(key);
            this.db.prepare("DELETE FROM lists WHERE key = ?").run(key);
          });
          transaction();
        } catch (error) {
          this.logger.error("Error deleting value from SQLite", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to delete value from SQLite", "delete", error);
        }
      }
      async list(prefix) {
        this.checkConnection();
        try {
          const rows = this.statements.list.all(prefix + "%");
          return rows.map((row) => row.key);
        } catch (error) {
          this.logger.error("Error listing keys from SQLite", {
            prefix,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to list keys from SQLite", "list", error);
        }
      }
      async append(key, item) {
        this.checkConnection();
        try {
          const serialized = JSON.stringify(item);
          const now = Date.now();
          const maxPosResult = this.db.prepare(
            "SELECT COALESCE(MAX(position), -1) + 1 as next_pos FROM lists WHERE key = ?"
          ).get(key);
          const nextPosition = maxPosResult.next_pos;
          this.db.prepare(
            "INSERT INTO lists (key, value, position, created_at) VALUES (?, ?, ?, ?)"
          ).run(key, serialized, nextPosition, now);
        } catch (error) {
          this.logger.error("Error appending to list in SQLite", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to append to list in SQLite", "append", error);
        }
      }
      async getRange(key, start, count) {
        this.checkConnection();
        try {
          const rows = this.statements.getRange.all(key, count, start);
          return rows.map((row) => JSON.parse(row.value));
        } catch (error) {
          this.logger.error("Error getting range from SQLite", {
            key,
            start,
            count,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to get range from SQLite", "getRange", error);
        }
      }
      // Private helper methods
      checkConnection() {
        if (!this.connected || !this.db) {
          throw new StorageError(ERROR_MESSAGES2.NOT_CONNECTED, "operation");
        }
      }
      createTables() {
        if (!this.db) {
          throw new StorageError("Database not initialized", "createTables");
        }
        this.db.exec(`
			CREATE TABLE IF NOT EXISTS store (
				key TEXT PRIMARY KEY,
				value TEXT NOT NULL,
				created_at INTEGER NOT NULL,
				updated_at INTEGER NOT NULL
			)
		`);
        this.db.exec(`
			CREATE TABLE IF NOT EXISTS lists (
				key TEXT NOT NULL,
				value TEXT NOT NULL,
				position INTEGER NOT NULL,
				created_at INTEGER NOT NULL,
				PRIMARY KEY (key, position)
			)
		`);
        this.db.exec(`
			CREATE TABLE IF NOT EXISTS list_metadata (
				key TEXT PRIMARY KEY,
				count INTEGER NOT NULL DEFAULT 0,
				created_at INTEGER NOT NULL,
				updated_at INTEGER NOT NULL
			)
		`);
        this.db.exec("CREATE INDEX IF NOT EXISTS idx_store_updated_at ON store(updated_at)");
        this.db.exec("CREATE INDEX IF NOT EXISTS idx_lists_key ON lists(key)");
        this.db.exec("CREATE INDEX IF NOT EXISTS idx_lists_created_at ON lists(created_at)");
        this.logger.debug("SQLite tables and indexes created");
      }
      prepareStatements() {
        if (!this.db) {
          throw new StorageError("Database not initialized", "prepareStatements");
        }
        this.statements = {
          get: this.db.prepare("SELECT value FROM store WHERE key = ?"),
          set: this.db.prepare(`
				INSERT OR REPLACE INTO store (key, value, created_at, updated_at)
				VALUES (?, ?, ?, ?)
			`),
          delete: this.db.prepare("DELETE FROM store WHERE key = ?"),
          list: this.db.prepare("SELECT key FROM store WHERE key LIKE ? ORDER BY key"),
          // Range query for lists
          getRange: this.db.prepare(`
				SELECT value FROM lists 
				WHERE key = ? 
				ORDER BY position 
				LIMIT ? OFFSET ?
			`),
          listCount: this.db.prepare("SELECT COUNT(*) as count FROM lists WHERE key = ?")
        };
        this.logger.debug("SQLite prepared statements created");
      }
      /**
       * Get database file size information
       */
      getDbInfo() {
        const info = { path: this.dbPath };
        if (this.connected && this.db) {
          try {
            const pragmaResult = this.db.pragma("page_count");
            const pageCount = Array.isArray(pragmaResult) ? pragmaResult[0] : pragmaResult;
            const numPageCount = typeof pageCount === "number" ? pageCount : Number(pageCount);
            if (!isNaN(numPageCount)) {
              info.pageCount = numPageCount;
            }
            const pageSizeResult = this.db.pragma("page_size");
            const pageSize = Array.isArray(pageSizeResult) ? pageSizeResult[0] : pageSizeResult;
            const numPageSize = typeof pageSize === "number" ? pageSize : Number(pageSize);
            if (!isNaN(numPageSize)) {
              info.pageSize = numPageSize;
            }
            if (info.pageCount && info.pageSize) {
              info.size = info.pageCount * info.pageSize;
            }
          } catch (error) {
            this.logger.warn("Error getting database info", { error });
          }
        }
        return info;
      }
      /**
       * Run database maintenance operations
       */
      async maintenance() {
        this.checkConnection();
        try {
          this.db.exec("ANALYZE");
          const freeListResult = this.db.pragma("freelist_count");
          const freePages = Array.isArray(freeListResult) ? freeListResult[0] : freeListResult;
          const freePageCount = typeof freePages === "number" ? freePages : Number(freePages);
          if (freePageCount > 100) {
            this.db.exec("VACUUM");
            this.logger.info("Database vacuumed", { freePages: freePageCount });
          }
          this.logger.debug("Database maintenance completed");
        } catch (error) {
          this.logger.error("Error during database maintenance", {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Database maintenance failed", "maintenance", error);
        }
      }
    };
  }
});

// src/core/storage/backend/postgresql.ts
var postgresql_exports = {};
__export(postgresql_exports, {
  PostgresBackend: () => PostgresBackend
});
var import_pg, PostgresBackend;
var init_postgresql = __esm({
  "src/core/storage/backend/postgresql.ts"() {
    init_cjs_shims();
    import_pg = require("pg");
    init_types6();
    init_logger2();
    PostgresBackend = class {
      pool;
      connected = false;
      config;
      logger;
      // Prepared statement cache
      statements = /* @__PURE__ */ new Map();
      constructor(config2) {
        this.config = config2;
        this.logger = createLogger();
        this.initializeStatements();
      }
      /**
       * Initialize prepared statement definitions
       */
      initializeStatements() {
        this.statements.set("get", "SELECT value FROM cipher_store WHERE key = $1");
        this.statements.set(
          "set",
          `
			INSERT INTO cipher_store (key, value, created_at, updated_at)
			VALUES ($1, $2, $3, $4)
			ON CONFLICT (key) DO UPDATE SET
				value = EXCLUDED.value,
				updated_at = EXCLUDED.updated_at
		`
        );
        this.statements.set("delete", "DELETE FROM cipher_store WHERE key = $1");
        this.statements.set("list", "SELECT key FROM cipher_store WHERE key LIKE $1 ORDER BY key");
        this.statements.set(
          "listAppend",
          `
			INSERT INTO cipher_lists (key, value, position, created_at)
			VALUES ($1, $2, COALESCE((SELECT MAX(position) + 1 FROM cipher_lists WHERE key = $1), 0), $3)
		`
        );
        this.statements.set(
          "getRange",
          `
			SELECT value FROM cipher_lists
			WHERE key = $1
			ORDER BY position
			LIMIT $2 OFFSET $3
		`
        );
        this.statements.set("deleteList", "DELETE FROM cipher_lists WHERE key = $1");
        this.statements.set(
          "updateListMetadata",
          `
			INSERT INTO cipher_list_metadata (key, count, created_at, updated_at)
			VALUES ($1, (SELECT COUNT(*) FROM cipher_lists WHERE key = $1), $2, $3)
			ON CONFLICT (key) DO UPDATE SET
				count = (SELECT COUNT(*) FROM cipher_lists WHERE key = $1),
				updated_at = EXCLUDED.updated_at
		`
        );
      }
      /**
       * Connect to PostgreSQL database
       */
      async connect() {
        if (this.connected) {
          this.logger.debug("PostgreSQL backend already connected");
          return;
        }
        try {
          this.logger.info("Connecting to PostgreSQL database");
          const poolConfig = this.buildPoolConfig();
          this.pool = new import_pg.Pool(poolConfig);
          const client = await this.pool.connect();
          try {
            await client.query("SELECT 1");
            this.logger.debug("PostgreSQL connection test successful");
          } finally {
            client.release();
          }
          await this.createTables();
          this.connected = true;
          this.logger.info("PostgreSQL backend connected successfully", {
            host: this.config.host || "localhost",
            database: this.config.database,
            pool: {
              max: poolConfig.max,
              min: poolConfig.min
            }
          });
        } catch (error) {
          this.logger.error("Failed to connect to PostgreSQL database", {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageConnectionError(
            "Failed to connect to PostgreSQL database",
            "postgres",
            error
          );
        }
      }
      /**
       * Disconnect from PostgreSQL database
       */
      async disconnect() {
        if (!this.connected || !this.pool) {
          return;
        }
        try {
          await this.pool.end();
          this.connected = false;
          this.pool = void 0;
          this.logger.info("PostgreSQL backend disconnected");
        } catch (error) {
          this.logger.error("Error disconnecting from PostgreSQL", {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to disconnect from PostgreSQL", "disconnect", error);
        }
      }
      /**
       * Check if backend is connected
       */
      isConnected() {
        return this.connected && this.pool !== void 0;
      }
      /**
       * Get backend type identifier
       */
      getBackendType() {
        return "postgres";
      }
      /**
       * Get value by key
       */
      async get(key) {
        this.checkConnection();
        try {
          const result = await this.pool.query(this.statements.get("get"), [key]);
          if (result.rows.length === 0) {
            return void 0;
          }
          const serialized = result.rows[0].value;
          return JSON.parse(serialized);
        } catch (error) {
          this.logger.error("Error getting value from PostgreSQL", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to get value from PostgreSQL", "get", error);
        }
      }
      /**
       * Set key-value pair
       */
      async set(key, value) {
        this.checkConnection();
        try {
          const serialized = JSON.stringify(value);
          const now = /* @__PURE__ */ new Date();
          await this.pool.query(this.statements.get("set"), [key, serialized, now, now]);
        } catch (error) {
          this.logger.error("Error setting value in PostgreSQL", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to set value in PostgreSQL", "set", error);
        }
      }
      /**
       * Delete key
       */
      async delete(key) {
        this.checkConnection();
        try {
          const client = await this.pool.connect();
          try {
            await client.query("BEGIN");
            await client.query(this.statements.get("delete"), [key]);
            await client.query(this.statements.get("deleteList"), [key]);
            await client.query("COMMIT");
          } catch (error) {
            await client.query("ROLLBACK");
            throw error;
          } finally {
            client.release();
          }
        } catch (error) {
          this.logger.error("Error deleting value from PostgreSQL", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to delete value from PostgreSQL", "delete", error);
        }
      }
      /**
       * List keys by prefix
       */
      async list(prefix) {
        this.checkConnection();
        try {
          const result = await this.pool.query(this.statements.get("list"), [prefix + "%"]);
          return result.rows.map((row) => row.key);
        } catch (error) {
          this.logger.error("Error listing keys from PostgreSQL", {
            prefix,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to list keys from PostgreSQL", "list", error);
        }
      }
      /**
       * Append item to list
       */
      async append(key, item) {
        this.checkConnection();
        try {
          const client = await this.pool.connect();
          try {
            await client.query("BEGIN");
            const serialized = JSON.stringify(item);
            const now = /* @__PURE__ */ new Date();
            await client.query(this.statements.get("listAppend"), [key, serialized, now]);
            await client.query(this.statements.get("updateListMetadata"), [key, now, now]);
            await client.query("COMMIT");
          } catch (error) {
            await client.query("ROLLBACK");
            throw error;
          } finally {
            client.release();
          }
        } catch (error) {
          this.logger.error("Error appending to list in PostgreSQL", {
            key,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to append to list in PostgreSQL", "append", error);
        }
      }
      /**
       * Get range of items from list
       */
      async getRange(key, start, count) {
        this.checkConnection();
        try {
          const result = await this.pool.query(this.statements.get("getRange"), [key, count, start]);
          return result.rows.map((row) => JSON.parse(row.value));
        } catch (error) {
          this.logger.error("Error getting range from PostgreSQL", {
            key,
            start,
            count,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("Failed to get range from PostgreSQL", "getRange", error);
        }
      }
      /**
       * Get database information
       */
      getInfo() {
        const baseInfo = {
          type: "postgres",
          connected: this.connected
        };
        if (!this.connected || !this.pool) {
          return baseInfo;
        }
        return {
          ...baseInfo,
          pool: {
            totalCount: this.pool.totalCount,
            idleCount: this.pool.idleCount,
            waitingCount: this.pool.waitingCount
          },
          config: {
            host: this.config.host || "localhost",
            port: this.config.port || 5432,
            database: this.config.database,
            ssl: this.config.ssl || false
          }
        };
      }
      /**
       * Perform database maintenance
       */
      async maintenance() {
        this.checkConnection();
        try {
          this.logger.info("Running PostgreSQL maintenance");
          await this.pool.query("ANALYZE cipher_store");
          await this.pool.query("ANALYZE cipher_lists");
          await this.pool.query("ANALYZE cipher_list_metadata");
          this.logger.info("PostgreSQL maintenance completed successfully");
        } catch (error) {
          this.logger.error("Error during PostgreSQL maintenance", {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new StorageError("PostgreSQL maintenance failed", "maintenance", error);
        }
      }
      // Private helper methods
      /**
       * Build connection pool configuration
       */
      buildPoolConfig() {
        if (this.config.url) {
          return {
            connectionString: this.config.url,
            max: this.config.pool?.max || this.config.maxConnections || 10,
            min: this.config.pool?.min || 2,
            idleTimeoutMillis: this.config.pool?.idleTimeoutMillis || this.config.idleTimeoutMillis || 3e4,
            connectionTimeoutMillis: this.config.pool?.acquireTimeoutMillis || this.config.connectionTimeoutMillis || 1e4,
            ssl: this.config.ssl
          };
        }
        return {
          host: this.config.host || "localhost",
          port: this.config.port || 5432,
          database: this.config.database,
          user: this.config.user,
          password: this.config.password,
          max: this.config.pool?.max || this.config.maxConnections || 10,
          min: this.config.pool?.min || 2,
          idleTimeoutMillis: this.config.pool?.idleTimeoutMillis || this.config.idleTimeoutMillis || 3e4,
          connectionTimeoutMillis: this.config.pool?.acquireTimeoutMillis || this.config.connectionTimeoutMillis || 1e4,
          ssl: this.config.ssl
        };
      }
      /**
       * Create database tables
       */
      async createTables() {
        if (!this.pool) {
          throw new StorageError("Database not connected", "createTables");
        }
        await this.pool.query(`
			CREATE TABLE IF NOT EXISTS cipher_store (
				key TEXT PRIMARY KEY,
				value TEXT NOT NULL,
				created_at TIMESTAMP WITH TIME ZONE NOT NULL,
				updated_at TIMESTAMP WITH TIME ZONE NOT NULL
			)
		`);
        await this.pool.query(`
			CREATE TABLE IF NOT EXISTS cipher_lists (
				key TEXT NOT NULL,
				value TEXT NOT NULL,
				position INTEGER NOT NULL,
				created_at TIMESTAMP WITH TIME ZONE NOT NULL,
				PRIMARY KEY (key, position)
			)
		`);
        await this.pool.query(`
			CREATE TABLE IF NOT EXISTS cipher_list_metadata (
				key TEXT PRIMARY KEY,
				count INTEGER NOT NULL DEFAULT 0,
				created_at TIMESTAMP WITH TIME ZONE NOT NULL,
				updated_at TIMESTAMP WITH TIME ZONE NOT NULL
			)
		`);
        await this.pool.query(
          "CREATE INDEX IF NOT EXISTS idx_cipher_store_updated_at ON cipher_store(updated_at)"
        );
        await this.pool.query("CREATE INDEX IF NOT EXISTS idx_cipher_lists_key ON cipher_lists(key)");
        await this.pool.query(
          "CREATE INDEX IF NOT EXISTS idx_cipher_lists_created_at ON cipher_lists(created_at)"
        );
        this.logger.debug("PostgreSQL tables and indexes created");
      }
      /**
       * Check if backend is connected
       */
      checkConnection() {
        if (!this.connected || !this.pool) {
          throw new StorageError("PostgreSQL backend not connected", "checkConnection");
        }
      }
    };
  }
});

// src/core/storage/manager.ts
var StorageManager;
var init_manager3 = __esm({
  "src/core/storage/manager.ts"() {
    init_cjs_shims();
    init_config3();
    init_logger2();
    init_constants2();
    StorageManager = class _StorageManager {
      // Core state
      cache;
      database;
      connected = false;
      config;
      logger;
      // Connection tracking
      connectionAttempts = 0;
      lastConnectionError;
      // Backend metadata
      cacheMetadata = {
        type: "unknown",
        isFallback: false,
        connectionTime: 0
      };
      databaseMetadata = {
        type: "unknown",
        isFallback: false,
        connectionTime: 0
      };
      // Lazy loading module references (static to share across instances)
      static redisModule;
      static sqliteModule;
      static postgresModule;
      // Health check configuration
      healthCheckKey = HEALTH_CHECK.KEY;
      healthCheckTimeout = TIMEOUTS.HEALTH_CHECK;
      /**
       * Creates a new StorageManager instance
       *
       * @param config - Storage configuration with cache and database backend configs
       * @throws {Error} If configuration is invalid
       */
      constructor(config2) {
        const validationResult2 = StorageSchema.safeParse(config2);
        if (!validationResult2.success) {
          throw new Error(
            `${ERROR_MESSAGES2.INVALID_CONFIG}: ${validationResult2.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`).join(", ")}`
          );
        }
        this.config = validationResult2.data;
        this.logger = createLogger({
          level: process.env.LOG_LEVEL || "info"
        });
        this.logger.info(`${LOG_PREFIXES2.MANAGER} Initialized with configuration`, {
          cacheType: this.config.cache.type,
          databaseType: this.config.database.type
        });
      }
      /**
       * Get the current storage configuration
       *
       * @returns The storage configuration
       */
      getConfig() {
        return this.config;
      }
      /**
       * Get information about the storage system
       *
       * @returns Storage system information including connection status and backend types
       */
      getInfo() {
        return {
          connected: this.connected,
          backends: {
            cache: {
              type: this.cacheMetadata.type,
              connected: this.cache?.isConnected() ?? false,
              fallback: this.cacheMetadata.isFallback
            },
            database: {
              type: this.databaseMetadata.type,
              connected: this.database?.isConnected() ?? false,
              fallback: this.databaseMetadata.isFallback
            }
          },
          connectionAttempts: this.connectionAttempts,
          lastError: this.lastConnectionError?.message
        };
      }
      /**
       * Get the current storage backends if connected
       *
       * @returns The storage backends or null if not connected
       */
      getBackends() {
        if (!this.connected || !this.cache || !this.database) {
          return null;
        }
        return {
          cache: this.cache,
          database: this.database
        };
      }
      /**
       * Check if the storage manager is connected
       *
       * @returns true if both backends are connected
       */
      isConnected() {
        return this.connected && this.cache?.isConnected() === true && this.database?.isConnected() === true;
      }
      // Placeholder methods for next phases
      /**
       * Connect to storage backends
       *
       * @returns The connected storage backends
       * @throws {StorageConnectionError} If strict backends fail to connect
       */
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES2.MANAGER} Already connected`, {
            cacheType: this.cacheMetadata.type,
            databaseType: this.databaseMetadata.type
          });
          return {
            cache: this.cache,
            database: this.database
          };
        }
        this.connectionAttempts++;
        this.logger.info(
          `${LOG_PREFIXES2.MANAGER} Starting connection attempt ${this.connectionAttempts}`
        );
        try {
          const cacheStartTime = Date.now();
          try {
            this.cache = await this.createCacheBackend();
            await this.cache.connect();
            this.cacheMetadata.connectionTime = Date.now() - cacheStartTime;
            this.logger.info(`${LOG_PREFIXES2.CACHE} Connected successfully`, {
              type: this.cacheMetadata.type,
              isFallback: this.cacheMetadata.isFallback,
              connectionTime: `${this.cacheMetadata.connectionTime}ms`
            });
          } catch (cacheError) {
            this.logger.warn(`${LOG_PREFIXES2.CACHE} Connection failed, attempting fallback`, {
              error: cacheError instanceof Error ? cacheError.message : String(cacheError),
              originalType: this.config.cache.type
            });
            if (this.config.cache.type !== BACKEND_TYPES.IN_MEMORY) {
              const { InMemoryBackend: InMemoryBackend4 } = await Promise.resolve().then(() => (init_in_memory(), in_memory_exports));
              this.cache = new InMemoryBackend4();
              await this.cache.connect();
              this.cacheMetadata.type = BACKEND_TYPES.IN_MEMORY;
              this.cacheMetadata.isFallback = true;
              this.cacheMetadata.connectionTime = Date.now() - cacheStartTime;
              this.logger.info(`${LOG_PREFIXES2.CACHE} Connected to fallback backend`, {
                type: this.cacheMetadata.type,
                originalType: this.config.cache.type
              });
            } else {
              throw cacheError;
            }
          }
          const dbStartTime = Date.now();
          try {
            this.database = await this.createDatabaseBackend();
            await this.database.connect();
            this.databaseMetadata.connectionTime = Date.now() - dbStartTime;
            this.logger.info(`${LOG_PREFIXES2.DATABASE} Connected successfully`, {
              type: this.databaseMetadata.type,
              isFallback: this.databaseMetadata.isFallback,
              connectionTime: `${this.databaseMetadata.connectionTime}ms`
            });
          } catch (err) {
            this.logger.error("Failed to connect to database backend", {
              error: err instanceof Error ? err.message : String(err),
              type: this.config.database.type,
              stack: err instanceof Error ? err.stack : void 0
            });
            if (this.config.database.type !== BACKEND_TYPES.IN_MEMORY) {
              this.logger.warn(
                `${LOG_PREFIXES2.DATABASE} Primary backend failed, attempting fallback to in-memory`,
                {
                  error: err instanceof Error ? err.message : String(err),
                  originalType: this.config.database.type
                }
              );
              try {
                const { InMemoryBackend: InMemoryBackend4 } = await Promise.resolve().then(() => (init_in_memory(), in_memory_exports));
                this.database = new InMemoryBackend4();
                await this.database.connect();
                this.databaseMetadata.type = BACKEND_TYPES.IN_MEMORY;
                this.databaseMetadata.isFallback = true;
                this.databaseMetadata.connectionTime = Date.now() - dbStartTime;
                this.logger.info(
                  `${LOG_PREFIXES2.DATABASE} Successfully connected to fallback backend`,
                  {
                    type: this.databaseMetadata.type,
                    originalType: this.config.database.type,
                    connectionTime: `${this.databaseMetadata.connectionTime}ms`
                  }
                );
              } catch (fallbackError) {
                this.logger.error(`${LOG_PREFIXES2.DATABASE} Fallback to in-memory also failed`, {
                  error: fallbackError instanceof Error ? fallbackError.message : String(fallbackError),
                  originalError: err instanceof Error ? err.message : String(err)
                });
                throw new Error(
                  `Database connection failed: Primary (${this.config.database.type}) and fallback (in-memory) both failed`
                );
              }
            } else {
              this.logger.error(
                `${LOG_PREFIXES2.DATABASE} In-memory backend failed, no fallback available`
              );
              throw err;
            }
          }
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES2.MANAGER} Storage system connected`, {
            cacheBackend: this.cacheMetadata.type,
            databaseBackend: this.databaseMetadata.type,
            totalConnectionTime: `${this.cacheMetadata.connectionTime + this.databaseMetadata.connectionTime}ms`
          });
          return {
            cache: this.cache,
            database: this.database
          };
        } catch (error) {
          this.lastConnectionError = error;
          if (this.cache?.isConnected()) {
            await this.cache.disconnect().catch(
              (err) => this.logger.error(`${LOG_PREFIXES2.CACHE} Error during cleanup disconnect`, {
                error: err
              })
            );
          }
          if (this.database?.isConnected()) {
            await this.database.disconnect().catch(
              (err) => this.logger.error(`${LOG_PREFIXES2.DATABASE} Error during cleanup disconnect`, {
                error: err
              })
            );
          }
          this.cache = void 0;
          this.database = void 0;
          this.connected = false;
          throw error;
        }
      }
      /**
       * Disconnect from all storage backends
       */
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES2.MANAGER} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES2.MANAGER} Disconnecting storage backends`);
        const disconnectPromises = [];
        if (this.cache?.isConnected()) {
          disconnectPromises.push(
            this.cache.disconnect().then(() => {
              this.logger.info(`${LOG_PREFIXES2.CACHE} Disconnected successfully`);
            }).catch((error) => {
              this.logger.error(`${LOG_PREFIXES2.CACHE} Disconnect error`, { error });
              throw error;
            })
          );
        }
        if (this.database?.isConnected()) {
          disconnectPromises.push(
            this.database.disconnect().then(() => {
              this.logger.info(`${LOG_PREFIXES2.DATABASE} Disconnected successfully`);
            }).catch((error) => {
              this.logger.error(`${LOG_PREFIXES2.DATABASE} Disconnect error`, { error });
              throw error;
            })
          );
        }
        try {
          await Promise.race([
            Promise.all(disconnectPromises),
            new Promise(
              (_, reject) => setTimeout(() => reject(new Error("Disconnect timeout")), TIMEOUTS.SHUTDOWN)
            )
          ]);
        } finally {
          this.cache = void 0;
          this.database = void 0;
          this.connected = false;
          this.cacheMetadata = {
            type: "unknown",
            isFallback: false,
            connectionTime: 0
          };
          this.databaseMetadata = {
            type: "unknown",
            isFallback: false,
            connectionTime: 0
          };
          this.logger.info(`${LOG_PREFIXES2.MANAGER} Storage system disconnected`);
        }
      }
      /**
       * Perform health check on all backends
       *
       * @returns Health check results for each backend
       */
      async healthCheck() {
        throw new Error("Not implemented yet - Phase 5");
      }
      // Private helper methods
      /**
       * Create cache backend based on configuration
       */
      async createCacheBackend() {
        const config2 = this.config.cache;
        this.logger.debug(`${LOG_PREFIXES2.CACHE} Creating backend`, { type: config2.type });
        switch (config2.type) {
          case BACKEND_TYPES.REDIS: {
            try {
              if (!_StorageManager.redisModule) {
                this.logger.debug(`${LOG_PREFIXES2.CACHE} Lazy loading Redis module`);
                const { RedisBackend: RedisBackend4 } = await Promise.resolve().then(() => (init_redis_backend(), redis_backend_exports));
                _StorageManager.redisModule = RedisBackend4;
              }
              const RedisBackend3 = _StorageManager.redisModule;
              this.cacheMetadata.type = BACKEND_TYPES.REDIS;
              this.cacheMetadata.isFallback = false;
              return new RedisBackend3(config2);
            } catch (error) {
              this.logger.debug(`${LOG_PREFIXES2.CACHE} Failed to create Redis backend`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES.IN_MEMORY:
          default: {
            const { InMemoryBackend: InMemoryBackend4 } = await Promise.resolve().then(() => (init_in_memory(), in_memory_exports));
            this.cacheMetadata.type = BACKEND_TYPES.IN_MEMORY;
            this.cacheMetadata.isFallback = false;
            return new InMemoryBackend4();
          }
        }
      }
      /**
       * Create database backend based on configuration
       */
      async createDatabaseBackend() {
        const config2 = this.config.database;
        this.logger.debug(`${LOG_PREFIXES2.DATABASE} Creating backend`, { type: config2.type });
        switch (config2.type) {
          case BACKEND_TYPES.SQLITE: {
            try {
              if (!_StorageManager.sqliteModule) {
                this.logger.debug(`${LOG_PREFIXES2.DATABASE} Lazy loading SQLite module`);
                const { SqliteBackend: SqliteBackend3 } = await Promise.resolve().then(() => (init_sqlite(), sqlite_exports));
                _StorageManager.sqliteModule = SqliteBackend3;
              }
              const SqliteBackend2 = _StorageManager.sqliteModule;
              this.databaseMetadata.type = BACKEND_TYPES.SQLITE;
              this.databaseMetadata.isFallback = false;
              return new SqliteBackend2(config2);
            } catch (error) {
              this.logger.debug(`${LOG_PREFIXES2.DATABASE} Failed to create SQLite backend`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES.POSTGRES: {
            try {
              if (!_StorageManager.postgresModule) {
                this.logger.debug(`${LOG_PREFIXES2.DATABASE} Lazy loading PostgreSQL module`);
                const { PostgresBackend: PostgresBackend3 } = await Promise.resolve().then(() => (init_postgresql(), postgresql_exports));
                _StorageManager.postgresModule = PostgresBackend3;
              }
              const PostgresBackend2 = _StorageManager.postgresModule;
              this.databaseMetadata.type = BACKEND_TYPES.POSTGRES;
              this.databaseMetadata.isFallback = false;
              return new PostgresBackend2(config2);
            } catch (error) {
              this.logger.debug(`${LOG_PREFIXES2.DATABASE} Failed to create PostgreSQL backend`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES.IN_MEMORY:
          default: {
            const { InMemoryBackend: InMemoryBackend4 } = await Promise.resolve().then(() => (init_in_memory(), in_memory_exports));
            this.databaseMetadata.type = BACKEND_TYPES.IN_MEMORY;
            this.databaseMetadata.isFallback = false;
            return new InMemoryBackend4();
          }
        }
      }
    };
  }
});

// src/core/session/persistence-types.ts
var SessionPersistenceError, SESSION_PERSISTENCE_CONSTANTS;
var init_persistence_types = __esm({
  "src/core/session/persistence-types.ts"() {
    init_cjs_shims();
    SessionPersistenceError = class extends Error {
      constructor(message, operation, sessionId, cause) {
        super(message);
        this.operation = operation;
        this.sessionId = sessionId;
        this.cause = cause;
        this.name = "SessionPersistenceError";
      }
    };
    SESSION_PERSISTENCE_CONSTANTS = {
      /** Current version of the serialization format */
      CURRENT_VERSION: "1.0.0",
      /** Default storage key prefix */
      DEFAULT_STORAGE_PREFIX: "cipher:sessions:",
      /** Default maximum number of sessions to save */
      DEFAULT_MAX_SESSIONS: 50,
      /** Default maximum session age (24 hours) */
      DEFAULT_MAX_AGE: 24 * 60 * 60 * 1e3,
      /** Default save timeout (30 seconds) */
      DEFAULT_SAVE_TIMEOUT: 30 * 1e3,
      /** Default load timeout (30 seconds) */
      DEFAULT_LOAD_TIMEOUT: 30 * 1e3
    };
  }
});

// src/core/session/coversation-session.ts
var import_timers, ConversationSession;
var init_coversation_session = __esm({
  "src/core/session/coversation-session.ts"() {
    init_cjs_shims();
    init_llm();
    init_logger2();
    init_env();
    init_factory4();
    init_factory5();
    init_content_detector();
    init_search_context_manager();
    init_factory();
    init_wal();
    init_manager3();
    import_timers = require("timers");
    init_persistence_types();
    init_openai2();
    init_azure2();
    init_anthropic2();
    ConversationSession = class _ConversationSession {
      /**
       * @param services - Required dependencies for the session, including unifiedToolManager
       * @param id - Session identifier
       * @param options - Optional advanced metadata options
       */
      constructor(services, id, options) {
        this.services = services;
        this.id = id;
        if (options?.sessionMemoryMetadata) {
          this.sessionMemoryMetadata = options.sessionMemoryMetadata;
        }
        if (options?.mergeMetadata) {
          this.mergeMetadata = options.mergeMetadata;
        }
        if (options?.metadataSchema) {
          this.metadataSchema = options.metadataSchema;
        }
        if (options?.beforeMemoryExtraction) {
          this.beforeMemoryExtraction = options.beforeMemoryExtraction;
        }
        if (typeof options?.historyEnabled === "boolean") {
          this.historyEnabled = options.historyEnabled;
        }
        if (options?.historyBackend) {
          this.historyBackend = options.historyBackend;
        }
        if (options?.sharedStorageManager) {
          this._storageManager = options.sharedStorageManager;
          this._storageInitialized = true;
        }
      }
      contextManager;
      _llmService;
      // Changed to lazy-loaded
      reasoningDetector;
      searchContextManager;
      _historyProvider;
      // Changed to lazy-loaded
      _storageManager;
      // Added for lazy loading
      historyEnabled = true;
      historyBackend = "database";
      sessionMemoryMetadata;
      mergeMetadata;
      metadataSchema;
      beforeMemoryExtraction;
      // Lazy initialization flags
      _servicesInitialized = false;
      _llmServiceInitialized = false;
      _storageInitialized = false;
      /**
       * Update session-level memory metadata after construction.
       */
      updateSessionMetadata(newMeta) {
        this.sessionMemoryMetadata = { ...this.sessionMemoryMetadata, ...newMeta };
      }
      /**
       * Initialize all services for the session, including history provider.
       */
      async init() {
        await this.initializeServices();
      }
      /**
       * Initializes the services for the session, including the history provider.
       */
      async initializeServices() {
        const llmConfig = this.services.stateManager.getLLMConfig(this.id);
        const formatter = this.getFormatterForProvider(llmConfig.provider);
        this.contextManager = new ContextManager(
          formatter,
          this.services.promptManager,
          void 0,
          // historyProvider will be lazy-loaded
          this.id
          // sessionId
        );
        if (this._storageManager && this.historyEnabled) {
          try {
            this._historyProvider = createDatabaseHistoryProvider(this._storageManager);
            logger.debug(
              `Session ${this.id}: History provider initialized during service initialization.`
            );
            this.contextManager.historyProvider = this._historyProvider;
          } catch (error) {
            logger.warn(
              `Session ${this.id}: Failed to initialize history provider during service initialization:`,
              error
            );
          }
        }
        this._servicesInitialized = true;
      }
      /**
       * Get the appropriate formatter for the provider
       */
      getFormatterForProvider(provider) {
        const normalizedProvider = provider.toLowerCase();
        switch (normalizedProvider) {
          case "openai":
          case "openrouter":
          case "ollama":
          case "lmstudio":
          case "qwen":
          case "gemini":
            return new OpenAIMessageFormatter();
          case "azure":
            return new AzureMessageFormatter();
          case "anthropic":
          case "aws":
            return new AnthropicMessageFormatter();
          default:
            throw new Error(
              `Unsupported provider: ${provider}. Supported providers: openai, anthropic, openrouter, ollama, lmstudio, qwen, aws, azure, gemini`
            );
        }
      }
      /**
       * Lazy initialization of LLM service
       */
      async getLLMServiceLazy() {
        if (!this._servicesInitialized || !this.contextManager) {
          throw new Error(
            "ConversationSession is not initialized. Call init() before accessing services."
          );
        }
        if (!this._llmServiceInitialized) {
          try {
            const llmConfig = this.services.stateManager.getLLMConfig(this.id);
            this._llmService = createLLMService(
              llmConfig,
              this.services.mcpManager,
              this.contextManager,
              this.services.unifiedToolManager,
              this.services.eventManager
            );
            this._llmServiceInitialized = true;
            logger.debug(`Session ${this.id}: LLM service lazy-initialized`);
          } catch (error) {
            logger.error(`Session ${this.id}: Failed to lazy-initialize LLM service`, { error });
            throw error;
          }
        }
        return this._llmService;
      }
      /**
       * Lazy initialization of storage manager and history provider with PostgreSQL/SQLite support
       */
      async getStorageManagerLazy() {
        if (!this._storageInitialized) {
          try {
            if (this.historyEnabled) {
              const multiBackendEnabled = !!process.env.CIPHER_MULTI_BACKEND;
              const flushIntervalMs = process.env.CIPHER_WAL_FLUSH_INTERVAL ? parseInt(process.env.CIPHER_WAL_FLUSH_INTERVAL, 10) : 5e3;
              if (multiBackendEnabled) {
                const primaryStorage = new StorageManager({
                  database: { type: "postgres", url: process.env.CIPHER_PG_URL },
                  cache: { type: "in-memory" }
                });
                await primaryStorage.connect();
                const backupStorage = new StorageManager({
                  database: { type: "sqlite", path: "./cipher-backup.db" },
                  cache: { type: "in-memory" }
                });
                await backupStorage.connect();
                const primaryProvider = createDatabaseHistoryProvider(primaryStorage);
                const backupProvider = createDatabaseHistoryProvider(backupStorage);
                const wal = new WALHistoryProvider();
                this._historyProvider = createMultiBackendHistoryProvider(
                  primaryProvider,
                  backupProvider,
                  wal,
                  flushIntervalMs
                );
                logger.debug(`Session ${this.id}: Multi-backend history provider lazy-initialized.`);
              } else if (this.historyBackend === "database") {
                if (!this._storageManager) {
                  await new Promise((resolve3) => setTimeout(resolve3, 25));
                }
                if (!this._storageManager) {
                  const postgresUrl = process.env.CIPHER_PG_URL;
                  const postgresHost = process.env.STORAGE_DATABASE_HOST;
                  const postgresDatabase = process.env.STORAGE_DATABASE_NAME;
                  let storageConfig;
                  if (postgresUrl || postgresHost && postgresDatabase) {
                    if (postgresUrl) {
                      storageConfig = {
                        database: { type: "postgres", url: postgresUrl },
                        cache: { type: "in-memory" }
                      };
                    } else {
                      storageConfig = {
                        database: {
                          type: "postgres",
                          host: postgresHost,
                          database: postgresDatabase,
                          port: process.env.STORAGE_DATABASE_PORT ? parseInt(process.env.STORAGE_DATABASE_PORT, 10) : 5432,
                          user: process.env.STORAGE_DATABASE_USER,
                          password: process.env.STORAGE_DATABASE_PASSWORD,
                          ssl: process.env.STORAGE_DATABASE_SSL === "true"
                        },
                        cache: { type: "in-memory" }
                      };
                    }
                    logger.debug(
                      `Session ${this.id}: Using PostgreSQL for history provider (lazy-loaded)`
                    );
                  } else {
                    storageConfig = {
                      database: {
                        type: "sqlite",
                        path: env.STORAGE_DATABASE_PATH || "./data",
                        database: env.STORAGE_DATABASE_NAME || "cipher-sessions.db"
                      },
                      cache: { type: "in-memory" }
                    };
                    logger.debug(`Session ${this.id}: Using SQLite for history provider (lazy-loaded)`);
                  }
                  this._storageManager = new StorageManager(storageConfig);
                  await this._storageManager.connect();
                  logger.debug(
                    `Session ${this.id}: Database history provider lazy-initialized with ${storageConfig.database.type} backend.`
                  );
                } else {
                  logger.debug(
                    `Session ${this.id}: Using shared storage manager for history provider.`
                  );
                }
                this._historyProvider = createDatabaseHistoryProvider(this._storageManager);
                logger.debug(`Session ${this.id}: History provider created with storage manager.`);
              } else {
                logger.debug(`Session ${this.id}: In-memory history provider selected (lazy-loaded).`);
              }
            }
            this._storageInitialized = true;
          } catch (error) {
            logger.warn(`Session ${this.id}: Failed to initialize storage manager:`, error);
          }
        }
        return this._storageManager;
      }
      /**
       * Lazy initialization of history provider
       */
      async getHistoryProviderLazy() {
        if (!this._storageInitialized) {
          await this.getStorageManagerLazy();
        }
        if (!this._historyProvider && this.historyEnabled && this._storageManager) {
          try {
            this._historyProvider = createDatabaseHistoryProvider(this._storageManager);
            logger.debug(`Session ${this.id}: History provider created in lazy initialization.`);
          } catch (error) {
            logger.warn(
              `Session ${this.id}: Failed to create history provider in lazy initialization:`,
              error
            );
          }
        }
        return this._historyProvider;
      }
      /**
       * Restore history when history provider is lazy-loaded
       */
      async restoreHistoryLazy() {
        if (this.historyEnabled) {
          try {
            const historyProvider = await this.getHistoryProviderLazy();
            if (historyProvider && this.contextManager) {
              this.contextManager.historyProvider = historyProvider;
              await this.contextManager.restoreHistory?.();
              logger.debug(`Session ${this.id}: Conversation history restored (lazy-loaded)`);
            }
          } catch (err) {
            logger.warn(`Session ${this.id}: Failed to restore conversation history: ${err}`);
          }
        }
      }
      /**
       * Extract session-level metadata, merging defaults, session, and per-run metadata.
       * Uses custom merge and validation if provided.
       * Now supports environment and extensible context.
       */
      getSessionMetadata(customMetadata) {
        const base = {
          sessionId: this.id,
          source: "conversation-session",
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          environment: process.env.NODE_ENV || "development",
          ...this.getSessionContext()
        };
        const sessionMeta = this.sessionMemoryMetadata || {};
        const customMeta = customMetadata && typeof customMetadata === "object" && !Array.isArray(customMetadata) ? customMetadata : {};
        let merged = this.mergeMetadata ? this.mergeMetadata(sessionMeta, customMeta) : { ...base, ...sessionMeta, ...customMeta };
        if (this.metadataSchema && !this.metadataSchema.safeParse(merged).success) {
          logger.warn(
            "ConversationSession: Metadata validation failed, using session-level metadata only."
          );
          merged = { ...base, ...sessionMeta };
        }
        return merged;
      }
      /**
       * Optionally override to provide additional session context for metadata.
       */
      getSessionContext() {
        return {};
      }
      /**
       * Run a conversation session with input, optional image data, streaming, and custom options.
       * @param input - User input string
       * @param imageDataInput - Optional image data
       * @param stream - Optional stream flag
       * @param options - Optional parameters for memory extraction:
       *   - memoryMetadata: Custom metadata to attach to memory extraction (merged with session defaults)
       *   - contextOverrides: Overrides for context fields passed to memory extraction
       *   - historyTracking: Enable/disable history tracking
       * @returns An object containing the response and a promise for background operations
       */
      async run(input, imageDataInput, stream, options) {
        if (typeof input !== "string" || input.trim() === "") {
          logger.error("ConversationSession.run: input must be a non-empty string");
          throw new Error("Input must be a non-empty string");
        }
        if (!this._servicesInitialized || !this.contextManager) {
          logger.error("ConversationSession.run: Session not initialized. Call init() before run().");
          throw new Error("ConversationSession is not initialized. Call init() before run().");
        }
        if (imageDataInput !== void 0 && (typeof imageDataInput !== "object" || !imageDataInput.image || typeof imageDataInput.image !== "string" || !imageDataInput.mimeType || typeof imageDataInput.mimeType !== "string")) {
          logger.error(
            "ConversationSession.run: imageDataInput must have image and mimeType as non-empty strings"
          );
          throw new Error("imageDataInput must have image and mimeType as non-empty strings");
        }
        if (stream !== void 0 && typeof stream !== "boolean") {
          logger.warn("ConversationSession.run: stream should be a boolean. Coercing to boolean.");
          stream = Boolean(stream);
        }
        if (options && typeof options === "object") {
          const allowedKeys = ["memoryMetadata", "contextOverrides", "historyTracking"];
          const unknownKeys = Object.keys(options).filter((k) => !allowedKeys.includes(k));
          if (unknownKeys.length > 0) {
            logger.warn(
              `ConversationSession.run: Unknown option keys provided: ${unknownKeys.join(", ")}`
            );
          }
        }
        logger.debug("ConversationSession.run called");
        logger.debug(
          `Running session ${this.id} with input: ${input} and imageDataInput: ${imageDataInput} and stream: ${stream}`
        );
        await this.initializeReasoningServices();
        await this.restoreHistoryLazy();
        const llmService = await this.getLLMServiceLazy();
        if (this.services.eventManager) {
          try {
            const sessionBus = this.services.eventManager.getSessionEventBus(this.id);
            sessionBus.emit("llm:thinking", {
              sessionId: this.id,
              timestamp: Date.now()
            });
            logger.debug("Emitted llm:thinking event", { sessionId: this.id });
          } catch (error) {
            logger.warn("Failed to emit thinking event", {
              sessionId: this.id,
              error: error instanceof Error ? error.message : String(error)
            });
          }
        }
        const response = await llmService.generate(input, imageDataInput, stream);
        const backgroundOperations = new Promise((resolve3) => {
          (0, import_timers.setImmediate)(async () => {
            const embeddingsDisabled = env.DISABLE_EMBEDDINGS || env.EMBEDDING_DISABLED;
            let embeddingManagerDisabled = false;
            if (this.services.embeddingManager) {
              try {
                const { EmbeddingSystemState } = require("../embedding/manager.js");
                if (EmbeddingSystemState.getInstance().isDisabled()) {
                  embeddingManagerDisabled = true;
                }
              } catch {
              }
              if (!this.services.embeddingManager?.hasAvailableEmbeddings()) {
                embeddingManagerDisabled = true;
              }
              const embeddingStatus = this.services.embeddingManager?.getEmbeddingStatus();
              if (embeddingStatus) {
                const disabledEmbedders = Object.values(embeddingStatus).filter(
                  (status) => status.status === "DISABLED"
                );
                if (disabledEmbedders.length > 0) {
                  embeddingManagerDisabled = true;
                }
              }
            }
            if (embeddingsDisabled || embeddingManagerDisabled) {
              logger.debug("Skipping all background memory operations - embeddings disabled", {
                sessionId: this.id,
                envDisabled: embeddingsDisabled,
                managerDisabled: embeddingManagerDisabled
              });
              resolve3();
              return;
            }
            logger.debug("Starting background memory operations", { sessionId: this.id });
            try {
              await this.enforceMemoryExtraction(input, response, options);
              logger.debug("Background memory operations completed successfully", {
                sessionId: this.id
              });
            } catch (error) {
              logger.debug("Background memory extraction failed", {
                sessionId: this.id,
                error: error instanceof Error ? error.message : String(error)
              });
            }
            resolve3();
          });
        });
        return { response, backgroundOperations };
      }
      /**
       * Programmatically enforce memory extraction after each user interaction (runs in background)
       * This ensures the extract_and_operate_memory tool is always called, regardless of AI decisions
       * NOTE: This method runs asynchronously in the background to avoid delaying the user response
       */
      async enforceMemoryExtraction(userInput, aiResponse, options) {
        logger.debug("ConversationSession.enforceMemoryExtraction called");
        logger.debug("enforceMemoryExtraction: unifiedToolManager at entry", {
          unifiedToolManager: this.services.unifiedToolManager,
          type: typeof this.services.unifiedToolManager
        });
        try {
          logger.debug("ConversationSession: Enforcing memory extraction for interaction");
          const embeddingsDisabled = env.DISABLE_EMBEDDINGS || env.EMBEDDING_DISABLED;
          let embeddingManagerDisabled = false;
          if (this.services.embeddingManager) {
            try {
              const { EmbeddingSystemState } = require("../embedding/manager.js");
              if (EmbeddingSystemState.getInstance().isDisabled()) {
                embeddingManagerDisabled = true;
              }
            } catch {
            }
            if (!this.services.embeddingManager?.hasAvailableEmbeddings()) {
              embeddingManagerDisabled = true;
            }
            const embeddingStatus = this.services.embeddingManager?.getEmbeddingStatus();
            if (embeddingStatus) {
              const disabledCount = Object.values(embeddingStatus).filter(
                (status) => status.status === "DISABLED"
              ).length;
              if (disabledCount > 0) {
                embeddingManagerDisabled = true;
              }
            }
          }
          if (embeddingsDisabled || embeddingManagerDisabled) {
            logger.debug("ConversationSession: Embeddings disabled, skipping memory extraction", {
              envDisabled: embeddingsDisabled,
              managerDisabled: embeddingManagerDisabled
            });
            return;
          }
          if (!this.services.unifiedToolManager) {
            logger.debug(
              "ConversationSession: UnifiedToolManager not available, skipping memory extraction"
            );
            return;
          }
          const workspaceMemoryEnabled = env.USE_WORKSPACE_MEMORY;
          const shouldDisableDefaultMemory2 = workspaceMemoryEnabled && env.DISABLE_DEFAULT_MEMORY;
          const shouldRunWorkspace = workspaceMemoryEnabled;
          const shouldRunDefault = !shouldDisableDefaultMemory2;
          if (embeddingsDisabled) {
            logger.debug("ConversationSession: Memory extraction skipped - embeddings disabled", {
              embeddingsDisabled,
              workspaceMemoryEnabled,
              shouldDisableDefaultMemory: shouldDisableDefaultMemory2
            });
            return;
          }
          logger.debug("ConversationSession: Memory tools to execute", {
            workspaceMemoryEnabled,
            shouldDisableDefaultMemory: shouldDisableDefaultMemory2,
            shouldRunWorkspace,
            shouldRunDefault
          });
          const comprehensiveInteractionData = await this.extractComprehensiveInteractionData(
            userInput,
            aiResponse
          );
          const defaultContext = {
            sessionId: this.id,
            conversationTopic: "Interactive CLI session",
            recentMessages: comprehensiveInteractionData
          };
          const mergedContext = {
            ...defaultContext,
            ...options?.contextOverrides && typeof options.contextOverrides === "object" && !Array.isArray(options.contextOverrides) ? options.contextOverrides : {}
          };
          let memoryMetadata = {};
          if (options?.memoryMetadata !== void 0) {
            if (typeof options.memoryMetadata === "object" && !Array.isArray(options.memoryMetadata)) {
              memoryMetadata = this.getSessionMetadata(options.memoryMetadata);
            } else {
              logger.warn(
                "ConversationSession: Invalid memoryMetadata provided, expected a plain object. Using session-level or default metadata."
              );
              memoryMetadata = this.getSessionMetadata();
            }
          } else {
            memoryMetadata = this.getSessionMetadata();
          }
          const memoryResults = [];
          if (shouldRunWorkspace) {
            const workspaceArgs = {
              interaction: comprehensiveInteractionData,
              context: mergedContext,
              options: {
                similarityThreshold: 0.8,
                confidenceThreshold: 0.6,
                enableBatchProcessing: true,
                autoExtractWorkspaceInfo: true
              }
            };
            try {
              const workspaceResult = await this.services.unifiedToolManager.executeToolWithoutLoading(
                "cipher_workspace_store",
                workspaceArgs
              );
              memoryResults.push({ tool: "cipher_workspace_store", result: workspaceResult });
              logger.debug("ConversationSession: Workspace memory tool executed successfully");
            } catch (error) {
              logger.debug("ConversationSession: Workspace memory tool execution failed", {
                error: error instanceof Error ? error.message : String(error)
              });
            }
          }
          if (shouldRunDefault) {
            const defaultArgs = {
              interaction: comprehensiveInteractionData,
              context: mergedContext,
              memoryMetadata,
              options: {
                similarityThreshold: 0.7,
                maxSimilarResults: 5,
                useLLMDecisions: true,
                confidenceThreshold: 0.4,
                enableDeleteOperations: true,
                historyTracking: options?.historyTracking ?? true
              }
            };
            try {
              const defaultResult = await this.services.unifiedToolManager.executeToolWithoutLoading(
                "cipher_extract_and_operate_memory",
                defaultArgs
              );
              memoryResults.push({ tool: "cipher_extract_and_operate_memory", result: defaultResult });
              logger.debug("ConversationSession: Default memory tool executed successfully");
            } catch (error) {
              logger.debug("ConversationSession: Default memory tool execution failed", {
                error: error instanceof Error ? error.message : String(error)
              });
            }
          }
          if (memoryResults.length === 0) {
            logger.debug("ConversationSession: No memory tools executed successfully");
            return;
          }
          let totalExtractedFacts = 0;
          let totalMemoryActions = 0;
          const toolSummary = {};
          const combinedActions = [];
          for (const { tool, result } of memoryResults) {
            if (result.success) {
              totalExtractedFacts += result.extraction?.extracted || 0;
              const actions = tool === "cipher_workspace_store" ? result.workspace || [] : result.memory || [];
              totalMemoryActions += actions.length;
              combinedActions.push(...actions);
              toolSummary[tool] = {
                success: true,
                extractedFacts: result.extraction?.extracted || 0,
                memoryActions: actions.length
              };
            } else {
              toolSummary[tool] = { success: false };
            }
          }
          logger.debug("ConversationSession: Memory extraction completed", {
            toolsExecuted: memoryResults.map((r) => r.tool),
            shouldRunWorkspace,
            shouldRunDefault,
            totalExtractedFacts,
            totalMemoryActions,
            toolSummary,
            actionBreakdown: combinedActions.length > 0 ? {
              ADD: combinedActions.filter((a) => a.event === "ADD").length,
              UPDATE: combinedActions.filter((a) => a.event === "UPDATE").length,
              DELETE: combinedActions.filter((a) => a.event === "DELETE").length,
              NONE: combinedActions.filter((a) => a.event === "NONE").length
            } : {}
          });
          const allTools = await this.services.unifiedToolManager.getAllTools();
          await this.enforceReflectionMemoryProcessing(userInput, aiResponse, allTools);
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("ConversationSession: Memory extraction failed", {
            error: errorMessage
          });
        }
      }
      /**
       * Initialize reasoning services (content detector and search context manager)
       */
      async initializeReasoningServices() {
        if (this.reasoningDetector && this.searchContextManager) {
          return;
        }
        try {
          const evalLlmConfig = this.services.stateManager.getEvalLLMConfig(this.id);
          this.reasoningDetector = new ReasoningContentDetector(
            this.services.promptManager,
            this.services.mcpManager,
            this.services.unifiedToolManager,
            evalLlmConfig,
            void 0
            // Use default options
          );
          this.searchContextManager = new SearchContextManager();
          logger.debug("ConversationSession: Reasoning services initialized", { sessionId: this.id });
        } catch (error) {
          logger.warn("ConversationSession: Failed to initialize reasoning services", {
            sessionId: this.id,
            error: error instanceof Error ? error.message : String(error)
          });
        }
      }
      /**
       * Programmatically enforce reflection memory processing after each interaction (runs in background)
       * This automatically extracts, evaluates, and stores reasoning patterns in the background
       * NOTE: This method is called from enforceMemoryExtraction which already runs asynchronously
       * @param userInput - The user input string
       * @param aiResponse - The AI response string
       * @param allTools - Pre-loaded tools to avoid redundant loading
       */
      async enforceReflectionMemoryProcessing(userInput, _aiResponse, allTools) {
        try {
          logger.debug("ConversationSession: Enforcing reflection memory processing");
          const embeddingsDisabled = env.DISABLE_EMBEDDINGS || env.EMBEDDING_DISABLED;
          if (!this.services.unifiedToolManager) {
            logger.debug(
              "ConversationSession: UnifiedToolManager not available, skipping reflection memory processing"
            );
            return;
          }
          const reflectionToolsAvailable = allTools["cipher_extract_reasoning_steps"] && allTools["cipher_store_reasoning_memory"];
          if (embeddingsDisabled || !reflectionToolsAvailable) {
            logger.debug("ConversationSession: Reflection memory processing skipped", {
              embeddingsDisabled,
              reflectionToolsAvailable: !!reflectionToolsAvailable,
              reason: embeddingsDisabled ? "embeddings disabled" : "reflection tools unavailable"
            });
            return;
          }
          if (env.DISABLE_REFLECTION_MEMORY) {
            logger.debug(
              "ConversationSession: Reflection memory force disabled via DISABLE_REFLECTION_MEMORY, skipping processing"
            );
            return;
          }
          await this.initializeReasoningServices();
          if (!this.reasoningDetector) {
            logger.debug(
              "ConversationSession: Reasoning detector not available, skipping reflection processing"
            );
            return;
          }
          const reasoningDetection = await this.reasoningDetector.detectReasoningContent(userInput, {
            sessionId: this.id,
            taskType: "conversation"
          });
          if (!reasoningDetection.containsReasoning) {
            logger.debug(
              "ConversationSession: No reasoning content detected in user input, skipping reflection processing",
              {
                confidence: reasoningDetection.confidence,
                detectedPatterns: reasoningDetection.detectedPatterns
              }
            );
            return;
          }
          logger.debug(
            "ConversationSession: Reasoning content detected, proceeding with reflection processing",
            {
              confidence: reasoningDetection.confidence,
              detectedPatterns: reasoningDetection.detectedPatterns
            }
          );
          let extractionResult;
          try {
            extractionResult = await this.services.unifiedToolManager.executeTool(
              "cipher_extract_reasoning_steps",
              {
                userInput,
                options: {
                  extractExplicit: true,
                  extractImplicit: true,
                  includeMetadata: true
                }
              },
              this.id
            );
            logger.debug("ConversationSession: Reasoning extraction completed", {
              success: extractionResult.success,
              stepCount: extractionResult.result?.trace?.steps?.length || 0,
              traceId: extractionResult.result?.trace?.id
            });
          } catch (extractError) {
            logger.debug("ConversationSession: Reasoning extraction failed", {
              error: extractError instanceof Error ? extractError.message : String(extractError)
            });
            return;
          }
          if (!extractionResult.success || !extractionResult.result?.trace?.steps?.length) {
            return;
          }
          const reasoningTrace = extractionResult.result.trace;
          let evaluationResult;
          try {
            const evalConfig = this.services.stateManager.getEvalLLMConfig(this.id);
            const evalContextManager = createContextManager(
              evalConfig,
              this.services.promptManager,
              void 0,
              void 0
            );
            const evalLLMService = createLLMService(
              evalConfig,
              this.services.mcpManager,
              evalContextManager,
              this.services.unifiedToolManager
            );
            evaluationResult = await this.services.unifiedToolManager.executeTool(
              "cipher_evaluate_reasoning",
              {
                trace: reasoningTrace,
                options: {
                  checkEfficiency: true,
                  detectLoops: true,
                  generateSuggestions: true
                },
                llmService: evalLLMService
              },
              this.id
            );
            logger.debug("ConversationSession: Reasoning evaluation completed", {
              success: evaluationResult.success,
              qualityScore: evaluationResult.result?.evaluation?.qualityScore,
              shouldStore: evaluationResult.result?.evaluation?.shouldStore
            });
          } catch (evalError) {
            logger.debug("ConversationSession: Reasoning evaluation failed", {
              error: evalError instanceof Error ? evalError.message : String(evalError),
              traceId: reasoningTrace.id
            });
            return;
          }
          if (!evaluationResult.result?.evaluation?.shouldStore) {
            logger.debug(
              "ConversationSession: Evaluation indicates should not store, skipping storage",
              {
                shouldStore: evaluationResult.result?.evaluation?.shouldStore,
                qualityScore: evaluationResult.result?.evaluation?.qualityScore
              }
            );
            return;
          }
          const evaluation = evaluationResult.result.evaluation;
          try {
            const storageResult = await this.services.unifiedToolManager.executeTool(
              "cipher_store_reasoning_memory",
              {
                trace: reasoningTrace,
                evaluation
              },
              this.id
            );
            logger.debug("ConversationSession: Reflection memory storage completed", {
              success: storageResult.success,
              stored: storageResult.result?.stored,
              traceId: storageResult.result?.traceId,
              vectorId: storageResult.result?.vectorId,
              stepCount: storageResult.result?.metrics?.stepCount,
              qualityScore: storageResult.result?.metrics?.qualityScore
            });
            if (storageResult.success && storageResult.result?.stored) {
              logger.debug("ConversationSession: Reflection memory processing completed successfully", {
                pipeline: "extract \u2192 evaluate \u2192 store",
                traceId: storageResult.result.traceId,
                stepCount: reasoningTrace.steps.length,
                qualityScore: evaluation.qualityScore.toFixed(3),
                issueCount: evaluation.issues?.length || 0,
                suggestionCount: evaluation.suggestions?.length || 0
              });
            }
          } catch (storageError) {
            logger.debug("ConversationSession: Reflection memory storage failed", {
              error: storageError instanceof Error ? storageError.message : String(storageError),
              traceId: reasoningTrace.id,
              qualityScore: evaluation.qualityScore
            });
          }
        } catch (error) {
          logger.debug("ConversationSession: Reflection memory processing failed", {
            error: error instanceof Error ? error.message : String(error)
          });
        }
      }
      /**
       * Extract comprehensive interaction data including tool calls and results
       * This captures the complete technical workflow, not just user input and final response
       */
      async extractComprehensiveInteractionData(userInput, aiResponse) {
        const interactionData = [];
        interactionData.push(`User: ${userInput}`);
        const recentMessages = this.contextManager.getRawMessages();
        const currentInteractionMessages = [];
        let foundUserMessage = false;
        for (let i = recentMessages.length - 1; i >= 0; i--) {
          const message = recentMessages[i];
          if (!message) {
            continue;
          }
          if (!foundUserMessage) {
            if (message.role === "user" && Array.isArray(message.content) && message.content.length > 0 && message.content[0] && message.content[0].type === "text" && "text" in message.content[0] && message.content[0].text === userInput) {
              foundUserMessage = true;
            }
            continue;
          }
          if (message.role === "assistant" || message.role === "tool") {
            currentInteractionMessages.unshift(message);
          } else {
            break;
          }
        }
        const toolsUsed = [];
        const toolResults = [];
        for (const message of currentInteractionMessages) {
          if (!message) {
            continue;
          }
          if (message.role === "assistant" && message.toolCalls && message.toolCalls.length > 0) {
            for (const toolCall of message.toolCalls) {
              const toolName = toolCall.function.name;
              let args = "";
              try {
                const parsedArgs = JSON.parse(toolCall.function.arguments);
                const keyArgs = this.summarizeToolArguments(toolName, parsedArgs);
                args = keyArgs ? ` with ${keyArgs}` : "";
              } catch {
                args = " with arguments";
              }
              toolsUsed.push(`${toolName}${args}`);
            }
          } else if (message.role === "tool") {
            const toolName = message.name || "unknown_tool";
            const resultSummary = this.summarizeToolResult(toolName, message.content);
            toolResults.push(`${toolName}: ${resultSummary}`);
          }
        }
        if (toolsUsed.length > 0) {
          interactionData.push(`Tools used: ${toolsUsed.join(", ")}`);
        }
        if (toolResults.length > 0) {
          interactionData.push(`Tool results: ${toolResults.join("; ")}`);
        }
        interactionData.push(`Assistant: ${aiResponse}`);
        logger.debug("ConversationSession: Extracted comprehensive interaction data", {
          userInput: userInput.substring(0, 50),
          toolsUsed: toolsUsed.length,
          toolResults: toolResults.length,
          totalDataPoints: interactionData.length
        });
        return interactionData;
      }
      /**
       * Summarize tool arguments for memory storage
       */
      summarizeToolArguments(toolName, args) {
        switch (toolName) {
          case "read_file":
            return args.path ? `path: ${args.path}` : "file read";
          case "write_file":
            return args.path ? `path: ${args.path}` : "file write";
          case "list_files":
            return args.path ? `directory: ${args.path}` : "directory listing";
          case "cipher_memory_search":
            return args.query ? `query: "${args.query.substring(0, 50)}${args.query.length > 50 ? "..." : ""}"` : "memory search";
          default:
            if (args.query) {
              return `query: "${args.query.substring(0, 30)}${args.query.length > 30 ? "..." : ""}"`;
            }
            if (args.path) {
              return `path: ${args.path}`;
            }
            if (args.file) {
              return `file: ${args.file}`;
            }
            return "arguments provided";
        }
      }
      /**
       * Summarize tool results for memory storage
       */
      summarizeToolResult(toolName, content) {
        try {
          if (typeof content === "string") {
            const parsed = JSON.parse(content);
            return this.formatToolResultSummary(toolName, parsed);
          }
          if (typeof content === "object") {
            return this.formatToolResultSummary(toolName, content);
          }
          return "result received";
        } catch {
          const contentStr = String(content);
          return contentStr.length > 100 ? `${contentStr.substring(0, 100)}...` : contentStr;
        }
      }
      /**
       * Format tool result summary based on tool type
       */
      formatToolResultSummary(toolName, result) {
        switch (toolName) {
          case "read_file":
            if (result.content && Array.isArray(result.content) && result.content.length > 0) {
              const text = result.content[0].text || "";
              const lines = text.split("\n").length;
              const size = text.length;
              return `file read (${lines} lines, ${size} chars)`;
            }
            return "file read";
          case "cipher_memory_search":
            if (result.results && Array.isArray(result.results)) {
              return `found ${result.results.length} memory entries`;
            }
            return "memory search completed";
          case "list_files":
            if (result.content && Array.isArray(result.content)) {
              const files = result.content.filter((item) => item.type === "file").length;
              const dirs = result.content.filter((item) => item.type === "directory").length;
              return `listed ${files} files, ${dirs} directories`;
            }
            return "directory listing";
          default:
            if (result.success !== void 0) {
              return result.success ? "completed successfully" : "failed";
            }
            if (result.error) {
              return `error: ${String(result.error).substring(0, 50)}`;
            }
            return "completed";
        }
      }
      /**
       * Disconnects the history provider if it exists (for session teardown).
       */
      async disconnect() {
        if (this._historyProvider && typeof this._historyProvider.disconnect === "function") {
          try {
            await this._historyProvider.disconnect();
            logger.debug(`Session ${this.id}: History provider disconnected.`);
          } catch (err) {
            logger.warn(`Session ${this.id}: Failed to disconnect history provider: ${err}`);
          }
        }
      }
      getContextManager() {
        return this.contextManager;
      }
      /**
       * Get LLM service with lazy initialization
       */
      async getLLMService() {
        return await this.getLLMServiceLazy();
      }
      getUnifiedToolManager() {
        return this.services.unifiedToolManager;
      }
      /**
       * Get the storageManager with lazy initialization
       */
      async getStorageManager() {
        return await this.getStorageManagerLazy();
      }
      /**
       * Get the history provider with lazy initialization
       */
      async getHistoryProvider() {
        return await this.getHistoryProviderLazy();
      }
      /**
       * Force refresh conversation history from the database
       */
      async refreshConversationHistory() {
        if (this.historyEnabled && this.contextManager) {
          try {
            if (typeof this.contextManager.clearMessages === "function") {
              this.contextManager.clearMessages();
              logger.debug(`Session ${this.id}: Cleared existing messages from context manager`);
            }
            const historyProvider = await this.getHistoryProviderLazy();
            if (historyProvider) {
              this.contextManager.historyProvider = historyProvider;
              logger.debug(`Session ${this.id}: Set/updated history provider in context manager`);
              const history = await historyProvider.getHistory(this.id);
              logger.debug(
                `Session ${this.id}: Retrieved ${history.length} messages from history provider`
              );
              let historyRestored = false;
              if (this.contextManager.restoreHistory && !historyRestored) {
                try {
                  await this.contextManager.restoreHistory();
                  historyRestored = true;
                  logger.debug(
                    `Session ${this.id}: Successfully restored history via context manager restoreHistory`
                  );
                } catch (restoreError) {
                  logger.debug(`Session ${this.id}: restoreHistory failed:`, restoreError);
                }
              }
              if (!historyRestored && typeof this.contextManager.setMessages === "function") {
                try {
                  this.contextManager.setMessages(history);
                  historyRestored = true;
                  logger.debug(
                    `Session ${this.id}: Successfully restored ${history.length} messages via setMessages`
                  );
                } catch (setError) {
                  logger.debug(`Session ${this.id}: setMessages failed:`, setError);
                }
              }
              if (!historyRestored && history.length > 0) {
                try {
                  let addedCount = 0;
                  for (const message of history) {
                    try {
                      await this.contextManager.addMessage(message);
                      addedCount++;
                    } catch (addError) {
                      logger.warn(
                        `Session ${this.id}: Failed to add message ${addedCount + 1}:`,
                        addError
                      );
                    }
                  }
                  historyRestored = addedCount > 0;
                  logger.debug(
                    `Session ${this.id}: Manually added ${addedCount}/${history.length} messages to context manager`
                  );
                } catch (manualError) {
                  logger.warn(`Session ${this.id}: Manual message addition failed:`, manualError);
                }
              }
              const contextMessages = this.contextManager.getRawMessages();
              logger.info(
                `Session ${this.id}: History refresh complete - Provider: ${history.length} messages, Context: ${contextMessages.length} messages, Restored: ${historyRestored}`
              );
            } else {
              logger.debug(`Session ${this.id}: No history provider available for refresh`);
              try {
                const messages = this.contextManager.getRawMessages();
                logger.debug(
                  `Session ${this.id}: Context manager has ${messages.length} existing messages`
                );
              } catch (fallbackError) {
                logger.debug(
                  `Session ${this.id}: Failed to get history from context manager:`,
                  fallbackError
                );
              }
            }
          } catch (error) {
            logger.warn(`Session ${this.id}: Failed to refresh conversation history:`, error);
          }
        } else {
          logger.debug(`Session ${this.id}: History not enabled or context manager not available`);
        }
      }
      /**
       * Get the current conversation history for debugging
       */
      async getConversationHistory() {
        if (this._historyProvider && this.historyEnabled) {
          try {
            const history = await this._historyProvider.getHistory(this.id);
            logger.debug(
              `Session ${this.id}: Current conversation history has ${history.length} messages`
            );
            return history;
          } catch (error) {
            logger.warn(`Session ${this.id}: Failed to get conversation history:`, error);
            return [];
          }
        } else {
          try {
            if (this.contextManager) {
              const messages = this.contextManager.getRawMessages();
              logger.debug(
                `Session ${this.id}: Got ${messages.length} messages from context manager as fallback`
              );
              return messages;
            }
          } catch (fallbackError) {
            logger.debug(
              `Session ${this.id}: Failed to get history from context manager:`,
              fallbackError
            );
          }
          logger.debug(`Session ${this.id}: No history provider available`);
          return [];
        }
      }
      /**
       * Get the current conversation history from context manager
       */
      getContextHistory() {
        if (this.contextManager) {
          const messages = this.contextManager.getRawMessages();
          logger.debug(`Session ${this.id}: Context manager has ${messages.length} messages`);
          return messages;
        } else {
          logger.debug(`Session ${this.id}: No context manager available`);
          return [];
        }
      }
      /**
       * Serialize the current session state for persistence
       * @returns SerializedSession containing all necessary data to restore this session
       */
      async serialize() {
        try {
          let conversationHistory = [];
          if (this._historyProvider && this.historyEnabled) {
            try {
              conversationHistory = await this._historyProvider.getHistory(this.id);
            } catch (error) {
              logger.warn(
                `Session ${this.id}: Failed to retrieve history for session during serialization:`,
                error
              );
            }
          }
          if (conversationHistory.length === 0 && this.contextManager) {
            try {
              const contextHistory = this.contextManager.getRawMessages();
              if (contextHistory.length > 0) {
                conversationHistory = contextHistory;
                logger.debug(
                  `Session ${this.id}: Using context manager history as fallback (${contextHistory.length} messages)`
                );
              }
            } catch (fallbackError) {
              logger.debug(
                `Session ${this.id}: Failed to get history from context manager during serialization:`,
                fallbackError
              );
            }
          }
          const options = {};
          if (this.metadataSchema) {
            try {
              options.hadMetadataSchema = true;
            } catch (error) {
              logger.warn(`Failed to serialize metadata schema for session ${this.id}:`, error);
            }
          }
          const serialized = {
            id: this.id,
            metadata: {
              createdAt: Date.now(),
              // We don't track creation time currently, use now as fallback
              lastActivity: Date.now(),
              ...this.sessionMemoryMetadata && {
                sessionMemoryMetadata: { ...this.sessionMemoryMetadata }
              },
              historyEnabled: this.historyEnabled,
              historyBackend: this.historyBackend
            },
            conversationHistory,
            options: Object.keys(options).length > 0 ? options : void 0,
            version: SESSION_PERSISTENCE_CONSTANTS.CURRENT_VERSION,
            serializedAt: Date.now()
          };
          logger.info(`Session ${this.id}: Serialized with ${conversationHistory.length} messages`);
          return serialized;
        } catch (error) {
          throw new SessionPersistenceError(
            `Failed to serialize session ${this.id}`,
            "serialize",
            this.id,
            error
          );
        }
      }
      /**
       * Deserialize and restore a session from serialized data
       * @param data - Serialized session data
       * @param services - Service dependencies required for session creation
       * @returns A new ConversationSession instance restored from the data
       */
      static async deserialize(data, services) {
        try {
          if (data.version !== SESSION_PERSISTENCE_CONSTANTS.CURRENT_VERSION) {
            logger.warn(
              `Session ${data.id} has version ${data.version}, current is ${SESSION_PERSISTENCE_CONSTANTS.CURRENT_VERSION}. Attempting to restore anyway.`
            );
          }
          const options = {
            sessionMemoryMetadata: data.metadata.sessionMemoryMetadata,
            historyEnabled: data.metadata.historyEnabled,
            historyBackend: data.metadata.historyBackend
          };
          if (data.options?.hadMetadataSchema) {
            logger.debug(
              `Session ${data.id} had a metadata schema, but it will need to be re-provided during configuration.`
            );
          }
          const sessionServices = {
            ...services,
            ...services.contextManager && {
              contextManager: services.contextManager
            }
          };
          const session = new _ConversationSession(sessionServices, data.id, options);
          await session.init();
          if (data.conversationHistory.length > 0) {
            try {
              const historyProvider = await session.getHistoryProvider();
              if (historyProvider) {
                await historyProvider.clearHistory(data.id);
                for (const message of data.conversationHistory) {
                  await historyProvider.saveMessage(data.id, message);
                }
                logger.debug(
                  `Session ${data.id}: Restored ${data.conversationHistory.length} messages to history provider`
                );
              } else {
                logger.warn(`Session ${data.id}: No history provider available for restoration`);
              }
              await session.refreshConversationHistory();
              logger.info(
                `Session ${data.id}: Restored ${data.conversationHistory.length} messages and refreshed context manager`
              );
            } catch (error) {
              logger.warn(
                `Session ${data.id}: Failed to restore conversation history from serialized data:`,
                error
              );
            }
          } else {
            logger.debug(`Session ${data.id}: No conversation history to restore`);
          }
          return session;
        } catch (error) {
          throw new SessionPersistenceError(
            `Failed to deserialize session ${data.id}`,
            "deserialize",
            data.id,
            error
          );
        }
      }
    };
  }
});

// src/core/session/session-manager.ts
var import_crypto2, SessionManager;
var init_session_manager = __esm({
  "src/core/session/session-manager.ts"() {
    init_cjs_shims();
    import_crypto2 = require("crypto");
    init_coversation_session();
    init_logger2();
    init_event_types();
    init_manager3();
    init_persistence_types();
    init_persistence_types();
    SessionManager = class {
      constructor(services, config2 = {}) {
        this.services = services;
        this.maxSessions = config2.maxSessions ?? 100;
        this.sessionTTL = config2.sessionTTL ?? 24 * 36e5;
        this.persistenceConfig = {
          storageKeyPrefix: SESSION_PERSISTENCE_CONSTANTS.DEFAULT_STORAGE_PREFIX,
          maxSessionsToSave: SESSION_PERSISTENCE_CONSTANTS.DEFAULT_MAX_SESSIONS,
          maxSessionAge: SESSION_PERSISTENCE_CONSTANTS.DEFAULT_MAX_AGE,
          compress: false,
          saveTimeout: SESSION_PERSISTENCE_CONSTANTS.DEFAULT_SAVE_TIMEOUT,
          loadTimeout: SESSION_PERSISTENCE_CONSTANTS.DEFAULT_LOAD_TIMEOUT,
          validateOnRestore: true,
          ...config2.persistence
        };
      }
      sessions = /* @__PURE__ */ new Map();
      maxSessions;
      sessionTTL;
      initialized = false;
      cleanupInterval;
      initializationPromise;
      pendingCreations = /* @__PURE__ */ new Map();
      // Performance optimizations
      sessionMetadataCache = /* @__PURE__ */ new Map();
      messageCountCache = /* @__PURE__ */ new Map();
      requestDeduplicator = /* @__PURE__ */ new Map();
      CACHE_TTL = 3e4;
      // 30 seconds
      BATCH_SIZE = 10;
      // Parallel processing batch size
      performanceMetrics = {
        cacheHits: 0,
        cacheMisses: 0,
        parallelLoads: 0,
        sequentialLoads: 0,
        averageLoadTime: 0
      };
      // Persistence-related fields
      persistenceConfig;
      storageManager;
      async init() {
        if (this.initialized) {
          return;
        }
        this.initialized = true;
        logger.debug("SessionManager: Starting initialization...");
        await this.initializePersistenceStorage();
        const restorationStats = await this.loadAllSessions();
        if (restorationStats.totalSessions > 0) {
          logger.info(
            `SessionManager: Initialization complete. Loaded ${restorationStats.restoredSessions}/${restorationStats.totalSessions} sessions from storage. Current in-memory sessions: ${this.sessions.size}`
          );
        } else {
          logger.debug(
            `SessionManager: Initialization complete. No sessions found in storage. Current in-memory sessions: ${this.sessions.size}`
          );
        }
        this.startCleanupInterval();
        logger.debug(
          `SessionManager initialized with max sessions: ${this.maxSessions}, TTL: ${this.sessionTTL}ms`
        );
      }
      async ensureInitialized() {
        if (!this.initialized) {
          if (!this.initializationPromise) {
            logger.debug("SessionManager: Starting initialization...");
            this.initializationPromise = this.init();
          }
          try {
            await this.initializationPromise;
            logger.debug("SessionManager: Initialization completed successfully");
          } catch (error) {
            logger.error("SessionManager: Initialization failed:", error);
            throw error;
          }
        }
      }
      async createSession(sessionId) {
        await this.ensureInitialized();
        const id = sessionId ?? (0, import_crypto2.randomUUID)();
        if (this.pendingCreations.has(id)) {
          return await this.pendingCreations.get(id);
        }
        if (this.sessions.has(id)) {
          await this.updateSessionActivity(id);
          return this.sessions.get(id).session;
        }
        const creationPromise = this.createSessionInternal(id);
        this.pendingCreations.set(id, creationPromise);
        try {
          const session = await creationPromise;
          return session;
        } finally {
          this.pendingCreations.delete(id);
        }
      }
      async createSessionInternal(sessionId) {
        logger.debug(`SessionManager: Creating new session ${sessionId}...`);
        if (this.sessions.size >= this.maxSessions) {
          logger.warn(
            `SessionManager: Reached max sessions limit (${this.maxSessions}), evicting oldest session...`
          );
          await this.evictOldestSession();
        }
        if (this.sessions.size > 0) {
          await new Promise((resolve3) => setTimeout(resolve3, 50));
        }
        logger.debug(
          `SessionManager: Creating session ${sessionId} with storage manager: ${this.storageManager ? "available" : "undefined"}`
        );
        const session = new ConversationSession(
          {
            ...this.services,
            contextManager: this.services.contextManager
          },
          sessionId,
          {
            ...this.storageManager && { sharedStorageManager: this.storageManager }
          }
        );
        await session.init();
        try {
          const historyProvider = await session.getHistoryProvider();
          if (historyProvider) {
            logger.debug(`SessionManager: History provider initialized for new session ${sessionId}`);
          }
          await session.refreshConversationHistory();
          logger.debug(`SessionManager: Initialized conversation history for new session ${sessionId}`);
          const contextHistory = session.getContextHistory();
          const history = await session.getConversationHistory();
          logger.debug(
            `SessionManager: New session ${sessionId} - Context: ${contextHistory.length} messages, History: ${history.length} messages`
          );
        } catch (error) {
          logger.warn(
            `SessionManager: Failed to initialize conversation history for new session ${sessionId}:`,
            error
          );
        }
        const now = Date.now();
        this.sessions.set(sessionId, {
          session,
          lastActivity: now,
          createdAt: now
        });
        this.services.eventManager.emitSessionEvent(sessionId, SessionEvents.SESSION_CREATED, {
          sessionId,
          timestamp: now
        });
        try {
          await this.saveSession(sessionId, session);
          logger.info(
            `SessionManager: Session ${sessionId} created and saved to persistent storage. Total active sessions: ${this.sessions.size}`
          );
        } catch (error) {
          logger.warn(
            `SessionManager: Failed to save session ${sessionId} to persistent storage:`,
            error
          );
        }
        return session;
      }
      async updateSessionActivity(sessionId) {
        const sessionMetadata = this.sessions.get(sessionId);
        if (sessionMetadata) {
          const now = Date.now();
          sessionMetadata.lastActivity = now;
          this.services.eventManager.emitSessionEvent(sessionId, SessionEvents.SESSION_ACTIVATED, {
            sessionId,
            timestamp: now
          });
          try {
            await this.saveSession(sessionId, sessionMetadata.session);
            logger.debug(`Updated activity for session: ${sessionId}`);
          } catch (error) {
            logger.warn(`Failed to save updated session ${sessionId} to persistent storage:`, error);
          }
        }
      }
      async getSession(sessionId) {
        await this.ensureInitialized();
        const sessionMetadata = this.sessions.get(sessionId);
        if (!sessionMetadata) {
          logger.debug(
            `SessionManager: Session ${sessionId} not found in memory (${this.sessions.size} active sessions), attempting to restore from persistent storage...`
          );
          try {
            const restoredSession = await this.loadSession(sessionId);
            if (restoredSession) {
              logger.info(
                `SessionManager: Successfully restored session ${sessionId} from persistent storage. Total active sessions: ${this.sessions.size}`
              );
              return restoredSession;
            } else {
              logger.debug(
                `SessionManager: Session ${sessionId} not found in persistent storage either`
              );
            }
          } catch (error) {
            logger.warn(
              `SessionManager: Failed to restore session ${sessionId} from persistent storage:`,
              error
            );
          }
          return null;
        }
        if (this.isSessionExpired(sessionMetadata)) {
          logger.debug(`SessionManager: Session ${sessionId} has expired, removing...`);
          await this.removeSession(sessionId);
          return null;
        }
        await this.updateSessionActivity(sessionId);
        logger.debug(
          `SessionManager: Retrieved active session ${sessionId}. Total active sessions: ${this.sessions.size}`
        );
        return sessionMetadata.session;
      }
      async removeSession(sessionId) {
        await this.ensureInitialized();
        if (!sessionId || sessionId === "null" || sessionId === "undefined") {
          logger.debug(`SessionManager: Cannot remove session with invalid ID: ${sessionId}`);
          return false;
        }
        const sessionMetadata = this.sessions.get(sessionId);
        if (!sessionMetadata && !this.storageManager?.isConnected()) {
          logger.debug(
            `SessionManager: Session ${sessionId} not found in memory and storage unavailable`
          );
          return false;
        }
        let memoryDeleted = false;
        let storageDeleted = false;
        const deletionErrors = [];
        try {
          memoryDeleted = this.sessions.delete(sessionId);
          logger.debug(`SessionManager: Memory deletion for ${sessionId}: ${memoryDeleted}`);
          if (this.storageManager?.isConnected()) {
            try {
              const backends = this.storageManager.getBackends();
              if (backends?.database) {
                const sessionKey = this.getSessionStorageKey(sessionId);
                const historyKey = `messages:${sessionId}`;
                try {
                  await backends.database.delete(sessionKey);
                  logger.debug(`SessionManager: Deleted session data ${sessionKey}`);
                } catch (sessionDeleteError) {
                  deletionErrors.push(`Session data: ${sessionDeleteError}`);
                }
                try {
                  await backends.database.delete(historyKey);
                  logger.debug(`SessionManager: Deleted conversation history ${historyKey}`);
                } catch (historyDeleteError) {
                  deletionErrors.push(`History data: ${historyDeleteError}`);
                }
                storageDeleted = true;
              }
            } catch (storageError) {
              deletionErrors.push(`Storage operation: ${storageError}`);
              logger.warn(`SessionManager: Storage deletion failed for ${sessionId}:`, storageError);
            }
          } else {
            logger.debug(`SessionManager: Storage manager not available for deletion of ${sessionId}`);
            storageDeleted = true;
          }
          this.clearSessionFromAllCaches(sessionId);
          if (memoryDeleted || storageDeleted) {
            this.services.eventManager.emitSessionEvent(sessionId, SessionEvents.SESSION_DELETED, {
              sessionId,
              timestamp: Date.now()
            });
            if (deletionErrors.length > 0) {
              logger.warn(
                `SessionManager: Session ${sessionId} partially deleted with errors: ${deletionErrors.join("; ")}. Remaining active sessions: ${this.sessions.size}`
              );
            } else {
              logger.info(
                `SessionManager: Successfully deleted session ${sessionId} from memory and storage. Remaining active sessions: ${this.sessions.size}`
              );
            }
            return true;
          }
          logger.warn(
            `SessionManager: Failed to delete session ${sessionId} - not found in memory or storage`
          );
          return false;
        } catch (error) {
          logger.error(
            `SessionManager: Critical error during session deletion for ${sessionId}:`,
            error
          );
          return memoryDeleted;
        }
      }
      async getAllSessions() {
        await this.ensureInitialized();
        return Array.from(this.sessions.values()).map((metadata) => metadata.session);
      }
      /**
       * Optimized session listing with parallel processing and caching
       */
      async getActiveSessionIds() {
        await this.ensureInitialized();
        const startTime = Date.now();
        const cacheKey = "active_session_ids";
        if (this.requestDeduplicator.has(cacheKey)) {
          logger.debug("SessionManager: Deduplicating concurrent request for active session IDs");
          return await this.requestDeduplicator.get(cacheKey);
        }
        try {
          this.performanceMetrics.cacheMisses++;
          const fetchPromise = this.fetchActiveSessionIds();
          this.requestDeduplicator.set(cacheKey, fetchPromise);
          const result = await fetchPromise;
          const validSessions = await this.filterValidSessions(result);
          const loadTime = Date.now() - startTime;
          this.updateAverageLoadTime(loadTime);
          logger.debug(
            `SessionManager: Fetched ${validSessions.length}/${result.length} valid session IDs in ${loadTime}ms`
          );
          return validSessions;
        } catch (error) {
          logger.error("SessionManager: Failed to get active session IDs:", error);
          const inMemorySessionIds = Array.from(this.sessions.keys());
          logger.debug(
            `SessionManager: Returning ${inMemorySessionIds.length} in-memory sessions as fallback`
          );
          return inMemorySessionIds;
        } finally {
          this.requestDeduplicator.delete(cacheKey);
        }
      }
      /**
       * Internal method to fetch active session IDs with optimized queries
       */
      async fetchActiveSessionIds() {
        await this.cleanupExpiredSessions();
        const inMemorySessionIds = Array.from(this.sessions.keys());
        logger.debug(`SessionManager: Found ${inMemorySessionIds.length} in-memory sessions`);
        let persistedSessionIds = [];
        try {
          const [sessionKeys, messageKeys] = await Promise.allSettled([
            this.getAllSessionKeys(),
            this.getAllMessageKeys()
          ]);
          const sessionIdsFromKeys = sessionKeys.status === "fulfilled" ? sessionKeys.value.map((key) => this.extractSessionIdFromKey(key)) : [];
          const sessionIdsFromMessages = messageKeys.status === "fulfilled" ? messageKeys.value.map((key) => key.replace("messages:", "")) : [];
          persistedSessionIds = [.../* @__PURE__ */ new Set([...sessionIdsFromKeys, ...sessionIdsFromMessages])];
          logger.debug(`SessionManager: Found ${persistedSessionIds.length} persisted sessions`);
        } catch (storageError) {
          logger.warn("SessionManager: Failed to get persisted sessions from storage:", storageError);
        }
        const allSessionIds = [.../* @__PURE__ */ new Set([...inMemorySessionIds, ...persistedSessionIds])];
        logger.debug(`SessionManager: Combined ${allSessionIds.length} unique session IDs`);
        return allSessionIds;
      }
      async getSessionCount() {
        await this.ensureInitialized();
        return this.sessions.size;
      }
      /**
       * Get detailed session statistics for monitoring
       */
      async getSessionStats() {
        await this.ensureInitialized();
        const backends = this.storageManager?.getBackends();
        const storageType = backends?.database?.getBackendType?.() || "none";
        const totalRequests = this.performanceMetrics.cacheHits + this.performanceMetrics.cacheMisses;
        const cacheHitRate = totalRequests > 0 ? this.performanceMetrics.cacheHits / totalRequests * 100 : 0;
        const totalLoads = this.performanceMetrics.parallelLoads + this.performanceMetrics.sequentialLoads;
        const parallelLoadRatio = totalLoads > 0 ? this.performanceMetrics.parallelLoads / totalLoads * 100 : 0;
        return {
          activeSessions: this.sessions.size,
          storageConnected: this.storageManager?.isConnected() || false,
          storageType,
          persistenceEnabled: !!this.storageManager,
          performanceMetrics: {
            cacheHitRate: Math.round(cacheHitRate * 100) / 100,
            parallelLoadRatio: Math.round(parallelLoadRatio * 100) / 100,
            averageLoadTime: Math.round(this.performanceMetrics.averageLoadTime * 100) / 100,
            cacheSize: this.sessionMetadataCache.size + this.messageCountCache.size
          }
        };
      }
      isSessionExpired(sessionMetadata) {
        const now = Date.now();
        return now - sessionMetadata.lastActivity > this.sessionTTL;
      }
      async evictOldestSession() {
        if (this.sessions.size === 0) {
          return;
        }
        let oldestSessionId = null;
        let oldestActivity = Number.MAX_SAFE_INTEGER;
        for (const [sessionId, metadata] of this.sessions) {
          if (metadata.lastActivity < oldestActivity) {
            oldestActivity = metadata.lastActivity;
            oldestSessionId = sessionId;
          }
        }
        if (oldestSessionId) {
          await this.removeSession(oldestSessionId);
          logger.debug(`Evicted oldest session: ${oldestSessionId}`);
        }
      }
      async cleanupExpiredSessions() {
        const expiredSessionIds = [];
        for (const [sessionId, metadata] of this.sessions) {
          if (this.isSessionExpired(metadata)) {
            expiredSessionIds.push(sessionId);
          }
        }
        if (expiredSessionIds.length > 0) {
          logger.info(`SessionManager: Cleaning up ${expiredSessionIds.length} expired sessions...`);
          for (const sessionId of expiredSessionIds) {
            this.services.eventManager.emitSessionEvent(sessionId, SessionEvents.SESSION_EXPIRED, {
              sessionId,
              timestamp: Date.now()
            });
            await this.removeSession(sessionId);
          }
          logger.info(
            `SessionManager: Cleanup complete. Remaining active sessions: ${this.sessions.size}`
          );
        }
      }
      startCleanupInterval() {
        const cleanupIntervalMs = 5 * 60 * 1e3;
        if (this.sessionTTL > 0) {
          this.cleanupInterval = setInterval(async () => {
            try {
              await this.cleanupExpiredSessions();
              this.cleanupExpiredCache();
            } catch (error) {
              logger.error("Error during session cleanup:", error);
            }
          }, cleanupIntervalMs);
        } else {
          logger.debug("Session cleanup disabled (sessionTTL = 0)");
        }
      }
      /**
       * Save all active sessions to persistent storage
       * @returns Statistics about the save operation
       */
      async saveAllSessions() {
        await this.ensureInitialized();
        const startTime = Date.now();
        const stats = {
          totalSessions: 0,
          savedSessions: 0,
          failedSessions: 0,
          persistenceTime: 0,
          failedSessionIds: [],
          errors: []
        };
        if (!this.storageManager) {
          const error = "No storage manager available for session persistence";
          stats.errors.push(error);
          logger.warn(`SessionManager.saveAllSessions: ${error}`);
          return stats;
        }
        if (!this.storageManager.isConnected()) {
          const error = "Storage manager is not connected";
          stats.errors.push(error);
          logger.warn(`SessionManager.saveAllSessions: ${error}`);
          return stats;
        }
        try {
          logger.info(
            `SessionManager: Starting to save ${this.sessions.size} sessions to persistent storage...`
          );
          const activeSessions = Array.from(this.sessions.entries());
          stats.totalSessions = activeSessions.length;
          if (activeSessions.length === 0) {
            logger.debug("SessionManager.saveAllSessions: No active sessions to save");
            stats.persistenceTime = Date.now() - startTime;
            return stats;
          }
          const saveTimeout = this.persistenceConfig.saveTimeout;
          const maxSessionsToSave = this.persistenceConfig.maxSessionsToSave;
          const savePromises = activeSessions.slice(0, maxSessionsToSave).map(async ([sessionId, metadata]) => {
            try {
              const savePromise = this.saveSession(sessionId, metadata.session);
              await Promise.race([
                savePromise,
                new Promise(
                  (_, reject) => setTimeout(() => reject(new Error("Save timeout")), saveTimeout)
                )
              ]);
              stats.savedSessions++;
              logger.debug(`SessionManager: Successfully saved session ${sessionId}`);
            } catch (error) {
              stats.failedSessions++;
              stats.failedSessionIds.push(sessionId);
              const errorMsg = error instanceof Error ? error.message : String(error);
              stats.errors.push(`Session ${sessionId}: ${errorMsg}`);
              logger.error(`SessionManager: Failed to save session ${sessionId}:`, error);
            }
          });
          await Promise.allSettled(savePromises);
        } catch (error) {
          const errorMsg = error instanceof Error ? error.message : String(error);
          stats.errors.push(`Save operation failed: ${errorMsg}`);
          logger.error("SessionManager: Failed to save sessions:", error);
        }
        stats.persistenceTime = Date.now() - startTime;
        logger.info(`SessionManager: Session persistence completed`, {
          totalSessions: stats.totalSessions,
          savedSessions: stats.savedSessions,
          failedSessions: stats.failedSessions,
          persistenceTime: stats.persistenceTime
        });
        return stats;
      }
      /**
       * Load all sessions from persistent storage
       * @returns Statistics about the load operation
       */
      async loadAllSessions() {
        await this.ensureInitialized();
        const startTime = Date.now();
        const stats = {
          totalSessions: 0,
          restoredSessions: 0,
          failedSessions: 0,
          restorationTime: 0,
          failedSessionIds: [],
          errors: []
        };
        if (!this.storageManager) {
          const error = "No storage manager available for session persistence";
          stats.errors.push(error);
          logger.debug(`SessionManager.loadAllSessions: ${error}`);
          return stats;
        }
        if (!this.storageManager.isConnected()) {
          const error = "Storage manager is not connected";
          stats.errors.push(error);
          logger.warn(`SessionManager.loadAllSessions: ${error}`);
          return stats;
        }
        try {
          const backends = this.storageManager.getBackends();
          if (!backends) {
            throw new SessionPersistenceError("No storage backends available", "load");
          }
          const sessionKeys = await this.getAllSessionKeys();
          stats.totalSessions = sessionKeys.length;
          if (sessionKeys.length === 0) {
            logger.debug("SessionManager.loadAllSessions: No sessions found in storage");
            stats.restorationTime = Date.now() - startTime;
            return stats;
          }
          const loadTimeout = this.persistenceConfig.loadTimeout;
          const maxAge = this.persistenceConfig.maxSessionAge;
          const now = Date.now();
          const loadPromises = sessionKeys.map(async (key) => {
            try {
              const sessionId = this.extractSessionIdFromKey(key);
              const loadPromise = this.loadSession(sessionId);
              const session = await Promise.race([
                loadPromise,
                new Promise(
                  (_, reject) => setTimeout(() => reject(new Error("Load timeout")), loadTimeout)
                )
              ]);
              if (session) {
                const sessionAge = now - (this.sessions.get(sessionId)?.createdAt || now);
                if (sessionAge > maxAge) {
                  logger.debug(
                    `SessionManager: Session ${sessionId} is too old (${sessionAge}ms), skipping`
                  );
                  return;
                }
                stats.restoredSessions++;
                logger.debug(`SessionManager: Successfully restored session ${sessionId}`);
              }
            } catch (error) {
              const sessionId = this.extractSessionIdFromKey(key);
              stats.failedSessions++;
              stats.failedSessionIds.push(sessionId);
              const errorMsg = error instanceof Error ? error.message : String(error);
              stats.errors.push(`Session ${sessionId}: ${errorMsg}`);
              logger.error(`SessionManager: Failed to restore session ${sessionId}:`, error);
            }
          });
          await Promise.allSettled(loadPromises);
        } catch (error) {
          const errorMsg = error instanceof Error ? error.message : String(error);
          stats.errors.push(`Load operation failed: ${errorMsg}`);
          logger.error("SessionManager: Failed to load sessions:", error);
        }
        stats.restorationTime = Date.now() - startTime;
        if (stats.failedSessions > 0 || stats.errors.length > 0) {
          logger.warn(`SessionManager: Session restoration completed with issues`, {
            totalSessions: stats.totalSessions,
            restoredSessions: stats.restoredSessions,
            failedSessions: stats.failedSessions,
            restorationTime: stats.restorationTime
          });
        } else if (stats.totalSessions > 0) {
          logger.debug(`SessionManager: Session restoration completed`, {
            totalSessions: stats.totalSessions,
            restoredSessions: stats.restoredSessions,
            restorationTime: stats.restorationTime
          });
        }
        return stats;
      }
      /**
       * Initialize storage manager for session persistence
       */
      async initializePersistenceStorage() {
        logger.info("SessionManager: Initializing persistence storage...");
        const postgresUrl = process.env.CIPHER_PG_URL;
        const postgresHost = process.env.STORAGE_DATABASE_HOST;
        const postgresDatabase = process.env.STORAGE_DATABASE_NAME;
        if (postgresUrl || postgresHost && postgresDatabase) {
          try {
            logger.debug("SessionManager: Attempting to initialize PostgreSQL storage...");
            let postgresConfig = {
              type: "postgres"
            };
            if (postgresUrl) {
              postgresConfig.url = postgresUrl;
              logger.debug("SessionManager: Using PostgreSQL connection URL");
            } else {
              postgresConfig.host = postgresHost;
              postgresConfig.database = postgresDatabase;
              postgresConfig.port = process.env.STORAGE_DATABASE_PORT ? parseInt(process.env.STORAGE_DATABASE_PORT, 10) : 5432;
              postgresConfig.user = process.env.STORAGE_DATABASE_USER;
              postgresConfig.password = process.env.STORAGE_DATABASE_PASSWORD;
              postgresConfig.ssl = process.env.STORAGE_DATABASE_SSL === "true";
              logger.debug("SessionManager: Using PostgreSQL individual parameters");
            }
            this.storageManager = new StorageManager({
              database: postgresConfig,
              cache: {
                type: "in-memory"
              }
            });
            await this.storageManager.connect();
            logger.info("SessionManager: PostgreSQL persistence storage initialized successfully");
          } catch (postgresError) {
            logger.warn(
              "SessionManager: PostgreSQL failed, falling back to SQLite storage",
              postgresError
            );
            await this.initializeSqliteStorage();
          }
        } else {
          await this.initializeSqliteStorage();
        }
        if (this.storageManager) {
          const backends = this.storageManager.getBackends();
          logger.info(
            `SessionManager: Storage initialized with database backend: ${backends?.database?.getBackendType?.() || "unknown"}`
          );
        } else {
          logger.warn("SessionManager: No storage manager available - sessions will not persist");
        }
      }
      /**
       * Initialize SQLite storage as fallback
       */
      async initializeSqliteStorage() {
        try {
          logger.debug("SessionManager: Attempting to initialize SQLite storage...");
          this.storageManager = new StorageManager({
            database: {
              type: "sqlite",
              path: "./data",
              database: "cipher-sessions.db"
            },
            cache: {
              type: "in-memory"
            }
          });
          await this.storageManager.connect();
          logger.info("SessionManager: SQLite persistence storage initialized successfully");
        } catch (sqliteError) {
          logger.warn(
            "SessionManager: SQLite failed, falling back to in-memory storage for sessions",
            sqliteError
          );
          try {
            logger.debug("SessionManager: Attempting to initialize in-memory storage...");
            this.storageManager = new StorageManager({
              database: {
                type: "in-memory"
              },
              cache: {
                type: "in-memory"
              }
            });
            await this.storageManager.connect();
            logger.warn(
              "SessionManager: In-memory persistence storage initialized (sessions will not persist across restarts)"
            );
          } catch (fallbackError) {
            logger.error(
              "SessionManager: Failed to initialize any persistence storage, continuing without session persistence",
              fallbackError
            );
            this.storageManager = void 0;
          }
        }
      }
      /**
       * Save a single session to storage
       */
      async saveSession(sessionId, session) {
        if (!this.storageManager?.isConnected()) {
          throw new SessionPersistenceError("Storage manager not connected", "save", sessionId);
        }
        const serialized = await session.serialize();
        if (serialized.conversationHistory.length === 0) {
          logger.debug(`SessionManager: Skipping save of session ${sessionId} with 0 messages`);
          return;
        }
        const key = this.getSessionStorageKey(sessionId);
        const backends = this.storageManager.getBackends();
        if (!backends) {
          throw new SessionPersistenceError("No storage backends available", "save", sessionId);
        }
        await backends.database.set(key, serialized);
        logger.debug(
          `SessionManager: Saved session ${sessionId} to persistent storage with ${serialized.conversationHistory.length} messages`
        );
      }
      /**
       * Load a single session from storage
       */
      async loadSession(sessionId) {
        if (!sessionId || sessionId === "null" || sessionId === "undefined") {
          logger.debug(`SessionManager: Invalid session ID provided: ${sessionId}`);
          return null;
        }
        if (!this.storageManager?.isConnected()) {
          throw new SessionPersistenceError("Storage manager not connected", "load", sessionId);
        }
        const key = this.getSessionStorageKey(sessionId);
        const backends = this.storageManager.getBackends();
        if (!backends) {
          throw new SessionPersistenceError("No storage backends available", "load", sessionId);
        }
        const serialized = await backends.database.get(key);
        if (!serialized) {
          logger.debug(`SessionManager: No serialized data found for session ${sessionId}`);
          return null;
        }
        if (this.persistenceConfig.validateOnRestore) {
          if (!this.validateSerializedSession(serialized)) {
            throw new SessionPersistenceError("Invalid serialized session data", "load", sessionId);
          }
        }
        const session = await ConversationSession.deserialize(serialized, this.services);
        this.sessions.set(sessionId, {
          session,
          lastActivity: serialized.metadata.lastActivity,
          createdAt: serialized.metadata.createdAt
        });
        try {
          await session.refreshConversationHistory();
          logger.debug(`SessionManager: Refreshed conversation history for session ${sessionId}`);
          const contextHistory = session.getContextHistory();
          logger.debug(
            `SessionManager: Context manager for session ${sessionId} has ${contextHistory.length} messages`
          );
          const history = await session.getConversationHistory();
          logger.debug(
            `SessionManager: History provider for session ${sessionId} has ${history.length} messages`
          );
        } catch (error) {
          logger.warn(
            `SessionManager: Failed to refresh conversation history for session ${sessionId}:`,
            error
          );
        }
        logger.debug(
          `SessionManager: Loaded session ${sessionId} from persistent storage with ${serialized.conversationHistory.length} messages`
        );
        return session;
      }
      /**
       * Get all session keys from storage with optimized batch processing
       */
      async getAllSessionKeys() {
        if (!this.storageManager?.isConnected()) {
          logger.warn("SessionManager: Storage manager not connected, cannot get session keys");
          return [];
        }
        const backends = this.storageManager.getBackends();
        if (!backends) {
          logger.warn("SessionManager: No storage backends available");
          return [];
        }
        try {
          const prefix = this.persistenceConfig.storageKeyPrefix;
          logger.debug(`SessionManager: Searching for session keys with prefix '${prefix}'`);
          const sessionKeys = await backends.database.list(prefix);
          logger.debug(
            `SessionManager: Found ${sessionKeys.length} session keys in storage with prefix '${prefix}'`
          );
          return sessionKeys;
        } catch (error) {
          logger.warn("SessionManager: Failed to get session keys from storage:", error);
          return [];
        }
      }
      /**
       * Get all message keys from storage for session discovery
       */
      async getAllMessageKeys() {
        if (!this.storageManager?.isConnected()) {
          return [];
        }
        const backends = this.storageManager.getBackends();
        if (!backends) {
          return [];
        }
        try {
          const messageKeys = await backends.database.list("messages:");
          logger.debug(`SessionManager: Found ${messageKeys.length} message keys in storage`);
          return messageKeys;
        } catch (error) {
          logger.warn("SessionManager: Failed to get message keys from storage:", error);
          return [];
        }
      }
      /**
       * Get the storage key for a session
       */
      getSessionStorageKey(sessionId) {
        return `${this.persistenceConfig.storageKeyPrefix}${sessionId}`;
      }
      /**
       * Extract session ID from storage key
       */
      extractSessionIdFromKey(key) {
        const prefix = this.persistenceConfig.storageKeyPrefix;
        return key.startsWith(prefix) ? key.substring(prefix.length) : key;
      }
      /**
       * Validate a serialized session object
       */
      validateSerializedSession(data) {
        try {
          return typeof data.id === "string" && typeof data.version === "string" && typeof data.serializedAt === "number" && data.metadata && typeof data.metadata.createdAt === "number" && typeof data.metadata.lastActivity === "number" && Array.isArray(data.conversationHistory);
        } catch {
          return false;
        }
      }
      async shutdown() {
        logger.info(
          `SessionManager: Starting shutdown process for ${this.sessions.size} active sessions...`
        );
        try {
          const saveStats = await this.saveAllSessions();
          logger.info(
            `SessionManager: Shutdown save completed - ${saveStats.savedSessions}/${saveStats.totalSessions} sessions saved`
          );
        } catch (error) {
          logger.error("SessionManager: Failed to save sessions during shutdown:", error);
        }
        if (this.cleanupInterval) {
          clearInterval(this.cleanupInterval);
          this.cleanupInterval = void 0;
        }
        this.sessionMetadataCache.clear();
        this.messageCountCache.clear();
        this.requestDeduplicator.clear();
        logger.info("SessionManager: Final performance metrics:", this.performanceMetrics);
        if (this.storageManager) {
          try {
            await this.storageManager.disconnect();
            logger.debug("SessionManager: Storage manager disconnected");
          } catch (error) {
            logger.warn("SessionManager: Failed to disconnect storage manager:", error);
          }
        }
        const sessionCount = this.sessions.size;
        this.sessions.clear();
        logger.info(`SessionManager: Shutdown complete. Cleared ${sessionCount} in-memory sessions`);
      }
      /**
       * Get the storageManager for a given session (if available)
       */
      getStorageManagerForSession(sessionId) {
        const sessionMetadata = this.sessions.get(sessionId);
        if (sessionMetadata && sessionMetadata.session && typeof sessionMetadata.session.getStorageManager === "function") {
          return sessionMetadata.session.getStorageManager();
        }
        return void 0;
      }
      /**
       * Performance optimization: Cache management methods
       */
      getCachedResult(key) {
        const now = Date.now();
        const metadataEntry = this.sessionMetadataCache.get(key);
        if (metadataEntry && now < metadataEntry.expiresAt) {
          return metadataEntry.metadata;
        }
        const countEntry = this.messageCountCache.get(key);
        if (countEntry && now < countEntry.expiresAt) {
          return countEntry.count;
        }
        return null;
      }
      setCacheResult(key, value) {
        const now = Date.now();
        const expiresAt = now + this.CACHE_TTL;
        if (Array.isArray(value)) {
          this.sessionMetadataCache.set(key, {
            metadata: value,
            cachedAt: now,
            expiresAt
          });
        } else if (typeof value === "number") {
          this.messageCountCache.set(key, {
            count: value,
            cachedAt: now,
            expiresAt
          });
        }
      }
      cleanupExpiredCache() {
        const now = Date.now();
        for (const [key, entry] of this.sessionMetadataCache.entries()) {
          if (now >= entry.expiresAt) {
            this.sessionMetadataCache.delete(key);
          }
        }
        for (const [key, entry] of this.messageCountCache.entries()) {
          if (now >= entry.expiresAt) {
            this.messageCountCache.delete(key);
          }
        }
      }
      updateAverageLoadTime(loadTime) {
        const alpha = 0.1;
        this.performanceMetrics.averageLoadTime = this.performanceMetrics.averageLoadTime === 0 ? loadTime : alpha * loadTime + (1 - alpha) * this.performanceMetrics.averageLoadTime;
      }
      /**
       * Clear session from all caches to prevent phantom sessions
       */
      clearSessionFromAllCaches(sessionId) {
        this.sessionMetadataCache.delete(`metadata_${sessionId}`);
        this.messageCountCache.delete(`count_${sessionId}`);
        this.sessionMetadataCache.delete("active_session_ids");
        logger.debug(`SessionManager: Cleared all caches for session ${sessionId}`);
      }
      /**
       * Filter out invalid sessions (empty sessions with 0 messages)
       */
      async filterValidSessions(sessionIds) {
        const validSessions = [];
        for (const sessionId of sessionIds) {
          try {
            const isInMemory = this.sessions.has(sessionId);
            const messageCount = await this.getSessionMessageCount(sessionId);
            if (isInMemory || messageCount > 0) {
              validSessions.push(sessionId);
            } else {
              logger.debug(`SessionManager: Cleaning up phantom session ${sessionId} with 0 messages`);
              await this.cleanupPhantomSession(sessionId);
            }
          } catch (error) {
            logger.warn(`SessionManager: Error validating session ${sessionId}:`, error);
            validSessions.push(sessionId);
          }
        }
        return validSessions;
      }
      /**
       * Clean up phantom sessions that have no messages
       */
      async cleanupPhantomSession(sessionId) {
        try {
          if (this.storageManager?.isConnected()) {
            const backends = this.storageManager.getBackends();
            if (backends?.database) {
              const sessionKey = this.getSessionStorageKey(sessionId);
              const historyKey = `messages:${sessionId}`;
              await Promise.allSettled([
                backends.database.delete(sessionKey),
                backends.database.delete(historyKey)
              ]);
              logger.debug(`SessionManager: Cleaned up phantom session ${sessionId}`);
            }
          }
        } catch (error) {
          logger.warn(`SessionManager: Failed to cleanup phantom session ${sessionId}:`, error);
        }
      }
      /**
       * Get batch session metadata with parallel processing and caching
       */
      async getBatchSessionMetadata(sessionIds) {
        const startTime = Date.now();
        const result = /* @__PURE__ */ new Map();
        const batches = [];
        for (let i = 0; i < sessionIds.length; i += this.BATCH_SIZE) {
          batches.push(sessionIds.slice(i, i + this.BATCH_SIZE));
        }
        for (const batch of batches) {
          const batchPromises = batch.map(async (sessionId) => {
            try {
              const cacheKey = `metadata_${sessionId}`;
              const cached = this.getCachedResult(cacheKey);
              if (cached) {
                this.performanceMetrics.cacheHits++;
                return { sessionId, metadata: cached };
              }
              this.performanceMetrics.cacheMisses++;
              let metadata = null;
              const sessionMetadata = this.sessions.get(sessionId);
              if (sessionMetadata) {
                metadata = {
                  id: sessionId,
                  messageCount: await this.getSessionMessageCount(sessionId),
                  createdAt: sessionMetadata.createdAt,
                  lastActivity: sessionMetadata.lastActivity
                };
              } else {
                metadata = await this.getSessionMetadataFromStorage(sessionId);
              }
              if (metadata) {
                this.setCacheResult(cacheKey, metadata);
                return { sessionId, metadata };
              }
              return { sessionId, metadata: null };
            } catch (error) {
              logger.warn(`Failed to get metadata for session ${sessionId}:`, error);
              return { sessionId, metadata: null };
            }
          });
          const batchResults = await Promise.allSettled(batchPromises);
          this.performanceMetrics.parallelLoads += batchResults.length;
          for (const promiseResult of batchResults) {
            if (promiseResult.status === "fulfilled" && promiseResult.value.metadata) {
              result.set(promiseResult.value.sessionId, promiseResult.value.metadata);
            }
          }
        }
        const loadTime = Date.now() - startTime;
        this.updateAverageLoadTime(loadTime);
        logger.debug(
          `SessionManager: Batch loaded ${result.size}/${sessionIds.length} session metadata in ${loadTime}ms`
        );
        return result;
      }
      /**
       * Get session message count with caching
       */
      async getSessionMessageCount(sessionId) {
        const cacheKey = `count_${sessionId}`;
        const cached = this.getCachedResult(cacheKey);
        if (cached !== null) {
          this.performanceMetrics.cacheHits++;
          return cached;
        }
        this.performanceMetrics.cacheMisses++;
        try {
          const sessionMetadata = this.sessions.get(sessionId);
          if (sessionMetadata) {
            const history = await sessionMetadata.session.getConversationHistory();
            const filteredHistory = history.filter((msg) => {
              if (msg.role === "user") return true;
              if (msg.role === "assistant" && (!msg.toolCalls || msg.toolCalls.length === 0))
                return true;
              return false;
            });
            const count = filteredHistory.length;
            this.setCacheResult(cacheKey, count);
            return count;
          }
          if (this.storageManager?.isConnected()) {
            const backends = this.storageManager.getBackends();
            if (backends?.database) {
              const historyKey = `messages:${sessionId}`;
              const historyData = await backends.database.get(historyKey);
              if (historyData && Array.isArray(historyData)) {
                const filteredHistory = historyData.filter((msg) => {
                  if (msg.role === "user") return true;
                  if (msg.role === "assistant" && (!msg.toolCalls || msg.toolCalls.length === 0))
                    return true;
                  return false;
                });
                const count = filteredHistory.length;
                this.setCacheResult(cacheKey, count);
                return count;
              }
              const sessionKey = this.getSessionStorageKey(sessionId);
              const sessionData = await backends.database.get(sessionKey);
              if (sessionData?.conversationHistory?.length) {
                const filteredHistory = sessionData.conversationHistory.filter((msg) => {
                  if (msg.role === "user") return true;
                  if (msg.role === "assistant" && (!msg.toolCalls || msg.toolCalls.length === 0))
                    return true;
                  return false;
                });
                const count = filteredHistory.length;
                this.setCacheResult(cacheKey, count);
                return count;
              }
            }
          }
          this.setCacheResult(cacheKey, 0);
          return 0;
        } catch (error) {
          logger.warn(`Failed to get message count for session ${sessionId}:`, error);
          this.setCacheResult(cacheKey, 0);
          return 0;
        }
      }
      /**
       * Get session metadata from storage
       */
      async getSessionMetadataFromStorage(sessionId) {
        if (!this.storageManager?.isConnected()) {
          return null;
        }
        const backends = this.storageManager.getBackends();
        if (!backends?.database) {
          return null;
        }
        try {
          const [messageCount, sessionData] = await Promise.allSettled([
            this.getSessionMessageCount(sessionId),
            backends.database.get(this.getSessionStorageKey(sessionId))
          ]);
          const count = messageCount.status === "fulfilled" ? messageCount.value : 0;
          const data = sessionData.status === "fulfilled" ? sessionData.value : null;
          const now = Date.now();
          return {
            id: sessionId,
            messageCount: count,
            createdAt: data?.metadata?.createdAt || now - count * 6e4,
            // Estimate based on message count
            lastActivity: data?.metadata?.lastActivity || now
          };
        } catch (error) {
          logger.warn(`Failed to get session metadata from storage for ${sessionId}:`, error);
          return null;
        }
      }
    };
  }
});

// src/core/brain/tools/types.ts
function createInternalToolName(baseName) {
  return baseName.startsWith(INTERNAL_TOOL_PREFIX) ? baseName : `${INTERNAL_TOOL_PREFIX}${baseName}`;
}
function isInternalToolName(toolName) {
  return toolName.startsWith(INTERNAL_TOOL_PREFIX);
}
var INTERNAL_TOOL_PREFIX;
var init_types7 = __esm({
  "src/core/brain/tools/types.ts"() {
    init_cjs_shims();
    INTERNAL_TOOL_PREFIX = "cipher_";
  }
});

// src/core/brain/tools/registry.ts
var InternalToolRegistry;
var init_registry2 = __esm({
  "src/core/brain/tools/registry.ts"() {
    init_cjs_shims();
    init_logger2();
    init_types7();
    InternalToolRegistry = class _InternalToolRegistry {
      static instance = null;
      tools = /* @__PURE__ */ new Map();
      toolsByCategory = /* @__PURE__ */ new Map();
      initialized = false;
      constructor() {
        this.toolsByCategory.set("memory", /* @__PURE__ */ new Set());
        this.toolsByCategory.set("session", /* @__PURE__ */ new Set());
        this.toolsByCategory.set("system", /* @__PURE__ */ new Set());
      }
      /**
       * Get the singleton instance of the registry
       */
      static getInstance() {
        if (!_InternalToolRegistry.instance) {
          _InternalToolRegistry.instance = new _InternalToolRegistry();
        }
        return _InternalToolRegistry.instance;
      }
      /**
       * Initialize the registry
       */
      async initialize() {
        if (this.initialized) {
          logger.warn("InternalToolRegistry: Already initialized");
          return;
        }
        logger.info("InternalToolRegistry: Initializing...");
        this.initialized = true;
        logger.info("InternalToolRegistry: Initialized successfully");
      }
      /**
       * Register a new internal tool
       */
      registerTool(tool) {
        try {
          const validation = this.validateTool(tool);
          if (!validation.valid) {
            return {
              success: false,
              message: `Tool validation failed: ${validation.error}`
            };
          }
          const toolName = createInternalToolName(tool.name);
          if (this.tools.has(toolName)) {
            return {
              success: false,
              message: `Tool '${toolName}' is already registered`,
              conflictedWith: toolName
            };
          }
          const normalizedTool = {
            ...tool,
            name: toolName
          };
          this.tools.set(toolName, normalizedTool);
          const categorySet = this.toolsByCategory.get(tool.category);
          if (categorySet) {
            categorySet.add(toolName);
          }
          return {
            success: true,
            message: `Tool '${toolName}' registered successfully`
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`InternalToolRegistry: Failed to register tool '${tool.name}': ${errorMessage}`);
          return {
            success: false,
            message: `Registration failed: ${errorMessage}`
          };
        }
      }
      /**
       * Unregister an internal tool
       */
      unregisterTool(toolName) {
        try {
          const normalizedName = createInternalToolName(toolName);
          const tool = this.tools.get(normalizedName);
          if (!tool) {
            logger.warn(`InternalToolRegistry: Tool '${normalizedName}' not found for unregistration`);
            return false;
          }
          this.tools.delete(normalizedName);
          const categorySet = this.toolsByCategory.get(tool.category);
          if (categorySet) {
            categorySet.delete(normalizedName);
          }
          logger.debug(`InternalToolRegistry: Unregistered tool '${normalizedName}'`);
          return true;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(
            `InternalToolRegistry: Failed to unregister tool '${toolName}': ${errorMessage}`
          );
          return false;
        }
      }
      /**
       * Get a specific tool by name
       */
      getTool(toolName) {
        const normalizedName = createInternalToolName(toolName);
        return this.tools.get(normalizedName);
      }
      /**
       * Get all registered tools
       */
      getAllTools() {
        const toolSet = {};
        for (const [name, tool] of this.tools) {
          toolSet[name] = tool;
        }
        return toolSet;
      }
      /**
       * Get tools by category
       */
      getToolsByCategory(category) {
        const toolSet = {};
        const categorySet = this.toolsByCategory.get(category);
        if (categorySet) {
          for (const toolName of categorySet) {
            const tool = this.tools.get(toolName);
            if (tool) {
              toolSet[toolName] = tool;
            }
          }
        }
        return toolSet;
      }
      /**
       * Check if a tool is registered
       */
      hasTool(toolName) {
        const normalizedName = createInternalToolName(toolName);
        return this.tools.has(normalizedName);
      }
      /**
       * Check if a tool name is an internal tool
       */
      isInternalTool(toolName) {
        const normalizedName = createInternalToolName(toolName);
        return this.hasTool(normalizedName) || this.hasTool(toolName);
      }
      /**
       * Get the total number of registered tools
       */
      getToolCount() {
        return this.tools.size;
      }
      /**
       * Get tool count by category
       */
      getToolCountByCategory() {
        const memoryCount = this.toolsByCategory.get("memory")?.size || 0;
        const sessionCount = this.toolsByCategory.get("session")?.size || 0;
        const systemCount = this.toolsByCategory.get("system")?.size || 0;
        const knowledgeGraphCount = this.toolsByCategory.get("knowledge_graph")?.size || 0;
        return {
          memory: memoryCount,
          session: sessionCount,
          system: systemCount,
          knowledge_graph: knowledgeGraphCount
        };
      }
      /**
       * Get all tool names
       */
      getToolNames() {
        return Array.from(this.tools.keys());
      }
      /**
       * Get tool names by category
       */
      getToolNamesByCategory(category) {
        const categorySet = this.toolsByCategory.get(category);
        return categorySet ? Array.from(categorySet) : [];
      }
      /**
       * Clear all registered tools
       */
      clear() {
        this.tools.clear();
        for (const categorySet of this.toolsByCategory.values()) {
          categorySet.clear();
        }
        logger.info("InternalToolRegistry: Cleared all registered tools");
      }
      /**
       * Validate tool structure and properties
       */
      validateTool(tool) {
        if (!tool.name || typeof tool.name !== "string") {
          return { valid: false, error: "Tool name is required and must be a string" };
        }
        if (!tool.description || typeof tool.description !== "string") {
          return { valid: false, error: "Tool description is required and must be a string" };
        }
        const validCategories = [
          "memory",
          "session",
          "system",
          "knowledge_graph"
        ];
        if (!tool.category || !validCategories.includes(tool.category)) {
          return { valid: false, error: `Tool category must be one of: ${validCategories.join(", ")}` };
        }
        if (tool.internal !== true) {
          return { valid: false, error: "Tool must have internal property set to true" };
        }
        if (!tool.handler || typeof tool.handler !== "function") {
          return { valid: false, error: "Tool handler is required and must be a function" };
        }
        if (!tool.parameters || typeof tool.parameters !== "object") {
          return { valid: false, error: "Tool parameters are required and must be an object" };
        }
        if (tool.parameters.type !== "object") {
          return { valid: false, error: 'Tool parameters type must be "object"' };
        }
        if (!tool.parameters.properties || typeof tool.parameters.properties !== "object") {
          return { valid: false, error: "Tool parameters must have a properties object" };
        }
        if (tool.name.includes(".") && !tool.name.startsWith("cipher_")) {
          return { valid: false, error: "Tool names with dots should start with cipher_ prefix" };
        }
        return { valid: true };
      }
      /**
       * Get registry statistics
       */
      getRegistryStats() {
        return {
          totalTools: this.getToolCount(),
          toolsByCategory: this.getToolCountByCategory(),
          initialized: this.initialized
        };
      }
      /**
       * Reset the singleton instance (mainly for testing)
       */
      static reset() {
        _InternalToolRegistry.instance = null;
      }
    };
  }
});

// src/core/brain/tools/manager.ts
var import_uuid10, DEFAULT_CONFIG, InternalToolManager;
var init_manager4 = __esm({
  "src/core/brain/tools/manager.ts"() {
    init_cjs_shims();
    init_logger2();
    init_registry2();
    init_types7();
    init_event_types();
    import_uuid10 = require("uuid");
    DEFAULT_CONFIG = {
      enabled: true,
      timeout: 3e4,
      // 30 seconds
      enableCache: true,
      cacheTimeout: 3e5
      // 5 minutes
    };
    InternalToolManager = class {
      config;
      registry;
      initialized = false;
      stats = /* @__PURE__ */ new Map();
      maxExecutionHistorySize = 100;
      eventManager;
      services;
      constructor(config2 = {}) {
        this.config = { ...DEFAULT_CONFIG, ...config2 };
        this.registry = InternalToolRegistry.getInstance();
        this.services = {
          embeddingManager: {
            getEmbedder: () => ({
              embed: async (_text) => Array(128).fill(0).map(() => Math.random())
            })
          },
          vectorStoreManager: {
            getStore: () => ({
              search: async (_embedding, _maxResults = 5) => [
                {
                  id: 1,
                  score: 0.7,
                  payload: { text: "Similar existing memory", tags: ["programming"] }
                }
              ],
              insert: async (_embeddings, _ids, _payloads) => {
                return;
              },
              update: async (_id, _embedding, _payload) => {
                return;
              },
              delete: async (_id) => {
                return;
              }
            })
          },
          llmService: {
            directGenerate: async (_prompt) => "Operation: ADD\nConfidence: 0.8\nReasoning: New technical information to store"
          }
        };
      }
      /**
       * Initialize the internal tool manager
       */
      async initialize() {
        if (this.initialized) {
          logger.warn("InternalToolManager: Already initialized");
          return;
        }
        if (!this.config.enabled) {
          logger.debug("InternalToolManager: Disabled by configuration");
          this.initialized = true;
          return;
        }
        try {
          await this.registry.initialize();
          this.initialized = true;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`InternalToolManager: Initialization failed: ${errorMessage}`);
          throw error;
        }
      }
      /**
       * Register a new internal tool
       */
      registerTool(tool) {
        this.ensureInitialized();
        const result = this.registry.registerTool(tool);
        if (result.success) {
          const normalizedName = createInternalToolName(tool.name);
          this.initializeToolStats(normalizedName);
        } else {
          logger.warn(`InternalToolManager: Failed to register tool '${tool.name}': ${result.message}`);
        }
        return result;
      }
      /**
       * Unregister an internal tool
       */
      unregisterTool(toolName) {
        this.ensureInitialized();
        const normalizedName = createInternalToolName(toolName);
        const result = this.registry.unregisterTool(normalizedName);
        if (result) {
          this.stats.delete(normalizedName);
          logger.info(`InternalToolManager: Successfully unregistered tool '${normalizedName}'`);
        }
        return result;
      }
      /**
       * Get all registered internal tools
       */
      getAllTools() {
        this.ensureInitialized();
        return this.registry.getAllTools();
      }
      /**
       * Get a specific internal tool by name
       */
      getTool(toolName) {
        this.ensureInitialized();
        return this.registry.getTool(toolName);
      }
      /**
       * Check if a tool name is an internal tool
       */
      isInternalTool(toolName) {
        if (!this.initialized || !this.config.enabled) {
          return false;
        }
        const normalizedName = createInternalToolName(toolName);
        return this.registry.isInternalTool(toolName) || this.registry.isInternalTool(normalizedName);
      }
      /**
       * Execute an internal tool
       */
      async executeTool(toolName, args, context) {
        this.ensureInitialized();
        const startTime = Date.now();
        const normalizedName = createInternalToolName(toolName);
        const executionId = (0, import_uuid10.v4)();
        const tool = this.registry.getTool(normalizedName);
        if (!tool) {
          throw new Error(`Internal tool '${normalizedName}' not found`);
        }
        const execContext = {
          toolName,
          startTime,
          sessionId: context?.sessionId,
          userId: context?.userId || "",
          metadata: context?.metadata,
          services: {
            ...this.services,
            ...context?.services
          }
        };
        if (this.eventManager && execContext.sessionId) {
          this.eventManager.emitSessionEvent(
            execContext.sessionId,
            SessionEvents.TOOL_EXECUTION_STARTED,
            {
              toolName: normalizedName,
              toolType: "internal",
              sessionId: execContext.sessionId,
              executionId,
              timestamp: startTime
            }
          );
        }
        logger.debug(`InternalToolManager: Executing tool '${normalizedName}'`, {
          toolName: normalizedName,
          category: tool.category,
          hasArgs: !!args,
          sessionId: execContext.sessionId,
          executionId
        });
        try {
          const result = await this.executeWithTimeout(tool, args, execContext);
          const executionTime = Date.now() - startTime;
          this.recordExecution(normalizedName, true, executionTime);
          if (this.eventManager && execContext.sessionId) {
            this.eventManager.emitSessionEvent(
              execContext.sessionId,
              SessionEvents.TOOL_EXECUTION_COMPLETED,
              {
                toolName: normalizedName,
                toolType: "internal",
                sessionId: execContext.sessionId,
                executionId,
                duration: executionTime,
                success: true,
                timestamp: Date.now()
              }
            );
          }
          logger.debug(`InternalToolManager: Tool '${normalizedName}' executed successfully`, {
            toolName: normalizedName,
            executionTime,
            executionId
          });
          return result;
        } catch (error) {
          const executionTime = Date.now() - startTime;
          this.recordExecution(normalizedName, false, executionTime);
          const errorMessage = error instanceof Error ? error.message : String(error);
          if (this.eventManager && execContext.sessionId) {
            this.eventManager.emitSessionEvent(
              execContext.sessionId,
              SessionEvents.TOOL_EXECUTION_FAILED,
              {
                toolName: normalizedName,
                toolType: "internal",
                sessionId: execContext.sessionId,
                executionId,
                error: errorMessage,
                duration: executionTime,
                timestamp: Date.now()
              }
            );
          }
          logger.error(`InternalToolManager: Tool '${normalizedName}' execution failed`, {
            toolName: normalizedName,
            error: errorMessage,
            executionTime,
            executionId
          });
          throw new Error(`Internal tool execution failed: ${errorMessage}`);
        }
      }
      /**
       * Get tools by category
       */
      getToolsByCategory(category) {
        this.ensureInitialized();
        return this.registry.getToolsByCategory(category);
      }
      /**
       * Get execution statistics for a tool
       */
      getToolStats(toolName) {
        const normalizedName = createInternalToolName(toolName);
        const entry = this.stats.get(normalizedName);
        return entry ? { ...entry.stats } : void 0;
      }
      /**
       * Get overall manager statistics
       */
      getManagerStats() {
        this.ensureInitialized();
        const registryStats = this.registry.getRegistryStats();
        let totalExecutions = 0;
        for (const entry of this.stats.values()) {
          totalExecutions += entry.stats.totalExecutions;
        }
        return {
          totalTools: registryStats.totalTools,
          toolsByCategory: registryStats.toolsByCategory,
          totalExecutions
        };
      }
      /**
       * Get all tool statistics
       */
      getStatistics() {
        const allStats = {};
        for (const [toolName, entry] of this.stats.entries()) {
          allStats[toolName] = { ...entry.stats };
        }
        return allStats;
      }
      /**
       * Get available tools list
       */
      async getAvailableTools() {
        this.ensureInitialized();
        const tools = this.registry.getAllTools();
        return Object.values(tools).map((tool) => ({
          name: tool.name,
          description: tool.description,
          category: tool.category
        }));
      }
      /**
       * Clear all execution statistics
       */
      clearStats() {
        this.stats.clear();
        logger.info("InternalToolManager: Cleared all execution statistics");
      }
      /**
       * Shutdown the internal tool manager
       */
      async shutdown() {
        if (!this.initialized) {
          return;
        }
        try {
          logger.info("InternalToolManager: Shutting down...");
          this.clearStats();
          this.registry.clear();
          this.initialized = false;
          logger.info("InternalToolManager: Shutdown completed");
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error(`InternalToolManager: Shutdown failed: ${errorMessage}`);
          throw error;
        }
      }
      /**
       * Execute tool with timeout handling
       */
      async executeWithTimeout(tool, args, context) {
        return new Promise((resolve3, reject) => {
          const timeoutId = setTimeout(() => {
            reject(new Error(`Tool execution timeout after ${this.config.timeout}ms`));
          }, this.config.timeout);
          tool.handler(args, context).then((result) => {
            clearTimeout(timeoutId);
            resolve3(result);
          }).catch((error) => {
            clearTimeout(timeoutId);
            reject(error);
          });
        });
      }
      /**
       * Record tool execution statistics
       */
      recordExecution(toolName, success, executionTime) {
        let entry = this.stats.get(toolName);
        if (!entry) {
          entry = this.createStatsEntry(toolName);
          this.stats.set(toolName, entry);
        }
        entry.stats.totalExecutions++;
        entry.stats.lastExecution = (/* @__PURE__ */ new Date()).toISOString();
        if (success) {
          entry.stats.successfulExecutions++;
        } else {
          entry.stats.failedExecutions++;
        }
        entry.executionTimes.push(executionTime);
        if (entry.executionTimes.length > entry.maxHistorySize) {
          entry.executionTimes = entry.executionTimes.slice(-entry.maxHistorySize);
        }
        entry.stats.averageExecutionTime = entry.executionTimes.reduce((sum, time) => sum + time, 0) / entry.executionTimes.length;
      }
      /**
       * Initialize statistics for a tool
       */
      initializeToolStats(toolName) {
        if (!this.stats.has(toolName)) {
          this.stats.set(toolName, this.createStatsEntry(toolName));
        }
      }
      /**
       * Create a new stats entry
       */
      createStatsEntry(toolName) {
        return {
          stats: {
            toolName,
            totalExecutions: 0,
            successfulExecutions: 0,
            failedExecutions: 0,
            averageExecutionTime: 0
          },
          executionTimes: [],
          maxHistorySize: this.maxExecutionHistorySize
        };
      }
      /**
       * Ensure the manager is initialized
       */
      ensureInitialized() {
        if (!this.initialized) {
          throw new Error(
            "InternalToolManager must be initialized before use. Call initialize() first."
          );
        }
        if (!this.config.enabled) {
          throw new Error("InternalToolManager is disabled by configuration");
        }
      }
      /**
       * Get configuration
       */
      getConfig() {
        return { ...this.config };
      }
      /**
       * Check if manager is initialized
       */
      isInitialized() {
        return this.initialized;
      }
      /**
       * Check if manager is enabled
       */
      isEnabled() {
        return this.config.enabled;
      }
      /**
       * Set agent services for tools that need access to them
       */
      setServices(services) {
        this.services = services;
      }
      /**
       * Set the event manager for emitting tool execution events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
    };
  }
});

// src/core/brain/tools/unified-tool-manager.ts
var import_uuid11, UnifiedToolManager;
var init_unified_tool_manager = __esm({
  "src/core/brain/tools/unified-tool-manager.ts"() {
    init_cjs_shims();
    init_logger2();
    init_types7();
    init_event_types();
    import_uuid11 = require("uuid");
    UnifiedToolManager = class {
      mcpManager;
      internalToolManager;
      config;
      eventManager;
      toolsAlreadyLogged = false;
      embeddingManager;
      // Reference to embedding manager for status checking
      constructor(mcpManager, internalToolManager, config2 = {}) {
        this.mcpManager = mcpManager;
        this.internalToolManager = internalToolManager;
        this.config = {
          enableInternalTools: true,
          enableMcpTools: true,
          conflictResolution: "prefix-internal",
          executionTimeout: 3e4,
          mode: "default",
          ...config2
        };
      }
      /**
       * Set the event manager for emitting tool execution events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      /**
       * Set the embedding manager for checking embedding status
       */
      setEmbeddingManager(embeddingManager) {
        this.embeddingManager = embeddingManager;
      }
      /**
       * Check if embeddings are disabled globally
       */
      areEmbeddingsDisabled() {
        if (this.embeddingManager) {
          return !this.embeddingManager.hasAvailableEmbeddings();
        }
        return true;
      }
      /**
       * Check if a tool is embedding-related and should be excluded when embeddings are disabled
       */
      isEmbeddingRelatedTool(toolName) {
        const embeddingToolPatterns = [
          "extract_and_operate_memory",
          "search_memory",
          "search_reasoning",
          "store_reasoning_memory",
          "extract_reasoning_steps",
          "evaluate_reasoning",
          "memory_operation",
          "knowledge_search",
          "vector_search",
          "embedding",
          "similarity",
          "cipher_extract_and_operate_memory",
          "cipher_search_memory",
          "cipher_store_reasoning_memory",
          "cipher_extract_reasoning_steps",
          "cipher_evaluate_reasoning",
          "cipher_search_reasoning_patterns",
          // Workspace memory tools still need embeddings
          "cipher_workspace_search",
          "cipher_workspace_store",
          "workspace_search",
          "workspace_store"
        ];
        return embeddingToolPatterns.some(
          (pattern) => toolName.toLowerCase().includes(pattern.toLowerCase())
        );
      }
      /**
       * Get all available tools from both sources
       * Filters tools based on mode:
       * - CLI mode: Only search tools + MCP tools (background tools excluded from agent access)
       * - Default MCP mode: Only ask_cipher tool
       * - Aggregator MCP mode: All tools
       */
      async getAllTools() {
        if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
          logger.debug("UnifiedToolManager: Getting all tools");
        }
        const combinedTools = {};
        try {
          if (this.config.mode === "default") {
            combinedTools["ask_cipher"] = {
              description: "Ask Cipher to perform tasks using its internal tools and capabilities",
              parameters: {
                type: "object",
                properties: {
                  query: {
                    type: "string",
                    description: "The task or question to ask Cipher"
                  }
                },
                required: ["query"]
              },
              source: "internal"
            };
            logger.debug("UnifiedToolManager: Default MCP mode - only ask_cipher tool exposed");
            return combinedTools;
          }
          if (this.config.enableMcpTools && (this.config.mode === "cli" || this.config.mode === "aggregator" || this.config.mode === "api")) {
            if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
              logger.debug("UnifiedToolManager: Loading MCP tools");
            }
            try {
              const mcpTools = await this.mcpManager.getAllTools();
              if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
                logger.debug(`UnifiedToolManager: Retrieved ${Object.keys(mcpTools).length} MCP tools`);
              }
              for (const [toolName, tool] of Object.entries(mcpTools)) {
                combinedTools[toolName] = {
                  description: tool.description,
                  parameters: tool.parameters,
                  source: "mcp"
                };
              }
              if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
                logger.debug(`UnifiedToolManager: Loaded ${Object.keys(mcpTools).length} MCP tools`);
              }
            } catch (error) {
              logger.warn("UnifiedToolManager: Failed to load MCP tools", { error });
            }
          }
          if (this.config.enableInternalTools) {
            if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
              logger.debug("UnifiedToolManager: Loading internal tools");
            }
            try {
              const internalTools = this.internalToolManager.getAllTools();
              if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
                logger.debug(
                  `UnifiedToolManager: Retrieved ${Object.keys(internalTools).length} internal tools`
                );
              }
              for (const [toolName, tool] of Object.entries(internalTools)) {
                if (this.areEmbeddingsDisabled() && this.isEmbeddingRelatedTool(toolName)) {
                  logger.debug(
                    `UnifiedToolManager: Skipping embedding-related tool '${toolName}' - embeddings are disabled`
                  );
                  continue;
                }
                if (this.config.mode === "cli") {
                  const isAgentAccessible = (tool.agentAccessible ?? true) && // Default to true if not specified
                  !toolName.includes("extract_and_operate_memory") && !toolName.includes("store_reasoning");
                  if (!isAgentAccessible) {
                    continue;
                  }
                } else if (this.config.mode === "aggregator") {
                } else {
                  if (tool.agentAccessible === false) {
                    logger.debug(
                      `UnifiedToolManager: Skipping internal-only tool '${toolName}' in ${this.config.mode} mode`
                    );
                    continue;
                  }
                }
                const normalizedName = toolName.startsWith("cipher_") ? toolName : `cipher_${toolName}`;
                if (combinedTools[normalizedName]) {
                  const conflictHandled = this.handleToolConflict(normalizedName, tool, combinedTools);
                  if (!conflictHandled) continue;
                }
                combinedTools[normalizedName] = {
                  description: tool.description,
                  parameters: tool.parameters,
                  source: "internal"
                };
              }
              if (this.config.mode === "cli") {
                const searchToolCount = Object.keys(combinedTools).filter(
                  (name) => combinedTools[name]?.source === "internal" && (name.includes("search") || name.includes("memory_") || name.includes("knowledge_"))
                ).length;
                logger.debug(
                  `UnifiedToolManager: CLI mode - ${searchToolCount} search tools accessible to LLM`
                );
              } else if (this.config.mode !== "aggregator") {
                logger.debug(
                  `UnifiedToolManager: Loaded ${Object.keys(internalTools).length} internal tools (${Object.keys(combinedTools).filter((name) => combinedTools[name]?.source === "internal").length} agent-accessible)`
                );
              }
            } catch (error) {
              logger.warn("UnifiedToolManager: Failed to load internal tools", { error });
            }
          }
          if (!this.toolsAlreadyLogged && this.config.mode !== "aggregator") {
            logger.debug(
              `UnifiedToolManager: Combined tools loaded successfully (mode: ${this.config.mode})`
            );
            this.toolsAlreadyLogged = true;
          }
          return combinedTools;
        } catch (error) {
          logger.error("UnifiedToolManager: Failed to get all tools", { error });
          throw error;
        }
      }
      /**
       * Execute a tool by routing to the appropriate manager
       */
      async executeTool(toolName, args, sessionId) {
        const executionId = (0, import_uuid11.v4)();
        const startTime = Date.now();
        const toolType = this.config.enableInternalTools && isInternalToolName(toolName) ? "internal" : "mcp";
        if (this.eventManager && sessionId) {
          logger.debug("UnifiedToolManager: Emitting tool execution started event", {
            toolName,
            toolType,
            sessionId,
            executionId,
            argsKeys: args ? Object.keys(args) : [],
            args
          });
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_STARTED, {
            toolName,
            toolType,
            sessionId,
            executionId,
            timestamp: startTime,
            args
            // Include tool arguments
          });
        }
        try {
          logger.debug(`UnifiedToolManager: Executing tool '${toolName}'`, {
            toolName,
            hasArgs: !!args,
            sessionId,
            executionId
          });
          let result;
          if (this.areEmbeddingsDisabled() && this.isEmbeddingRelatedTool(toolName)) {
            logger.warn(
              `UnifiedToolManager: Blocking execution of embedding-related tool '${toolName}' - embeddings are disabled`
            );
            throw new Error(
              `Tool '${toolName}' is not available - embeddings are disabled for this session`
            );
          }
          if (this.config.enableInternalTools && isInternalToolName(toolName)) {
            if (!this.internalToolManager.isInternalTool(toolName)) {
              throw new Error(`Internal tool '${toolName}' not found`);
            }
            logger.debug(`UnifiedToolManager: Routing '${toolName}' to internal tool manager`);
            result = await this.internalToolManager.executeTool(toolName, args);
          } else if (this.config.enableMcpTools) {
            logger.debug(`UnifiedToolManager: Routing '${toolName}' to MCP manager`);
            result = await this.mcpManager.executeTool(toolName, args);
          } else {
            throw new Error(`Tool '${toolName}' not available - no suitable manager enabled`);
          }
          if (this.eventManager && sessionId) {
            this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_COMPLETED, {
              toolName,
              toolType,
              sessionId,
              executionId,
              duration: Date.now() - startTime,
              success: true,
              result,
              timestamp: Date.now()
            });
          }
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const duration = Date.now() - startTime;
          if (this.eventManager && sessionId) {
            this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_FAILED, {
              toolName,
              toolType,
              sessionId,
              executionId,
              error: errorMessage,
              duration,
              timestamp: Date.now()
            });
          }
          logger.error(`UnifiedToolManager: Tool execution failed for '${toolName}'`, {
            toolName,
            error: errorMessage,
            sessionId,
            executionId,
            duration
          });
          throw error;
        }
      }
      /**
       * Execute a tool without triggering redundant tool loading (for background operations)
       * This method bypasses the normal tool loading process when tools are already loaded
       */
      async executeToolWithoutLoading(toolName, args, sessionId) {
        const executionId = (0, import_uuid11.v4)();
        const startTime = Date.now();
        const toolType = this.config.enableInternalTools && isInternalToolName(toolName) ? "internal" : "mcp";
        if (this.eventManager && sessionId) {
          logger.debug("UnifiedToolManager: Emitting tool execution started event", {
            toolName,
            toolType,
            sessionId,
            executionId,
            argsKeys: args ? Object.keys(args) : [],
            args
          });
          this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_STARTED, {
            toolName,
            toolType,
            sessionId,
            executionId,
            timestamp: startTime,
            args
            // Include tool arguments
          });
        }
        try {
          logger.debug(`UnifiedToolManager: Executing tool '${toolName}' (without loading)`, {
            toolName,
            hasArgs: !!args,
            sessionId,
            executionId
          });
          let result;
          if (this.config.enableInternalTools && isInternalToolName(toolName)) {
            if (!this.internalToolManager.isInternalTool(toolName)) {
              throw new Error(`Internal tool '${toolName}' not found`);
            }
            logger.debug(
              `UnifiedToolManager: Routing '${toolName}' to internal tool manager (without loading)`
            );
            result = await this.internalToolManager.executeTool(toolName, args);
          } else if (this.config.enableMcpTools) {
            logger.debug(`UnifiedToolManager: Routing '${toolName}' to MCP manager (without loading)`);
            result = await this.mcpManager.executeTool(toolName, args);
          } else {
            throw new Error(`Tool '${toolName}' not available - no suitable manager enabled`);
          }
          if (this.eventManager && sessionId) {
            this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_COMPLETED, {
              toolName,
              toolType,
              sessionId,
              executionId,
              duration: Date.now() - startTime,
              success: true,
              result,
              timestamp: Date.now()
            });
          }
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const duration = Date.now() - startTime;
          if (this.eventManager && sessionId) {
            this.eventManager.emitSessionEvent(sessionId, SessionEvents.TOOL_EXECUTION_FAILED, {
              toolName,
              toolType,
              sessionId,
              executionId,
              error: errorMessage,
              duration,
              timestamp: Date.now()
            });
          }
          logger.error(
            `UnifiedToolManager: Tool execution failed for '${toolName}' (without loading)`,
            {
              toolName,
              error: errorMessage,
              sessionId,
              executionId,
              duration
            }
          );
          throw error;
        }
      }
      /**
       * Check if a background tool exists (for internal execution, not agent access)
       */
      isBackgroundToolAvailable(toolName) {
        try {
          if (this.config.enableInternalTools && isInternalToolName(toolName)) {
            const tool = this.internalToolManager.getTool(toolName);
            return !!tool;
          }
          return false;
        } catch {
          return false;
        }
      }
      /**
       * Check if a tool is available (to agents) based on current mode
       */
      async isToolAvailable(toolName) {
        try {
          if (this.config.mode === "default") {
            return toolName === "ask_cipher";
          }
          if (this.config.enableInternalTools && isInternalToolName(toolName)) {
            const tool = this.internalToolManager.getTool(toolName);
            if (!tool) return false;
            if (this.config.mode === "cli") {
              const isSearchTool = toolName.includes("search") || toolName.includes("memory_") || toolName.includes("knowledge_") || toolName.includes("vector_") || toolName === "extract_and_operate_memory" || toolName === "cipher_extract_and_operate_memory";
              return isSearchTool && tool.agentAccessible !== false;
            } else if (this.config.mode === "aggregator") {
              return true;
            } else {
              return tool.agentAccessible !== false;
            }
          } else if (this.config.enableMcpTools && (this.config.mode === "cli" || this.config.mode === "aggregator" || this.config.mode === "api")) {
            const mcpTools = await this.mcpManager.getAllTools();
            return toolName in mcpTools;
          }
          return false;
        } catch (error) {
          logger.error(`UnifiedToolManager: Error checking tool availability for '${toolName}'`, {
            error
          });
          return false;
        }
      }
      /**
       * Get tool source (internal or mcp) for agent-accessible tools
       */
      async getToolSource(toolName) {
        try {
          if (this.config.mode === "default") {
            return toolName === "ask_cipher" ? "internal" : null;
          }
          if (this.config.enableInternalTools && isInternalToolName(toolName)) {
            const tool = this.internalToolManager.getTool(toolName);
            if (!tool) return null;
            if (this.config.mode === "cli") {
              const isSearchTool = toolName.includes("search") || toolName.includes("memory_") || toolName.includes("knowledge_") || toolName.includes("vector_") || toolName === "extract_and_operate_memory" || toolName === "cipher_extract_and_operate_memory";
              return isSearchTool && tool.agentAccessible !== false ? "internal" : null;
            } else if (this.config.mode === "aggregator") {
              return "internal";
            } else {
              return tool.agentAccessible !== false ? "internal" : null;
            }
          } else if (this.config.enableMcpTools && (this.config.mode === "cli" || this.config.mode === "aggregator" || this.config.mode === "api")) {
            const mcpTools = await this.mcpManager.getAllTools();
            return toolName in mcpTools ? "mcp" : null;
          }
          return null;
        } catch (error) {
          logger.error(`UnifiedToolManager: Error determining tool source for '${toolName}'`, {
            error
          });
          return null;
        }
      }
      /**
       * Get tools formatted for specific LLM providers
       */
      async getToolsForProvider(provider) {
        logger.info(`UnifiedToolManager: Getting tools for provider: ${provider}`);
        const allTools = await this.getAllTools();
        logger.info(`UnifiedToolManager: Got ${Object.keys(allTools).length} total tools`);
        switch (provider) {
          case "openai":
          case "openrouter":
            logger.info("UnifiedToolManager: Formatting tools for OpenAI");
            return this.formatToolsForOpenAI(allTools);
          case "qwen":
            return this.formatToolsForOpenAI(allTools);
          case "gemini":
            logger.info("UnifiedToolManager: Formatting tools for Gemini");
            return this.formatToolsForGemini(allTools);
          case "anthropic":
            logger.info("UnifiedToolManager: Formatting tools for Anthropic");
            return this.formatToolsForAnthropic(allTools);
          case "aws":
            logger.info("UnifiedToolManager: Formatting tools for AWS (Anthropic-compatible)");
            return this.formatToolsForAnthropic(allTools);
          // AWS Bedrock uses Anthropic-compatible format
          case "azure":
            logger.info("UnifiedToolManager: Formatting tools for Azure (OpenAI-compatible)");
            return this.formatToolsForOpenAI(allTools);
          // Azure OpenAI uses OpenAI-compatible format
          default:
            throw new Error(`Unsupported provider: ${provider}`);
        }
      }
      /**
       * Get manager statistics
       */
      getStats() {
        return {
          internalTools: this.config.enableInternalTools ? this.internalToolManager.getManagerStats() : null,
          mcpTools: this.config.enableMcpTools ? {
            clientCount: this.mcpManager.getClients().size,
            failedConnections: Object.keys(this.mcpManager.getFailedConnections()).length
          } : null,
          config: this.config
        };
      }
      /**
       * Handle tool name conflicts
       */
      handleToolConflict(toolName, _internalTool, _existingTools) {
        switch (this.config.conflictResolution) {
          case "prefix-internal":
            return true;
          case "prefer-internal":
            logger.warn(
              `UnifiedToolManager: Tool conflict for '${toolName}', preferring internal tool`
            );
            return true;
          case "prefer-mcp":
            logger.warn(`UnifiedToolManager: Tool conflict for '${toolName}', preferring MCP tool`);
            return false;
          case "error":
            throw new Error(`Tool name conflict: '${toolName}' exists in both MCP and internal tools`);
          default:
            return true;
        }
      }
      /**
       * Format tools for OpenAI/OpenRouter (function calling format)
       */
      formatToolsForOpenAI(tools) {
        return Object.entries(tools).map(([name, tool]) => ({
          type: "function",
          function: {
            name,
            description: tool.description,
            parameters: tool.parameters
          }
        }));
      }
      /**
       * Format tools for Anthropic (tool use format)
       */
      formatToolsForAnthropic(tools) {
        return Object.entries(tools).map(([name, tool]) => ({
          name,
          description: tool.description,
          input_schema: tool.parameters
        }));
      }
      /**
       * Format tools for Gemini (function calling format - same as OpenAI)
       */
      formatToolsForGemini(tools) {
        return Object.entries(tools).map(([name, tool]) => ({
          type: "function",
          function: {
            name,
            description: tool.description,
            parameters: tool.parameters
          }
        }));
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/payloads.ts
function createKnowledgePayload(id, text, tags, confidence, event, options) {
  const { env: env3 } = (init_env(), __toCommonJS(env_exports));
  return {
    id,
    text,
    tags,
    confidence,
    event,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    version: 2,
    ...options.domain && { domain: options.domain },
    ...options.sourceSessionId && { sourceSessionId: options.sourceSessionId },
    qualitySource: options.qualitySource,
    ...options.code_pattern && { code_pattern: options.code_pattern },
    ...options.old_memory && { old_memory: options.old_memory },
    // Add cross-tool sharing identifiers (env vars take precedence for security)
    userId: env3.CIPHER_USER_ID || options.userId,
    projectId: env3.CIPHER_PROJECT_NAME || options.projectId,
    workspaceMode: env3.CIPHER_WORKSPACE_MODE || options.workspaceMode || "isolated"
  };
}
function extractKnowledgeInfo(text) {
  const result = {};
  if (!text || typeof text !== "string") return result;
  const fenceMatch = text.match(/```[a-zA-Z0-9]*\n([\s\S]*?)```/);
  if (fenceMatch && fenceMatch[1]) {
    const snippet = fenceMatch[1].trim();
    if (snippet.length > 0 && snippet.length <= 4e3) {
      result.codePattern = snippet;
    }
  }
  const lower = text.toLowerCase();
  if (/\b(react|vue|angular|frontend|ui|css|html|typescript|javascript)\b/.test(lower)) {
    result.domain = "frontend";
  } else if (/\b(node|express|api|backend|django|flask|rails|spring)\b/.test(lower)) {
    result.domain = "backend";
  } else if (/\b(docker|kubernetes|ci|cd|pipeline|devops|infrastructure)\b/.test(lower)) {
    result.domain = "devops";
  }
  return result;
}
function mergeKnowledgeInfo(llmProvided = {}, extracted = {}) {
  return {
    ...(llmProvided.domain || extracted.domain) && {
      domain: llmProvided.domain || extracted.domain
    },
    ...(llmProvided.codePattern || extracted.codePattern) && {
      codePattern: llmProvided.codePattern || extracted.codePattern
    }
  };
}
function createReasoningPayload(id, text, reasoningSteps, evaluation, context, options = {}) {
  const { env: env3 } = (init_env(), __toCommonJS(env_exports));
  const stepCount = reasoningSteps.length;
  const stepTypes = Array.from(new Set(reasoningSteps.map((step) => step.type)));
  const issueCount = evaluation.issues.length;
  return {
    id,
    text,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    version: 2,
    tags: ["reasoning"],
    reasoningSteps,
    evaluation,
    context,
    stepCount,
    stepTypes,
    issueCount,
    ...options.sourceSessionId && { sourceSessionId: options.sourceSessionId },
    // Add cross-tool sharing identifiers (env vars take precedence for security)
    userId: env3.CIPHER_USER_ID || options.userId,
    projectId: env3.CIPHER_PROJECT_NAME || options.projectId,
    workspaceMode: env3.CIPHER_WORKSPACE_MODE || options.workspaceMode || "isolated"
  };
}
var init_payloads = __esm({
  "src/core/brain/tools/definitions/memory/payloads.ts"() {
    init_cjs_shims();
  }
});

// src/core/brain/tools/definitions/memory/memory_operation.ts
function parseLLMDecision(response) {
  try {
    if (!response || typeof response !== "string") {
      logger.debug("MemoryOperation: Empty or invalid LLM response", {
        responseType: typeof response
      });
      return null;
    }
    const cleanResponse = response.trim();
    const safeConvertId = (id) => {
      if (id === null || id === void 0) return null;
      const numId = parseInt(String(id), 10);
      return !isNaN(numId) && Number.isInteger(numId) && numId > 0 ? numId : null;
    };
    const jsonMatch = cleanResponse.match(/\{[\s\S]*\}/);
    if (jsonMatch) {
      try {
        const decision = JSON.parse(jsonMatch[0]);
        if (decision && decision.operation && typeof decision.confidence === "number") {
          return {
            operation: decision.operation,
            confidence: Math.max(0, Math.min(1, decision.confidence)),
            reasoning: decision.reasoning || decision.reason || "LLM decision",
            targetMemoryId: safeConvertId(
              decision.targetMemoryId || decision.target_id || decision.id
            )
          };
        }
      } catch (parseError) {
        logger.debug("MemoryOperation: Failed to parse JSON match", {
          jsonMatch: jsonMatch[0].substring(0, 100),
          error: parseError instanceof Error ? parseError.message : String(parseError)
        });
      }
    }
    for (let start = 0; start < cleanResponse.length; start++) {
      for (let end = cleanResponse.length; end > start + 10; end--) {
        const substr = cleanResponse.slice(start, end);
        if (substr[0] !== "{" || substr[substr.length - 1] !== "}") continue;
        try {
          const obj = JSON.parse(substr);
          if (obj && obj.operation && typeof obj.confidence === "number") {
            return {
              operation: obj.operation,
              confidence: Math.max(0, Math.min(1, obj.confidence)),
              reasoning: obj.reasoning || obj.reason || "LLM decision",
              targetMemoryId: safeConvertId(obj.targetMemoryId || obj.target_id || obj.id)
            };
          }
        } catch {
        }
      }
    }
    const operationMatch = cleanResponse.match(/(?:operation|action)['"]?\s*:\s*['"]?(\w+)['"]?/i);
    const confidenceMatch = cleanResponse.match(/confidence['"]?\s*:\s*([0-9.]+)/i);
    const reasoningMatch = cleanResponse.match(
      /(?:reasoning|reason)['"]?\s*:\s*['"]([^'"]+)['"]?/i
    );
    if (operationMatch && confidenceMatch) {
      const operation = operationMatch[1]?.toUpperCase() || "";
      const confidence = parseFloat(confidenceMatch[1] || "0");
      if (["ADD", "UPDATE", "DELETE", "NONE"].includes(operation) && !isNaN(confidence)) {
        logger.debug("MemoryOperation: Extracted decision using regex fallback", {
          operation,
          confidence,
          response: cleanResponse.substring(0, 200)
        });
        return {
          operation,
          confidence: Math.max(0, Math.min(1, confidence)),
          reasoning: reasoningMatch ? reasoningMatch[1] : "Parsed from LLM response",
          targetMemoryId: null
        };
      }
    }
    const responseUpper = cleanResponse.toUpperCase();
    let detectedOperation = null;
    if (responseUpper.includes("ADD") || responseUpper.includes("CREATE") || responseUpper.includes("NEW")) {
      detectedOperation = "ADD";
    } else if (responseUpper.includes("UPDATE") || responseUpper.includes("MODIFY") || responseUpper.includes("CHANGE")) {
      detectedOperation = "UPDATE";
    } else if (responseUpper.includes("DELETE") || responseUpper.includes("REMOVE")) {
      detectedOperation = "DELETE";
    } else if (responseUpper.includes("NONE") || responseUpper.includes("SKIP") || responseUpper.includes("IGNORE")) {
      detectedOperation = "NONE";
    }
    if (detectedOperation) {
      logger.debug("MemoryOperation: Detected operation using keyword fallback", {
        operation: detectedOperation,
        response: cleanResponse.substring(0, 200)
      });
      return {
        operation: detectedOperation,
        confidence: 0.6,
        // Default confidence for keyword detection
        reasoning: "Detected from LLM response keywords",
        targetMemoryId: null
      };
    }
    logger.debug("MemoryOperation: Failed to parse LLM decision using all methods", {
      response: cleanResponse.substring(0, 500),
      responseLength: cleanResponse.length
    });
    return null;
  } catch (error) {
    logger.warn("MemoryOperation: Error parsing LLM decision", {
      error: error instanceof Error ? error.message : String(error),
      responsePreview: typeof response === "string" ? response.substring(0, 100) : "non-string"
    });
    return null;
  }
}
function extractTechnicalTags(fact) {
  const tags = [];
  const languages = [
    "javascript",
    "typescript",
    "python",
    "java",
    "rust",
    "go",
    "php",
    "ruby",
    "swift",
    "kotlin"
  ];
  languages.forEach((lang) => {
    if (fact.toLowerCase().includes(lang)) {
      tags.push(lang);
    }
  });
  const frameworks = [
    "react",
    "vue",
    "angular",
    "svelte",
    "nextjs",
    "express",
    "fastify",
    "django",
    "flask"
  ];
  frameworks.forEach((framework) => {
    if (fact.toLowerCase().includes(framework)) {
      tags.push(framework);
    }
  });
  const tools = [
    "docker",
    "kubernetes",
    "git",
    "npm",
    "yarn",
    "webpack",
    "vite",
    "eslint",
    "prettier"
  ];
  tools.forEach((tool) => {
    if (fact.toLowerCase().includes(tool)) {
      tags.push(tool);
    }
  });
  if (fact.includes("```")) {
    tags.push("code-block");
  }
  if (fact.includes("function") || fact.includes("class") || fact.includes("const") || fact.includes("let") || fact.includes("var")) {
    tags.push("programming");
  }
  if (fact.includes("/") || fact.includes("\\") || fact.includes(".js") || fact.includes(".ts") || fact.includes(".py")) {
    tags.push("file-path");
  }
  if (fact.includes("error") || fact.includes("exception") || fact.includes("failed") || fact.includes("bug")) {
    tags.push("error-handling");
  }
  if (fact.includes("config") || fact.includes("setting") || fact.includes("option")) {
    tags.push("configuration");
  }
  if (fact.includes("api") || fact.includes("endpoint") || fact.includes("request") || fact.includes("response")) {
    tags.push("api");
  }
  if (tags.length === 0) {
    tags.push("general-knowledge");
  }
  return Array.from(new Set(tags)).map((tag) => tag.toLowerCase());
}
var MEMORY_OPERATION_PROMPTS;
var init_memory_operation = __esm({
  "src/core/brain/tools/definitions/memory/memory_operation.ts"() {
    init_cjs_shims();
    init_logger2();
    init_payloads();
    MEMORY_OPERATION_PROMPTS = {
      SYSTEM_PROMPT: `You analyze programming knowledge facts and decide ADD, UPDATE, DELETE, or NONE using similarity with existing memories and context.

Process only significant technical content (concepts, code details, patterns, implementations). Skip personal or trivial content.

Consider:
1) Technical relevance and value
2) Semantic similarity/overlap
3) Recency and contextual relevance
4) Quality and completeness
5) Conversation context and needs
6) Concrete code/pattern details

Rules:
- ADD: New, unique technical knowledge
- UPDATE: Improves/corrects existing technical knowledge
- DELETE: Outdated/incorrect/contradictory information
- NONE: Duplicate, already covered, or non-significant

Always preserve full code blocks/commands/patterns exactly as given.`,
      DECISION_PROMPT: `Analyze the knowledge fact and choose ADD, UPDATE, DELETE, or NONE.

Steps:
1) Compare with similar memories (semantic similarity, not keywords).
2) If none are similar (below threshold) -> ADD.
3) If more correct/complete than a similar memory -> UPDATE (include targetMemoryId).
4) If redundant/already present -> NONE.
5) If contradicts an existing memory -> DELETE (include targetMemoryId).
6) Always include a confidence score (0.0\u20131.0).

Examples:
- ADD
  Similar: none
  { "operation": "ADD", "confidence": 0.95, "targetMemoryId": null }

- UPDATE
  Similar: 12345 (0.91)
  { "operation": "UPDATE", "confidence": 0.90, "targetMemoryId": 12345 }

- NONE
  Similar: 67890 (0.95)
  { "operation": "NONE", "confidence": 0.98, "targetMemoryId": 67890 }

- DELETE
  Similar: 54321 (0.92)
  { "operation": "DELETE", "confidence": 0.93, "targetMemoryId": 54321 }

---
FACT: {fact}
SIMILAR: {similarMemories}
CONTEXT: {context}

Respond ONLY with:
{ "operation": "ADD|UPDATE|DELETE|NONE", "confidence": 0.0-1.0, "targetMemoryId": "id-if-updating-or-deleting-or-none" }`
    };
  }
});

// src/core/brain/tools/definitions/memory/extract_and_operate_memory.ts
function isSignificantKnowledge(content) {
  if (!content || content.trim().length === 0) {
    return false;
  }
  const text = content.toLowerCase().trim();
  const skipPatterns = [
    // Personal information and identity
    /\b(my name|user['']?s? name|find my name|search.*name|who am i|what['']?s my name)\b/i,
    /\b(personal|profile|identity|username|login|password|email|address|phone)\b/i,
    // Trivial search queries without technical context
    /^(user:|assistant:)?\s*(search|find|look|what|where|who|when|why|how)\s+(is|are|was|were|do|does|did|can|could|should|would|will|my|the|a|an)\b/i,
    // Simple greetings and social interactions
    /^(user:|assistant:)?\s*(hello|hi|hey|good morning|good afternoon|good evening|thanks|thank you|please|sorry|excuse me|bye|goodbye)\b/i,
    // Tool results with no meaningful content
    /^(cipher_memory_search|memory_search):\s*(found|completed|no results|error)/i,
    // Generic status messages
    /^(task completed|operation successful|processing|loading|waiting|done|finished|ready)\b/i,
    // Simple yes/no or acknowledgment responses
    /^(user:|assistant:)?\s*(yes|no|ok|okay|sure|fine|great|good|right|correct|wrong|true|false)\s*[.!?]?\s*$/i
  ];
  for (const pattern of skipPatterns) {
    if (pattern.test(text)) {
      return false;
    }
  }
  const technicalPatterns = [
    // Programming concepts and patterns
    /\b(function|method|class|interface|module|library|framework|algorithm|data structure|design pattern)\b/i,
    /\b(variable|constant|parameter|argument|return|async|await|promise|callback|closure|scope)\b/i,
    /\b(loop|iteration|recursion|condition|exception|error handling|debugging|testing|optimization)\b/i,
    // Code elements and syntax
    /\b(import|export|require|include|package|dependency|api|endpoint|request|response)\b/i,
    /\b(database|query|sql|nosql|schema|table|index|transaction|orm|migration)\b/i,
    /\b(git|version control|commit|merge|branch|pull request|repository|deployment)\b/i,
    // Technical implementations
    /\b(implements?|extends?|inherits?|overrides?|polymorphism|encapsulation|abstraction)\b/i,
    /\b(sort|search|filter|map|reduce|transform|parse|serialize|encrypt|decrypt)\b/i,
    /\b(authentication|authorization|security|validation|sanitization|middleware)\b/i,
    // Code blocks and technical syntax
    /```[\s\S]*```/,
    /`[^`]+`/,
    /\$[a-zA-Z_][a-zA-Z0-9_]*/,
    // Shell variables
    /\b(npm|yarn|pip|composer|cargo|go get|mvn|gradle)\b/i,
    // File and system operations
    /\b(file|directory|path|config|environment|server|client|host|port|url|http|https|ssl|tls)\b/i,
    /\b(dockerfile|docker|container|kubernetes|cloud|aws|azure|gcp|ci\/cd|pipeline)\b/i,
    // Programming languages and technologies
    /\b(javascript|typescript|python|java|c\+\+|c#|rust|go|php|ruby|swift|kotlin|scala|r)\b/i,
    /\b(react|vue|angular|node|express|django|flask|spring|rails|laravel|fastapi)\b/i,
    /\b(html|css|scss|sass|less|bootstrap|tailwind|webpack|vite|rollup|babel|eslint)\b/i,
    // Error messages and stack traces with technical context
    /\b(error|exception|traceback|stack trace|compilation|syntax error|runtime error|type error)\b/i,
    // Technical explanations and problem-solving
    /\b(solution|approach|implementation|technique|strategy|pattern|best practice|optimization)\b/i,
    /\b(performance|scalability|maintainability|refactoring|code review|documentation)\b/i
  ];
  for (const pattern of technicalPatterns) {
    if (pattern.test(text)) {
      return true;
    }
  }
  const codePatterns = [
    /[{}[\]()]/,
    // Brackets and parentheses
    /[=><!&|]/,
    // Operators
    /[;:,]/,
    // Punctuation common in code
    /\w+\.\w+/,
    // Dot notation
    /\w+\(\)/,
    // Function calls
    /\/\*[\s\S]*?\*\/|\/\/.*$/m
    // Comments
  ];
  let codePatternMatches = 0;
  for (const pattern of codePatterns) {
    if (pattern.test(text)) {
      codePatternMatches++;
    }
  }
  if (codePatternMatches >= 2) {
    return true;
  }
  const technicalWords = [
    "api",
    "sdk",
    "cli",
    "gui",
    "ui",
    "ux",
    "ide",
    "editor",
    "compiler",
    "interpreter",
    "runtime",
    "virtual",
    "machine",
    "container",
    "image",
    "build",
    "deploy",
    "release",
    "version",
    "update",
    "patch",
    "bug",
    "feature",
    "enhancement",
    "issue",
    "ticket",
    "workflow",
    "process",
    "pipeline",
    "automation",
    "script",
    "batch",
    "cron",
    "job",
    "service",
    "microservice",
    "monolith",
    "architecture",
    "pattern",
    "design",
    "system",
    "network",
    "protocol",
    "tcp",
    "udp",
    "http",
    "https",
    "ssl",
    "tls",
    "dns",
    "cdn",
    "cache",
    "redis",
    "memcached",
    "session",
    "cookie",
    "token",
    "jwt",
    "oauth",
    "auth",
    "encrypt",
    "decrypt",
    "hash",
    "salt",
    "key",
    "certificate",
    "public",
    "private",
    "binary",
    "ascii",
    "unicode",
    "utf8",
    "base64",
    "hex",
    "decimal",
    "octal",
    "buffer"
  ];
  const words = text.split(/\s+/);
  const technicalWordCount = words.filter(
    (word) => technicalWords.includes(word.replace(/[^\w]/g, ""))
  ).length;
  const technicalDensity = technicalWordCount / words.length;
  if (technicalDensity > 0.1 && words.length > 5) {
    return true;
  }
  if (text.length < 20) {
    return false;
  }
  const programmingKeywords = /\b(code|coding|program|programming|develop|development|software|hardware|tech|technical|digital|computer|computing|algorithm|logic|syntax|semantic|compile|runtime|execute|debug|test|deploy|implement|configure|setup|install|upgrade|migrate|scale|optimize|refactor)\b/i;
  if (programmingKeywords.test(text)) {
    return true;
  }
  return false;
}
function isRetrievedResult(content) {
  if (!content || typeof content !== "string") return false;
  const text = content.toLowerCase();
  const retrievedPatterns = [
    /^(cipher_memory_search|memory_search|cipher_search_reasoning_patterns|search_reasoning_patterns|search_graph|query_graph|enhanced_search):/i,
    /^(retrieved|result|results|found|matches|nodes|edges|search completed|query executed|suggestions?):/i,
    /^(knowledge results|reflection results|reasoning patterns|graph nodes|graph edges):/i,
    /^(reasoning trace|evaluation|step count|quality score|issue count):/i,
    /^(search results|retrieved knowledge|retrieved reasoning|retrieved entities):/i,
    /^(graph search|kg search|knowledge graph result|kg result):/i,
    /^(observation:|action:|thought:|conclusion:|reflection:)/i,
    // Reflection memory patterns
    /^(id:|timestamp:|similarity:|source:|memorytype:)/i,
    /^(message: 'query executed'|message: 'search completed')/i,
    /^(success: true|success: false)/i,
    /^(totalresults:|searchtime:|embeddingtime:)/i,
    /^(nodes:|edges:|related:|totalcount:|executiontime:)/i
  ];
  for (const pattern of retrievedPatterns) {
    if (pattern.test(text)) return true;
  }
  return false;
}
function generateSafeMemoryId(index) {
  const timestamp = Math.floor(Date.now() / 1e3);
  return timestamp * 1e3 + index % 1e3;
}
function inferDomainFromTags(tags) {
  const domainMapping = {
    javascript: "frontend",
    typescript: "frontend",
    react: "frontend",
    vue: "frontend",
    angular: "frontend",
    html: "frontend",
    css: "frontend",
    node: "backend",
    express: "backend",
    api: "backend",
    database: "backend",
    sql: "backend",
    docker: "devops",
    kubernetes: "devops",
    deployment: "devops",
    ci: "devops",
    cd: "devops",
    git: "version-control",
    github: "version-control",
    testing: "quality-assurance",
    debug: "quality-assurance"
  };
  for (const tag of tags) {
    if (domainMapping[tag.toLowerCase()]) {
      return domainMapping[tag.toLowerCase()];
    }
  }
  return void 0;
}
var extractAndOperateMemoryTool;
var init_extract_and_operate_memory = __esm({
  "src/core/brain/tools/definitions/memory/extract_and_operate_memory.ts"() {
    init_cjs_shims();
    init_logger2();
    init_memory_operation();
    init_payloads();
    extractAndOperateMemoryTool = {
      name: "extract_and_operate_memory",
      category: "memory",
      internal: true,
      agentAccessible: false,
      // Internal-only: programmatically called after each interaction
      description: "Extract knowledge facts from raw interaction(s) and immediately process them to determine memory operations (ADD, UPDATE, DELETE, NONE) in a single atomic step. This guarantees extraction always precedes operation.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          interaction: {
            oneOf: [
              {
                type: "string",
                description: "A single user interaction or conversation text to extract knowledge from."
              },
              {
                type: "array",
                items: {
                  type: "string"
                },
                description: "Multiple user interactions or conversation texts to extract knowledge from."
              }
            ],
            description: "Raw user interaction(s) or conversation text(s) to extract knowledge from."
          },
          existingMemories: {
            type: "array",
            description: "Array of existing memory entries to compare against for similarity analysis.",
            items: {
              type: "object",
              properties: {
                id: { type: "string", description: "Unique identifier of the existing memory" },
                text: { type: "string", description: "Content of the existing memory" },
                metadata: { type: "object", description: "Optional metadata for the memory" }
              },
              required: ["id", "text"]
            }
          },
          // Minimal structured hints for payload (agent > extraction)
          knowledgeInfo: {
            type: "object",
            description: "Optional LLM/agent-provided knowledge info (takes precedence over extraction)",
            properties: {
              domain: {
                type: "string",
                description: "Knowledge domain (frontend, backend, devops, ...)"
              },
              codePattern: { type: "string", description: "Canonical code pattern to store" }
            },
            additionalProperties: false
          },
          context: {
            type: "object",
            description: "Optional context information for memory operations",
            properties: {
              sessionId: {
                type: "string",
                description: "Current session identifier (stored as sourceSessionId)"
              }
            },
            additionalProperties: false
          },
          options: {
            type: "object",
            description: "Configuration options for memory operations",
            properties: {
              similarityThreshold: {
                type: "number",
                description: "Similarity threshold for memory matching (0.0 to 1.0)",
                minimum: 0,
                maximum: 1
              },
              maxSimilarResults: {
                type: "number",
                description: "Maximum number of similar memories to retrieve",
                minimum: 1,
                maximum: 10
              },
              enableBatchProcessing: {
                type: "boolean",
                description: "Whether to process multiple knowledge items in batch"
              },
              useLLMDecisions: {
                type: "boolean",
                description: "Whether to use LLM-powered decision making"
              },
              autoExtractKnowledgeInfo: {
                type: "boolean",
                description: "Whether to automatically extract knowledge info (agent > extraction)",
                default: true
              },
              confidenceThreshold: {
                type: "number",
                description: "Minimum confidence threshold for operations (0.0 to 1.0)",
                minimum: 0,
                maximum: 1
              },
              enableDeleteOperations: {
                type: "boolean",
                description: "Whether to enable DELETE operations"
              }
            },
            additionalProperties: false
          },
          memoryMetadata: {
            type: "object",
            description: "Custom metadata to attach to created memories (projectId, userId, etc.)",
            additionalProperties: true,
            properties: {
              projectId: { type: "string", description: "Project identifier for scoped memory" },
              userId: { type: "string", description: "User identifier for personalized memory" },
              environment: { type: "string", description: "Environment (dev, staging, prod)" },
              source: { type: "string", description: "Source of the memory (cli, api, web)" }
            }
          }
        },
        required: ["interaction"]
      },
      handler: async (args, context) => {
        try {
          const sessionState = context?.services?.embeddingManager?.getSessionState?.();
          if (sessionState?.isDisabled()) {
            const reason = sessionState.getDisabledReason();
            logger.debug(
              "ExtractAndOperateMemory: Embeddings disabled for this session, skipping memory operations",
              { reason }
            );
            return {
              success: true,
              mode: "chat-only",
              message: `Memory operations disabled: ${reason}`,
              extractedFacts: 0,
              memoryActions: 0,
              skipped: true
            };
          }
          if (context?.services?.embeddingManager && !context.services.embeddingManager.hasAvailableEmbeddings()) {
            logger.debug(
              "ExtractAndOperateMemory: No available embeddings, skipping memory operations"
            );
            return {
              success: true,
              mode: "chat-only",
              message: "No available embeddings - operating in chat-only mode",
              extractedFacts: 0,
              memoryActions: 0,
              skipped: true
            };
          }
          const embeddingStatus = context?.services?.embeddingManager?.getEmbeddingStatus();
          if (embeddingStatus) {
            const disabledEmbedders = Object.values(embeddingStatus).filter(
              (status) => status.status === "DISABLED"
            );
            if (disabledEmbedders.length > 0) {
              logger.debug(
                "ExtractAndOperateMemory: Some embedders are disabled, skipping memory operations"
              );
              return {
                success: true,
                mode: "chat-only",
                message: "Embedders are disabled - operating in chat-only mode",
                extractedFacts: 0,
                memoryActions: 0,
                skipped: true
              };
            }
          }
          let knowledgeArray;
          if (!args.interaction) {
            throw new Error("No interaction(s) provided for extraction");
          }
          if (typeof args.interaction === "string") {
            knowledgeArray = [args.interaction];
          } else if (Array.isArray(args.interaction)) {
            knowledgeArray = args.interaction;
          } else {
            throw new Error("Interaction must be a string or array of strings");
          }
          const validFacts = knowledgeArray.filter((fact) => fact && typeof fact === "string" && fact.trim().length > 0).map((fact) => fact.trim());
          const significantFacts = validFacts.filter((fact) => {
            if (isRetrievedResult(fact)) {
              logger.debug("ExtractAndOperateMemory: Skipping retrieved result", {
                factPreview: fact.substring(0, 100) + (fact.length > 100 ? "..." : ""),
                reason: "Fact is a retrieved result from search tool or knowledge graph"
              });
              return false;
            }
            const isSignificant = isSignificantKnowledge(fact);
            if (!isSignificant) {
              logger.debug("ExtractAndOperateMemory: Skipping non-significant fact", {
                factPreview: fact.substring(0, 100) + (fact.length > 100 ? "..." : ""),
                reason: "Does not contain significant programming knowledge or concepts"
              });
            }
            return isSignificant;
          });
          if (significantFacts.length === 0) {
            logger.debug("ExtractAndOperateMemory: No significant facts found after filtering", {
              originalFacts: validFacts.length,
              filteredFacts: significantFacts.length,
              interactionLength: args.interaction.length
            });
            return {
              success: true,
              extraction: {
                extracted: 0,
                skipped: knowledgeArray.length,
                facts: []
              },
              memory: [],
              summary: [],
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const extractionStats = {
            extracted: significantFacts.length,
            skipped: knowledgeArray.length - significantFacts.length,
            facts: significantFacts.map((fact, i) => ({
              id: `fact_${Math.floor(Date.now() / 1e3)}_${i}`,
              preview: fact.substring(0, 100) + (fact.length > 100 ? "..." : ""),
              metadata: {
                hasCodeBlock: fact.includes("```"),
                hasCommand: fact.includes("$") || fact.includes("npm") || fact.includes("git"),
                length: fact.length
              }
            }))
          };
          if (!context?.services) {
            logger.warn(
              "ExtractAndOperateMemory: No services context available, using basic processing"
            );
            const basicMemoryActions = significantFacts.map((fact, i) => ({
              id: generateSafeMemoryId(i),
              text: fact,
              event: "ADD",
              tags: extractTechnicalTags(fact),
              confidence: 0.7
            }));
            return {
              success: true,
              extraction: extractionStats,
              memory: basicMemoryActions,
              summary: basicMemoryActions.map((action) => ({
                factPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embeddingManager = context.services.embeddingManager;
          const vectorStoreManager = context.services.vectorStoreManager;
          const llmService = context.services.llmService;
          if (!embeddingManager || !vectorStoreManager) {
            logger.warn(
              "ExtractAndOperateMemory: Missing embedding or vector services, falling back to basic processing"
            );
            const basicMemoryActions = significantFacts.map((fact, i) => ({
              id: generateSafeMemoryId(i),
              text: fact,
              event: "ADD",
              tags: extractTechnicalTags(fact),
              confidence: 0.6
            }));
            return {
              success: true,
              extraction: extractionStats,
              memory: basicMemoryActions,
              summary: basicMemoryActions.map((action) => ({
                factPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embedder = embeddingManager.getEmbedder("default");
          let vectorStore;
          try {
            logger.debug("ExtractAndOperateMemory: Using knowledge collection");
            vectorStore = vectorStoreManager.getStore("knowledge") || vectorStoreManager.getStore();
          } catch (error) {
            logger.debug("ExtractAndOperateMemory: Falling back to default store", {
              error: error instanceof Error ? error.message : String(error)
            });
            vectorStore = vectorStoreManager.getStore();
          }
          if (!embedder || !vectorStore) {
            logger.warn(
              "ExtractAndOperateMemory: Embedder or vector store not available, using basic processing"
            );
            const basicMemoryActions = significantFacts.map((fact, i) => ({
              id: generateSafeMemoryId(i),
              text: fact,
              event: "ADD",
              tags: extractTechnicalTags(fact),
              confidence: 0.6
            }));
            return {
              success: true,
              extraction: extractionStats,
              memory: basicMemoryActions,
              summary: basicMemoryActions.map((action) => ({
                factPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (!embedder.embed || typeof embedder.embed !== "function") {
            throw new Error("Embedder is not properly initialized or missing embed() method");
          }
          const options = {
            similarityThreshold: args.options?.similarityThreshold ?? 0.6,
            maxSimilarResults: args.options?.maxSimilarResults ?? 5,
            useLLMDecisions: args.options?.useLLMDecisions ?? true
          };
          const memoryActions = [];
          const memorySummaries = [];
          for (let i = 0; i < significantFacts.length; i++) {
            const fact = significantFacts[i];
            if (!fact) {
              logger.warn(`ExtractAndOperateMemory: Skipping undefined fact at index ${i}`);
              continue;
            }
            logger.debug(
              `ExtractAndOperateMemory: Processing fact ${i + 1}/${significantFacts.length}`,
              {
                factPreview: fact.substring(0, 80) + (fact.length > 80 ? "..." : ""),
                factLength: fact.length
              }
            );
            try {
              let embedding;
              try {
                embedding = await embedder.embed(fact);
              } catch (embedError) {
                logger.error(
                  `ExtractAndOperateMemory: Failed to embed fact ${i + 1}, disabling embeddings for session`,
                  {
                    error: embedError instanceof Error ? embedError.message : String(embedError),
                    factPreview: fact.substring(0, 50),
                    provider: embedder.getConfig().type
                  }
                );
                if (context?.services?.embeddingManager && embedError instanceof Error) {
                  context.services.embeddingManager.handleRuntimeFailure(
                    embedError,
                    embedder.getConfig().type
                  );
                }
                return {
                  success: true,
                  mode: "chat-only",
                  message: `Embeddings disabled due to failure: ${embedError instanceof Error ? embedError.message : String(embedError)}`,
                  extractedFacts: significantFacts.length,
                  memoryActions: 0,
                  skipped: true,
                  error: embedError instanceof Error ? embedError.message : String(embedError)
                };
              }
              let similar = [];
              try {
                similar = await vectorStore.search(embedding, options.maxSimilarResults);
              } catch (searchError) {
                logger.warn(
                  `ExtractAndOperateMemory: Failed to search similar memories for fact ${i + 1}`,
                  {
                    error: searchError instanceof Error ? searchError.message : String(searchError),
                    factPreview: fact.substring(0, 50)
                  }
                );
              }
              logger.debug(`ExtractAndOperateMemory: Similarity search completed for fact ${i + 1}`, {
                similarMemoriesFound: similar.length,
                topSimilarity: similar.length > 0 ? similar[0]?.score?.toFixed(3) : "N/A",
                similarities: similar.slice(0, 3).map((s) => ({
                  id: s.id,
                  score: s.score?.toFixed(3),
                  preview: (s.payload?.text || "").substring(0, 50) + "..."
                }))
              });
              let action = "ADD";
              let targetId = null;
              let reason = "";
              let confidence = 0;
              let usedLLM = false;
              if (options.useLLMDecisions && llmService) {
                try {
                  const similarMemoriesStr = similar.map(
                    (mem, idx) => `  ${idx + 1}. ID: ${mem.id} (similarity: ${mem.score?.toFixed(2) ?? "N/A"})
     Content: ${(mem.payload?.text || "").substring(0, 200)}`
                  ).join("\n");
                  const DECISION_PROMPT = MEMORY_OPERATION_PROMPTS.DECISION_PROMPT;
                  const llmInput = DECISION_PROMPT.replace("{fact}", fact).replace("{similarMemories}", similarMemoriesStr || "No similar memories found.").replace("{context}", "");
                  let llmResponse;
                  try {
                    llmResponse = await Promise.race([
                      llmService.directGenerate(llmInput),
                      new Promise(
                        (_, reject) => setTimeout(() => reject(new Error("LLM decision timeout")), 3e4)
                      )
                    ]);
                  } catch (llmError) {
                    throw new Error(
                      `LLM call failed: ${llmError instanceof Error ? llmError.message : String(llmError)}`
                    );
                  }
                  const decision = parseLLMDecision(String(llmResponse));
                  if (decision && ["ADD", "UPDATE", "DELETE", "NONE"].includes(decision.operation)) {
                    action = decision.operation;
                    confidence = Math.max(0, Math.min(1, decision.confidence ?? 0.7));
                    reason = decision.reasoning || "LLM decision";
                    targetId = decision.targetMemoryId || null;
                    usedLLM = true;
                    logger.debug(`ExtractAndOperateMemory: LLM decision for fact ${i + 1}`, {
                      factPreview: fact.substring(0, 80) + (fact.length > 80 ? "..." : ""),
                      decision: action,
                      confidence: confidence.toFixed(2),
                      reasoning: reason,
                      targetMemoryId: targetId,
                      decisionMethod: "LLM"
                    });
                  } else {
                    throw new Error("LLM decision missing required fields or invalid operation");
                  }
                } catch (llmError) {
                  logger.warn(
                    `ExtractAndOperateMemory: LLM decision failed for fact ${i + 1}, using heuristic fallback`,
                    {
                      factPreview: fact.substring(0, 80),
                      error: llmError instanceof Error ? llmError.message : String(llmError)
                    }
                  );
                  usedLLM = false;
                }
              }
              if (!usedLLM) {
                const mostSimilar = similar.length > 0 ? similar[0] : null;
                confidence = mostSimilar?.score ?? 0;
                if (!mostSimilar || confidence < options.similarityThreshold) {
                  action = "ADD";
                  reason = "No highly similar memory found; adding as new.";
                } else {
                  if (fact === mostSimilar.payload?.text) {
                    action = "NONE";
                    reason = "Fact is redundant; already present.";
                    targetId = mostSimilar.id;
                  } else if (fact.length > (mostSimilar.payload?.text?.length ?? 0)) {
                    action = "UPDATE";
                    targetId = mostSimilar.id;
                    reason = "Fact is more complete/correct; updating existing memory.";
                  } else if (fact.includes("not") && mostSimilar.payload?.text && !mostSimilar.payload.text.includes("not")) {
                    action = "DELETE";
                    targetId = mostSimilar.id;
                    reason = "Fact contradicts existing memory; deleting old memory.";
                  } else {
                    action = "NONE";
                    reason = "Fact is similar but not more complete; ignoring.";
                    targetId = mostSimilar.id;
                  }
                }
                logger.debug(`ExtractAndOperateMemory: Heuristic decision for fact ${i + 1}`, {
                  factPreview: fact.substring(0, 80) + (fact.length > 80 ? "..." : ""),
                  decision: action,
                  confidence: confidence.toFixed(3),
                  reasoning: reason,
                  targetMemoryId: targetId,
                  decisionMethod: "Heuristic",
                  topSimilarityScore: mostSimilar?.score?.toFixed(3) || "N/A",
                  similarityThreshold: options.similarityThreshold.toFixed(2)
                });
              }
              memoryActions.push({
                id: action === "ADD" ? generateSafeMemoryId(i) : targetId && !isNaN(Number(targetId)) && Number(targetId) > 0 ? Number(targetId) : generateSafeMemoryId(i),
                text: fact,
                event: action,
                tags: extractTechnicalTags(fact),
                confidence
              });
              memorySummaries.push({
                factPreview: fact.substring(0, 80),
                action,
                confidence,
                targetId
              });
            } catch (factError) {
              logger.error(`ExtractAndOperateMemory: Failed to process fact ${i + 1}`, {
                error: factError instanceof Error ? factError.message : String(factError),
                factPreview: fact.substring(0, 50)
              });
              memoryActions.push({
                id: generateSafeMemoryId(i),
                text: fact,
                event: "ADD",
                tags: extractTechnicalTags(fact),
                confidence: 0.4
              });
            }
          }
          logger.debug("ExtractAndOperateMemory: Starting memory persistence operations", {
            totalActions: memoryActions.length,
            persistableActions: memoryActions.filter((a) => ["ADD", "UPDATE", "DELETE"].includes(a.event)).length,
            skippableActions: memoryActions.filter((a) => a.event === "NONE").length
          });
          let persistedCount = 0;
          for (const action of memoryActions) {
            if (["ADD", "UPDATE", "DELETE"].includes(action.event)) {
              if (!action.text) {
                logger.warn(`ExtractAndOperateMemory: Skipping action with undefined text`, {
                  memoryId: action.id,
                  event: action.event
                });
                continue;
              }
              try {
                const embedding = await embedder.embed(action.text);
                let qualitySource = "heuristic";
                qualitySource = "heuristic";
                const domainFromTags = inferDomainFromTags(action.tags);
                const options2 = {
                  qualitySource
                };
                if (context?.sessionId) {
                  options2.sourceSessionId = context.sessionId;
                }
                if (args.options?.autoExtractKnowledgeInfo !== false) {
                  const llmKnowledge = args.knowledgeInfo || {};
                  const extractedKnowledge = extractKnowledgeInfo(action.text);
                  const merged = mergeKnowledgeInfo(llmKnowledge, extractedKnowledge);
                  if (merged.domain) options2.domain = merged.domain;
                  if (merged.codePattern) options2.code_pattern = merged.codePattern;
                } else {
                  if (args.knowledgeInfo?.domain) {
                    options2.domain = args.knowledgeInfo.domain;
                  } else if (domainFromTags) {
                    options2.domain = domainFromTags;
                  }
                  if (args.knowledgeInfo?.codePattern) {
                    options2.code_pattern = args.knowledgeInfo.codePattern;
                  }
                }
                if ("old_memory" in action && action.old_memory) {
                  options2.old_memory = action.old_memory;
                }
                const payload = createKnowledgePayload(
                  action.id,
                  action.text,
                  action.tags,
                  action.confidence,
                  action.event,
                  options2
                );
                if (action.event === "ADD") {
                  await vectorStore.insert([embedding], [action.id], [payload]);
                  logger.debug(`ExtractAndOperateMemory: ${action.event} operation completed`, {
                    memoryId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    tags: action.tags,
                    confidence: action.confidence.toFixed(3)
                  });
                } else if (action.event === "UPDATE") {
                  await vectorStore.update(action.id, embedding, payload);
                  logger.debug(`ExtractAndOperateMemory: ${action.event} operation completed`, {
                    memoryId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    tags: action.tags,
                    confidence: action.confidence.toFixed(3)
                  });
                } else if (action.event === "DELETE") {
                  await vectorStore.delete(action.id);
                  logger.debug(`ExtractAndOperateMemory: ${action.event} operation completed`, {
                    memoryId: action.id
                  });
                }
                persistedCount++;
              } catch (persistError) {
                logger.error(
                  `ExtractAndOperateMemory: ${action.event} operation failed, continuing with others`,
                  {
                    memoryId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    error: persistError instanceof Error ? persistError.message : String(persistError)
                  }
                );
                if (context?.services?.embeddingManager && persistError instanceof Error) {
                  context.services.embeddingManager.handleRuntimeFailure(
                    persistError,
                    embedder.getConfig().type
                  );
                }
              }
            }
          }
          logger.debug("ExtractAndOperateMemory: Memory persistence completed", {
            totalProcessed: memoryActions.length,
            successfullyPersisted: persistedCount,
            actionsSummary: {
              ADD: memoryActions.filter((a) => a.event === "ADD").length,
              UPDATE: memoryActions.filter((a) => a.event === "UPDATE").length,
              DELETE: memoryActions.filter((a) => a.event === "DELETE").length,
              NONE: memoryActions.filter((a) => a.event === "NONE").length
            }
          });
          return {
            success: true,
            extraction: extractionStats,
            memory: memoryActions,
            summary: memorySummaries,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("ExtractAndOperateMemory: Critical failure in extract and operate", {
            error: errorMessage,
            stack: error instanceof Error ? error.stack : void 0
          });
          return {
            success: false,
            error: errorMessage,
            extraction: {
              extracted: 0,
              skipped: Array.isArray(args.interaction) ? args.interaction.length : 1,
              facts: []
            },
            memory: [],
            summary: [],
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/search_memory.ts
async function rewriteUserQuery(originalInput, llmService) {
  try {
    const rewritePrompt = `
        You are a query decomposition and disambiguation expert. Break down this question into search queries for a knowledge base while handling ambiguous terms.

        QUESTION: "${originalInput}"

		TASK: Create 2-5 concise search queries that capture the core information needs of the question.

		GUIDELINES:
		- Focus on the main intent of the question.
		- Use natural, searchable language (4-15 words per query).
		- Only create disambiguation queries for clearly ambiguous terms.
		- Avoid over-decomposing the question into too many subqueries.
		- Prefer fewer, more precise queries over exhaustive coverage.
		- Each query should stand alone and be understandable without additional context.

		DISAMBIGUATION (Only if needed):
		- For truly ambiguous terms (homonyms, abbreviations, etc.), include 1-2 alternate queries with different meanings.
		- Do not force disambiguation where the meaning is already clear from context.

        OUTPUT FORMAT:
        Respond with ONLY the queries, one per line, using this exact format:
        Query 1: [first query]
        Query 2: [second query]
        [continue as needed...]

        Do not include any explanations, introductions, or other text. Only the queries.

        EXAMPLES:
        Question: "What profession does Nicholas Ray and Elia Kazan have in common?"
        Query 1: Nicholas Ray profession career
        Query 2: Elia Kazan profession career

        Question: "Most total goals in a premier league season?"
        Query 1: Most total goals in a premier league season by a team
        Query 2: Most total goals in a premier league season by a player

        Now decompose and disambiguate: "${originalInput}"
        `;
    const rewriteResponse = await llmService.directGenerate(rewritePrompt);
    const queries = rewriteResponse.split("\n").map((line) => line.trim()).filter((line) => line.length > 0).map((line) => {
      const match = line.match(/^Query\s*\d+:\s*(.+)$/i);
      return match && match[1] ? match[1].trim() : null;
    }).filter((query2) => query2 !== null && query2.length >= 3).filter(
      (query2, index, array) => array.indexOf(query2) === index
    );
    logger.info(`Parsed queries:`, {
      queries,
      queryCount: queries.length
    });
    if (queries.length === 0) {
      return {
        queries: [originalInput]
      };
    }
    return {
      queries
    };
  } catch (error) {
    logger.warn("MemorySearch: Query rewriting failed", {
      originalInput: originalInput.substring(0, 100),
      error: error instanceof Error ? error.message : String(error)
    });
    return {
      queries: [originalInput]
    };
  }
}
var searchMemoryTool;
var init_search_memory = __esm({
  "src/core/brain/tools/definitions/memory/search_memory.ts"() {
    init_cjs_shims();
    init_logger2();
    init_env();
    searchMemoryTool = {
      name: "memory_search",
      category: "memory",
      internal: true,
      agentAccessible: true,
      // Agent-accessible: searches knowledge memory only
      description: "Perform semantic search over stored knowledge memory entries to retrieve relevant knowledge that can inform current decision-making.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "The search query to find relevant knowledge memories. Use natural language to describe what you are looking for.",
            minLength: 1,
            maxLength: 1e3
          },
          top_k: {
            type: "number",
            description: "Maximum number of results to return (default: 5)",
            minimum: 1,
            maximum: 50,
            default: 5
          },
          similarity_threshold: {
            type: "number",
            description: "Minimum similarity score for results (0.0 to 1.0, default: 0.3)",
            minimum: 0,
            maximum: 1,
            default: 0.3
          },
          include_metadata: {
            type: "boolean",
            description: "Whether to include detailed metadata in results (default: true)",
            default: true
          },
          enable_query_refinement: {
            type: "boolean",
            description: "Whether to apply query refinement for better search results (default: false)",
            default: false
          }
        },
        required: ["query"]
      },
      handler: async (args, context) => {
        const startTime = Date.now();
        const callId = Math.random().toString(36).substring(7);
        console.log(`\u{1F50D} [${callId}] search_memory tool called with:`, {
          query: args.query?.substring(0, 100),
          top_k: args.top_k,
          similarity_threshold: args.similarity_threshold,
          sessionId: context?.sessionId,
          toolName: context?.toolName
        });
        try {
          logger.debug("MemorySearch: Processing knowledge memory search request", {
            query: args.query?.substring(0, 100) || "undefined",
            top_k: args.top_k || 5,
            similarity_threshold: args.similarity_threshold || 0.3,
            enable_query_refinement: args.enable_query_refinement !== false
          });
          if (context?.services?.embeddingManager?.getSessionState()?.isDisabled()) {
            const reason = context.services.embeddingManager.getSessionState().getDisabledReason();
            logger.debug(
              "MemorySearch: Embeddings disabled for this session, returning empty results",
              {
                reason
              }
            );
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                knowledgeResults: 0,
                reflectionResults: 0,
                searchMode: "knowledge",
                usedFallback: true,
                queryRefinementApplied: false
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (context?.services?.embeddingManager && !context.services.embeddingManager.hasAvailableEmbeddings()) {
            logger.debug("MemorySearch: No available embeddings, returning empty results");
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                knowledgeResults: 0,
                reflectionResults: 0,
                searchMode: "knowledge",
                usedFallback: true,
                queryRefinementApplied: false
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (!args.query || typeof args.query !== "string" || args.query.trim().length === 0) {
            throw new Error("Query is required and must be a non-empty string");
          }
          const originalQuery = args.query.trim();
          const topK = Math.max(1, Math.min(50, args.top_k || 5));
          const similarityThreshold = Math.max(0, Math.min(1, args.similarity_threshold || 0.3));
          const enableQueryRefinement = args.enable_query_refinement === true || env.ENABLE_QUERY_REFINEMENT === true;
          if (!context?.services) {
            throw new Error("InternalToolContext.services is required for memory search");
          }
          const embeddingManager = context.services.embeddingManager;
          const vectorStoreManager = context.services.vectorStoreManager;
          if (!embeddingManager || !vectorStoreManager) {
            throw new Error("EmbeddingManager and VectorStoreManager are required in context.services");
          }
          const embedder = embeddingManager.getEmbedder("default");
          if (!embedder?.embed || typeof embedder.embed !== "function" || !embedder.embedBatch || typeof embedder.embedBatch !== "function") {
            throw new Error(
              "Embedder is not properly initialized or missing embed() or embedBatch() method"
            );
          }
          const embeddingStartTime = Date.now();
          logger.debug("MemorySearch: Generating embedding for query", {
            originalQueryLength: originalQuery.length,
            originalQueryPreview: originalQuery.substring(0, 50)
          });
          let finalQuery = [originalQuery];
          let queryEmbeddings;
          try {
            if (enableQueryRefinement) {
              const rewrittenQueries = await rewriteUserQuery(
                originalQuery,
                context.services.llmService
              );
              finalQuery = rewrittenQueries.queries;
            }
            logger.info("Embedding final query:", finalQuery);
            queryEmbeddings = await embedder.embedBatch(finalQuery);
          } catch (embedError) {
            logger.error("MemorySearch: Failed to generate embedding, disabling embeddings globally", {
              error: embedError instanceof Error ? embedError.message : String(embedError),
              provider: embedder.getConfig().type
            });
            if (context?.services?.embeddingManager && embedError instanceof Error) {
              context.services.embeddingManager.handleRuntimeFailure(
                embedError,
                embedder.getConfig().type
              );
            }
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                knowledgeResults: 0,
                reflectionResults: 0,
                searchMode: "knowledge",
                usedFallback: true
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embeddingTime = Date.now() - embeddingStartTime;
          logger.debug("MemorySearch: Embedding generated successfully", {
            embeddingTime: `${embeddingTime}ms`,
            embeddingDimensions: Array.isArray(queryEmbeddings[0]) ? queryEmbeddings[0].length : "unknown"
          });
          const searchStartTime = Date.now();
          let allResults = [];
          let knowledgeResultCount = 0;
          let usedFallback = false;
          const isDualManager = vectorStoreManager.constructor.name === "DualCollectionVectorManager" || typeof vectorStoreManager.getStore === "function" && vectorStoreManager.getStore.length === 1;
          let knowledgeStore = null;
          if (isDualManager) {
            logger.debug("MemorySearch: Using DualCollectionVectorManager for knowledge search", {
              isDualManager: true
            });
            try {
              knowledgeStore = vectorStoreManager.getStore("knowledge");
            } catch {
              try {
                knowledgeStore = vectorStoreManager.getStore();
                usedFallback = true;
              } catch (fallbackError) {
                throw new Error(
                  `Knowledge collection failed and fallback to default store also failed: ${fallbackError instanceof Error ? fallbackError.message : String(fallbackError)}`
                );
              }
            }
          } else {
            logger.debug("MemorySearch: Using single collection manager for knowledge search", {
              isDualManager: false
            });
            knowledgeStore = vectorStoreManager.getStore();
          }
          if (!knowledgeStore) {
            throw new Error("Knowledge vector store not available");
          }
          for (const queryEmbedding of queryEmbeddings) {
            try {
              const knowledgeResults = await knowledgeStore.search(queryEmbedding, topK * 2);
              const markedResults = knowledgeResults.map((result2) => ({
                ...result2,
                payload: {
                  ...result2.payload,
                  source: "knowledge",
                  memoryType: "knowledge"
                }
              }));
              allResults.push(...markedResults);
              logger.debug("MemorySearch: Knowledge collection search completed for query embedding", {
                resultsFound: knowledgeResults.length,
                totalAccumulatedResults: allResults.length
              });
            } catch (searchError) {
              console.log("MemorySearch: Knowledge store search failed", {
                error: searchError instanceof Error ? searchError.message : String(searchError),
                query: originalQuery.substring(0, 50)
              });
              throw searchError;
            }
          }
          const searchTime = Date.now() - searchStartTime;
          knowledgeResultCount = allResults.length;
          logger.info("Knowledge results:", allResults);
          logger.debug("MemorySearch: Knowledge search completed for all query embeddings", {
            searchTime: `${searchTime}ms`,
            totalResults: allResults.length,
            knowledgeResults: knowledgeResultCount
          });
          const filteredResults = allResults.filter((result2) => {
            if ((result2.score || 0) < similarityThreshold) return false;
            if (env.CIPHER_WORKSPACE_MODE === "shared") {
              const payload = result2.payload || {};
              const hasMatchingUserId = !env.CIPHER_USER_ID || !payload.userId || payload.userId === env.CIPHER_USER_ID;
              const hasMatchingProjectId = !env.CIPHER_PROJECT_NAME || !payload.projectId || payload.projectId === env.CIPHER_PROJECT_NAME;
              const isSharedMode = payload.workspaceMode === "shared";
              const shouldInclude = isSharedMode && (hasMatchingUserId || hasMatchingProjectId) || !payload.userId && !payload.projectId && !payload.workspaceMode;
              logger.debug("MemorySearch: Cross-tool sharing filter applied", {
                memoryId: result2.id,
                memoryUserId: payload.userId,
                memoryProjectId: payload.projectId,
                memoryWorkspaceMode: payload.workspaceMode,
                envUserId: env.CIPHER_USER_ID,
                envProjectId: env.CIPHER_PROJECT_NAME,
                envWorkspaceMode: env.CIPHER_WORKSPACE_MODE,
                shouldInclude
              });
              return shouldInclude;
            }
            return true;
          }).sort((a, b) => (b.score || 0) - (a.score || 0)).slice(0, topK).map((result2) => {
            const rawPayload = result2.payload || {};
            console.log("search rawPayload", rawPayload);
            const payload = rawPayload;
            console.log("search payload", payload);
            const baseResult = {
              id: result2.id || payload.id || "unknown",
              text: payload.text || "No content available",
              tags: payload.tags || [],
              timestamp: payload.timestamp || (/* @__PURE__ */ new Date()).toISOString(),
              similarity: result2.score || 0,
              version: payload.version || 2,
              // All data is V2 after cleanup
              source: "knowledge",
              memoryType: "knowledge"
            };
            console.log("search baseResult", baseResult);
            const knowledgePayload = payload;
            return {
              ...baseResult,
              confidence: knowledgePayload.confidence || 0,
              reasoning: "No reasoning available",
              // KnowledgePayload doesn't have reasoning field
              event: knowledgePayload.event,
              ...knowledgePayload.domain && { domain: knowledgePayload.domain },
              qualitySource: knowledgePayload.qualitySource,
              ...knowledgePayload.sourceSessionId && {
                sourceSessionId: knowledgePayload.sourceSessionId
              },
              ...knowledgePayload.code_pattern && { code_pattern: knowledgePayload.code_pattern },
              ...knowledgePayload.old_memory && { old_memory: knowledgePayload.old_memory }
            };
          });
          const totalResults = filteredResults.length;
          const similarities = filteredResults.map((r) => r.similarity);
          const maxSimilarity = similarities.length > 0 ? Math.max(...similarities) : 0;
          const minSimilarity = similarities.length > 0 ? Math.min(...similarities) : 0;
          const averageSimilarity = similarities.length > 0 ? similarities.reduce((a, b) => a + b, 0) / similarities.length : 0;
          const totalTime = Date.now() - startTime;
          const result = {
            success: true,
            query: originalQuery,
            results: filteredResults,
            metadata: {
              totalResults,
              searchTime: totalTime,
              embeddingTime,
              maxSimilarity,
              minSimilarity,
              averageSimilarity,
              knowledgeResults: totalResults,
              // All results are knowledge results
              reflectionResults: 0,
              // No reflection results in knowledge-only search
              searchMode: "knowledge",
              usedFallback
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
          logger.debug("MemorySearch: Knowledge search completed successfully", {
            query: originalQuery.substring(0, 50),
            resultsFound: totalResults,
            knowledgeResults: totalResults,
            maxSimilarity: maxSimilarity.toFixed(3),
            averageSimilarity: averageSimilarity.toFixed(3),
            totalTime: `${totalTime}ms`,
            usedFallback
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const totalTime = Date.now() - startTime;
          logger.error("MemorySearch: Search failed", {
            error: errorMessage,
            query: args.query?.substring(0, 50) || "undefined",
            processingTime: `${totalTime}ms`
          });
          if (context?.services?.embeddingManager && error instanceof Error) {
            const embeddingManager = context.services.embeddingManager;
            if (embeddingManager && typeof embeddingManager.handleRuntimeFailure === "function") {
              const embedder = embeddingManager.getEmbedder("default");
              const providerType = embedder?.getConfig()?.type || "unknown";
              embeddingManager.handleRuntimeFailure(error, providerType);
            }
          }
          return {
            success: false,
            query: args.query || "undefined",
            results: [],
            metadata: {
              totalResults: 0,
              searchTime: totalTime,
              embeddingTime: 0,
              maxSimilarity: 0,
              minSimilarity: 0,
              averageSimilarity: 0,
              knowledgeResults: 0,
              reflectionResults: 0,
              searchMode: "knowledge",
              usedFallback: true
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/store_reasoning_memory.ts
function generateSafeReasoningMemoryId(index) {
  const now = Date.now();
  const randomSuffix = Math.floor(Math.random() * 1e3);
  let vectorId = 666667 + (now % 3e5 * 1e3 + randomSuffix + index) % 333333;
  if (vectorId <= 666666 || vectorId > 999999) {
    vectorId = Math.floor(Math.random() * 333333) + 666667;
  }
  return vectorId;
}
var storeReasoningMemoryTool;
var init_store_reasoning_memory = __esm({
  "src/core/brain/tools/definitions/memory/store_reasoning_memory.ts"() {
    init_cjs_shims();
    init_logger2();
    init_env();
    init_payloads();
    storeReasoningMemoryTool = {
      name: "store_reasoning_memory",
      category: "memory",
      internal: true,
      agentAccessible: false,
      // Internal-only: programmatically called when reasoning content is detected
      description: "Store complete reasoning traces with task context and evaluations in reflection memory. Takes trace with auto-extracted context from cipher_extract_reasoning_steps and evaluation from cipher_evaluate_reasoning. Append-only operation.",
      version: "2.1.0",
      parameters: {
        type: "object",
        properties: {
          trace: {
            type: "object",
            description: "Complete reasoning trace from cipher_extract_reasoning_steps (includes steps and metadata with task context)",
            properties: {
              id: { type: "string" },
              steps: {
                type: "array",
                items: {
                  type: "object",
                  properties: {
                    type: {
                      type: "string",
                      enum: [
                        "thought",
                        "action",
                        "observation",
                        "decision",
                        "conclusion",
                        "reflection"
                      ]
                    },
                    content: { type: "string" }
                  },
                  required: ["type", "content"]
                }
              },
              metadata: {
                type: "object",
                properties: {
                  extractedAt: { type: "string" },
                  conversationLength: { type: "number" },
                  stepCount: { type: "number" },
                  hasExplicitMarkup: { type: "boolean" },
                  sessionId: { type: "string" },
                  taskContext: {
                    type: "object",
                    properties: {
                      goal: { type: "string", description: "What the agent was trying to achieve" },
                      input: {
                        type: "string",
                        description: "Original user request or problem statement"
                      },
                      taskType: {
                        type: "string",
                        description: "Type of task (e.g., code_generation, analysis, problem_solving)"
                      },
                      domain: {
                        type: "string",
                        description: "Problem domain (e.g., programming, math, planning)"
                      },
                      complexity: {
                        type: "string",
                        enum: ["low", "medium", "high"],
                        description: "Task complexity level"
                      }
                    }
                  }
                }
              }
            },
            required: ["id", "steps", "metadata"]
          },
          evaluation: {
            type: "object",
            description: "Quality evaluation of the reasoning trace from cipher_evaluate_reasoning",
            properties: {
              qualityScore: { type: "number", minimum: 0, maximum: 1 },
              efficiencyScore: { type: "number", minimum: 0, maximum: 1 },
              issues: {
                type: "array",
                items: {
                  type: "object",
                  properties: {
                    type: { type: "string" },
                    description: { type: "string" },
                    severity: { type: "string", enum: ["low", "medium", "high"] }
                  }
                }
              },
              suggestions: { type: "array", items: { type: "string" } },
              shouldStore: { type: "boolean" }
            },
            required: ["qualityScore", "issues", "suggestions"]
          }
        },
        required: ["trace", "evaluation"]
      },
      handler: async (args, context) => {
        const startTime = Date.now();
        logger.debug("StoreReasoningMemory: Processing unified reasoning storage request", {
          traceId: args.trace?.id || args.trace?.result?.trace?.id,
          stepCount: args.trace?.steps?.length || args.trace?.result?.trace?.steps?.length || 0,
          qualityScore: args.evaluation?.qualityScore || args.evaluation?.result?.evaluation?.qualityScore,
          hasTrace: !!args.trace,
          hasEvaluation: !!args.evaluation,
          hasTaskContext: !!(args.trace?.metadata?.taskContext || args.trace?.result?.trace?.metadata?.taskContext)
        });
        let trace = args.trace;
        let evaluation = args.evaluation;
        try {
          if (!args) {
            throw new Error("No arguments provided to StoreReasoningMemory");
          }
          const validationErrors = [];
          if (args.trace && args.trace.result && args.trace.result.trace) {
            trace = args.trace.result.trace;
            logger.debug("StoreReasoningMemory: Unwrapped trace from extraction tool result");
          }
          if (args.evaluation && args.evaluation.result && args.evaluation.result.evaluation) {
            evaluation = args.evaluation.result.evaluation;
            logger.debug("StoreReasoningMemory: Unwrapped evaluation from evaluation tool result");
          }
          if (!trace) {
            validationErrors.push(
              "trace parameter is missing - ensure cipher_extract_reasoning_steps was called successfully"
            );
          } else if (typeof trace !== "object") {
            validationErrors.push("trace must be an object");
          } else {
            if (!trace.steps) {
              validationErrors.push("trace.steps is missing - invalid reasoning trace");
            } else if (!Array.isArray(trace.steps)) {
              validationErrors.push("trace.steps must be an array");
            } else if (trace.steps.length === 0) {
              validationErrors.push("trace.steps array is empty - no reasoning steps to store");
            }
            if (!trace.metadata) {
              validationErrors.push("trace.metadata is missing - invalid reasoning trace");
            }
          }
          if (!evaluation) {
            validationErrors.push(
              "evaluation parameter is missing - ensure cipher_evaluate_reasoning was called successfully"
            );
          } else if (typeof evaluation !== "object") {
            validationErrors.push("evaluation must be an object");
          }
          if (validationErrors.length > 0) {
            const errorMessage = `Pipeline validation failed: ${validationErrors.join("; ")}`;
            logger.warn("StoreReasoningMemory: Validation failed, suggesting pipeline check", {
              errors: validationErrors,
              hasTrace: !!trace,
              hasEvaluation: !!evaluation,
              pipelineStage: !trace ? "extraction_failed" : !evaluation ? "evaluation_failed" : "unknown"
            });
            return {
              success: false,
              result: {
                error: errorMessage,
                stored: false,
                validationErrors,
                suggestion: "Ensure both cipher_extract_reasoning_steps and cipher_evaluate_reasoning completed successfully before calling store_reasoning_memory"
              },
              metadata: {
                toolName: "store_reasoning_memory",
                validationFailed: true,
                errors: validationErrors,
                missingDependencies: {
                  trace: !trace,
                  evaluation: !evaluation
                }
              }
            };
          }
          const validSteps = trace.steps.filter(
            (step) => step && typeof step === "object" && step.content && step.type
          );
          if (validSteps.length === 0) {
            logger.warn("StoreReasoningMemory: No valid reasoning steps found after filtering");
            return {
              success: false,
              result: {
                error: "No valid reasoning steps found - all steps are missing required fields (type, content)",
                stored: false
              },
              metadata: {
                toolName: "store_reasoning_memory",
                invalidSteps: true,
                originalStepCount: trace.steps.length,
                validStepCount: 0
              }
            };
          }
          if (validSteps.length < trace.steps.length) {
            logger.debug("StoreReasoningMemory: Filtered out invalid reasoning steps", {
              originalCount: trace.steps.length,
              validCount: validSteps.length,
              filteredCount: trace.steps.length - validSteps.length
            });
          }
          if (evaluation.shouldStore === false) {
            logger.debug(
              "StoreReasoningMemory: Skipping storage - evaluation indicates should not store",
              {
                success: true,
                qualityScore: evaluation.qualityScore,
                shouldStore: evaluation.shouldStore
              }
            );
            return {
              success: true,
              result: {
                message: "Storage skipped - quality threshold not met",
                stored: false
              },
              metadata: {
                toolName: "store_reasoning_memory",
                skipped: true,
                reason: "quality_threshold",
                qualityScore: evaluation.qualityScore
              }
            };
          }
          if (!context?.services) {
            logger.warn("StoreReasoningMemory: No services context available");
            return {
              success: false,
              result: {
                error: "Services context is required for reasoning storage",
                stored: false
              },
              metadata: {
                toolName: "store_reasoning_memory",
                missingServices: true
              }
            };
          }
          const embeddingManager = context.services.embeddingManager;
          const vectorStoreManager = context.services.vectorStoreManager;
          if (!embeddingManager || !vectorStoreManager) {
            logger.warn("StoreReasoningMemory: Required services not available");
            return {
              success: false,
              result: {
                error: "EmbeddingManager and VectorStoreManager are required for reasoning storage",
                stored: false
              },
              metadata: {
                toolName: "store_reasoning_memory",
                missingEmbeddingManager: !embeddingManager,
                missingVectorStoreManager: !vectorStoreManager
              }
            };
          }
          let reflectionStore = null;
          try {
            logger.debug("StoreReasoningMemory: Using dedicated reflection collection");
            reflectionStore = vectorStoreManager.getStore("reflection");
          } catch (error) {
            logger.error("StoreReasoningMemory: Failed to get reflection store", {
              error: error instanceof Error ? error.message : String(error),
              hasVectorStoreManager: !!vectorStoreManager
            });
            if (typeof vectorStoreManager.getStore === "function") {
              reflectionStore = vectorStoreManager.getStore();
            }
          }
          if (!reflectionStore) {
            logger.warn("StoreReasoningMemory: Reflection store not available");
            if (typeof vectorStoreManager.getStore === "function") {
              reflectionStore = vectorStoreManager.getStore();
            }
            if (!reflectionStore) {
              return {
                success: false,
                result: {
                  error: "Reflection vector store not available",
                  stored: false
                },
                metadata: {
                  toolName: "store_reasoning_memory",
                  reflectionStoreUnavailable: true
                }
              };
            }
          }
          const embedder = embeddingManager.getEmbedder("default");
          if (!embedder) {
            logger.warn("StoreReasoningMemory: Embedder not available");
            return {
              success: false,
              result: {
                error: "Embedder not available for reasoning storage",
                stored: false
              },
              metadata: {
                toolName: "store_reasoning_memory",
                embedderUnavailable: true
              }
            };
          }
          if (!embedder.embed || typeof embedder.embed !== "function") {
            throw new Error("Embedder is not properly initialized or missing embed() method");
          }
          const vectorId = generateSafeReasoningMemoryId(0);
          const searchableContent = [
            // Core reasoning steps (most important for similarity search)
            ...validSteps.map((step) => `${step.type}: ${step.content}`),
            // Quality score for filtering
            `Quality: ${evaluation.qualityScore.toFixed(2)}`
          ].join(" ");
          let contextString = "";
          if (trace.metadata?.taskContext?.goal) {
            contextString = String(trace.metadata.taskContext.goal);
          } else if (trace.metadata?.taskContext?.input) {
            contextString = String(trace.metadata.taskContext.input);
          } else if (typeof args.userInput === "string") {
            contextString = args.userInput.slice(0, 100);
          } else {
            contextString = "No context provided";
          }
          logger.debug("StoreReasoningMemory: Generating embedding for unified reasoning content", {
            traceId: trace.id,
            vectorId,
            contentLength: searchableContent.length,
            stepCount: validSteps.length,
            qualityScore: evaluation.qualityScore
          });
          let embedding;
          try {
            embedding = await embedder.embed(searchableContent);
          } catch (embedError) {
            logger.error(
              "StoreReasoningMemory: Failed to generate embedding, disabling embeddings globally",
              {
                error: embedError instanceof Error ? embedError.message : String(embedError),
                contentLength: searchableContent.length,
                provider: embedder.getConfig().type
              }
            );
            if (context?.services?.embeddingManager && embedError instanceof Error) {
              context.services.embeddingManager.handleRuntimeFailure(
                embedError,
                embedder.getConfig().type
              );
            }
            return {
              success: false,
              message: `Embeddings disabled due to failure: ${embedError instanceof Error ? embedError.message : String(embedError)}`,
              mode: "chat-only",
              error: embedError instanceof Error ? embedError.message : String(embedError),
              metadata: {
                toolName: "store_reasoning_memory",
                embeddingDisabled: true
              }
            };
          }
          const safeQualityScore = typeof evaluation.qualityScore === "number" ? evaluation.qualityScore : parseFloat(String(evaluation.qualityScore)) || 0.5;
          if (isNaN(safeQualityScore) || !isFinite(safeQualityScore)) {
            logger.warn("StoreReasoningMemory: Invalid quality score, using fallback", {
              originalValue: evaluation.qualityScore,
              originalType: typeof evaluation.qualityScore,
              fallbackValue: 0.5
            });
          }
          const payload = createReasoningPayload(
            vectorId,
            String(searchableContent),
            validSteps,
            // Raw reasoning steps
            {
              qualityScore: Math.max(0, Math.min(1, safeQualityScore)),
              issues: evaluation.issues,
              suggestions: evaluation.suggestions
            },
            contextString,
            {
              sourceSessionId: trace.metadata.sessionId || context?.sessionId
            }
          );
          logger.debug("StoreReasoningMemory: Pre-insert validation and analysis", {
            traceId: trace.id,
            vectorId,
            vectorIdType: typeof vectorId,
            vectorIdIsInteger: Number.isInteger(vectorId),
            vectorIdRange: `${vectorId} (should be 666667-999999)`,
            vectorIdValid: Number.isInteger(vectorId) && vectorId >= 666667 && vectorId <= 999999,
            embeddingType: typeof embedding,
            embeddingIsArray: Array.isArray(embedding),
            embeddingLength: Array.isArray(embedding) ? embedding.length : "not-array",
            embeddingFirstFew: Array.isArray(embedding) ? embedding.slice(0, 3) : "not-array",
            payloadType: typeof payload,
            payloadKeys: Object.keys(payload),
            payloadSize: JSON.stringify(payload).length,
            payloadIdType: typeof payload.id,
            payloadIdValue: payload.id,
            contentLength: searchableContent.length,
            stepCount: validSteps.length
          });
          if (!Array.isArray(embedding)) {
            const errorMsg = `Invalid embedding: expected array, got ${typeof embedding}`;
            logger.error("StoreReasoningMemory: Embedding validation failed", {
              error: errorMsg,
              embeddingType: typeof embedding,
              embeddingValue: embedding
            });
            throw new Error(errorMsg);
          }
          if (embedding.length === 0) {
            const errorMsg = "Invalid embedding: array is empty";
            logger.error("StoreReasoningMemory: Embedding validation failed", {
              error: errorMsg,
              embeddingLength: embedding.length
            });
            throw new Error(errorMsg);
          }
          const invalidEmbeddingElements = embedding.filter(
            (val, _idx) => typeof val !== "number" || isNaN(val) || !isFinite(val)
          );
          if (invalidEmbeddingElements.length > 0) {
            const errorMsg = `Invalid embedding: contains ${invalidEmbeddingElements.length} non-numeric/invalid values`;
            logger.error("StoreReasoningMemory: Embedding validation failed", {
              error: errorMsg,
              invalidCount: invalidEmbeddingElements.length,
              firstInvalid: invalidEmbeddingElements.slice(0, 3),
              embeddingLength: embedding.length
            });
            throw new Error(errorMsg);
          }
          if (!Number.isInteger(vectorId)) {
            const errorMsg = `Invalid vector ID: expected integer, got ${typeof vectorId} (${vectorId})`;
            logger.error("StoreReasoningMemory: Vector ID validation failed", {
              error: errorMsg,
              vectorId,
              vectorIdType: typeof vectorId,
              isInteger: Number.isInteger(vectorId)
            });
            throw new Error(errorMsg);
          }
          if (vectorId <= 0) {
            const errorMsg = `Invalid vector ID: must be positive, got ${vectorId}`;
            logger.error("StoreReasoningMemory: Vector ID validation failed", {
              error: errorMsg,
              vectorId,
              isPositive: vectorId > 0
            });
            throw new Error(errorMsg);
          }
          if (vectorId < 666667 || vectorId > 999999) {
            logger.warn("StoreReasoningMemory: Vector ID outside expected range", {
              vectorId,
              expectedRange: "666667-999999",
              actualRange: `${vectorId}`,
              willProceed: true
            });
          }
          if (typeof payload !== "object" || payload === null) {
            const errorMsg = `Invalid payload: expected object, got ${typeof payload}`;
            logger.error("StoreReasoningMemory: Payload validation failed", {
              error: errorMsg,
              payloadType: typeof payload,
              payload
            });
            throw new Error(errorMsg);
          }
          if (payload.id !== vectorId) {
            const errorMsg = `Payload ID mismatch: payload.id=${payload.id}, vectorId=${vectorId}`;
            logger.error("StoreReasoningMemory: Payload ID validation failed", {
              error: errorMsg,
              payloadId: payload.id,
              vectorId,
              payloadIdType: typeof payload.id,
              vectorIdType: typeof vectorId
            });
            throw new Error(errorMsg);
          }
          let collectionExists = false;
          let collectionInfo = null;
          let collectionCreated = false;
          try {
            const reflectionCollectionName = env.REFLECTION_VECTOR_STORE_COLLECTION;
            if (typeof reflectionStore.getCollectionInfo === "function") {
              collectionInfo = await reflectionStore.getCollectionInfo();
              collectionExists = true;
              logger.debug("StoreReasoningMemory: Collection exists and accessible", {
                collectionName: reflectionCollectionName,
                vectorsCount: collectionInfo?.vectors_count || "unknown",
                status: collectionInfo?.status || "unknown"
              });
            } else if (typeof reflectionStore.client?.getCollection === "function") {
              const collection = await reflectionStore.client.getCollection(
                reflectionCollectionName
              );
              collectionExists = true;
              collectionInfo = collection;
              logger.debug("StoreReasoningMemory: Collection verified via direct client", {
                collectionName: reflectionCollectionName,
                config: collection?.result?.config || "unknown"
              });
            } else if (typeof reflectionStore.isConnected === "function") {
              collectionExists = reflectionStore.isConnected();
              logger.debug("StoreReasoningMemory: Collection status via isConnected", {
                collectionName: reflectionCollectionName,
                connected: collectionExists
              });
            }
          } catch (collectionError) {
            logger.error(
              "StoreReasoningMemory: Collection verification failed - attempting auto-creation",
              {
                error: collectionError instanceof Error ? collectionError.message : String(collectionError),
                collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION,
                errorType: collectionError instanceof Error ? collectionError.constructor.name : typeof collectionError
              }
            );
            try {
              console.error("=== COLLECTION AUTO-CREATION ATTEMPT ===");
              console.error(
                "Collection Error:",
                collectionError instanceof Error ? collectionError.message : String(collectionError)
              );
              console.error("Attempting to create collection:", env.REFLECTION_VECTOR_STORE_COLLECTION);
              const dualManager = vectorStoreManager;
              if (dualManager && typeof dualManager.getManager === "function") {
                const reflectionManager = dualManager.getManager("reflection");
                if (reflectionManager && typeof reflectionManager.connect === "function") {
                  console.error("Attempting to reconnect reflection manager...");
                  await reflectionManager.connect();
                  collectionCreated = true;
                  collectionExists = true;
                  console.error("Collection creation/reconnection successful!");
                }
              }
              console.error("======================================");
            } catch (creationError) {
              console.error("=== COLLECTION CREATION FAILED ===");
              console.error(
                "Creation Error:",
                creationError instanceof Error ? creationError.message : String(creationError)
              );
              console.error("Root cause: Collection does not exist and cannot be created");
              console.error("==================================");
              throw new Error(
                `Reflection collection '${env.REFLECTION_VECTOR_STORE_COLLECTION}' does not exist and cannot be created: ${creationError instanceof Error ? creationError.message : String(creationError)}`
              );
            }
          }
          const storeInfo = {
            storeType: typeof reflectionStore,
            hasInsertMethod: typeof reflectionStore.insert === "function",
            storeConstructor: reflectionStore.constructor?.name,
            isConnected: typeof reflectionStore.isConnected === "function" ? reflectionStore.isConnected() : "unknown",
            collectionExists,
            collectionCreated,
            collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION,
            collectionInfo: collectionInfo ? "available" : "not_available"
          };
          logger.debug("StoreReasoningMemory: Reflection store analysis", {
            traceId: trace.id,
            vectorId,
            storeInfo,
            embeddingLength: embedding.length,
            payloadKeys: Object.keys(payload).length,
            payloadSummary: {
              id: payload.id,
              qualityScore: payload.evaluation.qualityScore,
              stepCount: payload.stepCount,
              textLength: payload.text?.length || 0,
              tagsCount: Array.isArray(payload.tags) ? payload.tags.length : "not_array"
            }
          });
          logger.debug("StoreReasoningMemory: Attempting vector store insert", {
            traceId: trace.id,
            vectorId,
            operation: "reflectionStore.insert",
            parameters: {
              embeddings: `Array[${embedding.length}]`,
              ids: `[${vectorId}]`,
              payloads: `[Object with ${Object.keys(payload).length} keys]`
            }
          });
          try {
            await reflectionStore.insert([embedding], [vectorId], [payload]);
            logger.debug("StoreReasoningMemory: Vector store insert successful", {
              traceId: trace.id,
              vectorId,
              insertedSuccessfully: true,
              embeddingLength: embedding.length,
              payloadSize: JSON.stringify(payload).length
            });
            logger.debug("StoreReasoningMemory: ADD operation completed", {
              memoryId: vectorId,
              textPreview: searchableContent.substring(0, 60) + (searchableContent.length > 60 ? "..." : ""),
              stepCount: validSteps.length,
              qualityScore: evaluation.qualityScore.toFixed(3)
            });
          } catch (insertError) {
            console.error("=== EMERGENCY DIAGNOSTIC ===");
            console.error("Vector Store Insert Failed - Full Analysis:");
            console.error(
              "VectorId:",
              vectorId,
              "Type:",
              typeof vectorId,
              "IsInteger:",
              Number.isInteger(vectorId)
            );
            console.error(
              "Embedding Length:",
              Array.isArray(embedding) ? embedding.length : "NOT_ARRAY"
            );
            console.error("Payload Keys:", Object.keys(payload));
            console.error("Payload.id:", payload.id, "Type:", typeof payload.id);
            console.error("Store Type:", reflectionStore.constructor?.name);
            console.error("Collection Name:", env.REFLECTION_VECTOR_STORE_COLLECTION);
            console.error("Collection Exists:", collectionExists);
            console.error("Collection Created:", collectionCreated);
            console.error("Expected Vector Dimension:", env.VECTOR_STORE_DIMENSION);
            console.error(
              "Actual Embedding Dimension:",
              Array.isArray(embedding) ? embedding.length : "NOT_ARRAY"
            );
            console.error(
              "Dimension Match:",
              Array.isArray(embedding) && embedding.length === env.VECTOR_STORE_DIMENSION
            );
            console.error(
              "Insert Error:",
              insertError instanceof Error ? insertError.message : String(insertError)
            );
            const underlyingError = insertError?.cause || insertError?.originalError;
            if (underlyingError) {
              console.error(
                "Underlying Qdrant Error:",
                underlyingError instanceof Error ? underlyingError.message : String(underlyingError)
              );
              console.error(
                "Qdrant Error Stack:",
                underlyingError instanceof Error ? underlyingError.stack : "No stack available"
              );
            }
            console.error(
              "Full Error Object:",
              JSON.stringify(
                {
                  message: insertError instanceof Error ? insertError.message : String(insertError),
                  name: insertError?.name,
                  cause: insertError?.cause?.message || "No cause",
                  stack: insertError instanceof Error ? insertError.stack?.split("\n").slice(0, 3).join("\n") : "No stack"
                },
                null,
                2
              )
            );
            console.error("===========================");
            const detailedError = insertError instanceof Error ? insertError.message : String(insertError);
            const errorStack = insertError instanceof Error ? insertError.stack : void 0;
            logger.debug("StoreReasoningMemory: Detailed insert failure analysis", {
              traceId: trace.id,
              vectorId,
              errorType: insertError instanceof Error ? insertError.constructor.name : typeof insertError,
              errorMessage: detailedError,
              errorStack: errorStack?.split("\n").slice(0, 5).join("\n"),
              // First 5 lines of stack
              insertCallParameters: {
                embeddingsParam: {
                  type: typeof [embedding],
                  isArray: Array.isArray([embedding]),
                  length: [embedding].length,
                  firstElementType: typeof embedding,
                  firstElementIsArray: Array.isArray(embedding),
                  firstElementLength: Array.isArray(embedding) ? embedding.length : "not-array"
                },
                idsParam: {
                  type: typeof [vectorId],
                  isArray: Array.isArray([vectorId]),
                  length: [vectorId].length,
                  firstElement: vectorId,
                  firstElementType: typeof vectorId,
                  firstElementIsInteger: Number.isInteger(vectorId)
                },
                payloadsParam: {
                  type: typeof [payload],
                  isArray: Array.isArray([payload]),
                  length: [payload].length,
                  firstElement: payload,
                  firstElementType: typeof payload,
                  firstElementKeys: Object.keys(payload)
                }
              },
              reflectionStoreInfo: storeInfo,
              possibleCauses: [
                "Qdrant collection not found or not accessible",
                "Vector dimension mismatch",
                "Payload contains invalid field types",
                "Connection timeout or network issue",
                "Insufficient permissions or API key issues",
                "Collection schema validation failed"
              ]
            });
            logger.error(
              "StoreReasoningMemory: Vector store insert failed with comprehensive details",
              {
                traceId: trace.id,
                vectorId,
                vectorIdValid: Number.isInteger(vectorId) && vectorId > 0,
                vectorIdInRange: vectorId >= 666667 && vectorId <= 999999,
                embeddingValid: Array.isArray(embedding) && embedding.length > 0,
                embeddingLength: Array.isArray(embedding) ? embedding.length : "not-array",
                embeddingHasValidNumbers: Array.isArray(embedding) && embedding.every((x) => typeof x === "number" && isFinite(x)),
                payloadValid: typeof payload === "object" && payload !== null,
                payloadSize: JSON.stringify(payload).length,
                payloadIdMatches: payload.id === vectorId,
                originalError: detailedError,
                errorType: insertError instanceof Error ? insertError.constructor.name : typeof insertError,
                storeConnected: storeInfo.isConnected
              }
            );
            throw new Error(`Vector store insert failed: ${detailedError}`);
          }
          const processingTime = Date.now() - startTime;
          logger.debug(
            "StoreReasoningMemory: Successfully stored unified reasoning entry with task context",
            {
              traceId: trace.id,
              vectorId,
              stepCount: validSteps.length,
              qualityScore: evaluation.qualityScore.toFixed(3),
              issueCount: evaluation.issues.length,
              suggestionCount: evaluation.suggestions.length,
              taskType: trace.metadata.taskContext?.taskType,
              domain: trace.metadata.taskContext?.domain,
              processingTime: `${processingTime}ms`
            }
          );
          return {
            success: true,
            result: {
              message: "Reasoning trace with task context and evaluation stored successfully",
              stored: true,
              traceId: trace.id,
              vectorId,
              taskContext: trace.metadata.taskContext,
              metrics: {
                stepCount: validSteps.length,
                qualityScore: evaluation.qualityScore,
                issueCount: evaluation.issues.length
              }
            },
            metadata: {
              toolName: "store_reasoning_memory",
              traceId: trace.id,
              vectorId,
              processingTime,
              hasTaskContext: !!trace.metadata.taskContext
            }
          };
        } catch (error) {
          const processingTime = Date.now() - startTime;
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("StoreReasoningMemory: Failed to store unified reasoning entry", {
            error: errorMessage,
            stepCount: trace?.steps?.length || 0,
            qualityScore: evaluation?.qualityScore,
            processingTime: `${processingTime}ms`
          });
          return {
            success: false,
            result: {
              error: `Failed to store reasoning: ${errorMessage}`,
              stored: false
            },
            metadata: {
              toolName: "store_reasoning_memory",
              error: errorMessage,
              processingTime
            }
          };
        }
      }
    };
  }
});

// src/core/brain/embedding/backend/types.ts
var EmbeddingError, EmbeddingConnectionError, EmbeddingDimensionError, EmbeddingRateLimitError, EmbeddingQuotaError, EmbeddingValidationError;
var init_types8 = __esm({
  "src/core/brain/embedding/backend/types.ts"() {
    init_cjs_shims();
    EmbeddingError = class extends Error {
      provider;
      cause;
      constructor(message, provider, cause) {
        super(message);
        this.name = "EmbeddingError";
        if (provider !== void 0) {
          this.provider = provider;
        }
        if (cause !== void 0) {
          this.cause = cause;
        }
      }
    };
    EmbeddingConnectionError = class extends EmbeddingError {
      constructor(message, provider, cause) {
        super(message, provider, cause);
        this.name = "EmbeddingConnectionError";
      }
    };
    EmbeddingDimensionError = class extends EmbeddingError {
      constructor(message, expected, actual, provider) {
        super(message, provider);
        this.expected = expected;
        this.actual = actual;
        this.name = "EmbeddingDimensionError";
      }
    };
    EmbeddingRateLimitError = class extends EmbeddingError {
      constructor(message, retryAfter, provider, cause) {
        super(message, provider, cause);
        this.retryAfter = retryAfter;
        this.name = "EmbeddingRateLimitError";
      }
    };
    EmbeddingQuotaError = class extends EmbeddingError {
      constructor(message, provider, cause) {
        super(message, provider, cause);
        this.name = "EmbeddingQuotaError";
      }
    };
    EmbeddingValidationError = class extends EmbeddingError {
      constructor(message, provider, cause) {
        super(message, provider, cause);
        this.name = "EmbeddingValidationError";
      }
    };
  }
});

// src/core/brain/embedding/types.ts
var init_types9 = __esm({
  "src/core/brain/embedding/types.ts"() {
    init_cjs_shims();
    init_types8();
  }
});

// src/core/brain/embedding/config.ts
function parseEmbeddingConfigFromEnv() {
  if (process.env.OPENAI_API_KEY) {
    return {
      type: "openai",
      apiKey: process.env.OPENAI_API_KEY,
      baseUrl: process.env.OPENAI_BASE_URL,
      model: process.env.OPENAI_EMBEDDING_MODEL || "text-embedding-3-small"
    };
  }
  if (process.env.GEMINI_API_KEY) {
    return {
      type: "gemini",
      apiKey: process.env.GEMINI_API_KEY,
      baseUrl: process.env.GEMINI_BASE_URL,
      model: process.env.GEMINI_EMBEDDING_MODEL || "gemini-embedding-001"
    };
  }
  if (process.env.QWEN_API_KEY || process.env.DASHSCOPE_API_KEY) {
    return {
      type: "qwen",
      apiKey: process.env.QWEN_API_KEY || process.env.DASHSCOPE_API_KEY,
      baseUrl: process.env.QWEN_BASE_URL,
      model: process.env.QWEN_EMBEDDING_MODEL || "text-embedding-v3"
    };
  }
  if (process.env.AWS_ACCESS_KEY_ID && process.env.AWS_SECRET_ACCESS_KEY) {
    return {
      type: "aws-bedrock",
      region: process.env.AWS_DEFAULT_REGION || "us-east-1",
      accessKeyId: process.env.AWS_ACCESS_KEY_ID,
      secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY,
      sessionToken: process.env.AWS_SESSION_TOKEN,
      model: process.env.AWS_BEDROCK_EMBEDDING_MODEL || "amazon.titan-embed-text-v2:0"
    };
  }
  if (process.env.OLLAMA_BASE_URL) {
    return {
      type: "ollama",
      baseUrl: process.env.OLLAMA_BASE_URL,
      model: process.env.OLLAMA_EMBEDDING_MODEL || "nomic-embed-text"
    };
  }
  if (process.env.LMSTUDIO_BASE_URL) {
    return {
      type: "lmstudio",
      baseUrl: process.env.LMSTUDIO_BASE_URL,
      model: process.env.LMSTUDIO_EMBEDDING_MODEL || "nomic-embed-text-v1.5"
    };
  }
  return null;
}
var OpenAIEmbeddingConfigSchema, GeminiEmbeddingConfigSchema, OllamaEmbeddingConfigSchema, VoyageEmbeddingConfigSchema, QwenEmbeddingConfigSchema, AWSBedrockEmbeddingConfigSchema, LMStudioEmbeddingConfigSchema, EmbeddingConfigSchema;
var init_config4 = __esm({
  "src/core/brain/embedding/config.ts"() {
    init_cjs_shims();
    init_zod();
    OpenAIEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("openai"),
      apiKey: external_exports.string().optional(),
      model: external_exports.enum(["text-embedding-3-small", "text-embedding-3-large", "text-embedding-ada-002"]).default("text-embedding-3-small"),
      baseUrl: external_exports.string().optional(),
      organization: external_exports.string().optional(),
      dimensions: external_exports.number().optional(),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    GeminiEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("gemini"),
      apiKey: external_exports.string().optional(),
      model: external_exports.enum(["gemini-embedding-001", "text-embedding-004"]).default("gemini-embedding-001"),
      baseUrl: external_exports.string().optional(),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    OllamaEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("ollama"),
      baseUrl: external_exports.string().default("http://localhost:11434"),
      model: external_exports.string().default("nomic-embed-text"),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    VoyageEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("voyage"),
      apiKey: external_exports.string().optional(),
      model: external_exports.enum(["voyage-3-large", "voyage-3", "voyage-2"]).default("voyage-3-large"),
      baseUrl: external_exports.string().optional(),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    QwenEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("qwen"),
      apiKey: external_exports.string().optional(),
      model: external_exports.enum(["text-embedding-v3"]).default("text-embedding-v3"),
      baseUrl: external_exports.string().optional(),
      dimensions: external_exports.number().refine((val) => [1024, 768, 512].includes(val)).default(1024),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    AWSBedrockEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("aws-bedrock"),
      model: external_exports.enum(["amazon.titan-embed-text-v2:0", "cohere.embed-english-v3"]).default("amazon.titan-embed-text-v2:0"),
      region: external_exports.string().optional(),
      accessKeyId: external_exports.string().optional(),
      secretAccessKey: external_exports.string().optional(),
      sessionToken: external_exports.string().optional(),
      dimensions: external_exports.number().refine((val) => [1024, 512, 256].includes(val)).default(1024),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    LMStudioEmbeddingConfigSchema = external_exports.object({
      type: external_exports.literal("lmstudio"),
      baseUrl: external_exports.string().default("http://localhost:1234/v1"),
      model: external_exports.string().default("nomic-embed-text-v1.5"),
      dimensions: external_exports.number().optional(),
      timeout: external_exports.number().default(3e4),
      maxRetries: external_exports.number().default(3)
    });
    EmbeddingConfigSchema = external_exports.union([
      OpenAIEmbeddingConfigSchema,
      GeminiEmbeddingConfigSchema,
      OllamaEmbeddingConfigSchema,
      VoyageEmbeddingConfigSchema,
      QwenEmbeddingConfigSchema,
      AWSBedrockEmbeddingConfigSchema,
      LMStudioEmbeddingConfigSchema
    ]);
  }
});

// src/core/brain/embedding/constants.ts
var OPENAI_MODELS, GEMINI_MODELS, OLLAMA_MODELS, VOYAGE_MODELS, QWEN_MODELS, AWS_BEDROCK_MODELS, LMSTUDIO_MODELS, MODEL_DIMENSIONS, MODEL_INPUT_LIMITS, DEFAULTS, RETRY_CONFIG, VALIDATION_LIMITS, ERROR_MESSAGES3, LOG_PREFIXES3, HTTP_STATUS;
var init_constants3 = __esm({
  "src/core/brain/embedding/constants.ts"() {
    init_cjs_shims();
    OPENAI_MODELS = {
      /** Latest small embedding model (1536 dimensions) */
      TEXT_EMBEDDING_3_SMALL: "text-embedding-3-small",
      /** Latest large embedding model (3072 dimensions) */
      TEXT_EMBEDDING_3_LARGE: "text-embedding-3-large",
      /** Legacy Ada v2 model (1536 dimensions) */
      TEXT_EMBEDDING_ADA_002: "text-embedding-ada-002"
    };
    GEMINI_MODELS = {
      /** Latest Gemini embedding model (768, 1536, or 3072 dimensions) */
      TEXT_EMBEDDING_004: "text-embedding-004",
      /** Gemini embedding model (768, 1536, or 3072 dimensions) */
      GEMINI_EMBEDDING_001: "gemini-embedding-001",
      /** Previous Gemini embedding model (768 dimensions) */
      EMBEDDING_001: "embedding-001"
    };
    OLLAMA_MODELS = {
      /** Nomic Embed text model */
      NOMIC_EMBED_TEXT: "nomic-embed-text",
      /** All-MiniLM model */
      ALL_MINILM: "all-minilm",
      /** MXBAI Embed Large */
      MXBAI_EMBED_LARGE: "mxbai-embed-large"
    };
    VOYAGE_MODELS = {
      /** Latest Voyage 3 large model (1024 dimensions) */
      VOYAGE_3_LARGE: "voyage-3-large",
      /** Standard Voyage 3 model (1024 dimensions) */
      VOYAGE_3: "voyage-3",
      /** Previous Voyage 2 model (1024 dimensions) */
      VOYAGE_2: "voyage-2"
    };
    QWEN_MODELS = {
      /** Qwen multilingual text embedding model (1024/768/512 dimensions) */
      TEXT_EMBEDDING_V3: "text-embedding-v3"
    };
    AWS_BEDROCK_MODELS = {
      /** Amazon Titan Text Embeddings V2 (1024/512/256 dimensions) */
      TITAN_EMBED_TEXT_V2: "amazon.titan-embed-text-v2:0",
      /** Cohere Embed English V3 (1024 dimensions) */
      COHERE_EMBED_ENGLISH_V3: "cohere.embed-english-v3"
    };
    LMSTUDIO_MODELS = {
      /** Nomic Embed Text v1.5 (768 dimensions) */
      NOMIC_EMBED_TEXT_V1_5: "nomic-embed-text-v1.5",
      /** Alternative Nomic model name */
      TEXT_EMBEDDING_NOMIC_EMBED_TEXT_V1_5: "text-embedding-nomic-embed-text-v1.5",
      /** BGE Large (1024 dimensions) */
      BGE_LARGE: "bge-large",
      /** BGE Base (768 dimensions) */
      BGE_BASE: "bge-base",
      /** BGE Small (384 dimensions) */
      BGE_SMALL: "bge-small",
      /** All-MiniLM (384 dimensions) */
      ALL_MINILM: "all-minilm"
    };
    MODEL_DIMENSIONS = {
      [OPENAI_MODELS.TEXT_EMBEDDING_3_SMALL]: 1536,
      [OPENAI_MODELS.TEXT_EMBEDDING_3_LARGE]: 3072,
      [OPENAI_MODELS.TEXT_EMBEDDING_ADA_002]: 1536,
      [GEMINI_MODELS.TEXT_EMBEDDING_004]: 768,
      // Default, configurable up to 3072
      [GEMINI_MODELS.GEMINI_EMBEDDING_001]: 768,
      // Default, configurable up to 3072
      [GEMINI_MODELS.EMBEDDING_001]: 768,
      [OLLAMA_MODELS.NOMIC_EMBED_TEXT]: 768,
      [OLLAMA_MODELS.ALL_MINILM]: 384,
      [OLLAMA_MODELS.MXBAI_EMBED_LARGE]: 1024,
      [VOYAGE_MODELS.VOYAGE_3_LARGE]: 1024,
      [VOYAGE_MODELS.VOYAGE_3]: 1024,
      [VOYAGE_MODELS.VOYAGE_2]: 1024,
      [QWEN_MODELS.TEXT_EMBEDDING_V3]: 1024,
      // Default, configurable to 768 or 512
      [AWS_BEDROCK_MODELS.TITAN_EMBED_TEXT_V2]: 1024,
      // Default, configurable to 512 or 256
      [AWS_BEDROCK_MODELS.COHERE_EMBED_ENGLISH_V3]: 1024,
      [LMSTUDIO_MODELS.NOMIC_EMBED_TEXT_V1_5]: 768,
      [LMSTUDIO_MODELS.TEXT_EMBEDDING_NOMIC_EMBED_TEXT_V1_5]: 768,
      [LMSTUDIO_MODELS.BGE_LARGE]: 1024,
      [LMSTUDIO_MODELS.BGE_BASE]: 768,
      [LMSTUDIO_MODELS.BGE_SMALL]: 384
      // Note: ALL_MINILM already defined in OLLAMA_MODELS with same dimensions
    };
    MODEL_INPUT_LIMITS = {
      [OPENAI_MODELS.TEXT_EMBEDDING_3_SMALL]: 8191,
      // tokens
      [OPENAI_MODELS.TEXT_EMBEDDING_3_LARGE]: 8191,
      // tokens
      [OPENAI_MODELS.TEXT_EMBEDDING_ADA_002]: 8191
      // tokens
    };
    DEFAULTS = {
      /** Default OpenAI model */
      OPENAI_MODEL: OPENAI_MODELS.TEXT_EMBEDDING_3_SMALL,
      /** Default Gemini model */
      GEMINI_MODEL: GEMINI_MODELS.GEMINI_EMBEDDING_001,
      /** Default Ollama model */
      OLLAMA_MODEL: OLLAMA_MODELS.NOMIC_EMBED_TEXT,
      /** Default LM Studio model */
      LMSTUDIO_MODEL: LMSTUDIO_MODELS.NOMIC_EMBED_TEXT_V1_5,
      /** Default request timeout in milliseconds */
      TIMEOUT: 3e4,
      // 30 seconds
      /** Default maximum retry attempts */
      MAX_RETRIES: 3,
      /** Default batch size for batch operations */
      BATCH_SIZE: 100,
      /** Default OpenAI API base URL */
      OPENAI_BASE_URL: "https://api.openai.com/v1",
      /** Default Gemini API base URL */
      GEMINI_BASE_URL: "https://generativelanguage.googleapis.com/v1beta",
      /** Default Ollama API base URL */
      OLLAMA_BASE_URL: "http://localhost:11434/api",
      /** Default LM Studio API base URL */
      LMSTUDIO_BASE_URL: "http://localhost:1234/v1",
      /** Default embedding dimension */
      DIMENSION: MODEL_DIMENSIONS[OPENAI_MODELS.TEXT_EMBEDDING_3_SMALL]
    };
    RETRY_CONFIG = {
      /** Initial retry delay in milliseconds */
      INITIAL_DELAY: 1e3,
      /** Maximum retry delay in milliseconds */
      MAX_DELAY: 6e4,
      /** Backoff multiplier for exponential backoff */
      BACKOFF_MULTIPLIER: 2,
      /** Jitter factor for randomizing retry delays */
      JITTER_FACTOR: 0.1
    };
    VALIDATION_LIMITS = {
      /** Maximum text length for single embedding */
      MAX_TEXT_LENGTH: 32768,
      // characters
      /** Maximum number of texts in batch operation */
      MAX_BATCH_SIZE: 2048,
      /** Minimum text length (empty strings not allowed) */
      MIN_TEXT_LENGTH: 1
    };
    ERROR_MESSAGES3 = {
      PROVIDER_NOT_SUPPORTED: (provider) => `Embedding provider '${provider}' is not supported`,
      MODEL_NOT_SUPPORTED: (model, provider) => `Model '${model}' is not supported by provider '${provider}'`,
      API_KEY_REQUIRED: (provider) => `API key is required for provider '${provider}'`,
      CONNECTION_FAILED: (provider) => `Failed to connect to ${provider} embedding service`,
      TEXT_TOO_LONG: (length, max) => `Text length ${length} exceeds maximum of ${max} characters`,
      BATCH_TOO_LARGE: (size, max) => `Batch size ${size} exceeds maximum of ${max} items`,
      EMPTY_TEXT: "Text cannot be empty",
      DIMENSION_MISMATCH: (expected, actual) => `Expected embedding dimension ${expected}, but got ${actual}`,
      RATE_LIMIT_EXCEEDED: "Rate limit exceeded for embedding provider",
      QUOTA_EXCEEDED: "API quota exceeded for embedding provider",
      INVALID_API_KEY: (provider) => `Invalid API key for provider '${provider}'`,
      REQUEST_TIMEOUT: (timeout) => `Request timed out after ${timeout}ms`
    };
    LOG_PREFIXES3 = {
      EMBEDDING: "[EMBEDDING]",
      OPENAI: "[EMBEDDING:OPENAI]",
      GEMINI: "[EMBEDDING:GEMINI]",
      OLLAMA: "[EMBEDDING:OLLAMA]",
      VOYAGE: "[EMBEDDING:VOYAGE]",
      QWEN: "[EMBEDDING:QWEN]",
      AWS_BEDROCK: "[EMBEDDING:AWS-BEDROCK]",
      LMSTUDIO: "[EMBEDDING:LMSTUDIO]",
      FACTORY: "[EMBEDDING:FACTORY]",
      MANAGER: "[EMBEDDING:MANAGER]",
      HEALTH: "[EMBEDDING:HEALTH]",
      BATCH: "[EMBEDDING:BATCH]",
      CIRCUIT_BREAKER: "[EMBEDDING:CIRCUIT-BREAKER]",
      FALLBACK: "[EMBEDDING:FALLBACK]"
    };
    HTTP_STATUS = {
      OK: 200,
      BAD_REQUEST: 400,
      UNAUTHORIZED: 401,
      FORBIDDEN: 403,
      NOT_FOUND: 404,
      TOO_MANY_REQUESTS: 429,
      INTERNAL_SERVER_ERROR: 500,
      SERVICE_UNAVAILABLE: 503
    };
  }
});

// src/core/brain/embedding/backend/openai.ts
var import_openai7, OpenAIEmbedder;
var init_openai4 = __esm({
  "src/core/brain/embedding/backend/openai.ts"() {
    init_cjs_shims();
    import_openai7 = __toESM(require("openai"), 1);
    init_logger2();
    init_types8();
    init_constants3();
    OpenAIEmbedder = class {
      openai;
      config;
      model;
      dimension;
      constructor(config2) {
        this.config = config2;
        this.model = config2.model || "text-embedding-3-small";
        if (!config2.apiKey || config2.apiKey.trim() === "") {
          throw new EmbeddingError("OpenAI API key is required", "openai");
        }
        const openaiConfig = {
          apiKey: config2.apiKey,
          timeout: config2.timeout || 3e4,
          // Default to 30 seconds if not specified
          maxRetries: config2.maxRetries || 3
          // Default to 3 retries if not specified
        };
        if (config2.baseUrl && config2.baseUrl.trim() !== "") {
          openaiConfig.baseURL = config2.baseUrl;
        }
        if (config2.organization && config2.organization.trim() !== "") {
          openaiConfig.organization = config2.organization;
        }
        this.openai = new import_openai7.default(openaiConfig);
        this.dimension = config2.dimensions || MODEL_DIMENSIONS[this.model] || 1536;
        logger.debug(`${LOG_PREFIXES3.OPENAI} Initialized OpenAI embedder`, {
          model: this.model,
          dimension: this.dimension,
          baseUrl: config2.baseUrl,
          hasOrganization: !!config2.organization
        });
      }
      async embed(text) {
        logger.silly(`${LOG_PREFIXES3.OPENAI} Embedding single text`, {
          textLength: text.length,
          model: this.model
        });
        this.validateInput(text);
        const startTime = Date.now();
        try {
          const params = {
            model: this.model,
            input: text
          };
          if (this.config.dimensions !== void 0) {
            params.dimensions = this.config.dimensions;
          }
          const response = await this.createEmbeddingWithRetry(params);
          if (!response.data || !Array.isArray(response.data) || !response.data[0] || !response.data[0].embedding) {
            throw new EmbeddingError("OpenAI API did not return a valid embedding", "openai");
          }
          const embedding = response.data[0].embedding;
          this.validateEmbeddingDimension(embedding);
          return embedding;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.OPENAI} Failed to create embedding`, {
            error: error instanceof Error ? error.message : String(error),
            model: this.model,
            processingTime,
            textLength: text.length
          });
          throw this.handleApiError(error);
        }
      }
      async embedBatch(texts) {
        logger.debug(`${LOG_PREFIXES3.BATCH} Embedding batch of texts`, {
          count: texts.length,
          model: this.model
        });
        this.validateBatchInput(texts);
        const startTime = Date.now();
        try {
          const batchParams = {
            model: this.model,
            input: texts
          };
          if (this.config.dimensions !== void 0) {
            batchParams.dimensions = this.config.dimensions;
          }
          const response = await this.createEmbeddingWithRetry(batchParams);
          const embeddings = response.data.map((item) => item.embedding);
          embeddings.forEach(this.validateEmbeddingDimension.bind(this));
          return embeddings;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.BATCH} Failed to create batch embeddings`, {
            error: error instanceof Error ? error.message : String(error),
            model: this.model,
            processingTime,
            count: texts.length
          });
          throw this.handleApiError(error);
        }
      }
      getDimension() {
        return this.dimension;
      }
      getConfig() {
        return { ...this.config };
      }
      async isHealthy() {
        try {
          logger.silly(`${LOG_PREFIXES3.HEALTH} Checking OpenAI embedder health`);
          await this.embed("test");
          logger.debug(`${LOG_PREFIXES3.HEALTH} OpenAI embedder is healthy`);
          return true;
        } catch (error) {
          logger.warn(`${LOG_PREFIXES3.HEALTH} OpenAI embedder health check failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          return false;
        }
      }
      async disconnect() {
        logger.debug(`${LOG_PREFIXES3.OPENAI} Disconnecting OpenAI embedder`);
      }
      /**
       * Create embedding with retry logic
       */
      async createEmbeddingWithRetry(params) {
        let lastError;
        let delay = RETRY_CONFIG.INITIAL_DELAY;
        for (let attempt = 0; attempt <= this.config.maxRetries; attempt++) {
          try {
            if (attempt > 0) {
              logger.debug(`${LOG_PREFIXES3.OPENAI} Retrying embedding request`, {
                attempt,
                delay,
                maxRetries: this.config.maxRetries
              });
              await new Promise((resolve3) => setTimeout(resolve3, delay));
              delay = Math.min(delay * RETRY_CONFIG.BACKOFF_MULTIPLIER, RETRY_CONFIG.MAX_DELAY);
              const jitter = delay * RETRY_CONFIG.JITTER_FACTOR * Math.random();
              delay = Math.floor(delay + jitter);
            }
            const response = await this.openai.embeddings.create(params);
            if (attempt > 0) {
              logger.info(`${LOG_PREFIXES3.OPENAI} Embedding request succeeded after retry`, {
                attempt,
                model: params.model
              });
            }
            return response;
          } catch (error) {
            lastError = error instanceof Error ? error : new Error(String(error));
            if (!this.shouldRetry(error, attempt)) {
              break;
            }
            logger.warn(`${LOG_PREFIXES3.OPENAI} Embedding request failed, will retry`, {
              attempt: attempt + 1,
              maxRetries: this.config.maxRetries,
              error: lastError.message,
              nextDelay: delay
            });
          }
        }
        throw lastError || new EmbeddingError("Unknown error during embedding request", "openai");
      }
      /**
       * Determine if an error is retryable
       */
      shouldRetry(error, attempt) {
        if (attempt >= this.config.maxRetries) {
          return false;
        }
        if (error && typeof error === "object" && "status" in error) {
          const status = error.status;
          return [
            HTTP_STATUS.TOO_MANY_REQUESTS,
            HTTP_STATUS.INTERNAL_SERVER_ERROR,
            HTTP_STATUS.SERVICE_UNAVAILABLE
          ].includes(status);
        }
        if (error instanceof Error) {
          const message = error.message.toLowerCase();
          return message.includes("network") || message.includes("timeout") || message.includes("connection") || message.includes("econnreset") || message.includes("enotfound");
        }
        return false;
      }
      /**
       * Handle and categorize API errors
       */
      handleApiError(error) {
        if (error && typeof error === "object" && "status" in error) {
          const apiError = error;
          const status = apiError.status;
          const message = apiError.message || String(error);
          switch (status) {
            case HTTP_STATUS.UNAUTHORIZED:
              return new EmbeddingConnectionError(
                ERROR_MESSAGES3.INVALID_API_KEY("OpenAI"),
                "openai",
                apiError
              );
            case HTTP_STATUS.TOO_MANY_REQUESTS: {
              const retryAfter = apiError.headers?.["retry-after"];
              return new EmbeddingRateLimitError(
                ERROR_MESSAGES3.RATE_LIMIT_EXCEEDED,
                retryAfter ? parseInt(retryAfter, 10) : void 0,
                "openai",
                apiError
              );
            }
            case HTTP_STATUS.FORBIDDEN:
              return new EmbeddingQuotaError(ERROR_MESSAGES3.QUOTA_EXCEEDED, "openai", apiError);
            case HTTP_STATUS.BAD_REQUEST:
              return new EmbeddingValidationError(message, "openai", apiError);
            default:
              return new EmbeddingConnectionError(
                ERROR_MESSAGES3.CONNECTION_FAILED("OpenAI"),
                "openai",
                apiError
              );
          }
        }
        if (error instanceof Error) {
          return new EmbeddingConnectionError(error.message, "openai", error);
        }
        return new EmbeddingError(String(error), "openai");
      }
      /**
       * Validate single text input
       */
      validateInput(text) {
        if (!text || text.length < VALIDATION_LIMITS.MIN_TEXT_LENGTH) {
          throw new EmbeddingValidationError(ERROR_MESSAGES3.EMPTY_TEXT, "openai");
        }
        if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
          throw new EmbeddingValidationError(
            ERROR_MESSAGES3.TEXT_TOO_LONG(text.length, VALIDATION_LIMITS.MAX_TEXT_LENGTH),
            "openai"
          );
        }
      }
      /**
       * Validate batch input
       */
      validateBatchInput(texts) {
        if (!Array.isArray(texts) || texts.length === 0) {
          throw new EmbeddingValidationError("Batch input must be a non-empty array", "openai");
        }
        if (texts.length > VALIDATION_LIMITS.MAX_BATCH_SIZE) {
          throw new EmbeddingValidationError(
            ERROR_MESSAGES3.BATCH_TOO_LARGE(texts.length, VALIDATION_LIMITS.MAX_BATCH_SIZE),
            "openai"
          );
        }
        texts.forEach((text, index) => {
          try {
            this.validateInput(text);
          } catch (error) {
            if (error instanceof EmbeddingValidationError) {
              throw new EmbeddingValidationError(
                `Batch item ${index}: ${error.message}`,
                "openai",
                error
              );
            }
            throw error;
          }
        });
      }
      /**
       * Validate embedding dimension
       */
      validateEmbeddingDimension(embedding) {
        if (embedding.length !== this.dimension) {
          throw new EmbeddingDimensionError(
            ERROR_MESSAGES3.DIMENSION_MISMATCH(this.dimension, embedding.length),
            this.dimension,
            embedding.length,
            "openai"
          );
        }
      }
    };
  }
});

// src/core/brain/embedding/backend/gemini.ts
var GeminiEmbedder;
var init_gemini2 = __esm({
  "src/core/brain/embedding/backend/gemini.ts"() {
    init_cjs_shims();
    init_logger2();
    init_types8();
    init_constants3();
    GeminiEmbedder = class {
      config;
      baseUrl;
      apiKey;
      model;
      dimension;
      timeout;
      maxRetries;
      constructor(config2) {
        this.config = config2;
        this.apiKey = config2.apiKey || "";
        this.model = config2.model || "gemini-embedding-001";
        this.dimension = config2.dimensions || 1536;
        this.timeout = config2.timeout || 3e4;
        this.maxRetries = config2.maxRetries || 3;
        this.baseUrl = config2.baseUrl || "https://generativelanguage.googleapis.com/v1beta";
        logger.debug(`${LOG_PREFIXES3.EMBEDDING} Gemini embedder initialized`, {
          model: this.model,
          dimension: this.dimension,
          baseUrl: this.baseUrl
        });
      }
      async embed(text) {
        if (!text || text.trim().length === 0) {
          throw new EmbeddingValidationError("Text cannot be empty");
        }
        if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
          throw new EmbeddingValidationError(
            `Text length ${text.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_TEXT_LENGTH} characters`
          );
        }
        const startTime = Date.now();
        try {
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Starting Gemini embedding`, {
            textLength: text.length,
            model: this.model
          });
          const response = await this.makeRequest("/models/" + this.model + ":embedContent", {
            content: {
              parts: [{ text }]
            },
            outputDimensionality: this.dimension
          });
          const embedding = response.embedding.values;
          const processingTime = Date.now() - startTime;
          if (embedding.length !== this.dimension) {
            throw new EmbeddingDimensionError(
              `Expected ${this.dimension} dimensions, got ${embedding.length}`,
              this.dimension,
              embedding.length,
              "gemini"
            );
          }
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Gemini embedding completed`, {
            dimension: embedding.length,
            processingTime
          });
          return embedding;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.EMBEDDING} Gemini embedding failed`, {
            error: error instanceof Error ? error.message : String(error),
            errorType: error?.constructor?.name,
            stack: error instanceof Error ? error.stack : void 0,
            processingTime
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(
            `Gemini embedding failed: ${error instanceof Error ? error.message : String(error)}`,
            "gemini",
            error instanceof Error ? error : void 0
          );
        }
      }
      async embedBatch(texts) {
        if (!texts || texts.length === 0) {
          throw new EmbeddingValidationError("Text array cannot be empty");
        }
        if (texts.length > VALIDATION_LIMITS.MAX_BATCH_SIZE) {
          throw new EmbeddingValidationError(
            `Batch size ${texts.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_BATCH_SIZE}`
          );
        }
        for (let i = 0; i < texts.length; i++) {
          const text = texts[i];
          if (!text || text.trim().length === 0) {
            throw new EmbeddingValidationError(`Text at index ${i} cannot be empty`);
          }
          if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
            throw new EmbeddingValidationError(
              `Text at index ${i} length ${text.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_TEXT_LENGTH} characters`
            );
          }
        }
        const startTime = Date.now();
        try {
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Starting Gemini batch embedding`, {
            batchSize: texts.length,
            model: this.model
          });
          const embeddings = [];
          for (const text of texts) {
            const embedding = await this.embed(text);
            embeddings.push(embedding);
          }
          const processingTime = Date.now() - startTime;
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Gemini batch embedding completed`, {
            batchSize: embeddings.length,
            dimension: embeddings[0]?.length,
            processingTime
          });
          return embeddings;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.EMBEDDING} Gemini batch embedding failed`, {
            error: error instanceof Error ? error.message : String(error),
            batchSize: texts.length,
            processingTime
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(
            `Gemini batch embedding failed: ${error instanceof Error ? error.message : String(error)}`,
            "gemini",
            error instanceof Error ? error : void 0
          );
        }
      }
      async isHealthy() {
        try {
          logger.debug(`${LOG_PREFIXES3.HEALTH} Checking Gemini embedder health`);
          await this.embed("health check");
          logger.debug(`${LOG_PREFIXES3.HEALTH} Gemini embedder is healthy`);
          return true;
        } catch (error) {
          logger.warn(`${LOG_PREFIXES3.HEALTH} Gemini embedder health check failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          return false;
        }
      }
      getDimension() {
        return this.dimension;
      }
      getModel() {
        return this.model;
      }
      getProvider() {
        return "gemini";
      }
      getConfig() {
        return this.config;
      }
      async disconnect() {
        logger.debug(`${LOG_PREFIXES3.EMBEDDING} Gemini embedder disconnected`);
      }
      /**
       * Make authenticated request to Gemini API
       */
      async makeRequest(endpoint, body2) {
        const url = `${this.baseUrl}${endpoint}?key=${this.apiKey}`;
        let lastError;
        let delay = RETRY_CONFIG.INITIAL_DELAY;
        for (let attempt = 0; attempt <= this.maxRetries; attempt++) {
          try {
            logger.silly(`${LOG_PREFIXES3.EMBEDDING} Making Gemini API request`, {
              attempt: attempt + 1,
              maxRetries: this.maxRetries + 1,
              endpoint
            });
            const controller = new AbortController();
            const timeoutId = setTimeout(() => controller.abort(), this.timeout);
            const response = await globalThis.fetch(url, {
              method: "POST",
              headers: {
                "Content-Type": "application/json"
              },
              body: JSON.stringify(body2),
              signal: controller.signal
            });
            clearTimeout(timeoutId);
            if (!response.ok) {
              const errorData = await response.json().catch(() => ({}));
              const errorMessage = errorData.error?.message || `HTTP ${response.status}`;
              switch (response.status) {
                case HTTP_STATUS.UNAUTHORIZED:
                  throw new EmbeddingError(`Invalid Gemini API key: ${errorMessage}`, "gemini");
                case HTTP_STATUS.FORBIDDEN:
                  throw new EmbeddingError(`Gemini API access forbidden: ${errorMessage}`, "gemini");
                case HTTP_STATUS.TOO_MANY_REQUESTS:
                  throw new EmbeddingRateLimitError(`Gemini API rate limit exceeded: ${errorMessage}`);
                case HTTP_STATUS.BAD_REQUEST:
                  throw new EmbeddingValidationError(`Invalid request to Gemini API: ${errorMessage}`);
                default:
                  throw new EmbeddingConnectionError(`Gemini API error: ${errorMessage}`);
              }
            }
            const data = await response.json();
            if (!data.embedding?.values) {
              throw new EmbeddingError("Invalid response format from Gemini API", "gemini");
            }
            return data;
          } catch (error) {
            lastError = error instanceof Error ? error : new Error(String(error));
            if (error instanceof EmbeddingValidationError || error instanceof EmbeddingError) {
              throw error;
            }
            if (attempt === this.maxRetries) {
              break;
            }
            logger.debug(`${LOG_PREFIXES3.EMBEDDING} Retrying Gemini request in ${delay}ms`, {
              attempt: attempt + 1,
              error: lastError.message
            });
            await new Promise((resolve3) => setTimeout(resolve3, delay));
            delay = Math.min(delay * RETRY_CONFIG.BACKOFF_MULTIPLIER, 6e4);
          }
        }
        throw new EmbeddingConnectionError(
          `Failed to connect to Gemini API after ${this.maxRetries + 1} attempts: ${lastError?.message}`,
          "gemini",
          lastError
        );
      }
    };
  }
});

// src/core/brain/embedding/backend/ollama.ts
var OllamaEmbedder;
var init_ollama2 = __esm({
  "src/core/brain/embedding/backend/ollama.ts"() {
    init_cjs_shims();
    init_logger2();
    init_types8();
    init_constants3();
    OllamaEmbedder = class {
      config;
      baseUrl;
      model;
      dimension;
      timeout;
      maxRetries;
      constructor(config2) {
        this.config = config2;
        this.model = config2.model || "nomic-embed-text";
        this.timeout = config2.timeout || 3e4;
        this.maxRetries = config2.maxRetries || 3;
        let baseUrl = config2.baseUrl || "http://localhost:11434";
        baseUrl = baseUrl.replace(/\/$/, "");
        if (baseUrl.endsWith("/v1")) {
          baseUrl = baseUrl.replace(/\/v1$/, "");
        }
        this.baseUrl = baseUrl;
        this.dimension = config2.dimensions || MODEL_DIMENSIONS[this.model] || 768;
        logger.debug(`${LOG_PREFIXES3.EMBEDDING} Ollama embedder initialized`, {
          model: this.model,
          dimension: this.dimension,
          baseUrl: this.baseUrl
        });
      }
      async embed(text) {
        if (!text || text.trim().length === 0) {
          throw new EmbeddingValidationError("Text cannot be empty");
        }
        if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
          throw new EmbeddingValidationError(
            `Text length ${text.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_TEXT_LENGTH} characters`
          );
        }
        const startTime = Date.now();
        try {
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Starting Ollama embedding`, {
            textLength: text.length,
            model: this.model
          });
          const response = await this.makeRequest("/api/embeddings", {
            model: this.model,
            prompt: text
          });
          const embedding = response.embedding;
          const processingTime = Date.now() - startTime;
          if (!embedding || !Array.isArray(embedding)) {
            throw new EmbeddingError("Invalid embedding response from Ollama API", "ollama");
          }
          if (embedding.length !== this.dimension) {
            logger.debug(
              `${LOG_PREFIXES3.EMBEDDING} Updating dimension from ${this.dimension} to ${embedding.length}`
            );
            this.dimension = embedding.length;
          }
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Ollama embedding completed`, {
            dimension: embedding.length,
            processingTime
          });
          return embedding;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.EMBEDDING} Ollama embedding failed`, {
            error: error instanceof Error ? error.message : String(error),
            processingTime
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(
            `Ollama embedding failed: ${error instanceof Error ? error.message : String(error)}`,
            "ollama",
            error instanceof Error ? error : void 0
          );
        }
      }
      async embedBatch(texts) {
        if (!texts || texts.length === 0) {
          throw new EmbeddingValidationError("Text array cannot be empty");
        }
        if (texts.length > VALIDATION_LIMITS.MAX_BATCH_SIZE) {
          throw new EmbeddingValidationError(
            `Batch size ${texts.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_BATCH_SIZE}`
          );
        }
        for (let i = 0; i < texts.length; i++) {
          const text = texts[i];
          if (!text || text.trim().length === 0) {
            throw new EmbeddingValidationError(`Text at index ${i} cannot be empty`);
          }
          if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
            throw new EmbeddingValidationError(
              `Text at index ${i} length ${text.length} exceeds maximum of ${VALIDATION_LIMITS.MAX_TEXT_LENGTH} characters`
            );
          }
        }
        const startTime = Date.now();
        try {
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Starting Ollama batch embedding`, {
            batchSize: texts.length,
            model: this.model
          });
          const embeddings = [];
          for (const text of texts) {
            const embedding = await this.embed(text);
            embeddings.push(embedding);
          }
          const processingTime = Date.now() - startTime;
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Ollama batch embedding completed`, {
            batchSize: embeddings.length,
            dimension: embeddings[0]?.length,
            processingTime
          });
          return embeddings;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.EMBEDDING} Ollama batch embedding failed`, {
            error: error instanceof Error ? error.message : String(error),
            batchSize: texts.length,
            processingTime
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(
            `Ollama batch embedding failed: ${error instanceof Error ? error.message : String(error)}`,
            "ollama",
            error instanceof Error ? error : void 0
          );
        }
      }
      async isHealthy() {
        try {
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Checking Ollama embedder health`);
          await this.embed("health check");
          logger.debug(`${LOG_PREFIXES3.EMBEDDING} Ollama embedder is healthy`);
          return true;
        } catch (error) {
          logger.warn(`${LOG_PREFIXES3.EMBEDDING} Ollama embedder health check failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          return false;
        }
      }
      getDimension() {
        return this.dimension;
      }
      getModel() {
        return this.model;
      }
      getProvider() {
        return "ollama";
      }
      getConfig() {
        return this.config;
      }
      async disconnect() {
        logger.debug(`${LOG_PREFIXES3.EMBEDDING} Ollama embedder disconnected`);
      }
      /**
       * Make request to Ollama API
       */
      async makeRequest(endpoint, body2) {
        const url = `${this.baseUrl}${endpoint}`;
        let lastError;
        let delay = RETRY_CONFIG.INITIAL_DELAY;
        for (let attempt = 0; attempt <= this.maxRetries; attempt++) {
          try {
            logger.silly(`${LOG_PREFIXES3.EMBEDDING} Making Ollama API request`, {
              attempt: attempt + 1,
              maxRetries: this.maxRetries + 1,
              endpoint,
              baseUrl: this.baseUrl
            });
            const controller = new AbortController();
            const timeoutId = setTimeout(() => controller.abort(), this.timeout);
            const response = await globalThis.fetch(url, {
              method: "POST",
              headers: {
                "Content-Type": "application/json"
              },
              body: JSON.stringify(body2),
              signal: controller.signal
            });
            clearTimeout(timeoutId);
            if (!response.ok) {
              const errorText = await response.text().catch(() => "Unknown error");
              let errorData;
              try {
                errorData = JSON.parse(errorText);
              } catch {
                errorData = { error: errorText };
              }
              const errorMessage = errorData.error || `HTTP ${response.status}`;
              switch (response.status) {
                case HTTP_STATUS.NOT_FOUND:
                  throw new EmbeddingValidationError(
                    `Ollama model '${this.model}' not found. Make sure the model is pulled: ollama pull ${this.model}`
                  );
                case HTTP_STATUS.BAD_REQUEST:
                  throw new EmbeddingValidationError(`Invalid request to Ollama API: ${errorMessage}`);
                case HTTP_STATUS.SERVICE_UNAVAILABLE:
                  throw new EmbeddingConnectionError(
                    `Ollama service unavailable. Make sure Ollama is running: ${errorMessage}`
                  );
                default:
                  throw new EmbeddingConnectionError(`Ollama API error: ${errorMessage}`);
              }
            }
            const data = await response.json();
            if (!data.embedding) {
              throw new EmbeddingError("Invalid response format from Ollama API", "ollama");
            }
            return data;
          } catch (error) {
            lastError = error instanceof Error ? error : new Error(String(error));
            logger.error(`${LOG_PREFIXES3.EMBEDDING} Ollama API request failed`, {
              attempt: attempt + 1,
              error: lastError.message,
              errorType: lastError.constructor.name,
              stack: lastError.stack,
              url,
              body: JSON.stringify(body2)
            });
            if (error instanceof EmbeddingValidationError || error instanceof EmbeddingError) {
              throw error;
            }
            if (attempt === this.maxRetries) {
              break;
            }
            logger.debug(`${LOG_PREFIXES3.EMBEDDING} Retrying Ollama request in ${delay}ms`, {
              attempt: attempt + 1,
              error: lastError.message
            });
            await new Promise((resolve3) => setTimeout(resolve3, delay));
            delay = Math.min(delay * RETRY_CONFIG.BACKOFF_MULTIPLIER, 6e4);
          }
        }
        throw new EmbeddingConnectionError(
          `Failed to connect to Ollama API after ${this.maxRetries + 1} attempts: ${lastError?.message}. Make sure Ollama is running at ${this.baseUrl}`,
          "ollama",
          lastError
        );
      }
    };
  }
});

// src/core/brain/embedding/backend/voyage.ts
var VoyageEmbedder;
var init_voyage = __esm({
  "src/core/brain/embedding/backend/voyage.ts"() {
    init_cjs_shims();
    init_logger2();
    init_types8();
    init_constants3();
    VoyageEmbedder = class {
      config;
      apiKey;
      baseUrl;
      model;
      dimension;
      constructor(config2) {
        this.config = config2;
        this.apiKey = config2.apiKey || process.env.VOYAGE_API_KEY || "";
        this.baseUrl = config2.baseUrl || "https://api.voyageai.com/v1";
        this.model = config2.model || "voyage-3-large";
        this.dimension = 1024;
        if (!this.apiKey) {
          throw new EmbeddingValidationError("Voyage API key is required");
        }
        logger.debug(`${LOG_PREFIXES3.VOYAGE} Initialized Voyage embedder`, {
          model: this.model,
          dimension: this.dimension,
          baseUrl: this.baseUrl,
          hasApiKey: !!this.apiKey
        });
      }
      async embed(text) {
        logger.silly(`${LOG_PREFIXES3.VOYAGE} Generating Voyage embedding`, {
          textLength: text.length,
          model: this.model
        });
        const startTime = Date.now();
        try {
          const response = await this.makeRequest({
            input: [text],
            model: this.model
          });
          if (!response.data || response.data.length === 0) {
            throw new EmbeddingError("No embedding data received from Voyage API");
          }
          const embedding = response.data[0].embedding;
          const processingTime = Date.now() - startTime;
          logger.debug(`${LOG_PREFIXES3.VOYAGE} Successfully generated Voyage embedding`, {
            model: this.model,
            dimension: embedding.length,
            processingTime: `${processingTime}ms`,
            textLength: text.length
          });
          return embedding;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.VOYAGE} Failed to generate Voyage embedding`, {
            error: error instanceof Error ? error.message : String(error),
            model: this.model,
            processingTime,
            textLength: text.length
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(`Failed to generate Voyage embedding: ${error}`);
        }
      }
      async embedBatch(texts) {
        logger.debug(`${LOG_PREFIXES3.VOYAGE} Generating Voyage batch embeddings`, {
          count: texts.length,
          model: this.model
        });
        const startTime = Date.now();
        try {
          const response = await this.makeRequest({
            input: texts,
            model: this.model
          });
          if (!response.data || response.data.length !== texts.length) {
            throw new EmbeddingError("Unexpected response format from Voyage API");
          }
          const embeddings = response.data.map((item) => item.embedding);
          const processingTime = Date.now() - startTime;
          logger.debug(`${LOG_PREFIXES3.VOYAGE} Successfully generated Voyage batch embeddings`, {
            model: this.model,
            count: embeddings.length,
            dimension: embeddings[0]?.length || 0,
            processingTime: `${processingTime}ms`
          });
          return embeddings;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.VOYAGE} Failed to generate Voyage batch embeddings`, {
            error: error instanceof Error ? error.message : String(error),
            model: this.model,
            processingTime,
            count: texts.length
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(`Failed to generate Voyage batch embeddings: ${error}`);
        }
      }
      getDimension() {
        return this.dimension;
      }
      getConfig() {
        return { ...this.config };
      }
      async isHealthy() {
        try {
          await this.embed("test");
          return true;
        } catch {
          return false;
        }
      }
      async disconnect() {
        logger.debug(`${LOG_PREFIXES3.VOYAGE} Voyage embedder disconnected`);
      }
      async makeRequest(body2) {
        const timeout = this.config.timeout || 3e4;
        const maxRetries = this.config.maxRetries || 3;
        for (let attempt = 1; attempt <= maxRetries; attempt++) {
          try {
            const controller = new AbortController();
            const timeoutId = setTimeout(() => controller.abort(), timeout);
            const response = await globalThis.fetch(`${this.baseUrl}/embeddings`, {
              method: "POST",
              headers: {
                "Content-Type": "application/json",
                Authorization: `Bearer ${this.apiKey}`
              },
              body: JSON.stringify(body2),
              signal: controller.signal
            });
            clearTimeout(timeoutId);
            if (!response.ok) {
              const errorText = await response.text();
              if (response.status === 429) {
                if (attempt < maxRetries) {
                  await new Promise((resolve3) => setTimeout(resolve3, 1e3 * attempt));
                  continue;
                }
              }
              throw new EmbeddingConnectionError(`Voyage API error: ${response.status} ${errorText}`);
            }
            return await response.json();
          } catch (error) {
            if (error instanceof EmbeddingError) {
              throw error;
            }
            if (attempt === maxRetries) {
              throw new EmbeddingConnectionError(
                `Failed to connect to Voyage API after ${maxRetries} attempts: ${error}`
              );
            }
            await new Promise((resolve3) => setTimeout(resolve3, 1e3 * attempt));
          }
        }
      }
    };
  }
});

// src/core/brain/embedding/backend/qwen.ts
var QwenEmbedder;
var init_qwen2 = __esm({
  "src/core/brain/embedding/backend/qwen.ts"() {
    init_cjs_shims();
    init_types8();
    QwenEmbedder = class {
      config;
      apiKey;
      baseUrl;
      model;
      dimensions;
      constructor(config2) {
        this.config = config2;
        this.apiKey = config2.apiKey || process.env.QWEN_API_KEY || process.env.DASHSCOPE_API_KEY || "";
        this.baseUrl = config2.baseUrl || "https://dashscope-intl.aliyuncs.com/compatible-mode/v1";
        this.model = config2.model || "text-embedding-v3";
        this.dimensions = config2.dimensions || 1024;
        if (!this.apiKey) {
          throw new EmbeddingValidationError(
            "Qwen API key is required (QWEN_API_KEY or DASHSCOPE_API_KEY)"
          );
        }
      }
      async embed(text) {
        try {
          const response = await this.makeRequest({
            model: this.model,
            input: [text],
            dimensions: this.dimensions
          });
          if (!response.data || response.data.length === 0) {
            throw new EmbeddingError("No embedding data received from Qwen API");
          }
          return response.data[0].embedding;
        } catch (error) {
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(`Failed to generate Qwen embedding: ${error}`);
        }
      }
      async embedBatch(texts) {
        try {
          const batchSize = 10;
          const results = [];
          for (let i = 0; i < texts.length; i += batchSize) {
            const batch = texts.slice(i, i + batchSize);
            const response = await this.makeRequest({
              model: this.model,
              input: batch,
              dimensions: this.dimensions
            });
            if (!response.data || response.data.length !== batch.length) {
              throw new EmbeddingError("Unexpected response format from Qwen API");
            }
            results.push(...response.data.map((item) => item.embedding));
          }
          return results;
        } catch (error) {
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(`Failed to generate Qwen batch embeddings: ${error}`);
        }
      }
      getDimension() {
        return this.dimensions;
      }
      getConfig() {
        return { ...this.config };
      }
      async isHealthy() {
        try {
          await this.embed("test");
          return true;
        } catch {
          return false;
        }
      }
      async disconnect() {
      }
      async makeRequest(body2) {
        const timeout = this.config.timeout || 3e4;
        const maxRetries = this.config.maxRetries || 3;
        for (let attempt = 1; attempt <= maxRetries; attempt++) {
          try {
            const controller = new AbortController();
            const timeoutId = setTimeout(() => controller.abort(), timeout);
            const response = await globalThis.fetch(`${this.baseUrl}/embeddings`, {
              method: "POST",
              headers: {
                "Content-Type": "application/json",
                Authorization: `Bearer ${this.apiKey}`
              },
              body: JSON.stringify(body2),
              signal: controller.signal
            });
            clearTimeout(timeoutId);
            if (!response.ok) {
              const errorText = await response.text();
              if (response.status === 429) {
                if (attempt < maxRetries) {
                  await new Promise((resolve3) => setTimeout(resolve3, 1e3 * attempt));
                  continue;
                }
              }
              throw new EmbeddingConnectionError(`Qwen API error: ${response.status} ${errorText}`);
            }
            return await response.json();
          } catch (error) {
            if (error instanceof EmbeddingError) {
              throw error;
            }
            if (attempt === maxRetries) {
              throw new EmbeddingConnectionError(
                `Failed to connect to Qwen API after ${maxRetries} attempts: ${error}`
              );
            }
            await new Promise((resolve3) => setTimeout(resolve3, 1e3 * attempt));
          }
        }
      }
    };
  }
});

// src/core/brain/embedding/backend/aws.ts
var import_client_bedrock_runtime2, import_util5, AWSBedrockEmbedder;
var init_aws3 = __esm({
  "src/core/brain/embedding/backend/aws.ts"() {
    init_cjs_shims();
    import_client_bedrock_runtime2 = require("@aws-sdk/client-bedrock-runtime");
    import_util5 = require("util");
    init_types8();
    AWSBedrockEmbedder = class {
      config;
      client;
      model;
      dimensions;
      constructor(config2) {
        this.config = config2;
        this.model = config2.model || "amazon.titan-embed-text-v2:0";
        this.dimensions = config2.dimensions || 1024;
        const clientConfig = {
          region: config2.region || process.env.AWS_DEFAULT_REGION || "us-east-1"
        };
        const accessKeyId = config2.accessKeyId || process.env.AWS_ACCESS_KEY_ID;
        const secretAccessKey = config2.secretAccessKey || process.env.AWS_SECRET_ACCESS_KEY;
        const sessionToken = config2.sessionToken || process.env.AWS_SESSION_TOKEN;
        if (accessKeyId && secretAccessKey) {
          clientConfig.credentials = {
            accessKeyId,
            secretAccessKey,
            ...sessionToken && { sessionToken }
          };
        }
        this.client = new import_client_bedrock_runtime2.BedrockRuntimeClient(clientConfig);
      }
      async embed(text) {
        try {
          console.log("aws bedrock embedder received: ", text);
          const body2 = this.buildRequestBody(text);
          console.log("aws bedrock embedder body: ", JSON.stringify(body2, null, 2));
          const command = new import_client_bedrock_runtime2.InvokeModelCommand({
            modelId: this.model,
            body: JSON.stringify(body2),
            contentType: "application/json",
            accept: "application/json"
          });
          console.log("aws bedrock embedder calling: ", command);
          console.log("aws bedrock embedder model: ", this.model);
          console.log("aws bedrock embedder region: ", this.config.region);
          console.log(
            "aws bedrock embedder client config: ",
            JSON.stringify(
              {
                region: this.config.region,
                credentials: {
                  accessKeyId: this.config.accessKeyId ? "SET" : "NOT SET",
                  secretAccessKey: this.config.secretAccessKey ? "SET" : "NOT SET"
                }
              },
              null,
              2
            )
          );
          const response = await this.client.send(command);
          const responseBody = JSON.parse(new import_util5.TextDecoder().decode(response.body));
          console.log("aws bedrock embedder responsed: ", responseBody);
          return this.extractEmbedding(responseBody);
        } catch (error) {
          const bedrockError = error;
          console.error("AWS Bedrock embedding error details:", {
            error: bedrockError.message,
            code: bedrockError.code,
            statusCode: bedrockError.statusCode,
            requestId: bedrockError.requestId,
            model: this.model,
            region: this.config.region
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(`Failed to generate AWS Bedrock embedding: ${error}`);
        }
      }
      async embedBatch(texts) {
        try {
          const results = [];
          for (const text of texts) {
            const embedding = await this.embed(text);
            results.push(embedding);
          }
          return results;
        } catch (error) {
          const bedrockError = error;
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw new EmbeddingError(
            `Failed to generate AWS Bedrock batch embeddings: ${bedrockError.message}`
          );
        }
      }
      getDimension() {
        return this.dimensions;
      }
      getConfig() {
        return { ...this.config };
      }
      async isHealthy() {
        try {
          await this.embed("test");
          return true;
        } catch {
          return false;
        }
      }
      async disconnect() {
        this.client.destroy();
      }
      buildRequestBody(text) {
        if (this.model.startsWith("amazon.titan-embed")) {
          return {
            inputText: text,
            dimensions: this.dimensions
          };
        } else if (this.model.startsWith("cohere.embed")) {
          return {
            texts: [text],
            input_type: "search_document",
            embedding_types: ["float"]
          };
        }
        throw new EmbeddingValidationError(`Unsupported AWS Bedrock model: ${this.model}`);
      }
      extractEmbedding(response) {
        if (this.model.startsWith("amazon.titan-embed")) {
          if (!response.embedding) {
            throw new EmbeddingError("No embedding data received from AWS Bedrock Titan API");
          }
          return response.embedding;
        } else if (this.model.startsWith("cohere.embed")) {
          if (!response.embeddings || response.embeddings.length === 0) {
            throw new EmbeddingError("No embedding data received from AWS Bedrock Cohere API");
          }
          return response.embeddings[0].values;
        }
        throw new EmbeddingError(`Unknown response format for model: ${this.model}`);
      }
    };
  }
});

// src/core/brain/embedding/backend/lmstudio.ts
var import_openai8, LM_STUDIO_MODEL_DIMENSIONS, LMStudioEmbedder;
var init_lmstudio2 = __esm({
  "src/core/brain/embedding/backend/lmstudio.ts"() {
    init_cjs_shims();
    import_openai8 = __toESM(require("openai"), 1);
    init_logger2();
    init_types8();
    init_constants3();
    LM_STUDIO_MODEL_DIMENSIONS = {
      "nomic-embed-text-v1.5": 768,
      "text-embedding-nomic-embed-text-v1.5": 768,
      "all-minilm": 384,
      "bge-large": 1024,
      "bge-base": 768,
      "bge-small": 384
    };
    LMStudioEmbedder = class {
      openai;
      config;
      model;
      dimension;
      constructor(config2) {
        this.config = config2;
        this.model = config2.model || "nomic-embed-text-v1.5";
        this.openai = new import_openai8.default({
          apiKey: "lm-studio",
          // LM Studio uses this as a placeholder
          baseURL: config2.baseUrl || "http://localhost:1234/v1",
          timeout: config2.timeout || 3e4,
          maxRetries: config2.maxRetries || 3
        });
        this.dimension = config2.dimensions || LM_STUDIO_MODEL_DIMENSIONS[this.model] || 768;
        this.config.maxRetries = config2.maxRetries || 3;
        this.config.timeout = config2.timeout || 3e4;
        logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Initialized LM Studio embedder`, {
          model: this.model,
          dimension: this.dimension,
          baseUrl: config2.baseUrl || "http://localhost:1234/v1"
        });
      }
      async embed(text) {
        const { model } = this;
        logger.silly(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Embedding single text`, {
          textLength: text.length,
          model: this.model
        });
        this.validateInput(text);
        const startTime = Date.now();
        try {
          const cleanText = text.replace(/\n/g, " ");
          const params = {
            model: this.model,
            input: cleanText
          };
          if (this.config.dimensions !== void 0) {
            params.dimensions = this.config.dimensions;
          }
          const response = await this.createEmbeddingWithRetry(params);
          if (!response.data || !Array.isArray(response.data) || !response.data[0] || !response.data[0].embedding) {
            throw new EmbeddingError("LM Studio API did not return a valid embedding", "lmstudio");
          }
          const embedding = response.data[0].embedding;
          this.validateEmbeddingDimension(embedding);
          const processingTime = Date.now() - startTime;
          logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Successfully created embedding`, {
            model,
            dimension: embedding.length,
            processingTime
          });
          return embedding;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Failed to create embedding`, {
            error: error instanceof Error ? error.message : String(error),
            model: this.model,
            processingTime,
            textLength: text.length
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw this.handleApiError(error);
        }
      }
      async embedBatch(texts) {
        const { model } = this;
        logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Embedding batch of texts`, {
          count: texts.length,
          model: this.model
        });
        this.validateBatchInput(texts);
        const startTime = Date.now();
        try {
          const cleanTexts = texts.map((text) => text.replace(/\n/g, " "));
          const batchParams = {
            model: this.model,
            input: cleanTexts
          };
          if (this.config.dimensions !== void 0) {
            batchParams.dimensions = this.config.dimensions;
          }
          const response = await this.createEmbeddingWithRetry(batchParams);
          const embeddings = response.data.map((item) => item.embedding);
          embeddings.forEach(this.validateEmbeddingDimension.bind(this));
          const processingTime = Date.now() - startTime;
          logger.debug(
            `${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Successfully created batch embeddings`,
            {
              model,
              count: embeddings.length,
              processingTime
            }
          );
          return embeddings;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Failed to create batch embeddings`, {
            error: error instanceof Error ? error.message : String(error),
            model,
            processingTime,
            count: texts.length
          });
          if (error instanceof EmbeddingError) {
            throw error;
          }
          throw this.handleApiError(error);
        }
      }
      getDimension() {
        return this.dimension;
      }
      getConfig() {
        return { ...this.config };
      }
      async isHealthy() {
        try {
          logger.silly(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Checking LM Studio embedder health`);
          await this.embed("health check");
          logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} LM Studio embedder is healthy`);
          return true;
        } catch (error) {
          logger.warn(
            `${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} LM Studio embedder health check failed`,
            {
              error: error instanceof Error ? error.message : String(error),
              baseUrl: this.config.baseUrl || "http://localhost:1234/v1"
            }
          );
          return false;
        }
      }
      async disconnect() {
        logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Disconnecting LM Studio embedder`);
      }
      /**
       * Create embedding with retry logic
       */
      async createEmbeddingWithRetry(params) {
        let lastError;
        let delay = 100;
        for (let attempt = 0; attempt <= this.config.maxRetries; attempt++) {
          try {
            if (attempt > 0) {
              logger.debug(`${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Retrying embedding request`, {
                attempt,
                delay,
                maxRetries: this.config.maxRetries
              });
              await new Promise((resolve3) => setTimeout(resolve3, delay));
              delay = Math.min(delay * 2, 500);
            }
            const response = await this.openai.embeddings.create(params);
            if (attempt > 0) {
              logger.info(
                `${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Embedding request succeeded after retry`,
                {
                  attempt,
                  model: params.model
                }
              );
            }
            return response;
          } catch (error) {
            lastError = error instanceof Error ? error : new Error(String(error));
            logger.debug(`Attempt ${attempt + 1} failed with error:`, {
              errorType: error && typeof error === "object" && "constructor" in error ? error.constructor.name : void 0,
              errorMessage: error && typeof error === "object" && "message" in error ? error.message : String(error),
              hasStatus: error && typeof error === "object" && "status" in error,
              status: error && typeof error === "object" && "status" in error ? error.status : "none"
            });
            if (!this.shouldRetry(error, attempt)) {
              logger.debug(`Not retrying - error is not retryable`);
              break;
            }
            logger.warn(
              `${LOG_PREFIXES3.LMSTUDIO || "[LMSTUDIO]"} Embedding request failed, will retry`,
              {
                attempt: attempt + 1,
                maxRetries: this.config.maxRetries,
                error: lastError.message,
                nextDelay: delay
              }
            );
          }
        }
        if (lastError) {
          if (lastError instanceof EmbeddingError) {
            throw lastError;
          }
          throw this.handleApiError(lastError);
        }
        throw new EmbeddingError("Unknown error during embedding request", "lmstudio");
      }
      /**
       * Determine if an error is retryable
       */
      shouldRetry(error, attempt) {
        if (attempt >= this.config.maxRetries) {
          return false;
        }
        if (error && typeof error === "object" && "status" in error) {
          const status = error.status;
          return [
            HTTP_STATUS.TOO_MANY_REQUESTS,
            HTTP_STATUS.INTERNAL_SERVER_ERROR,
            HTTP_STATUS.SERVICE_UNAVAILABLE
          ].includes(status);
        }
        if (error instanceof Error) {
          const message = error.message.toLowerCase();
          return message.includes("network") || message.includes("timeout") || message.includes("connection") || message.includes("econnreset") || message.includes("enotfound") || message.includes("econnrefused") || // Common with local servers
          message.includes("fetch failed");
        }
        return false;
      }
      /**
       * Handle and categorize API errors
       */
      handleApiError(error) {
        logger.debug(`handleApiError called with:`, {
          errorType: error?.constructor?.name,
          errorMessage: error instanceof Error ? error.message : String(error),
          hasStatus: error && typeof error === "object" && "status" in error,
          status: error && typeof error === "object" && "status" in error ? error.status : "none",
          errorKeys: error && typeof error === "object" ? Object.keys(error) : []
        });
        if (error && typeof error === "object" && "status" in error) {
          const apiError = error;
          const status = apiError.status;
          const message = apiError.message || String(error);
          logger.debug(`Handling API error with status ${status}: ${message}`);
          switch (status) {
            case HTTP_STATUS.UNAUTHORIZED:
              return new EmbeddingConnectionError(
                "LM Studio server rejected the request. Ensure the embedding model is loaded.",
                "lmstudio",
                apiError
              );
            case HTTP_STATUS.TOO_MANY_REQUESTS:
              return new EmbeddingRateLimitError(
                "LM Studio server is busy. Try again later.",
                void 0,
                "lmstudio",
                apiError
              );
            case HTTP_STATUS.BAD_REQUEST:
              return new EmbeddingValidationError(
                `LM Studio server rejected the request: ${message}`,
                "lmstudio",
                apiError
              );
            case HTTP_STATUS.NOT_FOUND:
              return new EmbeddingConnectionError(
                `LM Studio embedding model '${this.model}' not found. Ensure the model is loaded.`,
                "lmstudio",
                apiError
              );
            default:
              return new EmbeddingConnectionError(
                `LM Studio server error (${status}): ${message}`,
                "lmstudio",
                apiError
              );
          }
        }
        if (error instanceof Error) {
          const message = error.message.toLowerCase();
          if (message.includes("econnrefused") || message.includes("fetch failed")) {
            return new EmbeddingConnectionError(
              "Cannot connect to LM Studio server at " + (this.config.baseUrl || "http://localhost:1234/v1") + ". Ensure LM Studio is running and the embedding model is loaded.",
              "lmstudio",
              error
            );
          }
          return new EmbeddingConnectionError(error.message, "lmstudio", error);
        }
        return new EmbeddingError(String(error), "lmstudio");
      }
      /**
       * Validate single text input
       */
      validateInput(text) {
        if (!text || text.length < VALIDATION_LIMITS.MIN_TEXT_LENGTH) {
          throw new EmbeddingValidationError("Text input cannot be empty", "lmstudio");
        }
        if (text.length > VALIDATION_LIMITS.MAX_TEXT_LENGTH) {
          throw new EmbeddingValidationError(
            `Text too long: ${text.length} characters exceeds limit of ${VALIDATION_LIMITS.MAX_TEXT_LENGTH}`,
            "lmstudio"
          );
        }
      }
      /**
       * Validate batch input
       */
      validateBatchInput(texts) {
        if (!Array.isArray(texts) || texts.length === 0) {
          throw new EmbeddingValidationError("Batch input must be a non-empty array", "lmstudio");
        }
        if (texts.length > VALIDATION_LIMITS.MAX_BATCH_SIZE) {
          throw new EmbeddingValidationError(
            `Batch too large: ${texts.length} items exceeds limit of ${VALIDATION_LIMITS.MAX_BATCH_SIZE}`,
            "lmstudio"
          );
        }
        texts.forEach((text, index) => {
          try {
            this.validateInput(text);
          } catch (error) {
            if (error instanceof EmbeddingValidationError) {
              throw new EmbeddingValidationError(
                `Batch item ${index}: ${error.message}`,
                "lmstudio",
                error
              );
            }
            throw error;
          }
        });
      }
      /**
       * Validate embedding dimension
       */
      validateEmbeddingDimension(embedding) {
        if (embedding.length !== this.dimension) {
          throw new EmbeddingDimensionError(
            `Embedding dimension mismatch: expected ${this.dimension}, got ${embedding.length}`,
            this.dimension,
            embedding.length,
            "lmstudio"
          );
        }
      }
    };
  }
});

// src/core/brain/embedding/factory.ts
async function createEmbedder(config2) {
  const factory = EMBEDDING_FACTORIES.get(config2.type);
  if (!factory) {
    throw new EmbeddingValidationError(`Unsupported embedding provider: ${config2.type}`);
  }
  logger.debug("Creating embedder", { provider: config2.type, model: config2.model });
  return factory.createEmbedder(config2);
}
async function createEmbedderFromEnv() {
  console.log("Attempting to parse embedding config from environment variables...");
  const envConfig = parseEmbeddingConfigFromEnv();
  if (!envConfig) {
    logger.debug("No embedding configuration found in environment");
    return null;
  }
  try {
    const backendConfig = {
      ...envConfig,
      timeout: 3e4,
      maxRetries: 3
    };
    const embedder = await createEmbedder(backendConfig);
    const info = {
      provider: envConfig.type,
      model: envConfig.model || "default",
      dimension: embedder.getDimension()
    };
    return { embedder, info };
  } catch (error) {
    logger.warn("Failed to create embedder from environment config", {
      error: error instanceof Error ? error.message : String(error),
      type: envConfig.type
    });
    return null;
  }
}
var OpenAIEmbeddingFactory, GeminiEmbeddingFactory, OllamaEmbeddingFactory, VoyageEmbeddingFactory, QwenEmbeddingFactory, AWSBedrockEmbeddingFactory, LMStudioEmbeddingFactory, EMBEDDING_FACTORIES;
var init_factory6 = __esm({
  "src/core/brain/embedding/factory.ts"() {
    init_cjs_shims();
    init_logger2();
    init_config4();
    init_types8();
    init_openai4();
    init_gemini2();
    init_ollama2();
    init_voyage();
    init_qwen2();
    init_aws3();
    init_lmstudio2();
    OpenAIEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "openai") {
          throw new EmbeddingValidationError("Invalid config type for OpenAI factory");
        }
        return new OpenAIEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "openai";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "openai";
      }
    };
    GeminiEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "gemini") {
          throw new EmbeddingValidationError("Invalid config type for Gemini factory");
        }
        return new GeminiEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "gemini";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "gemini";
      }
    };
    OllamaEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "ollama") {
          throw new EmbeddingValidationError("Invalid config type for Ollama factory");
        }
        return new OllamaEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "ollama";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "ollama";
      }
    };
    VoyageEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "voyage") {
          throw new EmbeddingValidationError("Invalid config type for Voyage factory");
        }
        return new VoyageEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "voyage";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "voyage";
      }
    };
    QwenEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "qwen") {
          throw new EmbeddingValidationError("Invalid config type for Qwen factory");
        }
        return new QwenEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "qwen";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "qwen";
      }
    };
    AWSBedrockEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "aws-bedrock") {
          throw new EmbeddingValidationError("Invalid config type for AWS Bedrock factory");
        }
        return new AWSBedrockEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "aws-bedrock";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "aws-bedrock";
      }
    };
    LMStudioEmbeddingFactory = class {
      async createEmbedder(config2) {
        if (config2.type !== "lmstudio") {
          throw new EmbeddingValidationError("Invalid config type for LM Studio factory");
        }
        return new LMStudioEmbedder(config2);
      }
      validateConfig(config2) {
        try {
          return typeof config2 === "object" && config2 !== null && config2.type === "lmstudio";
        } catch {
          return false;
        }
      }
      getProviderType() {
        return "lmstudio";
      }
    };
    EMBEDDING_FACTORIES = /* @__PURE__ */ new Map([
      ["openai", new OpenAIEmbeddingFactory()],
      ["gemini", new GeminiEmbeddingFactory()],
      ["ollama", new OllamaEmbeddingFactory()],
      ["voyage", new VoyageEmbeddingFactory()],
      ["qwen", new QwenEmbeddingFactory()],
      ["aws-bedrock", new AWSBedrockEmbeddingFactory()],
      ["lmstudio", new LMStudioEmbeddingFactory()]
    ]);
  }
});

// src/core/brain/embedding/manager.ts
var SessionEmbeddingState, EmbeddingManager;
var init_manager5 = __esm({
  "src/core/brain/embedding/manager.ts"() {
    init_cjs_shims();
    init_logger2();
    init_factory6();
    init_constants3();
    SessionEmbeddingState = class {
      disabled = false;
      disabledReason = "";
      disableForSession(reason) {
        this.disabled = true;
        this.disabledReason = reason;
        logger.warn(`${LOG_PREFIXES3.MANAGER} Embeddings disabled for this session: ${reason}`);
      }
      isDisabled() {
        return this.disabled;
      }
      getDisabledReason() {
        return this.disabledReason;
      }
    };
    EmbeddingManager = class {
      embedders = /* @__PURE__ */ new Map();
      embedderInfo = /* @__PURE__ */ new Map();
      sessionState = new SessionEmbeddingState();
      constructor() {
        logger.debug(`${LOG_PREFIXES3.MANAGER} Simple embedding manager initialized`);
      }
      /**
       * Create and register an embedder instance
       *
       * @param config - Embedding configuration
       * @param id - Optional custom ID for the embedder
       * @returns Promise resolving to embedder instance and info
       */
      async createEmbedder(config2, id) {
        const embedderId = id || this.generateId();
        logger.debug(`${LOG_PREFIXES3.MANAGER} Creating embedder`, {
          id: embedderId,
          type: config2.type
        });
        try {
          const configWithApiKey = { ...config2, apiKey: config2.apiKey || "" };
          const baseEmbedder = await createEmbedder(configWithApiKey);
          const info = {
            id: embedderId,
            provider: config2.type,
            model: config2.model || "unknown",
            dimension: baseEmbedder.getDimension(),
            config: config2,
            createdAt: /* @__PURE__ */ new Date()
          };
          this.embedders.set(embedderId, baseEmbedder);
          this.embedderInfo.set(embedderId, info);
          logger.info(`${LOG_PREFIXES3.MANAGER} Successfully created embedder`, {
            id: embedderId,
            provider: info.provider,
            model: info.model,
            dimension: info.dimension
          });
          return { embedder: baseEmbedder, info };
        } catch (error) {
          logger.error(`${LOG_PREFIXES3.MANAGER} Failed to create embedder`, {
            id: embedderId,
            type: config2.type,
            error: error instanceof Error ? error.message : String(error)
          });
          throw error;
        }
      }
      /**
       * Create and register an embedder from YAML configuration
       *
       * @param config - Embedding configuration from YAML
       * @param id - Optional custom ID for the embedder
       * @returns Promise resolving to embedder instance and info, or null
       */
      async createEmbedderFromConfig(config2, id) {
        logger.debug(`${LOG_PREFIXES3.MANAGER} Creating embedder from YAML configuration`, {
          provider: config2.type,
          model: config2.model
        });
        if ("disabled" in config2 && config2.disabled) {
          logger.info(`${LOG_PREFIXES3.MANAGER} Embeddings are disabled via YAML configuration`);
          return null;
        }
        const embedder = await createEmbedder(config2);
        if (!embedder) {
          logger.warn(`${LOG_PREFIXES3.MANAGER} Failed to create embedder from YAML configuration`);
          return null;
        }
        const embedderId = id || this.generateId();
        const backendConfig = embedder.getConfig();
        const info = {
          id: embedderId,
          provider: backendConfig.type,
          model: backendConfig.model || "unknown",
          dimension: embedder.getDimension(),
          config: backendConfig,
          createdAt: /* @__PURE__ */ new Date()
        };
        this.embedders.set(embedderId, embedder);
        this.embedderInfo.set(embedderId, info);
        const logDetails = {
          id: embedderId,
          provider: info.provider,
          model: info.model,
          dimension: info.dimension
        };
        if (info.provider === "voyage") {
          logDetails.voyageModel = info.model;
          logDetails.voyageDimensions = info.dimension;
          logger.info(
            `${LOG_PREFIXES3.MANAGER} Voyage embedder registered successfully from YAML`,
            logDetails
          );
        } else if (info.provider === "openai") {
          logDetails.openaiModel = info.model;
          logDetails.openaiDimensions = info.dimension;
          logger.info(
            `${LOG_PREFIXES3.MANAGER} OpenAI embedder registered successfully from YAML`,
            logDetails
          );
        } else if (info.provider === "qwen") {
          logDetails.qwenModel = info.model;
          logDetails.qwenDimensions = info.dimension;
          logger.info(
            `${LOG_PREFIXES3.MANAGER} Qwen embedder registered successfully from YAML`,
            logDetails
          );
        } else {
          logger.info(`${LOG_PREFIXES3.MANAGER} Embedder registered successfully from YAML`, logDetails);
        }
        return { embedder, info };
      }
      /**
       * Handle runtime embedding failure and disable globally if needed
       *
       * This method is called when any embedding-related tool fails.
       * It immediately disables embeddings globally to prevent further failures
       * and allow the application to continue in chat-only mode.
       */
      handleRuntimeFailure(error, provider) {
        const errorMessage = error.message.toLowerCase();
        const isCriticalError = errorMessage.includes("unauthorized") || errorMessage.includes("invalid api key") || errorMessage.includes("authentication") || errorMessage.includes("401") || errorMessage.includes("403") || errorMessage.includes("api key") || errorMessage.includes("model is not embedding") || errorMessage.includes("failed to load model") || errorMessage.includes("cannot connect") || errorMessage.includes("connection") || errorMessage.includes("not found") || errorMessage.includes("model not found") || errorMessage.includes("server rejected") || errorMessage.includes("econnrefused") || errorMessage.includes("fetch failed") || errorMessage.includes("timeout") || errorMessage.includes("network") || // LM Studio specific errors
        errorMessage.includes("lm studio server") || errorMessage.includes("embedding model") || errorMessage.includes("ensure the model is loaded");
        if (isCriticalError) {
          logger.error(
            `${LOG_PREFIXES3.MANAGER} Critical embedding failure - disabling for this session`,
            {
              provider,
              error: error.message
            }
          );
          this.sessionState.disableForSession(`${provider} embedding failed: ${error.message}`);
        } else {
          logger.warn(`${LOG_PREFIXES3.MANAGER} Embedding operation failed but will retry`, {
            provider,
            error: error.message
          });
        }
      }
      /**
       * Create embedder from environment variables
       *
       * @param id - Optional custom ID for the embedder
       * @returns Promise resolving to embedder instance and info, or null
       */
      async createEmbedderFromEnv(id) {
        logger.debug(`${LOG_PREFIXES3.MANAGER} Creating embedder from environment`);
        if (process.env.DISABLE_EMBEDDINGS === "true" || process.env.EMBEDDING_DISABLED === "true") {
          logger.info(`${LOG_PREFIXES3.MANAGER} Embeddings are disabled via environment variable`);
          return null;
        }
        const result = await createEmbedderFromEnv();
        if (!result) {
          logger.warn(`${LOG_PREFIXES3.MANAGER} No embedder configuration found in environment`);
          return null;
        }
        const { embedder } = result;
        const embedderId = id || this.generateId();
        const config2 = embedder.getConfig();
        const info = {
          id: embedderId,
          provider: config2.type,
          model: config2.model || "unknown",
          dimension: embedder.getDimension(),
          config: config2,
          createdAt: /* @__PURE__ */ new Date()
        };
        this.embedders.set(embedderId, embedder);
        this.embedderInfo.set(embedderId, info);
        logger.info(`${LOG_PREFIXES3.MANAGER} Successfully created embedder from environment`, {
          id: embedderId,
          provider: info.provider,
          model: info.model
        });
        return { embedder, info };
      }
      /**
       * Get embedder instance by ID
       *
       * @param id - Embedder ID
       * @returns Embedder instance or undefined
       */
      getEmbedder(id) {
        return this.embedders.get(id);
      }
      /**
       * Get embedder information by ID
       *
       * @param id - Embedder ID
       * @returns Embedder information or undefined
       */
      getEmbedderInfo(id) {
        return this.embedderInfo.get(id);
      }
      /**
       * Get all registered embedders
       *
       * @returns Map of embedder ID to embedder instance
       */
      getAllEmbedders() {
        return new Map(this.embedders);
      }
      /**
       * Get all embedder information
       *
       * @returns Map of embedder ID to embedder information
       */
      getAllEmbedderInfo() {
        return new Map(this.embedderInfo);
      }
      /**
       * Remove and disconnect an embedder
       *
       * @param id - Embedder ID
       * @returns Promise resolving to true if removed, false if not found
       */
      async removeEmbedder(id) {
        const embedder = this.embedders.get(id);
        if (!embedder) {
          logger.warn(`${LOG_PREFIXES3.MANAGER} Embedder not found for removal`, { id });
          return false;
        }
        logger.debug(`${LOG_PREFIXES3.MANAGER} Removing embedder`, { id });
        try {
          await embedder.disconnect();
          this.embedders.delete(id);
          this.embedderInfo.delete(id);
          logger.info(`${LOG_PREFIXES3.MANAGER} Successfully removed embedder`, { id });
          return true;
        } catch (error) {
          logger.error(`${LOG_PREFIXES3.MANAGER} Error disconnecting embedder`, {
            id,
            error: error instanceof Error ? error.message : String(error)
          });
          this.embedders.delete(id);
          this.embedderInfo.delete(id);
          return true;
        }
      }
      /**
       * Perform health check on a specific embedder
       *
       * @param id - Embedder ID
       * @returns Promise resolving to health check result
       */
      async checkHealth(id) {
        const embedder = this.embedders.get(id);
        const info = this.embedderInfo.get(id);
        if (!embedder || !info) {
          logger.warn(`${LOG_PREFIXES3.HEALTH} Embedder not found for health check`, { id });
          return null;
        }
        logger.silly(`${LOG_PREFIXES3.HEALTH} Performing health check`, { id });
        const startTime = Date.now();
        try {
          const healthy = await embedder.isHealthy();
          const responseTime = Date.now() - startTime;
          const result = {
            healthy,
            provider: info.provider,
            model: info.model || "unknown",
            dimension: info.dimension,
            responseTime,
            timestamp: /* @__PURE__ */ new Date()
          };
          this.embedderInfo.set(id, {
            ...info,
            lastHealthCheck: result
          });
          logger.debug(`${LOG_PREFIXES3.HEALTH} Health check completed`, {
            id,
            healthy,
            responseTime
          });
          return result;
        } catch (error) {
          const responseTime = Date.now() - startTime;
          const result = {
            healthy: false,
            provider: info.provider,
            model: info.model || "unknown",
            dimension: info.dimension,
            responseTime,
            error: error instanceof Error ? error.message : String(error),
            timestamp: /* @__PURE__ */ new Date()
          };
          this.embedderInfo.set(id, {
            ...info,
            lastHealthCheck: result
          });
          logger.warn(`${LOG_PREFIXES3.HEALTH} Health check failed`, {
            id,
            error: result.error,
            responseTime
          });
          return result;
        }
      }
      /**
       * Perform health check on all embedders
       *
       * @returns Promise resolving to map of embedder ID to health check result
       */
      async checkAllHealth() {
        logger.debug(`${LOG_PREFIXES3.HEALTH} Performing health check on all embedders`);
        const results = /* @__PURE__ */ new Map();
        const healthCheckPromises = Array.from(this.embedders.keys()).map(async (id) => {
          const result = await this.checkHealth(id);
          if (result) {
            results.set(id, result);
          }
        });
        await Promise.all(healthCheckPromises);
        logger.debug(`${LOG_PREFIXES3.HEALTH} Completed health check on all embedders`, {
          total: this.embedders.size,
          checked: results.size
        });
        return results;
      }
      /**
       * Start periodic health checks (simplified - no automatic scheduling)
       *
       * @param intervalMs - Health check interval in milliseconds (default: 5 minutes)
       */
      startHealthChecks(_intervalMs = 5 * 60 * 1e3) {
        logger.debug(`${LOG_PREFIXES3.HEALTH} Health checks can be run manually via checkAllHealth()`);
      }
      /**
       * Stop periodic health checks (simplified - no automatic scheduling)
       */
      stopHealthChecks() {
        logger.debug(`${LOG_PREFIXES3.HEALTH} No periodic health checks to stop`);
      }
      /**
       * Get current statistics (simplified - basic stats only)
       *
       * @returns Current embedding statistics
       */
      getStats() {
        return {
          totalEmbeds: 0,
          totalBatchEmbeds: 0,
          totalTexts: 0,
          totalProcessingTime: 0,
          successfulOperations: 0,
          failedOperations: 0,
          averageProcessingTime: 0
        };
      }
      /**
       * Reset statistics (simplified - no-op)
       */
      resetStats() {
        logger.debug(`${LOG_PREFIXES3.MANAGER} Statistics reset (simplified implementation)`);
      }
      /**
       * Update statistics (simplified - no-op)
       */
      updateStats(type, textCount, processingTime, success) {
        logger.silly(`${LOG_PREFIXES3.MANAGER} Operation completed`, {
          type,
          textCount,
          processingTime,
          success
        });
      }
      /**
       * Get embedding status for all embedders
       */
      getEmbeddingStatus() {
        const status = {};
        for (const [id] of this.embedders) {
          const info = this.embedderInfo.get(id);
          if (info) {
            const isDisabled = this.sessionState.isDisabled();
            status[id] = {
              status: isDisabled ? "DISABLED" : "HEALTHY",
              provider: info.provider,
              isHealthy: !isDisabled,
              stats: { disabled: isDisabled, reason: this.sessionState.getDisabledReason() }
            };
          }
        }
        return status;
      }
      /**
       * Check if embeddings are available for this session
       */
      hasAvailableEmbeddings() {
        if (this.sessionState.isDisabled()) {
          return false;
        }
        return this.embedders.size > 0;
      }
      /**
       * Get session embedding state
       */
      getSessionState() {
        return this.sessionState;
      }
      // Removed complex circuit breaker and resilient embedder methods
      /**
       * Disconnect all embedders and cleanup
       */
      async disconnect() {
        logger.info(`${LOG_PREFIXES3.MANAGER} Disconnecting all embedders`);
        this.stopHealthChecks();
        const disconnectPromises = Array.from(this.embedders.entries()).map(async ([id, embedder]) => {
          try {
            await embedder.disconnect();
            logger.debug(`${LOG_PREFIXES3.MANAGER} Disconnected embedder`, { id });
          } catch (error) {
            logger.warn(`${LOG_PREFIXES3.MANAGER} Error disconnecting embedder`, {
              id,
              error: error instanceof Error ? error.message : String(error)
            });
          }
        });
        await Promise.all(disconnectPromises);
        this.embedders.clear();
        this.embedderInfo.clear();
        logger.info(`${LOG_PREFIXES3.MANAGER} Successfully disconnected all embedders`);
      }
      /**
       * Generate a unique ID for embedders
       */
      generateId() {
        return `embedder_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
      }
    };
  }
});

// src/core/brain/embedding/utils.ts
var init_utils4 = __esm({
  "src/core/brain/embedding/utils.ts"() {
    init_cjs_shims();
    init_env();
    init_constants3();
  }
});

// src/core/brain/embedding/index.ts
var init_embedding = __esm({
  "src/core/brain/embedding/index.ts"() {
    init_cjs_shims();
    init_types9();
    init_factory6();
    init_manager5();
    init_config4();
    init_constants3();
    init_utils4();
  }
});

// src/core/vector_storage/constants.ts
var LOG_PREFIXES4, ERROR_MESSAGES4, TIMEOUTS2, BACKEND_TYPES2, DEFAULTS2, DISTANCE_METRICS;
var init_constants4 = __esm({
  "src/core/vector_storage/constants.ts"() {
    init_cjs_shims();
    LOG_PREFIXES4 = {
      MANAGER: "[VectorStoreManager]",
      BACKEND: "[VectorStoreBackend]",
      FACTORY: "[VectorStoreFactory]",
      SEARCH: "[VectorStore:Search]",
      INDEX: "[VectorStore:Index]",
      QDRANT: "[VectorStore:Qdrant]",
      MILVUS: "[VectorStore:Milvus]",
      CHROMA: "[VectorStore:Chroma]",
      PINECONE: "[VectorStore:Pinecone]",
      PGVECTOR: "[VectorStore:PgVector]",
      FAISS: "[VectorStore:Faiss]",
      REDIS: "[VectorStore:Redis]",
      WEAVIATE: "[VectorStore:Weaviate]",
      MEMORY: "[VectorStore:Memory]"
    };
    ERROR_MESSAGES4 = {
      // Connection errors
      CONNECTION_FAILED: "Failed to connect to vector store backend",
      ALREADY_CONNECTED: "Vector store is already connected",
      NOT_CONNECTED: "Vector store is not connected",
      // Backend errors
      BACKEND_NOT_FOUND: "Vector store backend not found",
      INVALID_BACKEND_TYPE: "Invalid backend type specified",
      MODULE_LOAD_FAILED: "Failed to load backend module",
      // Operation errors
      INVALID_DIMENSION: "Vector dimension mismatch",
      COLLECTION_NOT_FOUND: "Collection does not exist",
      VECTOR_NOT_FOUND: "Vector not found",
      BATCH_SIZE_EXCEEDED: "Batch size exceeds maximum allowed",
      SEARCH_FAILED: "Vector search operation failed",
      // Configuration errors
      INVALID_CONFIG: "Invalid vector store configuration",
      MISSING_REQUIRED_CONFIG: "Missing required configuration",
      INVALID_COLLECTION_NAME: "Invalid collection name"
    };
    TIMEOUTS2 = {
      CONNECTION: 3e4,
      // 30 seconds (vector stores may take longer)
      OPERATION: 6e4,
      // 60 seconds
      SEARCH: 1e4,
      // 10 seconds
      SHUTDOWN: 5e3
      // 5 seconds
    };
    BACKEND_TYPES2 = {
      QDRANT: "qdrant",
      PINECONE: "pinecone",
      WEAVIATE: "weaviate",
      CHROMA: "chroma",
      IN_MEMORY: "in-memory",
      MILVUS: "milvus",
      PGVECTOR: "pgvector",
      FAISS: "faiss",
      REDIS: "redis"
    };
    DEFAULTS2 = {
      // Search defaults
      SEARCH_LIMIT: 10,
      SEARCH_SCORE_THRESHOLD: 0,
      // Vector defaults
      DIMENSION: 1536,
      // OpenAI ada-002 dimension
      MAX_BATCH_SIZE: 100,
      // Connection defaults
      MAX_RETRIES: 3,
      RETRY_DELAY: 1e3,
      // 1 second
      MAX_CONNECTIONS: 10,
      IDLE_TIMEOUT: 3e4,
      // 30 seconds
      // Qdrant defaults
      QDRANT_PORT: 6333,
      QDRANT_GRPC_PORT: 6334,
      QDRANT_DISTANCE: "Cosine",
      // ChromaDB defaults
      CHROMA_PORT: 8e3,
      CHROMA_DISTANCE: "cosine",
      // Pinecone defaults
      PINECONE_REGION: "us-east-1",
      PINECONE_PROVIDER: "aws",
      // PGVector defaults
      PGVECTOR_INDEXTYPE: "hnsw",
      PGVECTOR_INDEXMETRIC: "vector_l2_ops"
    };
    DISTANCE_METRICS = {
      COSINE: "Cosine",
      EUCLIDEAN: "Euclidean",
      DOT_PRODUCT: "Dot",
      MANHATTAN: "Manhattan"
    };
  }
});

// src/core/vector_storage/config.ts
var BaseVectorStoreSchema, InMemoryBackendSchema2, QdrantBackendSchema, MilvusBackendSchema, FaissBackendSchema, ChromaBackendSchema, PineconeBackendSchema, PgVectorBackendSchema, RedisBackendSchema2, WeaviateBackendSchema, BackendConfigSchema2, VectorStoreSchema;
var init_config5 = __esm({
  "src/core/vector_storage/config.ts"() {
    init_cjs_shims();
    init_zod();
    init_constants4();
    BaseVectorStoreSchema = external_exports.object({
      /** Name of the collection/index to use */
      collectionName: external_exports.string().min(1).describe("Collection name"),
      /** Dimension of vectors (must match embedding model output) */
      dimension: external_exports.number().int().positive().default(DEFAULTS2.DIMENSION).describe("Vector dimension"),
      /** Maximum number of concurrent connections */
      maxConnections: external_exports.number().int().positive().optional().describe("Maximum connections"),
      /** Connection timeout in milliseconds */
      connectionTimeoutMillis: external_exports.number().int().positive().optional().describe("Connection timeout"),
      /** Backend-specific options */
      options: external_exports.record(external_exports.any()).optional().describe("Backend-specific options")
    });
    InMemoryBackendSchema2 = BaseVectorStoreSchema.extend({
      type: external_exports.literal("in-memory"),
      /** Maximum number of vectors to store (prevents memory overflow) */
      maxVectors: external_exports.number().int().positive().default(1e4).describe("Maximum vectors to store")
    }).strict();
    QdrantBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("qdrant"),
      /** Qdrant connection URL (http://...) - overrides individual params if provided */
      url: external_exports.string().url().optional().describe("Qdrant connection URL"),
      /** Qdrant server hostname */
      host: external_exports.string().optional().describe("Qdrant host"),
      /** Qdrant REST API port (default: 6333) */
      port: external_exports.number().int().positive().default(DEFAULTS2.QDRANT_PORT).optional().describe("Qdrant port"),
      /** Qdrant API key for authentication */
      apiKey: external_exports.string().optional().describe("Qdrant API key"),
      /** Store vectors on disk (for large datasets) */
      onDisk: external_exports.boolean().optional().describe("Store vectors on disk"),
      /** Path for local Qdrant storage (if not using remote server) */
      path: external_exports.string().optional().describe("Local storage path"),
      /** Distance metric for similarity search */
      distance: external_exports.enum([
        DISTANCE_METRICS.COSINE,
        DISTANCE_METRICS.EUCLIDEAN,
        DISTANCE_METRICS.DOT_PRODUCT,
        DISTANCE_METRICS.MANHATTAN
      ]).default(DEFAULTS2.QDRANT_DISTANCE).optional().describe("Distance metric")
    }).strict();
    MilvusBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("milvus"),
      /** Milvus connection URL (http://...) - overrides individual params if provided */
      url: external_exports.string().url().optional().describe("Milvus connection URL"),
      /** Milvus server hostname */
      host: external_exports.string().optional().describe("Milvus host"),
      /** Milvus REST API port (default: 19530) */
      port: external_exports.number().int().positive().default(19530).optional().describe("Milvus port"),
      /** Milvus username for authentication (Zilliz Cloud) */
      username: external_exports.string().optional().describe("Milvus username"),
      /** Milvus password for authentication (Zilliz Cloud) */
      password: external_exports.string().optional().describe("Milvus password"),
      /** Milvus API token for authentication (Zilliz Cloud) */
      token: external_exports.string().optional().describe("Milvus API token")
    }).strict();
    FaissBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("faiss"),
      /** Distance metric for similarity search */
      distance: external_exports.enum(["Cosine", "Euclidean", "IP"]).default("Cosine").optional().describe("Distance metric"),
      /** Path to store the FAISS index file (for persistence) */
      baseStoragePath: external_exports.string().optional().describe("Base directory for FAISS collection data")
    }).strict();
    ChromaBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("chroma"),
      /** ChromaDB connection URL (http://...) - overrides individual params if provided */
      url: external_exports.string().url().optional().describe("ChromaDB connection URL"),
      /** ChromaDB server hostname */
      host: external_exports.string().optional().describe("ChromaDB host"),
      /** ChromaDB HTTP port (default: 8000) */
      port: external_exports.number().int().positive().default(8e3).optional().describe("ChromaDB port"),
      /** Use SSL/TLS for connection (default: false) */
      ssl: external_exports.boolean().default(false).optional().describe("Use SSL/TLS for connection"),
      /** Custom HTTP headers for authentication */
      headers: external_exports.record(external_exports.string()).optional().describe("Custom HTTP headers"),
      /** Distance metric for similarity search */
      distance: external_exports.enum(["cosine", "l2", "euclidean", "ip", "dot"]).default("cosine").optional().describe("Distance metric"),
      /** Custom path for ChromaDB API endpoints */
      path: external_exports.string().optional().describe("Custom API path")
    }).strict();
    PineconeBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("pinecone"),
      /** Pinecone API key for authentication */
      apiKey: external_exports.string().min(1).describe("Pinecone API key"),
      /** Pinecone provider (optional) */
      provider: external_exports.string().optional().describe("Pinecone provider"),
      /** Pinecone region (default: 'us-west1') */
      region: external_exports.string().default(DEFAULTS2.PINECONE_REGION).optional().describe("Pinecone region"),
      /** Distance metric for similarity search */
      metric: external_exports.enum(["cosine", "euclidean", "dotproduct"]).default("cosine").optional().describe("Distance metric"),
      /** Pinecone pod type (for performance tuning) */
      podType: external_exports.string().optional().describe("Pinecone pod type"),
      /** Number of replicas for high availability */
      replicas: external_exports.number().int().positive().optional().describe("Number of replicas"),
      /** Source collection for cloning */
      sourceCollection: external_exports.string().optional().describe("Source collection for cloning")
    }).strict();
    PgVectorBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("pgvector"),
      /** PostgreSQL connection URL (postgresql://...) - overrides individual params if provided */
      url: external_exports.string().url().optional().describe("PostgreSQL connection URL"),
      /** Use SSL/TLS for connection (default: false) */
      ssl: external_exports.boolean().default(false).optional().describe("Use SSL/TLS for connection"),
      /** Distance metric for similarity search */
      distance: external_exports.enum(["Cosine", "Euclidean", "Dot", "Manhattan"]).default("Cosine").optional().describe("Distance metric for pgvector"),
      /** Connection pool size (default: 10) */
      poolSize: external_exports.number().int().positive().default(10).optional().describe("Connection pool size"),
      /** Index type for vector similarity search */
      indexType: external_exports.enum(["ivfflat", "hnsw"]).default("hnsw").optional().describe("Vector index type"),
      indexMetric: external_exports.enum(["vector_l2_ops", "vector_ip_ops", "vector_cosine_ops"]).default("vector_l2_ops").optional().describe("Vector index metric for pgvector"),
      /** Schema name (default: 'public') */
      schema: external_exports.string().default("public").optional().describe("PostgreSQL schema name")
    }).strict();
    RedisBackendSchema2 = BaseVectorStoreSchema.extend({
      type: external_exports.literal("redis"),
      url: external_exports.string().url().describe("Redis connection URL (redis://...)"),
      host: external_exports.string().optional().describe("Redis host"),
      port: external_exports.number().int().positive().default(6379).optional().describe("Redis port"),
      username: external_exports.string().optional().describe("Redis username"),
      password: external_exports.string().optional().describe("Redis password"),
      distance: external_exports.enum(["COSINE", "L2", "IP"]).default("COSINE").optional().describe("Distance metric for Redis vector similarity search")
    }).strict();
    WeaviateBackendSchema = BaseVectorStoreSchema.extend({
      type: external_exports.literal("weaviate"),
      /** Weaviate connection URL (http://...) - overrides individual params if provided */
      url: external_exports.string().optional().describe("Weaviate connection URL"),
      /** Weaviate server hostname */
      host: external_exports.string().optional().describe("Weaviate host"),
      /** Weaviate REST API port (default: 8080) */
      port: external_exports.number().int().positive().default(8080).optional().describe("Weaviate port"),
      /** Weaviate gRPC port (default: 50051) */
      grpcPort: external_exports.number().int().positive().default(50051).optional().describe("Weaviate gRPC port"),
      /** Weaviate API key for authentication */
      apiKey: external_exports.string().optional().describe("Weaviate API key"),
      /** Weaviate username for authentication */
      username: external_exports.string().optional().describe("Weaviate username"),
      /** Weaviate password for authentication */
      password: external_exports.string().optional().describe("Weaviate password"),
      /** Additional headers (e.g., for third-party API keys) */
      headers: external_exports.record(external_exports.string()).optional().describe("Additional headers"),
      /** Use HTTPS/secure connection */
      secure: external_exports.boolean().default(true).optional().describe("Use secure connection"),
      /** Connection timeout in milliseconds */
      timeout: external_exports.number().int().positive().default(3e4).optional().describe("Connection timeout"),
      /** Distance metric for similarity search */
      distance: external_exports.enum(["Cosine", "Euclidean", "IP"]).default("Cosine").optional().describe("Distance metric")
    }).strict();
    BackendConfigSchema2 = external_exports.discriminatedUnion(
      "type",
      [
        InMemoryBackendSchema2,
        QdrantBackendSchema,
        MilvusBackendSchema,
        ChromaBackendSchema,
        PineconeBackendSchema,
        PgVectorBackendSchema,
        FaissBackendSchema,
        RedisBackendSchema2,
        WeaviateBackendSchema
      ],
      {
        errorMap: (issue, ctx) => {
          if (issue.code === external_exports.ZodIssueCode.invalid_union_discriminator) {
            return {
              message: `Invalid backend type. Expected 'in-memory', 'qdrant', 'milvus', 'chroma', 'pinecone', 'pgvector', 'redis', or 'weaviate'.`
            };
          }
          return { message: ctx.defaultError };
        }
      }
    ).describe("Backend configuration for vector storage system").superRefine((data, ctx) => {
      if (data.type === "qdrant") {
        if (!data.url && !data.host) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Qdrant backend requires either 'url' or 'host' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "milvus") {
        if (!data.url && !data.host) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Milvus backend requires either 'url' or 'host' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "chroma") {
        if (!data.url && !data.host) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "ChromaDB backend requires either 'url' or 'host' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "pinecone") {
        if (!data.apiKey) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Pinecone backend requires 'apiKey' to be specified",
            path: ["apiKey"]
          });
        }
      }
      if (data.type === "pgvector") {
        if (!data.url) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "PgVector backend requires either 'url' or both 'host' and 'database' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "faiss") {
        if (!data.baseStoragePath) {
          console.log("Faiss backend requires baseStoragePath");
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Faiss backend requires 'baseStoragePath' to be specified",
            path: ["baseStoragePath"]
          });
        }
      }
      if (data.type === "redis") {
        if (!data.url && (!data.host || !data.port)) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Redis backend requires either 'url' or both 'host' and 'port' to be specified",
            path: ["url"]
          });
        }
      }
      if (data.type === "weaviate") {
        if (!data.url && !data.host) {
          ctx.addIssue({
            code: external_exports.ZodIssueCode.custom,
            message: "Weaviate backend requires either 'url' or 'host' to be specified",
            path: ["url"]
          });
        }
      }
      if (!/^[a-zA-Z0-9_-]+$/.test(data.collectionName)) {
        ctx.addIssue({
          code: external_exports.ZodIssueCode.custom,
          message: "Collection name must contain only letters, numbers, underscores, and hyphens",
          path: ["collectionName"]
        });
      }
    });
    VectorStoreSchema = BackendConfigSchema2;
  }
});

// src/core/vector_storage/event-aware-store.ts
var EventAwareVectorStore;
var init_event_aware_store = __esm({
  "src/core/vector_storage/event-aware-store.ts"() {
    init_cjs_shims();
    init_event_types();
    init_logger();
    EventAwareVectorStore = class {
      store;
      eventManager;
      sessionId;
      constructor(store, eventManager, sessionId) {
        this.store = store;
        this.eventManager = eventManager;
        this.sessionId = sessionId;
      }
      /**
       * Set the session context for event emission
       */
      setSessionContext(sessionId) {
        this.sessionId = sessionId;
      }
      /**
       * Set the event manager for event emission
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      async insert(vectors, ids, payloads) {
        const startTime = Date.now();
        try {
          await this.store.insert(vectors, ids, payloads);
          if (this.eventManager && this.sessionId) {
            this.eventManager.emitSessionEvent(this.sessionId, SessionEvents.MEMORY_STORED, {
              sessionId: this.sessionId,
              type: "knowledge",
              size: vectors.length,
              timestamp: Date.now()
            });
          }
          logger.debug("Memory operation: insert completed", {
            vectorCount: vectors.length,
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
        } catch (error) {
          logger.error("Memory operation: insert failed", {
            error: error instanceof Error ? error.message : String(error),
            vectorCount: vectors.length,
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async search(query2, limit, filters) {
        const startTime = Date.now();
        try {
          const results = await this.store.search(query2, limit, filters);
          if (this.eventManager && this.sessionId) {
            this.eventManager.emitSessionEvent(this.sessionId, SessionEvents.MEMORY_SEARCHED, {
              sessionId: this.sessionId,
              query: "vector_search",
              resultCount: results.length,
              duration: Date.now() - startTime,
              timestamp: Date.now()
            });
          }
          logger.debug("Memory operation: search completed", {
            resultCount: results.length,
            limit: limit || 10,
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
          return results;
        } catch (error) {
          logger.error("Memory operation: search failed", {
            error: error instanceof Error ? error.message : String(error),
            limit: limit || 10,
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async get(vectorId) {
        const startTime = Date.now();
        try {
          const result = await this.store.get(vectorId);
          if (this.eventManager && this.sessionId) {
            this.eventManager.emitSessionEvent(this.sessionId, SessionEvents.MEMORY_RETRIEVED, {
              sessionId: this.sessionId,
              type: "knowledge",
              count: result ? 1 : 0,
              timestamp: Date.now()
            });
          }
          logger.debug("Memory operation: get completed", {
            vectorId,
            found: !!result,
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
          return result;
        } catch (error) {
          logger.error("Memory operation: get failed", {
            error: error instanceof Error ? error.message : String(error),
            vectorId,
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async update(vectorId, vector, payload) {
        const startTime = Date.now();
        try {
          await this.store.update(vectorId, vector, payload);
          if (this.eventManager && this.sessionId) {
            this.eventManager.emitSessionEvent(this.sessionId, SessionEvents.MEMORY_STORED, {
              sessionId: this.sessionId,
              type: "knowledge",
              size: 1,
              timestamp: Date.now()
            });
          }
          logger.debug("Memory operation: update completed", {
            vectorId,
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
        } catch (error) {
          logger.error("Memory operation: update failed", {
            error: error instanceof Error ? error.message : String(error),
            vectorId,
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async delete(vectorId) {
        const startTime = Date.now();
        try {
          await this.store.delete(vectorId);
          if (this.eventManager && this.sessionId) {
            this.eventManager.emitSessionEvent(this.sessionId, SessionEvents.MEMORY_STORED, {
              sessionId: this.sessionId,
              type: "knowledge",
              size: 1,
              timestamp: Date.now()
            });
          }
          logger.debug("Memory operation: delete completed", {
            vectorId,
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
        } catch (error) {
          logger.error("Memory operation: delete failed", {
            error: error instanceof Error ? error.message : String(error),
            vectorId,
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async deleteCollection() {
        const startTime = Date.now();
        try {
          await this.store.deleteCollection();
          logger.info("Memory operation: collection deleted", {
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
        } catch (error) {
          logger.error("Memory operation: deleteCollection failed", {
            error: error instanceof Error ? error.message : String(error),
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      async list(filters, limit) {
        const startTime = Date.now();
        try {
          const [results, total] = await this.store.list(filters, limit);
          logger.debug("Memory operation: list completed", {
            resultCount: results.length,
            totalCount: total,
            limit: limit || "unlimited",
            duration: Date.now() - startTime,
            sessionId: this.sessionId
          });
          return [results, total];
        } catch (error) {
          logger.error("Memory operation: list failed", {
            error: error instanceof Error ? error.message : String(error),
            limit: limit || "unlimited",
            sessionId: this.sessionId
          });
          throw error;
        }
      }
      // Delegate connection management methods
      async connect() {
        return this.store.connect();
      }
      async disconnect() {
        return this.store.disconnect();
      }
      isConnected() {
        return this.store.isConnected();
      }
      // Delegate metadata methods
      getBackendType() {
        return this.store.getBackendType();
      }
      getDimension() {
        return this.store.getDimension();
      }
      getCollectionName() {
        return this.store.getCollectionName();
      }
    };
  }
});

// src/core/vector_storage/backend/types.ts
var VectorStoreError, VectorStoreConnectionError, VectorDimensionError;
var init_types10 = __esm({
  "src/core/vector_storage/backend/types.ts"() {
    init_cjs_shims();
    VectorStoreError = class extends Error {
      constructor(message, operation, cause) {
        super(message);
        this.message = message;
        this.operation = operation;
        this.cause = cause;
        this.name = "VectorStoreError";
        this.cause = cause;
      }
    };
    VectorStoreConnectionError = class extends VectorStoreError {
      constructor(message, backendType, cause) {
        super(message, "connection", cause);
        this.message = message;
        this.backendType = backendType;
        this.cause = cause;
        this.name = "VectorStoreConnectionError";
      }
    };
    VectorDimensionError = class extends VectorStoreError {
      constructor(message, expectedDimension, actualDimension, cause) {
        super(message, "dimension_mismatch", cause);
        this.message = message;
        this.expectedDimension = expectedDimension;
        this.actualDimension = actualDimension;
        this.cause = cause;
        this.name = "VectorDimensionError";
      }
    };
  }
});

// src/core/vector_storage/backend/in-memory.ts
var in_memory_exports2 = {};
__export(in_memory_exports2, {
  InMemoryBackend: () => InMemoryBackend2
});
var InMemoryBackend2;
var init_in_memory2 = __esm({
  "src/core/vector_storage/backend/in-memory.ts"() {
    init_cjs_shims();
    init_types10();
    init_logger2();
    init_constants4();
    InMemoryBackend2 = class {
      config;
      collectionName;
      dimension;
      maxVectors;
      logger;
      connected = false;
      // In-memory storage
      vectors = /* @__PURE__ */ new Map();
      constructor(config2) {
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.maxVectors = config2.maxVectors || 1e4;
        this.logger = createLogger({
          level: process.env.LOG_LEVEL || "info"
        });
        this.logger.debug(`${LOG_PREFIXES4.MEMORY} Initialized`, {
          collection: this.collectionName,
          dimension: this.dimension,
          maxVectors: this.maxVectors
        });
      }
      /**
       * Calculate cosine similarity between two vectors
       */
      cosineSimilarity(a, b) {
        let dotProduct = 0;
        let normA = 0;
        let normB = 0;
        const minLength = Math.min(a.length, b.length);
        for (let i = 0; i < minLength; i++) {
          const aVal = a[i];
          const bVal = b[i];
          dotProduct += aVal * bVal;
          normA += aVal * aVal;
          normB += bVal * bVal;
        }
        normA = Math.sqrt(normA);
        normB = Math.sqrt(normB);
        if (normA === 0 || normB === 0) {
          return 0;
        }
        return dotProduct / (normA * normB);
      }
      /**
       * Deep clone an object to prevent reference issues
       */
      deepClone(obj) {
        if (obj === null || typeof obj !== "object") {
          return obj;
        }
        if (Array.isArray(obj)) {
          return obj.map((item) => this.deepClone(item));
        }
        const cloned = {};
        for (const key in obj) {
          if (Object.prototype.hasOwnProperty.call(obj, key)) {
            cloned[key] = this.deepClone(obj[key]);
          }
        }
        return cloned;
      }
      /**
       * Check if a vector entry matches the given filters
       */
      matchesFilters(entry, filters) {
        if (!filters) return true;
        for (const [key, value] of Object.entries(filters)) {
          const payloadValue = entry.payload[key];
          if (value === null || value === void 0) continue;
          if (typeof value === "object" && !Array.isArray(value) && ("gte" in value || "gt" in value || "lte" in value || "lt" in value)) {
            if (typeof payloadValue !== "number") return false;
            if ("gte" in value && payloadValue < value.gte) return false;
            if ("gt" in value && payloadValue <= value.gt) return false;
            if ("lte" in value && payloadValue > value.lte) return false;
            if ("lt" in value && payloadValue >= value.lt) return false;
          } else if (typeof value === "object" && !Array.isArray(value) && ("any" in value || "all" in value)) {
            if ("any" in value && Array.isArray(value.any)) {
              if (!value.any.includes(payloadValue)) return false;
            }
          } else {
            if (payloadValue !== value) return false;
          }
        }
        return true;
      }
      /**
       * Validate vector dimension
       */
      validateDimension(vector, _operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      // VectorStore implementation
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        if (this.vectors.size + vectors.length > this.maxVectors) {
          throw new VectorStoreError(
            `Cannot insert ${vectors.length} vectors. Would exceed max limit of ${this.maxVectors}`,
            "insert"
          );
        }
        for (let i = 0; i < vectors.length; i++) {
          const vector = vectors[i];
          const id = ids[i];
          const payload = payloads[i];
          if (!vector || typeof id !== "number" || !Number.isInteger(id) || !payload) {
            throw new VectorStoreError(
              `Invalid input at index ${i}: vector, integer id, and payload are required`,
              "insert"
            );
          }
          this.validateDimension(vector, "insert");
          this.vectors.set(id, {
            id,
            vector: [...vector],
            // Clone to prevent external modification
            payload: this.deepClone(payload)
            // Deep clone payload
          });
        }
        this.logger.debug(`${LOG_PREFIXES4.INDEX} Inserted ${vectors.length} vectors`);
      }
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        const results = [];
        for (const entry of this.vectors.values()) {
          if (!this.matchesFilters(entry, filters)) {
            continue;
          }
          const score = this.cosineSimilarity(query2, entry.vector);
          results.push({ entry, score });
        }
        results.sort((a, b) => b.score - a.score);
        const topResults = results.slice(0, limit);
        const formattedResults = topResults.map(({ entry, score }) => ({
          id: entry.id,
          vector: [...entry.vector],
          payload: this.deepClone(entry.payload),
          score
        }));
        this.logger.debug(`${LOG_PREFIXES4.SEARCH} Found ${formattedResults.length} results`);
        return formattedResults;
      }
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        const entry = this.vectors.get(vectorId);
        if (!entry) {
          return null;
        }
        return {
          id: entry.id,
          vector: [...entry.vector],
          // Clone vector
          payload: this.deepClone(entry.payload),
          // Deep clone payload
          score: 1
          // Perfect match for direct retrieval
        };
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        if (!this.vectors.has(vectorId)) {
          throw new VectorStoreError(`Vector with ID ${vectorId} not found`, "update");
        }
        this.vectors.set(vectorId, {
          id: vectorId,
          vector: [...vector],
          payload: this.deepClone(payload)
        });
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Updated vector ${vectorId}`);
      }
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        if (!this.vectors.has(vectorId)) {
          this.logger.warn(`${LOG_PREFIXES4.BACKEND} Vector ${vectorId} not found for deletion`);
          return;
        }
        this.vectors.delete(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Deleted vector ${vectorId}`);
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        const count = this.vectors.size;
        this.vectors.clear();
        this.logger.info(
          `${LOG_PREFIXES4.BACKEND} Deleted collection ${this.collectionName} with ${count} vectors`
        );
      }
      async list(filters, limit = 100) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        const results = [];
        let count = 0;
        for (const entry of this.vectors.values()) {
          if (this.matchesFilters(entry, filters)) {
            count++;
            if (results.length < limit) {
              results.push({
                id: entry.id,
                vector: [...entry.vector],
                payload: this.deepClone(entry.payload),
                score: 1
                // Default score for list operations
              });
            }
          }
        }
        this.logger.info(`${LOG_PREFIXES4.BACKEND} Listed ${results.length} of ${count} vectors`);
        return [results, count];
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.MEMORY} Already connected`);
          return;
        }
        this.connected = true;
        this.logger.debug(`${LOG_PREFIXES4.MEMORY} Connected (in-memory)`);
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.MEMORY} Already disconnected`);
          return;
        }
        this.connected = false;
        this.vectors.clear();
        this.logger.info(`${LOG_PREFIXES4.MEMORY} Disconnected and cleared data`);
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "in-memory";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
    };
  }
});

// src/core/vector_storage/backend/qdrant.ts
var qdrant_exports = {};
__export(qdrant_exports, {
  QdrantBackend: () => QdrantBackend
});
var import_js_client_rest, QdrantBackend;
var init_qdrant = __esm({
  "src/core/vector_storage/backend/qdrant.ts"() {
    init_cjs_shims();
    import_js_client_rest = require("@qdrant/js-client-rest");
    init_types10();
    init_logger2();
    init_constants4();
    QdrantBackend = class {
      client;
      config;
      collectionName;
      dimension;
      logger;
      connected = false;
      constructor(config2) {
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: process.env.LOG_LEVEL || "info"
        });
        if (config2.url) {
          const clientParams = {
            url: config2.url
          };
          if (config2.apiKey) {
            clientParams.apiKey = config2.apiKey;
          }
          this.client = new import_js_client_rest.QdrantClient(clientParams);
        } else {
          const params = {
            host: config2.host || "localhost",
            port: config2.port || DEFAULTS2.QDRANT_PORT
          };
          if (config2.apiKey) {
            params.apiKey = config2.apiKey;
          }
          this.client = new import_js_client_rest.QdrantClient(params);
        }
        this.logger.debug(`${LOG_PREFIXES4.QDRANT} Initialized`, {
          collection: this.collectionName,
          dimension: this.dimension,
          host: config2.host || config2.url || "local"
        });
      }
      /**
       * Convert search filters to Qdrant filter format
       */
      createFilter(filters) {
        if (!filters) return void 0;
        const conditions = [];
        for (const [key, value] of Object.entries(filters)) {
          if (value === null || value === void 0) continue;
          if (typeof value === "object" && !Array.isArray(value) && ("gte" in value || "gt" in value || "lte" in value || "lt" in value)) {
            conditions.push({
              key,
              range: value
            });
          } else if (typeof value === "object" && !Array.isArray(value) && ("any" in value || "all" in value)) {
            if ("any" in value && Array.isArray(value.any)) {
              for (const item of value.any) {
                conditions.push({
                  key,
                  match: { value: item }
                });
              }
            }
          } else {
            conditions.push({
              key,
              match: { value }
            });
          }
        }
        return conditions.length ? { must: conditions } : void 0;
      }
      /**
       * Validate vector dimension
       */
      validateDimension(vector, _operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      // Qdrant only supports integer IDs for points in this deployment
      validateId(id) {
        if (!Number.isInteger(id) || isNaN(id)) {
          throw new VectorStoreError("Qdrant point IDs must be valid integers", "id");
        }
      }
      // VectorStore implementation
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        for (let i = 0; i < vectors.length; i++) {
          const vector = vectors[i];
          const id = ids[i];
          if (!vector) {
            throw new VectorStoreError(`Vector missing at index ${i}`, "insert");
          }
          if (id === void 0 || id === null) {
            throw new VectorStoreError(`ID missing at index ${i}`, "insert");
          }
          this.validateDimension(vector, "insert");
          this.validateId(id);
        }
        this.logger.debug(`${LOG_PREFIXES4.INDEX} Inserting ${vectors.length} vectors`);
        try {
          const points = vectors.map((vector, idx) => {
            const payload = payloads[idx];
            const id = ids[idx];
            if (!payload) throw new VectorStoreError(`Payload missing at index ${idx}`, "insert");
            if (!vector) throw new VectorStoreError(`Vector missing at index ${idx}`, "insert");
            if (id === void 0 || id === null)
              throw new VectorStoreError(`ID missing at index ${idx}`, "insert");
            return {
              id,
              // Always use integer IDs
              vector,
              payload
            };
          });
          await this.client.upsert(this.collectionName, { points });
          this.logger.debug(`${LOG_PREFIXES4.INDEX} Successfully inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.INDEX} Insert failed`, { error });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        this.logger.debug(`${LOG_PREFIXES4.SEARCH} Searching with limit ${limit}`, {
          hasFilters: !!filters
        });
        try {
          const queryFilter = this.createFilter(filters);
          const searchParams = {
            vector: query2,
            limit,
            with_vector: true,
            with_payload: true
          };
          if (queryFilter) {
            searchParams.filter = queryFilter;
          }
          const searchResponse = await this.client.search(this.collectionName, searchParams);
          const results = searchResponse.result || searchResponse;
          const formattedResults = results.map((hit) => ({
            id: hit.id,
            score: hit.score,
            payload: hit.payload
          }));
          this.logger.debug(`${LOG_PREFIXES4.SEARCH} Found ${formattedResults.length} results`);
          return formattedResults;
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.SEARCH} Search failed`, { error });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Getting vector ${vectorId}`);
        try {
          const response = await this.client.retrieve(this.collectionName, {
            ids: [vectorId],
            with_vector: true,
            with_payload: true
          });
          const results = response.result || response;
          if (!results.length) {
            return null;
          }
          const firstResult = results[0];
          if (!firstResult) {
            return null;
          }
          return {
            id: vectorId,
            // Qdrant uses integer IDs
            vector: firstResult.vector || [],
            payload: firstResult.payload || {},
            score: 1
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.BACKEND} Get failed`, { error });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Updating vector ${vectorId}`);
        try {
          const point = {
            id: vectorId,
            vector,
            payload
          };
          await this.client.upsert(this.collectionName, {
            points: [point]
          });
          this.logger.debug(`${LOG_PREFIXES4.BACKEND} Successfully updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.BACKEND} Update failed`, { error });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Deleting vector ${vectorId}`);
        try {
          await this.client.delete(this.collectionName, {
            points: [vectorId]
          });
          this.logger.debug(`${LOG_PREFIXES4.BACKEND} Successfully deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.BACKEND} Delete failed`, { error });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        this.logger.warn(`${LOG_PREFIXES4.BACKEND} Deleting collection ${this.collectionName}`);
        try {
          await this.client.deleteCollection(this.collectionName);
          this.logger.info(
            `${LOG_PREFIXES4.BACKEND} Successfully deleted collection ${this.collectionName}`
          );
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.BACKEND} Delete collection failed`, { error });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        this.logger.debug(`${LOG_PREFIXES4.BACKEND} Listing vectors`, {
          hasFilters: !!filters,
          limit
        });
        try {
          const queryFilter = this.createFilter(filters);
          const scrollRequest = {
            limit,
            with_payload: true,
            with_vector: true
          };
          if (queryFilter) {
            scrollRequest.filter = queryFilter;
          }
          const response = await this.client.scroll(this.collectionName, scrollRequest);
          const results = (response.result?.points || response.points || []).map(
            (point) => ({
              id: point.id,
              score: 1,
              // Default score for exact match
              payload: point.payload
            })
          );
          const countRequest = {};
          if (queryFilter) {
            countRequest.filter = queryFilter;
          }
          const countResponse = await this.client.count(this.collectionName, countRequest);
          this.logger.info(`${LOG_PREFIXES4.BACKEND} Listed ${results.length} vectors`);
          return [results, countResponse.count];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.BACKEND} List failed`, { error });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.QDRANT} Already connected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.QDRANT} Connecting to Qdrant`);
        try {
          const collections = await this.client.getCollections();
          const exists = collections.collections.some((c) => c.name === this.collectionName);
          if (!exists) {
            this.logger.info(`${LOG_PREFIXES4.QDRANT} Creating collection ${this.collectionName}`);
            const distanceMap = {
              Cosine: "Cosine",
              Euclidean: "Euclid",
              Dot: "Dot",
              Manhattan: "Manhattan"
            };
            const distance = this.config.distance || DEFAULTS2.QDRANT_DISTANCE;
            const qdrantDistance = distanceMap[distance] || "Cosine";
            await this.client.createCollection(this.collectionName, {
              vectors: {
                size: this.dimension,
                distance: qdrantDistance
              }
            });
          } else {
            const collectionInfo = await this.client.getCollection(this.collectionName);
            const vectorConfig = collectionInfo?.config?.params?.vectors;
            if (vectorConfig && "size" in vectorConfig && vectorConfig.size !== this.dimension) {
              throw new VectorStoreConnectionError(
                `Collection ${this.collectionName} exists with different dimension. Expected: ${this.dimension}, got: ${vectorConfig.size}`,
                "qdrant"
              );
            }
          }
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES4.QDRANT} Successfully connected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.QDRANT} Connection failed`, { error });
          if (error instanceof VectorStoreConnectionError) {
            throw error;
          }
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "qdrant",
            error
          );
        }
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.QDRANT} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.QDRANT} Disconnecting from Qdrant`);
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.QDRANT} Successfully disconnected`);
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "qdrant";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
    };
  }
});

// src/core/vector_storage/connection-pool.ts
var import_milvus2_sdk_node, MilvusConnectionPool, getMilvusConnectionPool;
var init_connection_pool = __esm({
  "src/core/vector_storage/connection-pool.ts"() {
    init_cjs_shims();
    import_milvus2_sdk_node = require("@zilliz/milvus2-sdk-node");
    init_logger2();
    init_env();
    MilvusConnectionPool = class _MilvusConnectionPool {
      static instance = null;
      connections = /* @__PURE__ */ new Map();
      maxConnections = 10;
      // Prevent connection bloat
      connectionTtl = 3e5;
      // 5 minutes TTL
      healthCheckInterval = 6e4;
      // 1 minute health checks
      healthCheckTimer;
      constructor() {
        this.startHealthCheckTimer();
      }
      /**
       * Get the singleton instance of MilvusConnectionPool
       */
      static getInstance() {
        if (!_MilvusConnectionPool.instance) {
          _MilvusConnectionPool.instance = new _MilvusConnectionPool();
        }
        return _MilvusConnectionPool.instance;
      }
      /**
       * Generate a unique connection key based on configuration
       */
      generateConnectionKey(config2) {
        const address = config2.url || (config2.host && config2.port ? `http://${config2.host}:${config2.port}` : env.VECTOR_STORE_URL);
        const username = config2.username || env.VECTOR_STORE_USERNAME || "";
        const password = config2.password ? "[REDACTED]" : "";
        return `${address}:${username}:${!!password}`;
      }
      /**
       * Get or create a Milvus client from the pool
       */
      async getClient(config2) {
        const connectionKey = this.generateConnectionKey(config2);
        const existingConnection = this.connections.get(connectionKey);
        if (existingConnection && existingConnection.isHealthy) {
          existingConnection.lastUsed = Date.now();
          existingConnection.refCount++;
          logger.debug("MilvusConnectionPool: Reusing existing connection", {
            connectionKey,
            refCount: existingConnection.refCount,
            poolSize: this.connections.size
          });
          return existingConnection.client;
        }
        if (existingConnection && !existingConnection.isHealthy) {
          logger.debug("MilvusConnectionPool: Removing unhealthy connection", { connectionKey });
          this.connections.delete(connectionKey);
        }
        if (this.connections.size >= this.maxConnections) {
          await this.evictLeastRecentlyUsedConnection();
        }
        logger.debug("MilvusConnectionPool: Creating new connection", {
          connectionKey,
          poolSize: this.connections.size
        });
        const client = await this.createMilvusClient(config2);
        const pooledConnection = {
          client,
          createdAt: Date.now(),
          lastUsed: Date.now(),
          isHealthy: true,
          refCount: 1
        };
        this.connections.set(connectionKey, pooledConnection);
        logger.debug("MilvusConnectionPool: Connection created and pooled", {
          connectionKey,
          poolSize: this.connections.size
        });
        return client;
      }
      /**
       * Release a client reference (decrement ref count)
       */
      releaseClient(config2) {
        const connectionKey = this.generateConnectionKey(config2);
        const connection = this.connections.get(connectionKey);
        if (connection) {
          connection.refCount = Math.max(0, connection.refCount - 1);
          logger.debug("MilvusConnectionPool: Client reference released", {
            connectionKey,
            refCount: connection.refCount
          });
        }
      }
      /**
       * Create a new Milvus client with the given configuration
       */
      async createMilvusClient(config2) {
        const address = config2.url || (config2.host && config2.port ? `http://${config2.host}:${config2.port}` : env.VECTOR_STORE_URL || "");
        const username = config2.username || env.VECTOR_STORE_USERNAME || "";
        const password = config2.password || env.VECTOR_STORE_PASSWORD || "";
        try {
          const client = new import_milvus2_sdk_node.MilvusClient({
            address,
            username,
            password
          });
          await client.showCollections();
          return client;
        } catch (error) {
          logger.error("MilvusConnectionPool: Failed to create client", {
            error: error instanceof Error ? error.message : String(error),
            address
          });
          throw error;
        }
      }
      /**
       * Evict the least recently used connection to make room for new ones
       */
      async evictLeastRecentlyUsedConnection() {
        let oldestKey = null;
        let oldestTime = Date.now();
        for (const [key, connection] of this.connections) {
          if (connection.refCount === 0 && connection.lastUsed < oldestTime) {
            oldestTime = connection.lastUsed;
            oldestKey = key;
          }
        }
        if (!oldestKey) {
          for (const [key, connection] of this.connections) {
            if (connection.lastUsed < oldestTime) {
              oldestTime = connection.lastUsed;
              oldestKey = key;
            }
          }
        }
        if (oldestKey) {
          logger.debug("MilvusConnectionPool: Evicting LRU connection", {
            evictedKey: oldestKey,
            poolSize: this.connections.size
          });
          await this.closeConnection(oldestKey);
        }
      }
      /**
       * Start periodic health check timer
       */
      startHealthCheckTimer() {
        this.healthCheckTimer = setInterval(() => {
          this.performHealthChecks();
        }, this.healthCheckInterval);
      }
      /**
       * Perform health checks on all pooled connections
       */
      async performHealthChecks() {
        const now = Date.now();
        const connectionsToRemove = [];
        for (const [key, connection] of this.connections) {
          if (now - connection.createdAt > this.connectionTtl) {
            logger.debug("MilvusConnectionPool: Connection expired", { key });
            connectionsToRemove.push(key);
            continue;
          }
          try {
            await connection.client.showCollections();
            connection.isHealthy = true;
          } catch (error) {
            logger.warn("MilvusConnectionPool: Connection health check failed", {
              key,
              error: error instanceof Error ? error.message : String(error)
            });
            connection.isHealthy = false;
            if (connection.refCount === 0) {
              connectionsToRemove.push(key);
            }
          }
        }
        for (const key of connectionsToRemove) {
          await this.closeConnection(key);
        }
        if (connectionsToRemove.length > 0) {
          logger.debug("MilvusConnectionPool: Health check completed", {
            removedConnections: connectionsToRemove.length,
            remainingConnections: this.connections.size
          });
        }
      }
      /**
       * Close and remove a connection from the pool
       */
      async closeConnection(key) {
        const connection = this.connections.get(key);
        if (connection) {
          try {
            this.connections.delete(key);
            logger.debug("MilvusConnectionPool: Connection closed and removed", { key });
          } catch (error) {
            logger.warn("MilvusConnectionPool: Error closing connection", {
              key,
              error: error instanceof Error ? error.message : String(error)
            });
            this.connections.delete(key);
          }
        }
      }
      /**
       * Get pool statistics for monitoring
       */
      getStats() {
        const now = Date.now();
        const connectionDetails = Array.from(this.connections.entries()).map(([key, connection]) => ({
          key,
          createdAt: connection.createdAt,
          lastUsed: connection.lastUsed,
          refCount: connection.refCount,
          isHealthy: connection.isHealthy,
          age: now - connection.createdAt
        }));
        return {
          totalConnections: this.connections.size,
          maxConnections: this.maxConnections,
          connectionDetails
        };
      }
      /**
       * Force close all connections and cleanup
       */
      async shutdown() {
        if (this.healthCheckTimer) {
          clearInterval(this.healthCheckTimer);
          this.healthCheckTimer = void 0;
        }
        const connectionKeys = Array.from(this.connections.keys());
        await Promise.allSettled(connectionKeys.map((key) => this.closeConnection(key)));
        logger.debug("MilvusConnectionPool: Shutdown completed", {
          closedConnections: connectionKeys.length
        });
      }
      /**
       * Get current pool size
       */
      size() {
        return this.connections.size;
      }
      /**
       * Check if a connection exists for the given config
       */
      hasConnection(config2) {
        const connectionKey = this.generateConnectionKey(config2);
        const connection = this.connections.get(connectionKey);
        return connection !== void 0 && connection.isHealthy;
      }
    };
    getMilvusConnectionPool = () => {
      return MilvusConnectionPool.getInstance();
    };
  }
});

// src/core/vector_storage/backend/milvus.ts
var milvus_exports = {};
__export(milvus_exports, {
  MilvusBackend: () => MilvusBackend
});
function filtersToExpr(filters) {
  if (!filters) return void 0;
  return Object.entries(filters).map(([key, value]) => {
    if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
      return `${key} == "${value}"`;
    }
    if (value && typeof value === "object") {
      if ("any" in value && Array.isArray(value.any)) {
        const arr = value.any.map((v) => `"${v}"`).join(", ");
        return `${key} in [${arr}]`;
      }
      const ops = { gte: ">=", lte: "<=", gt: ">", lt: "<" };
      return Object.entries(ops).filter(([op]) => op in value).map(([op, symbol]) => `${key} ${symbol} ${value[op]}`).join(" && ");
    }
    return "";
  }).filter(Boolean).join(" && ");
}
var import_milvus2_sdk_node2, MilvusBackend;
var init_milvus = __esm({
  "src/core/vector_storage/backend/milvus.ts"() {
    init_cjs_shims();
    import_milvus2_sdk_node2 = require("@zilliz/milvus2-sdk-node");
    init_types10();
    init_logger2();
    init_constants4();
    init_env();
    init_connection_pool();
    MilvusBackend = class {
      client = null;
      config;
      collectionName;
      dimension;
      logger;
      connected = false;
      connectionConfig;
      // Milvus collection configuration
      MILVUS_COLLECTION_CONFIG = {
        schema: [
          {
            name: "id",
            description: "Primary key",
            data_type: import_milvus2_sdk_node2.DataType.Int64,
            is_primary_key: true
          },
          {
            name: "vector",
            description: "Vector field",
            data_type: import_milvus2_sdk_node2.DataType.FloatVector
          },
          {
            name: "payload",
            description: "Payload",
            data_type: import_milvus2_sdk_node2.DataType.JSON
          }
        ]
      };
      constructor(config2) {
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: env.CIPHER_LOG_LEVEL || "info"
        });
        this.connectionConfig = {
          url: config2.url || void 0,
          host: config2.host || void 0,
          port: config2.port || void 0,
          username: config2.username || env.VECTOR_STORE_USERNAME || void 0,
          password: config2.password || env.VECTOR_STORE_PASSWORD || void 0
        };
        this.logger.debug(`${LOG_PREFIXES4.MILVUS} Backend initialized with connection pooling`, {
          collection: this.collectionName,
          dimension: this.dimension,
          connectionKey: this.generateConnectionKey()
        });
      }
      /**
       * Generate a connection key for logging (without sensitive data)
       */
      generateConnectionKey() {
        const address = this.connectionConfig.url || (this.connectionConfig.host && this.connectionConfig.port ? `${this.connectionConfig.host}:${this.connectionConfig.port}` : env.VECTOR_STORE_URL);
        return `${address}:${this.connectionConfig.username || "anonymous"}`;
      }
      /**
       * Ensure the client is available and connected
       */
      ensureClient() {
        if (!this.client) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "operation");
        }
        return this.client;
      }
      validateDimension(vector, _operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.MILVUS} Already connected`);
          return;
        }
        this.logger.debug(`${LOG_PREFIXES4.MILVUS} Connecting to Milvus via connection pool`);
        try {
          const connectionPool = getMilvusConnectionPool();
          this.client = await connectionPool.getClient(this.connectionConfig);
          const client = this.ensureClient();
          const collections = await client.showCollections();
          const exists = collections.data.some((c) => c.name === this.collectionName);
          if (!exists) {
            this.logger.debug(`${LOG_PREFIXES4.MILVUS} Creating collection ${this.collectionName}`);
            const schema = this.MILVUS_COLLECTION_CONFIG.schema.map(
              (field) => field.name === "vector" ? { ...field, dim: this.dimension } : { ...field }
            );
            await client.createCollection({
              collection_name: this.collectionName,
              fields: schema,
              // Include index parameters during collection creation
              index_params: [
                {
                  field_name: "vector",
                  index_name: "vector_index",
                  index_type: "AUTOINDEX",
                  metric_type: "COSINE"
                }
              ]
            });
            this.logger.debug(
              `${LOG_PREFIXES4.MILVUS} Collection created with indexes: ${this.collectionName}`
            );
          } else {
            this.logger.debug(
              `${LOG_PREFIXES4.MILVUS} Collection already exists: ${this.collectionName}`
            );
            try {
              const indexes = await client.describeIndex({
                collection_name: this.collectionName,
                field_name: "vector"
              });
              if (!indexes || !indexes.index_descriptions || indexes.index_descriptions.length === 0) {
                this.logger.debug(`${LOG_PREFIXES4.MILVUS} Creating missing vector index`);
                await this.createMissingIndexes();
              }
            } catch {
              this.logger.debug(`${LOG_PREFIXES4.MILVUS} Creating missing vector index`);
              await this.createMissingIndexes();
            }
          }
          const clientForLoad = this.ensureClient();
          await clientForLoad.loadCollection({ collection_name: this.collectionName });
          this.connected = true;
          this.logger.debug(`${LOG_PREFIXES4.MILVUS} Successfully connected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.MILVUS} Connection failed`, { error });
          if (error instanceof VectorStoreConnectionError) {
            throw error;
          }
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "milvus",
            error
          );
        }
      }
      /**
       * Create missing indexes for existing collections
       * This is a fallback method for collections that were created without indexes
       */
      async createMissingIndexes() {
        try {
          const clientForIndex = this.ensureClient();
          await clientForIndex.createIndex({
            collection_name: this.collectionName,
            field_name: "vector",
            index_name: "vector_index",
            index_type: "AUTOINDEX",
            metric_type: "COSINE"
          });
          await this.createPayloadFieldIndexes();
          this.logger.debug(`${LOG_PREFIXES4.MILVUS} Reloading collection after index creation`);
          const clientForReload = this.ensureClient();
          await clientForReload.loadCollection({ collection_name: this.collectionName });
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.MILVUS} Failed to create indexes and reload collection`);
          throw error;
        }
      }
      /**
       * Create indexes for payload fields to improve filtering performance
       */
      async createPayloadFieldIndexes() {
        const payloadFields = ["type", "category", "sessionId", "traceId", "timestamp"];
        for (const fieldName of payloadFields) {
          try {
            this.logger.debug(`${LOG_PREFIXES4.MILVUS} Creating index for field: ${fieldName}`);
            const clientForFieldIndex = this.ensureClient();
            await clientForFieldIndex.createIndex({
              collection_name: this.collectionName,
              field_name: `payload.${fieldName}`,
              index_type: "AUTOINDEX"
            });
          } catch (error) {
            this.logger.warn(
              `${LOG_PREFIXES4.MILVUS} Failed to create index for field ${fieldName}:`,
              error
            );
          }
        }
      }
      async insert(vectors, ids, payloads) {
        if (!this.connected)
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert"));
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          return Promise.reject(
            new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert")
          );
        }
        for (const vector of vectors) this.validateDimension(vector, "insert");
        const data = vectors.map((vector, idx) => ({
          id: ids[idx],
          vector,
          payload: payloads[idx]
        }));
        try {
          const clientForInsert = this.ensureClient();
          await clientForInsert.insert({
            collection_name: this.collectionName,
            data
          });
          this.logger.debug(`Inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`Insert failed`, { error });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = 10, filters) {
        if (!this.connected) throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        this.validateDimension(query2, "search");
        try {
          const expr = filtersToExpr(filters);
          const searchParams = {
            collection_name: this.collectionName,
            data: [query2],
            anns_field: "vector",
            params: { nprobe: 10 },
            limit,
            output_fields: ["id", "payload"],
            ...expr ? { filter: expr } : {}
          };
          const clientForSearch = this.ensureClient();
          const res = await clientForSearch.search(searchParams);
          return res.results.map((hit) => ({
            id: hit.id,
            score: hit.score,
            payload: hit.payload
          }));
        } catch (error) {
          this.logger.error(`Search failed`, { error });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected)
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get"));
        try {
          const clientForQuery = this.ensureClient();
          const res = await clientForQuery.query({
            collection_name: this.collectionName,
            output_fields: ["id", "vector", "payload"],
            filter: `id == ${vectorId}`
          });
          if (!res.data.length || !res.data[0]) return null;
          const doc = res.data[0];
          if (!doc) return null;
          return {
            id: vectorId,
            vector: doc.vector,
            payload: doc.payload,
            score: 1
          };
        } catch (error) {
          this.logger.error(`Get failed`, { error });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected)
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update"));
        this.validateDimension(vector, "update");
        try {
          const clientForUpsert = this.ensureClient();
          await clientForUpsert.upsert({
            collection_name: this.collectionName,
            data: [{ id: vectorId, vector, payload }]
          });
          this.logger.debug(`${LOG_PREFIXES4.MILVUS} Updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`Update failed`, { error });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected)
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete"));
        try {
          const clientForDelete = this.ensureClient();
          await clientForDelete.deleteEntities({
            collection_name: this.collectionName,
            expr: `id == ${vectorId}`
          });
          this.logger.debug(`${LOG_PREFIXES4.MILVUS} Deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`Delete failed`, { error });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected)
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        try {
          const clientForDrop = this.ensureClient();
          await clientForDrop.dropCollection({ collection_name: this.collectionName });
          this.logger.info(`Deleted collection ${this.collectionName}`);
        } catch (error) {
          this.logger.error(`Delete collection failed`, { error });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        try {
          const expr = filtersToExpr(filters);
          const queryParams = {
            collection_name: this.collectionName,
            output_fields: ["id", "vector", "payload"],
            limit,
            ...expr ? { filter: expr } : {}
          };
          const clientForList = this.ensureClient();
          const res = await clientForList.query(queryParams);
          const results = (res.data || []).map(
            (doc) => doc && {
              id: doc.id,
              vector: doc.vector,
              payload: doc.payload,
              score: 1
            }
          ).filter(Boolean);
          return [results, results.length];
        } catch (error) {
          this.logger.error(`List failed`, { error });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      async disconnect() {
        if (this.client) {
          const connectionPool = getMilvusConnectionPool();
          connectionPool.releaseClient(this.connectionConfig);
          this.client = null;
        }
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.MILVUS} Disconnected and released connection to pool`);
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "milvus";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
      async listCollections() {
        if (!this.connected)
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "listCollections"));
        const clientForListCollections = this.ensureClient();
        const collections = await clientForListCollections.showCollections();
        return collections.data.map((c) => c.name);
      }
    };
  }
});

// src/core/vector_storage/backend/chroma-payload-adapter.ts
var DEFAULT_CONFIG2, DefaultChromaPayloadAdapter;
var init_chroma_payload_adapter = __esm({
  "src/core/vector_storage/backend/chroma-payload-adapter.ts"() {
    init_cjs_shims();
    init_logger2();
    init_constants4();
    DEFAULT_CONFIG2 = {
      defaultStrategy: "json-string",
      autoFlattenNested: true,
      maxNestingDepth: 3,
      fieldConfigs: {
        // Legacy field configurations for backward compatibility
        tags: { strategy: "comma-separated" },
        currentProgress: { strategy: "json-string" },
        bugsEncountered: { strategy: "json-string" },
        workContext: { strategy: "json-string" },
        // Common field patterns
        id: { strategy: "preserve" },
        created_at: { strategy: "preserve" },
        updated_at: { strategy: "preserve" },
        timestamp: { strategy: "preserve" },
        score: { strategy: "preserve" },
        version: { strategy: "preserve" }
      }
    };
    DefaultChromaPayloadAdapter = class {
      config;
      logger;
      constructor(config2) {
        this.config = { ...DEFAULT_CONFIG2, ...config2 };
        this.logger = createLogger({
          level: process.env.CIPHER_LOG_LEVEL || "info"
        });
        if (config2?.fieldConfigs) {
          this.config.fieldConfigs = { ...DEFAULT_CONFIG2.fieldConfigs, ...config2.fieldConfigs };
        }
      }
      /**
       * Serialize complex payload to ChromaDB-compatible metadata
       */
      serialize(payload) {
        if (!payload || typeof payload !== "object") {
          return {};
        }
        const result = {};
        for (const [key, value] of Object.entries(payload)) {
          if (value === null || value === void 0) {
            continue;
          }
          const fieldConfig = this.config.fieldConfigs[key];
          const strategy = fieldConfig?.strategy || this.getStrategyForValue(value);
          try {
            const transformedEntries = this.serializeField(key, value, strategy, fieldConfig);
            Object.assign(result, transformedEntries);
          } catch (error) {
            this.logger.warn(`${LOG_PREFIXES4.CHROMA} Failed to serialize field '${key}'`, {
              key,
              strategy,
              error: error instanceof Error ? error.message : String(error)
            });
            if (strategy !== "json-string") {
              try {
                result[key] = JSON.stringify(value);
              } catch (jsonError) {
                this.logger.error(`${LOG_PREFIXES4.CHROMA} Failed JSON fallback for field '${key}'`, {
                  key,
                  jsonError: jsonError instanceof Error ? jsonError.message : String(jsonError)
                });
              }
            }
          }
        }
        return result;
      }
      /**
       * Deserialize ChromaDB metadata back to original payload format
       */
      deserialize(metadata) {
        if (!metadata || typeof metadata !== "object") {
          return {};
        }
        const result = {};
        const processedKeys = /* @__PURE__ */ new Set();
        for (const [key, value] of Object.entries(metadata)) {
          if (processedKeys.has(key)) {
            continue;
          }
          const fieldConfig = this.config.fieldConfigs[key];
          if (fieldConfig) {
            try {
              result[key] = this.deserializeField(key, value, fieldConfig.strategy, fieldConfig);
              processedKeys.add(key);
            } catch (error) {
              this.logger.warn(`${LOG_PREFIXES4.CHROMA} Failed to deserialize field '${key}'`, {
                key,
                strategy: fieldConfig.strategy,
                error: error instanceof Error ? error.message : String(error)
              });
              result[key] = value;
              processedKeys.add(key);
            }
          }
        }
        if (this.config.autoFlattenNested) {
          const nestedGroups = this.groupFlattenedKeys(metadata, processedKeys);
          for (const [rootKey, nestedData] of Object.entries(nestedGroups)) {
            result[rootKey] = this.reconstructNestedObject(nestedData);
            for (const flatKey in metadata) {
              if (flatKey.startsWith(rootKey + "_")) {
                processedKeys.add(flatKey);
              }
            }
          }
        }
        for (const [key, value] of Object.entries(metadata)) {
          if (!processedKeys.has(key)) {
            const strategy = this.inferDeserializationStrategy(value);
            result[key] = this.deserializeField(key, value, strategy);
          }
        }
        return result;
      }
      /**
       * Get transformation configuration
       */
      getConfig() {
        return { ...this.config };
      }
      /**
       * Update transformation configuration
       */
      updateConfig(config2) {
        const mergedFieldConfigs = config2.fieldConfigs ? { ...this.config.fieldConfigs, ...config2.fieldConfigs } : this.config.fieldConfigs;
        this.config = {
          ...this.config,
          ...config2,
          fieldConfigs: mergedFieldConfigs
        };
      }
      /**
       * Determine the best strategy for a given value
       */
      getStrategyForValue(value) {
        if (value === null || value === void 0) {
          return "preserve";
        }
        if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
          return "preserve";
        }
        if (Array.isArray(value)) {
          if (value.every((item) => typeof item === "string" || typeof item === "number")) {
            return "comma-separated";
          }
          return "json-string";
        }
        if (typeof value === "object") {
          if (Object.keys(value).length === 0) {
            return "json-string";
          }
          if (this.isSimpleObject(value)) {
            return "dot-notation";
          }
          return "json-string";
        }
        return this.config.defaultStrategy;
      }
      /**
       * Infer deserialization strategy based on stored value characteristics
       */
      inferDeserializationStrategy(value) {
        if (typeof value === "string") {
          if (value.includes(",") && !value.startsWith("{") && !value.startsWith("[")) {
            return "comma-separated";
          }
          if (value.startsWith("{") && value.endsWith("}") || value.startsWith("[") && value.endsWith("]")) {
            return "json-string";
          }
        }
        return "preserve";
      }
      /**
       * Check if an object is simple enough for dot notation flattening
       */
      isSimpleObject(obj, depth = 0) {
        if (depth >= this.config.maxNestingDepth) {
          return false;
        }
        if (!obj || typeof obj !== "object" || Array.isArray(obj)) {
          return false;
        }
        const keys = Object.keys(obj);
        if (keys.length > 10) {
          return false;
        }
        return Object.values(obj).every((value) => {
          if (value === null || value === void 0) return true;
          if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
            return true;
          }
          if (Array.isArray(value)) {
            return value.length <= 5 && value.every((item) => typeof item === "string" || typeof item === "number");
          }
          if (typeof value === "object") {
            return this.isSimpleObject(value, depth + 1);
          }
          return false;
        });
      }
      /**
       * Serialize a single field based on strategy
       */
      serializeField(key, value, strategy, config2) {
        const result = {};
        if (config2?.customTransformer) {
          const transformed = config2.customTransformer.serialize(value);
          if (typeof transformed === "object" && !Array.isArray(transformed)) {
            return transformed;
          }
          result[key] = transformed;
          return result;
        }
        switch (strategy) {
          case "preserve":
            if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
              result[key] = value;
            } else {
              result[key] = String(value);
            }
            break;
          case "json-string":
            result[key] = JSON.stringify(value);
            break;
          case "comma-separated":
            if (Array.isArray(value)) {
              result[key] = value.filter((item) => item != null).join(",");
            } else {
              result[key] = String(value);
            }
            break;
          case "boolean-flags":
            if (Array.isArray(value)) {
              const prefix = config2?.prefix || key;
              for (const item of value) {
                if (item != null) {
                  result[`${prefix}_${String(item)}`] = true;
                }
              }
            } else {
              result[key] = Boolean(value);
            }
            break;
          case "dot-notation":
            if (typeof value === "object" && value !== null && !Array.isArray(value)) {
              const flattened = this.flattenObject(value, key);
              Object.assign(result, flattened);
            } else {
              result[key] = String(value);
            }
            break;
          default:
            result[key] = JSON.stringify(value);
        }
        return result;
      }
      /**
       * Deserialize a single field based on strategy
       */
      deserializeField(key, value, strategy, config2) {
        if (config2?.customTransformer) {
          return config2.customTransformer.deserialize(value);
        }
        switch (strategy) {
          case "preserve":
            return value;
          case "json-string":
            if (typeof value === "string") {
              try {
                return JSON.parse(value);
              } catch {
                return value;
              }
            }
            return value;
          case "comma-separated":
            if (typeof value === "string") {
              if (value === "") {
                return [];
              }
              return value.split(",").filter((item) => item.trim().length > 0).map((item) => {
                const trimmed = item.trim();
                if (/^-?\d+(\.\d+)?$/.test(trimmed)) {
                  const num = Number(trimmed);
                  return isNaN(num) ? trimmed : num;
                }
                return trimmed;
              });
            }
            return Array.isArray(value) ? value : [value];
          case "boolean-flags":
            return value;
          case "dot-notation":
            return value;
          default:
            if (typeof value === "string" && (value.startsWith("{") || value.startsWith("["))) {
              try {
                return JSON.parse(value);
              } catch {
                return value;
              }
            }
            return value;
        }
      }
      /**
       * Flatten nested object using dot notation
       */
      flattenObject(obj, prefix = "", depth = 0) {
        const result = {};
        if (depth >= this.config.maxNestingDepth) {
          result[prefix] = JSON.stringify(obj);
          return result;
        }
        for (const [key, value] of Object.entries(obj)) {
          if (value === null || value === void 0) {
            continue;
          }
          const newKey = prefix ? `${prefix}_${key}` : key;
          if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
            result[newKey] = value;
          } else if (Array.isArray(value)) {
            if (value.every((item) => typeof item === "string" || typeof item === "number")) {
              result[newKey] = value.join(",");
            } else {
              result[newKey] = JSON.stringify(value);
            }
          } else if (typeof value === "object") {
            Object.assign(result, this.flattenObject(value, newKey, depth + 1));
          } else {
            result[newKey] = String(value);
          }
        }
        return result;
      }
      /**
       * Group flattened keys by their root key
       */
      groupFlattenedKeys(metadata, processedKeys) {
        const groups = {};
        for (const key in metadata) {
          if (processedKeys.has(key)) {
            continue;
          }
          const underscoreIndex = key.indexOf("_");
          if (underscoreIndex > 0) {
            const rootKey = key.substring(0, underscoreIndex);
            const remainingKey = key.substring(underscoreIndex + 1);
            if (!groups[rootKey]) {
              groups[rootKey] = {};
            }
            groups[rootKey][remainingKey] = metadata[key];
          }
        }
        return groups;
      }
      /**
       * Reconstruct nested object from flattened keys
       */
      reconstructNestedObject(flatData) {
        const result = {};
        for (const [key, value] of Object.entries(flatData)) {
          const parts = key.split("_");
          let current = result;
          for (let i = 0; i < parts.length - 1; i++) {
            const part = parts[i];
            if (!part) continue;
            if (!(part in current)) {
              current[part] = {};
            }
            current = current[part];
          }
          const finalKey = parts[parts.length - 1];
          if (!finalKey) continue;
          if (typeof value === "string" && value.includes(",")) {
            const arrayValue = value.split(",").filter((item) => item.trim().length > 0).map((item) => {
              const trimmed = item.trim();
              if (/^-?\d+(\.\d+)?$/.test(trimmed)) {
                const num = Number(trimmed);
                return isNaN(num) ? trimmed : num;
              }
              return trimmed;
            });
            current[finalKey] = arrayValue.length > 1 ? arrayValue : value;
          } else {
            current[finalKey] = value;
          }
        }
        return result;
      }
    };
  }
});

// src/core/vector_storage/backend/chroma.ts
var chroma_exports = {};
__export(chroma_exports, {
  ChromaBackend: () => ChromaBackend
});
var import_chromadb, ChromaBackend;
var init_chroma = __esm({
  "src/core/vector_storage/backend/chroma.ts"() {
    init_cjs_shims();
    import_chromadb = require("chromadb");
    init_types10();
    init_logger2();
    init_constants4();
    init_chroma_payload_adapter();
    ChromaBackend = class {
      client;
      collection = null;
      config;
      collectionName;
      dimension;
      logger;
      payloadAdapter;
      connected = false;
      constructor(config2, payloadAdapter) {
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: process.env.CIPHER_LOG_LEVEL || "info"
        });
        this.payloadAdapter = payloadAdapter || new DefaultChromaPayloadAdapter();
        const clientConfig = {};
        if (config2.url) {
          const url = new URL(config2.url);
          clientConfig.host = url.hostname;
          clientConfig.port = parseInt(url.port) || (url.protocol === "https:" ? 443 : 80);
          clientConfig.ssl = url.protocol === "https:";
        } else if (config2.host) {
          clientConfig.host = config2.host;
          clientConfig.port = config2.port || 8e3;
          clientConfig.ssl = config2.ssl || false;
        }
        if (config2.headers) {
          clientConfig.fetchOptions = {
            headers: config2.headers
          };
        }
        this.client = new import_chromadb.ChromaClient(clientConfig);
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Initialized`, {
          collection: this.collectionName,
          dimension: this.dimension,
          url: config2.url || `${clientConfig.ssl ? "https" : "http"}://${clientConfig.host}:${clientConfig.port}` || "embedded"
        });
      }
      /**
       * Convert search filters to ChromaDB filter format
       */
      createFilter(filters) {
        if (!filters) return void 0;
        const chromaFilter = {};
        for (const [key, value] of Object.entries(filters)) {
          if (value === null || value === void 0) continue;
          if (typeof value === "object" && !Array.isArray(value) && ("gte" in value || "gt" in value || "lte" in value || "lt" in value)) {
            const rangeConditions = {};
            if ("gte" in value && value.gte !== void 0) rangeConditions.$gte = value.gte;
            if ("gt" in value && value.gt !== void 0) rangeConditions.$gt = value.gt;
            if ("lte" in value && value.lte !== void 0) rangeConditions.$lte = value.lte;
            if ("lt" in value && value.lt !== void 0) rangeConditions.$lt = value.lt;
            chromaFilter[key] = rangeConditions;
          } else if (typeof value === "object" && !Array.isArray(value) && ("any" in value || "all" in value)) {
            if ("any" in value && Array.isArray(value.any)) {
              chromaFilter[key] = { $in: value.any };
            } else if ("all" in value && Array.isArray(value.all) && value.all.length > 0) {
              chromaFilter[key] = value.all[0];
            }
          } else {
            chromaFilter[key] = value;
          }
        }
        return Object.keys(chromaFilter).length > 0 ? chromaFilter : void 0;
      }
      /**
       * Validate vector dimension
       */
      validateDimension(vector, operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      /**
       * Validate ID (ChromaDB supports string and number IDs, we'll convert numbers to strings)
       */
      validateId(id) {
        if (!Number.isInteger(id) || isNaN(id)) {
          throw new VectorStoreError("ChromaDB point IDs must be valid integers", "id");
        }
      }
      /**
       * Get the payload adapter instance
       */
      getPayloadAdapter() {
        return this.payloadAdapter;
      }
      // VectorStore implementation
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (!this.collection) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Collection is null during insert`);
          throw new VectorStoreError("ChromaDB collection not initialized", "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        for (let i = 0; i < vectors.length; i++) {
          const vector = vectors[i];
          const id = ids[i];
          if (!vector) {
            throw new VectorStoreError(`Vector missing at index ${i}`, "insert");
          }
          if (id === void 0 || id === null) {
            throw new VectorStoreError(`ID missing at index ${i}`, "insert");
          }
          this.validateDimension(vector, "insert");
          this.validateId(id);
        }
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Inserting ${vectors.length} vectors`);
        const stringIds = ids.map((id) => id.toString());
        const chromaCompatiblePayloads = payloads.map(
          (payload) => this.payloadAdapter.serialize(payload)
        );
        const addParams = {
          ids: stringIds,
          embeddings: vectors,
          metadatas: chromaCompatiblePayloads
        };
        try {
          await this.collection.upsert(addParams);
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Successfully inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Insert failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorCount: vectors.length,
            collectionName: this.collectionName
          });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        if (!this.collection) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Collection is null during search`);
          throw new VectorStoreError("ChromaDB collection not initialized", "search");
        }
        this.validateDimension(query2, "search");
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Searching with limit ${limit}`, {
          hasFilters: !!filters
        });
        try {
          const chromaFilter = this.createFilter(filters);
          const searchParams = {
            queryEmbeddings: [query2],
            nResults: limit
          };
          if (chromaFilter) {
            searchParams.where = chromaFilter;
          }
          const searchResponse = await this.collection.query(searchParams);
          const results = [];
          if (searchResponse.ids && searchResponse.ids[0]) {
            for (let i = 0; i < searchResponse.ids[0].length; i++) {
              const id = searchResponse.ids[0][i];
              const distance = searchResponse.distances?.[0]?.[i] ?? 0;
              const metadata = searchResponse.metadatas?.[0]?.[i] ?? {};
              const score = 1 - distance;
              results.push({
                id: parseInt(id, 10),
                // Convert back to number
                score,
                payload: this.payloadAdapter.deserialize(metadata)
              });
            }
          }
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Found ${results.length} results`);
          return results;
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Search failed`, {
            error: error instanceof Error ? error.message : String(error),
            queryLength: query2.length,
            limit,
            collectionName: this.collectionName
          });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Getting vector ${vectorId}`);
        try {
          const response = await this.collection.get({
            ids: [vectorId.toString()],
            include: ["embeddings", "metadatas"]
          });
          if (!response.ids || response.ids.length === 0) {
            return null;
          }
          const id = response.ids[0];
          const embedding = response.embeddings?.[0];
          const metadata = response.metadatas?.[0] ?? {};
          return {
            id: parseInt(id, 10),
            vector: embedding || [],
            payload: this.payloadAdapter.deserialize(metadata),
            score: 1
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Get failed`, { error });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Updating vector ${vectorId}`);
        try {
          const convertedPayload = this.payloadAdapter.serialize(payload);
          await this.collection.upsert({
            ids: [vectorId.toString()],
            embeddings: [vector],
            metadatas: [convertedPayload]
          });
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Successfully updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Update failed`, { error });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Deleting vector ${vectorId}`);
        try {
          await this.collection.delete({
            ids: [vectorId.toString()]
          });
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Successfully deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Delete failed`, { error });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        this.logger.warn(`${LOG_PREFIXES4.CHROMA} Deleting collection ${this.collectionName}`);
        try {
          await this.client.deleteCollection({ name: this.collectionName });
          this.logger.info(
            `${LOG_PREFIXES4.CHROMA} Successfully deleted collection ${this.collectionName}`
          );
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Delete collection failed`, { error });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        this.logger.debug(`${LOG_PREFIXES4.CHROMA} Listing vectors`, {
          hasFilters: !!filters,
          limit
        });
        try {
          const chromaFilter = this.createFilter(filters);
          const getParams = {
            include: ["embeddings", "metadatas"],
            limit
          };
          if (chromaFilter) {
            getParams.where = chromaFilter;
          }
          const response = await this.collection.get(getParams);
          const results = [];
          if (response.ids) {
            for (let i = 0; i < response.ids.length; i++) {
              const id = response.ids[i];
              const embedding = response.embeddings?.[i];
              const metadata = response.metadatas?.[i] ?? {};
              results.push({
                id: parseInt(id, 10),
                score: 1,
                // Default score for exact match
                payload: this.payloadAdapter.deserialize(metadata),
                vector: embedding
              });
            }
          }
          this.logger.info(`${LOG_PREFIXES4.CHROMA} Listed ${results.length} vectors`);
          return [results, results.length];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} List failed`, { error });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Already connected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.CHROMA} Connecting to ChromaDB`);
        try {
          try {
            this.collection = await this.client.getCollection({
              name: this.collectionName,
              embeddingFunction: null
              // Use null to indicate no embedding function
            });
            this.logger.debug(
              `${LOG_PREFIXES4.CHROMA} Collection already exists: ${this.collectionName}`
            );
          } catch (error) {
            this.logger.debug(`${LOG_PREFIXES4.CHROMA} Creating collection ${this.collectionName}`);
            const distance = this.config.distance || "cosine";
            let metricName = "cosine";
            if (distance === "euclidean" || distance === "l2") {
              metricName = "l2";
            } else if (distance === "ip" || distance === "dot") {
              metricName = "ip";
            }
            this.collection = await this.client.createCollection({
              name: this.collectionName,
              metadata: {
                "hnsw:space": metricName
              },
              embeddingFunction: null
              // Use null to indicate no embedding function
            });
          }
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES4.CHROMA} Successfully connected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.CHROMA} Connection failed`, { error });
          if (error instanceof VectorStoreConnectionError) {
            throw error;
          }
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "chroma",
            error
          );
        }
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.CHROMA} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.CHROMA} Disconnecting from ChromaDB`);
        this.collection = null;
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.CHROMA} Successfully disconnected`);
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "chroma";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
    };
  }
});

// src/core/vector_storage/backend/pinecone.ts
var pinecone_exports = {};
__export(pinecone_exports, {
  PineconeBackend: () => PineconeBackend
});
var import_pinecone, PineconeBackend;
var init_pinecone = __esm({
  "src/core/vector_storage/backend/pinecone.ts"() {
    init_cjs_shims();
    import_pinecone = require("@pinecone-database/pinecone");
    init_types10();
    init_logger2();
    init_constants4();
    PineconeBackend = class {
      client;
      index = null;
      config;
      indexName;
      dimension;
      provider;
      region;
      logger;
      connected = false;
      constructor(config2) {
        this.config = config2;
        this.indexName = config2.collectionName;
        this.dimension = config2.dimension;
        this.provider = config2.provider || DEFAULTS2.PINECONE_PROVIDER;
        this.region = config2.region || DEFAULTS2.PINECONE_REGION;
        this.logger = createLogger({
          level: process.env.CIPHER_LOG_LEVEL || "info"
        });
        this.client = new import_pinecone.Pinecone({
          apiKey: this.config.apiKey
        });
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Initialized`, {
          indexName: this.indexName,
          dimension: this.dimension
        });
        console.log("PineconeBackend initialized", {
          indexName: this.indexName,
          dimension: this.dimension
        });
      }
      /**
       * Convert search filters to Pinecone filter format
       */
      createFilter(filters) {
        if (!filters) return void 0;
        const pineconeFilter = {};
        for (const [key, value] of Object.entries(filters)) {
          if (value === null || value === void 0) continue;
          if (typeof value === "object" && !Array.isArray(value) && ("gte" in value || "gt" in value || "lte" in value || "lt" in value)) {
            const rangeConditions = {};
            if ("gte" in value && value.gte !== void 0) rangeConditions.$gte = value.gte;
            if ("gt" in value && value.gt !== void 0) rangeConditions.$gt = value.gt;
            if ("lte" in value && value.lte !== void 0) rangeConditions.$lte = value.lte;
            if ("lt" in value && value.lt !== void 0) rangeConditions.$lt = value.lt;
            pineconeFilter[key] = rangeConditions;
          } else if (typeof value === "object" && !Array.isArray(value) && ("any" in value || "all" in value)) {
            if ("any" in value && Array.isArray(value.any)) {
              pineconeFilter[key] = { $in: value.any };
            } else if ("all" in value && Array.isArray(value.all) && value.all.length > 0) {
              pineconeFilter[key] = value.all[0];
            }
          } else {
            pineconeFilter[key] = { $eq: value };
          }
        }
        return Object.keys(pineconeFilter).length > 0 ? pineconeFilter : void 0;
      }
      /**
       * Validate vector dimension
       */
      validateDimension(vector, operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      /**
       * Validate ID (Pinecone supports string IDs)
       */
      validateId(id) {
        if (id === void 0 || id === null || !Number.isInteger(id)) {
          throw new VectorStoreError("Pinecone point IDs must be valid integers", "id");
        }
      }
      /**
       * Map provider string to valid Pinecone cloud values
       */
      getCloudprovider(provider) {
        switch (provider.toLowerCase()) {
          case "aws":
          case "amazon":
            return "aws";
          case "gcp":
          case "google":
          case "gcloud":
            return "gcp";
          case "azure":
          case "microsoft":
            return "azure";
          default:
            this.logger.warn(
              `${LOG_PREFIXES4.PINECONE} Unknown provider '${provider}', defaulting to 'aws'`
            );
            return "aws";
        }
      }
      /**
       * Create index in Pinecone
       */
      async createIndex() {
        try {
          this.logger.info(`${LOG_PREFIXES4.PINECONE} Creating index '${this.indexName}'`, {
            dimension: this.dimension,
            metric: this.config.metric || "cosine"
          });
          const Cloudprovider = this.getCloudprovider(this.provider);
          const result = await this.client.createIndex({
            name: this.indexName,
            dimension: this.dimension,
            metric: this.config.metric || "cosine",
            spec: {
              serverless: {
                cloud: Cloudprovider,
                region: this.region
              }
            }
          });
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Failed to create index - DETAILED ERROR`, {
            error: error instanceof Error ? error.message : String(error),
            indexName: this.indexName,
            errorType: error?.constructor?.name,
            errorCode: error?.code,
            errorStatus: error?.status,
            errorResponse: error?.response?.data,
            fullError: JSON.stringify(error, null, 2)
          });
          throw error;
        }
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Already connected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.PINECONE} Connecting to Pinecone`);
        try {
          console.log("Trying to list indexes");
          const indexList = await this.client.listIndexes();
          const indexExists = indexList.indexes?.some((index) => index.name === this.indexName);
          console.log("indexExists", indexExists);
          if (!indexExists) {
            this.logger.info(
              `${LOG_PREFIXES4.PINECONE} Index '${this.indexName}' does not exist, creating...`
            );
            await this.createIndex();
          }
          this.index = this.client.index(this.indexName);
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES4.PINECONE} Successfully connected`, {
            indexName: this.indexName,
            dimension: this.dimension
          });
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Connection failed`, {
            error: error instanceof Error ? error.message : String(error),
            indexName: this.indexName
          });
          if (error instanceof Error) {
            if (error.message.includes("404") || error.message.includes("not found") || error.message.includes("does not exist")) {
              throw new VectorStoreConnectionError(
                `Pinecone index '${this.indexName}' not found (HTTP 404). Please create the index in your Pinecone dashboard first or set PINECONE_AUTO_CREATE_INDEX=true.`,
                "pinecone",
                error
              );
            }
            if (error.message.includes("401") || error.message.includes("403") || error.message.includes("Unauthorized") || error.message.includes("Forbidden")) {
              throw new VectorStoreConnectionError(
                `Pinecone authentication failed. Please check your API key.`,
                "pinecone",
                error
              );
            }
            if (error.message.includes("429") || error.message.includes("rate limit")) {
              throw new VectorStoreConnectionError(
                `Pinecone rate limit exceeded. Please try again later.`,
                "pinecone",
                error
              );
            }
          }
          if (error instanceof VectorStoreConnectionError) {
            throw error;
          }
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "pinecone",
            error
          );
        }
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.PINECONE} Disconnecting from Pinecone`);
        this.index = null;
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.PINECONE} Successfully disconnected`);
      }
      async insert(vectors, ids, payloads) {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        if (vectors.length === 0) {
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} No vectors to insert`);
          return;
        }
        for (let i = 0; i < vectors.length; i++) {
          const vector = vectors[i];
          const id = ids[i];
          if (!vector) {
            throw new Error(`Vector at index ${i} is missing`);
          }
          if (id == null) {
            throw new Error(`ID at index ${i} is missing`);
          }
          this.validateDimension(vector, "insert");
          this.validateId(id);
        }
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Inserting ${vectors.length} vectors`);
        try {
          const upsertData = vectors.map((vector, idx) => {
            const id = ids[idx];
            if (id == null) {
              throw new Error(`ID at index ${idx} is missing`);
            }
            return {
              id: id.toString(),
              values: vector,
              metadata: payloads[idx] || {}
            };
          });
          await this.index.upsert(upsertData);
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Successfully inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Insert failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorCount: vectors.length
          });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        if (limit <= 0) {
          throw new VectorStoreError("Search limit must be positive", "search");
        }
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Searching with limit ${limit}`, {
          hasFilters: !!filters
        });
        try {
          const pineconeFilter = this.createFilter(filters);
          const queryRequest = {
            vector: query2,
            topK: Math.min(limit, 1e4),
            // Pinecone limit
            includeMetadata: true,
            includeValues: false
            // Usually not needed for search results
          };
          if (pineconeFilter) {
            queryRequest.filter = pineconeFilter;
          }
          const searchResponse = await this.index.query(queryRequest);
          const results = [];
          if (searchResponse.matches) {
            for (const match of searchResponse.matches) {
              results.push({
                id: parseInt(match.id, 10),
                score: match.score || 0,
                payload: match.metadata || {},
                vector: match.values || []
              });
            }
          }
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Found ${results.length} results`);
          return results;
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Search failed`, {
            error: error instanceof Error ? error.message : String(error),
            limit
          });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Getting vector ${vectorId}`);
        try {
          const fetchRequest = {
            ids: [vectorId.toString()]
          };
          const response = await this.index.fetch(fetchRequest);
          if (!response.vectors || !response.vectors[vectorId.toString()]) {
            return null;
          }
          const vector = response.vectors[vectorId.toString()];
          return {
            id: vectorId,
            vector: vector.values || [],
            payload: vector.metadata || {},
            score: 1
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Get failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Updating vector ${vectorId}`);
        try {
          const upsertRequest = [
            {
              id: vectorId.toString(),
              values: vector,
              metadata: payload || {}
            }
          ];
          await this.index.upsert(upsertRequest);
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Successfully updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Update failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.PINECONE} Deleting vector ${vectorId}`);
        try {
          const deleteRequest = {
            ids: [vectorId.toString()]
          };
          await this.index.delete(deleteRequest);
          this.logger.debug(`${LOG_PREFIXES4.PINECONE} Successfully deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Delete failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected || !this.index) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        try {
          const deleteRequest = {
            deleteAll: true
          };
          await this.index.delete(deleteRequest);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PINECONE} Delete collection failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        throw new VectorStoreError(
          "Pinecone does not support listing all vectors directly. Use search with appropriate filters instead.",
          "list"
        );
      }
      // Getter methods
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "pinecone";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.indexName;
      }
    };
  }
});

// src/core/vector_storage/backend/pgvector.ts
var pgvector_exports = {};
__export(pgvector_exports, {
  PgVectorBackend: () => PgVectorBackend
});
var import_pg2, VECTOR_OID, parseVector, PgVectorBackend;
var init_pgvector = __esm({
  "src/core/vector_storage/backend/pgvector.ts"() {
    init_cjs_shims();
    import_pg2 = require("pg");
    init_types10();
    init_logger2();
    init_constants4();
    VECTOR_OID = 16405;
    parseVector = (value) => {
      if (value.startsWith("[") && value.endsWith("]")) {
        return value.substring(1, value.length - 1).split(",").map(parseFloat);
      }
      return [];
    };
    import_pg2.types.setTypeParser(VECTOR_OID, parseVector);
    PgVectorBackend = class {
      pool;
      config;
      collectionName;
      dimension;
      logger;
      connected = false;
      vectorOid;
      constructor(config2) {
        if (config2.type !== "pgvector") {
          throw new VectorStoreError("Invalid config type for PgVectorBackend", "initialization");
        }
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: process.env.LOG_LEVEL || "info"
        });
        const Poolconfig = {
          max: config2.poolSize || 10
        };
        if (config2.url) {
          Poolconfig.connectionString = config2.url;
          Poolconfig.ssl = config2.ssl || false;
        } else {
          throw new VectorStoreError(
            "Missing connection configuration for PgVectorBackend",
            "initialization"
          );
        }
        this.pool = new import_pg2.Pool(Poolconfig);
        this.logger.debug(`${LOG_PREFIXES4.PGVECTOR} Initialized`, {
          collection: this.collectionName,
          dimension: this.dimension
        });
      }
      // @override
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.PGVECTOR} Already connected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.PGVECTOR} Connecting to PostgreSQL`);
        try {
          const client = await this.pool.connect();
          try {
            await client.query("CREATE EXTENSION IF NOT EXISTS vector");
            const { rows } = await client.query("SELECT oid FROM pg_type WHERE typname = 'vector'");
            this.vectorOid = rows[0]?.oid;
            if (this.vectorOid) import_pg2.types.setTypeParser(this.vectorOid, parseVector);
            const tableExists = await client.query(
              `SELECT EXISTS (
            SELECT FROM information_schema.tables
            WHERE table_name = $1
          )`,
              [this.collectionName]
            );
            if (!tableExists.rows[0].exists) {
              this.logger.info(`${LOG_PREFIXES4.PGVECTOR} Creating table ${this.collectionName}`);
              await client.query(
                `CREATE TABLE "${this.collectionName}" (
              id BIGSERIAL PRIMARY KEY,
              vector vector(${this.dimension}),
              payload JSONB
            )`
              );
              const indexType = this.config.indexType || "ivfflat";
              const indexMetric = this.config.indexMetric || "vector_l2_ops";
              await client.query(
                `CREATE INDEX IF NOT EXISTS ${this.collectionName}_vector_idx
             ON "${this.collectionName}"
             USING ${indexType} (vector ${indexMetric})
             WITH (lists = 100)`
              );
            }
            const payloadFields = ["type", "category", "sessionId", "traceId", "timestamp"];
            for (const field of payloadFields) {
              await client.query(
                `CREATE INDEX IF NOT EXISTS ${this.collectionName}_payload_${field}_idx ON "${this.collectionName}" ((payload->>'${field}'))`
              );
            }
            await client.query(
              `CREATE INDEX IF NOT EXISTS ${this.collectionName}_tags_gin_idx ON "${this.collectionName}" USING GIN ((payload->'tags'));`
            );
          } finally {
            client.release();
          }
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES4.PGVECTOR} Successfully connected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PGVECTOR} Connection failed: ${error}`);
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "pgvector",
            error
          );
        }
      }
      // @override
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.PGVECTOR} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.PGVECTOR} Disconnecting from PostgreSQL`);
        await this.pool.end();
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.PGVECTOR} Successfully disconnected`);
      }
      // @override
      isConnected() {
        return this.connected;
      }
      // @override
      getBackendType() {
        return "pgvector";
      }
      // @override
      getDimension() {
        return this.dimension;
      }
      // @override
      getCollectionName() {
        return this.collectionName;
      }
      // @override
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        const client = await this.pool.connect();
        try {
          await client.query("BEGIN");
          const values = [];
          const placeholders = [];
          for (let i = 0; i < vectors.length; i++) {
            const vector = vectors[i];
            const payload = payloads[i];
            if (!vector || !Array.isArray(vector)) {
              throw new VectorStoreError("Invalid vector", "insert");
            }
            if (!payload || typeof payload !== "object") {
              throw new VectorStoreError("Invalid payload", "insert");
            }
            this.validateDimension(vector, "insert");
            values.push(ids[i], `[${vector.join(",")}]`, payload);
            placeholders.push(`($${i * 3 + 1}, $${i * 3 + 2}, $${i * 3 + 3})`);
          }
          await client.query(
            `INSERT INTO ${this.collectionName} (id, vector, payload) VALUES ${placeholders.join(",")}`,
            values
          );
          await client.query("COMMIT");
        } catch (error) {
          await client.query("ROLLBACK");
          this.logger.error(`${LOG_PREFIXES4.PGVECTOR} Insert failed`, { error });
          if (error instanceof VectorDimensionError) {
            throw error;
          }
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        } finally {
          client.release();
        }
      }
      // @override
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        const metricOp = this.resolveDistanceOp(this.config.distance || "Cosine");
        const filterClauses = filters ? this.buildFilterClauses(filters) : "";
        const queryString = `
      SELECT id, payload, vector ${metricOp} $1 AS score
      FROM ${this.collectionName}
      ${filterClauses}
      ORDER BY score
      LIMIT $2
    `;
        const queryParams = [`[${query2.join(",")}]`, limit];
        try {
          const result = await this.pool.query(queryString, queryParams);
          return result.rows.map((row) => ({
            id: parseInt(row.id, 10),
            score: row.score,
            payload: row.payload
          }));
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.PGVECTOR} Search failed`, { error });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      // @override
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        const result = await this.pool.query(
          `SELECT id, vector, payload FROM ${this.collectionName} WHERE id = $1`,
          [vectorId]
        );
        if (result.rows.length === 0) {
          return null;
        }
        const row = result.rows[0];
        return {
          id: parseInt(row.id, 10),
          vector: row.vector,
          payload: row.payload,
          score: 1
        };
      }
      // @override
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        await this.pool.query(
          `UPDATE ${this.collectionName} SET vector = $1, payload = $2 WHERE id = $3`,
          [`[${vector.join(",")}]`, payload, vectorId]
        );
      }
      // @override
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        await this.pool.query(`DELETE FROM ${this.collectionName} WHERE id = $1`, [vectorId]);
      }
      // @override
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        await this.pool.query(`DROP TABLE IF EXISTS ${this.collectionName}`);
      }
      // @override
      async list(filters, limit = 100) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        let filterClauses = "";
        if (filters && filters.category) {
          filterClauses = `WHERE category = '${filters.category}'`;
        } else if (filters) {
          filterClauses = this.buildFilterClauses(filters);
        }
        const results = await this.pool.query(
          `SELECT id, vector, payload FROM ${this.collectionName} ${filterClauses} LIMIT ${limit}`
        );
        const countResult = await this.pool.query(
          `SELECT COUNT(*) FROM ${this.collectionName} ${filterClauses}`
        );
        const formattedResults = results.rows.map((row) => ({
          id: parseInt(row.id, 10),
          vector: row.vector,
          payload: row.payload,
          score: 1
        }));
        return [formattedResults, parseInt(countResult.rows[0].count, 10)];
      }
      validateDimension(vector, _operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      resolveDistanceOp(distance) {
        switch (distance) {
          case DISTANCE_METRICS.EUCLIDEAN:
            return "<->";
          case DISTANCE_METRICS.COSINE:
            return "<=>";
          case DISTANCE_METRICS.DOT_PRODUCT:
            return "<#>";
          case DISTANCE_METRICS.MANHATTAN:
            return "<+>";
          default:
            throw new Error(`Unknown distance metric: ${distance}`);
        }
      }
      buildFilterClauses(filters) {
        const conditions = Object.entries(filters).map(([key, value]) => {
          if (typeof value === "object" && value !== null) {
            if ("gte" in value) return `(payload->>'${key}')::numeric >= ${value.gte}`;
            if ("gt" in value) return `(payload->>'${key}')::numeric > ${value.gt}`;
            if ("lte" in value) return `(payload->>'${key}')::numeric <= ${value.lte}`;
            if ("lt" in value) return `(payload->>'${key}')::numeric < ${value.lt}`;
            if ("any" in value)
              return `payload->'${key}' @> '[${value.any.map((v) => `"${v}"`).join(",")}]'::jsonb`;
          }
          return `payload->>'${key}' = '${value}'`;
        });
        return conditions.length > 0 ? `WHERE ${conditions.join(" AND ")}` : "";
      }
    };
  }
});

// src/core/vector_storage/backend/faiss.ts
var faiss_exports = {};
__export(faiss_exports, {
  FaissBackend: () => FaissBackend
});
var fs5, path5, import_faiss_node, FaissBackend;
var init_faiss = __esm({
  "src/core/vector_storage/backend/faiss.ts"() {
    init_cjs_shims();
    init_types10();
    init_logger2();
    init_constants4();
    init_env();
    fs5 = __toESM(require("fs/promises"), 1);
    path5 = __toESM(require("path"), 1);
    import_faiss_node = require("faiss-node");
    FaissBackend = class {
      config;
      collectionName;
      dimension;
      logger;
      connected = false;
      faissIndex;
      // Native FAISS index
      payloads = /* @__PURE__ */ new Map();
      // Store all entry data
      collectionFilePath;
      needsSave = false;
      // Track if data needs to be saved
      constructor(config2) {
        this.config = config2;
        this.collectionName = config2.collectionName;
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: env.CIPHER_LOG_LEVEL || "info"
        });
        this.logger.debug(`${LOG_PREFIXES4.FAISS} Backend initialized`, {
          collection: this.collectionName,
          dimension: this.dimension
        });
        this.collectionFilePath = path5.join(
          this.config.baseStoragePath || "./faiss-data",
          `${this.collectionName}.json`
        );
        this.config.normalize = false;
        switch (this.config.distance) {
          case "Cosine":
            this.faissIndex = new import_faiss_node.IndexFlatIP(this.dimension);
            this.config.normalize = true;
            break;
          case "Euclidean":
            this.faissIndex = new import_faiss_node.IndexFlatL2(this.dimension);
            break;
          case "IP":
            this.faissIndex = new import_faiss_node.IndexFlatIP(this.dimension);
            break;
          default:
            this.faissIndex = new import_faiss_node.IndexFlatL2(this.dimension);
            this.logger.warn(
              `${LOG_PREFIXES4.FAISS} No distance metric specified or unsupported, defaulting to Euclidean (L2).`
            );
        }
      }
      /**
       * Validate the dimension of a given vector.
       */
      validateDimension(vector, _operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      normalizeVector(vector) {
        const magnitude = Math.sqrt(vector.reduce((sum, val) => sum + val * val, 0));
        if (magnitude === 0) {
          this.logger.warn(`${LOG_PREFIXES4.FAISS} Zero magnitude vector encountered`);
          return [...vector];
        }
        return vector.map((val) => val / magnitude);
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.FAISS} Already connected`);
          return;
        }
        this.logger.debug(`${LOG_PREFIXES4.FAISS} Connecting to in-memory FAISS store`);
        try {
          this.logger.debug(`${LOG_PREFIXES4.FAISS} Successfully connected`);
          await this.loadData();
          this.connected = true;
          this.logger.debug(`${LOG_PREFIXES4.FAISS} Data loaded from ${this.config.baseStoragePath}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Connection failed`, { error });
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "faiss",
            error
          );
        }
      }
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        const newEntries = [];
        for (let i = 0; i < vectors.length; i++) {
          let vector = vectors[i];
          const id = ids[i];
          const payload = payloads[i];
          if (this.config.normalize) {
            vector = this.normalizeVector(vector);
          }
          this.validateDimension(vector, "insert");
          const entry = { id, vector, payload };
          this.payloads.set(id, entry);
          newEntries.push(entry);
        }
        await this.saveData(newEntries);
        this.logger.debug(`${LOG_PREFIXES4.FAISS} Inserted ${vectors.length} vectors`);
      }
      async search(query2, limit = 2, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        if (this.config.normalize) {
          query2 = this.normalizeVector(query2);
        }
        if (!this.faissIndex) {
          throw new VectorStoreError("FAISS index not initialized", "search");
        }
        limit = Math.min(limit, this.faissIndex.ntotal());
        const { distances, labels } = this.faissIndex.search(query2, limit);
        const results = [];
        for (let i = 0; i < labels.length; i++) {
          const idx = labels[i];
          if (idx === void 0) {
            this.logger.warn(`${LOG_PREFIXES4.FAISS} Search returned undefined label for index ${i}`);
            continue;
          }
          if (idx < 0) continue;
          const entry = this.payloads.get(idx);
          if (!entry) continue;
          const distanceRow = distances[0];
          if (!distanceRow || typeof distanceRow[i] !== "number") continue;
          const distance = distanceRow[i];
          results.push({
            id: entry.id,
            vector: entry.vector,
            payload: entry.payload,
            score: this.config.distance === "Cosine" || this.config.distance === "IP" ? distance : 1 / (1 + distance)
          });
        }
        return results;
      }
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        try {
          const payload = this.payloads.get(vectorId);
          if (!payload) return null;
          return {
            id: vectorId,
            vector: payload.vector,
            payload: payload.payload,
            // Corrected to return only the user-defined payload
            score: 1
            // For direct retrieval, score is 1.0
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Get failed`, { error });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        try {
          if (!this.payloads.has(vectorId)) {
            throw new VectorStoreError(`Vector with ID ${vectorId} not found for update`, "update");
          }
          const oldEntry = this.payloads.get(vectorId);
          if (oldEntry && this.faissIndex) {
          }
          const updatedEntry = { ...this.payloads.get(vectorId), vector, payload };
          this.payloads.set(vectorId, updatedEntry);
          this.logger.debug(`${LOG_PREFIXES4.FAISS} Updated vector ${vectorId}`);
          await this.saveData([{ id: vectorId, vector, payload }]);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Update failed`, { error });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        try {
          const deleted = this.payloads.delete(vectorId);
          if (!deleted) {
            this.logger.warn(`${LOG_PREFIXES4.FAISS} Vector with ID ${vectorId} not found for deletion`);
          } else {
            this.logger.debug(`${LOG_PREFIXES4.FAISS} Deleted vector ${vectorId}`);
            await this.removeFromFile(vectorId);
          }
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Delete failed`, { error });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        try {
          this.payloads.clear();
          this.logger.info(`${LOG_PREFIXES4.FAISS} Cleared collection ${this.collectionName}`);
          if (this.config.baseStoragePath) {
            try {
              await fs5.unlink(this.collectionFilePath);
              this.logger.info(
                `${LOG_PREFIXES4.FAISS} Deleted collection file ${this.collectionFilePath}`
              );
            } catch (fileError) {
              if (fileError.code !== "ENOENT") {
                this.logger.warn(
                  `${LOG_PREFIXES4.FAISS} Failed to delete collection file: ${fileError}`
                );
              }
            }
          }
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Delete collection failed`, { error });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        try {
          let results = Array.from(this.payloads.entries()).map((entry) => ({
            id: entry[0],
            vector: entry[1].vector,
            payload: entry[1].payload,
            // Corrected to return only the user-defined payload
            score: 1
            // Score is 1.0 for listing all
          }));
          if (filters) {
            results = results.filter(
              (entry) => Object.entries(filters).every(([key, value]) => {
                const payloadValue = entry.payload[key];
                if (typeof value === "object" && value !== null) {
                  if ("any" in value && Array.isArray(value.any)) {
                    return value.any.includes(payloadValue);
                  }
                  if (typeof payloadValue === "number") {
                    if ("gte" in value && payloadValue < value.gte) return false;
                    if ("lte" in value && payloadValue > value.lte) return false;
                    if ("gt" in value && payloadValue <= value.gt) return false;
                    if ("lt" in value && payloadValue >= value.lt) return false;
                  }
                  return true;
                }
                return payloadValue === value;
              })
            );
          }
          const paginatedResults = results.slice(0, limit);
          return [paginatedResults, paginatedResults.length];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} List failed`, { error });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      async disconnect() {
        if (this.connected) {
          this.connected = false;
          this.logger.info(`${LOG_PREFIXES4.FAISS} Disconnected`);
        }
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "faiss";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
      async listCollections() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "listCollections");
        }
        return [this.collectionName];
      }
      /**
       * Saves or updates specific vector data in the JSON file.
       */
      async saveData(specificEntries) {
        if (!this.config.baseStoragePath) {
          this.logger.debug(`${LOG_PREFIXES4.FAISS} No storage path configured, skipping save.`);
          return;
        }
        try {
          const baseDir = path5.dirname(this.collectionFilePath);
          await fs5.mkdir(baseDir, { recursive: true });
          let existingData = [];
          try {
            const fileContent = await fs5.readFile(this.collectionFilePath, "utf8");
            existingData = JSON.parse(fileContent);
          } catch (error) {
            if (error.code !== "ENOENT") {
              throw error;
            }
          }
          if (specificEntries) {
            for (const newEntry of specificEntries) {
              const existingIndex = existingData.findIndex((entry) => entry.id === newEntry.id);
              if (existingIndex >= 0) {
                existingData[existingIndex] = newEntry;
              } else {
                existingData.push(newEntry);
              }
            }
          } else {
            existingData = Array.from(this.payloads.entries()).map(([id, entry]) => ({
              id,
              vector: entry.vector,
              payload: entry.payload
            }));
          }
          await fs5.writeFile(this.collectionFilePath, JSON.stringify(existingData, null, 2), "utf8");
          this.needsSave = false;
          if (specificEntries) {
            this.logger.debug(
              `${LOG_PREFIXES4.FAISS} Updated ${specificEntries.length} specific entries in ${this.collectionFilePath}`
            );
          } else {
            this.logger.debug(
              `${LOG_PREFIXES4.FAISS} Full save of ${existingData.length} vectors to ${this.collectionFilePath}`
            );
          }
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.FAISS} Failed to save data to ${this.collectionFilePath}`, {
            error
          });
          throw error;
        }
      }
      /**
       * Removes specific vector data from the JSON file.
       */
      async removeFromFile(vectorId) {
        if (!this.config.baseStoragePath) {
          return;
        }
        try {
          let existingData = [];
          try {
            const fileContent = await fs5.readFile(this.collectionFilePath, "utf8");
            existingData = JSON.parse(fileContent);
          } catch (error) {
            if (error.code === "ENOENT") {
              return;
            }
            throw error;
          }
          const filteredData = existingData.filter((entry) => entry.id !== vectorId);
          if (filteredData.length !== existingData.length) {
            await fs5.writeFile(this.collectionFilePath, JSON.stringify(filteredData, null, 2), "utf8");
            this.logger.debug(
              `${LOG_PREFIXES4.FAISS} Removed vector ID ${vectorId} from ${this.collectionFilePath}`
            );
          }
        } catch (error) {
          this.logger.error(
            `${LOG_PREFIXES4.FAISS} Failed to remove vector ID ${vectorId} from ${this.collectionFilePath}`,
            {
              error
            }
          );
          throw error;
        }
      }
      /**
       * Loads vector data from a single JSON file into memory.
       */
      async loadData() {
        if (!this.config.baseStoragePath) {
          this.logger.debug(`${LOG_PREFIXES4.FAISS} No storage path configured, skipping load.`);
          return;
        }
        try {
          this.payloads.clear();
          const fileContent = await fs5.readFile(this.collectionFilePath, "utf8");
          const entries = JSON.parse(fileContent);
          for (const entry of entries) {
            this.payloads.set(entry.id, entry);
            this.faissIndex?.add(entry.vector);
            this.logger.debug(`${LOG_PREFIXES4.FAISS} Loaded vector ID ${entry.id}`);
          }
          this.needsSave = false;
          this.logger.debug(
            `${LOG_PREFIXES4.FAISS} Loaded ${entries.length} vectors from ${this.collectionFilePath}`
          );
        } catch (error) {
          if (error.code === "ENOENT") {
            this.logger.debug(
              `${LOG_PREFIXES4.FAISS} Collection file not found at ${this.collectionFilePath}. Starting fresh.`
            );
          } else {
            this.logger.error(
              `${LOG_PREFIXES4.FAISS} Failed to load data from ${this.collectionFilePath}`,
              {
                error
              }
            );
            throw error;
          }
        }
      }
    };
  }
});

// src/core/vector_storage/backend/weaviate.ts
var weaviate_exports = {};
__export(weaviate_exports, {
  WeaviateBackend: () => WeaviateBackend
});
var import_weaviate_ts_client, import_uuid12, weaviate, WEAVIATE_UUID_NAMESPACE, WeaviateBackend;
var init_weaviate = __esm({
  "src/core/vector_storage/backend/weaviate.ts"() {
    init_cjs_shims();
    import_weaviate_ts_client = require("weaviate-ts-client");
    init_types10();
    init_logger2();
    init_constants4();
    init_env();
    import_uuid12 = require("uuid");
    weaviate = require("weaviate-ts-client").default;
    WEAVIATE_UUID_NAMESPACE = "6ba7b810-9dad-11d1-80b4-00c04fd430c8";
    WeaviateBackend = class {
      client = void 0;
      config;
      dimension;
      collectionName;
      logger;
      connected = false;
      connectionConfig = {};
      constructor(config2) {
        this.config = config2;
        this.collectionName = config2.collectionName.toLocaleLowerCase();
        this.collectionName = this.collectionName.charAt(0).toUpperCase() + this.collectionName.slice(1);
        this.dimension = config2.dimension;
        this.logger = createLogger({
          level: env.CIPHER_LOG_LEVEL || "info"
        });
        this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Backend initialized`, {
          collection: this.collectionName,
          dimension: this.dimension,
          url: config2.url,
          host: config2.host,
          port: config2.port
        });
      }
      /**
       * Ensure the client is available and connected
       */
      ensureClient() {
        if (!this.client) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "operation");
        }
        return this.client;
      }
      /**
       * Convert numeric ID to UUID v5 for Weaviate compatibility
       */
      generateUuidFromId(id) {
        return (0, import_uuid12.v5)(id.toString(), WEAVIATE_UUID_NAMESPACE);
      }
      validateDimension(vector, operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Already connected`);
          return;
        }
        this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Connecting to Weaviate`);
        try {
          if (this.config.url) {
            console.log("Connecting to Weaviate Cloud with URL: ", this.config.url);
            let host = this.config.url;
            if (host.startsWith("https://")) {
              host = host.replace("https://", "");
            } else if (host.startsWith("http://")) {
              host = host.replace("http://", "");
            }
            this.connectionConfig = {
              scheme: "https",
              host,
              apiKey: new import_weaviate_ts_client.ApiKey(this.config.apiKey || "")
            };
          } else if (this.config.host) {
            console.log("Connecting to local/custom Weaviate instance: ", this.config.host);
            let scheme = "http";
            let host = this.config.host;
            if (this.config.host.includes("https://")) {
              scheme = "https";
              host = this.config.host.replace("https://", "");
            } else if (this.config.host.includes("http://")) {
              scheme = "http";
              host = this.config.host.replace("http://", "");
            }
            this.connectionConfig = {
              scheme,
              host
            };
            if (this.config.apiKey) {
              this.connectionConfig.apiKey = new import_weaviate_ts_client.ApiKey(this.config.apiKey);
            }
          } else {
            throw new VectorStoreConnectionError(
              "Either url (for Weaviate Cloud) or host (for local instance) must be provided",
              "weaviate",
              new Error("Missing connection configuration")
            );
          }
          console.log("Connection config: ", this.connectionConfig);
          this.client = weaviate.client(this.connectionConfig);
          if (!this.client) {
            throw new VectorStoreConnectionError(
              "Weaviate client not initialized",
              "weaviate",
              new Error("Connection check failed")
            );
          }
          const isReady = await this.client.misc.liveChecker().do();
          if (!isReady) {
            throw new VectorStoreConnectionError(
              "Weaviate instance is not ready",
              "weaviate",
              new Error("Connection check failed")
            );
          }
          await this.ensureCollection();
          this.connected = true;
          this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Successfully connected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Connection failed`, { error });
          if (error instanceof VectorStoreConnectionError) {
            throw error;
          }
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "weaviate",
            error
          );
        }
      }
      /**
       * Ensure the collection exists, create if it doesn't
       */
      async ensureCollection() {
        const client = this.ensureClient();
        try {
          const schema = await client.schema.getter().do();
          if (!schema.classes) {
            this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Schema has no classes`);
            throw new VectorStoreError("Schema has no classes", "ensureCollection");
          }
          let classExists = false;
          for (const cls of schema.classes) {
            if (!cls.class) {
              this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Class has no class name`);
              throw new VectorStoreError("Class has no class name", "ensureCollection");
            }
            if (cls.class === this.collectionName) {
              classExists = true;
              break;
            }
          }
          if (!classExists) {
            this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Creating class ${this.collectionName}`);
            const classObj = {
              class: this.collectionName,
              //benchmark
              description: `Vector store collection for ${this.collectionName}`,
              properties: [
                {
                  name: "payload",
                  dataType: ["text"],
                  description: "JSON string containing all metadata and content"
                }
              ],
              vectorizer: "none"
              // We provide our own vectors
            };
            await client.schema.classCreator().withClass(classObj).do();
            this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Class created: ${this.collectionName}`);
          } else {
            const existingClass = schema.classes?.find((cls) => cls.class === this.collectionName);
            const expectedPayloadProperty = existingClass?.properties?.find(
              (prop) => prop.name === "payload"
            );
            if (!Array.isArray(expectedPayloadProperty?.dataType) || !existingClass || !existingClass?.properties || !(existingClass.properties.length === 1) || !expectedPayloadProperty.dataType.every((v) => typeof v === "string")) {
              this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Payload property is not a string[]`);
              throw new VectorStoreError("Payload property is not a string[]", "ensureCollection");
            }
            const schemaMatches = expectedPayloadProperty && expectedPayloadProperty.dataType.includes("text") && existingClass.properties && existingClass.properties.length === 1;
            if (!schemaMatches) {
              this.logger.warn(
                `${LOG_PREFIXES4.WEAVIATE} Class ${this.collectionName} schema doesn't match expected structure. Recreating...`
              );
              await client.schema.classDeleter().withClassName(this.collectionName).do();
              this.logger.debug(
                `${LOG_PREFIXES4.WEAVIATE} Deleted existing class: ${this.collectionName}`
              );
              const classObj = {
                class: this.collectionName,
                description: `Vector store collection for ${this.collectionName}`,
                properties: [
                  {
                    name: "payload",
                    dataType: ["text"],
                    description: "JSON string containing all metadata and content"
                  }
                ],
                vectorizer: "none"
                // We provide our own vectors
              };
              await client.schema.classCreator().withClass(classObj).do();
              this.logger.debug(
                `${LOG_PREFIXES4.WEAVIATE} Recreated class with correct schema: ${this.collectionName}`
              );
            } else {
              this.logger.debug(
                `${LOG_PREFIXES4.WEAVIATE} Class already exists with correct schema: ${this.collectionName}`
              );
            }
          }
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Failed to ensure class`, { error });
          throw error;
        }
      }
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert"));
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          return Promise.reject(
            new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert")
          );
        }
        for (const vector of vectors) {
          this.validateDimension(vector, "insert");
        }
        try {
          const client = this.ensureClient();
          const batcher = client.batch.objectsBatcher();
          for (let i = 0; i < vectors.length; i++) {
            const id = ids[i];
            const vector = vectors[i];
            if (id === void 0) {
              this.logger.error(`${LOG_PREFIXES4.WEAVIATE} ID is undefined`);
              throw new VectorStoreError("ID is undefined", "insert");
            }
            if (!vector) {
              this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Vector is undefined`);
              throw new VectorStoreError("Vector is undefined", "insert");
            }
            const obj = {
              class: this.collectionName,
              id: this.generateUuidFromId(id),
              properties: {
                payload: JSON.stringify(payloads[i] || {})
              },
              vector
            };
            batcher.withObject(obj);
          }
          const response = await batcher.do();
          if (response && response.some((r) => r.result?.errors)) {
            const errors = response.filter((r) => r.result?.errors).map((r) => r.result.errors.error.map((e) => e.message).join(", ")).join("; ");
            throw new VectorStoreError(`Insert failed: ${errors}`, "insert");
          }
          this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Insert failed`, { error });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = 10, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        try {
          const client = this.ensureClient();
          let searchBuilder = client.graphql.get().withClassName(this.collectionName).withNearVector({ vector: query2 }).withLimit(limit).withFields("_additional { id certainty distance } payload");
          if (filters) {
            const whereFilter = this.buildWhereFilter(filters);
            if (whereFilter) {
              searchBuilder = searchBuilder.withWhere(whereFilter);
            }
          }
          const response = await searchBuilder.do();
          const results = response.data.Get[this.collectionName] || [];
          return results.map((obj) => ({
            id: parseInt(obj._additional.id) || 0,
            score: obj._additional.certainty || obj._additional.distance || 0,
            payload: obj.payload ? JSON.parse(obj.payload) : {}
          }));
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Search failed`, { error });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected) {
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get"));
        }
        try {
          const client = this.ensureClient();
          const response = await client.data.getterById().withClassName(this.collectionName).withId(this.generateUuidFromId(vectorId)).withVector().do();
          if (!response) {
            return null;
          }
          return {
            id: vectorId,
            vector: response.vector || [],
            payload: response.properties?.payload && typeof response.properties.payload === "string" ? JSON.parse(response.properties.payload) : {},
            score: 1
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Get failed`, { error });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update"));
        }
        this.validateDimension(vector, "update");
        try {
          const client = this.ensureClient();
          await client.data.updater().withClassName(this.collectionName).withId(this.generateUuidFromId(vectorId)).withProperties({
            payload: JSON.stringify(payload)
          }).withVector(vector).do();
          this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Update failed`, { error });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected) {
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete"));
        }
        try {
          const client = this.ensureClient();
          await client.data.deleter().withClassName(this.collectionName).withId(this.generateUuidFromId(vectorId)).do();
          this.logger.debug(`${LOG_PREFIXES4.WEAVIATE} Deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Delete failed`, { error });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        try {
          const client = this.ensureClient();
          await client.schema.classDeleter().withClassName(this.collectionName).do();
          this.logger.info(`${LOG_PREFIXES4.WEAVIATE} Deleted collection ${this.collectionName}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} Delete collection failed`, { error });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        try {
          const client = this.ensureClient();
          let queryBuilder = client.graphql.get().withClassName(this.collectionName).withLimit(limit).withFields("_additional { id } payload");
          if (filters) {
            const whereFilter = this.buildWhereFilter(filters);
            if (whereFilter) {
              queryBuilder = queryBuilder.withWhere(whereFilter);
            }
          }
          const response = await queryBuilder.do();
          const results = response.data.Get[this.collectionName] || [];
          const transformedResults = results.map((obj) => ({
            id: parseInt(obj._additional.id) || 0,
            vector: [],
            // Vector not included in list operations
            payload: obj.payload ? JSON.parse(obj.payload) : {},
            score: 1
          }));
          return [transformedResults, transformedResults.length];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} List failed`, { error });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      async disconnect() {
        this.client = void 0;
        this.connected = false;
        this.logger.info(`${LOG_PREFIXES4.WEAVIATE} Disconnected`);
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "weaviate";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
      async listCollections() {
        if (!this.connected) {
          return Promise.reject(new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "listCollections"));
        }
        try {
          const client = this.ensureClient();
          const schema = await client.schema.getter().do();
          return schema.classes?.map((cls) => cls.class) || [];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.WEAVIATE} List collections failed`, { error });
          throw new VectorStoreError("Failed to list collections", "listCollections", error);
        }
      }
      /**
       * Build Weaviate where filter from SearchFilters
       */
      buildWhereFilter(filters) {
        const conditions = [];
        for (const [key, value] of Object.entries(filters)) {
          if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
            conditions.push({
              path: ["payload"],
              operator: "Like",
              valueText: `*"${key}":"${value}"*`
            });
          } else if (value && typeof value === "object") {
            if ("any" in value && Array.isArray(value.any)) {
              const orConditions = value.any.map((v) => ({
                path: ["payload"],
                operator: "Like",
                valueText: `*"${key}":"${v}"*`
              }));
              if (orConditions.length > 1) {
                conditions.push({ operator: "Or", operands: orConditions });
              } else if (orConditions.length === 1) {
                conditions.push(orConditions[0]);
              }
            } else {
              if ("gte" in value) {
                conditions.push({
                  path: ["payload"],
                  operator: "Like",
                  valueText: `*"${key}":*`
                });
              }
              if ("lte" in value) {
                conditions.push({
                  path: ["payload"],
                  operator: "Like",
                  valueText: `*"${key}":*`
                });
              }
              if ("gt" in value) {
                conditions.push({
                  path: ["payload"],
                  operator: "Like",
                  valueText: `*"${key}":*`
                });
              }
              if ("lt" in value) {
                conditions.push({
                  path: ["payload"],
                  operator: "Like",
                  valueText: `*"${key}":*`
                });
              }
            }
          }
        }
        if (conditions.length === 0) {
          return void 0;
        } else if (conditions.length === 1) {
          return conditions[0];
        } else {
          return { operator: "And", operands: conditions };
        }
      }
    };
  }
});

// src/core/vector_storage/backend/redis.ts
var redis_exports = {};
__export(redis_exports, {
  RedisBackend: () => RedisBackend2
});
var import_ioredis2, RedisBackend2;
var init_redis = __esm({
  "src/core/vector_storage/backend/redis.ts"() {
    init_cjs_shims();
    import_ioredis2 = require("ioredis");
    init_types10();
    init_logger2();
    init_constants4();
    RedisBackend2 = class {
      client;
      config;
      dimension;
      logger;
      collectionName;
      indexName;
      distance;
      connected = false;
      indexCreated = false;
      constructor(config2) {
        this.config = config2;
        this.dimension = config2.dimension || DEFAULTS2.DIMENSION;
        this.collectionName = config2.collectionName || "vectors";
        this.indexName = `${this.collectionName}_idx`;
        this.distance = config2.distance || "COSINE";
        this.logger = createLogger({
          level: process.env.CIPHER_LOG_LEVEL || "info"
        });
        if (this.config.url) {
          this.client = new import_ioredis2.Redis(this.config.url);
        } else {
          this.client = new import_ioredis2.Redis({
            host: this.config.host || "localhost",
            port: this.config.port ?? 6379,
            password: this.config.password || "",
            username: this.config.username || "",
            enableOfflineQueue: false,
            maxRetriesPerRequest: 3
          });
        }
        this.client.on("error", (error) => {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Connection error:`, error);
          this.connected = false;
        });
        this.client.on("connect", () => {
          this.logger.info(`${LOG_PREFIXES4.REDIS} Connected successfully`);
          this.connected = true;
        });
        this.client.on("close", () => {
          this.logger.info(`${LOG_PREFIXES4.REDIS} Connection closed`);
          this.connected = false;
        });
        this.client.on("ready", () => {
          this.logger.info(`${LOG_PREFIXES4.REDIS} Client ready`);
          this.connected = true;
        });
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Initialized`, {
          collectionName: this.collectionName,
          indexName: this.indexName,
          dimension: this.dimension,
          distance: this.distance
        });
      }
      /**
       * Generate Redis key for vector document
       */
      getDocumentKey(id) {
        return `${this.collectionName}:${id}`;
      }
      /**
       * Validate vector dimension
       */
      validateDimension(vector, operation) {
        if (vector.length !== this.dimension) {
          throw new VectorDimensionError(
            `${ERROR_MESSAGES4.INVALID_DIMENSION}: expected ${this.dimension}, got ${vector.length}`,
            this.dimension,
            vector.length
          );
        }
      }
      /**
       * Validate ID (must be a valid integer)
       */
      validateId(id) {
        if (id === void 0 || id === null || !Number.isInteger(id)) {
          throw new VectorStoreError("Redis point IDs must be valid integers", "id");
        }
      }
      /**
       * Convert vector to buffer for Redis storage
       */
      vectorToBuffer(vector) {
        const buffer = Buffer.allocUnsafe(vector.length * 4);
        for (let i = 0; i < vector.length; i++) {
          buffer.writeFloatLE(vector[i] ?? 0, i * 4);
        }
        return buffer;
      }
      /**
       * Convert buffer back to vector
       */
      bufferToVector(buffer) {
        const vector = [];
        for (let i = 0; i < buffer.length; i += 4) {
          vector.push(buffer.readFloatLE(i));
        }
        return vector;
      }
      /**
       * Create vector search index in Redis using raw commands
       */
      async createIndex() {
        if (this.indexCreated) return;
        try {
          try {
            await this.client.call("FT.INFO", this.indexName);
            this.indexCreated = true;
            this.logger.debug(`${LOG_PREFIXES4.REDIS} Index ${this.indexName} already exists`);
            return;
          } catch (error) {
          }
          const createArgs = [
            this.indexName,
            "ON",
            "JSON",
            "PREFIX",
            "1",
            `${this.collectionName}:`,
            "SCHEMA",
            "$.vector",
            "AS",
            "vector",
            "VECTOR",
            "FLAT",
            "6",
            "TYPE",
            "FLOAT32",
            "DIM",
            this.dimension.toString(),
            "DISTANCE_METRIC",
            this.distance
          ];
          await this.client.call("FT.CREATE", ...createArgs);
          this.indexCreated = true;
          this.logger.info(`${LOG_PREFIXES4.REDIS} Created vector index: ${this.indexName}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Failed to create index`, {
            indexName: this.indexName,
            error: error instanceof Error ? error.message : String(error)
          });
          throw new VectorStoreError("Failed to create vector index", "createIndex", error);
        }
      }
      /**
       * Build filter query string for FT.SEARCH
       */
      buildFilterQuery(filters) {
        if (!filters || Object.keys(filters).length === 0) return "*";
        const conditions = [];
        for (const [key, value] of Object.entries(filters)) {
          if (value === null || value === void 0) continue;
          if (typeof value === "object" && !Array.isArray(value) && ("gte" in value || "gt" in value || "lte" in value || "lt" in value)) {
            let rangeQuery = `@${key}:[`;
            if ("gte" in value && value.gte !== void 0) {
              rangeQuery += value.gte;
            } else if ("gt" in value && value.gt !== void 0) {
              rangeQuery += `(${value.gt}`;
            } else {
              rangeQuery += "-inf";
            }
            rangeQuery += " ";
            if ("lte" in value && value.lte !== void 0) {
              rangeQuery += value.lte;
            } else if ("lt" in value && value.lt !== void 0) {
              rangeQuery += `${value.lt})`;
            } else {
              rangeQuery += "+inf";
            }
            rangeQuery += "]";
            conditions.push(rangeQuery);
          } else if (typeof value === "object" && !Array.isArray(value) && "any" in value && Array.isArray(value.any)) {
            const values = value.any.map((v) => `"${v}"`).join("|");
            conditions.push(`@${key}:(${values})`);
          } else {
            conditions.push(`@${key}:"${value}"`);
          }
        }
        return conditions.length > 0 ? conditions.join(" ") : "*";
      }
      async connect() {
        if (this.connected && this.client.status === "ready") {
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Already connected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.REDIS} Connecting to Redis Stack`);
        try {
          if (this.client.status !== "ready") {
            await new Promise((resolve3, reject) => {
              let settled = false;
              const timeout = setTimeout(() => {
                if (!settled) {
                  settled = true;
                  reject(new Error("Connection timeout"));
                }
              }, 1e4);
              const onReady = () => {
                if (!settled) {
                  settled = true;
                  clearTimeout(timeout);
                  resolve3(void 0);
                }
              };
              const onError = (error) => {
                if (!settled) {
                  settled = true;
                  clearTimeout(timeout);
                  reject(error);
                }
              };
              this.client.once("ready", onReady);
              this.client.once("error", onError);
            });
          }
          this.connected = true;
          await this.createIndex();
          this.logger.info(`${LOG_PREFIXES4.REDIS} Successfully connected to Redis Stack`, {
            collectionName: this.collectionName,
            indexName: this.indexName,
            dimension: this.dimension
          });
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Connection failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new VectorStoreConnectionError(
            ERROR_MESSAGES4.CONNECTION_FAILED,
            "redis",
            error
          );
        }
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.REDIS} Disconnecting from Redis`);
        try {
          await this.client.quit();
          this.connected = false;
          this.logger.info(`${LOG_PREFIXES4.REDIS} Successfully disconnected`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Error during disconnect:`, error);
        }
      }
      async insert(vectors, ids, payloads) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "insert");
        }
        if (vectors.length !== ids.length || vectors.length !== payloads.length) {
          throw new VectorStoreError("Vectors, IDs, and payloads must have the same length", "insert");
        }
        if (vectors.length === 0) {
          this.logger.debug(`${LOG_PREFIXES4.REDIS} No vectors to insert`);
          return;
        }
        for (let i = 0; i < vectors.length; i++) {
          this.validateDimension(vectors[i], "insert");
          this.validateId(ids[i]);
        }
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Inserting ${vectors.length} vectors`);
        try {
          const pipeline = this.client.pipeline();
          for (let i = 0; i < vectors.length; i++) {
            const id = ids[i];
            const vector = vectors[i];
            const payload = payloads[i] || {};
            const documentKey = this.getDocumentKey(id ?? 0);
            const vectorBuffer = this.vectorToBuffer(vector ?? []);
            const document2 = {
              id,
              vector: Array.from(vectorBuffer),
              ...payload
            };
            pipeline.call("JSON.SET", documentKey, "$", JSON.stringify(document2));
          }
          await pipeline.exec();
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Successfully inserted ${vectors.length} vectors`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Insert failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorCount: vectors.length
          });
          throw new VectorStoreError("Failed to insert vectors", "insert", error);
        }
      }
      async search(query2, limit = DEFAULTS2.SEARCH_LIMIT, filters) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "search");
        }
        this.validateDimension(query2, "search");
        if (limit <= 0) {
          throw new VectorStoreError("Search limit must be positive", "search");
        }
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Searching with limit ${limit}`, {
          hasFilters: !!filters
        });
        try {
          const queryBuffer = this.vectorToBuffer(query2);
          const filterQuery = this.buildFilterQuery(filters);
          const searchQuery = `${filterQuery} => [KNN ${limit} @vector $query_vec AS score]`;
          const searchArgs = [
            this.indexName,
            searchQuery,
            "PARAMS",
            "2",
            "query_vec",
            queryBuffer,
            "RETURN",
            "2",
            "$",
            "score",
            "SORTBY",
            "score",
            "DIALECT",
            "2"
          ];
          const searchResult = await this.client.call("FT.SEARCH", ...searchArgs);
          const results = [];
          const totalResults = searchResult[0];
          for (let i = 1; i < searchResult.length; i += 2) {
            const docKey = searchResult[i];
            const docFields = searchResult[i + 1];
            let docData = {};
            let score = 0;
            for (let j = 0; j < docFields.length; j += 2) {
              const fieldName = docFields[j];
              const fieldValue = docFields[j + 1];
              if (fieldName === "$") {
                docData = JSON.parse(fieldValue);
              } else if (fieldName === "score") {
                score = parseFloat(fieldValue);
              }
            }
            const id = parseInt(docData.id);
            const similarityScore = 1 - score;
            const vectorArray = docData.vector;
            const vectorBuffer = Buffer.from(vectorArray);
            const vector = this.bufferToVector(vectorBuffer);
            const payload = { ...docData };
            delete payload.id;
            delete payload.vector;
            results.push({
              id,
              score: similarityScore,
              payload,
              vector
            });
          }
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Found ${results.length} results`);
          return results;
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Search failed`, {
            error: error instanceof Error ? error.message : String(error),
            limit
          });
          throw new VectorStoreError(ERROR_MESSAGES4.SEARCH_FAILED, "search", error);
        }
      }
      async get(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "get");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Getting vector ${vectorId}`);
        try {
          const documentKey = this.getDocumentKey(vectorId);
          const documentJson = await this.client.call("JSON.GET", documentKey, "$");
          if (!documentJson) {
            return null;
          }
          const docData = JSON.parse(documentJson)[0];
          const vectorArray = docData.vector;
          const vectorBuffer = Buffer.from(vectorArray);
          const vector = this.bufferToVector(vectorBuffer);
          const payload = { ...docData };
          delete payload.id;
          delete payload.vector;
          return {
            id: vectorId,
            vector,
            payload,
            score: 1
            // Perfect match for exact retrieval
          };
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Get failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to retrieve vector", "get", error);
        }
      }
      async update(vectorId, vector, payload) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "update");
        }
        this.validateDimension(vector, "update");
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Updating vector ${vectorId}`);
        try {
          const documentKey = this.getDocumentKey(vectorId);
          const existing = await this.client.call("JSON.GET", documentKey, "$");
          if (!existing) {
            throw new VectorStoreError(`Vector ${vectorId} does not exist`, "update");
          }
          const vectorBuffer = this.vectorToBuffer(vector);
          const document2 = {
            id: vectorId,
            vector: Array.from(vectorBuffer),
            ...payload
          };
          await this.client.call("JSON.SET", documentKey, "$", JSON.stringify(document2));
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Successfully updated vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Update failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to update vector", "update", error);
        }
      }
      async delete(vectorId) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "delete");
        }
        this.validateId(vectorId);
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Deleting vector ${vectorId}`);
        try {
          const documentKey = this.getDocumentKey(vectorId);
          await this.client.del(documentKey);
          this.logger.debug(`${LOG_PREFIXES4.REDIS} Successfully deleted vector ${vectorId}`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Delete failed`, {
            error: error instanceof Error ? error.message : String(error),
            vectorId
          });
          throw new VectorStoreError("Failed to delete vector", "delete", error);
        }
      }
      async deleteCollection() {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "deleteCollection");
        }
        this.logger.info(`${LOG_PREFIXES4.REDIS} Deleting collection: ${this.collectionName}`);
        try {
          try {
            await this.client.call("FT.DROPINDEX", this.indexName);
            this.indexCreated = false;
            this.logger.info(`${LOG_PREFIXES4.REDIS} Dropped index: ${this.indexName}`);
          } catch (error) {
            this.logger.debug(
              `${LOG_PREFIXES4.REDIS} Index ${this.indexName} does not exist or already dropped`
            );
          }
          const pattern = `${this.collectionName}:*`;
          let cursor = "0";
          let deletedCount = 0;
          do {
            const result = await this.client.scan(cursor, "MATCH", pattern, "COUNT", 1e3);
            cursor = result[0];
            const keys = result[1];
            if (keys.length > 0) {
              await this.client.del(...keys);
              deletedCount += keys.length;
            }
          } while (cursor !== "0");
          this.logger.info(
            `${LOG_PREFIXES4.REDIS} Deleted ${deletedCount} documents from collection: ${this.collectionName}`
          );
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} Delete collection failed`, {
            error: error instanceof Error ? error.message : String(error)
          });
          throw new VectorStoreError("Failed to delete collection", "deleteCollection", error);
        }
      }
      async list(filters, limit = 1e4) {
        if (!this.connected) {
          throw new VectorStoreError(ERROR_MESSAGES4.NOT_CONNECTED, "list");
        }
        this.logger.debug(`${LOG_PREFIXES4.REDIS} Listing vectors with limit ${limit}`, {
          hasFilters: !!filters
        });
        try {
          const filterQuery = this.buildFilterQuery(filters);
          const searchArgs = [
            this.indexName,
            filterQuery,
            "LIMIT",
            "0",
            limit.toString(),
            "RETURN",
            "1",
            "$"
          ];
          const searchResult = await this.client.call("FT.SEARCH", ...searchArgs);
          const results = [];
          const totalCount = searchResult[0];
          for (let i = 1; i < searchResult.length; i += 2) {
            const docKey = searchResult[i];
            const docFields = searchResult[i + 1];
            let docData = {};
            for (let j = 0; j < docFields.length; j += 2) {
              const fieldName = docFields[j];
              const fieldValue = docFields[j + 1];
              if (fieldName === "$") {
                docData = JSON.parse(fieldValue);
                break;
              }
            }
            const id = parseInt(docData.id);
            const vectorArray = docData.vector;
            const vectorBuffer = Buffer.from(vectorArray);
            const vector = this.bufferToVector(vectorBuffer);
            const payload = { ...docData };
            delete payload.id;
            delete payload.vector;
            results.push({
              id,
              vector,
              payload,
              score: 1
            });
          }
          this.logger.debug(
            `${LOG_PREFIXES4.REDIS} Listed ${results.length} vectors (total: ${totalCount})`
          );
          return [results, totalCount];
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.REDIS} List failed`, {
            error: error instanceof Error ? error.message : String(error),
            limit
          });
          throw new VectorStoreError("Failed to list vectors", "list", error);
        }
      }
      // Getter methods
      isConnected() {
        return this.connected && this.client.status === "ready";
      }
      getBackendType() {
        return "redis";
      }
      getDimension() {
        return this.dimension;
      }
      getCollectionName() {
        return this.collectionName;
      }
    };
  }
});

// src/core/vector_storage/manager.ts
var VectorStoreManager;
var init_manager6 = __esm({
  "src/core/vector_storage/manager.ts"() {
    init_cjs_shims();
    init_config5();
    init_logger2();
    init_constants4();
    init_event_types();
    init_event_aware_store();
    VectorStoreManager = class _VectorStoreManager {
      // Core state
      store;
      connected = false;
      config;
      logger;
      eventManager;
      // Connection tracking
      connectionAttempts = 0;
      lastConnectionError;
      // Backend metadata
      backendMetadata = {
        type: "unknown",
        isFallback: false,
        connectionTime: 0
      };
      // Lazy loading module references (static to share across instances)
      static qdrantModule;
      static inMemoryModule;
      static milvusModule;
      static chromaModule;
      static pgVectorModule;
      static pineconeModule;
      static faissModule;
      static redisModule;
      static weaviateModule;
      // In VectorStoreManager, track if in-memory is used as fallback or primary
      usedFallback = false;
      factoryFallback = false;
      /**
       * Creates a new VectorStoreManager instance
       *
       * @param config - Vector storage configuration
       * @throws {Error} If configuration is invalid
       */
      constructor(config2) {
        const configWithFallback = config2;
        if (configWithFallback._fallbackFrom) {
          this.factoryFallback = true;
          this.usedFallback = true;
          delete configWithFallback._fallbackFrom;
        }
        const validationResult2 = VectorStoreSchema.safeParse(config2);
        if (!validationResult2.success) {
          throw new Error(
            `${ERROR_MESSAGES4.INVALID_CONFIG}: ${validationResult2.error.errors.map((e) => `${e.path.join(".")}: ${e.message}`).join(", ")}`
          );
        }
        this.config = validationResult2.data;
        this.logger = createLogger({
          level: process.env.LOG_LEVEL || "info"
        });
        this.logger.debug(`${LOG_PREFIXES4.MANAGER} Initialized with configuration`, {
          type: this.config.type,
          collection: this.config.collectionName,
          dimension: this.config.dimension,
          fallback: this.usedFallback
        });
      }
      /**
       * Set the event manager for emitting memory operation events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
      }
      /**
       * Get the current vector storage configuration
       *
       * @returns The vector storage configuration
       */
      getConfig() {
        return this.config;
      }
      /**
       * Get information about the vector storage system
       *
       * @returns Vector storage system information including connection status and backend type
       */
      getInfo() {
        return {
          connected: this.connected,
          backend: {
            type: this.backendMetadata.type,
            connected: this.store?.isConnected() ?? false,
            fallback: this.usedFallback,
            collectionName: this.config.collectionName,
            dimension: this.config.dimension
          },
          connectionAttempts: this.connectionAttempts,
          lastError: this.lastConnectionError?.message
        };
      }
      /**
       * Get the current vector store if connected
       *
       * @returns The vector store or null if not connected
       */
      getStore() {
        if (!this.connected || !this.store) {
          return null;
        }
        return this.store;
      }
      /**
       * Get an event-aware vector store for a specific session
       *
       * @param sessionId - The session ID to associate with memory operations
       * @returns Event-aware vector store or null if not connected
       */
      getEventAwareStore(sessionId) {
        if (!this.connected || !this.store) {
          return null;
        }
        if (this.eventManager) {
          return new EventAwareVectorStore(this.store, this.eventManager, sessionId);
        }
        return this.store;
      }
      /**
       * Check if the vector storage manager is connected
       *
       * @returns true if backend is connected
       */
      isConnected() {
        return this.connected && this.store?.isConnected() === true;
      }
      /**
       * Connect to vector storage backend
       *
       * @returns The connected vector store
       * @throws {VectorStoreConnectionError} If backend fails to connect
       */
      async connect() {
        if (!this.factoryFallback) {
          this.usedFallback = false;
        }
        if (this.connected && this.store) {
          this.logger.debug(`${LOG_PREFIXES4.MANAGER} Already connected`, {
            type: this.backendMetadata.type
          });
          return this.store;
        }
        this.connectionAttempts++;
        this.logger.debug(
          `${LOG_PREFIXES4.MANAGER} Starting connection attempt ${this.connectionAttempts} for ${this.config.collectionName}`
        );
        try {
          const startTime = Date.now();
          try {
            this.store = await this.createBackend();
            await this.store.connect();
            if (!this.factoryFallback) {
              this.usedFallback = false;
            }
            this.logger.debug(`${LOG_PREFIXES4.MANAGER} Connected to ${this.config.collectionName}`, {
              type: this.backendMetadata.type,
              isFallback: this.backendMetadata.isFallback,
              connectionTime: `${this.backendMetadata.connectionTime}ms`
            });
          } catch (backendError) {
            this.logger.warn(`${LOG_PREFIXES4.MANAGER} Backend connection failed, attempting fallback`, {
              error: backendError instanceof Error ? backendError.message : String(backendError),
              originalType: this.config.type
            });
            if (this.config.type !== BACKEND_TYPES2.IN_MEMORY) {
              const { InMemoryBackend: InMemoryBackend4 } = await Promise.resolve().then(() => (init_in_memory2(), in_memory_exports2));
              this.store = new InMemoryBackend4({
                type: "in-memory",
                collectionName: this.config.collectionName,
                dimension: this.config.dimension,
                maxVectors: 1e4
              });
              await this.store.connect();
              this.backendMetadata.type = BACKEND_TYPES2.IN_MEMORY;
              this.backendMetadata.isFallback = true;
              this.backendMetadata.connectionTime = Date.now() - startTime;
              this.usedFallback = true;
              this.logger.info(`${LOG_PREFIXES4.MANAGER} Connected to fallback backend`, {
                type: this.backendMetadata.type,
                originalType: this.config.type
              });
            } else {
              if (!this.factoryFallback) {
                this.usedFallback = false;
              }
              throw backendError;
            }
          }
          this.connected = true;
          if (this.eventManager) {
            this.eventManager.emitServiceEvent(ServiceEvents.VECTOR_STORE_CONNECTED, {
              provider: this.backendMetadata.type,
              timestamp: Date.now()
            });
          }
          this.logger.info(`${LOG_PREFIXES4.MANAGER} Vector storage system connected`, {
            backend: this.backendMetadata.type,
            totalConnectionTime: `${this.backendMetadata.connectionTime}ms`
          });
          return this.store;
        } catch (error) {
          this.lastConnectionError = error;
          if (this.store?.isConnected()) {
            await this.store.disconnect().catch(
              (err) => this.logger.error(`${LOG_PREFIXES4.MANAGER} Error during cleanup disconnect`, {
                error: err
              })
            );
          }
          this.store = void 0;
          this.connected = false;
          this.usedFallback = false;
          throw error;
        }
      }
      /**
       * Disconnect from vector storage backend
       */
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES4.MANAGER} Already disconnected`);
          return;
        }
        this.logger.info(`${LOG_PREFIXES4.MANAGER} Disconnecting vector storage backend`);
        try {
          if (this.store?.isConnected()) {
            await Promise.race([
              this.store.disconnect(),
              new Promise(
                (_, reject) => setTimeout(() => reject(new Error("Disconnect timeout")), TIMEOUTS2.SHUTDOWN)
              )
            ]);
            if (this.eventManager) {
              this.eventManager.emitServiceEvent(ServiceEvents.VECTOR_STORE_DISCONNECTED, {
                provider: this.backendMetadata.type,
                reason: "Normal disconnection",
                timestamp: Date.now()
              });
            }
            this.logger.info(`${LOG_PREFIXES4.MANAGER} Disconnected successfully`);
          }
        } catch (error) {
          if (this.eventManager) {
            this.eventManager.emitServiceEvent(ServiceEvents.VECTOR_STORE_ERROR, {
              provider: this.backendMetadata.type,
              error: error instanceof Error ? error.message : String(error),
              timestamp: Date.now()
            });
          }
          this.logger.error(`${LOG_PREFIXES4.MANAGER} Disconnect error`, { error });
          throw error;
        } finally {
          this.store = void 0;
          this.connected = false;
          this.usedFallback = false;
          this.backendMetadata = {
            type: "unknown",
            isFallback: false,
            connectionTime: 0
          };
          this.logger.info(`${LOG_PREFIXES4.MANAGER} Vector storage system disconnected`);
        }
      }
      /**
       * Perform health check on the backend
       *
       * @returns Health check results
       */
      async healthCheck() {
        if (!this.connected || !this.store) {
          return {
            backend: false,
            overall: false,
            details: {
              backend: { status: "not_connected" }
            }
          };
        }
        this.logger.debug(`${LOG_PREFIXES4.MANAGER} Performing health check`);
        try {
          const startTime = Date.now();
          const isConnected = this.store.isConnected();
          const latency = Date.now() - startTime;
          const result = {
            backend: isConnected,
            overall: isConnected,
            details: {
              backend: {
                status: isConnected ? "healthy" : "unhealthy",
                latency
              }
            }
          };
          this.logger.info(`${LOG_PREFIXES4.MANAGER} Health check completed`, {
            overall: result.overall,
            latency: `${latency}ms`
          });
          return result;
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES4.MANAGER} Health check failed`, { error });
          return {
            backend: false,
            overall: false,
            details: {
              backend: {
                status: "error",
                error: error instanceof Error ? error.message : String(error)
              }
            }
          };
        }
      }
      // Private helper methods
      /**
       * Create vector store backend based on configuration
       */
      async createBackend() {
        const config2 = this.config;
        switch (config2.type) {
          case BACKEND_TYPES2.QDRANT: {
            try {
              if (!_VectorStoreManager.qdrantModule) {
                this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading Qdrant module`);
                const { QdrantBackend: QdrantBackend3 } = await Promise.resolve().then(() => (init_qdrant(), qdrant_exports));
                _VectorStoreManager.qdrantModule = QdrantBackend3;
              }
              const QdrantBackend2 = _VectorStoreManager.qdrantModule;
              this.backendMetadata.type = BACKEND_TYPES2.QDRANT;
              this.backendMetadata.isFallback = false;
              return new QdrantBackend2(config2);
            } catch (error) {
              this.logger.debug(`${LOG_PREFIXES4.MANAGER} Failed to create Qdrant backend`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES2.MILVUS: {
            try {
              if (!_VectorStoreManager.milvusModule) {
                this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading Milvus module`);
                const { MilvusBackend: MilvusBackend3 } = await Promise.resolve().then(() => (init_milvus(), milvus_exports));
                _VectorStoreManager.milvusModule = MilvusBackend3;
              }
              const MilvusBackend2 = _VectorStoreManager.milvusModule;
              this.backendMetadata.type = BACKEND_TYPES2.MILVUS;
              this.backendMetadata.isFallback = false;
              return new MilvusBackend2(config2);
            } catch (error) {
              this.logger.info(`${LOG_PREFIXES4.MANAGER} Failed to create Milvus backend: ${error}`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES2.CHROMA: {
            try {
              if (!_VectorStoreManager.chromaModule) {
                this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading ChromaDB module`);
                const { ChromaBackend: ChromaBackend3 } = await Promise.resolve().then(() => (init_chroma(), chroma_exports));
                _VectorStoreManager.chromaModule = ChromaBackend3;
              }
              const ChromaBackend2 = _VectorStoreManager.chromaModule;
              this.backendMetadata.type = BACKEND_TYPES2.CHROMA;
              this.backendMetadata.isFallback = false;
              return new ChromaBackend2(config2);
            } catch (error) {
              this.logger.info(`${LOG_PREFIXES4.MANAGER} Failed to create ChromaDB backend: ${error}`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES2.PINECONE: {
            try {
              if (!_VectorStoreManager.pineconeModule) {
                this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading Pinecone module`);
                const { PineconeBackend: PineconeBackend3 } = await Promise.resolve().then(() => (init_pinecone(), pinecone_exports));
                _VectorStoreManager.pineconeModule = PineconeBackend3;
              }
              const PineconeBackend2 = _VectorStoreManager.pineconeModule;
              this.backendMetadata.type = BACKEND_TYPES2.PINECONE;
              this.backendMetadata.isFallback = false;
              return new PineconeBackend2(config2);
            } catch (error) {
              this.logger.info(`${LOG_PREFIXES4.MANAGER} Failed to create Pinecone backend: ${error}`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES2.PGVECTOR: {
            if (!_VectorStoreManager.pgVectorModule) {
              this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading PgVector module`);
              const { PgVectorBackend: PgVectorBackend3 } = await Promise.resolve().then(() => (init_pgvector(), pgvector_exports));
              _VectorStoreManager.pgVectorModule = PgVectorBackend3;
            }
            const PgVectorBackend2 = _VectorStoreManager.pgVectorModule;
            this.backendMetadata.type = BACKEND_TYPES2.PGVECTOR;
            this.backendMetadata.isFallback = false;
            return new PgVectorBackend2(config2);
          }
          case BACKEND_TYPES2.FAISS: {
            if (!_VectorStoreManager.faissModule) {
              this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading in-memory module`);
              const { FaissBackend: FaissBackend3 } = await Promise.resolve().then(() => (init_faiss(), faiss_exports));
              _VectorStoreManager.faissModule = FaissBackend3;
            }
            const FaissBackend2 = _VectorStoreManager.faissModule;
            this.backendMetadata.type = BACKEND_TYPES2.FAISS;
            this.backendMetadata.isFallback = false;
            return new FaissBackend2(config2);
          }
          case BACKEND_TYPES2.WEAVIATE: {
            try {
              if (!_VectorStoreManager.weaviateModule) {
                this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading Weaviate module`);
                const { WeaviateBackend: WeaviateBackend3 } = await Promise.resolve().then(() => (init_weaviate(), weaviate_exports));
                _VectorStoreManager.weaviateModule = WeaviateBackend3;
              }
              const WeaviateBackend2 = _VectorStoreManager.weaviateModule;
              this.backendMetadata.type = BACKEND_TYPES2.WEAVIATE;
              this.backendMetadata.isFallback = false;
              return new WeaviateBackend2(config2);
            } catch (error) {
              this.logger.info(`${LOG_PREFIXES4.MANAGER} Failed to create Weaviate backend: ${error}`, {
                error: error instanceof Error ? error.message : String(error)
              });
              throw error;
            }
          }
          case BACKEND_TYPES2.REDIS: {
            if (!_VectorStoreManager.redisModule) {
              this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading Redis module`);
              const { RedisBackend: RedisBackend4 } = await Promise.resolve().then(() => (init_redis(), redis_exports));
              _VectorStoreManager.redisModule = RedisBackend4;
            }
            const RedisBackend3 = _VectorStoreManager.redisModule;
            this.backendMetadata.type = BACKEND_TYPES2.REDIS;
            this.backendMetadata.isFallback = false;
            return new RedisBackend3(config2);
          }
          case BACKEND_TYPES2.IN_MEMORY:
          default: {
            if (!_VectorStoreManager.inMemoryModule) {
              this.logger.debug(`${LOG_PREFIXES4.MANAGER} Lazy loading in-memory module`);
              const { InMemoryBackend: InMemoryBackend5 } = await Promise.resolve().then(() => (init_in_memory2(), in_memory_exports2));
              _VectorStoreManager.inMemoryModule = InMemoryBackend5;
            }
            const InMemoryBackend4 = _VectorStoreManager.inMemoryModule;
            this.backendMetadata.type = BACKEND_TYPES2.IN_MEMORY;
            this.backendMetadata.isFallback = false;
            return new InMemoryBackend4(config2);
          }
        }
      }
    };
  }
});

// src/core/vector_storage/dual-collection-manager.ts
var DualCollectionVectorManager;
var init_dual_collection_manager = __esm({
  "src/core/vector_storage/dual-collection-manager.ts"() {
    init_cjs_shims();
    init_manager6();
    init_logger2();
    init_env();
    DualCollectionVectorManager = class {
      knowledgeManager;
      reflectionManager;
      logger;
      reflectionEnabled;
      eventManager;
      constructor(baseConfig) {
        this.logger = createLogger({
          level: env.CIPHER_LOG_LEVEL || "info"
        });
        this.reflectionEnabled = !!(env.REFLECTION_VECTOR_STORE_COLLECTION && env.REFLECTION_VECTOR_STORE_COLLECTION.trim());
        this.knowledgeManager = new VectorStoreManager(baseConfig);
        if (this.reflectionEnabled) {
          const reflectionConfig = {
            ...baseConfig,
            collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION
          };
          this.reflectionManager = new VectorStoreManager(reflectionConfig);
          this.logger.info("DualCollectionVectorManager: Initialized with dual collections", {
            backend: baseConfig.type,
            knowledgeCollection: baseConfig.collectionName,
            reflectionCollection: env.REFLECTION_VECTOR_STORE_COLLECTION,
            dimension: baseConfig.dimension
          });
        } else {
          this.reflectionManager = null;
          this.logger.info("DualCollectionVectorManager: Initialized with single collection", {
            backend: baseConfig.type,
            knowledgeCollection: baseConfig.collectionName,
            dimension: baseConfig.dimension
          });
        }
      }
      /**
       * Set the event manager for emitting memory operation events
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
        this.knowledgeManager.setEventManager(eventManager);
        if (this.reflectionManager) {
          this.reflectionManager.setEventManager(eventManager);
        }
      }
      /**
       * Connect both collections
       */
      async connect() {
        this.logger.info("DualCollectionVectorManager: Connecting collections...");
        await this.knowledgeManager.connect();
        this.logger.info("DualCollectionVectorManager: Knowledge collection connected");
        if (this.reflectionEnabled && this.reflectionManager) {
          try {
            await this.reflectionManager.connect();
            this.logger.info("DualCollectionVectorManager: Reflection collection connected");
          } catch (error) {
            this.logger.warn("DualCollectionVectorManager: Failed to connect reflection collection", {
              error: error instanceof Error ? error.message : String(error)
            });
          }
        }
      }
      /**
       * Disconnect both collections
       */
      async disconnect() {
        this.logger.info("DualCollectionVectorManager: Disconnecting collections...");
        await Promise.allSettled([
          this.knowledgeManager.disconnect(),
          this.reflectionManager?.disconnect()
        ]);
        this.logger.info("DualCollectionVectorManager: All collections disconnected");
      }
      /**
       * Get a vector store by collection type
       */
      getStore(type) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager.getStore();
          case "reflection":
            if (!this.reflectionEnabled || !this.reflectionManager) {
              this.logger.debug("DualCollectionVectorManager: Reflection memory is disabled");
              return null;
            }
            return this.reflectionManager.getStore();
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Get an event-aware vector store by collection type for a specific session
       */
      getEventAwareStore(type, sessionId) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager.getEventAwareStore(sessionId);
          case "reflection":
            if (!this.reflectionEnabled || !this.reflectionManager) {
              this.logger.debug("DualCollectionVectorManager: Reflection memory is disabled");
              return null;
            }
            return this.reflectionManager.getEventAwareStore(sessionId);
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Check if collections are connected
       */
      isConnected(type) {
        if (type) {
          switch (type) {
            case "knowledge":
              return this.knowledgeManager.isConnected();
            case "reflection":
              if (!this.reflectionEnabled) return false;
              return this.reflectionManager?.isConnected() === true;
            default:
              throw new Error(`Unknown collection type: ${type}`);
          }
        }
        const knowledgeConnected = this.knowledgeManager.isConnected();
        const reflectionConnected = !this.reflectionEnabled || this.reflectionManager?.isConnected() === true;
        return knowledgeConnected && reflectionConnected;
      }
      /**
       * Get information about both collections
       */
      getInfo() {
        return {
          knowledge: {
            connected: this.knowledgeManager.isConnected(),
            collectionName: this.knowledgeManager.getConfig().collectionName,
            manager: this.knowledgeManager
          },
          reflection: {
            connected: this.reflectionManager?.isConnected() === true,
            collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION,
            manager: this.reflectionManager,
            enabled: this.reflectionEnabled
          },
          overallConnected: this.isConnected()
        };
      }
      /**
       * Get a manager by collection type (for advanced usage)
       */
      getManager(type) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager;
          case "reflection":
            return this.reflectionManager;
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Health check for both collections
       */
      async healthCheck() {
        const knowledgeHealth = await this.knowledgeManager.healthCheck();
        const reflectionHealth = this.reflectionManager ? await this.reflectionManager.healthCheck() : { overall: true };
        return {
          knowledge: knowledgeHealth,
          reflection: reflectionHealth,
          overall: knowledgeHealth.overall && reflectionHealth.overall
        };
      }
    };
  }
});

// src/core/vector_storage/multi-collection-manager.ts
var multi_collection_manager_exports = {};
__export(multi_collection_manager_exports, {
  MultiCollectionVectorManager: () => MultiCollectionVectorManager
});
var MultiCollectionVectorManager;
var init_multi_collection_manager = __esm({
  "src/core/vector_storage/multi-collection-manager.ts"() {
    init_cjs_shims();
    init_manager6();
    init_logger2();
    init_env();
    init_factory7();
    MultiCollectionVectorManager = class {
      knowledgeManager;
      reflectionManager;
      workspaceManager;
      logger;
      reflectionEnabled;
      workspaceEnabled;
      eventManager;
      constructor(baseConfig) {
        this.logger = createLogger({
          level: env.CIPHER_LOG_LEVEL || "info"
        });
        this.reflectionEnabled = !!(env.REFLECTION_VECTOR_STORE_COLLECTION && env.REFLECTION_VECTOR_STORE_COLLECTION.trim());
        this.workspaceEnabled = !!(env.USE_WORKSPACE_MEMORY && env.WORKSPACE_VECTOR_STORE_COLLECTION && env.WORKSPACE_VECTOR_STORE_COLLECTION.trim());
        this.knowledgeManager = new VectorStoreManager(baseConfig);
        if (this.reflectionEnabled) {
          const reflectionConfig = {
            ...baseConfig,
            collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION
          };
          this.reflectionManager = new VectorStoreManager(reflectionConfig);
        } else {
          this.reflectionManager = null;
        }
        if (this.workspaceEnabled) {
          const workspaceConfig = getWorkspaceVectorStoreConfigFromEnv(baseConfig);
          this.workspaceManager = new VectorStoreManager(workspaceConfig);
          this.logger.info("MultiCollectionVectorManager: Initialized with multiple collections", {
            backend: baseConfig.type,
            knowledgeCollection: baseConfig.collectionName,
            reflectionCollection: this.reflectionEnabled ? env.REFLECTION_VECTOR_STORE_COLLECTION : "disabled",
            workspaceCollection: env.WORKSPACE_VECTOR_STORE_COLLECTION,
            dimension: workspaceConfig.dimension
            // Use dimension from workspaceConfig
          });
        } else {
          this.workspaceManager = null;
          this.logger.info("MultiCollectionVectorManager: Initialized without workspace memory", {
            backend: baseConfig.type,
            knowledgeCollection: baseConfig.collectionName,
            reflectionCollection: this.reflectionEnabled ? env.REFLECTION_VECTOR_STORE_COLLECTION : "disabled",
            workspaceCollection: "disabled",
            dimension: baseConfig.dimension
          });
        }
      }
      /**
       * Set event manager for event-aware operations
       */
      setEventManager(eventManager) {
        this.eventManager = eventManager;
        this.knowledgeManager.setEventManager(eventManager);
        if (this.reflectionManager) {
          this.reflectionManager.setEventManager(eventManager);
        }
        if (this.workspaceManager) {
          this.workspaceManager.setEventManager(eventManager);
        }
      }
      /**
       * Connect to all enabled vector stores
       */
      async connect() {
        const promises = [];
        promises.push(this.knowledgeManager.connect());
        if (this.reflectionManager) {
          promises.push(this.reflectionManager.connect());
        }
        if (this.workspaceManager) {
          promises.push(this.workspaceManager.connect());
        }
        try {
          const results = await Promise.all(promises);
          const knowledgeConnected = !!results[0];
          if (!knowledgeConnected) {
            this.logger.error("MultiCollectionVectorManager: Failed to connect knowledge manager");
            return false;
          }
          this.logger.info("MultiCollectionVectorManager: Connection completed", {
            knowledge: knowledgeConnected,
            reflection: this.reflectionEnabled ? !!results[1] : "disabled",
            workspace: this.workspaceEnabled ? !!results[results.length - 1] : "disabled"
          });
          return true;
        } catch (error) {
          this.logger.error("MultiCollectionVectorManager: Connection failed", {
            error: error instanceof Error ? error.message : String(error)
          });
          return false;
        }
      }
      /**
       * Disconnect from all vector stores
       */
      async disconnect() {
        const promises = [];
        promises.push(this.knowledgeManager.disconnect());
        if (this.reflectionManager) {
          promises.push(this.reflectionManager.disconnect());
        }
        if (this.workspaceManager) {
          promises.push(this.workspaceManager.disconnect());
        }
        await Promise.all(promises);
      }
      /**
       * Get vector store by collection type
       */
      getStore(type) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager.getStore();
          case "reflection":
            if (!this.reflectionEnabled || !this.reflectionManager) {
              this.logger.debug("MultiCollectionVectorManager: Reflection memory is disabled");
              return null;
            }
            return this.reflectionManager.getStore();
          case "workspace":
            if (!this.workspaceEnabled || !this.workspaceManager) {
              this.logger.debug("MultiCollectionVectorManager: Workspace memory is disabled");
              return null;
            }
            return this.workspaceManager.getStore();
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Get vector store by named collection (for backward compatibility)
       */
      getNamedStore(collectionName) {
        if (collectionName === env.WORKSPACE_VECTOR_STORE_COLLECTION) {
          return this.getStore("workspace");
        }
        if (collectionName === env.REFLECTION_VECTOR_STORE_COLLECTION) {
          return this.getStore("reflection");
        }
        return this.getStore("knowledge");
      }
      /**
       * Get an event-aware vector store by collection type for a specific session
       */
      getEventAwareStore(type, sessionId) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager.getEventAwareStore(sessionId);
          case "reflection":
            if (!this.reflectionEnabled || !this.reflectionManager) {
              this.logger.debug("MultiCollectionVectorManager: Reflection memory is disabled");
              return null;
            }
            return this.reflectionManager.getEventAwareStore(sessionId);
          case "workspace":
            if (!this.workspaceEnabled || !this.workspaceManager) {
              this.logger.debug("MultiCollectionVectorManager: Workspace memory is disabled");
              return null;
            }
            return this.workspaceManager.getEventAwareStore(sessionId);
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Health check for all collections
       */
      async healthCheck() {
        const knowledgeHealthy = (await this.knowledgeManager.healthCheck()).overall;
        let reflectionHealthy = true;
        if (this.reflectionEnabled && this.reflectionManager) {
          reflectionHealthy = (await this.reflectionManager.healthCheck()).overall;
        }
        let workspaceHealthy = true;
        if (this.workspaceEnabled && this.workspaceManager) {
          workspaceHealthy = (await this.workspaceManager.healthCheck()).overall;
        }
        return {
          knowledge: knowledgeHealthy,
          reflection: reflectionHealthy,
          workspace: workspaceHealthy,
          overall: knowledgeHealthy && reflectionHealthy && workspaceHealthy
        };
      }
      /**
       * Get vector store manager by collection type
       */
      getManager(type) {
        switch (type) {
          case "knowledge":
            return this.knowledgeManager;
          case "reflection":
            return this.reflectionManager;
          case "workspace":
            return this.workspaceManager;
          default:
            throw new Error(`Unknown collection type: ${type}`);
        }
      }
      /**
       * Get information about all collections
       */
      getInfo() {
        return {
          knowledge: {
            connected: this.knowledgeManager.isConnected(),
            collectionName: this.knowledgeManager.getInfo().backend.collectionName,
            manager: this.knowledgeManager
          },
          reflection: {
            connected: this.reflectionManager?.isConnected() ?? false,
            collectionName: env.REFLECTION_VECTOR_STORE_COLLECTION || "",
            manager: this.reflectionManager,
            enabled: this.reflectionEnabled
          },
          workspace: {
            connected: this.workspaceManager?.isConnected() ?? false,
            collectionName: env.WORKSPACE_VECTOR_STORE_COLLECTION || "",
            manager: this.workspaceManager,
            enabled: this.workspaceEnabled
          },
          overallConnected: this.knowledgeManager.isConnected() && (!this.reflectionEnabled || this.reflectionManager?.isConnected() === true) && (!this.workspaceEnabled || this.workspaceManager?.isConnected() === true)
        };
      }
      /**
       * Check if any collection is connected
       */
      isConnected() {
        return this.knowledgeManager.isConnected();
      }
    };
  }
});

// src/core/vector_storage/factory.ts
async function createVectorStore(config2) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  logger2.debug(`${LOG_PREFIXES4.FACTORY} Creating vector storage system`, {
    type: config2.type,
    collection: config2.collectionName,
    dimension: config2.dimension
  });
  const manager = new VectorStoreManager(config2);
  try {
    const store = await manager.connect();
    logger2.info(`${LOG_PREFIXES4.FACTORY} Vector storage system created successfully`, {
      type: manager.getInfo().backend.type,
      collection: config2.collectionName,
      connected: manager.isConnected()
    });
    return { manager, store };
  } catch (error) {
    await manager.disconnect().catch(() => {
    });
    logger2.error(`${LOG_PREFIXES4.FACTORY} Failed to create vector storage system`, {
      error: error instanceof Error ? error.message : String(error)
    });
    throw error;
  }
}
async function createVectorStoreFromEnv(agentConfig) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const config2 = getVectorStoreConfigFromEnv(agentConfig);
  logger2.info(`${LOG_PREFIXES4.FACTORY} Creating vector storage from environment`, {
    type: config2.type,
    collection: config2.collectionName,
    dimension: config2.dimension
  });
  return createVectorStore(config2);
}
async function createDualCollectionVectorStoreFromEnv(agentConfig) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const config2 = getVectorStoreConfigFromEnv(agentConfig);
  const serviceCache = getServiceCache();
  const cacheKey = createServiceKey("dualCollectionVectorStore", {
    type: config2.type,
    collection: config2.collectionName,
    reflectionCollection: env.REFLECTION_VECTOR_STORE_COLLECTION || "",
    // Include dimension for proper cache key differentiation
    dimension: config2.dimension
  });
  return await serviceCache.getOrCreate(cacheKey, async () => {
    logger2.debug("Creating new dual collection vector store instance");
    return await createDualCollectionVectorStoreInternal(config2, logger2);
  });
}
async function createDualCollectionVectorStoreInternal(config2, logger2) {
  const reflectionCollection = (env.REFLECTION_VECTOR_STORE_COLLECTION || "").trim();
  if (!reflectionCollection) {
    logger2.info(
      `${LOG_PREFIXES4.FACTORY} Reflection collection not set, creating single collection manager only`,
      {
        type: config2.type,
        knowledgeCollection: config2.collectionName
      }
    );
    const manager2 = new DualCollectionVectorManager(config2);
    try {
      await manager2.connect();
      const knowledgeStore = manager2.getStore("knowledge");
      if (!knowledgeStore) {
        throw new Error("Failed to get knowledge store from dual collection manager");
      }
      return {
        manager: manager2,
        knowledgeStore,
        reflectionStore: null
      };
    } catch (error) {
      logger2.warn(
        `${LOG_PREFIXES4.FACTORY} Primary vector store connection failed, using fallback`,
        {
          originalType: config2.type,
          error: error instanceof Error ? error.message : String(error)
        }
      );
      await manager2.disconnect().catch(() => {
      });
      const fallbackConfig = {
        type: "in-memory",
        collectionName: config2.collectionName,
        dimension: config2.dimension,
        maxVectors: 1e4
      };
      const fallbackManager = new DualCollectionVectorManager(fallbackConfig);
      await fallbackManager.connect();
      const knowledgeStore = fallbackManager.getStore("knowledge");
      if (!knowledgeStore) {
        throw new Error("Failed to get knowledge store from fallback dual collection manager");
      }
      logger2.info(`${LOG_PREFIXES4.FACTORY} Successfully created fallback in-memory store`, {
        fallbackType: "in-memory",
        originalType: config2.type
      });
      return {
        manager: fallbackManager,
        knowledgeStore,
        reflectionStore: null
      };
    }
  }
  logger2.info(`${LOG_PREFIXES4.FACTORY} Creating dual collection vector storage from environment`, {
    type: config2.type,
    knowledgeCollection: config2.collectionName,
    reflectionCollection,
    refectionEnabled: true
  });
  const manager = new DualCollectionVectorManager(config2);
  try {
    await manager.connect();
    const knowledgeStore = manager.getStore("knowledge");
    const reflectionStore = manager.getStore("reflection");
    if (!knowledgeStore) {
      throw new Error("Failed to get knowledge store from dual collection manager");
    }
    return {
      manager,
      knowledgeStore,
      reflectionStore
    };
  } catch (error) {
    await manager.disconnect().catch(() => {
    });
    const fallbackConfig = {
      type: "in-memory",
      collectionName: config2.collectionName,
      dimension: config2.dimension,
      maxVectors: 1e4
    };
    try {
      const fallbackManager = new DualCollectionVectorManager(fallbackConfig);
      await fallbackManager.connect();
      const knowledgeStore = fallbackManager.getStore("knowledge");
      const reflectionStore = fallbackManager.getStore("reflection");
      if (!knowledgeStore) {
        throw new Error("Failed to get knowledge store from fallback dual collection manager");
      }
      logger2.info(
        `${LOG_PREFIXES4.FACTORY} Successfully created fallback in-memory dual collection`,
        {
          knowledge: !!knowledgeStore,
          reflection: !!reflectionStore,
          originalType: config2.type
        }
      );
      return {
        manager: fallbackManager,
        knowledgeStore,
        reflectionStore
      };
    } catch (fallbackError) {
      logger2.error(
        `${LOG_PREFIXES4.FACTORY} Failed to create fallback dual collection vector storage`,
        {
          originalError: error instanceof Error ? error.message : String(error),
          fallbackError: fallbackError instanceof Error ? fallbackError.message : String(fallbackError)
        }
      );
      throw new Error(
        `Failed to create both primary and fallback dual collection vector storage: ${error instanceof Error ? error.message : String(error)}`
      );
    }
  }
}
function getVectorStoreConfigFromEnv(agentConfig) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const storeType = env.VECTOR_STORE_TYPE;
  const collectionName = env.VECTOR_STORE_COLLECTION;
  let dimension = env.VECTOR_STORE_DIMENSION !== void 0 && !Number.isNaN(env.VECTOR_STORE_DIMENSION) ? env.VECTOR_STORE_DIMENSION : 1536;
  const maxVectors = env.VECTOR_STORE_MAX_VECTORS !== void 0 && !Number.isNaN(env.VECTOR_STORE_MAX_VECTORS) ? env.VECTOR_STORE_MAX_VECTORS : 1e4;
  if (agentConfig?.embedding && typeof agentConfig.embedding === "object" && agentConfig.embedding.dimensions) {
    const embeddingDimension = agentConfig.embedding.dimensions;
    if (typeof embeddingDimension === "number" && embeddingDimension > 0) {
      logger2.debug("Overriding vector store dimension from agent config", {
        envDimension: dimension,
        agentDimension: embeddingDimension,
        embeddingType: agentConfig.embedding.type
      });
      dimension = embeddingDimension;
    }
  }
  if (storeType === "qdrant") {
    const host = env.VECTOR_STORE_HOST;
    const url = env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT;
    const apiKey = env.VECTOR_STORE_API_KEY;
    const distance = env.VECTOR_STORE_DISTANCE;
    const onDisk = env.VECTOR_STORE_ON_DISK;
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Qdrant
        _fallbackFrom: "qdrant"
      };
    }
    return {
      type: "qdrant",
      collectionName,
      dimension,
      url,
      host,
      port,
      apiKey,
      distance,
      onDisk
    };
  } else if (storeType === "milvus") {
    const host = env.VECTOR_STORE_HOST;
    const url = env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT;
    const username = env.VECTOR_STORE_USERNAME;
    const password = env.VECTOR_STORE_PASSWORD;
    const token = env.VECTOR_STORE_API_KEY;
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Milvus
        _fallbackFrom: "milvus"
      };
    }
    console.log("building milvus backend", {
      collectionName,
      dimension,
      url,
      host,
      port
    });
    return {
      type: "milvus",
      collectionName,
      dimension,
      url,
      host,
      port,
      username,
      password,
      token
    };
  } else if (storeType === "chroma") {
    const host = env.VECTOR_STORE_HOST;
    const url = env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT;
    const headers = void 0;
    const envDistance = env.VECTOR_STORE_DISTANCE;
    let distance = "cosine";
    if (envDistance) {
      switch (envDistance.toLowerCase()) {
        case "cosine":
          distance = "cosine";
          break;
        case "euclidean":
        case "l2":
          distance = "l2";
          break;
        case "dot":
        case "ip":
          distance = "ip";
          break;
        default:
          distance = "cosine";
      }
    }
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from ChromaDB
        _fallbackFrom: "chroma"
      };
    }
    return {
      type: "chroma",
      collectionName,
      dimension,
      url,
      host,
      port,
      headers,
      distance
    };
  } else if (storeType === "pinecone") {
    const apiKey = env.VECTOR_STORE_API_KEY;
    const collectionName2 = env.VECTOR_STORE_COLLECTION || "default";
    const provider = env.PINECONE_PROVIDER || "aws";
    const region = env.PINECONE_REGION || "us-east-1";
    const envmetric = env.VECTOR_STORE_DISTANCE || "cosine";
    const dimension2 = env.VECTOR_STORE_DIMENSION || 1536;
    let metric = "cosine";
    switch (envmetric) {
      case "Cosine":
        metric = "cosine";
        break;
      case "Euclidean":
        metric = "euclidean";
        break;
      case "Dot":
        metric = "dotproduct";
        break;
      default:
        metric = "cosine";
        break;
    }
    if (!apiKey) {
      return {
        type: "in-memory",
        collectionName: collectionName2,
        dimension: dimension2,
        maxVectors,
        // Add a special property to indicate this is a fallback from Pinecone
        _fallbackFrom: "pinecone"
      };
    }
    return {
      type: "pinecone",
      apiKey,
      collectionName: collectionName2,
      provider,
      region,
      metric,
      dimension: dimension2
    };
  } else if (storeType === "pgvector") {
    const url = env.VECTOR_STORE_URL;
    const collectionName2 = env.VECTOR_STORE_COLLECTION || "pgvector_memory";
    const dimension2 = env.VECTOR_STORE_DIMENSION || 1536;
    const envdistance = env.VECTOR_STORE_DISTANCE;
    let distance = "Cosine";
    switch (envdistance) {
      case "Cosine":
        distance = "Cosine";
        break;
      case "Euclidean":
        distance = "Euclidean";
        break;
      case "Dot":
        distance = "Dot";
        break;
      default:
        distance = "Cosine";
    }
    if (!url) {
      return {
        type: "in-memory",
        collectionName: collectionName2,
        dimension: dimension2,
        maxVectors,
        // Add a special property to indicate this is a fallback from PgVector
        _fallbackFrom: "pgvector"
      };
    }
    return {
      type: "pgvector",
      url,
      collectionName: collectionName2,
      dimension: dimension2,
      distance
    };
  } else if (storeType === "redis") {
    const url = env.VECTOR_STORE_URL;
    const host = env.VECTOR_STORE_HOST;
    const port = env.VECTOR_STORE_PORT;
    const username = env.VECTOR_STORE_USERNAME;
    const password = env.VECTOR_STORE_PASSWORD;
    const envdistance = env.WORKSPACE_VECTOR_STORE_DISTANCE || "Cosine";
    let distance = "COSINE";
    switch (envdistance) {
      case "Euclidean":
        distance = "L2";
        break;
      case "Dot":
        distance = "IP";
        break;
      case "Cosine":
        distance = "COSINE";
        break;
      default:
        distance = "COSINE";
    }
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Redis
        _fallbackFrom: "redis"
      };
    }
    return {
      type: "redis",
      url: url || "",
      host,
      port,
      username,
      password,
      collectionName,
      dimension,
      distance
    };
  } else if (storeType === "faiss") {
    const baseStoragePath = env.FAISS_BASE_STORAGE_PATH;
    const envmetric = env.VECTOR_STORE_DISTANCE;
    let distance = "Cosine";
    if (distance) {
      switch (distance.toLowerCase()) {
        case "Cosine":
          distance = "Cosine";
          break;
        case "L2":
          distance = "Euclidean";
          break;
        case "IP":
          distance = "IP";
          break;
        default:
          distance = "Cosine";
      }
    }
    if (!baseStoragePath) {
      logger2.warn("Faiss base storage path not provided, falling back to in-memory storage", {
        hasBaseStoragePath: !!baseStoragePath
      });
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        _fallbackFrom: "faiss"
      };
    }
    logger2.debug("Creating FAISS configuration", {
      baseStoragePath,
      dimension,
      distance
    });
    return {
      type: "faiss",
      collectionName,
      dimension,
      baseStoragePath,
      distance
    };
  } else if (storeType === "weaviate") {
    const host = env.VECTOR_STORE_HOST;
    const url = env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT;
    const username = env.VECTOR_STORE_USERNAME;
    const password = env.VECTOR_STORE_PASSWORD;
    const apiKey = env.VECTOR_STORE_API_KEY;
    const envdistance = env.VECTOR_STORE_DISTANCE;
    let distance = "Cosine";
    if (envdistance) {
      switch (envdistance.toLowerCase()) {
        case "Cosine":
          distance = "Cosine";
          break;
        case "L2":
          distance = "Euclidean";
          break;
        case "IP":
          distance = "IP";
          break;
        default:
          distance = "Cosine";
      }
    }
    if (!url && !host) {
      logger2.warn("Weaviate URL or host not provided, falling back to in-memory storage", {
        hasUrl: !!url,
        hasHost: !!host
      });
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        _fallbackFrom: "weaviate"
      };
    }
    logger2.debug("Creating Weaviate configuration", {
      url,
      host,
      port,
      username,
      password,
      apiKey,
      dimension,
      distance
    });
    return {
      type: "weaviate",
      url,
      host,
      port,
      collectionName,
      dimension,
      username,
      password,
      apiKey,
      distance
    };
  } else {
    return {
      type: "in-memory",
      collectionName,
      dimension,
      maxVectors
    };
  }
}
function getWorkspaceVectorStoreConfigFromEnv(agentConfig) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const storeType = env.WORKSPACE_VECTOR_STORE_TYPE || env.VECTOR_STORE_TYPE;
  const collectionName = env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory";
  let dimension = env.WORKSPACE_VECTOR_STORE_DIMENSION !== void 0 && !Number.isNaN(env.WORKSPACE_VECTOR_STORE_DIMENSION) ? env.WORKSPACE_VECTOR_STORE_DIMENSION : env.VECTOR_STORE_DIMENSION !== void 0 && !Number.isNaN(env.VECTOR_STORE_DIMENSION) ? env.VECTOR_STORE_DIMENSION : 1536;
  const maxVectors = env.WORKSPACE_VECTOR_STORE_MAX_VECTORS !== void 0 && !Number.isNaN(env.WORKSPACE_VECTOR_STORE_MAX_VECTORS) ? env.WORKSPACE_VECTOR_STORE_MAX_VECTORS : env.VECTOR_STORE_MAX_VECTORS !== void 0 && !Number.isNaN(env.VECTOR_STORE_MAX_VECTORS) ? env.VECTOR_STORE_MAX_VECTORS : 1e4;
  if (agentConfig?.embedding && typeof agentConfig.embedding === "object" && agentConfig.embedding.dimensions) {
    const embeddingDimension = agentConfig.embedding.dimensions;
    if (typeof embeddingDimension === "number" && embeddingDimension > 0) {
      logger2.debug("Overriding workspace vector store dimension from agent config", {
        envDimension: dimension,
        agentDimension: embeddingDimension,
        embeddingType: agentConfig.embedding.type
      });
      dimension = embeddingDimension;
    }
  }
  logger2.debug("Creating workspace vector store config", {
    storeType,
    collectionName,
    dimension,
    maxVectors,
    usingWorkspaceSpecificType: !!env.WORKSPACE_VECTOR_STORE_TYPE,
    usingWorkspaceSpecificCollection: !!env.WORKSPACE_VECTOR_STORE_COLLECTION
  });
  console.log("storeType", storeType);
  if (storeType === "qdrant") {
    const host = env.WORKSPACE_VECTOR_STORE_HOST || env.VECTOR_STORE_HOST;
    const url = env.WORKSPACE_VECTOR_STORE_URL || env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.WORKSPACE_VECTOR_STORE_PORT) ? Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT : env.WORKSPACE_VECTOR_STORE_PORT;
    const apiKey = env.WORKSPACE_VECTOR_STORE_API_KEY || env.VECTOR_STORE_API_KEY;
    const distance = env.WORKSPACE_VECTOR_STORE_DISTANCE || env.VECTOR_STORE_DISTANCE;
    const onDisk = env.WORKSPACE_VECTOR_STORE_ON_DISK;
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Qdrant
        _fallbackFrom: "qdrant-workspace"
      };
    }
    return {
      type: "qdrant",
      collectionName,
      dimension,
      url,
      host,
      port,
      apiKey,
      distance,
      onDisk
    };
  } else if (storeType === "milvus") {
    const host = env.WORKSPACE_VECTOR_STORE_HOST || env.VECTOR_STORE_HOST;
    const url = env.WORKSPACE_VECTOR_STORE_URL || env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.WORKSPACE_VECTOR_STORE_PORT) ? Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT : env.WORKSPACE_VECTOR_STORE_PORT;
    const username = env.WORKSPACE_VECTOR_STORE_USERNAME || env.VECTOR_STORE_USERNAME;
    const password = env.WORKSPACE_VECTOR_STORE_PASSWORD || env.VECTOR_STORE_PASSWORD;
    const token = env.WORKSPACE_VECTOR_STORE_API_KEY || env.VECTOR_STORE_API_KEY;
    if (!url && !host) {
      console.log("Milvus backend not initialzed, returning in-memory config with fallback marker");
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Milvus
        _fallbackFrom: "milvus-workspace"
      };
    }
    return {
      type: "milvus",
      collectionName,
      dimension,
      url,
      host,
      port,
      username,
      password,
      token
    };
  } else if (storeType === "chroma") {
    const host = env.WORKSPACE_VECTOR_STORE_HOST || env.VECTOR_STORE_HOST;
    const url = env.WORKSPACE_VECTOR_STORE_URL || env.VECTOR_STORE_URL;
    const port = Number.isNaN(env.WORKSPACE_VECTOR_STORE_PORT) ? Number.isNaN(env.VECTOR_STORE_PORT) ? void 0 : env.VECTOR_STORE_PORT : env.WORKSPACE_VECTOR_STORE_PORT;
    const headers = void 0;
    const envDistance = env.WORKSPACE_VECTOR_STORE_DISTANCE || env.VECTOR_STORE_DISTANCE;
    let distance = "cosine";
    if (envDistance) {
      switch (envDistance.toLowerCase()) {
        case "cosine":
          distance = "cosine";
          break;
        case "euclidean":
        case "l2":
          distance = "l2";
          break;
        case "dot":
        case "ip":
          distance = "ip";
          break;
        default:
          distance = "cosine";
      }
    }
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from ChromaDB
        _fallbackFrom: "chroma-workspace"
      };
    }
    return {
      type: "chroma",
      collectionName,
      dimension,
      url,
      host,
      port,
      headers,
      distance
    };
  } else if (storeType === "pinecone") {
    const apiKey = env.WORKSPACE_VECTOR_STORE_API_KEY;
    const collectionName2 = env.WORKSPACE_VECTOR_STORE_COLLECTION || "default";
    const provider = env.WORKSPACE_PINECONE_PROVIDER || "aws";
    const region = env.WORKSPACE_PINECONE_REGION || "us-east-1";
    const envmetric = env.WORKSPACE_VECTOR_STORE_DISTANCE || "cosine";
    const dimension2 = env.WORKSPACE_VECTOR_STORE_DIMENSION || 1536;
    let metric = "cosine";
    switch (envmetric) {
      case "Cosine":
        metric = "cosine";
        break;
      case "Euclidean":
        metric = "euclidean";
        break;
      case "Dot":
        metric = "dotproduct";
        break;
      default:
        metric = "cosine";
        break;
    }
    if (!apiKey) {
      return {
        type: "in-memory",
        collectionName: collectionName2,
        dimension: dimension2,
        maxVectors,
        // Add a special property to indicate this is a fallback from PgVector
        _fallbackFrom: "pinecone"
      };
    }
    return {
      type: "pinecone",
      apiKey,
      collectionName: collectionName2,
      provider,
      region,
      metric,
      dimension: dimension2
    };
  } else if (storeType === "pgvector") {
    const url = env.WORKSPACE_VECTOR_STORE_URL;
    const collectionName2 = env.WORKSPACE_VECTOR_STORE_COLLECTION || "pgvector_memory";
    const dimension2 = env.WORKSPACE_VECTOR_STORE_DIMENSION || 1536;
    const distance = env.WORKSPACE_VECTOR_STORE_DISTANCE || "Cosine";
    if (!url) {
      return {
        type: "in-memory",
        collectionName: collectionName2,
        dimension: dimension2,
        maxVectors,
        // Add a special property to indicate this is a fallback from PgVector
        _fallbackFrom: "pgvector"
      };
    }
    return {
      type: "pgvector",
      url,
      collectionName: collectionName2,
      dimension: dimension2,
      distance
    };
  } else if (storeType === "redis") {
    const url = env.WORKSPACE_VECTOR_STORE_URL;
    const host = env.WORKSPACE_VECTOR_STORE_HOST;
    const port = env.WORKSPACE_VECTOR_STORE_PORT;
    const username = env.WORKSPACE_VECTOR_STORE_USERNAME;
    const password = env.WORKSPACE_VECTOR_STORE_PASSWORD;
    const envdistance = env.WORKSPACE_VECTOR_STORE_DISTANCE || "Cosine";
    let distance = "COSINE";
    switch (envdistance) {
      case "Euclidean":
        distance = "L2";
        break;
      case "Dot":
        distance = "IP";
        break;
      case "Cosine":
        distance = "COSINE";
        break;
      default:
        distance = "COSINE";
    }
    if (!url && !host) {
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        // Add a special property to indicate this is a fallback from Redis
        _fallbackFrom: "redis"
      };
    }
    return {
      type: "redis",
      url: url || "",
      host,
      port,
      username,
      password,
      collectionName,
      dimension,
      distance
    };
  } else if (storeType === "weaviate") {
    const host = env.WORKSPACE_VECTOR_STORE_HOST;
    const url = env.WORKSPACE_VECTOR_STORE_URL;
    const port = Number.isNaN(env.WORKSPACE_VECTOR_STORE_PORT) ? void 0 : env.WORKSPACE_VECTOR_STORE_PORT;
    const username = env.WORKSPACE_VECTOR_STORE_USERNAME;
    const password = env.WORKSPACE_VECTOR_STORE_PASSWORD;
    const apiKey = env.WORKSPACE_VECTOR_STORE_API_KEY;
    const envdistance = env.WORKSPACE_VECTOR_STORE_DISTANCE;
    let distance = "Cosine";
    if (envdistance) {
      switch (envdistance.toLowerCase()) {
        case "Cosine":
          distance = "Cosine";
          break;
        case "L2":
          distance = "Euclidean";
          break;
        case "IP":
          distance = "IP";
          break;
        default:
          distance = "Cosine";
      }
    }
    if (!url && !host) {
      logger2.warn("Weaviate URL or host not provided, falling back to in-memory storage", {
        hasUrl: !!url,
        hasHost: !!host
      });
      return {
        type: "in-memory",
        collectionName,
        dimension,
        maxVectors,
        _fallbackFrom: "weaviate"
      };
    }
    logger2.debug("Creating Weaviate configuration", {
      url,
      host,
      port,
      username,
      password,
      apiKey,
      dimension,
      distance
    });
    return {
      type: "weaviate",
      url,
      host,
      port,
      collectionName,
      dimension,
      username,
      password,
      apiKey,
      distance
    };
  } else {
    return {
      type: "in-memory",
      collectionName,
      dimension,
      maxVectors
    };
  }
}
async function createMultiCollectionVectorStoreFromEnv(agentConfig) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const config2 = getVectorStoreConfigFromEnv(agentConfig);
  const serviceCache = getServiceCache();
  const cacheKey = createServiceKey("multiCollectionVectorStore", {
    type: config2.type,
    collection: config2.collectionName,
    reflectionCollection: env.REFLECTION_VECTOR_STORE_COLLECTION || "",
    workspaceCollection: env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory",
    workspaceEnabled: !!env.USE_WORKSPACE_MEMORY,
    // Include dimension for proper cache key differentiation
    dimension: config2.dimension
  });
  return await serviceCache.getOrCreate(cacheKey, async () => {
    logger2.debug("Creating new multi collection vector store instance");
    return await createMultiCollectionVectorStoreInternal(config2, logger2);
  });
}
async function createMultiCollectionVectorStoreInternal(config2, logger2) {
  const { MultiCollectionVectorManager: MultiCollectionVectorManager2 } = await Promise.resolve().then(() => (init_multi_collection_manager(), multi_collection_manager_exports));
  logger2.info(`${LOG_PREFIXES4.FACTORY} Creating multi collection vector storage from environment`, {
    type: config2.type,
    knowledgeCollection: config2.collectionName,
    reflectionCollection: env.REFLECTION_VECTOR_STORE_COLLECTION || "disabled",
    workspaceCollection: env.USE_WORKSPACE_MEMORY ? env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory" : "disabled",
    workspaceEnabled: !!env.USE_WORKSPACE_MEMORY
  });
  const manager = new MultiCollectionVectorManager2(config2);
  try {
    const connected = await manager.connect();
    if (!connected) {
      throw new Error("Failed to connect multi collection vector manager");
    }
    const knowledgeStore = manager.getStore("knowledge");
    const reflectionStore = manager.getStore("reflection");
    const workspaceStore = manager.getStore("workspace");
    if (!knowledgeStore) {
      throw new Error("Failed to get knowledge store from multi collection manager");
    }
    logger2.info(`${LOG_PREFIXES4.FACTORY} Multi collection vector storage created successfully`, {
      knowledge: !!knowledgeStore,
      reflection: !!reflectionStore,
      workspace: !!workspaceStore
    });
    return {
      manager,
      knowledgeStore,
      reflectionStore,
      workspaceStore
    };
  } catch (error) {
    logger2.warn(`${LOG_PREFIXES4.FACTORY} Multi collection connection failed, attempting fallback`, {
      originalType: config2.type,
      error: error instanceof Error ? error.message : String(error)
    });
    await manager.disconnect().catch(() => {
    });
    const fallbackConfig = {
      type: "in-memory",
      collectionName: config2.collectionName,
      dimension: config2.dimension,
      maxVectors: 1e4
    };
    try {
      const fallbackManager = new MultiCollectionVectorManager2(fallbackConfig);
      const connected = await fallbackManager.connect();
      if (!connected) {
        throw new Error("Failed to connect fallback multi collection vector manager");
      }
      const knowledgeStore = fallbackManager.getStore("knowledge");
      const reflectionStore = fallbackManager.getStore("reflection");
      const workspaceStore = fallbackManager.getStore("workspace");
      if (!knowledgeStore) {
        throw new Error("Failed to get knowledge store from fallback multi collection manager");
      }
      logger2.info(
        `${LOG_PREFIXES4.FACTORY} Successfully created fallback in-memory multi collection`,
        {
          knowledge: !!knowledgeStore,
          reflection: !!reflectionStore,
          workspace: !!workspaceStore,
          originalType: config2.type
        }
      );
      return {
        manager: fallbackManager,
        knowledgeStore,
        reflectionStore,
        workspaceStore
      };
    } catch (fallbackError) {
      logger2.error(
        `${LOG_PREFIXES4.FACTORY} Failed to create fallback multi collection vector storage`,
        {
          originalError: error instanceof Error ? error.message : String(error),
          fallbackError: fallbackError instanceof Error ? fallbackError.message : String(fallbackError)
        }
      );
      throw new Error(
        `Failed to create both primary and fallback multi collection vector storage: ${error instanceof Error ? error.message : String(error)}`
      );
    }
  }
}
var init_factory7 = __esm({
  "src/core/vector_storage/factory.ts"() {
    init_cjs_shims();
    init_manager6();
    init_dual_collection_manager();
    init_logger2();
    init_constants4();
    init_env();
    init_service_cache();
  }
});

// src/core/brain/memory/lazy-service-wrapper.ts
function getDefaultLazyConfig() {
  return {
    enableEmbeddingLazy: env.ENABLE_LAZY_LOADING === "true",
    enableVectorStoreLazy: env.ENABLE_LAZY_LOADING === "true",
    enableLLMServiceLazy: env.ENABLE_LAZY_LOADING === "true",
    lazyInitTimeout: parseInt(env.LAZY_INIT_TIMEOUT || "10000", 10),
    skipHeavyServicesOnStartup: env.ENABLE_LAZY_LOADING === "true"
  };
}
var LazyEmbeddingManager, LazyVectorStoreManager, LazyLLMService;
var init_lazy_service_wrapper = __esm({
  "src/core/brain/memory/lazy-service-wrapper.ts"() {
    init_cjs_shims();
    init_logger2();
    init_env();
    LazyEmbeddingManager = class {
      _instance = null;
      _loading = null;
      _factory = null;
      _config;
      constructor(factory, config2 = {}) {
        this._factory = factory;
        this._config = { ...getDefaultLazyConfig(), ...config2 };
      }
      /**
       * Get the embedding manager, loading it lazily if needed
       */
      async getInstance() {
        if (this._instance) {
          return this._instance;
        }
        if (this._loading) {
          return this._loading;
        }
        if (!this._factory) {
          throw new Error("LazyEmbeddingManager: No factory provided");
        }
        logger.debug("LazyEmbeddingManager: Loading embedding manager");
        const startTime = Date.now();
        this._loading = Promise.race([
          this._factory(),
          new Promise((_, reject) => {
            setTimeout(() => {
              reject(
                new Error(`LazyEmbeddingManager: Timeout after ${this._config.lazyInitTimeout}ms`)
              );
            }, this._config.lazyInitTimeout || 1e4);
          })
        ]);
        try {
          this._instance = await this._loading;
          const loadTime = Date.now() - startTime;
          logger.debug(`LazyEmbeddingManager: Loaded in ${loadTime}ms`);
          return this._instance;
        } catch (error) {
          this._loading = null;
          throw error;
        }
      }
      /**
       * Check if the service is already loaded
       */
      isLoaded() {
        return this._instance !== null;
      }
      /**
       * Get the instance if already loaded, otherwise return null
       */
      getLoadedInstance() {
        return this._instance;
      }
    };
    LazyVectorStoreManager = class {
      _instance = null;
      _loading = null;
      _factory = null;
      _config;
      constructor(factory, config2 = {}) {
        this._factory = factory;
        this._config = { ...getDefaultLazyConfig(), ...config2 };
      }
      /**
       * Get the vector store manager, loading it lazily if needed
       */
      async getInstance() {
        if (this._instance) {
          return this._instance;
        }
        if (this._loading) {
          return this._loading;
        }
        if (!this._factory) {
          throw new Error("LazyVectorStoreManager: No factory provided");
        }
        logger.debug("LazyVectorStoreManager: Loading vector store manager");
        const startTime = Date.now();
        this._loading = Promise.race([
          this._factory(),
          new Promise((_, reject) => {
            setTimeout(() => {
              reject(
                new Error(`LazyVectorStoreManager: Timeout after ${this._config.lazyInitTimeout}ms`)
              );
            }, this._config.lazyInitTimeout || 1e4);
          })
        ]);
        try {
          this._instance = await this._loading;
          const loadTime = Date.now() - startTime;
          logger.debug(`LazyVectorStoreManager: Loaded in ${loadTime}ms`);
          return this._instance;
        } catch (error) {
          this._loading = null;
          throw error;
        }
      }
      /**
       * Check if the service is already loaded
       */
      isLoaded() {
        return this._instance !== null;
      }
      /**
       * Get the instance if already loaded, otherwise return null
       */
      getLoadedInstance() {
        return this._instance;
      }
    };
    LazyLLMService = class {
      _instance = null;
      _loading = null;
      _factory = null;
      _config;
      constructor(factory, config2 = {}) {
        this._factory = factory;
        this._config = { ...getDefaultLazyConfig(), ...config2 };
      }
      /**
       * Get the LLM service, loading it lazily if needed
       */
      async getInstance() {
        if (this._instance) {
          return this._instance;
        }
        if (this._loading) {
          return this._loading;
        }
        if (!this._factory) {
          throw new Error("LazyLLMService: No factory provided");
        }
        logger.debug("LazyLLMService: Loading LLM service");
        const startTime = Date.now();
        this._loading = Promise.race([
          this._factory(),
          new Promise((_, reject) => {
            setTimeout(() => {
              reject(new Error(`LazyLLMService: Timeout after ${this._config.lazyInitTimeout}ms`));
            }, this._config.lazyInitTimeout || 1e4);
          })
        ]);
        try {
          this._instance = await this._loading;
          const loadTime = Date.now() - startTime;
          logger.debug(`LazyLLMService: Loaded in ${loadTime}ms`);
          return this._instance;
        } catch (error) {
          this._loading = null;
          throw error;
        }
      }
      /**
       * Check if the service is already loaded
       */
      isLoaded() {
        return this._instance !== null;
      }
      /**
       * Get the instance if already loaded, otherwise return null
       */
      getLoadedInstance() {
        return this._instance;
      }
    };
  }
});

// src/core/brain/memory/enhanced-service-initializer.ts
async function createEnhancedAgentServices(agentConfig, options = {}) {
  const startTime = Date.now();
  const lazyConfig = {
    ...getDefaultLazyConfig(),
    ...options.lazyConfig,
    // Override with explicit option
    ...options.enableLazyLoading !== void 0 && {
      enableEmbeddingLazy: options.enableLazyLoading,
      enableVectorStoreLazy: options.enableLazyLoading,
      enableLLMServiceLazy: options.enableLazyLoading
    }
  };
  const shouldUseLazyLoading = options.enableLazyLoading ?? (env.ENABLE_LAZY_LOADING === "true" || lazyConfig.skipHeavyServicesOnStartup);
  logger.debug("EnhancedServiceInitializer: Starting service initialization", {
    lazyLoading: shouldUseLazyLoading,
    appMode: options.appMode,
    skipHeavyServices: lazyConfig.skipHeavyServicesOnStartup
  });
  if (!shouldUseLazyLoading) {
    logger.debug("EnhancedServiceInitializer: Using standard service initialization");
    const services = await createAgentServices(agentConfig, options.appMode);
    const initTime2 = Date.now() - startTime;
    logger.debug(`EnhancedServiceInitializer: Standard initialization completed in ${initTime2}ms`);
    return {
      ...services,
      lazyConfig
    };
  }
  logger.debug("EnhancedServiceInitializer: Using lazy loading initialization");
  const modifiedConfig = { ...agentConfig };
  let originalEmbeddingConfig = null;
  if (lazyConfig.enableEmbeddingLazy && lazyConfig.skipHeavyServicesOnStartup) {
    originalEmbeddingConfig = modifiedConfig.embedding;
    modifiedConfig.embedding = { disabled: true };
  }
  const coreServices = await createAgentServices(modifiedConfig, options.appMode);
  if (originalEmbeddingConfig !== null) {
    modifiedConfig.embedding = originalEmbeddingConfig;
  }
  const lazyServices = {
    ...coreServices,
    lazyConfig
  };
  if (lazyConfig.enableEmbeddingLazy) {
    lazyServices.lazyEmbeddingManager = new LazyEmbeddingManager(async () => {
      logger.debug("LazyEmbeddingManager: Creating embedding manager");
      const embeddingManager = new EmbeddingManager();
      if (agentConfig.embedding && typeof agentConfig.embedding === "object") {
        const result = await embeddingManager.createEmbedderFromConfig(
          agentConfig.embedding,
          "default"
        );
        if (!result) {
          throw new Error("Failed to create embedder from config");
        }
      }
      return embeddingManager;
    }, lazyConfig);
    if (lazyConfig.skipHeavyServicesOnStartup) {
      delete lazyServices.embeddingManager;
    }
  }
  if (lazyConfig.enableVectorStoreLazy) {
    lazyServices.lazyVectorStoreManager = new LazyVectorStoreManager(async () => {
      logger.debug("LazyVectorStoreManager: Creating vector store manager");
      const reflectionEnabled = !env.DISABLE_REFLECTION_MEMORY && env.REFLECTION_VECTOR_STORE_COLLECTION && env.REFLECTION_VECTOR_STORE_COLLECTION.trim() !== "";
      if (reflectionEnabled) {
        const { manager } = await createDualCollectionVectorStoreFromEnv(agentConfig);
        return manager;
      } else {
        const { manager } = await createVectorStoreFromEnv(agentConfig);
        return manager;
      }
    }, lazyConfig);
  }
  if (lazyConfig.enableLLMServiceLazy) {
    lazyServices.lazyLLMService = new LazyLLMService(async () => {
      logger.debug("LazyLLMService: Creating LLM service");
      const llmConfig = { ...agentConfig.llm, maxIterations: agentConfig.llm.maxIterations || 50 };
      const contextManager = createContextManager(
        llmConfig,
        coreServices.promptManager,
        void 0,
        void 0
      );
      const llmService = createLLMService(llmConfig, coreServices.mcpManager, contextManager);
      return llmService;
    }, lazyConfig);
    if (lazyConfig.skipHeavyServicesOnStartup) {
      delete lazyServices.llmService;
    }
  }
  const initTime = Date.now() - startTime;
  logger.info("EnhancedServiceInitializer: Lazy initialization completed", {
    initTime: `${initTime}ms`,
    lazyEmbedding: !!lazyServices.lazyEmbeddingManager,
    lazyVectorStore: !!lazyServices.lazyVectorStoreManager,
    lazyLLMService: !!lazyServices.lazyLLMService
  });
  return lazyServices;
}
function shouldEnableLazyLoading(options = {}) {
  if (options.enableLazyLoading !== void 0) {
    return options.enableLazyLoading;
  }
  const lazyConfig = getDefaultLazyConfig();
  return env.ENABLE_LAZY_LOADING === "true" || Boolean(lazyConfig.skipHeavyServicesOnStartup) || Boolean(lazyConfig.enableEmbeddingLazy) || Boolean(lazyConfig.enableVectorStoreLazy) || Boolean(lazyConfig.enableLLMServiceLazy);
}
async function getEmbeddingManager(services) {
  if (services.lazyEmbeddingManager) {
    try {
      return await services.lazyEmbeddingManager.getInstance();
    } catch (error) {
      logger.warn("Failed to get lazy embedding manager", {
        error: error instanceof Error ? error.message : String(error)
      });
      return void 0;
    }
  }
  return services.embeddingManager;
}
async function getVectorStoreManager(services) {
  if (services.lazyVectorStoreManager) {
    try {
      return await services.lazyVectorStoreManager.getInstance();
    } catch (error) {
      logger.warn("Failed to get lazy vector store manager", {
        error: error instanceof Error ? error.message : String(error)
      });
      return void 0;
    }
  }
  return services.vectorStoreManager;
}
async function getLLMService2(services) {
  if (services.lazyLLMService) {
    try {
      return await services.lazyLLMService.getInstance();
    } catch (error) {
      logger.warn("Failed to get lazy LLM service", {
        error: error instanceof Error ? error.message : String(error)
      });
      return void 0;
    }
  }
  return services.llmService;
}
var init_enhanced_service_initializer = __esm({
  "src/core/brain/memory/enhanced-service-initializer.ts"() {
    init_cjs_shims();
    init_service_initializer();
    init_logger2();
    init_embedding();
    init_factory5();
    init_factory4();
    init_factory7();
    init_env();
    init_lazy_service_wrapper();
  }
});

// src/core/brain/memory/lazy-extract-and-operate.ts
function getLazyMemoryConfig() {
  return {
    enableLazyLoading: env.ENABLE_LAZY_LOADING === "true",
    lazyLoadingThreshold: 3,
    enableLightweightProcessing: true,
    operationTimeout: 8e3
  };
}
function processLightweight(args, startTime) {
  const processingTime = Date.now() - startTime;
  logger.debug(`LazyExtractAndOperate: Lightweight processing completed in ${processingTime}ms`);
  return {
    success: true,
    extracted_facts: [],
    memory_operations: [],
    processing_summary: {
      total_facts: 0,
      memory_operations: 0,
      processing_mode: "lightweight",
      processing_time_ms: processingTime
    },
    message: "Processed with lightweight optimization - no significant technical content detected."
  };
}
function createEmptyResult(args, startTime) {
  const processingTime = Date.now() - startTime;
  return {
    success: false,
    extracted_facts: [],
    memory_operations: [],
    processing_summary: {
      total_facts: 0,
      memory_operations: 0,
      processing_mode: "skipped",
      processing_time_ms: processingTime
    },
    message: "Memory operations skipped - required services not available."
  };
}
var lazyExtractAndOperateMemoryTool;
var init_lazy_extract_and_operate = __esm({
  "src/core/brain/memory/lazy-extract-and-operate.ts"() {
    init_cjs_shims();
    init_logger2();
    init_enhanced_service_initializer();
    init_extract_and_operate_memory();
    init_env();
    lazyExtractAndOperateMemoryTool = {
      name: "lazy_extract_and_operate_memory",
      category: "memory",
      internal: true,
      description: extractAndOperateMemoryTool.description + " (with lazy loading optimizations)",
      version: "2.0.0",
      parameters: extractAndOperateMemoryTool.parameters,
      handler: async (args, context) => {
        const startTime = Date.now();
        const lazyConfig = getLazyMemoryConfig();
        try {
          const shouldUseLazyLoading = lazyConfig.enableLazyLoading && context?.services && "lazyEmbeddingManager" in context.services;
          if (!shouldUseLazyLoading) {
            logger.debug("LazyExtractAndOperate: Using standard processing");
            return await extractAndOperateMemoryTool.handler(args, context);
          }
          const lazyServices = context.services;
          const interaction = args.interaction || "";
          const isSimpleOperation = interaction.length < 100 && !interaction.includes("```") && !interaction.includes("function") && !interaction.includes("class");
          if (lazyConfig.enableLightweightProcessing && isSimpleOperation) {
            logger.debug("LazyExtractAndOperate: Using lightweight processing for simple operation");
            return processLightweight(args, startTime);
          }
          logger.debug("LazyExtractAndOperate: Loading services lazily for complex operation");
          const embeddingManager = await getEmbeddingManager(lazyServices);
          if (!embeddingManager) {
            logger.warn("LazyExtractAndOperate: No embedding manager available, skipping");
            return createEmptyResult(args, startTime);
          }
          const vectorStoreManager = await getVectorStoreManager(lazyServices);
          const llmService = await getLLMService2(lazyServices);
          const enhancedContext = {
            ...context,
            services: {
              embeddingManager,
              vectorStoreManager,
              llmService,
              knowledgeGraphManager: lazyServices.knowledgeGraphManager
            }
          };
          const result = await extractAndOperateMemoryTool.handler(args, enhancedContext);
          const processingTime = Date.now() - startTime;
          logger.debug(`LazyExtractAndOperate: Completed lazy processing in ${processingTime}ms`);
          return result;
        } catch (error) {
          const processingTime = Date.now() - startTime;
          logger.error("LazyExtractAndOperate: Error during lazy processing", {
            error: error instanceof Error ? error.message : String(error),
            processingTime: `${processingTime}ms`
          });
          try {
            return await extractAndOperateMemoryTool.handler(args, context);
          } catch (fallbackError) {
            logger.error("LazyExtractAndOperate: Fallback also failed", {
              fallbackError: fallbackError instanceof Error ? fallbackError.message : String(fallbackError)
            });
            throw error;
          }
        }
      }
    };
  }
});

// src/core/brain/tools/def_reflective_memory_tools.ts
function generateTraceId() {
  return `trace_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
}
function detectReasoningLoops(steps) {
  const contentSet = /* @__PURE__ */ new Set();
  for (const step of steps) {
    if (contentSet.has(step.content)) {
      return true;
    }
    contentSet.add(step.content);
  }
  return false;
}
async function extractReasoningFromContent(userInput, reasoningContent, options) {
  const steps = [];
  const lines = userInput.split("\n").map((line) => line.trim()).filter((line) => line.length > 0);
  if (options.extractExplicit !== false) {
    for (const line of lines) {
      const thoughtMatch = line.match(/^(?:#\s*)?Thought:\s*(.+)$/i);
      if (thoughtMatch && thoughtMatch[1]) {
        steps.push({
          type: "thought",
          content: thoughtMatch[1]
        });
        continue;
      }
      const actionMatch = line.match(/^(?:#\s*)?Action:\s*(.+)$/i);
      if (actionMatch && actionMatch[1]) {
        steps.push({
          type: "action",
          content: actionMatch[1]
        });
        continue;
      }
      const observationMatch = line.match(/^(?:#\s*)?Observation:\s*(.+)$/i);
      if (observationMatch && observationMatch[1]) {
        steps.push({
          type: "observation",
          content: observationMatch[1]
        });
        continue;
      }
      const resultMatch = line.match(/^(?:#\s*)?Result:\s*(.+)$/i);
      if (resultMatch && resultMatch[1]) {
        steps.push({
          type: "conclusion",
          content: resultMatch[1]
        });
        continue;
      }
      const conclusionMatch = line.match(/^(?:#\s*)?Conclusion:\s*(.+)$/i);
      if (conclusionMatch && conclusionMatch[1]) {
        steps.push({
          type: "conclusion",
          content: conclusionMatch[1]
        });
        continue;
      }
    }
  }
  if (options.extractImplicit !== false && steps.length === 0) {
    const reasoningKeywords = [
      "think",
      "consider",
      "analyze",
      "approach",
      "strategy",
      "implement",
      "solve",
      "problem",
      "solution",
      "method",
      "algorithm",
      "optimize"
    ];
    for (const line of lines) {
      if (line.length < 10) continue;
      const hasReasoningKeywords = reasoningKeywords.some(
        (keyword) => line.toLowerCase().includes(keyword)
      );
      if (hasReasoningKeywords) {
        let stepType = "thought";
        if (line.toLowerCase().includes("implement") || line.toLowerCase().includes("write")) {
          stepType = "action";
        } else if (line.toLowerCase().includes("result") || line.toLowerCase().includes("works")) {
          stepType = "observation";
        }
        steps.push({
          type: stepType,
          content: line
        });
      }
    }
  }
  return steps;
}
async function extractTaskContextFromContent(userInput, _reasoningContent) {
  const content = userInput;
  let goal;
  if (userInput.toLowerCase().includes("write") || userInput.toLowerCase().includes("create")) {
    goal = "Create or write code/solution";
  } else if (userInput.toLowerCase().includes("fix") || userInput.toLowerCase().includes("debug")) {
    goal = "Fix or debug issue";
  } else if (userInput.toLowerCase().includes("explain") || userInput.toLowerCase().includes("understand")) {
    goal = "Explain or understand concept";
  }
  let taskType;
  if (content.toLowerCase().includes("function") || content.toLowerCase().includes("code")) {
    taskType = "code_generation";
  } else if (content.toLowerCase().includes("analyze") || content.toLowerCase().includes("review")) {
    taskType = "analysis";
  } else if (content.toLowerCase().includes("solve") || content.toLowerCase().includes("problem")) {
    taskType = "problem_solving";
  }
  let domain;
  if (content.toLowerCase().includes("javascript") || content.toLowerCase().includes("typescript")) {
    domain = "javascript";
  } else if (content.toLowerCase().includes("python")) {
    domain = "python";
  } else if (content.toLowerCase().includes("react") || content.toLowerCase().includes("component")) {
    domain = "frontend";
  } else if (content.toLowerCase().includes("server") || content.toLowerCase().includes("api")) {
    domain = "backend";
  }
  let complexity;
  const wordCount = content.split(" ").length;
  if (wordCount < 50) {
    complexity = "low";
  } else if (wordCount < 200) {
    complexity = "medium";
  } else {
    complexity = "high";
  }
  return {
    ...goal && { goal },
    ...userInput && { input: userInput },
    ...taskType && { taskType },
    ...domain && { domain },
    ...complexity && { complexity }
  };
}
async function evaluateReasoningQuality(trace, options) {
  const issues = [];
  const suggestions = [];
  if (trace.steps.length === 0) {
    issues.push({
      type: "empty_trace",
      description: "Reasoning trace contains no steps",
      severity: "high"
    });
  }
  if (options.detectLoops !== false) {
    if (detectReasoningLoops(trace.steps)) {
      issues.push({
        type: "reasoning_loop",
        description: "Detected repetitive reasoning patterns",
        severity: "medium"
      });
      suggestions.push("Avoid repetitive reasoning steps");
    }
    const semanticRedundancy = detectSemanticRedundancy(trace.steps);
    if (semanticRedundancy > 0.5) {
      issues.push({
        type: "semantic_redundancy",
        description: "Multiple steps contain very similar content",
        severity: "medium"
      });
      suggestions.push("Consolidate similar reasoning steps for clarity");
    }
    const trivialSteps = trace.steps.filter(
      (step) => step.content.length < 20 || /^(ok|yes|no|done|good)$/i.test(step.content.trim())
    );
    if (trivialSteps.length > trace.steps.length * 0.3) {
      issues.push({
        type: "trivial_content",
        description: "Too many trivial or very short reasoning steps",
        severity: "low"
      });
      suggestions.push("Focus on substantial reasoning steps that add value");
    }
  }
  const stepTypes = new Set(trace.steps.map((s) => s.type));
  if (stepTypes.size === 1 && trace.steps.length > 3) {
    issues.push({
      type: "low_diversity",
      description: "All reasoning steps are of the same type",
      severity: "low"
    });
    suggestions.push(
      "Include different types of reasoning steps (thoughts, actions, observations)"
    );
  }
  let qualityScore = 1;
  qualityScore -= issues.filter((i) => i.severity === "high").length * 0.3;
  qualityScore -= issues.filter((i) => i.severity === "medium").length * 0.2;
  qualityScore -= issues.filter((i) => i.severity === "low").length * 0.1;
  qualityScore = Math.max(0, Math.min(1, qualityScore));
  const shouldStore = qualityScore >= 0.4 && // Lowered threshold from 0.6 to 0.4 (more inclusive)
  issues.filter((i) => i.severity === "high").length === 0;
  return {
    qualityScore,
    issues,
    suggestions,
    shouldStore,
    metrics: {
      efficiency: Math.min(1, trace.steps.length / 10),
      // Prefer concise reasoning
      clarity: stepTypes.size / Math.min(4, trace.steps.length),
      // Prefer diverse step types
      completeness: Math.min(1, trace.steps.length / 5)
      // Prefer adequate detail
    }
  };
}
function detectSemanticRedundancy(steps) {
  if (steps.length < 2) return 0;
  let redundantPairs = 0;
  let totalPairs = 0;
  for (let i = 0; i < steps.length; i++) {
    for (let j = i + 1; j < steps.length; j++) {
      const similarity = calculateQuerySimilarity(steps[i]?.content || "", steps[j]?.content || "");
      if (similarity > 0.7) {
        redundantPairs++;
      }
      totalPairs++;
    }
  }
  return totalPairs > 0 ? redundantPairs / totalPairs : 0;
}
function calculateQuerySimilarity(query1, query2) {
  const words1 = new Set(query1.toLowerCase().split(/\s+/));
  const words2 = new Set(query2.toLowerCase().split(/\s+/));
  const intersection = new Set([...words1].filter((x) => words2.has(x)));
  const union = /* @__PURE__ */ new Set([...words1, ...words2]);
  return intersection.size / union.size;
}
var ReasoningStepSchema, ReasoningTraceSchema, ReasoningEvaluationSchema, extractReasoningInputSchema, evaluateReasoningInputSchema, searchReasoningInputSchema, extractReasoningSteps, evaluateReasoning, searchReasoningPatterns;
var init_def_reflective_memory_tools = __esm({
  "src/core/brain/tools/def_reflective_memory_tools.ts"() {
    init_cjs_shims();
    init_zod();
    init_logger2();
    init_search_memory();
    init_env();
    ReasoningStepSchema = external_exports.object({
      type: external_exports.enum(["thought", "action", "observation", "decision", "conclusion", "reflection"]),
      content: external_exports.string().min(1)
    });
    ReasoningTraceSchema = external_exports.object({
      id: external_exports.string(),
      steps: external_exports.array(ReasoningStepSchema),
      metadata: external_exports.object({
        extractedAt: external_exports.string(),
        conversationLength: external_exports.number(),
        stepCount: external_exports.number(),
        hasExplicitMarkup: external_exports.boolean(),
        sessionId: external_exports.string().optional(),
        taskContext: external_exports.object({
          goal: external_exports.string().optional(),
          input: external_exports.string().optional(),
          taskType: external_exports.string().optional(),
          domain: external_exports.string().optional(),
          complexity: external_exports.enum(["low", "medium", "high"]).optional()
        }).optional(),
        extractionOptions: external_exports.object({
          extractExplicit: external_exports.boolean().optional(),
          extractImplicit: external_exports.boolean().optional(),
          includeMetadata: external_exports.boolean().optional()
        }).optional()
      })
    });
    ReasoningEvaluationSchema = external_exports.object({
      qualityScore: external_exports.number().min(0).max(1),
      issues: external_exports.array(
        external_exports.object({
          type: external_exports.string(),
          description: external_exports.string(),
          severity: external_exports.enum(["low", "medium", "high"]).optional()
        })
      ),
      suggestions: external_exports.array(external_exports.string()),
      shouldStore: external_exports.boolean(),
      metrics: external_exports.object({
        efficiency: external_exports.number().min(0).max(1),
        clarity: external_exports.number().min(0).max(1),
        completeness: external_exports.number().min(0).max(1)
      }).optional()
    });
    extractReasoningInputSchema = external_exports.object({
      userInput: external_exports.string().min(1),
      reasoningContent: external_exports.string().optional(),
      // Made optional since we only extract from userInput
      options: external_exports.object({
        extractExplicit: external_exports.boolean().default(true),
        extractImplicit: external_exports.boolean().default(true),
        includeMetadata: external_exports.boolean().default(true)
      }).optional().default({})
    });
    evaluateReasoningInputSchema = external_exports.object({
      trace: ReasoningTraceSchema,
      options: external_exports.object({
        checkEfficiency: external_exports.boolean().default(true),
        detectLoops: external_exports.boolean().default(true),
        generateSuggestions: external_exports.boolean().default(true)
      }).optional().default({})
    });
    searchReasoningInputSchema = external_exports.object({
      query: external_exports.string().min(1),
      options: external_exports.object({
        maxResults: external_exports.number().min(1).max(50).default(10),
        minQualityScore: external_exports.number().min(0).max(1).default(0.3),
        includeEvaluations: external_exports.boolean().default(true),
        enable_query_refinement: external_exports.boolean().default(false)
      }).optional().default({})
    });
    extractReasoningSteps = {
      name: "extract_reasoning_steps",
      category: "memory",
      internal: true,
      agentAccessible: false,
      // Internal-only: programmatically called when reasoning content is detected
      description: "Extract reasoning steps from user input only. Analyzes both explicit thought markup and implicit reasoning patterns to create structured reasoning traces. Only called when user input contains reasoning content.",
      version: "1.1.0",
      parameters: {
        type: "object",
        properties: {
          userInput: {
            type: "string",
            description: "The original user input or request containing reasoning steps"
          },
          reasoningContent: {
            type: "string",
            description: "[DEPRECATED] The reasoning content parameter - kept for backward compatibility but not used"
          },
          options: {
            type: "object",
            description: "Optional configuration for extraction behavior",
            properties: {
              extractExplicit: {
                type: "boolean",
                description: "Whether to extract explicit reasoning markup (Thought:, Action:, etc.)",
                default: true
              },
              extractImplicit: {
                type: "boolean",
                description: "Whether to extract implicit reasoning patterns from natural language",
                default: true
              },
              includeMetadata: {
                type: "boolean",
                description: "Whether to include timing and context metadata",
                default: true
              }
            }
          }
        },
        required: ["userInput"]
      },
      handler: async (args, context) => {
        logger.debug("Starting reasoning extraction from user input only", {
          userInputLength: args.userInput?.length || 0,
          options: args.options
        });
        try {
          const input = extractReasoningInputSchema.parse(args);
          const steps = await extractReasoningFromContent(
            input.userInput,
            input.reasoningContent || "",
            // Pass empty string if reasoningContent is not provided
            input.options || {}
          );
          const taskContext = await extractTaskContextFromContent(
            input.userInput,
            input.reasoningContent || ""
            // Pass empty string if reasoningContent is not provided
          );
          const trace = {
            id: generateTraceId(),
            steps,
            metadata: {
              extractedAt: (/* @__PURE__ */ new Date()).toISOString(),
              conversationLength: input.userInput.length,
              // Only count user input length
              stepCount: steps.length,
              hasExplicitMarkup: steps.length > 0 && steps.some((s) => s.content.includes(":")),
              sessionId: context?.sessionId,
              // Include extracted task context
              taskContext,
              ...input.options?.includeMetadata && {
                extractionOptions: input.options
              }
            }
          };
          logger.debug("Successfully extracted reasoning steps", {
            traceId: trace.id,
            stepCount: steps.length
          });
          return {
            success: true,
            result: { trace },
            metadata: {
              toolName: "extract_reasoning_steps",
              traceId: trace.id,
              stepCount: steps.length
            }
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("Failed to extract reasoning steps", { error: errorMessage });
          return {
            success: false,
            result: { error: `Extraction failed: ${errorMessage}` },
            metadata: { toolName: "extract_reasoning_steps", error: errorMessage }
          };
        }
      }
    };
    evaluateReasoning = {
      name: "evaluate_reasoning",
      category: "memory",
      internal: true,
      agentAccessible: false,
      // Internal-only: programmatically called when reasoning content is detected
      description: "Evaluate the quality and efficiency of extracted reasoning. Analyzes reasoning patterns for issues, calculates quality metrics, and generates improvement suggestions.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          trace: {
            type: "object",
            description: "The reasoning trace to evaluate (from extract_reasoning_steps)",
            properties: {
              id: { type: "string" },
              steps: {
                type: "array",
                items: {
                  type: "object",
                  properties: {
                    type: {
                      type: "string",
                      enum: [
                        "thought",
                        "action",
                        "observation",
                        "decision",
                        "conclusion",
                        "reflection"
                      ]
                    },
                    content: { type: "string" }
                  },
                  required: ["type", "content"]
                }
              },
              metadata: { type: "object" }
            },
            required: ["id", "steps"]
          },
          options: {
            type: "object",
            description: "Optional evaluation configuration",
            properties: {
              checkEfficiency: {
                type: "boolean",
                description: "Whether to analyze reasoning efficiency",
                default: true
              },
              detectLoops: {
                type: "boolean",
                description: "Whether to detect reasoning loops and redundancy",
                default: true
              },
              generateSuggestions: {
                type: "boolean",
                description: "Whether to generate improvement suggestions",
                default: true
              }
            }
          }
        },
        required: ["trace"]
      },
      handler: async (args, _context) => {
        logger.debug("Starting reasoning evaluation", {
          traceId: args.trace?.id,
          stepCount: args.trace?.steps?.length || 0,
          options: args.options
        });
        try {
          const input = evaluateReasoningInputSchema.parse(args);
          const evaluation = await evaluateReasoningQuality(input.trace, input.options || {});
          logger.debug("Successfully evaluated reasoning", {
            traceId: input.trace.id,
            qualityScore: evaluation.qualityScore,
            issueCount: evaluation.issues.length,
            suggestionCount: evaluation.suggestions.length,
            shouldStore: evaluation.shouldStore
          });
          return {
            success: true,
            result: { evaluation },
            metadata: {
              toolName: "evaluate_reasoning",
              traceId: input.trace.id,
              qualityScore: evaluation.qualityScore
            }
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("Failed to evaluate reasoning", { error: errorMessage });
          return {
            success: false,
            result: { error: `Evaluation failed: ${errorMessage}` },
            metadata: { toolName: "evaluate_reasoning", error: errorMessage }
          };
        }
      }
    };
    searchReasoningPatterns = {
      name: "search_reasoning_patterns",
      category: "memory",
      internal: true,
      agentAccessible: true,
      // Agent-accessible: one of two search tools available to agent
      description: "Search reflection memory for relevant reasoning patterns. Finds similar reasoning traces and evaluations that can inform current decision-making. Automatically deduplicates similar queries and batches searches for efficiency.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "Search query describing the type of reasoning pattern needed"
          },
          context: {
            type: "object",
            description: "Optional context to filter results",
            properties: {
              taskType: {
                type: "string",
                description: "Type of task (e.g., code_generation, analysis, problem_solving)"
              },
              domain: {
                type: "string",
                description: "Problem domain (e.g., javascript, python, frontend, backend)"
              },
              complexity: {
                type: "string",
                enum: ["low", "medium", "high"],
                description: "Task complexity level"
              }
            }
          },
          options: {
            type: "object",
            description: "Search configuration options",
            properties: {
              maxResults: {
                type: "number",
                description: "Maximum number of results to return",
                minimum: 1,
                maximum: 50,
                default: 10
              },
              minQualityScore: {
                type: "number",
                description: "Minimum quality score for results",
                minimum: 0,
                maximum: 1,
                default: 0.3
              },
              includeEvaluations: {
                type: "boolean",
                description: "Whether to include quality evaluations in results",
                default: true
              },
              deduplicateQueries: {
                type: "boolean",
                description: "Whether to deduplicate similar queries within the same session",
                default: true
              },
              enable_query_refinement: {
                type: "boolean",
                description: "Whether to apply query refinement for better search results (default: false)",
                default: false
              }
            }
          }
        },
        required: ["query"]
      },
      handler: async (args, _context) => {
        logger.debug("Starting reasoning pattern search", {
          query: args.query,
          context: args.context,
          options: args.options
        });
        try {
          const input = searchReasoningInputSchema.parse(args);
          const shouldDeduplicate = true;
          if (shouldDeduplicate && _context?.sessionId) {
            const queryKey = `search_patterns_${_context.sessionId}`;
            const recentQueries = global[queryKey] || [];
            const now = Date.now();
            const similarQuery = recentQueries.find(
              (q) => now - q.timestamp < 3e5 && // 5 minutes
              calculateQuerySimilarity(q.query, input.query) > 0.8
            );
            if (similarQuery) {
              logger.debug("ReasoningPatternSearch: Skipping duplicate query", {
                originalQuery: input.query,
                similarQuery: similarQuery.query,
                timeSinceLastQuery: now - similarQuery.timestamp
              });
              return {
                success: true,
                result: {
                  patterns: [],
                  metadata: {
                    searchTime: 0,
                    totalResults: 0,
                    deduplicatedQuery: true,
                    originalQuery: similarQuery.query
                  }
                }
              };
            }
            recentQueries.push({ query: input.query, timestamp: now });
            if (recentQueries.length > 10) {
              recentQueries.shift();
            }
            global[queryKey] = recentQueries;
          }
          let patterns = [];
          let usedMock = false;
          let fallback = false;
          let finalQueries = [input.query];
          const enableQueryRefinement = input.options?.enable_query_refinement === true || env.ENABLE_QUERY_REFINEMENT === true;
          if (enableQueryRefinement && _context?.services?.llmService) {
            try {
              const rewrittenQueries = await rewriteUserQuery(
                input.query,
                _context.services.llmService
              );
              finalQueries = rewrittenQueries.queries;
              logger.debug("ReasoningPatternSearch: Applied query refinement", {
                originalQuery: input.query,
                refinedQueries: finalQueries,
                queryCount: finalQueries.length
              });
            } catch (refinementError) {
              logger.warn("ReasoningPatternSearch: Query refinement failed, using original query", {
                error: refinementError instanceof Error ? refinementError.message : String(refinementError)
              });
              finalQueries = [input.query];
            }
          }
          if (_context && _context.services && _context.services.vectorStoreManager && _context.services.embeddingManager) {
            try {
              const embedder = _context.services.embeddingManager.getEmbedder("default");
              if (!embedder?.embed || typeof embedder.embed !== "function") {
                logger.debug("ReasoningPatternSearch: Embedder not available, using fallback");
                fallback = true;
              } else {
                logger.debug("ReasoningPatternSearch: Generating embedding for query", {
                  queryLength: input.query.length,
                  queryPreview: input.query.substring(0, 50),
                  refinedQueryCount: finalQueries.length
                });
                let queryEmbeddings;
                try {
                  queryEmbeddings = await embedder.embedBatch(finalQueries);
                } catch (embedError) {
                  logger.error(
                    "ReasoningPatternSearch: Failed to generate embedding, disabling embeddings globally",
                    {
                      error: embedError instanceof Error ? embedError.message : String(embedError),
                      provider: embedder.getConfig().type
                    }
                  );
                  if (_context?.services?.embeddingManager && embedError instanceof Error) {
                    _context.services.embeddingManager.handleRuntimeFailure(
                      embedError,
                      embedder.getConfig().type
                    );
                  }
                  fallback = true;
                  patterns = [];
                  usedMock = true;
                  logger.debug("ReasoningPatternSearch: Using mock response - embeddings disabled");
                  return {
                    success: true,
                    patterns: [],
                    metadata: {
                      totalPatterns: 0,
                      usedMock: true,
                      embeddingDisabled: true,
                      error: embedError instanceof Error ? embedError.message : String(embedError)
                    },
                    timestamp: (/* @__PURE__ */ new Date()).toISOString()
                  };
                }
                logger.debug("ReasoningPatternSearch: Embedding generated successfully", {
                  embeddingDimensions: Array.isArray(queryEmbeddings[0]) ? queryEmbeddings[0].length : "unknown"
                });
                const isDualManager = _context.services.vectorStoreManager.constructor.name === "DualCollectionVectorManager" || typeof _context.services.vectorStoreManager.getStore === "function" && _context.services.vectorStoreManager.getStore.length === 1;
                let store = null;
                if (isDualManager) {
                  logger.debug(
                    "ReasoningPatternSearch: Using DualCollectionVectorManager, accessing reflection collection"
                  );
                  store = _context.services.vectorStoreManager.getStore("reflection");
                } else {
                  logger.debug("ReasoningPatternSearch: Using single collection manager");
                  store = _context.services.vectorStoreManager.getStore();
                }
                if (store && typeof store.search === "function") {
                  patterns = await store.search(queryEmbeddings[0], input.options?.maxResults || 10);
                  usedMock = false;
                  logger.debug("ReasoningPatternSearch: Successfully accessed vector store", {
                    isDualManager,
                    resultsFound: patterns.length
                  });
                } else {
                  fallback = true;
                  logger.debug("ReasoningPatternSearch: Store not available or missing search method");
                }
              }
            } catch (error) {
              fallback = true;
              logger.debug("ReasoningPatternSearch: Error accessing vector store, using fallback", {
                error: error instanceof Error ? error.message : String(error)
              });
            }
          } else {
            fallback = true;
            logger.debug("ReasoningPatternSearch: No services context, using fallback");
          }
          if (fallback) {
            patterns = [];
            usedMock = true;
            logger.debug("ReasoningPatternSearch: Using mock response - vector store not available");
          }
          const filteredPatterns = patterns.filter((pattern) => {
            const rawPayload = pattern.payload || {};
            const qualityScore = rawPayload.evaluation?.qualityScore || rawPayload.qualityScore;
            if (qualityScore !== void 0 && qualityScore < (input.options?.minQualityScore || 0.3)) {
              logger.debug("ReasoningPatternSearch: Filtered out pattern due to low quality score", {
                patternId: pattern.id,
                qualityScore,
                threshold: input.options?.minQualityScore || 0.3
              });
              return false;
            }
            if (env.CIPHER_WORKSPACE_MODE === "shared") {
              const payload = rawPayload || {};
              const hasMatchingUserId = !env.CIPHER_USER_ID || !payload.userId || payload.userId === env.CIPHER_USER_ID;
              const hasMatchingProjectId = !env.CIPHER_PROJECT_NAME || !payload.projectId || payload.projectId === env.CIPHER_PROJECT_NAME;
              const isSharedMode = payload.workspaceMode === "shared";
              const shouldInclude = isSharedMode && (hasMatchingUserId || hasMatchingProjectId) || !payload.userId && !payload.projectId && !payload.workspaceMode;
              logger.debug("ReasoningPatternSearch: Cross-tool sharing filter applied", {
                patternId: pattern.id,
                memoryUserId: payload.userId,
                memoryProjectId: payload.projectId,
                memoryWorkspaceMode: payload.workspaceMode,
                envUserId: env.CIPHER_USER_ID,
                envProjectId: env.CIPHER_PROJECT_NAME,
                envWorkspaceMode: env.CIPHER_WORKSPACE_MODE,
                shouldInclude
              });
              if (!shouldInclude) return false;
            }
            return true;
          }).map((pattern) => {
            const rawPayload = pattern.payload || {};
            return {
              ...pattern,
              context: rawPayload.context || ""
            };
          });
          logger.debug("ReasoningPatternSearch: Search completed", {
            query: input.query,
            totalPatterns: patterns.length,
            filteredPatterns: filteredPatterns.length,
            usedMock,
            fallback
          });
          return {
            success: true,
            result: {
              patterns: filteredPatterns,
              metadata: {
                searchTime: 0,
                // Placeholder for Phase 2
                totalResults: filteredPatterns.length,
                usedMock,
                fallback,
                queryRefinementApplied: finalQueries.length > 1,
                originalQuery: input.query,
                refinedQueries: finalQueries
              }
            },
            metadata: {
              toolName: "search_reasoning_patterns",
              resultsFound: filteredPatterns.length
            }
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("Failed to search reasoning patterns", { error: errorMessage });
          return {
            success: false,
            result: { error: `Search failed: ${errorMessage}` },
            metadata: { toolName: "search_reasoning_patterns", error: errorMessage }
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/workspace_search.ts
var workspaceSearchTool;
var init_workspace_search = __esm({
  "src/core/brain/tools/definitions/memory/workspace_search.ts"() {
    init_cjs_shims();
    init_logger2();
    init_env();
    init_search_memory();
    workspaceSearchTool = {
      name: "workspace_search",
      category: "memory",
      internal: true,
      agentAccessible: true,
      // Agent-accessible: searches workspace memory only
      description: "Search workspace memory for team and project information including progress, bugs, and collaboration context.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "The search query to find relevant workspace memories. Use natural language to describe team activities, project progress, or collaboration context.",
            minLength: 1,
            maxLength: 1e3
          },
          top_k: {
            type: "number",
            description: "Maximum number of results to return (default: 10)",
            minimum: 1,
            maximum: 50,
            default: 10
          },
          similarity_threshold: {
            type: "number",
            description: `Minimum similarity score for results (0.0 to 1.0, default: ${env.WORKSPACE_SEARCH_THRESHOLD})`,
            minimum: 0,
            maximum: 1,
            default: env.WORKSPACE_SEARCH_THRESHOLD
          },
          filters: {
            type: "object",
            description: "Optional filters to narrow search results",
            properties: {
              domain: {
                type: "string",
                description: "Filter by domain (frontend, backend, devops, quality-assurance, design)",
                enum: ["frontend", "backend", "devops", "quality-assurance", "design"]
              },
              team_member: {
                type: "string",
                description: "Filter by team member name or identifier"
              },
              project: {
                type: "string",
                description: "Filter by project name or identifier"
              },
              status: {
                type: "string",
                description: "Filter by progress status",
                enum: ["in-progress", "completed", "blocked", "reviewing"]
              }
            },
            additionalProperties: false
          },
          include_metadata: {
            type: "boolean",
            description: "Whether to include detailed metadata in results (default: true)",
            default: true
          },
          enable_query_refinement: {
            type: "boolean",
            description: "Whether to apply query refinement for better search results (default: false)",
            default: false
          }
        },
        required: ["query"]
      },
      handler: async (args, context) => {
        const startTime = Date.now();
        try {
          if (!env.USE_WORKSPACE_MEMORY) {
            logger.debug("WorkspaceSearch: Workspace memory is disabled, returning empty results");
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                workspaceResults: 0,
                searchMode: "workspace",
                usedFallback: true
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          logger.debug("WorkspaceSearch: Processing workspace memory search request", {
            query: args.query?.substring(0, 100) || "undefined",
            top_k: args.top_k || 10,
            similarity_threshold: args.similarity_threshold || env.WORKSPACE_SEARCH_THRESHOLD,
            filters: args.filters
          });
          if (context?.services?.embeddingManager?.getSessionState()?.isDisabled()) {
            const reason = context.services.embeddingManager.getSessionState().getDisabledReason();
            logger.debug(
              "WorkspaceSearch: Embeddings disabled for this session, returning empty results",
              { reason }
            );
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                workspaceResults: 0,
                searchMode: "workspace",
                usedFallback: true
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (context?.services?.embeddingManager && !context.services.embeddingManager.hasAvailableEmbeddings()) {
            logger.debug("WorkspaceSearch: No available embeddings, returning empty results");
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                workspaceResults: 0,
                searchMode: "workspace",
                usedFallback: true
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (!args.query || typeof args.query !== "string" || args.query.trim().length === 0) {
            throw new Error("Query is required and must be a non-empty string");
          }
          const originalQuery = args.query.trim();
          const topK = Math.max(1, Math.min(50, args.top_k || 10));
          const similarityThreshold = Math.max(
            0,
            Math.min(1, args.similarity_threshold || env.WORKSPACE_SEARCH_THRESHOLD)
          );
          const filters = args.filters || {};
          if (!context?.services) {
            throw new Error("InternalToolContext.services is required for workspace search");
          }
          const embeddingManager = context.services.embeddingManager;
          const vectorStoreManager = context.services.vectorStoreManager;
          if (!embeddingManager || !vectorStoreManager) {
            throw new Error("EmbeddingManager and VectorStoreManager are required in context.services");
          }
          const embedder = embeddingManager.getEmbedder("default");
          if (!embedder?.embed || typeof embedder.embed !== "function") {
            throw new Error("Embedder is not properly initialized or missing embed() method");
          }
          const embeddingStartTime = Date.now();
          logger.debug("WorkspaceSearch: Generating embedding for query", {
            queryLength: originalQuery.length,
            queryPreview: originalQuery.substring(0, 50)
          });
          let queries = [originalQuery];
          const enableQueryRefinement = args.enable_query_refinement === true || env.ENABLE_QUERY_REFINEMENT === true;
          if (enableQueryRefinement) {
            const rewrittenQueries = await rewriteUserQuery(
              originalQuery,
              context?.services?.llmService
            );
            logger.debug("WorkspaceSearch: Rewritten queries", {
              rewrittenQueries
            });
            queries = rewrittenQueries.queries;
          }
          let queryEmbeddings;
          try {
            queryEmbeddings = await embedder?.embedBatch(queries);
          } catch (embedError) {
            logger.error(
              "WorkspaceSearch: Failed to generate embedding, disabling embeddings globally",
              {
                error: embedError instanceof Error ? embedError.message : String(embedError),
                provider: embedder.getConfig().type
              }
            );
            if (context?.services?.embeddingManager && embedError instanceof Error) {
              context.services.embeddingManager.handleRuntimeFailure(
                embedError,
                embedder.getConfig().type
              );
            }
            return {
              success: true,
              query: args.query || "",
              results: [],
              metadata: {
                totalResults: 0,
                searchTime: Date.now() - startTime,
                embeddingTime: 0,
                maxSimilarity: 0,
                minSimilarity: 0,
                averageSimilarity: 0,
                workspaceResults: 0,
                searchMode: "workspace",
                usedFallback: true
              },
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embeddingTime = Date.now() - embeddingStartTime;
          logger.debug("WorkspaceSearch: Embedding generated successfully", {
            embeddingTime: `${embeddingTime}ms`,
            embeddingDimensions: Array.isArray(queryEmbeddings[0]) ? queryEmbeddings[0].length : "unknown"
          });
          const searchStartTime = Date.now();
          let workspaceResults = [];
          let usedFallback = false;
          const workspaceCollectionName = env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory";
          let workspaceStore = null;
          const managerName = vectorStoreManager.constructor.name;
          logger.debug("WorkspaceSearch: Detecting vector manager type", {
            managerName,
            collectionName: workspaceCollectionName,
            hasGetStore: typeof vectorStoreManager.getStore === "function"
          });
          try {
            if (managerName === "MultiCollectionVectorManager" || managerName.includes("Multi")) {
              logger.debug("WorkspaceSearch: Using MultiCollectionVectorManager for workspace search");
              workspaceStore = vectorStoreManager.getStore("workspace");
            } else if (managerName === "DualCollectionVectorManager" || managerName.includes("Dual")) {
              logger.debug(
                "WorkspaceSearch: Using DualCollectionVectorManager - trying workspace store"
              );
              try {
                workspaceStore = vectorStoreManager.getStore("workspace");
              } catch {
                logger.debug(
                  "WorkspaceSearch: DualCollectionVectorManager has no workspace - using knowledge store"
                );
                workspaceStore = vectorStoreManager.getStore("knowledge");
                usedFallback = true;
              }
            } else {
              logger.debug("WorkspaceSearch: Using single collection manager");
              workspaceStore = vectorStoreManager.getNamedStore?.(workspaceCollectionName) || vectorStoreManager.getStore();
              if (!vectorStoreManager.getNamedStore) {
                usedFallback = true;
              }
            }
          } catch (error) {
            logger.debug("WorkspaceSearch: All workspace access methods failed, using default store", {
              error: error instanceof Error ? error.message : String(error)
            });
            workspaceStore = vectorStoreManager.getStore();
            usedFallback = true;
          }
          if (!workspaceStore) {
            throw new Error("Workspace vector store not available");
          }
          for (const queryEmbedding of queryEmbeddings) {
            const results = await workspaceStore.search(queryEmbedding, topK * 2);
            workspaceResults.push(...results);
          }
          const allResults = workspaceResults.map((result2) => ({
            ...result2,
            payload: {
              ...result2.payload,
              source: "workspace",
              memoryType: "workspace"
            }
          }));
          logger.debug("WorkspaceSearch: Workspace collection search completed", {
            resultsFound: workspaceResults.length
          });
          const searchTime = Date.now() - searchStartTime;
          logger.debug("WorkspaceSearch: Workspace search completed", {
            searchTime: `${searchTime}ms`,
            totalResults: allResults.length,
            workspaceResults: workspaceResults.length
          });
          let filteredResults = allResults.filter((result2) => {
            if ((result2.score || 0) < similarityThreshold) return false;
            if (env.CIPHER_WORKSPACE_MODE === "shared") {
              const payload = result2.payload || {};
              const hasMatchingUserId = !env.CIPHER_USER_ID || !payload.userId || payload.userId === env.CIPHER_USER_ID;
              const hasMatchingProjectId = !env.CIPHER_PROJECT_NAME || !payload.projectId || payload.projectId === env.CIPHER_PROJECT_NAME;
              const isSharedMode = payload.workspaceMode === "shared";
              const shouldInclude = isSharedMode && (hasMatchingUserId || hasMatchingProjectId) || !payload.userId && !payload.projectId && !payload.workspaceMode;
              logger.debug("WorkspaceSearch: Cross-tool sharing filter applied", {
                memoryId: result2.id,
                memoryUserId: payload.userId,
                memoryProjectId: payload.projectId,
                memoryWorkspaceMode: payload.workspaceMode,
                envUserId: env.CIPHER_USER_ID,
                envProjectId: env.CIPHER_PROJECT_NAME,
                envWorkspaceMode: env.CIPHER_WORKSPACE_MODE,
                shouldInclude
              });
              return shouldInclude;
            }
            return true;
          }).sort((a, b) => (b.score || 0) - (a.score || 0));
          if (filters.domain) {
            filteredResults = filteredResults.filter(
              (result2) => result2.payload?.domain === filters.domain
            );
          }
          if (filters.team_member) {
            filteredResults = filteredResults.filter(
              (result2) => result2.payload?.teamMember?.toLowerCase().includes(filters.team_member.toLowerCase())
            );
          }
          if (filters.project) {
            filteredResults = filteredResults.filter(
              (result2) => result2.payload?.workContext?.project?.toLowerCase().includes(filters.project.toLowerCase())
            );
          }
          if (filters.status) {
            filteredResults = filteredResults.filter(
              (result2) => result2.payload?.currentProgress?.status === filters.status
            );
          }
          const finalResults = filteredResults.slice(0, topK).map((result2) => {
            const rawPayload = result2.payload || {};
            const payload = rawPayload;
            const baseResult = {
              id: result2.id || payload.id || "unknown",
              text: payload.text || "No content available",
              tags: payload.tags || [],
              timestamp: payload.timestamp || (/* @__PURE__ */ new Date()).toISOString(),
              similarity: result2.score || 0,
              version: payload.version || 2,
              source: "workspace",
              memoryType: "workspace"
            };
            return {
              ...baseResult,
              ...payload.teamMember && { teamMember: payload.teamMember },
              ...payload.currentProgress && { currentProgress: payload.currentProgress },
              ...payload.bugsEncountered && { bugsEncountered: payload.bugsEncountered },
              ...payload.workContext && { workContext: payload.workContext },
              confidence: payload.confidence || 0,
              event: payload.event,
              ...payload.domain && { domain: payload.domain },
              qualitySource: payload.qualitySource,
              ...payload.sourceSessionId && { sourceSessionId: payload.sourceSessionId }
            };
          });
          const totalResults = finalResults.length;
          const similarities = finalResults.map((r) => r.similarity);
          const maxSimilarity = similarities.length > 0 ? Math.max(...similarities) : 0;
          const minSimilarity = similarities.length > 0 ? Math.min(...similarities) : 0;
          const averageSimilarity = similarities.length > 0 ? similarities.reduce((a, b) => a + b, 0) / similarities.length : 0;
          const totalTime = Date.now() - startTime;
          const result = {
            success: true,
            query: originalQuery,
            results: finalResults,
            metadata: {
              totalResults,
              searchTime: totalTime,
              embeddingTime,
              maxSimilarity,
              minSimilarity,
              averageSimilarity,
              workspaceResults: totalResults,
              searchMode: "workspace",
              usedFallback,
              ...Object.keys(filters).length > 0 && { filters }
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
          logger.debug("WorkspaceSearch: Workspace search completed successfully", {
            query: originalQuery.substring(0, 50),
            resultsFound: totalResults,
            workspaceResults: totalResults,
            maxSimilarity: maxSimilarity.toFixed(3),
            averageSimilarity: averageSimilarity.toFixed(3),
            totalTime: `${totalTime}ms`,
            usedFallback,
            appliedFilters: Object.keys(filters)
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          const totalTime = Date.now() - startTime;
          logger.error("WorkspaceSearch: Search failed", {
            error: errorMessage,
            query: args.query?.substring(0, 50) || "undefined",
            processingTime: `${totalTime}ms`
          });
          if (context?.services?.embeddingManager && error instanceof Error) {
            const embeddingManager = context.services.embeddingManager;
            if (embeddingManager && typeof embeddingManager.handleRuntimeFailure === "function") {
              const embedder = embeddingManager.getEmbedder("default");
              const providerType = embedder?.getConfig()?.type || "unknown";
              embeddingManager.handleRuntimeFailure(error, providerType);
            }
          }
          return {
            success: false,
            query: args.query || "undefined",
            results: [],
            metadata: {
              totalResults: 0,
              searchTime: totalTime,
              embeddingTime: 0,
              maxSimilarity: 0,
              minSimilarity: 0,
              averageSimilarity: 0,
              workspaceResults: 0,
              searchMode: "workspace",
              usedFallback: true
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/workspace-payloads.ts
async function createWorkspacePayload(id, text, tags, confidence, event, options) {
  const { env: env3 } = await Promise.resolve().then(() => (init_env(), env_exports));
  return {
    id: typeof id === "string" ? parseInt(id, 10) || 0 : id,
    text,
    tags,
    confidence,
    event,
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    version: 2,
    ...options.teamMember && { teamMember: options.teamMember },
    ...options.currentProgress && { currentProgress: options.currentProgress },
    ...options.bugsEncountered && { bugsEncountered: options.bugsEncountered },
    ...options.workContext && { workContext: options.workContext },
    ...options.domain && { domain: options.domain },
    ...options.sourceSessionId && { sourceSessionId: options.sourceSessionId },
    qualitySource: options.qualitySource,
    // Add cross-tool sharing identifiers (env vars take precedence for security)
    ...(env3.CIPHER_USER_ID || options.userId) && { userId: env3.CIPHER_USER_ID || options.userId },
    ...(env3.CIPHER_PROJECT_NAME || options.projectId) && {
      projectId: env3.CIPHER_PROJECT_NAME || options.projectId
    },
    workspaceMode: env3.CIPHER_WORKSPACE_MODE || options.workspaceMode || "isolated"
  };
}
function mergeWorkspaceInfo(llmProvided = {}, regexExtracted) {
  const result = {};
  result.teamMember = llmProvided.teamMember || regexExtracted.teamMember;
  if (llmProvided.currentProgress || regexExtracted.currentProgress) {
    result.currentProgress = {
      ...regexExtracted.currentProgress,
      ...llmProvided.currentProgress
    };
  }
  result.bugsEncountered = llmProvided.bugsEncountered || regexExtracted.bugsEncountered;
  if (llmProvided.workContext || regexExtracted.workContext) {
    result.workContext = {
      ...regexExtracted.workContext,
      ...llmProvided.workContext
    };
  }
  result.domain = llmProvided.domain || regexExtracted.domain;
  return result;
}
function extractWorkspaceInfo(text) {
  const result = {};
  const teamMemberPatterns = [
    /(?:worked on by|assigned to|developer|dev|team member|teammate)\s+([a-zA-Z]+(?:\s+[a-zA-Z]+)?)/i,
    /(@[a-zA-Z_]+)/,
    // @username mentions
    /([A-Z][a-z]+(?:\s+[A-Z][a-z]+)?)\s+(?:is working on|completed|fixed|implemented)/i
  ];
  for (const pattern of teamMemberPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      result.teamMember = match[1].replace("@", "").trim();
      break;
    }
  }
  const progressPatterns = [
    /(?:working on|implementing|developing|building)\s+([^.!?]+)/i,
    /(?:blocked on|stuck on|waiting on)\s+([^.!?]+)/i,
    /(?:feature|component|module):\s*([^.!?]+)/i,
    /(?:task|ticket|issue):\s*([^.!?]+)/i
  ];
  const statusPatterns = [
    { pattern: /\b(?:completed|done|finished|deployed|released)\b/i, status: "completed" },
    { pattern: /\b(?:blocked|stuck|waiting|pending)\b/i, status: "blocked" },
    { pattern: /\b(?:reviewing|review|testing|qa)\b/i, status: "reviewing" },
    { pattern: /\b(?:in progress|ongoing|working|developing)\b/i, status: "in-progress" }
  ];
  for (const pattern of progressPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      const feature = match[1].trim();
      let status = "in-progress";
      for (const statusPattern of statusPatterns) {
        if (statusPattern.pattern.test(text)) {
          status = statusPattern.status;
          break;
        }
      }
      let completion;
      const completionMatch = text.match(/(\d+)%/);
      if (completionMatch && completionMatch[1]) {
        completion = parseInt(completionMatch[1], 10);
      }
      result.currentProgress = { feature, status, completion };
      break;
    }
  }
  const bugPatterns = [
    /(?:bug|issue|error|problem):\s*([^.!?]+)/i,
    /(?:fixed|resolved|closed)\s+(?:bug|issue|error):\s*([^.!?]+)/i,
    /(?:critical|high|medium|low)\s+(?:priority|severity)\s+(?:bug|issue):\s*([^.!?]+)/i
  ];
  const severityPatterns = [
    { pattern: /\bcritical\b/i, severity: "critical" },
    { pattern: /\bhigh\b/i, severity: "high" },
    { pattern: /\bmedium\b/i, severity: "medium" },
    { pattern: /\blow\b/i, severity: "low" }
  ];
  for (const pattern of bugPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      const description = match[1].trim();
      let severity = "medium";
      for (const severityPattern of severityPatterns) {
        if (severityPattern.pattern.test(text)) {
          severity = severityPattern.severity;
          break;
        }
      }
      let status = "open";
      if (/\b(?:fixed|resolved|closed)\b/i.test(text)) {
        status = "fixed";
      } else if (/\b(?:fixing|working on|investigating)\b/i.test(text)) {
        status = "in-progress";
      }
      result.bugsEncountered = [{ description, severity, status }];
      break;
    }
  }
  const workContext = {};
  const projectPatterns = [
    /(?:project|app|application):\s*([a-zA-Z0-9_-]+)/i,
    /(?:in|for|on)\s+(?:the\s+)?([a-zA-Z0-9_-]+)\s+project/i
  ];
  for (const pattern of projectPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      workContext.project = match[1].trim();
      break;
    }
  }
  const repoPatterns = [
    /(?:repo|repository):\s*([a-zA-Z0-9_/.-]+)/i,
    /(?:github\.com|gitlab\.com)\/([a-zA-Z0-9_/-]+)/i,
    /git\s+clone\s+.*\/([a-zA-Z0-9_-]+)\.git/i
  ];
  for (const pattern of repoPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      workContext.repository = match[1].trim();
      break;
    }
  }
  const branchPatterns = [
    /(?:branch|git checkout)\s+([a-zA-Z0-9_/-]+)/i,
    /(?:on|in)\s+(?:the\s+)?([a-zA-Z0-9_/-]+)\s+branch/i,
    /(?:feature|hotfix|bugfix)\/([a-zA-Z0-9_/-]+)/i
  ];
  for (const pattern of branchPatterns) {
    const match = text.match(pattern);
    if (match && match[1]) {
      workContext.branch = match[1].trim();
      break;
    }
  }
  if (Object.keys(workContext).length > 0) {
    result.workContext = workContext;
  }
  const domainPatterns = [
    {
      pattern: /\b(?:frontend|front-end|ui|ux|react|vue|angular|html|css|javascript|typescript)\b/i,
      domain: "frontend"
    },
    {
      pattern: /\b(?:backend|back-end|server|api|database|sql|node|express|django|flask)\b/i,
      domain: "backend"
    },
    {
      pattern: /\b(?:devops|deployment|docker|kubernetes|ci|cd|pipeline|infrastructure)\b/i,
      domain: "devops"
    },
    {
      pattern: /\b(?:testing|qa|quality|unit test|integration test|e2e)\b/i,
      domain: "quality-assurance"
    },
    { pattern: /\b(?:design|ux|ui|mockup|wireframe|prototype)\b/i, domain: "design" }
  ];
  for (const domainPattern of domainPatterns) {
    if (domainPattern.pattern.test(text)) {
      result.domain = domainPattern.domain;
      break;
    }
  }
  return result;
}
var init_workspace_payloads = __esm({
  "src/core/brain/tools/definitions/memory/workspace-payloads.ts"() {
    init_cjs_shims();
  }
});

// src/core/brain/tools/definitions/memory/workspace_store.ts
function isWorkspaceSignificantContent(content) {
  if (!content || content.trim().length === 0) {
    return false;
  }
  const text = content.toLowerCase().trim();
  const skipPatterns = [
    // Personal information and identity queries
    /\b(my name|user['']?s? name|find my name|search.*name|who am i|what['']?s my name)\b/i,
    /\b(personal|profile|identity|username|login|password|email|address|phone)\b/i,
    // Tool results and system messages
    /^(cipher_.*|workspace_search|workspace_store):\s*(found|completed|no results|error)/i,
    /^(retrieved|result|results|found|matches|search completed|query executed):/i,
    // Simple greetings and social interactions
    /^(user:|assistant:)?\s*(hello|hi|hey|good morning|good afternoon|good evening|thanks|thank you|please|sorry|excuse me|bye|goodbye)\b/i,
    // Generic status messages without context
    /^(task completed|operation successful|processing|loading|waiting|done|finished|ready)\b/i,
    // Simple yes/no responses
    /^(user:|assistant:)?\s*(yes|no|ok|okay|sure|fine|great|good|right|correct|wrong|true|false)\s*[.!?]?\s*$/i
  ];
  for (const pattern of skipPatterns) {
    if (pattern.test(text)) {
      return false;
    }
  }
  const workspacePatterns = [
    // Team and collaboration
    /\b(team|teammate|colleague|collaborat|pair programm|code review|pull request|merge request)\b/i,
    /\b(assigned to|working with|helping|mentoring|onboarding|standup|meeting|discussion)\b/i,
    /\b(project manager|tech lead|senior|junior|intern|developer|engineer|designer|qa)\b/i,
    // Project and work progress
    /\b(project|milestone|deadline|sprint|iteration|epic|story|task|ticket|issue|feature)\b/i,
    /\b(progress|status|update|completion|percentage|completed|finished|deployed|released)\b/i,
    /\b(blocked|blocker|impediment|waiting|pending|reviewing|testing|in progress)\b/i,
    /\b(priority|urgent|critical|high|medium|low|backlog|todo|done|wip)\b/i,
    // Bug tracking and issues
    /\b(bug|error|issue|problem|defect|crash|failure|exception|fix|patch|hotfix)\b/i,
    /\b(severity|critical|high|medium|low|reported|reproduced|investigated|resolved)\b/i,
    // Repository and version control
    /\b(repository|repo|branch|commit|push|pull|merge|git|github|gitlab|bitbucket)\b/i,
    /\b(main|master|develop|feature|hotfix|bugfix|release|version|tag)\b/i,
    // Deployment and environment
    /\b(deploy|deployment|environment|staging|production|dev|test|ci|cd|pipeline)\b/i,
    /\b(build|compile|test|integration|e2e|unit|smoke|regression)\b/i,
    // Technical domains and architecture
    /\b(frontend|backend|fullstack|devops|infrastructure|database|api|microservice)\b/i,
    /\b(architecture|design|pattern|framework|library|component|module|service)\b/i,
    // Work context mentions
    /@[a-zA-Z_]+/,
    // @username mentions
    /\b([A-Z][a-z]+(?:\s+[A-Z][a-z]+)?)\s+(?:is working on|completed|fixed|implemented|assigned)/i
  ];
  for (const pattern of workspacePatterns) {
    if (pattern.test(text)) {
      return true;
    }
  }
  if (/\d+%/.test(text) && /\b(complete|done|progress|finish)\b/i.test(text)) {
    return true;
  }
  if (/\b(today|yesterday|tomorrow|monday|tuesday|wednesday|thursday|friday|weekend)\b/i.test(text) && /\b(deadline|due|meeting|standup|demo|release|deploy)\b/i.test(text)) {
    return true;
  }
  if (text.length < 15) {
    return false;
  }
  const workKeywords = [
    "work",
    "task",
    "project",
    "team",
    "feature",
    "bug",
    "fix",
    "implement",
    "develop",
    "build",
    "deploy",
    "test",
    "review",
    "merge",
    "commit",
    "branch",
    "issue",
    "ticket",
    "milestone",
    "deadline",
    "progress",
    "status",
    "complete",
    "done",
    "blocked",
    "priority",
    "assign",
    "collaborate",
    "pair",
    "help",
    "support",
    "discuss",
    "meeting",
    "standup"
  ];
  const words = text.split(/\s+/);
  const workKeywordCount = words.filter(
    (word) => workKeywords.includes(word.replace(/[^\w]/g, "").toLowerCase())
  ).length;
  const workDensity = workKeywordCount / words.length;
  if (workDensity > 0.15 && words.length > 5) {
    return true;
  }
  return false;
}
function isRetrievedWorkspaceResult(content) {
  if (!content || typeof content !== "string") return false;
  const text = content.toLowerCase();
  const retrievedPatterns = [
    /^(cipher_workspace_search|workspace_search|cipher_workspace_store|workspace_store):/i,
    /^(workspace results|team results|project results|collaboration results):/i,
    /^(team member|current progress|bugs encountered|work context):/i,
    /^(retrieved workspace|workspace search|team search|project search):/i,
    /^(observation:|action:|thought:|conclusion:|reflection:)/i,
    /^(id:|timestamp:|similarity:|source:|memorytype:)/i,
    /^(message: 'workspace query executed'|message: 'workspace search completed')/i,
    /^(workspaceresults:|teamresults:|projectresults:)/i
  ];
  for (const pattern of retrievedPatterns) {
    if (pattern.test(text)) return true;
  }
  return false;
}
function generateSafeWorkspaceId(index) {
  const timestamp = Math.floor(Date.now() / 1e3);
  return timestamp * 1e3 + index % 1e3 + 5e5;
}
function extractWorkspaceTags(content) {
  const tags = /* @__PURE__ */ new Set();
  const text = content.toLowerCase();
  const domainPatterns = [
    {
      pattern: /\b(?:frontend|front-end|ui|ux|react|vue|angular|html|css|javascript|typescript)\b/i,
      tag: "frontend"
    },
    {
      pattern: /\b(?:backend|back-end|server|api|database|sql|node|express|django|flask)\b/i,
      tag: "backend"
    },
    {
      pattern: /\b(?:devops|deployment|docker|kubernetes|ci|cd|pipeline|infrastructure)\b/i,
      tag: "devops"
    },
    { pattern: /\b(?:testing|qa|quality|unit test|integration test|e2e)\b/i, tag: "testing" },
    { pattern: /\b(?:design|ux|ui|mockup|wireframe|prototype)\b/i, tag: "design" }
  ];
  for (const domainPattern of domainPatterns) {
    if (domainPattern.pattern.test(text)) {
      tags.add(domainPattern.tag);
    }
  }
  const statusPatterns = [
    { pattern: /\b(?:completed|done|finished|deployed|released)\b/i, tag: "completed" },
    { pattern: /\b(?:blocked|stuck|waiting|pending)\b/i, tag: "blocked" },
    { pattern: /\b(?:reviewing|review|testing|qa)\b/i, tag: "reviewing" },
    { pattern: /\b(?:in progress|ongoing|working|developing)\b/i, tag: "in-progress" }
  ];
  for (const statusPattern of statusPatterns) {
    if (statusPattern.pattern.test(text)) {
      tags.add(statusPattern.tag);
    }
  }
  const typePatterns = [
    { pattern: /\b(?:bug|error|issue|problem|defect)\b/i, tag: "bug" },
    { pattern: /\b(?:feature|enhancement|improvement)\b/i, tag: "feature" },
    { pattern: /\b(?:task|ticket|story|epic)\b/i, tag: "task" },
    { pattern: /\b(?:team|collaboration|pair|meeting)\b/i, tag: "collaboration" },
    { pattern: /\b(?:project|milestone|deadline|release)\b/i, tag: "project" }
  ];
  for (const typePattern of typePatterns) {
    if (typePattern.pattern.test(text)) {
      tags.add(typePattern.tag);
    }
  }
  tags.add("workspace");
  return Array.from(tags);
}
var workspaceStoreTool;
var init_workspace_store = __esm({
  "src/core/brain/tools/definitions/memory/workspace_store.ts"() {
    init_cjs_shims();
    init_logger2();
    init_workspace_payloads();
    init_env();
    workspaceStoreTool = {
      name: "workspace_store",
      category: "memory",
      internal: true,
      agentAccessible: false,
      // Internal-only: programmatically called after each interaction
      description: "Background tool that automatically stores team-related information including project progress, bugs, and collaboration context.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          interaction: {
            oneOf: [
              {
                type: "string",
                description: "A single user interaction or conversation text to extract workspace information from."
              },
              {
                type: "array",
                items: { type: "string" },
                description: "Multiple user interactions or conversation texts to extract workspace information from."
              }
            ],
            description: "Raw user interaction(s) or conversation text(s) to extract workspace information from."
          },
          workspaceInfo: {
            type: "object",
            description: "Optional LLM-provided structured workspace information (takes precedence over regex extraction)",
            properties: {
              teamMember: {
                type: "string",
                description: "Name/ID of team member"
              },
              currentProgress: {
                type: "object",
                description: "Current feature progress information",
                properties: {
                  feature: { type: "string", description: "Feature being worked on" },
                  status: {
                    type: "string",
                    enum: ["in-progress", "completed", "blocked", "reviewing"],
                    description: "Current status of the feature"
                  },
                  completion: {
                    type: "number",
                    minimum: 0,
                    maximum: 100,
                    description: "Completion percentage (0-100)"
                  }
                },
                required: ["feature", "status"],
                additionalProperties: false
              },
              bugsEncountered: {
                type: "array",
                description: "Array of bug information",
                items: {
                  type: "object",
                  properties: {
                    description: { type: "string", description: "Bug description" },
                    severity: {
                      type: "string",
                      enum: ["low", "medium", "high", "critical"],
                      description: "Bug severity level"
                    },
                    status: {
                      type: "string",
                      enum: ["open", "in-progress", "fixed"],
                      description: "Bug resolution status"
                    }
                  },
                  required: ["description", "severity", "status"],
                  additionalProperties: false
                }
              },
              workContext: {
                type: "object",
                description: "Work context information",
                properties: {
                  project: { type: "string", description: "Project identifier" },
                  repository: { type: "string", description: "Git repository if relevant" },
                  branch: { type: "string", description: "Current working branch" }
                },
                additionalProperties: false
              },
              domain: {
                type: "string",
                description: "Technical domain (e.g., frontend, backend, devops)"
              }
            },
            additionalProperties: false
          },
          context: {
            type: "object",
            description: "Optional context information for workspace operations",
            properties: {
              sessionId: { type: "string", description: "Current session identifier" },
              projectId: {
                type: "string",
                description: "Project identifier for scoped workspace memory"
              },
              userId: { type: "string", description: "User identifier" }
            },
            additionalProperties: false
          },
          options: {
            type: "object",
            description: "Configuration options for workspace memory operations",
            properties: {
              similarityThreshold: {
                type: "number",
                description: "Similarity threshold for workspace memory matching (0.0 to 1.0)",
                minimum: 0,
                maximum: 1,
                default: 0.8
              },
              confidenceThreshold: {
                type: "number",
                description: "Minimum confidence threshold for operations (0.0 to 1.0)",
                minimum: 0,
                maximum: 1,
                default: 0.6
              },
              enableBatchProcessing: {
                type: "boolean",
                description: "Whether to process multiple workspace items in batch",
                default: true
              },
              autoExtractWorkspaceInfo: {
                type: "boolean",
                description: "Whether to automatically extract workspace context",
                default: true
              }
            },
            additionalProperties: false
          }
        },
        required: ["interaction"]
      },
      handler: async (args, context) => {
        try {
          if (!env.USE_WORKSPACE_MEMORY) {
            logger.debug("WorkspaceStore: Workspace memory is disabled, skipping workspace operations");
            return {
              success: true,
              mode: "disabled",
              message: "Workspace memory is disabled",
              extractedFacts: 0,
              workspaceActions: 0,
              skipped: true
            };
          }
          const sessionState = context?.services?.embeddingManager?.getSessionState?.();
          if (sessionState?.isDisabled()) {
            const reason = sessionState.getDisabledReason();
            logger.debug(
              "WorkspaceStore: Embeddings disabled for this session, skipping workspace operations",
              { reason }
            );
            return {
              success: true,
              mode: "chat-only",
              message: `Workspace operations disabled: ${reason}`,
              extractedFacts: 0,
              workspaceActions: 0,
              skipped: true
            };
          }
          if (context?.services?.embeddingManager && !context.services.embeddingManager.hasAvailableEmbeddings()) {
            logger.debug("WorkspaceStore: No available embeddings, skipping workspace operations");
            return {
              success: true,
              mode: "chat-only",
              message: "No available embeddings - operating in chat-only mode",
              extractedFacts: 0,
              workspaceActions: 0,
              skipped: true
            };
          }
          let workspaceContentArray;
          if (!args.interaction) {
            throw new Error("No interaction(s) provided for workspace extraction");
          }
          if (typeof args.interaction === "string") {
            workspaceContentArray = [args.interaction];
          } else if (Array.isArray(args.interaction)) {
            workspaceContentArray = args.interaction;
          } else {
            throw new Error("Interaction must be a string or array of strings");
          }
          const validContent = workspaceContentArray.filter((content) => content && typeof content === "string" && content.trim().length > 0).map((content) => content.trim());
          const significantContent = validContent.filter((content) => {
            if (isRetrievedWorkspaceResult(content)) {
              logger.debug("WorkspaceStore: Skipping retrieved workspace result", {
                contentPreview: content.substring(0, 100) + (content.length > 100 ? "..." : ""),
                reason: "Content is a retrieved result from workspace search tool"
              });
              return false;
            }
            const isSignificant = isWorkspaceSignificantContent(content);
            if (!isSignificant) {
              logger.debug("WorkspaceStore: Skipping non-workspace-significant content", {
                contentPreview: content.substring(0, 100) + (content.length > 100 ? "..." : ""),
                reason: "Does not contain significant workspace or team collaboration information"
              });
            }
            return isSignificant;
          });
          if (significantContent.length === 0) {
            logger.debug("WorkspaceStore: No significant workspace content found after filtering", {
              originalContent: validContent.length,
              filteredContent: significantContent.length,
              interactionLength: args.interaction.length
            });
            return {
              success: true,
              extraction: {
                extracted: 0,
                skipped: workspaceContentArray.length,
                content: []
              },
              workspace: [],
              summary: [],
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const extractionStats = {
            extracted: significantContent.length,
            skipped: workspaceContentArray.length - significantContent.length,
            content: significantContent.map((content, i) => ({
              id: `workspace_content_${Math.floor(Date.now() / 1e3)}_${i}`,
              preview: content.substring(0, 100) + (content.length > 100 ? "..." : ""),
              metadata: {
                hasTeamMention: /@[a-zA-Z_]+/.test(content) || /\b(team|colleague|developer)\b/i.test(content),
                hasProgress: /\b(progress|complete|done|finish|\d+%)\b/i.test(content),
                hasBugInfo: /\b(bug|error|issue|fix|resolve)\b/i.test(content),
                hasProjectInfo: /\b(project|milestone|deadline|feature)\b/i.test(content),
                length: content.length
              }
            }))
          };
          if (!context?.services) {
            logger.warn("WorkspaceStore: No services context available, using basic processing");
            const basicWorkspaceActions = significantContent.map((content, i) => ({
              id: generateSafeWorkspaceId(i),
              text: content,
              event: "ADD",
              tags: extractWorkspaceTags(content),
              confidence: 0.6
            }));
            return {
              success: true,
              extraction: extractionStats,
              workspace: basicWorkspaceActions,
              summary: basicWorkspaceActions.map((action) => ({
                contentPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embeddingManager = context.services.embeddingManager;
          const vectorStoreManager = context.services.vectorStoreManager;
          if (!embeddingManager || !vectorStoreManager) {
            logger.warn(
              "WorkspaceStore: Missing embedding or vector services, falling back to basic processing"
            );
            const basicWorkspaceActions = significantContent.map((content, i) => ({
              id: generateSafeWorkspaceId(i),
              text: content,
              event: "ADD",
              tags: extractWorkspaceTags(content),
              confidence: 0.5,
              reasoning: "Fallback processing due to missing services"
            }));
            return {
              success: true,
              extraction: extractionStats,
              workspace: basicWorkspaceActions,
              summary: basicWorkspaceActions.map((action) => ({
                contentPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                reason: action.reasoning,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const embedder = embeddingManager.getEmbedder("default");
          const workspaceCollectionName = env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory";
          let workspaceStore;
          try {
            logger.debug("WorkspaceStore: Using workspace collection", {
              collectionName: workspaceCollectionName,
              hasWorkspaceSpecificType: !!env.WORKSPACE_VECTOR_STORE_TYPE,
              hasWorkspaceSpecificHost: !!(env.WORKSPACE_VECTOR_STORE_HOST || env.WORKSPACE_VECTOR_STORE_URL)
            });
            workspaceStore = vectorStoreManager.getStore("workspace") || vectorStoreManager.getNamedStore?.(workspaceCollectionName) || vectorStoreManager.getStore();
          } catch (error) {
            logger.debug("WorkspaceStore: Falling back to default store", {
              error: error instanceof Error ? error.message : String(error)
            });
            workspaceStore = vectorStoreManager.getStore();
          }
          if (!embedder || !workspaceStore) {
            logger.warn(
              "WorkspaceStore: Embedder or workspace store not available, using basic processing"
            );
            const basicWorkspaceActions = significantContent.map((content, i) => ({
              id: generateSafeWorkspaceId(i),
              text: content,
              event: "ADD",
              tags: extractWorkspaceTags(content),
              confidence: 0.5,
              reasoning: "Basic processing - embedder/workspace store unavailable"
            }));
            return {
              success: true,
              extraction: extractionStats,
              workspace: basicWorkspaceActions,
              summary: basicWorkspaceActions.map((action) => ({
                contentPreview: action.text.substring(0, 80),
                action: action.event,
                confidence: action.confidence,
                reason: action.reasoning,
                targetId: action.id
              })),
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          if (!embedder.embed || typeof embedder.embed !== "function") {
            throw new Error("Embedder is not properly initialized or missing embed() method");
          }
          const options = {
            similarityThreshold: args.options?.similarityThreshold ?? 0.8,
            confidenceThreshold: args.options?.confidenceThreshold ?? 0.6,
            autoExtractWorkspaceInfo: args.options?.autoExtractWorkspaceInfo ?? true
          };
          const workspaceActions = [];
          const workspaceSummaries = [];
          for (let i = 0; i < significantContent.length; i++) {
            const content = significantContent[i];
            if (!content) {
              logger.warn(`WorkspaceStore: Skipping undefined content at index ${i}`);
              continue;
            }
            logger.debug(`WorkspaceStore: Processing content ${i + 1}/${significantContent.length}`, {
              contentPreview: content.substring(0, 80) + (content.length > 80 ? "..." : ""),
              contentLength: content.length
            });
            try {
              let embedding;
              try {
                embedding = await embedder.embed(content);
              } catch (embedError) {
                logger.error(
                  `WorkspaceStore: Failed to embed content ${i + 1}, disabling embeddings for session`,
                  {
                    error: embedError instanceof Error ? embedError.message : String(embedError),
                    contentPreview: content.substring(0, 50),
                    provider: embedder.getConfig().type
                  }
                );
                if (context?.services?.embeddingManager && embedError instanceof Error) {
                  context.services.embeddingManager.handleRuntimeFailure(
                    embedError,
                    embedder.getConfig().type
                  );
                }
                return {
                  success: true,
                  mode: "chat-only",
                  message: `Embeddings disabled due to failure: ${embedError instanceof Error ? embedError.message : String(embedError)}`,
                  extractedFacts: significantContent.length,
                  workspaceActions: 0,
                  skipped: true,
                  error: embedError instanceof Error ? embedError.message : String(embedError)
                };
              }
              let similar = [];
              try {
                similar = await workspaceStore.search(embedding, 5);
              } catch (searchError) {
                logger.warn(
                  `WorkspaceStore: Failed to search similar workspace memories for content ${i + 1}`,
                  {
                    error: searchError instanceof Error ? searchError.message : String(searchError),
                    contentPreview: content.substring(0, 50)
                  }
                );
              }
              logger.debug(`WorkspaceStore: Similarity search completed for content ${i + 1}`, {
                similarMemoriesFound: similar.length,
                topSimilarity: similar.length > 0 ? similar[0]?.score?.toFixed(3) : "N/A"
              });
              let action = "ADD";
              let targetId = null;
              let reason = "";
              let confidence = options.confidenceThreshold;
              const mostSimilar = similar.length > 0 ? similar[0] : null;
              const similarity = mostSimilar?.score ?? 0;
              if (!mostSimilar || similarity < options.similarityThreshold) {
                action = "ADD";
                reason = "No highly similar workspace memory found; adding as new.";
                confidence = Math.max(options.confidenceThreshold, similarity);
              } else {
                if (content === mostSimilar.payload?.text) {
                  action = "NONE";
                  reason = "Content is redundant; already present in workspace memory.";
                  targetId = mostSimilar.id;
                  confidence = similarity;
                } else if (content.length > (mostSimilar.payload?.text?.length ?? 0)) {
                  action = "UPDATE";
                  targetId = mostSimilar.id;
                  reason = "Content provides more complete workspace information; updating existing memory.";
                  confidence = similarity;
                } else {
                  action = "NONE";
                  reason = "Content is similar but not more complete; ignoring.";
                  targetId = mostSimilar.id;
                  confidence = similarity;
                }
              }
              let workspaceInfo = {};
              if (options.autoExtractWorkspaceInfo) {
                const llmWorkspaceInfo = args.workspaceInfo || {};
                const regexWorkspaceInfo = extractWorkspaceInfo(content);
                workspaceInfo = mergeWorkspaceInfo(llmWorkspaceInfo, regexWorkspaceInfo);
              }
              workspaceActions.push({
                id: action === "ADD" ? generateSafeWorkspaceId(i) : targetId && !isNaN(Number(targetId)) && Number(targetId) > 0 ? Number(targetId) : generateSafeWorkspaceId(i),
                text: content,
                event: action,
                tags: extractWorkspaceTags(content),
                confidence,
                reasoning: reason,
                workspaceInfo
              });
              workspaceSummaries.push({
                contentPreview: content.substring(0, 80),
                action,
                confidence,
                reason,
                targetId,
                workspaceInfo: Object.keys(workspaceInfo).length > 0 ? workspaceInfo : void 0
              });
              logger.debug(`WorkspaceStore: Decision for content ${i + 1}`, {
                contentPreview: content.substring(0, 80) + (content.length > 80 ? "..." : ""),
                decision: action,
                confidence: confidence.toFixed(3),
                reasoning: reason,
                targetMemoryId: targetId,
                decisionMethod: "Heuristic",
                topSimilarityScore: mostSimilar?.score?.toFixed(3) || "N/A",
                similarityThreshold: options.similarityThreshold.toFixed(2),
                extractedWorkspaceInfo: Object.keys(workspaceInfo).length
              });
            } catch (contentError) {
              logger.error(`WorkspaceStore: Failed to process content ${i + 1}`, {
                error: contentError instanceof Error ? contentError.message : String(contentError),
                contentPreview: content.substring(0, 50)
              });
              workspaceActions.push({
                id: generateSafeWorkspaceId(i),
                text: content,
                event: "ADD",
                tags: extractWorkspaceTags(content),
                confidence: 0.4,
                reasoning: `Fallback due to processing error: ${contentError instanceof Error ? contentError.message : String(contentError)}`,
                workspaceInfo: {}
              });
            }
          }
          logger.debug("WorkspaceStore: Starting workspace memory persistence operations", {
            totalActions: workspaceActions.length,
            persistableActions: workspaceActions.filter(
              (a) => ["ADD", "UPDATE", "DELETE"].includes(a.event)
            ).length,
            skippableActions: workspaceActions.filter((a) => a.event === "NONE").length
          });
          let persistedCount = 0;
          for (const action of workspaceActions) {
            if (["ADD", "UPDATE", "DELETE"].includes(action.event)) {
              if (!action.text) {
                logger.warn(`WorkspaceStore: Skipping action with undefined text`, {
                  workspaceId: action.id,
                  event: action.event
                });
                continue;
              }
              try {
                const embedding = await embedder.embed(action.text);
                const qualitySource = "heuristic";
                const options2 = {
                  qualitySource,
                  ...action.workspaceInfo
                };
                if (context?.sessionId) {
                  options2.sourceSessionId = context.sessionId;
                }
                const payload = await createWorkspacePayload(
                  action.id,
                  action.text,
                  action.tags,
                  action.confidence,
                  action.event,
                  options2
                );
                if (action.event === "ADD") {
                  await workspaceStore.insert([embedding], [action.id], [payload]);
                  logger.debug(`WorkspaceStore: ${action.event} operation completed`, {
                    workspaceId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    tags: action.tags,
                    confidence: action.confidence.toFixed(3),
                    workspaceInfo: Object.keys(action.workspaceInfo || {}).length
                  });
                } else if (action.event === "UPDATE") {
                  await workspaceStore.update(action.id, embedding, payload);
                  logger.debug(`WorkspaceStore: ${action.event} operation completed`, {
                    workspaceId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    tags: action.tags,
                    confidence: action.confidence.toFixed(3),
                    workspaceInfo: Object.keys(action.workspaceInfo || {}).length
                  });
                } else if (action.event === "DELETE") {
                  await workspaceStore.delete(action.id);
                  logger.debug(`WorkspaceStore: ${action.event} operation completed`, {
                    workspaceId: action.id,
                    reasoning: action.reasoning
                  });
                }
                persistedCount++;
              } catch (persistError) {
                logger.error(
                  `WorkspaceStore: ${action.event} operation failed, continuing with others`,
                  {
                    workspaceId: action.id,
                    textPreview: action.text.substring(0, 60) + (action.text.length > 60 ? "..." : ""),
                    error: persistError instanceof Error ? persistError.message : String(persistError)
                  }
                );
                if (context?.services?.embeddingManager && persistError instanceof Error) {
                  context.services.embeddingManager.handleRuntimeFailure(
                    persistError,
                    embedder.getConfig().type
                  );
                }
              }
            }
          }
          logger.debug("WorkspaceStore: Workspace memory persistence completed", {
            totalProcessed: workspaceActions.length,
            successfullyPersisted: persistedCount,
            actionsSummary: {
              ADD: workspaceActions.filter((a) => a.event === "ADD").length,
              UPDATE: workspaceActions.filter((a) => a.event === "UPDATE").length,
              DELETE: workspaceActions.filter((a) => a.event === "DELETE").length,
              NONE: workspaceActions.filter((a) => a.event === "NONE").length
            }
          });
          return {
            success: true,
            extraction: extractionStats,
            workspace: workspaceActions,
            summary: workspaceSummaries,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("WorkspaceStore: Critical failure in workspace store", {
            error: errorMessage,
            stack: error instanceof Error ? error.stack : void 0
          });
          return {
            success: false,
            error: errorMessage,
            extraction: {
              extracted: 0,
              skipped: Array.isArray(args.interaction) ? args.interaction.length : 1,
              content: []
            },
            workspace: [],
            summary: [],
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/config/workspace-memory-config.schema.ts
var WorkspaceToolConfigSchema, WorkspaceBehaviorConfigSchema, WorkspaceVectorStoreConfigSchema, WorkspaceEmbeddingConfigSchema, WorkspaceMemoryConfigSchema;
var init_workspace_memory_config_schema = __esm({
  "src/core/config/workspace-memory-config.schema.ts"() {
    init_cjs_shims();
    init_zod();
    WorkspaceToolConfigSchema = external_exports.object({
      // Search tool configuration
      search: external_exports.object({
        name: external_exports.string().default("cipher_workspace_search"),
        description: external_exports.string().default(
          "Search workspace memory for team and project information including progress, bugs, and collaboration context."
        ),
        enabled: external_exports.boolean().default(true),
        similarity_threshold: external_exports.number().min(0).max(1).default(0.7),
        max_results: external_exports.number().positive().default(10),
        timeout_ms: external_exports.number().positive().default(15e3)
      }),
      // Store tool configuration
      store: external_exports.object({
        name: external_exports.string().default("cipher_workspace_store"),
        description: external_exports.string().default(
          "Background tool that automatically stores team-related information including project progress, bugs, and collaboration context."
        ),
        enabled: external_exports.boolean().default(true),
        auto_extraction: external_exports.boolean().default(true),
        confidence_threshold: external_exports.number().min(0).max(1).default(0.6),
        batch_processing: external_exports.boolean().default(true),
        skip_patterns: external_exports.array(external_exports.string()).default(["search_results", "retrieved_content", "tool_output", "system_message"])
      })
    });
    WorkspaceBehaviorConfigSchema = external_exports.object({
      // Search behavior triggers
      search_triggers: external_exports.object({
        keywords: external_exports.array(external_exports.string()).default([
          "team",
          "project",
          "progress",
          "feature",
          "bug",
          "issue",
          "collaborate",
          "working on",
          "assigned to",
          "status",
          "milestone",
          "deadline",
          "repository",
          "branch"
        ]),
        patterns: external_exports.array(external_exports.string()).default([
          "who.*working.*on",
          "what.*status.*of",
          "progress.*on.*feature",
          "bugs.*in.*project",
          "team.*member.*assigned",
          "repository.*branch",
          "current.*milestone"
        ]),
        semantic_triggers: external_exports.array(external_exports.string()).default([
          "asking about team member activities",
          "inquiring about project status",
          "requesting progress updates",
          "looking for bug reports",
          "checking collaboration history"
        ])
      }),
      // Store behavior triggers
      store_triggers: external_exports.object({
        keywords: external_exports.array(external_exports.string()).default([
          "completed",
          "working on",
          "implemented",
          "fixed",
          "bug",
          "issue",
          "feature",
          "milestone",
          "deployed",
          "released",
          "blocked",
          "reviewing",
          "testing",
          "merged",
          "committed"
        ]),
        patterns: external_exports.array(external_exports.string()).default([
          ".*completed.*feature",
          ".*working.*on.*task",
          ".*fixed.*bug",
          ".*implemented.*component",
          ".*deployed.*to.*environment",
          ".*merged.*pull.*request",
          ".*created.*branch",
          ".*assigned.*to.*team"
        ]),
        semantic_triggers: external_exports.array(external_exports.string()).default([
          "reporting progress on tasks",
          "documenting bug fixes",
          "sharing feature implementations",
          "updating project status",
          "recording team assignments"
        ])
      }),
      // Field extraction rules for automatic information parsing
      field_extraction: external_exports.object({
        team_member: external_exports.object({
          patterns: external_exports.array(external_exports.string()).default([
            "(?:worked on by|assigned to|developer|dev|team member)\\s+([a-zA-Z]+(?:\\s+[a-zA-Z]+)?)",
            "(@[a-zA-Z_]+)",
            "([A-Z][a-z]+(?:\\s+[A-Z][a-z]+)?)\\s+(?:is working on|completed|fixed|implemented)"
          ]),
          required: external_exports.boolean().default(false)
        }),
        project_context: external_exports.object({
          patterns: external_exports.array(external_exports.string()).default([
            "(?:project|app|application):\\s*([a-zA-Z0-9_-]+)",
            "(?:in|for|on)\\s+(?:the\\s+)?([a-zA-Z0-9_-]+)\\s+project",
            "(?:repo|repository):\\s*([a-zA-Z0-9_/-]+)",
            "(?:branch|git checkout)\\s+([a-zA-Z0-9_/-]+)"
          ]),
          required: external_exports.boolean().default(false)
        }),
        progress_status: external_exports.object({
          patterns: external_exports.array(external_exports.string()).default([
            "(?:working on|implementing|developing|building)\\s+([^.!?]+)",
            "(?:completed|done|finished|deployed|released)\\b",
            "(?:blocked|stuck|waiting|pending)\\b",
            "(?:reviewing|review|testing|qa)\\b"
          ]),
          required: external_exports.boolean().default(false)
        }),
        bug_tracking: external_exports.object({
          patterns: external_exports.array(external_exports.string()).default([
            "(?:bug|issue|error|problem):\\s*([^.!?]+)",
            "(?:fixed|resolved|closed)\\s+(?:bug|issue|error):\\s*([^.!?]+)",
            "(?:critical|high|medium|low)\\s+(?:priority|severity)"
          ]),
          required: external_exports.boolean().default(false)
        })
      })
    });
    WorkspaceVectorStoreConfigSchema = external_exports.object({
      collection_name: external_exports.string().default("workspace_memory"),
      similarity_threshold: external_exports.number().min(0).max(1).default(0.7),
      max_results: external_exports.number().positive().default(10),
      rerank: external_exports.boolean().default(false),
      time_decay_factor: external_exports.number().min(0).max(1).optional(),
      // Workspace-specific search options
      search_options: external_exports.object({
        include_team_context: external_exports.boolean().default(true),
        include_project_context: external_exports.boolean().default(true),
        include_progress_info: external_exports.boolean().default(true),
        include_bug_reports: external_exports.boolean().default(true),
        filter_by_domain: external_exports.boolean().default(false),
        filter_by_team_member: external_exports.boolean().default(false)
      })
    });
    WorkspaceEmbeddingConfigSchema = external_exports.object({
      model: external_exports.string().default("text-embedding-3-small"),
      dimension: external_exports.number().positive().default(1536),
      batch_size: external_exports.number().positive().default(50),
      timeout_ms: external_exports.number().positive().default(3e4),
      // Workspace-specific preprocessing
      preprocessing: external_exports.object({
        preserve_team_mentions: external_exports.boolean().default(true),
        preserve_project_structure: external_exports.boolean().default(true),
        normalize_progress_terms: external_exports.boolean().default(true),
        extract_technical_context: external_exports.boolean().default(true)
      })
    });
    WorkspaceMemoryConfigSchema = external_exports.object({
      enabled: external_exports.boolean().default(false),
      disable_default_memory: external_exports.boolean().default(false),
      // Tool configurations
      tools: WorkspaceToolConfigSchema,
      // Behavior configurations
      behavior: WorkspaceBehaviorConfigSchema,
      // Vector store configuration
      vector_store: WorkspaceVectorStoreConfigSchema,
      // Embedding configuration
      embedding: WorkspaceEmbeddingConfigSchema,
      // Performance and monitoring
      performance: external_exports.object({
        cache_enabled: external_exports.boolean().default(true),
        cache_ttl_seconds: external_exports.number().positive().default(300),
        batch_operations: external_exports.boolean().default(true),
        async_storage: external_exports.boolean().default(true),
        max_concurrent_operations: external_exports.number().positive().default(3)
      }),
      // Error handling
      error_handling: external_exports.object({
        retry_attempts: external_exports.number().int().min(0).default(3),
        retry_delay_ms: external_exports.number().positive().default(1e3),
        fallback_to_heuristic: external_exports.boolean().default(true),
        log_extraction_failures: external_exports.boolean().default(true)
      })
    });
  }
});

// src/core/brain/tools/definitions/memory/workspace-tools.ts
async function getWorkspaceTools(options = {}) {
  const { embeddingEnabled = true } = options;
  if (env.USE_WORKSPACE_MEMORY !== true) {
    logger.debug("Workspace memory is disabled - excluding all workspace tools");
    return {};
  }
  if (!embeddingEnabled) {
    logger.warn("Embeddings disabled - excluding all embedding-dependent workspace tools", {
      excludedTools: ["cipher_workspace_search", "cipher_workspace_store"]
    });
    return {};
  }
  return {
    cipher_workspace_search: workspaceSearchTool,
    cipher_workspace_store: workspaceStoreTool
  };
}
async function getAllWorkspaceToolDefinitions(options = {}) {
  const { embeddingEnabled = true } = options;
  if (env.USE_WORKSPACE_MEMORY !== true) {
    logger.debug("Workspace memory is disabled - no workspace tool definitions");
    return {};
  }
  if (!embeddingEnabled) {
    return {};
  }
  const tools = {
    workspace_search: workspaceSearchTool,
    workspace_store: workspaceStoreTool
  };
  return tools;
}
function shouldDisableDefaultMemory() {
  return env.USE_WORKSPACE_MEMORY === true && env.DISABLE_DEFAULT_MEMORY === true;
}
function getWorkspaceCollectionName() {
  return env.WORKSPACE_VECTOR_STORE_COLLECTION || "workspace_memory";
}
function validateWorkspaceMemorySetup() {
  const issues = [];
  const warnings = [];
  if (!env.USE_WORKSPACE_MEMORY) {
    warnings.push("Workspace memory is disabled (USE_WORKSPACE_MEMORY=false)");
  }
  if (env.USE_WORKSPACE_MEMORY && !env.DISABLE_DEFAULT_MEMORY) {
    warnings.push(
      "Both workspace and default memory are enabled - consider setting DISABLE_DEFAULT_MEMORY=true for workspace-only mode"
    );
  }
  const collectionName = env.WORKSPACE_VECTOR_STORE_COLLECTION;
  if (!collectionName) {
    warnings.push('WORKSPACE_VECTOR_STORE_COLLECTION not set, using default "workspace_memory"');
  } else if (collectionName === env.VECTOR_STORE_COLLECTION) {
    issues.push(
      "Workspace and default memory collections have the same name - this will cause conflicts"
    );
  }
  if (env.WORKSPACE_VECTOR_STORE_TYPE && env.WORKSPACE_VECTOR_STORE_TYPE !== env.VECTOR_STORE_TYPE) {
    warnings.push(
      `Workspace memory will use ${env.WORKSPACE_VECTOR_STORE_TYPE} while default memory uses ${env.VECTOR_STORE_TYPE}`
    );
  }
  return {
    isValid: issues.length === 0,
    issues,
    warnings
  };
}
function logWorkspaceMemoryStatus() {
  const validation = validateWorkspaceMemorySetup();
  if (env.USE_WORKSPACE_MEMORY) {
    logger.info("Workspace memory is enabled", {
      collection: getWorkspaceCollectionName(),
      disableDefault: env.DISABLE_DEFAULT_MEMORY,
      workspaceVectorStoreType: env.WORKSPACE_VECTOR_STORE_TYPE || "same as default"
    });
    if (validation.warnings.length > 0) {
      logger.warn("Workspace memory configuration warnings", {
        warnings: validation.warnings
      });
    }
    if (validation.issues.length > 0) {
      logger.error("Workspace memory configuration issues", {
        issues: validation.issues
      });
    }
  } else {
    logger.debug("Workspace memory is disabled");
  }
}
var WORKSPACE_TOOL_INFO;
var init_workspace_tools = __esm({
  "src/core/brain/tools/definitions/memory/workspace-tools.ts"() {
    init_cjs_shims();
    init_workspace_search();
    init_workspace_store();
    init_env();
    init_logger2();
    init_workspace_payloads();
    init_workspace_memory_config_schema();
    WORKSPACE_TOOL_INFO = {
      workspace_search: {
        category: "memory",
        purpose: "Search workspace memory for team and project information including progress, bugs, and collaboration context.",
        useCase: "Use when you need to find information about team activities, project status, bug reports, or collaboration history."
      },
      workspace_store: {
        category: "memory",
        purpose: "Background tool that automatically stores team-related information including project progress, bugs, and collaboration context.",
        useCase: "Automatically captures workspace information in the background to build team knowledge and project context."
      }
    };
  }
});

// src/core/brain/tools/definitions/memory/index.ts
var memory_exports = {};
__export(memory_exports, {
  MEMORY_TOOL_INFO: () => MEMORY_TOOL_INFO,
  evaluateReasoning: () => evaluateReasoning,
  extractAndOperateMemoryTool: () => extractAndOperateMemoryTool,
  extractReasoningSteps: () => extractReasoningSteps,
  getAllMemoryToolDefinitions: () => getAllMemoryToolDefinitions,
  getAllWorkspaceToolDefinitions: () => getAllWorkspaceToolDefinitions,
  getMemoryTools: () => getMemoryTools,
  getMemoryToolsArray: () => getMemoryToolsArray,
  getWorkspaceTools: () => getWorkspaceTools,
  logWorkspaceMemoryStatus: () => logWorkspaceMemoryStatus,
  searchMemoryTool: () => searchMemoryTool,
  searchReasoningPatterns: () => searchReasoningPatterns,
  shouldDisableDefaultMemory: () => shouldDisableDefaultMemory,
  storeReasoningMemoryTool: () => storeReasoningMemoryTool
});
async function getMemoryToolsArray(options = {}) {
  const toolMap = await getAllMemoryToolDefinitions(options);
  return Object.values(toolMap);
}
async function getMemoryTools(options = {}) {
  const { embeddingEnabled = true } = options;
  if (!embeddingEnabled) {
    logger.warn("Embeddings disabled - excluding all embedding-dependent memory tools", {
      excludedTools: EMBEDDING_DEPENDENT_TOOLS
    });
    return {};
  }
  const useLazyMemoryTool = env.ENABLE_LAZY_LOADING === "true";
  const extractAndOperateTool = useLazyMemoryTool ? lazyExtractAndOperateMemoryTool : extractAndOperateMemoryTool;
  const isTestEnvironment = process.env.NODE_ENV === "test" || process.env.VITEST === "true";
  const disableDefaultMemory = isTestEnvironment ? false : shouldDisableDefaultMemory();
  const defaultTools = disableDefaultMemory ? {} : {
    // Knowledge tools (always available for functionality)
    cipher_extract_and_operate_memory: extractAndOperateTool,
    cipher_memory_search: searchMemoryTool,
    // Reflection tools (only available if reflection memory is enabled)
    ...env.DISABLE_REFLECTION_MEMORY !== true && {
      cipher_store_reasoning_memory: storeReasoningMemoryTool,
      cipher_extract_reasoning_steps: extractReasoningSteps,
      cipher_evaluate_reasoning: evaluateReasoning,
      cipher_search_reasoning_patterns: searchReasoningPatterns
    }
  };
  const workspaceTools = await getWorkspaceTools(options);
  if (disableDefaultMemory && Object.keys(workspaceTools).length > 0) {
    logger.info("Using workspace-only memory mode", {
      workspaceTools: Object.keys(workspaceTools)
    });
    return workspaceTools;
  }
  return {
    ...defaultTools,
    ...workspaceTools
  };
}
async function getAllMemoryToolDefinitions(options = {}) {
  const { embeddingEnabled = true } = options;
  if (!embeddingEnabled) {
    logger.warn("Embeddings disabled - excluding all embedding-dependent memory tools", {
      excludedTools: EMBEDDING_DEPENDENT_TOOLS
    });
    return {};
  }
  logWorkspaceMemoryStatus();
  const isTestEnvironment = process.env.NODE_ENV === "test" || process.env.VITEST === "true";
  const disableDefaultMemory = isTestEnvironment ? false : shouldDisableDefaultMemory();
  const workspaceToolDefinitions = await getAllWorkspaceToolDefinitions(options);
  if (disableDefaultMemory && Object.keys(workspaceToolDefinitions).length > 0) {
    logger.info("Using workspace-only memory tool definitions", {
      workspaceTools: Object.keys(workspaceToolDefinitions)
    });
    return workspaceToolDefinitions;
  }
  const useLazyMemoryTool = env.ENABLE_LAZY_LOADING === "true";
  const extractAndOperateTool = useLazyMemoryTool ? lazyExtractAndOperateMemoryTool : extractAndOperateMemoryTool;
  const defaultTools = disableDefaultMemory ? {} : {
    // Knowledge tools (always available for functionality)
    cipher_extract_and_operate_memory: extractAndOperateTool,
    cipher_memory_search: searchMemoryTool,
    // Reflection tools (only available if reflection memory is enabled)
    ...env.DISABLE_REFLECTION_MEMORY !== true && {
      cipher_store_reasoning_memory: storeReasoningMemoryTool,
      cipher_extract_reasoning_steps: extractReasoningSteps,
      cipher_evaluate_reasoning: evaluateReasoning,
      cipher_search_reasoning_patterns: searchReasoningPatterns
    }
  };
  return {
    ...defaultTools,
    ...workspaceToolDefinitions
  };
}
var EMBEDDING_DEPENDENT_TOOLS, MEMORY_TOOL_INFO;
var init_memory = __esm({
  "src/core/brain/tools/definitions/memory/index.ts"() {
    init_cjs_shims();
    init_extract_and_operate_memory();
    init_search_memory();
    init_store_reasoning_memory();
    init_lazy_extract_and_operate();
    init_env();
    init_def_reflective_memory_tools();
    init_workspace_tools();
    init_logger2();
    init_workspace_tools();
    EMBEDDING_DEPENDENT_TOOLS = [
      "cipher_extract_and_operate_memory",
      "cipher_memory_search",
      "cipher_store_reasoning_memory",
      "cipher_search_reasoning_patterns",
      "cipher_workspace_search",
      "cipher_workspace_store"
    ];
    MEMORY_TOOL_INFO = {
      extract_and_operate_memory: {
        category: "memory",
        purpose: "Extract knowledge facts from raw interaction(s) and immediately process them to determine memory operations (ADD, UPDATE, DELETE, NONE) in a single atomic step.",
        useCase: "Use when you need to capture important technical information, code patterns, or implementation details and immediately manage memory in a single, atomic operation."
      },
      memory_search: {
        category: "memory",
        purpose: "Perform semantic search over stored memory entries to retrieve relevant knowledge and reasoning traces that can inform current decision-making.",
        useCase: "Use when you need to find previously stored knowledge, code patterns, or technical information that may be relevant to answering current questions or solving problems."
      },
      store_reasoning_memory: {
        category: "memory",
        purpose: "Store reasoning traces and evaluations in reflection memory for future pattern analysis and reuse. Only stores high-quality reasoning and operates in append-only mode.",
        useCase: "Use in background after reasoning is complete to capture successful reasoning patterns for future reference. Only high-quality reasoning is stored."
      },
      // Workspace memory tools
      ...WORKSPACE_TOOL_INFO
      // extract_knowledge: { ... },
      // memory_operation: { ... },
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/add-node.ts
var add_node_exports = {};
__export(add_node_exports, {
  addNodeTool: () => addNodeTool
});
async function addNodeHandler(args, context) {
  try {
    logger.info("AddNode: Processing add node request", {
      nodeId: args.id,
      labels: args.labels,
      hasProperties: !!args.properties,
      propertiesCount: args.properties ? Object.keys(args.properties).length : 0
    });
    if (!args.id || typeof args.id !== "string" || args.id.trim().length === 0) {
      return {
        success: false,
        message: "Node ID must be a non-empty string",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    if (!Array.isArray(args.labels) || args.labels.length === 0) {
      return {
        success: false,
        message: "Node must have at least one label",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const invalidLabels = args.labels.filter(
      (label) => !label || typeof label !== "string" || label.trim().length === 0
    );
    if (invalidLabels.length > 0) {
      return {
        success: false,
        message: "All labels must be non-empty strings",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const kgManager = context?.services?.knowledgeGraphManager;
    if (!kgManager) {
      return {
        success: false,
        message: "KnowledgeGraphManager not available in context.services",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const graph = kgManager.getGraph();
    if (!graph) {
      return {
        success: false,
        message: "Knowledge graph backend is not connected",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const existingNode = await graph.getNode(args.id.trim());
    if (existingNode) {
      logger.warn("AddNode: Attempting to add node that already exists", {
        nodeId: args.id,
        existingLabels: existingNode.labels
      });
      return {
        success: false,
        message: `Node '${args.id}' already exists in the knowledge graph`,
        nodeId: args.id,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    await graph.addNode({
      id: args.id.trim(),
      labels: args.labels.map((label) => label.trim()),
      properties: args.properties || {}
    });
    logger.info("AddNode: Node added successfully", {
      nodeId: args.id,
      labels: args.labels,
      properties: args.properties ? Object.keys(args.properties) : []
    });
    return {
      success: true,
      message: `Node '${args.id}' added to knowledge graph`,
      nodeId: args.id,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("AddNode: Failed to add node", {
      error: errorMessage,
      nodeId: args.id,
      labels: args.labels
    });
    return {
      success: false,
      message: `Failed to add node: ${errorMessage}`,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
}
var addNodeTool;
var init_add_node = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/add-node.ts"() {
    init_cjs_shims();
    init_logger2();
    addNodeTool = {
      name: "add_node",
      description: "Add a node to the knowledge graph with labels and properties",
      category: "knowledge_graph",
      internal: true,
      version: "1.0.0",
      handler: addNodeHandler,
      parameters: {
        type: "object",
        properties: {
          id: {
            type: "string",
            description: "Unique identifier for the node"
          },
          labels: {
            type: "array",
            items: { type: "string" },
            description: 'Array of labels/types for the node (e.g., ["Function", "Code"])',
            minItems: 1
          },
          properties: {
            type: "object",
            description: "Optional properties/attributes for the node",
            additionalProperties: true
          }
        },
        required: ["id", "labels"]
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/add-edge.ts
var add_edge_exports = {};
__export(add_edge_exports, {
  addEdgeTool: () => addEdgeTool
});
async function addEdgeHandler(args, context) {
  try {
    logger.info("AddEdge: Processing add edge request", {
      sourceId: args.sourceId,
      targetId: args.targetId,
      edgeType: args.edgeType,
      hasProperties: !!args.properties,
      propertiesCount: args.properties ? Object.keys(args.properties).length : 0
    });
    if (!args.sourceId || typeof args.sourceId !== "string" || args.sourceId.trim().length === 0) {
      return {
        success: false,
        message: "Source node ID must be a non-empty string",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    if (!args.targetId || typeof args.targetId !== "string" || args.targetId.trim().length === 0) {
      return {
        success: false,
        message: "Target node ID must be a non-empty string",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    if (!args.edgeType || typeof args.edgeType !== "string" || args.edgeType.trim().length === 0) {
      return {
        success: false,
        message: "Edge type must be a non-empty string",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const kgManager = context?.services?.knowledgeGraphManager;
    if (!kgManager) {
      return {
        success: false,
        message: "KnowledgeGraphManager not available in context.services",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const graph = kgManager.getGraph();
    if (!graph) {
      return {
        success: false,
        message: "Knowledge graph backend is not connected",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const sourceNode = await graph.getNode(args.sourceId.trim());
    if (!sourceNode) {
      return {
        success: false,
        message: `Source node '${args.sourceId}' does not exist in the knowledge graph`,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const targetNode = await graph.getNode(args.targetId.trim());
    if (!targetNode) {
      return {
        success: false,
        message: `Target node '${args.targetId}' does not exist in the knowledge graph`,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const edgeId = `edge_${args.sourceId.trim()}_${args.targetId.trim()}_${args.edgeType.trim()}_${Date.now()}`;
    await graph.addEdge({
      id: edgeId,
      type: args.edgeType.trim(),
      startNodeId: args.sourceId.trim(),
      endNodeId: args.targetId.trim(),
      properties: args.properties || {}
    });
    logger.info("AddEdge: Edge added successfully", {
      edgeId,
      sourceId: args.sourceId,
      targetId: args.targetId,
      edgeType: args.edgeType,
      properties: args.properties ? Object.keys(args.properties) : []
    });
    return {
      success: true,
      message: `Edge '${args.edgeType}' added between '${args.sourceId}' and '${args.targetId}'`,
      edgeId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("AddEdge: Failed to add edge", {
      error: errorMessage,
      sourceId: args.sourceId,
      targetId: args.targetId,
      edgeType: args.edgeType
    });
    return {
      success: false,
      message: `Failed to add edge: ${errorMessage}`,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
}
var addEdgeTool;
var init_add_edge = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/add-edge.ts"() {
    init_cjs_shims();
    init_logger2();
    addEdgeTool = {
      name: "add_edge",
      description: "Add an edge (relationship) between two nodes in the knowledge graph",
      category: "knowledge_graph",
      internal: true,
      version: "1.0.0",
      handler: addEdgeHandler,
      parameters: {
        type: "object",
        properties: {
          sourceId: {
            type: "string",
            description: "ID of the source node"
          },
          targetId: {
            type: "string",
            description: "ID of the target node"
          },
          edgeType: {
            type: "string",
            description: 'Type/label of the relationship (e.g., "CALLS", "IMPORTS", "DEPENDS_ON")'
          },
          properties: {
            type: "object",
            description: "Optional properties for the edge",
            additionalProperties: true
          }
        },
        required: ["sourceId", "targetId", "edgeType"]
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/search-graph.ts
var search_graph_exports = {};
__export(search_graph_exports, {
  SEARCH_GRAPH_TOOL: () => SEARCH_GRAPH_TOOL,
  searchGraphTool: () => searchGraphTool
});
function buildNodeFilters(properties, textSearch) {
  const filters = {};
  if (properties && Object.keys(properties).length > 0) {
    Object.assign(filters, properties);
  }
  if (textSearch && textSearch.trim().length > 0) {
    filters.name = textSearch.trim();
  }
  return Object.keys(filters).length > 0 ? filters : void 0;
}
function buildEdgeFilters(properties, textSearch) {
  const filters = {};
  if (properties && Object.keys(properties).length > 0) {
    Object.assign(filters, properties);
  }
  if (textSearch && textSearch.trim().length > 0) {
    filters.description = textSearch.trim();
  }
  return Object.keys(filters).length > 0 ? filters : void 0;
}
var SEARCH_GRAPH_TOOL, searchGraphTool;
var init_search_graph = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/search-graph.ts"() {
    init_cjs_shims();
    init_logger2();
    SEARCH_GRAPH_TOOL = {
      type: "function",
      function: {
        name: "search_graph",
        description: "Search for nodes and edges in the knowledge graph with optional filters.",
        parameters: {
          type: "object",
          properties: {
            searchType: {
              type: "string",
              enum: ["nodes", "edges", "both"],
              description: "Type of entities to search for",
              default: "both"
            },
            nodeLabels: {
              type: "array",
              description: "Filter by node labels (only applicable when searching nodes)",
              items: {
                type: "string"
              }
            },
            edgeTypes: {
              type: "array",
              description: "Filter by edge types (only applicable when searching edges)",
              items: {
                type: "string"
              }
            },
            // Legacy parameter for backward compatibility
            labels: {
              type: "array",
              description: "Filter by node labels or edge types (deprecated, use nodeLabels/edgeTypes)",
              items: {
                type: "string"
              }
            },
            properties: {
              type: "object",
              description: "Filter by properties (key-value pairs)",
              additionalProperties: true
            },
            textSearch: {
              type: "string",
              description: "Full-text search in properties and names"
            },
            limit: {
              type: "number",
              description: "Maximum number of results to return",
              minimum: 1,
              maximum: 1e3,
              default: 50
            }
          },
          additionalProperties: false
        }
      }
    };
    searchGraphTool = {
      name: "search_graph",
      category: "knowledge_graph",
      internal: true,
      description: "Search for nodes and edges in the knowledge graph with optional filters.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          searchType: {
            type: "string",
            enum: ["nodes", "edges", "both"],
            description: "Type of entities to search for"
          },
          nodeLabels: {
            type: "array",
            description: "Filter by node labels (only applicable when searching nodes)",
            items: {
              type: "string"
            }
          },
          edgeTypes: {
            type: "array",
            description: "Filter by edge types (only applicable when searching edges)",
            items: {
              type: "string"
            }
          },
          // Legacy parameter for backward compatibility
          labels: {
            type: "array",
            description: "Filter by node labels or edge types (deprecated, use nodeLabels/edgeTypes)",
            items: {
              type: "string"
            }
          },
          properties: {
            type: "object",
            description: "Filter by properties (key-value pairs)",
            additionalProperties: true
          },
          textSearch: {
            type: "string",
            description: "Full-text search in properties and names"
          },
          limit: {
            type: "number",
            description: "Maximum number of results to return",
            minimum: 1,
            maximum: 1e3
          }
        },
        required: []
      },
      handler: async (args, context) => {
        try {
          logger.debug("SearchGraph: Processing search request", {
            searchType: args.searchType,
            hasNodeLabels: !!args.nodeLabels,
            hasEdgeTypes: !!args.edgeTypes,
            hasLegacyLabels: !!args.labels,
            hasProperties: !!args.properties,
            hasTextSearch: !!args.textSearch
          });
          const searchType = args.searchType || "both";
          const limit = args.limit !== void 0 ? args.limit : 50;
          if (limit < 1 || limit > 1e3) {
            return {
              success: false,
              message: "Limit must be between 1 and 1000",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const kgManager = context?.services?.knowledgeGraphManager;
          if (!kgManager) {
            return {
              success: false,
              message: "KnowledgeGraphManager not available in context.services",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const graph = kgManager.getGraph();
          if (!graph) {
            return {
              success: false,
              message: "Knowledge graph backend is not connected",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const nodeLabelsToUse = args.nodeLabels || (searchType === "nodes" ? args.labels : void 0);
          const edgeTypesToUse = args.edgeTypes || (searchType === "edges" ? args.labels : void 0);
          let nodes = [];
          let edges = [];
          const startTime = Date.now();
          if (searchType === "nodes" || searchType === "both") {
            const nodeFilters = buildNodeFilters(args.properties, args.textSearch);
            nodes = await graph.findNodes(nodeFilters, nodeLabelsToUse, limit);
            logger.debug("SearchGraph: Found nodes", {
              count: nodes.length,
              labels: nodeLabelsToUse,
              filters: nodeFilters
            });
          }
          if (searchType === "edges" || searchType === "both") {
            const edgeFilters = buildEdgeFilters(args.properties, args.textSearch);
            if (edgeTypesToUse && edgeTypesToUse.length > 0) {
              const edgeResults = await Promise.all(
                edgeTypesToUse.map(
                  (edgeType) => graph.findEdges(edgeFilters, edgeType, Math.ceil(limit / edgeTypesToUse.length))
                )
              );
              edges = edgeResults.flat().slice(0, limit);
            } else {
              edges = await graph.findEdges(edgeFilters, void 0, limit);
            }
            logger.debug("SearchGraph: Found edges", {
              count: edges.length,
              types: edgeTypesToUse,
              filters: edgeFilters
            });
          }
          const executionTime = Date.now() - startTime;
          return {
            success: true,
            results: {
              nodes,
              edges,
              totalCount: nodes.length + edges.length,
              searchMetadata: {
                searchType,
                appliedFilters: {
                  nodeLabels: nodeLabelsToUse || [],
                  edgeTypes: edgeTypesToUse || [],
                  legacyLabels: args.labels || [],
                  // For backward compatibility tracking
                  properties: args.properties || {},
                  textSearch: args.textSearch
                },
                limit,
                executionTime
              }
            },
            message: `Search completed - found ${nodes.length} nodes and ${edges.length} edges`,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("SearchGraph: Search failed", {
            error: errorMessage,
            searchType: args.searchType,
            limit: args.limit
          });
          return {
            success: false,
            message: `Search failed: ${errorMessage}`,
            results: {
              nodes: [],
              edges: [],
              totalCount: 0
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/get-neighbors.ts
var get_neighbors_exports = {};
__export(get_neighbors_exports, {
  getNeighborsTool: () => getNeighborsTool
});
async function getNeighborsHandler(args, context) {
  try {
    if (!args.nodeId || typeof args.nodeId !== "string") {
      return {
        success: false,
        message: "Node ID must be a non-empty string"
      };
    }
    const direction = args.direction || "both";
    const limit = args.limit || 10;
    const kgManager = context?.services?.knowledgeGraphManager;
    if (!kgManager) {
      return {
        success: false,
        message: "KnowledgeGraphManager not available in context.services"
      };
    }
    const graph = kgManager.getGraph();
    if (!graph) {
      return {
        success: false,
        message: "Knowledge graph backend is not connected"
      };
    }
    const neighbors = await graph.getNeighbors(args.nodeId, direction, args.edgeTypes, limit);
    return {
      success: true,
      message: "Neighbors retrieved",
      neighbors
    };
  } catch (error) {
    return {
      success: false,
      message: `Failed to get neighbors: ${error.message}`
    };
  }
}
var getNeighborsTool;
var init_get_neighbors = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/get-neighbors.ts"() {
    init_cjs_shims();
    getNeighborsTool = {
      name: "get_neighbors",
      description: "Get neighboring nodes in the knowledge graph with optional filtering",
      category: "knowledge_graph",
      internal: true,
      handler: getNeighborsHandler,
      parameters: {
        type: "object",
        properties: {
          nodeId: {
            type: "string",
            description: "ID of the node to get neighbors for"
          },
          direction: {
            type: "string",
            enum: ["in", "out", "both"],
            description: "Direction of relationships to traverse",
            default: "both"
          },
          edgeTypes: {
            type: "array",
            items: { type: "string" },
            description: 'Optional edge types to filter by (e.g., ["DEPENDS_ON", "CALLS"])'
          },
          limit: {
            type: "number",
            description: "Maximum number of neighbors to return",
            minimum: 1,
            maximum: 100,
            default: 10
          }
        },
        required: ["nodeId"]
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/extract-entities.ts
var extract_entities_exports = {};
__export(extract_entities_exports, {
  extractEntitiesTool: () => extractEntitiesTool
});
async function extractEntitiesFromText(text, entityTypes, llmService) {
  const entities = [];
  const timestamp = Date.now();
  if (llmService && text.length > 20) {
    try {
      const entityExtractionPrompt = `
You are an expert entity extraction system. Analyze the following text and extract entities of the specified types.

Entity Types to Extract: ${entityTypes.join(", ")}

Text to Analyze:
"""
${text}
"""

For each entity found, respond with a JSON array containing objects with these fields:
- name: The entity name/identifier
- type: One of the specified entity types
- confidence: Confidence score from 0.0 to 1.0
- context: A brief phrase showing the entity in context
- description: Brief description of what this entity represents

Focus on meaningful entities that represent important concepts, objects, or relationships in the text. Avoid extracting overly generic terms.

Respond ONLY with the JSON array, no other text:`;
      const response = await llmService.generate(entityExtractionPrompt);
      try {
        const cleanResponse = response.replace(/```json|```/g, "").trim();
        const llmEntities = JSON.parse(cleanResponse);
        if (Array.isArray(llmEntities)) {
          llmEntities.forEach((entity, index) => {
            if (entity.name && entity.type && entityTypes.includes(entity.type)) {
              const entityName = String(entity.name);
              const searchIndex = text.toLowerCase().indexOf(entityName.toLowerCase());
              entities.push({
                id: `${entity.type.toLowerCase()}_${entityName.replace(/[^a-zA-Z0-9]/g, "_")}_${timestamp}_${index}`,
                name: entityName,
                type: entity.type,
                confidence: Math.max(0.1, Math.min(1, Number(entity.confidence) || 0.8)),
                start: searchIndex >= 0 ? searchIndex : 0,
                end: searchIndex >= 0 ? searchIndex + entityName.length : entityName.length,
                originalText: entity.context || entityName
              });
            }
          });
        }
      } catch (parseError) {
        logger.debug(
          "ExtractEntities: Failed to parse LLM response, falling back to regex patterns",
          {
            parseError: parseError instanceof Error ? parseError.message : String(parseError),
            response: response.substring(0, 200)
          }
        );
      }
    } catch (llmError) {
      logger.debug("ExtractEntities: LLM extraction failed, falling back to regex patterns", {
        llmError: llmError instanceof Error ? llmError.message : String(llmError)
      });
    }
  }
  if (entities.length === 0) {
    await extractEntitiesWithRegex(text, entityTypes, entities, timestamp);
  }
  const uniqueEntities = entities.filter(
    (entity, index, self) => index === self.findIndex((e) => e.name === entity.name && e.type === entity.type)
  );
  return uniqueEntities;
}
async function extractEntitiesWithRegex(text, entityTypes, entities, timestamp) {
  if (entityTypes.includes("Function")) {
    const functionPatterns = [
      /(?:function|def|fn)\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g,
      /(?:const|let|var)\s+([a-zA-Z_$][a-zA-Z0-9_$]*)\s*=\s*(?:async\s+)?\(/g,
      /([a-zA-Z_$][a-zA-Z0-9_$]*)\s*\([^)]*\)\s*\{/g
    ];
    functionPatterns.forEach((pattern) => {
      let match;
      while ((match = pattern.exec(text)) !== null) {
        const name = match[1];
        if (name && typeof name === "string" && name.length > 1) {
          entities.push({
            id: `function_${name}_${timestamp}_${entities.length}`,
            name,
            type: "Function",
            confidence: 0.85,
            start: match.index,
            end: match.index + match[0].length,
            originalText: match[0]
          });
        }
      }
    });
  }
  if (entityTypes.includes("Class")) {
    const classPattern = /class\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g;
    let match;
    while ((match = classPattern.exec(text)) !== null) {
      const name = match[1];
      if (name && typeof name === "string") {
        entities.push({
          id: `class_${name}_${timestamp}_${entities.length}`,
          name,
          type: "Class",
          confidence: 0.9,
          start: match.index,
          end: match.index + match[0].length,
          originalText: match[0]
        });
      }
    }
  }
  if (entityTypes.includes("File")) {
    const filePattern = /['"`]([^'"`]*\.(?:js|ts|py|java|cpp|c|h|json|md|txt|yml|yaml|xml|css|scss|html))['"]/gi;
    let match;
    while ((match = filePattern.exec(text)) !== null) {
      const path12 = match[1];
      if (path12 && typeof path12 === "string") {
        entities.push({
          id: `file_${path12.replace(/[^a-zA-Z0-9]/g, "_")}_${timestamp}_${entities.length}`,
          name: path12,
          type: "File",
          confidence: 0.95,
          start: match.index,
          end: match.index + match[0].length,
          originalText: match[0]
        });
      }
    }
  }
  if (entityTypes.includes("Technology") || entityTypes.includes("Tool")) {
    const techKeywords = [
      "React",
      "Vue",
      "Angular",
      "Node.js",
      "Python",
      "TypeScript",
      "JavaScript",
      "Java",
      "C++",
      "Docker",
      "Kubernetes",
      "Git",
      "npm",
      "yarn",
      "webpack",
      "babel",
      "MongoDB",
      "PostgreSQL",
      "Redis",
      "AWS",
      "Azure",
      "GCP"
    ];
    techKeywords.forEach((tech) => {
      const regex2 = new RegExp(`\\b${tech}\\b`, "gi");
      let match;
      while ((match = regex2.exec(text)) !== null) {
        entities.push({
          id: `tech_${tech}_${timestamp}_${entities.length}`,
          name: tech,
          type: "Technology",
          confidence: 0.8,
          start: match.index,
          end: match.index + match[0].length,
          originalText: match[0]
        });
      }
    });
  }
  if (entityTypes.includes("Variable")) {
    const varPattern = /(?:const|let|var)\s+([a-zA-Z_$][a-zA-Z0-9_$]*)/g;
    let match;
    while ((match = varPattern.exec(text)) !== null) {
      const name = match[1];
      if (name && typeof name === "string" && name.length > 1 && !["const", "let", "var"].includes(name)) {
        entities.push({
          id: `var_${name}_${timestamp}_${entities.length}`,
          name,
          type: "Variable",
          confidence: 0.7,
          start: match.index,
          end: match.index + match[0].length,
          originalText: match[0]
        });
      }
    }
  }
}
async function generateEntityRelationships(entities, linkTypes, text, llmService) {
  const relationships = [];
  if (llmService && entities.length > 1 && text.length > 50) {
    try {
      const entityList = entities.map((e) => `- ${e.name} (${e.type})`).join("\n");
      const relationshipPrompt = `
You are an expert relationship analyzer. Given the following entities extracted from text, identify meaningful relationships between them.

Entities:
${entityList}

Available Relationship Types: ${linkTypes.join(", ")}

Original Text Context:
"""
${text}
"""

For each meaningful relationship you identify, respond with a JSON array containing objects with these fields:
- sourceEntity: Name of the source entity
- targetEntity: Name of the target entity
- relationshipType: One of the available relationship types that best describes the connection
- confidence: Confidence score from 0.0 to 1.0
- reasoning: Brief explanation of why this relationship exists

Focus on relationships that are clearly evident from the text context. Avoid speculative connections.

Respond ONLY with the JSON array, no other text:`;
      const response = await llmService.generate(relationshipPrompt);
      try {
        const cleanResponse = response.replace(/```json|```/g, "").trim();
        const llmRelationships = JSON.parse(cleanResponse);
        if (Array.isArray(llmRelationships)) {
          llmRelationships.forEach((rel) => {
            if (rel.sourceEntity && rel.targetEntity && rel.relationshipType) {
              const sourceEntity = entities.find((e) => e.name === rel.sourceEntity);
              const targetEntity = entities.find((e) => e.name === rel.targetEntity);
              if (sourceEntity && targetEntity && linkTypes.includes(rel.relationshipType)) {
                const contextStart = Math.max(
                  0,
                  Math.min(sourceEntity.start, targetEntity.start) - 50
                );
                const contextEnd = Math.min(
                  text.length,
                  Math.max(sourceEntity.end, targetEntity.end) + 50
                );
                const context = text.substring(contextStart, contextEnd);
                relationships.push({
                  id: `rel_${sourceEntity.id}_${targetEntity.id}_${Date.now()}_${relationships.length}`,
                  type: rel.relationshipType,
                  startNodeId: sourceEntity.id,
                  endNodeId: targetEntity.id,
                  confidence: Math.max(0.1, Math.min(1, Number(rel.confidence) || 0.7)),
                  context
                });
              }
            }
          });
        }
      } catch (parseError) {
        logger.debug(
          "ExtractEntities: Failed to parse LLM relationship response, falling back to rule-based approach",
          {
            parseError: parseError instanceof Error ? parseError.message : String(parseError),
            response: response.substring(0, 200)
          }
        );
      }
    } catch (llmError) {
      logger.debug(
        "ExtractEntities: LLM relationship analysis failed, falling back to rule-based approach",
        {
          llmError: llmError instanceof Error ? llmError.message : String(llmError)
        }
      );
    }
  }
  if (relationships.length === 0) {
    for (let i = 0; i < entities.length; i++) {
      for (let j = i + 1; j < entities.length; j++) {
        const entity1 = entities[i];
        const entity2 = entities[j];
        if (!entity1 || !entity2) continue;
        const relType = determineRelationshipType(entity1, entity2, linkTypes);
        if (!relType) continue;
        const distance = Math.abs(entity1.start - entity2.start);
        const maxDistance = 500;
        const proximityScore = Math.max(0, 1 - distance / maxDistance);
        const confidence = Math.min(0.9, 0.3 + proximityScore * 0.6);
        const contextStart = Math.max(0, Math.min(entity1.start, entity2.start) - 50);
        const contextEnd = Math.min(text.length, Math.max(entity1.end, entity2.end) + 50);
        const context = text.substring(contextStart, contextEnd);
        relationships.push({
          id: `rel_${entity1.id}_${entity2.id}_${Date.now()}`,
          type: relType,
          startNodeId: entity1.id,
          endNodeId: entity2.id,
          confidence,
          context
        });
      }
    }
  }
  return relationships;
}
function determineRelationshipType(entity1, entity2, linkTypes) {
  const type1 = entity1.type;
  const type2 = entity2.type;
  if (type1 === "Function" && type2 === "Class" || type1 === "Class" && type2 === "Function") {
    return linkTypes.includes("BELONGS_TO") ? "BELONGS_TO" : "MENTIONS";
  }
  if (type1 === "File" && ["Function", "Class", "Variable"].includes(type2) || ["Function", "Class", "Variable"].includes(type1) && type2 === "File") {
    return linkTypes.includes("CONTAINS") ? "CONTAINS" : "MENTIONS";
  }
  if (type1 === "Technology" && ["Function", "Class", "Project"].includes(type2) || ["Function", "Class", "Project"].includes(type1) && type2 === "Technology") {
    return linkTypes.includes("USES") ? "USES" : "MENTIONS";
  }
  if (type1 === "Function" && type2 === "Function") {
    return linkTypes.includes("CALLS") ? "CALLS" : "MENTIONS";
  }
  return linkTypes.includes("MENTIONS") ? "MENTIONS" : linkTypes[0] || "RELATED_TO";
}
var extractEntitiesTool;
var init_extract_entities = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/extract-entities.ts"() {
    init_cjs_shims();
    init_logger2();
    extractEntitiesTool = {
      name: "extract_entities",
      category: "knowledge_graph",
      internal: true,
      description: "Extract entities from text and add them to the knowledge graph.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          text: {
            type: "string",
            description: "Text to extract entities from"
          },
          options: {
            type: "object",
            description: "Extraction options",
            properties: {
              entityTypes: {
                type: "array",
                items: { type: "string" },
                description: 'Entity types to focus on (e.g., ["Person", "Organization"])'
              },
              autoLink: {
                type: "boolean",
                description: "Whether to automatically create relationships between extracted entities",
                default: true
              },
              linkTypes: {
                type: "array",
                items: { type: "string" },
                description: 'Relationship types to create (e.g., ["IMPLEMENTS", "USES"])'
              }
            }
          },
          // Legacy parameters for backward compatibility
          entityTypes: {
            type: "array",
            items: { type: "string" },
            description: "Optional entity types to focus on (deprecated, use options.entityTypes)"
          },
          autoLink: {
            type: "boolean",
            description: "Whether to automatically create relationships (deprecated, use options.autoLink)",
            default: true
          }
        },
        required: ["text"]
      },
      handler: async (args, context) => {
        try {
          logger.info("ExtractEntities: Processing text for entity extraction", {
            textLength: args.text?.length || 0,
            hasOptions: !!args.options
          });
          if (!args.text || typeof args.text !== "string" || args.text.trim().length === 0) {
            throw new Error("Text is required and must be a non-empty string");
          }
          const text = args.text.trim();
          const entityTypes = args.options?.entityTypes || args.entityTypes || [
            "Person",
            "Function",
            "Class",
            "Variable",
            "Project",
            "File",
            "Concept",
            "Technology",
            "Command",
            "Tool"
          ];
          const autoLink = args.options?.autoLink !== void 0 ? args.options.autoLink : args.autoLink !== void 0 ? args.autoLink : true;
          const linkTypes = args.options?.linkTypes || [
            "IMPLEMENTS",
            "USES",
            "DEPENDS_ON",
            "CALLS",
            "CREATES",
            "MODIFIES",
            "MENTIONS",
            "WORKS_WITH"
          ];
          const llmService = context?.services?.llmService;
          const extractedEntities = await extractEntitiesFromText(text, entityTypes, llmService);
          const createdRelationships = [];
          const kgManager = context?.services?.knowledgeGraphManager;
          if (kgManager && extractedEntities.length > 0) {
            const graph = kgManager.getGraph();
            if (graph) {
              for (const entity of extractedEntities) {
                try {
                  await graph.addNode({
                    id: entity.id,
                    labels: [entity.type],
                    properties: {
                      name: entity.name,
                      confidence: entity.confidence,
                      extractedAt: (/* @__PURE__ */ new Date()).toISOString(),
                      originalText: entity.originalText,
                      position: { start: entity.start, end: entity.end }
                    }
                  });
                } catch (error) {
                  logger.debug("ExtractEntities: Failed to add entity node", {
                    entityId: entity.id,
                    error: error instanceof Error ? error.message : String(error)
                  });
                }
              }
              if (autoLink && extractedEntities.length > 1) {
                const relationships = await generateEntityRelationships(
                  extractedEntities,
                  linkTypes,
                  text,
                  llmService
                );
                for (const rel of relationships) {
                  try {
                    await graph.addEdge({
                      id: rel.id,
                      type: rel.type,
                      startNodeId: rel.startNodeId,
                      endNodeId: rel.endNodeId,
                      properties: {
                        confidence: rel.confidence,
                        extractedAt: (/* @__PURE__ */ new Date()).toISOString(),
                        basedOnText: rel.context
                      }
                    });
                    createdRelationships.push(rel);
                  } catch (error) {
                    logger.debug("ExtractEntities: Failed to add relationship edge", {
                      relationshipId: rel.id,
                      error: error instanceof Error ? error.message : String(error)
                    });
                  }
                }
              }
            }
          }
          const extractedCount = extractedEntities.length;
          const linkedCount = createdRelationships.length;
          logger.info("ExtractEntities: Entity extraction completed", {
            extractedCount,
            linkedCount,
            textLength: text.length,
            entityTypes: entityTypes.length
          });
          return {
            success: true,
            extracted: extractedCount,
            linked: linkedCount,
            entities: extractedEntities,
            relationships: createdRelationships,
            message: "Entity extraction completed successfully",
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("ExtractEntities: Failed to extract entities", {
            error: errorMessage,
            textLength: args.text?.length || 0
          });
          return {
            success: false,
            error: errorMessage,
            extracted: 0,
            linked: 0,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/update-node.ts
var update_node_exports = {};
__export(update_node_exports, {
  updateNodeTool: () => updateNodeTool
});
async function updateNodeHandler(args, context) {
  try {
    logger.info("UpdateNode: Processing update request", {
      nodeId: args.nodeId,
      hasProperties: !!args.properties,
      propertiesCount: args.properties ? Object.keys(args.properties).length : 0,
      hasLabels: !!args.labels,
      labelsCount: args.labels ? args.labels.length : 0
    });
    if (!args.nodeId || typeof args.nodeId !== "string" || args.nodeId.trim().length === 0) {
      return {
        success: false,
        message: "Node ID must be a non-empty string",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    if (!args.properties || typeof args.properties !== "object" || Object.keys(args.properties).length === 0) {
      return {
        success: false,
        message: "Properties must be provided as a non-empty object",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    if (args.labels && (!Array.isArray(args.labels) || args.labels.length === 0)) {
      return {
        success: false,
        message: "Labels must be a non-empty array when provided",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const kgManager = context?.services?.knowledgeGraphManager;
    if (!kgManager) {
      return {
        success: false,
        message: "KnowledgeGraphManager not available in context.services",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    const graph = kgManager.getGraph();
    if (!graph) {
      return {
        success: false,
        message: "Knowledge graph backend is not connected",
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      };
    }
    await graph.updateNode(args.nodeId.trim(), args.properties, args.labels);
    logger.info("UpdateNode: Node updated successfully", {
      nodeId: args.nodeId,
      updatedProperties: Object.keys(args.properties),
      updatedLabels: args.labels
    });
    return {
      success: true,
      message: `Node '${args.nodeId}' updated in knowledge graph`,
      nodeId: args.nodeId,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("UpdateNode: Failed to update node", {
      error: errorMessage,
      nodeId: args.nodeId,
      properties: args.properties ? Object.keys(args.properties) : []
    });
    return {
      success: false,
      message: `Failed to update node: ${errorMessage}`,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
  }
}
var updateNodeTool;
var init_update_node = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/update-node.ts"() {
    init_cjs_shims();
    init_logger2();
    updateNodeTool = {
      name: "update_node",
      description: "Update an existing node in the knowledge graph",
      category: "knowledge_graph",
      internal: true,
      version: "1.0.0",
      handler: updateNodeHandler,
      parameters: {
        type: "object",
        properties: {
          nodeId: {
            type: "string",
            description: "ID of the node to update"
          },
          properties: {
            type: "object",
            description: "Properties to update (will be merged with existing properties)",
            additionalProperties: true
          },
          labels: {
            type: "array",
            items: { type: "string" },
            description: "Optional new labels to set for the node"
          }
        },
        required: ["nodeId", "properties"]
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/delete-node.ts
var delete_node_exports = {};
__export(delete_node_exports, {
  deleteNodeTool: () => deleteNodeTool
});
var deleteNodeTool;
var init_delete_node = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/delete-node.ts"() {
    init_cjs_shims();
    init_logger2();
    deleteNodeTool = {
      name: "delete_node",
      category: "knowledge_graph",
      internal: true,
      description: "Delete a node and its relationships from the knowledge graph.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          id: {
            type: "string",
            description: "Node ID to delete"
          }
        },
        required: ["id"]
      },
      handler: async (args, context) => {
        try {
          logger.info("DeleteNode: Processing delete request", {
            nodeId: args.id
          });
          if (!args.id || typeof args.id !== "string" || args.id.trim().length === 0) {
            return {
              success: false,
              message: "Node ID must be a non-empty string",
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const kgManager = context?.services?.knowledgeGraphManager;
          if (!kgManager) {
            return {
              success: false,
              message: "KnowledgeGraphManager not available in context.services",
              nodeId: args.id,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const graph = kgManager.getGraph();
          if (!graph) {
            return {
              success: false,
              message: "Knowledge graph backend is not connected",
              nodeId: args.id,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const existingNode = await graph.getNode(args.id.trim());
          if (!existingNode) {
            logger.warn("DeleteNode: Attempting to delete non-existent node", {
              nodeId: args.id
            });
            return {
              success: true,
              message: `Node '${args.id}' does not exist (already deleted or never existed)`,
              nodeId: args.id,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          await graph.deleteNode(args.id.trim());
          logger.info("DeleteNode: Node deleted successfully", {
            nodeId: args.id,
            nodeLabels: existingNode.labels
          });
          return {
            success: true,
            nodeId: args.id,
            message: "Node deleted from knowledge graph",
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("DeleteNode: Failed to delete node", {
            error: errorMessage,
            nodeId: args.id
          });
          return {
            success: false,
            nodeId: args.id,
            message: `Failed to delete node: ${errorMessage}`,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/query-graph.ts
var query_graph_exports = {};
__export(query_graph_exports, {
  queryGraphTool: () => queryGraphTool
});
var queryGraphTool;
var init_query_graph = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/query-graph.ts"() {
    init_cjs_shims();
    queryGraphTool = {
      name: "query_graph",
      category: "knowledge_graph",
      internal: true,
      description: "Execute custom queries against the knowledge graph.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "Query string (Cypher-like syntax or structured query)"
          },
          queryType: {
            type: "string",
            enum: ["node", "edge", "path", "cypher"],
            description: "Type of query to execute",
            default: "cypher"
          },
          parameters: {
            type: "object",
            description: "Query parameters for parameterized queries",
            additionalProperties: true
          },
          limit: {
            type: "number",
            description: "Maximum number of results to return",
            minimum: 1,
            maximum: 1e3,
            default: 100
          }
        },
        required: ["query"]
      },
      handler: async (args, context) => {
        try {
          const queryType = args.queryType || "cypher";
          const limit = args.limit !== void 0 ? args.limit : 100;
          if (!args.query || typeof args.query !== "string" || args.query.trim().length === 0) {
            throw new Error("Query is required and must be a non-empty string");
          }
          if (limit < 1 || limit > 1e3) {
            return {
              success: false,
              message: "Limit must be between 1 and 1000",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const kgManager = context?.services?.knowledgeGraphManager;
          if (!kgManager) {
            return {
              success: false,
              message: "KnowledgeGraphManager not available in context.services",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const graph = kgManager.getGraph();
          if (!graph) {
            return {
              success: false,
              message: "Knowledge graph backend is not connected",
              results: null,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            };
          }
          const graphQuery = {
            type: queryType,
            parameters: args.parameters || {},
            limit
          };
          if (queryType === "cypher") {
            graphQuery.query = args.query.trim();
          } else {
            const pattern = {};
            if (args.query.includes("labels:")) {
              const labelsMatch = args.query.match(/labels:\s*\[([^\]]+)\]/);
              if (labelsMatch && labelsMatch[1]) {
                pattern.labels = labelsMatch[1].split(",").map((l) => l.trim().replace(/['"]/g, ""));
              }
            }
            if (args.query.includes("type:")) {
              const typeMatch = args.query.match(/type:\s*(?:['"]([^'"]+)['"]|([A-Z_][A-Z0-9_]*))/);
              if (typeMatch) {
                pattern.type = typeMatch[1] || typeMatch[2];
              }
            }
            graphQuery.pattern = pattern;
          }
          const result = await graph.query(graphQuery);
          return {
            success: true,
            results: result,
            query: {
              original: args.query,
              type: queryType,
              parameters: args.parameters || {},
              limit
            },
            message: "Query executed",
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          return {
            success: false,
            error: errorMessage,
            results: {
              nodes: [],
              edges: [],
              metadata: {
                totalCount: 0,
                queryType: args.queryType || "cypher"
              }
            },
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/intelligent-processor.ts
var intelligent_processor_exports = {};
__export(intelligent_processor_exports, {
  intelligentProcessorTool: () => intelligentProcessorTool
});
async function analyzeTextIntent(text, options, _llmService) {
  const analysisPrompt = `
You are an expert natural language processing system that analyzes text to extract entities and relationships for a knowledge graph. Your job is to understand the user's intent and provide structured information about entities and relationships.

Instructions:
1. Determine the primary intent: create (new information), update (modify existing), delete (remove information), or mixed (combination)
2. Extract all entities mentioned with their types and required actions
3. Identify relationships between entities
4. Handle negations and corrections (e.g., "not Long but Nam" means delete Long, add Nam)
5. Provide confidence scores for each extraction

Text to analyze:
"""
${text}
"""

${options.context ? `Additional context: ${options.context}` : ""}
${options.previousContext ? `Previous context: ${options.previousContext}` : ""}

Common entity types to consider:
- Person: Individual people (e.g., Long, Trang, Nam, John)
- Organization: Companies, institutions (e.g., Google, Microsoft)
- Concept: Abstract ideas, things (e.g., ice-cream, software engineering, happiness)
- Location: Places, addresses (e.g., San Francisco, home, office)
- Event: Activities, occasions (e.g., meeting, party, conference)
- Skill: Abilities, expertise (e.g., programming, cooking, singing)
- Object: Physical or digital items (e.g., book, computer, application)

Common relationship types:
- LIKES: Someone likes something
- WORKS_AT: Employment relationship
- LIVES_IN: Residence relationship
- KNOWS: Personal relationship
- HAS_SKILL: Skill possession
- PARTICIPATES_IN: Event participation
- OWNS: Ownership relationship
- RELATED_TO: General relationship

Examples:
- "Long and Trang likes ice-cream" -> Create entities: Long (Person), Trang (Person), ice-cream (Concept). Create relationships: Long LIKES ice-cream, Trang LIKES ice-cream
- "not Long but Nam and Trang" -> Delete: Long. Create: Nam (Person). Keep: Trang (if exists)
- "John works at Google as a software engineer" -> Create: John (Person), Google (Organization), software engineer (Skill). Relationships: John WORKS_AT Google, John HAS_SKILL software engineer

Respond with a JSON object in this format:
{
  "intent": "create" | "update" | "delete" | "mixed",
  "entities": [
    {
      "name": "entity name",
      "type": "entity type",
      "action": "create" | "update" | "delete",
      "confidence": 0.0-1.0,
      "properties": { "optional": "properties" },
      "context": "optional context from text"
    }
  ],
  "relationships": [
    {
      "source": "source entity name",
      "target": "target entity name", 
      "type": "relationship type",
      "action": "create" | "update" | "delete",
      "confidence": 0.0-1.0,
      "properties": { "optional": "properties" }
    }
  ],
  "updateContext": {
    "replacements": [{"from": "old entity", "to": "new entity"}],
    "deletions": ["entity to delete"],
    "additions": ["entity to add"]
  }
}

Respond ONLY with the JSON object, no other text:`;
  try {
    const response = await _llmService.generate(analysisPrompt);
    const cleanResponse = response.replace(/```json|```/g, "").trim();
    const analysis = JSON.parse(cleanResponse);
    return {
      intent: analysis.intent || "create",
      entities: Array.isArray(analysis.entities) ? analysis.entities : [],
      relationships: Array.isArray(analysis.relationships) ? analysis.relationships : [],
      updateContext: analysis.updateContext || void 0
    };
  } catch (error) {
    logger.warn("IntelligentProcessor: Failed to parse LLM analysis, using fallback", {
      error: error instanceof Error ? error.message : String(error)
    });
    return {
      intent: "create",
      entities: extractSimpleEntities(text),
      relationships: []
    };
  }
}
function extractSimpleEntities(text) {
  const entities = [];
  const personPattern = /\b[A-Z][a-z]+\b/g;
  let match;
  while ((match = personPattern.exec(text)) !== null) {
    const name = match[0];
    if (name.length > 1 && !["The", "And", "Or", "But", "Not"].includes(name)) {
      entities.push({
        name,
        type: "Person",
        action: "create",
        confidence: 0.6
      });
    }
  }
  return entities;
}
async function findRelatedEntities(extractedEntities, kgManager) {
  const graph = kgManager.getGraph();
  if (!graph) return [];
  const existingEntities = [];
  for (const entity of extractedEntities) {
    try {
      const exactMatches = await graph.findNodes({ name: entity.name }, [entity.type], 10);
      existingEntities.push(...exactMatches);
      const fuzzyMatches = await graph.findNodes({}, [entity.type], 50);
      const similarEntities = fuzzyMatches.filter(
        (existing) => existing.properties.name && existing.properties.name.toLowerCase().includes(entity.name.toLowerCase()) || entity.name.toLowerCase().includes(existing.properties.name.toLowerCase())
      );
      existingEntities.push(...similarEntities);
    } catch (error) {
      logger.debug("IntelligentProcessor: Error searching for existing entities", {
        entityName: entity.name,
        error: error instanceof Error ? error.message : String(error)
      });
    }
  }
  const uniqueEntities = existingEntities.filter(
    (entity, index, self) => index === self.findIndex((e) => e.id === entity.id)
  );
  return uniqueEntities;
}
async function processEntityOperations(analysis, existingEntities, _options, kgManager, _llmService, result) {
  const graph = kgManager?.getGraph();
  if (!graph) return;
  for (const entityData of analysis.entities) {
    try {
      const existingEntity = existingEntities.find(
        (e) => e.properties.name?.toLowerCase() === entityData.name.toLowerCase() && e.labels.includes(entityData.type)
      );
      if (entityData.action === "delete") {
        if (existingEntity) {
          await graph.deleteNode(existingEntity.id);
          result.analysis.entities.deleted.push({
            id: existingEntity.id,
            name: entityData.name,
            reason: "Explicitly requested for deletion"
          });
        }
      } else if (entityData.action === "create" || !existingEntity) {
        const entityId = `${entityData.type.toLowerCase()}_${entityData.name.replace(/[^a-zA-Z0-9]/g, "_")}_${Date.now()}`;
        const newNode = {
          id: entityId,
          labels: [entityData.type],
          properties: {
            name: entityData.name,
            confidence: entityData.confidence,
            createdAt: (/* @__PURE__ */ new Date()).toISOString(),
            extractedFrom: result.analysis.originalText,
            ...entityData.properties || {}
          }
        };
        await graph.addNode(newNode);
        result.analysis.entities.created.push({
          id: entityId,
          name: entityData.name,
          type: entityData.type,
          confidence: entityData.confidence
        });
      } else if (entityData.action === "update" && existingEntity) {
        const updateProperties = {
          ...entityData.properties,
          updatedAt: (/* @__PURE__ */ new Date()).toISOString(),
          lastExtractedFrom: result.analysis.originalText
        };
        await graph.updateNode(existingEntity.id, updateProperties);
        result.analysis.entities.updated.push({
          id: existingEntity.id,
          name: entityData.name,
          changes: Object.keys(updateProperties)
        });
      }
    } catch (error) {
      logger.error("IntelligentProcessor: Error processing entity operation", {
        entityName: entityData.name,
        action: entityData.action,
        error: error instanceof Error ? error.message : String(error)
      });
      result.analysis.conflicts.push({
        type: "entity_operation_error",
        description: `Failed to ${entityData.action} entity ${entityData.name}: ${error instanceof Error ? error.message : String(error)}`
      });
    }
  }
}
async function processRelationshipOperations(relationships, result, _kgManager, _llmService) {
  const graph = _kgManager?.getGraph();
  if (!graph) return;
  for (const relData of relationships) {
    try {
      const sourceNodes = await graph.findNodes({ name: relData.source }, void 0, 10);
      const targetNodes = await graph.findNodes({ name: relData.target }, void 0, 10);
      if (sourceNodes.length === 0 || targetNodes.length === 0) {
        result.analysis.conflicts.push({
          type: "missing_entity",
          description: `Cannot create relationship ${relData.type} between ${relData.source} and ${relData.target}: one or both entities not found`
        });
        continue;
      }
      const sourceNode = sourceNodes[0];
      const targetNode = targetNodes[0];
      if (relData.action === "create") {
        const relationshipId = `rel_${sourceNode.id}_${targetNode.id}_${relData.type}_${Date.now()}`;
        const newEdge = {
          id: relationshipId,
          type: relData.type,
          startNodeId: sourceNode.id,
          endNodeId: targetNode.id,
          properties: {
            confidence: relData.confidence,
            createdAt: (/* @__PURE__ */ new Date()).toISOString(),
            extractedFrom: result.analysis.originalText,
            ...relData.properties || {}
          }
        };
        await graph.addEdge(newEdge);
        result.analysis.relationships.created.push({
          id: relationshipId,
          type: relData.type,
          from: relData.source,
          to: relData.target,
          confidence: relData.confidence
        });
      }
    } catch (error) {
      logger.error("IntelligentProcessor: Error processing relationship operation", {
        relationship: `${relData.source} ${relData.type} ${relData.target}`,
        action: relData.action,
        error: error instanceof Error ? error.message : String(error)
      });
      result.analysis.conflicts.push({
        type: "relationship_operation_error",
        description: `Failed to ${relData.action} relationship ${relData.source} ${relData.type} ${relData.target}: ${error instanceof Error ? error.message : String(error)}`
      });
    }
  }
}
async function resolveConflicts(conflicts, _options, _kgManager, _llmService) {
  for (const conflict of conflicts) {
    try {
      if (conflict.type === "missing_entity") {
        conflict.resolution = "Entities will be created automatically if needed";
      } else if (conflict.type === "entity_operation_error") {
        conflict.resolution = "Operation skipped, check entity data";
      } else if (conflict.type === "relationship_operation_error") {
        conflict.resolution = "Relationship creation skipped, check entity existence";
      }
    } catch (error) {
      logger.warn("IntelligentProcessor: Error resolving conflict", {
        conflictType: conflict.type,
        error: error instanceof Error ? error.message : String(error)
      });
    }
  }
}
var intelligentProcessorTool;
var init_intelligent_processor = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/intelligent-processor.ts"() {
    init_cjs_shims();
    init_logger2();
    intelligentProcessorTool = {
      name: "intelligent_processor",
      category: "knowledge_graph",
      internal: true,
      description: "Intelligently process natural language to automatically manage entities and relationships in the knowledge graph.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          text: {
            type: "string",
            description: "Natural language text to process"
          },
          options: {
            type: "object",
            description: "Processing configuration options",
            properties: {
              autoResolve: {
                type: "boolean",
                description: "Whether to automatically resolve entity conflicts",
                default: true
              },
              confidenceThreshold: {
                type: "number",
                description: "Confidence threshold for entity matching (0.0-1.0)",
                minimum: 0,
                maximum: 1,
                default: 0.7
              },
              autoCreateRelationships: {
                type: "boolean",
                description: "Whether to create relationships automatically",
                default: true
              },
              context: {
                type: "string",
                description: "Additional context to help with processing"
              },
              previousContext: {
                type: "string",
                description: "Previous conversation context for handling updates"
              }
            }
          }
        },
        required: ["text"]
      },
      handler: async (args, context) => {
        try {
          logger.info("IntelligentProcessor: Processing natural language text", {
            textLength: args.text?.length || 0,
            hasOptions: !!args.options
          });
          if (!args.text || typeof args.text !== "string" || args.text.trim().length === 0) {
            throw new Error("Text is required and must be a non-empty string");
          }
          const text = args.text.trim();
          const options = {
            autoResolve: true,
            confidenceThreshold: 0.7,
            autoCreateRelationships: true,
            ...args.options
          };
          const kgManager = context?.services?.knowledgeGraphManager;
          const llmService = context?.services?.llmService;
          if (!llmService) {
            throw new Error("LLM service is required for intelligent processing");
          }
          const result = {
            success: false,
            message: "",
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            analysis: {
              originalText: text,
              intent: "create",
              entities: {
                created: [],
                updated: [],
                deleted: [],
                merged: []
              },
              relationships: {
                created: [],
                updated: [],
                deleted: []
              },
              conflicts: []
            }
          };
          const analysis = await analyzeTextIntent(text, options, llmService);
          result.analysis.intent = analysis.intent;
          const existingEntities = kgManager ? await findRelatedEntities(analysis.entities, kgManager) : [];
          await processEntityOperations(
            analysis,
            existingEntities,
            options,
            kgManager,
            llmService,
            result
          );
          if (options.autoCreateRelationships && analysis.relationships.length > 0) {
            await processRelationshipOperations(analysis.relationships, result, kgManager, llmService);
          }
          if (result.analysis.conflicts.length > 0 && options.autoResolve) {
            await resolveConflicts(result.analysis.conflicts, options, kgManager, llmService);
          }
          result.success = true;
          result.message = `Intelligent processing completed: ${result.analysis.entities.created.length} entities created, ${result.analysis.entities.updated.length} updated, ${result.analysis.entities.deleted.length} deleted, ${result.analysis.relationships.created.length} relationships created`;
          logger.info("IntelligentProcessor: Processing completed successfully", {
            intent: result.analysis.intent,
            entitiesCreated: result.analysis.entities.created.length,
            entitiesUpdated: result.analysis.entities.updated.length,
            entitiesDeleted: result.analysis.entities.deleted.length,
            relationshipsCreated: result.analysis.relationships.created.length,
            conflicts: result.analysis.conflicts.length
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("IntelligentProcessor: Processing failed", {
            error: errorMessage,
            textLength: args.text?.length || 0
          });
          return {
            success: false,
            message: `Intelligent processing failed: ${errorMessage}`,
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            analysis: {
              originalText: args.text || "",
              intent: "create",
              entities: { created: [], updated: [], deleted: [], merged: [] },
              relationships: { created: [], updated: [], deleted: [] },
              conflicts: []
            },
            error: errorMessage
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/enhanced-search.ts
var enhanced_search_exports = {};
__export(enhanced_search_exports, {
  enhancedSearchTool: () => enhancedSearchTool
});
async function processSearchQuery(query2, options, llmService) {
  if (!options.naturalLanguage || !llmService) {
    return {
      original: query2,
      processed: query2,
      type: "exact",
      confidence: 1
    };
  }
  try {
    const queryProcessingPrompt = `
You are a query processing system for a knowledge graph search. Your job is to understand the user's search intent and convert it into an optimized search query.

Original query: "${query2}"

Analyze the query and provide:
1. The search intent (what is the user looking for?)
2. Key entities or concepts to search for
3. Relationship context if any
4. Optimized search terms
5. Confidence level (0.0-1.0)

Examples:
- "find people who like ice cream" -> entities: ["people", "ice cream"], relationship: "LIKES", optimized: "Person LIKES ice cream"
- "Long's friends" -> entities: ["Long"], relationship: "KNOWS", optimized: "Long KNOWS Person"
- "companies in San Francisco" -> entities: ["companies", "San Francisco"], relationship: "LOCATED_IN", optimized: "Organization LOCATED_IN San Francisco"

Respond with a JSON object:
{
  "intent": "description of search intent",
  "entities": ["entity1", "entity2"],
  "relationships": ["RELATIONSHIP_TYPE"],
  "optimizedQuery": "optimized search terms",
  "searchType": "exact" | "fuzzy" | "semantic" | "natural",
  "confidence": 0.0-1.0
}

Respond ONLY with the JSON object:`;
    const response = await llmService.generate(queryProcessingPrompt);
    const cleanResponse = response.replace(/```json|```/g, "").trim();
    const analysis = JSON.parse(cleanResponse);
    return {
      original: query2,
      processed: analysis.optimizedQuery || query2,
      type: analysis.searchType || "semantic",
      confidence: Math.max(0.1, Math.min(1, analysis.confidence || 0.8))
    };
  } catch (error) {
    logger.debug("EnhancedSearch: Failed to process query with LLM, using fallback", {
      error: error instanceof Error ? error.message : String(error)
    });
    return {
      original: query2,
      processed: query2,
      type: "fuzzy",
      confidence: 0.7
    };
  }
}
async function determineSearchType(processedQuery, _llmService) {
  if (processedQuery.type === "semantic" || processedQuery.type === "natural") {
    return "both";
  }
  const queryLower = processedQuery.processed.toLowerCase();
  const relationshipKeywords = [
    "relationship",
    "connection",
    "relates",
    "connected",
    "links",
    "depends",
    "calls"
  ];
  if (relationshipKeywords.some((keyword) => queryLower.includes(keyword))) {
    return "edges";
  }
  const entityKeywords = ["person", "people", "company", "organization", "concept", "thing"];
  if (entityKeywords.some((keyword) => queryLower.includes(keyword))) {
    return "nodes";
  }
  return "both";
}
async function executeEnhancedSearch(processedQuery, searchType, options, graph, llmService, limit = 50) {
  const nodes = [];
  const edges = [];
  try {
    if (searchType === "nodes" || searchType === "both") {
      const exactNodes = await searchNodesExact(processedQuery.processed, graph, limit / 2);
      exactNodes.forEach((node) => {
        nodes.push({
          ...node,
          score: 1,
          matchReason: "Exact name match"
        });
      });
    }
    if (searchType === "edges" || searchType === "both") {
      const exactEdges = await searchEdgesExact(processedQuery.processed, graph, limit / 2);
      exactEdges.forEach((edge) => {
        edges.push({
          ...edge,
          score: 1,
          matchReason: "Exact type match"
        });
      });
    }
    if (options.fuzzyMatching && nodes.length + edges.length < limit / 2) {
      if (searchType === "nodes" || searchType === "both") {
        const fuzzyNodes = await searchNodesFuzzy(
          processedQuery.processed,
          graph,
          options.fuzzyThreshold || 0.6,
          limit / 2
        );
        fuzzyNodes.forEach((node) => {
          if (!nodes.find((n) => n.id === node.id)) {
            nodes.push({
              ...node,
              score: 0.8,
              matchReason: "Fuzzy name match"
            });
          }
        });
      }
      if (searchType === "edges" || searchType === "both") {
        const fuzzyEdges = await searchEdgesFuzzy(
          processedQuery.processed,
          graph,
          options.fuzzyThreshold || 0.6,
          limit / 2
        );
        fuzzyEdges.forEach((edge) => {
          if (!edges.find((e) => e.id === edge.id)) {
            edges.push({
              ...edge,
              score: 0.8,
              matchReason: "Fuzzy type match"
            });
          }
        });
      }
    }
    if (options.searchProperties && options.searchProperties.length > 0) {
      const propertyResults = await searchByProperties(
        processedQuery.processed,
        options.searchProperties,
        searchType,
        graph,
        limit / 2
      );
      propertyResults.nodes.forEach((node) => {
        if (!nodes.find((n) => n.id === node.id)) {
          nodes.push({
            ...node,
            score: 0.7,
            matchReason: "Property match"
          });
        }
      });
      propertyResults.edges.forEach((edge) => {
        if (!edges.find((e) => e.id === edge.id)) {
          edges.push({
            ...edge,
            score: 0.7,
            matchReason: "Property match"
          });
        }
      });
    }
    nodes.sort((a, b) => (b.score || 0) - (a.score || 0));
    edges.sort((a, b) => (b.score || 0) - (a.score || 0));
    const limitedNodes = nodes.slice(0, limit);
    const limitedEdges = edges.slice(0, limit);
    return {
      nodes: limitedNodes,
      edges: limitedEdges,
      related: [],
      totalCount: limitedNodes.length + limitedEdges.length,
      executionTime: 0
      // Will be set by caller
    };
  } catch (error) {
    logger.error("EnhancedSearch: Error executing enhanced search", {
      error: error instanceof Error ? error.message : String(error)
    });
    return {
      nodes: [],
      edges: [],
      related: [],
      totalCount: 0,
      executionTime: 0
    };
  }
}
async function searchNodesExact(query2, graph, limit) {
  try {
    const exactMatches = await graph.findNodes({ name: query2 }, void 0, limit);
    return exactMatches;
  } catch (error) {
    logger.debug("EnhancedSearch: Error in exact node search", {
      error: error instanceof Error ? error.message : String(error)
    });
    return [];
  }
}
async function searchEdgesExact(query2, graph, limit) {
  try {
    const exactMatches = await graph.findEdges({}, query2, limit);
    return exactMatches;
  } catch (error) {
    logger.debug("EnhancedSearch: Error in exact edge search", {
      error: error instanceof Error ? error.message : String(error)
    });
    return [];
  }
}
async function searchNodesFuzzy(query2, graph, threshold, limit) {
  try {
    const allNodes = await graph.findNodes({}, void 0, limit * 5);
    const queryLower = query2.toLowerCase();
    const fuzzyMatches = allNodes.filter((node) => {
      const name = node.properties.name?.toLowerCase() || "";
      return name.includes(queryLower) || queryLower.includes(name) || calculateSimilarity(name, queryLower) >= threshold;
    });
    return fuzzyMatches.slice(0, limit);
  } catch (error) {
    logger.debug("EnhancedSearch: Error in fuzzy node search", {
      error: error instanceof Error ? error.message : String(error)
    });
    return [];
  }
}
async function searchEdgesFuzzy(query2, graph, threshold, limit) {
  try {
    const allEdges = await graph.findEdges({}, void 0, limit * 5);
    const queryLower = query2.toLowerCase();
    const fuzzyMatches = allEdges.filter((edge) => {
      const type = edge.type.toLowerCase();
      return type.includes(queryLower) || queryLower.includes(type) || calculateSimilarity(type, queryLower) >= threshold;
    });
    return fuzzyMatches.slice(0, limit);
  } catch (error) {
    logger.debug("EnhancedSearch: Error in fuzzy edge search", {
      error: error instanceof Error ? error.message : String(error)
    });
    return [];
  }
}
async function searchByProperties(query2, properties, searchType, graph, limit) {
  const nodes = [];
  const edges = [];
  try {
    for (const property of properties) {
      if (searchType === "nodes" || searchType === "both") {
        const nodeMatches = await graph.findNodes({ [property]: query2 }, void 0, limit);
        nodes.push(...nodeMatches);
      }
      if (searchType === "edges" || searchType === "both") {
        const edgeMatches = await graph.findEdges({ [property]: query2 }, void 0, limit);
        edges.push(...edgeMatches);
      }
    }
  } catch (error) {
    logger.debug("EnhancedSearch: Error in property search", {
      error: error instanceof Error ? error.message : String(error)
    });
  }
  const uniqueNodes = nodes.filter(
    (node, index, self) => index === self.findIndex((n) => n.id === node.id)
  );
  const uniqueEdges = edges.filter(
    (edge, index, self) => index === self.findIndex((e) => e.id === edge.id)
  );
  return {
    nodes: uniqueNodes.slice(0, limit),
    edges: uniqueEdges.slice(0, limit)
  };
}
async function findRelatedEntities2(nodes, depth, graph) {
  const related = [];
  try {
    for (const node of nodes.slice(0, 3)) {
      const neighbors = await graph.getNeighbors(node.id, "both", void 0, 10);
      neighbors.forEach((neighbor) => {
        if (neighbor.node.id !== node.id && !related.find((r) => r.entity.id === neighbor.node.id)) {
          related.push({
            entity: neighbor.node,
            relationship: neighbor.edge,
            distance: 1
          });
        }
      });
    }
  } catch (error) {
    logger.debug("EnhancedSearch: Error finding related entities", {
      error: error instanceof Error ? error.message : String(error)
    });
  }
  return related.slice(0, 10);
}
async function generateSearchSuggestions(query2, graph, llmService) {
  try {
    const suggestionPrompt = `
The user searched for "${query2}" in a knowledge graph but found no results. 
Generate 3-5 alternative search suggestions that might help them find what they're looking for.

Consider:
- Alternative spellings or synonyms
- Broader or more specific terms
- Related concepts
- Common entity types (Person, Organization, Concept, etc.)

Examples:
- If they searched "ice cream", suggest "dessert", "food", "frozen treats"
- If they searched "Google", suggest "technology company", "search engine", "Alphabet"

Respond with a JSON array of suggestion strings:
["suggestion1", "suggestion2", "suggestion3"]

Respond ONLY with the JSON array:`;
    const response = await llmService.generate(suggestionPrompt);
    const cleanResponse = response.replace(/```json|```/g, "").trim();
    const suggestions = JSON.parse(cleanResponse);
    return Array.isArray(suggestions) ? suggestions.slice(0, 5) : [];
  } catch (error) {
    logger.debug("EnhancedSearch: Error generating suggestions", {
      error: error instanceof Error ? error.message : String(error)
    });
    return [];
  }
}
function calculateSimilarity(str1, str2) {
  const longer = str1.length > str2.length ? str1 : str2;
  const shorter = str1.length > str2.length ? str2 : str1;
  if (longer.length === 0) return 1;
  const distance = levenshteinDistance(longer, shorter);
  return (longer.length - distance) / longer.length;
}
function levenshteinDistance(str1, str2) {
  const matrix = Array(str2.length + 1).fill(null).map(() => Array(str1.length + 1).fill(0));
  for (let i = 0; i <= str1.length; i++) {
    matrix[0][i] = i;
  }
  for (let j = 0; j <= str2.length; j++) {
    matrix[j][0] = j;
  }
  for (let j = 1; j <= str2.length; j++) {
    for (let i = 1; i <= str1.length; i++) {
      const indicator = str1[i - 1] === str2[j - 1] ? 0 : 1;
      matrix[j][i] = Math.min(
        matrix[j][i - 1] + 1,
        // deletion
        matrix[j - 1][i] + 1,
        // insertion
        matrix[j - 1][i - 1] + indicator
        // substitution
      );
    }
  }
  return matrix[str2.length][str1.length];
}
var enhancedSearchTool;
var init_enhanced_search = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/enhanced-search.ts"() {
    init_cjs_shims();
    init_logger2();
    enhancedSearchTool = {
      name: "enhanced_search",
      category: "knowledge_graph",
      internal: true,
      description: "Advanced search with semantic capabilities, fuzzy matching, and intelligent query understanding.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          query: {
            type: "string",
            description: "Search query (can be natural language or structured)"
          },
          searchType: {
            type: "string",
            enum: ["nodes", "edges", "both", "auto"],
            description: "Type of entities to search for",
            default: "auto"
          },
          options: {
            type: "object",
            description: "Enhanced search configuration",
            properties: {
              semanticSearch: {
                type: "boolean",
                description: "Enable semantic search using LLM",
                default: true
              },
              fuzzyMatching: {
                type: "boolean",
                description: "Enable fuzzy matching for entity names",
                default: true
              },
              fuzzyThreshold: {
                type: "number",
                description: "Fuzzy match threshold (0.0-1.0)",
                minimum: 0,
                maximum: 1,
                default: 0.6
              },
              includeRelated: {
                type: "boolean",
                description: "Include related entities in results",
                default: true
              },
              relationDepth: {
                type: "number",
                description: "Maximum depth for relationship traversal",
                minimum: 1,
                maximum: 3,
                default: 2
              },
              searchProperties: {
                type: "array",
                items: { type: "string" },
                description: "Properties to search across",
                default: ["name", "description", "content", "title"]
              },
              naturalLanguage: {
                type: "boolean",
                description: "Use natural language query understanding",
                default: true
              }
            }
          },
          limit: {
            type: "number",
            description: "Maximum number of results to return",
            minimum: 1,
            maximum: 1e3,
            default: 50
          }
        },
        required: ["query"]
      },
      handler: async (args, context) => {
        const startTime = Date.now();
        try {
          logger.info("EnhancedSearch: Processing enhanced search query", {
            queryLength: args.query?.length || 0,
            searchType: args.searchType,
            hasOptions: !!args.options
          });
          if (!args.query || typeof args.query !== "string" || args.query.trim().length === 0) {
            throw new Error("Query is required and must be a non-empty string");
          }
          const query2 = args.query.trim();
          const searchType = args.searchType || "auto";
          const limit = args.limit || 50;
          const options = {
            semanticSearch: true,
            fuzzyMatching: true,
            fuzzyThreshold: 0.6,
            includeRelated: true,
            relationDepth: 2,
            searchProperties: ["name", "description", "content", "title"],
            naturalLanguage: true,
            ...args.options
          };
          const kgManager = context?.services?.knowledgeGraphManager;
          const llmService = context?.services?.llmService;
          if (!kgManager) {
            throw new Error("KnowledgeGraphManager not available in context.services");
          }
          const graph = kgManager.getGraph();
          if (!graph) {
            throw new Error("Knowledge graph backend is not connected");
          }
          const result = {
            success: false,
            message: "",
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            query: {
              original: query2,
              processed: query2,
              type: "exact",
              confidence: 1
            },
            results: {
              nodes: [],
              edges: [],
              related: [],
              totalCount: 0,
              executionTime: 0
            }
          };
          const processedQuery = await processSearchQuery(query2, options, llmService);
          result.query = processedQuery;
          const finalSearchType = searchType === "auto" ? await determineSearchType(processedQuery, llmService) : searchType;
          const searchResults = await executeEnhancedSearch(
            processedQuery,
            finalSearchType,
            options,
            graph,
            llmService,
            limit
          );
          result.results = searchResults;
          if (options.includeRelated && searchResults.nodes.length > 0) {
            const relatedEntities = await findRelatedEntities2(
              searchResults.nodes.slice(0, 5),
              // Limit to top 5 for performance
              options.relationDepth || 2,
              graph
            );
            result.results.related = relatedEntities;
          }
          if (searchResults.totalCount === 0 && llmService) {
            result.suggestions = await generateSearchSuggestions(query2, graph, llmService);
          }
          result.results.executionTime = Date.now() - startTime;
          result.results.totalCount = result.results.nodes.length + result.results.edges.length;
          result.success = true;
          result.message = `Enhanced search completed - found ${result.results.nodes.length} nodes, ${result.results.edges.length} edges, ${result.results.related.length} related entities`;
          logger.info("EnhancedSearch: Search completed successfully", {
            queryType: result.query.type,
            queryConfidence: result.query.confidence,
            nodesFound: result.results.nodes.length,
            edgesFound: result.results.edges.length,
            relatedFound: result.results.related.length,
            executionTime: result.results.executionTime
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("EnhancedSearch: Search failed", {
            error: errorMessage,
            queryLength: args.query?.length || 0
          });
          return {
            success: false,
            message: `Enhanced search failed: ${errorMessage}`,
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            query: {
              original: args.query || "",
              processed: args.query || "",
              type: "exact",
              confidence: 0
            },
            results: {
              nodes: [],
              edges: [],
              related: [],
              totalCount: 0,
              executionTime: Date.now() - startTime
            },
            error: errorMessage
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/relationship-manager.ts
var relationship_manager_exports = {};
__export(relationship_manager_exports, {
  relationshipManagerTool: () => relationshipManagerTool
});
async function analyzeRelationshipInstruction(instruction, targets, operation, llmService) {
  if (operation !== "auto") {
    return {
      type: operation,
      description: instruction,
      affectedEntities: 0,
      affectedRelationships: 0,
      confidence: 0.9,
      params: targets
    };
  }
  try {
    const analysisPrompt = `
You are an expert at analyzing relationship management instructions for a knowledge graph. Your job is to understand what operation the user wants to perform and extract the relevant parameters.

Instruction: "${instruction}"

Possible operations:
1. replace_entity: Replace one entity with another in all relationships (e.g., "not Long but Nam", "replace Google with Alphabet")
2. update_relationship: Update properties of specific relationships (e.g., "update John's role to senior engineer")
3. merge_entities: Merge two entities and combine their relationships (e.g., "merge Google and Alphabet")
4. delete_relationships: Remove relationships matching criteria (e.g., "delete all connections to deprecated services")
5. bulk_update: Update multiple relationships at once (e.g., "update all employees' status to active")
6. conditional_update: Update relationships based on conditions (e.g., "promote all junior developers to mid-level")

Analyze the instruction and provide:
1. The operation type
2. Entities involved (source, target, etc.)
3. Relationship types or criteria
4. Confidence level (0.0-1.0)
5. Specific parameters for the operation

Examples:
- "not Long but Nam and Trang" -> replace_entity: replace "Long" with "Nam", keep "Trang"
- "John works at Microsoft now, not Google" -> replace_entity in WORKS_AT relationship
- "merge Google and Alphabet" -> merge_entities: combine all relationships
- "update John's role to senior engineer" -> update_relationship: modify role property

Respond with a JSON object:
{
  "operationType": "replace_entity" | "update_relationship" | "merge_entities" | "delete_relationships" | "bulk_update" | "conditional_update",
  "entities": {
    "source": "source entity name",
    "target": "target entity name", 
    "toReplace": "entity to replace",
    "replacement": "replacement entity"
  },
  "relationships": {
    "type": "relationship type",
    "properties": {"property": "value"}
  },
  "criteria": {
    "conditions": "matching criteria"
  },
  "confidence": 0.0-1.0,
  "description": "human-readable description of the operation"
}

Respond ONLY with the JSON object:`;
    const response = await llmService.generate(analysisPrompt);
    const cleanResponse = response.replace(/```json|```/g, "").trim();
    const analysis = JSON.parse(cleanResponse);
    return {
      type: analysis.operationType || "replace_entity",
      description: analysis.description || instruction,
      affectedEntities: 0,
      // Will be calculated during execution
      affectedRelationships: 0,
      // Will be calculated during execution
      confidence: Math.max(0.1, Math.min(1, analysis.confidence || 0.8)),
      params: {
        entities: analysis.entities || {},
        relationships: analysis.relationships || {},
        criteria: analysis.criteria || {}
      }
    };
  } catch (error) {
    logger.warn("RelationshipManager: Failed to analyze instruction with LLM, using fallback", {
      error: error instanceof Error ? error.message : String(error)
    });
    const instructionLower = instruction.toLowerCase();
    if (instructionLower.includes("not") && instructionLower.includes("but")) {
      return {
        type: "replace_entity",
        description: "Entity replacement based on pattern matching",
        affectedEntities: 0,
        affectedRelationships: 0,
        confidence: 0.6
      };
    } else if (instructionLower.includes("merge")) {
      return {
        type: "merge_entities",
        description: "Entity merge based on pattern matching",
        affectedEntities: 0,
        affectedRelationships: 0,
        confidence: 0.6
      };
    } else if (instructionLower.includes("update") || instructionLower.includes("change")) {
      return {
        type: "update_relationship",
        description: "Relationship update based on pattern matching",
        affectedEntities: 0,
        affectedRelationships: 0,
        confidence: 0.6
      };
    } else if (instructionLower.includes("delete") || instructionLower.includes("remove")) {
      return {
        type: "delete_relationships",
        description: "Relationship deletion based on pattern matching",
        affectedEntities: 0,
        affectedRelationships: 0,
        confidence: 0.6
      };
    }
    return {
      type: "replace_entity",
      description: "Default operation",
      affectedEntities: 0,
      affectedRelationships: 0,
      confidence: 0.5
    };
  }
}
async function executeEntityReplacement(operation, graph, options, result) {
  try {
    const params = operation.params || {};
    const entities = params.entities || {};
    const toReplace = entities.toReplace || entities.source;
    const replacement = entities.replacement || entities.target;
    if (!toReplace || !replacement) {
      throw new Error("Both source and target entities must be specified for replacement");
    }
    const sourceEntities = await graph.findNodes({ name: toReplace }, void 0, 10);
    if (sourceEntities.length === 0) {
      result.conflicts.push({
        type: "entity_not_found",
        description: `Entity to replace "${toReplace}" not found in graph`
      });
      return;
    }
    let replacementEntities = await graph.findNodes({ name: replacement }, void 0, 10);
    let replacementEntity;
    if (replacementEntities.length === 0) {
      const entityId = `person_${replacement.replace(/[^a-zA-Z0-9]/g, "_")}_${Date.now()}`;
      replacementEntity = {
        id: entityId,
        labels: ["Person"],
        // Default type, could be inferred
        properties: {
          name: replacement,
          createdAt: (/* @__PURE__ */ new Date()).toISOString(),
          createdBy: "relationship_manager"
        }
      };
      await graph.addNode(replacementEntity);
      result.changes.entitiesCreated.push({
        id: entityId,
        name: replacement,
        type: "Person"
      });
    } else {
      replacementEntity = replacementEntities[0];
    }
    for (const sourceEntity of sourceEntities) {
      const neighbors = await graph.getNeighbors(sourceEntity.id, "both", void 0, 1e3);
      for (const neighbor of neighbors) {
        const edge = neighbor.edge;
        const newEdgeId = `rel_${edge.startNodeId === sourceEntity.id ? replacementEntity.id : edge.startNodeId}_${edge.endNodeId === sourceEntity.id ? replacementEntity.id : edge.endNodeId}_${edge.type}_${Date.now()}`;
        const newEdge = {
          id: newEdgeId,
          type: edge.type,
          startNodeId: edge.startNodeId === sourceEntity.id ? replacementEntity.id : edge.startNodeId,
          endNodeId: edge.endNodeId === sourceEntity.id ? replacementEntity.id : edge.endNodeId,
          properties: {
            ...edge.properties,
            replacedFrom: sourceEntity.id,
            replacedAt: (/* @__PURE__ */ new Date()).toISOString()
          }
        };
        await graph.addEdge(newEdge);
        result.changes.relationshipsCreated.push({
          id: newEdgeId,
          type: edge.type,
          description: `Replaced relationship from ${sourceEntity.properties.name}`
        });
        await graph.deleteEdge(edge.id);
        result.changes.relationshipsDeleted.push({
          id: edge.id,
          reason: "Entity replacement"
        });
        result.operation.affectedRelationships++;
      }
      await graph.deleteNode(sourceEntity.id);
      result.changes.entitiesDeleted.push({
        id: sourceEntity.id,
        name: sourceEntity.properties.name || "Unknown",
        reason: "Replaced by another entity"
      });
      result.operation.affectedEntities++;
    }
  } catch (error) {
    logger.error("RelationshipManager: Error in entity replacement", {
      error: error instanceof Error ? error.message : String(error)
    });
    result.conflicts.push({
      type: "operation_error",
      description: `Entity replacement failed: ${error instanceof Error ? error.message : String(error)}`
    });
  }
}
async function executeRelationshipUpdate(operation, graph, options, result) {
  result.conflicts.push({
    type: "not_implemented",
    description: "Relationship update operation not yet implemented"
  });
}
async function executeEntityMerge(operation, graph, options, result) {
  result.conflicts.push({
    type: "not_implemented",
    description: "Entity merge operation not yet implemented"
  });
}
async function executeRelationshipDeletion(operation, graph, options, result) {
  result.conflicts.push({
    type: "not_implemented",
    description: "Relationship deletion operation not yet implemented"
  });
}
async function executeBulkUpdate(operation, graph, options, result) {
  result.conflicts.push({
    type: "not_implemented",
    description: "Bulk update operation not yet implemented"
  });
}
async function executeConditionalUpdate(operation, graph, options, result) {
  result.conflicts.push({
    type: "not_implemented",
    description: "Conditional update operation not yet implemented"
  });
}
async function resolveOperationConflicts(conflicts, _graph, _llmService) {
  for (const conflict of conflicts) {
    try {
      if (conflict.type === "entity_not_found") {
        conflict.resolution = "Entity will be created if needed";
      } else if (conflict.type === "operation_error") {
        conflict.resolution = "Operation skipped due to error";
      } else if (conflict.type === "not_implemented") {
        conflict.resolution = "Feature will be implemented in future version";
      }
    } catch (error) {
      logger.warn("RelationshipManager: Error resolving conflict", {
        conflictType: conflict.type,
        error: error instanceof Error ? error.message : String(error)
      });
    }
  }
}
var relationshipManagerTool;
var init_relationship_manager = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/relationship-manager.ts"() {
    init_cjs_shims();
    init_logger2();
    relationshipManagerTool = {
      name: "relationship_manager",
      category: "knowledge_graph",
      internal: true,
      description: "Intelligently manage entity relationships with support for complex operations like entity replacement, merging, and bulk updates.",
      version: "1.0.0",
      parameters: {
        type: "object",
        properties: {
          instruction: {
            type: "string",
            description: "Natural language instruction for the relationship operation"
          },
          operation: {
            type: "string",
            enum: [
              "replace_entity",
              "update_relationship",
              "merge_entities",
              "delete_relationships",
              "bulk_update",
              "conditional_update",
              "auto"
            ],
            description: "Type of relationship operation (auto will determine from instruction)",
            default: "auto"
          },
          targets: {
            type: "object",
            description: "Target entities or relationships for the operation",
            properties: {
              sourceEntity: {
                type: "string",
                description: "Source entity name or ID"
              },
              targetEntity: {
                type: "string",
                description: "Target entity name or ID"
              },
              relationshipType: {
                type: "string",
                description: "Type of relationship to operate on"
              },
              criteria: {
                type: "object",
                description: "Criteria for bulk operations",
                additionalProperties: true
              }
            }
          },
          options: {
            type: "object",
            description: "Operation configuration options",
            properties: {
              preserveHistory: {
                type: "boolean",
                description: "Whether to preserve historical data",
                default: true
              },
              confidenceThreshold: {
                type: "number",
                description: "Confidence threshold for automatic operations",
                minimum: 0,
                maximum: 1,
                default: 0.8
              },
              autoResolveConflicts: {
                type: "boolean",
                description: "Whether to automatically resolve conflicts",
                default: true
              },
              bulkLimit: {
                type: "number",
                description: "Maximum entities to affect in bulk operations",
                minimum: 1,
                maximum: 1e3,
                default: 100
              },
              validateRelationships: {
                type: "boolean",
                description: "Whether to validate relationships before operations",
                default: true
              }
            }
          }
        },
        required: ["instruction"]
      },
      handler: async (args, context) => {
        try {
          logger.info("RelationshipManager: Processing relationship operation", {
            instructionLength: args.instruction?.length || 0,
            operation: args.operation,
            hasTargets: !!args.targets
          });
          if (!args.instruction || typeof args.instruction !== "string" || args.instruction.trim().length === 0) {
            throw new Error("Instruction is required and must be a non-empty string");
          }
          const instruction = args.instruction.trim();
          const operation = args.operation || "auto";
          const options = {
            preserveHistory: true,
            confidenceThreshold: 0.8,
            autoResolveConflicts: true,
            bulkLimit: 100,
            validateRelationships: true,
            ...args.options
          };
          const kgManager = context?.services?.knowledgeGraphManager;
          const llmService = context?.services?.llmService;
          if (!kgManager) {
            throw new Error("KnowledgeGraphManager not available in context.services");
          }
          if (!llmService) {
            throw new Error("LLM service is required for intelligent relationship management");
          }
          const graph = kgManager.getGraph();
          if (!graph) {
            throw new Error("Knowledge graph backend is not connected");
          }
          const result = {
            success: false,
            message: "",
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            operation: {
              type: "replace_entity",
              description: instruction,
              affectedEntities: 0,
              affectedRelationships: 0,
              confidence: 0
            },
            changes: {
              entitiesCreated: [],
              entitiesUpdated: [],
              entitiesDeleted: [],
              entitiesMerged: [],
              relationshipsCreated: [],
              relationshipsUpdated: [],
              relationshipsDeleted: []
            },
            conflicts: []
          };
          const operationAnalysis = await analyzeRelationshipInstruction(
            instruction,
            args.targets,
            operation,
            llmService
          );
          result.operation = operationAnalysis;
          switch (operationAnalysis.type) {
            case "replace_entity":
              await executeEntityReplacement(operationAnalysis, graph, options, result);
              break;
            case "update_relationship":
              await executeRelationshipUpdate(operationAnalysis, graph, options, result);
              break;
            case "merge_entities":
              await executeEntityMerge(operationAnalysis, graph, options, result);
              break;
            case "delete_relationships":
              await executeRelationshipDeletion(operationAnalysis, graph, options, result);
              break;
            case "bulk_update":
              await executeBulkUpdate(operationAnalysis, graph, options, result);
              break;
            case "conditional_update":
              await executeConditionalUpdate(operationAnalysis, graph, options, result);
              break;
            default:
              throw new Error(`Unsupported operation type: ${operationAnalysis.type}`);
          }
          if (result.conflicts.length > 0 && options.autoResolveConflicts) {
            await resolveOperationConflicts(result.conflicts, graph, llmService);
          }
          result.success = true;
          result.message = `Relationship operation completed: ${result.operation.description}. Affected ${result.operation.affectedEntities} entities and ${result.operation.affectedRelationships} relationships.`;
          logger.info("RelationshipManager: Operation completed successfully", {
            operationType: result.operation.type,
            confidence: result.operation.confidence,
            entitiesAffected: result.operation.affectedEntities,
            relationshipsAffected: result.operation.affectedRelationships,
            conflicts: result.conflicts.length
          });
          return result;
        } catch (error) {
          const errorMessage = error instanceof Error ? error.message : String(error);
          logger.error("RelationshipManager: Operation failed", {
            error: errorMessage,
            instructionLength: args.instruction?.length || 0
          });
          return {
            success: false,
            message: `Relationship operation failed: ${errorMessage}`,
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            operation: {
              type: "replace_entity",
              description: args.instruction || "",
              affectedEntities: 0,
              affectedRelationships: 0,
              confidence: 0
            },
            changes: {
              entitiesCreated: [],
              entitiesUpdated: [],
              entitiesDeleted: [],
              entitiesMerged: [],
              relationshipsCreated: [],
              relationshipsUpdated: [],
              relationshipsDeleted: []
            },
            conflicts: [],
            error: errorMessage
          };
        }
      }
    };
  }
});

// src/core/brain/tools/definitions/knowledge_graph/index.ts
var knowledge_graph_exports = {};
__export(knowledge_graph_exports, {
  addEdgeTool: () => addEdgeTool,
  addNodeTool: () => addNodeTool,
  deleteNodeTool: () => deleteNodeTool,
  enhancedSearchTool: () => enhancedSearchTool,
  extractEntitiesTool: () => extractEntitiesTool,
  getKnowledgeGraphTools: () => getKnowledgeGraphTools,
  getNeighborsTool: () => getNeighborsTool,
  intelligentProcessorTool: () => intelligentProcessorTool,
  queryGraphTool: () => queryGraphTool,
  relationshipManagerTool: () => relationshipManagerTool,
  searchGraphTool: () => searchGraphTool,
  updateNodeTool: () => updateNodeTool
});
async function getKnowledgeGraphTools() {
  const [
    { addNodeTool: addNodeTool2 },
    { addEdgeTool: addEdgeTool2 },
    { searchGraphTool: searchGraphTool2 },
    { getNeighborsTool: getNeighborsTool2 },
    { extractEntitiesTool: extractEntitiesTool2 },
    { updateNodeTool: updateNodeTool2 },
    { deleteNodeTool: deleteNodeTool2 },
    { queryGraphTool: queryGraphTool2 },
    { intelligentProcessorTool: intelligentProcessorTool2 },
    { enhancedSearchTool: enhancedSearchTool2 },
    { relationshipManagerTool: relationshipManagerTool2 }
  ] = await Promise.all([
    Promise.resolve().then(() => (init_add_node(), add_node_exports)),
    Promise.resolve().then(() => (init_add_edge(), add_edge_exports)),
    Promise.resolve().then(() => (init_search_graph(), search_graph_exports)),
    Promise.resolve().then(() => (init_get_neighbors(), get_neighbors_exports)),
    Promise.resolve().then(() => (init_extract_entities(), extract_entities_exports)),
    Promise.resolve().then(() => (init_update_node(), update_node_exports)),
    Promise.resolve().then(() => (init_delete_node(), delete_node_exports)),
    Promise.resolve().then(() => (init_query_graph(), query_graph_exports)),
    Promise.resolve().then(() => (init_intelligent_processor(), intelligent_processor_exports)),
    Promise.resolve().then(() => (init_enhanced_search(), enhanced_search_exports)),
    Promise.resolve().then(() => (init_relationship_manager(), relationship_manager_exports))
  ]);
  return {
    [addNodeTool2.name]: addNodeTool2,
    [addEdgeTool2.name]: addEdgeTool2,
    [searchGraphTool2.name]: searchGraphTool2,
    [getNeighborsTool2.name]: getNeighborsTool2,
    [extractEntitiesTool2.name]: extractEntitiesTool2,
    [updateNodeTool2.name]: updateNodeTool2,
    [deleteNodeTool2.name]: deleteNodeTool2,
    [queryGraphTool2.name]: queryGraphTool2,
    [intelligentProcessorTool2.name]: intelligentProcessorTool2,
    [enhancedSearchTool2.name]: enhancedSearchTool2,
    [relationshipManagerTool2.name]: relationshipManagerTool2
  };
}
var init_knowledge_graph = __esm({
  "src/core/brain/tools/definitions/knowledge_graph/index.ts"() {
    init_cjs_shims();
    init_add_node();
    init_add_edge();
    init_search_graph();
    init_get_neighbors();
    init_extract_entities();
    init_update_node();
    init_delete_node();
    init_query_graph();
    init_intelligent_processor();
    init_enhanced_search();
    init_relationship_manager();
  }
});

// src/core/brain/tools/definitions/system/bash.ts
async function executeBashCommand(options) {
  const { command, timeout = 3e4, workingDir, environment, shell = "/bin/bash" } = options;
  logger.debug("Executing bash command", { command, timeout, workingDir });
  const startTime = Date.now();
  return new Promise((resolve3, reject) => {
    const timeoutId = setTimeout(() => {
      reject(new Error(`Command timeout after ${timeout}ms`));
    }, timeout);
    let output = "";
    let error = "";
    const childProcess = (0, import_child_process.spawn)(shell, ["-c", command], {
      cwd: workingDir || process.cwd(),
      env: { ...process.env, ...environment },
      stdio: ["pipe", "pipe", "pipe"]
    });
    childProcess.stdout?.on("data", (data) => {
      output += data.toString();
    });
    childProcess.stderr?.on("data", (data) => {
      error += data.toString();
    });
    childProcess.on("close", (code) => {
      clearTimeout(timeoutId);
      const result = {
        output: output.trim(),
        error: error.trim(),
        exitCode: code || 0,
        command,
        duration: Date.now() - startTime,
        pid: childProcess.pid,
        workingDir: workingDir || process.cwd() || void 0
      };
      logger.debug("Bash command completed", {
        command,
        exitCode: result.exitCode,
        duration: result.duration
      });
      resolve3(result);
    });
    childProcess.on("error", (err) => {
      clearTimeout(timeoutId);
      logger.error("Bash command failed", { command, error: err.message });
      reject(err);
    });
  });
}
var import_child_process, import_events, BashSession, BashSessionManager, bashHandler, bashTool;
var init_bash = __esm({
  "src/core/brain/tools/definitions/system/bash.ts"() {
    init_cjs_shims();
    import_child_process = require("child_process");
    import_events = require("events");
    init_types7();
    init_logger2();
    BashSession = class extends import_events.EventEmitter {
      process = null;
      outputBuffer = "";
      errorBuffer = "";
      isRunning = false;
      currentWorkingDir;
      sessionId;
      constructor(sessionId, workingDir = process.cwd()) {
        super();
        this.sessionId = sessionId;
        this.currentWorkingDir = workingDir;
      }
      /**
       * Initialize the bash session
       */
      async initialize() {
        if (this.process) {
          logger.warn(`BashSession ${this.sessionId}: Already initialized`);
          return;
        }
        try {
          this.process = (0, import_child_process.spawn)("/bin/bash", [], {
            cwd: this.currentWorkingDir,
            stdio: ["pipe", "pipe", "pipe"],
            env: { ...process.env }
          });
          this.isRunning = true;
          this.process.stdout?.on("data", (data) => {
            this.outputBuffer += data.toString();
          });
          this.process.stderr?.on("data", (data) => {
            this.errorBuffer += data.toString();
          });
          this.process.on("exit", (code, signal) => {
            this.isRunning = false;
            logger.debug(
              `BashSession ${this.sessionId}: Process exited with code ${code}, signal ${signal}`
            );
            this.emit("exit", { code, signal });
          });
          this.process.on("error", (error) => {
            this.isRunning = false;
            logger.error(`BashSession ${this.sessionId}: Process error`, error);
            this.emit("error", error);
          });
          await new Promise((resolve3) => setTimeout(resolve3, 100));
          logger.debug(`BashSession ${this.sessionId}: Initialized successfully`);
        } catch (error) {
          logger.error(`BashSession ${this.sessionId}: Failed to initialize`, error);
          throw error;
        }
      }
      /**
       * Execute a command in the session
       */
      async executeCommand(command, timeout = 3e4) {
        if (!this.process || !this.isRunning) {
          throw new Error("Bash session is not running");
        }
        const startTime = Date.now();
        this.outputBuffer = "";
        this.errorBuffer = "";
        return new Promise((resolve3, reject) => {
          const timeoutId = setTimeout(() => {
            reject(new Error(`Command timeout after ${timeout}ms`));
          }, timeout);
          const marker = `CMD_COMPLETE_${Date.now()}`;
          const fullCommand = `${command}; echo "${marker}"; echo "EXIT_CODE:$?" `;
          this.process.stdin?.write(`${fullCommand}
`);
          const checkCompletion = () => {
            if (this.outputBuffer.includes(marker)) {
              clearTimeout(timeoutId);
              const parts = this.outputBuffer.split(marker);
              const output = parts[0]?.trim() || "";
              const exitCodeMatch = this.outputBuffer.match(/EXIT_CODE:(\d+)/);
              const exitCode = exitCodeMatch?.[1] ? parseInt(exitCodeMatch[1], 10) : 0;
              const result = {
                output,
                error: this.errorBuffer.trim(),
                exitCode,
                command,
                duration: Date.now() - startTime,
                pid: this.process?.pid,
                workingDir: this.currentWorkingDir || void 0
              };
              resolve3(result);
            } else {
              setTimeout(checkCompletion, 100);
            }
          };
          setTimeout(checkCompletion, 50);
        });
      }
      /**
       * Close the bash session
       */
      async close() {
        if (this.process) {
          this.process.stdin?.write("exit\n");
          this.process.kill();
          this.process = null;
          this.isRunning = false;
          logger.debug(`BashSession ${this.sessionId}: Closed`);
        }
      }
      /**
       * Check if session is running
       */
      isActive() {
        return this.isRunning && this.process !== null;
      }
    };
    BashSessionManager = class _BashSessionManager {
      sessions = /* @__PURE__ */ new Map();
      static instance;
      static getInstance() {
        if (!_BashSessionManager.instance) {
          _BashSessionManager.instance = new _BashSessionManager();
        }
        return _BashSessionManager.instance;
      }
      /**
       * Get or create a bash session
       */
      async getSession(sessionId, workingDir) {
        let session = this.sessions.get(sessionId);
        if (!session || !session.isActive()) {
          session = new BashSession(sessionId, workingDir);
          await session.initialize();
          this.sessions.set(sessionId, session);
        }
        return session;
      }
      /**
       * Close a specific session
       */
      async closeSession(sessionId) {
        const session = this.sessions.get(sessionId);
        if (session) {
          await session.close();
          this.sessions.delete(sessionId);
        }
      }
      /**
       * Close all sessions
       */
      async closeAllSessions() {
        const closePromises = Array.from(this.sessions.values()).map((session) => session.close());
        await Promise.all(closePromises);
        this.sessions.clear();
      }
    };
    bashHandler = async (args, context) => {
      const {
        command,
        timeout = 3e4,
        workingDir,
        persistent = false,
        sessionId: customSessionId
      } = args;
      if (!command || typeof command !== "string") {
        return {
          content: "Error: Command is required and must be a string",
          isError: true
        };
      }
      try {
        let result;
        if (persistent) {
          const sessionManager = BashSessionManager.getInstance();
          const sessionId = customSessionId || context?.sessionId || "default";
          const session = await sessionManager.getSession(sessionId, workingDir);
          result = await session.executeCommand(command, timeout);
        } else {
          result = await executeBashCommand({
            command,
            timeout,
            workingDir
          });
        }
        const formattedOutput = [
          `Command: ${result.command}`,
          `Exit Code: ${result.exitCode}`,
          `Duration: ${result.duration}ms`,
          result.workingDir ? `Working Dir: ${result.workingDir}` : "",
          "",
          "Output:",
          result.output || "(no output)",
          result.error ? "\nError:" : "",
          result.error || ""
        ].filter(Boolean).join("\n");
        if (context?.services?.vectorStoreManager && result.output) {
          try {
            logger.debug("Command result could be stored in memory", { command });
          } catch (memoryError) {
            logger.warn("Failed to store command result in memory", memoryError);
          }
        }
        return {
          content: formattedOutput,
          isError: result.exitCode !== 0
        };
      } catch (error) {
        const errorMessage = error instanceof Error ? error.message : String(error);
        logger.error("Bash tool execution failed", { command, error: errorMessage });
        return {
          content: `Error executing command: ${errorMessage}`,
          isError: true
        };
      }
    };
    bashTool = {
      name: createInternalToolName("bash"),
      category: "system",
      internal: true,
      agentAccessible: true,
      description: "Execute bash commands in the system shell. Supports both one-off commands and persistent session execution.",
      parameters: {
        type: "object",
        properties: {
          command: {
            type: "string",
            description: "The bash command to execute. Can be a single command or a chain of commands using && or ;"
          },
          timeout: {
            type: "number",
            description: "Timeout in milliseconds for command execution (default: 30000)",
            default: 3e4
          },
          workingDir: {
            type: "string",
            description: "Working directory for command execution (defaults to current directory)"
          },
          persistent: {
            type: "boolean",
            description: "Whether to use a persistent bash session (maintains state between commands)",
            default: false
          },
          sessionId: {
            type: "string",
            description: "Session ID for persistent sessions (defaults to context session ID)"
          }
        },
        required: ["command"]
      },
      handler: bashHandler,
      version: "1.0.0"
    };
  }
});

// src/core/brain/tools/definitions/system/index.ts
var system_exports = {};
__export(system_exports, {
  bashTool: () => bashTool,
  getSystemTools: () => getSystemTools
});
function getSystemTools() {
  return {
    [bashTool.name]: bashTool
  };
}
var init_system = __esm({
  "src/core/brain/tools/definitions/system/index.ts"() {
    init_cjs_shims();
    init_bash();
  }
});

// src/core/brain/tools/definitions/index.ts
async function getAllToolDefinitions(options = {}) {
  try {
    const memoryTools = await Promise.resolve().then(() => (init_memory(), memory_exports)).then((m) => m.getMemoryTools(options));
    const systemTools = await Promise.resolve().then(() => (init_system(), system_exports)).then((m) => m.getSystemTools());
    let knowledgeGraphTools = {};
    if (env.KNOWLEDGE_GRAPH_ENABLED) {
      logger.debug("Knowledge graph enabled, loading knowledge graph tools");
      knowledgeGraphTools = await Promise.resolve().then(() => (init_knowledge_graph(), knowledge_graph_exports)).then(
        (m) => m.getKnowledgeGraphTools()
      );
    } else {
      logger.debug("Knowledge graph disabled, skipping knowledge graph tools");
    }
    const allTools = {
      ...memoryTools,
      ...systemTools,
      ...knowledgeGraphTools
    };
    return allTools;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("Failed to load tool definitions", { error: errorMessage });
    throw new Error(`Failed to load tool definitions: ${errorMessage}`);
  }
}
async function registerAllTools(toolManager, options = {}) {
  try {
    const tools = await getAllToolDefinitions(options);
    const registered = [];
    const failed = [];
    for (const [toolName, tool] of Object.entries(tools)) {
      try {
        const result2 = toolManager.registerTool(tool);
        if (result2.success) {
          registered.push(toolName);
        } else {
          failed.push({ name: toolName, error: result2.message });
          logger.warn(`Failed to register tool ${toolName}: ${result2.message}`);
        }
      } catch (error) {
        const errorMessage = error instanceof Error ? error.message : String(error);
        failed.push({ name: toolName, error: errorMessage });
        logger.error(`Error registering tool ${toolName}: ${errorMessage}`);
      }
    }
    const result = {
      registered,
      failed,
      total: Object.keys(tools).length
    };
    if (result.failed.length > 0) {
      logger.debug("Tool registration completed", {
        totalTools: result.total,
        successfullyRegistered: result.registered.length,
        failed: result.failed.length,
        embeddingEnabled: options.embeddingEnabled
      });
    }
    return result;
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("Failed to register tools", { error: errorMessage });
    throw new Error(`Failed to register tools: ${errorMessage}`);
  }
}
var init_definitions = __esm({
  "src/core/brain/tools/definitions/index.ts"() {
    init_cjs_shims();
    init_memory();
    init_knowledge_graph();
    init_system();
    init_logger2();
    init_env();
  }
});

// src/core/knowledge_graph/backend/types.ts
var KnowledgeGraphError, KnowledgeGraphConnectionError, NodeNotFoundError, EdgeNotFoundError, InvalidQueryError, GraphValidationError;
var init_types11 = __esm({
  "src/core/knowledge_graph/backend/types.ts"() {
    init_cjs_shims();
    KnowledgeGraphError = class _KnowledgeGraphError extends Error {
      constructor(message, operation, cause) {
        super(message);
        this.message = message;
        this.operation = operation;
        this.cause = cause;
        this.name = "KnowledgeGraphError";
        if (Error.captureStackTrace) {
          Error.captureStackTrace(this, _KnowledgeGraphError);
        }
        if (cause) {
          this.message = `${message}: ${cause.message}`;
        }
      }
    };
    KnowledgeGraphConnectionError = class extends KnowledgeGraphError {
      constructor(message, backendType, cause) {
        super(message, "connection", cause);
        this.message = message;
        this.backendType = backendType;
        this.cause = cause;
        this.name = "KnowledgeGraphConnectionError";
        this.message = `${backendType} connection failed: ${message}`;
      }
    };
    NodeNotFoundError = class extends KnowledgeGraphError {
      constructor(message, nodeId, cause) {
        super(message, "get", cause);
        this.message = message;
        this.nodeId = nodeId;
        this.cause = cause;
        this.name = "NodeNotFoundError";
        this.message = `Node not found: ${nodeId} - ${message}`;
      }
    };
    EdgeNotFoundError = class extends KnowledgeGraphError {
      constructor(message, edgeId, cause) {
        super(message, "get", cause);
        this.message = message;
        this.edgeId = edgeId;
        this.cause = cause;
        this.name = "EdgeNotFoundError";
        this.message = `Edge not found: ${edgeId} - ${message}`;
      }
    };
    InvalidQueryError = class extends KnowledgeGraphError {
      constructor(message, query2, cause) {
        super(message, "query", cause);
        this.message = message;
        this.query = query2;
        this.cause = cause;
        this.name = "InvalidQueryError";
        this.message = `Invalid query: ${message}`;
      }
    };
    GraphValidationError = class extends KnowledgeGraphError {
      constructor(message, validationType, cause) {
        super(message, "validation", cause);
        this.message = message;
        this.validationType = validationType;
        this.cause = cause;
        this.name = "GraphValidationError";
        this.message = `${validationType} validation failed: ${message}`;
      }
    };
  }
});

// src/core/knowledge_graph/constants.ts
var BACKEND_TYPES3, DEFAULTS3, ERROR_MESSAGES5, TIMEOUTS3, LOG_PREFIXES5;
var init_constants5 = __esm({
  "src/core/knowledge_graph/constants.ts"() {
    init_cjs_shims();
    BACKEND_TYPES3 = {
      NEO4J: "neo4j",
      IN_MEMORY: "in-memory"
    };
    DEFAULTS3 = {
      // Connection settings
      CONNECTION_TIMEOUT: 3e4,
      // 30 seconds
      MAX_RETRIES: 3,
      POOL_SIZE: 10,
      // Query settings
      QUERY_LIMIT: 100,
      QUERY_TIMEOUT: 6e4,
      // 60 seconds
      BATCH_SIZE: 1e3,
      // In-memory backend limits
      MAX_NODES: 1e4,
      MAX_EDGES: 5e4,
      // Neo4j specific
      NEO4J_PORT: 7687,
      NEO4J_DATABASE: "neo4j",
      NEO4J_MAX_TRANSACTION_RETRY_TIME: 3e4,
      NEO4J_CONNECTION_ACQUISITION_TIMEOUT: 6e4,
      NEO4J_MAX_CONNECTION_LIFETIME: 36e5,
      // 1 hour
      NEO4J_CONNECTION_LIVENESS_CHECK_TIMEOUT: 3e4,
      // Cache settings
      QUERY_CACHE_TTL: 3e5,
      // 5 minutes
      SCHEMA_CACHE_TTL: 6e5,
      // 10 minutes
      // Path finding
      MAX_PATH_DEPTH: 10,
      MAX_NEIGHBORS: 50
    };
    ERROR_MESSAGES5 = {
      // Connection errors
      NOT_CONNECTED: "Knowledge graph backend is not connected",
      CONNECTION_FAILED: "Failed to connect to knowledge graph backend",
      CONNECTION_TIMEOUT: "Connection to knowledge graph backend timed out",
      AUTHENTICATION_FAILED: "Authentication failed for knowledge graph backend",
      // Node errors
      NODE_NOT_FOUND: "Node not found in knowledge graph",
      INVALID_NODE_DATA: "Invalid node data provided",
      DUPLICATE_NODE_ID: "Node with this ID already exists",
      NODE_VALIDATION_FAILED: "Node validation failed",
      // Edge errors
      EDGE_NOT_FOUND: "Edge not found in knowledge graph",
      INVALID_EDGE_DATA: "Invalid edge data provided",
      DUPLICATE_EDGE_ID: "Edge with this ID already exists",
      EDGE_VALIDATION_FAILED: "Edge validation failed",
      NODES_NOT_FOUND_FOR_EDGE: "Start or end node not found for edge",
      // Query errors
      INVALID_QUERY: "Invalid graph query provided",
      QUERY_EXECUTION_FAILED: "Graph query execution failed",
      QUERY_TIMEOUT: "Graph query timed out",
      UNSUPPORTED_QUERY_TYPE: "Unsupported query type",
      // Backend errors
      BACKEND_NOT_SUPPORTED: "Knowledge graph backend type not supported",
      BACKEND_INITIALIZATION_FAILED: "Failed to initialize knowledge graph backend",
      BACKEND_OPERATION_FAILED: "Knowledge graph backend operation failed",
      // Validation errors
      SCHEMA_VALIDATION_FAILED: "Schema validation failed",
      CONFIGURATION_INVALID: "Knowledge graph configuration is invalid",
      PROPERTY_VALIDATION_FAILED: "Property validation failed",
      // Transaction errors
      TRANSACTION_FAILED: "Graph transaction failed",
      TRANSACTION_TIMEOUT: "Graph transaction timed out",
      CONCURRENT_MODIFICATION: "Concurrent modification detected",
      // Memory errors
      MEMORY_LIMIT_EXCEEDED: "Memory limit exceeded for in-memory backend",
      NODE_LIMIT_EXCEEDED: "Maximum number of nodes exceeded",
      EDGE_LIMIT_EXCEEDED: "Maximum number of edges exceeded"
    };
    TIMEOUTS3 = {
      // Connection timeouts
      CONNECTION: 3e4,
      // 30 seconds
      HEALTH_CHECK: 5e3,
      // 5 seconds
      DISCONNECTION: 1e4,
      // 10 seconds
      // Operation timeouts
      QUERY: 6e4,
      // 60 seconds
      TRANSACTION: 3e4,
      // 30 seconds
      BATCH_OPERATION: 12e4,
      // 2 minutes
      // Cache timeouts
      QUERY_CACHE: 3e5,
      // 5 minutes
      SCHEMA_CACHE: 6e5,
      // 10 minutes
      METRICS_CACHE: 6e4
      // 1 minute
    };
    LOG_PREFIXES5 = {
      MANAGER: "[KG-Manager]",
      BACKEND: "[KG-Backend]",
      NEO4J: "[KG-Neo4j]",
      IN_MEMORY: "[KG-Memory]",
      QUERY: "[KG-Query]",
      TRANSACTION: "[KG-Tx]",
      FACTORY: "[KG-Factory]",
      VALIDATION: "[KG-Validation]"
    };
  }
});

// src/core/knowledge_graph/backend/neo4j.ts
var neo4j_exports = {};
__export(neo4j_exports, {
  Neo4jBackend: () => Neo4jBackend
});
var import_neo4j_driver, Neo4jBackend;
var init_neo4j = __esm({
  "src/core/knowledge_graph/backend/neo4j.ts"() {
    init_cjs_shims();
    import_neo4j_driver = __toESM(require("neo4j-driver"), 1);
    init_logger();
    init_types11();
    init_constants5();
    Neo4jBackend = class {
      config;
      logger;
      driver = null;
      connected = false;
      constructor(config2) {
        this.config = {
          ...config2,
          timeout: config2.timeout ?? DEFAULTS3.CONNECTION_TIMEOUT,
          maxRetries: config2.maxRetries ?? DEFAULTS3.MAX_RETRIES,
          enablePooling: config2.enablePooling ?? true,
          poolSize: config2.poolSize ?? DEFAULTS3.POOL_SIZE,
          database: config2.database ?? DEFAULTS3.NEO4J_DATABASE,
          encrypted: config2.encrypted ?? false,
          trustServerCertificate: config2.trustServerCertificate ?? false,
          maxTransactionRetryTime: config2.maxTransactionRetryTime ?? DEFAULTS3.NEO4J_MAX_TRANSACTION_RETRY_TIME,
          connectionAcquisitionTimeout: config2.connectionAcquisitionTimeout ?? DEFAULTS3.NEO4J_CONNECTION_ACQUISITION_TIMEOUT,
          maxConnectionLifetime: config2.maxConnectionLifetime ?? DEFAULTS3.NEO4J_MAX_CONNECTION_LIFETIME,
          connectionLivenessCheckTimeout: config2.connectionLivenessCheckTimeout ?? DEFAULTS3.NEO4J_CONNECTION_LIVENESS_CHECK_TIMEOUT
        };
        this.logger = createLogger({
          level: "debug"
        });
        this.logger.debug(`${LOG_PREFIXES5.NEO4J} Initialized with config:`, {
          uri: this.buildConnectionUri(),
          database: this.config.database,
          encrypted: this.config.encrypted,
          poolSize: this.config.poolSize
        });
      }
      // Connection management
      async connect() {
        if (this.connected && this.driver) {
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Already connected`);
          return;
        }
        try {
          const uri = this.buildConnectionUri();
          const auth = import_neo4j_driver.default.auth.basic(this.config.username, this.config.password);
          const driverConfig = {
            maxConnectionPoolSize: this.config.poolSize,
            connectionAcquisitionTimeout: this.config.connectionAcquisitionTimeout,
            maxTransactionRetryTime: this.config.maxTransactionRetryTime,
            maxConnectionLifetime: this.config.maxConnectionLifetime,
            connectionLivenessCheckTimeout: this.config.connectionLivenessCheckTimeout
          };
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Creating driver for ${uri}`);
          this.driver = import_neo4j_driver.default.driver(uri, auth, driverConfig);
          await this.driver.verifyConnectivity();
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES5.NEO4J} Connected successfully to ${uri}`);
          await this.createIndexes();
        } catch (error) {
          console.error("Neo4j connect error:", error);
          const connectionError = new KnowledgeGraphConnectionError(
            `${ERROR_MESSAGES5.CONNECTION_FAILED}: ${error.message}`,
            "neo4j",
            error
          );
          this.logger.error(`${LOG_PREFIXES5.NEO4J} Connection failed:`, connectionError);
          throw connectionError;
        }
      }
      async disconnect() {
        if (!this.connected || !this.driver) {
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Already disconnected`);
          return;
        }
        try {
          await this.driver.close();
          this.driver = null;
          this.connected = false;
          this.logger.info(`${LOG_PREFIXES5.NEO4J} Disconnected successfully`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES5.NEO4J} Error during disconnect:`, error);
          throw error;
        }
      }
      isConnected() {
        return this.connected && this.driver !== null;
      }
      getBackendType() {
        return "neo4j";
      }
      // Node operations
      async addNode(node) {
        this.ensureConnected();
        this.validateNode(node);
        const session = this.getSession();
        try {
          const labelsStr = node.labels.map((label) => `\`${label}\``).join(":");
          const query2 = `CREATE (n:${labelsStr}) SET n = $properties, n.id = $id RETURN n`;
          const result = await session.run(query2, {
            id: node.id,
            properties: node.properties
          });
          if (result.records.length === 0) {
            throw new GraphValidationError("Failed to create node", "node");
          }
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Added node:`, { id: node.id, labels: node.labels });
        } catch (error) {
          if (error.code === "Neo.ClientError.Schema.ConstraintValidationFailed") {
            throw new GraphValidationError(
              `Node with ID '${node.id}' already exists`,
              "node",
              error
            );
          }
          throw error;
        } finally {
          await session.close();
        }
      }
      async addNodes(nodes) {
        this.ensureConnected();
        for (const node of nodes) {
          this.validateNode(node);
        }
        const session = this.getSession();
        try {
          const tx = session.beginTransaction();
          for (const node of nodes) {
            const labelsStr = node.labels.map((label) => `\`${label}\``).join(":");
            const query2 = `CREATE (n:${labelsStr}) SET n = $properties, n.id = $id`;
            await tx.run(query2, {
              id: node.id,
              properties: node.properties
            });
          }
          await tx.commit();
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Added ${nodes.length} nodes`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES5.NEO4J} Error adding nodes:`, error);
          throw error;
        } finally {
          await session.close();
        }
      }
      async getNode(nodeId) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const query2 = "MATCH (n) WHERE n.id = $id RETURN n, labels(n) as nodeLabels";
          const result = await session.run(query2, { id: nodeId });
          if (result.records.length === 0) {
            return null;
          }
          const record = result.records[0];
          const nodeData = record.get("n").properties;
          const labels = record.get("nodeLabels");
          return {
            id: nodeId,
            labels,
            properties: this.convertNeo4jProperties(nodeData)
          };
        } finally {
          await session.close();
        }
      }
      async updateNode(nodeId, properties, labels) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const existsResult = await session.run("MATCH (n) WHERE n.id = $id RETURN n", { id: nodeId });
          if (existsResult.records.length === 0) {
            throw new NodeNotFoundError(ERROR_MESSAGES5.NODE_NOT_FOUND, nodeId);
          }
          let query2 = "MATCH (n) WHERE n.id = $id SET n += $properties";
          const params = { id: nodeId, properties };
          if (labels && labels.length > 0) {
            const labelsStr = labels.map((label) => `\`${label}\``).join(":");
            query2 = `MATCH (n) WHERE n.id = $id REMOVE n:${labelsStr} SET n:${labelsStr}, n += $properties`;
          }
          await session.run(query2, params);
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Updated node:`, { id: nodeId });
        } finally {
          await session.close();
        }
      }
      async deleteNode(nodeId) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const query2 = "MATCH (n) WHERE n.id = $id DETACH DELETE n";
          await session.run(query2, { id: nodeId });
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Deleted node:`, { id: nodeId });
        } finally {
          await session.close();
        }
      }
      async findNodes(filters, labels, limit) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          let query2 = "MATCH (n)";
          const params = {};
          if (labels && labels.length > 0) {
            const labelConstraints = labels.map((label) => `n:\`${label}\``).join(" OR ");
            query2 += ` WHERE (${labelConstraints})`;
          }
          if (filters) {
            const filterConstraints = this.buildFilterConstraints(filters, "n", params);
            if (filterConstraints) {
              query2 += labels && labels.length > 0 ? ` AND ${filterConstraints}` : ` WHERE ${filterConstraints}`;
            }
          }
          query2 += " RETURN n, labels(n) as nodeLabels";
          if (limit) {
            query2 += ` LIMIT ${limit}`;
          }
          const result = await session.run(query2, params);
          const nodes = [];
          for (const record of result.records) {
            const nodeData = record.get("n").properties;
            const nodeLabels = record.get("nodeLabels");
            nodes.push({
              id: nodeData.id,
              labels: nodeLabels,
              properties: this.convertNeo4jProperties(nodeData)
            });
          }
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Found ${nodes.length} nodes`);
          return nodes;
        } finally {
          await session.close();
        }
      }
      // Edge operations
      async addEdge(edge) {
        this.ensureConnected();
        this.validateEdge(edge);
        const session = this.getSession();
        try {
          const query2 = `
				MATCH (a), (b) 
				WHERE a.id = $startId AND b.id = $endId 
				CREATE (a)-[r:\`${edge.type}\`]->(b) 
				SET r = $properties, r.id = $edgeId 
				RETURN r
			`;
          const result = await session.run(query2, {
            startId: edge.startNodeId,
            endId: edge.endNodeId,
            edgeId: edge.id,
            properties: edge.properties
          });
          if (result.records.length === 0) {
            throw new GraphValidationError(
              `Failed to create edge: start node '${edge.startNodeId}' or end node '${edge.endNodeId}' not found`,
              "edge"
            );
          }
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Added edge:`, {
            id: edge.id,
            type: edge.type,
            from: edge.startNodeId,
            to: edge.endNodeId
          });
        } catch (error) {
          if (error.code === "Neo.ClientError.Schema.ConstraintValidationFailed") {
            throw new GraphValidationError(
              `Edge with ID '${edge.id}' already exists`,
              "edge",
              error
            );
          }
          throw error;
        } finally {
          await session.close();
        }
      }
      async addEdges(edges) {
        this.ensureConnected();
        for (const edge of edges) {
          this.validateEdge(edge);
        }
        const session = this.getSession();
        try {
          const tx = session.beginTransaction();
          for (const edge of edges) {
            const query2 = `
					MATCH (a), (b) 
					WHERE a.id = $startId AND b.id = $endId 
					CREATE (a)-[r:\`${edge.type}\`]->(b) 
					SET r = $properties, r.id = $edgeId
				`;
            await tx.run(query2, {
              startId: edge.startNodeId,
              endId: edge.endNodeId,
              edgeId: edge.id,
              properties: edge.properties
            });
          }
          await tx.commit();
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Added ${edges.length} edges`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES5.NEO4J} Error adding edges:`, error);
          throw error;
        } finally {
          await session.close();
        }
      }
      async getEdge(edgeId) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const query2 = `
				MATCH (a)-[r]->(b) 
				WHERE r.id = $id 
				RETURN r, a.id as startId, b.id as endId, type(r) as edgeType
			`;
          const result = await session.run(query2, { id: edgeId });
          if (result.records.length === 0) {
            return null;
          }
          const record = result.records[0];
          const edgeData = record.get("r").properties;
          const startId = record.get("startId");
          const endId = record.get("endId");
          const edgeType = record.get("edgeType");
          return {
            id: edgeId,
            type: edgeType,
            startNodeId: startId,
            endNodeId: endId,
            properties: this.convertNeo4jProperties(edgeData)
          };
        } finally {
          await session.close();
        }
      }
      async updateEdge(edgeId, properties) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const existsResult = await session.run("MATCH ()-[r]-() WHERE r.id = $id RETURN r", {
            id: edgeId
          });
          if (existsResult.records.length === 0) {
            throw new EdgeNotFoundError(ERROR_MESSAGES5.EDGE_NOT_FOUND, edgeId);
          }
          const query2 = "MATCH ()-[r]-() WHERE r.id = $id SET r += $properties";
          await session.run(query2, { id: edgeId, properties });
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Updated edge:`, { id: edgeId });
        } finally {
          await session.close();
        }
      }
      async deleteEdge(edgeId) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const query2 = "MATCH ()-[r]-() WHERE r.id = $id DELETE r";
          await session.run(query2, { id: edgeId });
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Deleted edge:`, { id: edgeId });
        } finally {
          await session.close();
        }
      }
      async findEdges(filters, edgeType, limit) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          let query2 = "MATCH (a)-[r]->(b)";
          const params = {};
          if (edgeType) {
            query2 = `MATCH (a)-[r:\`${edgeType}\`]->(b)`;
          }
          if (filters) {
            const filterConstraints = this.buildFilterConstraints(filters, "r", params);
            if (filterConstraints) {
              query2 += ` WHERE ${filterConstraints}`;
            }
          }
          query2 += " RETURN r, a.id as startId, b.id as endId, type(r) as edgeType";
          if (limit) {
            query2 += ` LIMIT ${limit}`;
          }
          const result = await session.run(query2, params);
          const edges = [];
          for (const record of result.records) {
            const edgeData = record.get("r").properties;
            const startId = record.get("startId");
            const endId = record.get("endId");
            const type = record.get("edgeType");
            edges.push({
              id: edgeData.id,
              type,
              startNodeId: startId,
              endNodeId: endId,
              properties: this.convertNeo4jProperties(edgeData)
            });
          }
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Found ${edges.length} edges`);
          return edges;
        } finally {
          await session.close();
        }
      }
      // Advanced operations
      async query(query2) {
        this.ensureConnected();
        const startTime = Date.now();
        const session = this.getSession();
        try {
          let result;
          switch (query2.type) {
            case "node":
              result = await this.executeNodeQuery(query2, session);
              break;
            case "edge":
              result = await this.executeEdgeQuery(query2, session);
              break;
            case "path":
              result = await this.executePathQuery(query2, session);
              break;
            case "cypher":
              result = await this.executeCypherQuery(query2, session);
              break;
            default:
              throw new InvalidQueryError(`Unsupported query type: ${query2.type}`, query2);
          }
          result.metadata.executionTime = Date.now() - startTime;
          return result;
        } catch (error) {
          throw new InvalidQueryError(
            `Query execution failed: ${error.message}`,
            query2,
            error
          );
        } finally {
          await session.close();
        }
      }
      async getNeighbors(nodeId, direction = "both", edgeTypes, limit) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          let relationshipPattern;
          switch (direction) {
            case "in":
              relationshipPattern = "<-[r]-";
              break;
            case "out":
              relationshipPattern = "-[r]->";
              break;
            case "both":
              relationshipPattern = "-[r]-";
              break;
          }
          if (edgeTypes && edgeTypes.length > 0) {
            const typeStr = edgeTypes.map((type) => `\`${type}\``).join("|");
            relationshipPattern = relationshipPattern.replace("[r]", `[r:${typeStr}]`);
          }
          let query2 = `
				MATCH (n)${relationshipPattern}(m) 
				WHERE n.id = $nodeId 
				RETURN m, labels(m) as nodeLabels, r, type(r) as edgeType, n.id as startId, m.id as endId
			`;
          if (limit) {
            query2 += ` LIMIT ${limit}`;
          }
          const result = await session.run(query2, { nodeId });
          const neighbors = [];
          for (const record of result.records) {
            const nodeData = record.get("m").properties;
            const nodeLabels = record.get("nodeLabels");
            const edgeData = record.get("r").properties;
            const edgeType = record.get("edgeType");
            const startId = record.get("startId");
            const endId = record.get("endId");
            neighbors.push({
              node: {
                id: nodeData.id,
                labels: nodeLabels,
                properties: this.convertNeo4jProperties(nodeData)
              },
              edge: {
                id: edgeData.id,
                type: edgeType,
                startNodeId: startId,
                endNodeId: endId,
                properties: this.convertNeo4jProperties(edgeData)
              }
            });
          }
          this.logger.debug(
            `${LOG_PREFIXES5.NEO4J} Found ${neighbors.length} neighbors for node ${nodeId}`
          );
          return neighbors;
        } finally {
          await session.close();
        }
      }
      async findPath(startNodeId, endNodeId, maxDepth = DEFAULTS3.MAX_PATH_DEPTH, edgeTypes) {
        this.ensureConnected();
        const session = this.getSession();
        try {
          let relationshipPattern = "*1.." + maxDepth;
          if (edgeTypes && edgeTypes.length > 0) {
            const typeStr = edgeTypes.map((type) => `\`${type}\``).join("|");
            relationshipPattern = `:${typeStr}${relationshipPattern}`;
          }
          const query2 = `
				MATCH path = shortestPath((a)-[${relationshipPattern}]-(b)) 
				WHERE a.id = $startId AND b.id = $endId 
				RETURN path
			`;
          const result = await session.run(query2, {
            startId: startNodeId,
            endId: endNodeId
          });
          if (result.records.length === 0) {
            this.logger.debug(
              `${LOG_PREFIXES5.NEO4J} No path found from ${startNodeId} to ${endNodeId}`
            );
            return null;
          }
          const path12 = result.records[0].get("path");
          const nodes = [];
          const edges = [];
          for (const nodeRecord of path12.segments.flatMap((segment) => [
            segment.start,
            segment.end
          ])) {
            const nodeData = nodeRecord.properties;
            const labels = nodeRecord.labels;
            nodes.push({
              id: nodeData.id,
              labels,
              properties: this.convertNeo4jProperties(nodeData)
            });
          }
          for (const segment of path12.segments) {
            const edgeData = segment.relationship.properties;
            const startNodeData = segment.start.properties;
            const endNodeData = segment.end.properties;
            edges.push({
              id: edgeData.id,
              type: segment.relationship.type,
              startNodeId: startNodeData.id,
              endNodeId: endNodeData.id,
              properties: this.convertNeo4jProperties(edgeData)
            });
          }
          const uniqueNodes = nodes.filter(
            (node, index, self) => index === self.findIndex((n) => n.id === node.id)
          );
          this.logger.debug(
            `${LOG_PREFIXES5.NEO4J} Found path from ${startNodeId} to ${endNodeId} with ${edges.length} edges`
          );
          return { nodes: uniqueNodes, edges };
        } finally {
          await session.close();
        }
      }
      // Graph management
      async clearGraph() {
        this.ensureConnected();
        const session = this.getSession();
        try {
          await session.run("MATCH (n) DETACH DELETE n");
          this.logger.info(`${LOG_PREFIXES5.NEO4J} Graph cleared`);
        } finally {
          await session.close();
        }
      }
      async getStatistics() {
        this.ensureConnected();
        const session = this.getSession();
        try {
          const nodeCountResult = await session.run("MATCH (n) RETURN count(n) as count");
          const nodeCount = nodeCountResult.records[0].get("count").toNumber();
          const edgeCountResult = await session.run("MATCH ()-[r]-() RETURN count(r) as count");
          const edgeCount = edgeCountResult.records[0].get("count").toNumber();
          const labelCountsResult = await session.run(`
				CALL db.labels() YIELD label
				CALL apoc.cypher.run('MATCH (n:\`' + label + '\`) RETURN count(n) as count', {}) YIELD value
				RETURN label, value.count as count
			`);
          const labelCounts = {};
          for (const record of labelCountsResult.records) {
            const label = record.get("label");
            const count = record.get("count").toNumber();
            labelCounts[label] = count;
          }
          const edgeTypeCountsResult = await session.run(`
				CALL db.relationshipTypes() YIELD relationshipType
				CALL apoc.cypher.run('MATCH ()-[r:\`' + relationshipType + '\`]-() RETURN count(r) as count', {}) YIELD value
				RETURN relationshipType, value.count as count
			`);
          const edgeTypeCounts = {};
          for (const record of edgeTypeCountsResult.records) {
            const type = record.get("relationshipType");
            const count = record.get("count").toNumber();
            edgeTypeCounts[type] = count;
          }
          return {
            nodeCount,
            edgeCount,
            labelCounts,
            edgeTypeCounts
          };
        } finally {
          await session.close();
        }
      }
      // Private helper methods
      ensureConnected() {
        if (!this.connected || !this.driver) {
          throw new KnowledgeGraphConnectionError(ERROR_MESSAGES5.NOT_CONNECTED, "neo4j");
        }
      }
      getSession() {
        if (!this.driver) {
          throw new KnowledgeGraphConnectionError(ERROR_MESSAGES5.NOT_CONNECTED, "neo4j");
        }
        return this.driver.session({
          database: this.config.database,
          defaultAccessMode: import_neo4j_driver.default.session.WRITE
        });
      }
      buildConnectionUri() {
        if (this.config.uri) {
          return this.config.uri;
        }
        const protocol = this.config.encrypted ? "neo4j+s" : "neo4j";
        const port = this.config.port ?? DEFAULTS3.NEO4J_PORT;
        return `${protocol}://${this.config.host}:${port}`;
      }
      validateNode(node) {
        if (!node.id || typeof node.id !== "string") {
          throw new GraphValidationError("Node ID must be a non-empty string", "node");
        }
        if (!Array.isArray(node.labels) || node.labels.length === 0) {
          throw new GraphValidationError("Node must have at least one label", "node");
        }
        if (!node.properties || typeof node.properties !== "object") {
          throw new GraphValidationError("Node properties must be an object", "node");
        }
      }
      validateEdge(edge) {
        if (!edge.id || typeof edge.id !== "string") {
          throw new GraphValidationError("Edge ID must be a non-empty string", "edge");
        }
        if (!edge.type || typeof edge.type !== "string") {
          throw new GraphValidationError("Edge type must be a non-empty string", "edge");
        }
        if (!edge.startNodeId || typeof edge.startNodeId !== "string") {
          throw new GraphValidationError("Edge startNodeId must be a non-empty string", "edge");
        }
        if (!edge.endNodeId || typeof edge.endNodeId !== "string") {
          throw new GraphValidationError("Edge endNodeId must be a non-empty string", "edge");
        }
        if (!edge.properties || typeof edge.properties !== "object") {
          throw new GraphValidationError("Edge properties must be an object", "edge");
        }
      }
      convertNeo4jProperties(properties) {
        const converted = {};
        for (const [key, value] of Object.entries(properties)) {
          if (value && typeof value === "object" && "toNumber" in value) {
            converted[key] = value.toNumber();
          } else {
            converted[key] = value;
          }
        }
        return converted;
      }
      buildFilterConstraints(filters, prefix, params) {
        const constraints = [];
        for (const [, filter] of Object.entries(filters)) {
          const paramKey = `${prefix}_${Object.keys(params).length}`;
          if (typeof filter === "object" && filter !== null && !Array.isArray(filter)) {
            if ("gte" in filter) {
              constraints.push(`${prefix}.${Object.keys(params).length} >= $${paramKey}_gte`);
              params[`${paramKey}_gte`] = filter.gte;
            }
            if ("gt" in filter) {
              constraints.push(`${prefix}.${Object.keys(params).length} > $${paramKey}_gt`);
              params[`${paramKey}_gt`] = filter.gt;
            }
            if ("lte" in filter) {
              constraints.push(`${prefix}.${Object.keys(params).length} <= $${paramKey}_lte`);
              params[`${paramKey}_lte`] = filter.lte;
            }
            if ("lt" in filter) {
              constraints.push(`${prefix}.${Object.keys(params).length} < $${paramKey}_lt`);
              params[`${paramKey}_lt`] = filter.lt;
            }
            if ("any" in filter && Array.isArray(filter.any)) {
              constraints.push(`${prefix}.${Object.keys(params).length} IN $${paramKey}`);
              params[paramKey] = filter.any;
            }
            if ("all" in filter && Array.isArray(filter.all)) {
              constraints.push(
                `all(x IN $${paramKey} WHERE x IN ${prefix}.${Object.keys(params).length})`
              );
              params[paramKey] = filter.all;
            }
          } else {
            constraints.push(`${prefix}.${Object.keys(params).length} = $${paramKey}`);
            params[paramKey] = filter;
          }
        }
        return constraints.join(" AND ");
      }
      async createIndexes() {
        const session = this.getSession();
        try {
          await session.run(
            "CREATE CONSTRAINT node_id_unique IF NOT EXISTS FOR (n:Entity) REQUIRE n.id IS UNIQUE"
          );
          await session.run("CREATE INDEX node_id_index IF NOT EXISTS FOR (n:Entity) ON (n.id)");
          await session.run(
            "CREATE INDEX edge_id_index IF NOT EXISTS FOR ()-[r:CONNECTED_TO]-() ON (r.id)"
          );
          this.logger.debug(`${LOG_PREFIXES5.NEO4J} Created indexes`);
        } catch (_error) {
          console.log(_error);
          this.logger.warn(`${LOG_PREFIXES5.NEO4J} Could not create indexes:`, _error);
        } finally {
          await session.close();
        }
      }
      async executeNodeQuery(query2, _session) {
        const { pattern, limit } = query2;
        const labels = pattern?.labels;
        const properties = pattern?.properties;
        const nodes = await this.findNodes(properties, labels, limit);
        return {
          nodes,
          edges: [],
          metadata: {
            totalCount: nodes.length,
            queryType: "node"
          }
        };
      }
      async executeEdgeQuery(query2, _session) {
        const { pattern, limit } = query2;
        const edgeType = pattern?.type;
        const properties = pattern?.properties;
        const edges = await this.findEdges(properties, edgeType, limit);
        return {
          nodes: [],
          edges,
          metadata: {
            totalCount: edges.length,
            queryType: "edge"
          }
        };
      }
      async executePathQuery(query2, _session) {
        const { pattern, parameters } = query2;
        const startNode = pattern?.startNode;
        const endNode = pattern?.endNode;
        const maxDepth = parameters?.maxDepth ?? DEFAULTS3.MAX_PATH_DEPTH;
        if (!startNode?.id || !endNode?.id) {
          throw new InvalidQueryError("Path query requires startNode.id and endNode.id", query2);
        }
        const path12 = await this.findPath(startNode.id, endNode.id, maxDepth);
        if (path12) {
          return {
            nodes: path12.nodes,
            edges: path12.edges,
            paths: [
              {
                nodes: path12.nodes,
                edges: path12.edges,
                length: path12.edges.length
              }
            ],
            metadata: {
              totalCount: 1,
              queryType: "path"
            }
          };
        }
        return {
          nodes: [],
          edges: [],
          paths: [],
          metadata: {
            totalCount: 0,
            queryType: "path"
          }
        };
      }
      async executeCypherQuery(query2, session) {
        if (!query2.query) {
          throw new InvalidQueryError("Cypher query requires query string", query2);
        }
        const result = await session.run(query2.query, query2.parameters || {});
        const nodes = [];
        const edges = [];
        for (const record of result.records) {
          for (const [, value] of record.entries()) {
            if (value && typeof value === "object") {
              if ("labels" in value && "properties" in value) {
                nodes.push({
                  id: value.properties.id,
                  labels: value.labels,
                  properties: this.convertNeo4jProperties(value.properties)
                });
              } else if ("type" in value && "start" in value && "end" in value) {
                edges.push({
                  id: value.properties.id,
                  type: value.type,
                  startNodeId: value.start.properties.id,
                  endNodeId: value.end.properties.id,
                  properties: this.convertNeo4jProperties(value.properties)
                });
              }
            }
          }
        }
        return {
          nodes,
          edges,
          metadata: {
            totalCount: result.records.length,
            queryType: "cypher"
          }
        };
      }
    };
  }
});

// src/core/knowledge_graph/backend/in-memory.ts
var in_memory_exports3 = {};
__export(in_memory_exports3, {
  InMemoryBackend: () => InMemoryBackend3
});
var InMemoryBackend3;
var init_in_memory3 = __esm({
  "src/core/knowledge_graph/backend/in-memory.ts"() {
    init_cjs_shims();
    init_logger();
    init_types11();
    init_constants5();
    InMemoryBackend3 = class {
      config;
      logger;
      connected = false;
      // Core storage
      nodes = /* @__PURE__ */ new Map();
      edges = /* @__PURE__ */ new Map();
      // Adjacency lists for graph traversal
      outgoingEdges = /* @__PURE__ */ new Map();
      // node ID -> set of outgoing edge IDs
      incomingEdges = /* @__PURE__ */ new Map();
      // node ID -> set of incoming edge IDs
      // Indexes for performance
      nodeIndex = {
        labels: /* @__PURE__ */ new Map(),
        edgeTypes: /* @__PURE__ */ new Map(),
        properties: /* @__PURE__ */ new Map()
      };
      edgeIndex = {
        labels: /* @__PURE__ */ new Map(),
        edgeTypes: /* @__PURE__ */ new Map(),
        properties: /* @__PURE__ */ new Map()
      };
      // Statistics
      stats = {
        nodeCount: 0,
        edgeCount: 0,
        operations: 0,
        queriesExecuted: 0,
        lastOperation: 0
      };
      constructor(config2) {
        this.config = {
          ...config2,
          maxNodes: config2.maxNodes ?? DEFAULTS3.MAX_NODES,
          maxEdges: config2.maxEdges ?? DEFAULTS3.MAX_EDGES,
          enableIndexing: config2.enableIndexing ?? true,
          enableGarbageCollection: config2.enableGarbageCollection ?? false
        };
        this.logger = createLogger({
          level: "debug"
        });
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Initialized with config:`, {
          maxNodes: this.config.maxNodes,
          maxEdges: this.config.maxEdges,
          enableIndexing: this.config.enableIndexing,
          enableGarbageCollection: this.config.enableGarbageCollection
        });
      }
      // Connection management
      async connect() {
        if (this.connected) {
          this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Already connected`);
          return;
        }
        try {
          this.clear();
          this.connected = true;
          this.logger.info(`${LOG_PREFIXES5.IN_MEMORY} Connected successfully`);
        } catch (error) {
          const connectionError = new KnowledgeGraphConnectionError(
            ERROR_MESSAGES5.CONNECTION_FAILED,
            "in-memory",
            error
          );
          this.logger.error(`${LOG_PREFIXES5.IN_MEMORY} Connection failed:`, connectionError);
          throw connectionError;
        }
      }
      async disconnect() {
        if (!this.connected) {
          this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Already disconnected`);
          return;
        }
        try {
          if (this.config.enableGarbageCollection) {
            this.clear();
          }
          this.connected = false;
          this.logger.info(`${LOG_PREFIXES5.IN_MEMORY} Disconnected successfully`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES5.IN_MEMORY} Error during disconnect:`, error);
          throw error;
        }
      }
      isConnected() {
        return this.connected;
      }
      getBackendType() {
        return "in-memory";
      }
      // Node operations
      async addNode(node) {
        this.ensureConnected();
        this.validateNode(node);
        if (this.nodes.has(node.id)) {
          throw new GraphValidationError(`Node with ID '${node.id}' already exists`, "node");
        }
        if (this.stats.nodeCount >= this.config.maxNodes) {
          throw new GraphValidationError(
            `Maximum number of nodes (${this.config.maxNodes}) exceeded`,
            "node"
          );
        }
        const entry = {
          data: this.deepClone(node),
          createdAt: Date.now(),
          updatedAt: Date.now()
        };
        this.nodes.set(node.id, entry);
        this.stats.nodeCount++;
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        if (this.config.enableIndexing) {
          this.updateNodeIndex(node.id, node, "add");
        }
        this.outgoingEdges.set(node.id, /* @__PURE__ */ new Set());
        this.incomingEdges.set(node.id, /* @__PURE__ */ new Set());
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Added node:`, {
          id: node.id,
          labels: node.labels
        });
      }
      async addNodes(nodes) {
        this.ensureConnected();
        if (this.stats.nodeCount + nodes.length > this.config.maxNodes) {
          throw new GraphValidationError(
            `Adding ${nodes.length} nodes would exceed maximum (${this.config.maxNodes})`,
            "node"
          );
        }
        for (const node of nodes) {
          this.validateNode(node);
          if (this.nodes.has(node.id)) {
            throw new GraphValidationError(`Node with ID '${node.id}' already exists`, "node");
          }
        }
        for (const node of nodes) {
          await this.addNode(node);
        }
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Added ${nodes.length} nodes`);
      }
      async getNode(nodeId) {
        this.ensureConnected();
        const entry = this.nodes.get(nodeId);
        if (!entry) {
          return null;
        }
        return this.deepClone(entry.data);
      }
      async updateNode(nodeId, properties, labels) {
        this.ensureConnected();
        const entry = this.nodes.get(nodeId);
        if (!entry) {
          throw new NodeNotFoundError(ERROR_MESSAGES5.NODE_NOT_FOUND, nodeId);
        }
        if (this.config.enableIndexing) {
          this.updateNodeIndex(nodeId, entry.data, "remove");
        }
        const updatedNode = {
          ...entry.data,
          properties: { ...entry.data.properties, ...properties },
          labels: labels ?? entry.data.labels
        };
        entry.data = updatedNode;
        entry.updatedAt = Date.now();
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        if (this.config.enableIndexing) {
          this.updateNodeIndex(nodeId, updatedNode, "add");
        }
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Updated node:`, { id: nodeId });
      }
      async deleteNode(nodeId) {
        this.ensureConnected();
        const entry = this.nodes.get(nodeId);
        if (!entry) {
          return;
        }
        const outgoing = this.outgoingEdges.get(nodeId) || /* @__PURE__ */ new Set();
        const incoming = this.incomingEdges.get(nodeId) || /* @__PURE__ */ new Set();
        for (const edgeId of [...outgoing, ...incoming]) {
          await this.deleteEdge(edgeId);
        }
        if (this.config.enableIndexing) {
          this.updateNodeIndex(nodeId, entry.data, "remove");
        }
        this.nodes.delete(nodeId);
        this.outgoingEdges.delete(nodeId);
        this.incomingEdges.delete(nodeId);
        this.stats.nodeCount--;
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Deleted node:`, { id: nodeId });
      }
      async findNodes(filters, labels, limit) {
        this.ensureConnected();
        const maxResults = limit ?? DEFAULTS3.QUERY_LIMIT;
        const results = [];
        let candidateIds = /* @__PURE__ */ new Set();
        if (labels && labels.length > 0 && this.config.enableIndexing) {
          for (const label of labels) {
            const labelIds = this.nodeIndex.labels.get(label) || /* @__PURE__ */ new Set();
            if (candidateIds.size === 0) {
              candidateIds = new Set(labelIds);
            } else {
              candidateIds = new Set([...candidateIds].filter((id) => labelIds.has(id)));
            }
          }
        } else {
          candidateIds = new Set(this.nodes.keys());
        }
        for (const nodeId of candidateIds) {
          if (results.length >= maxResults) break;
          const entry = this.nodes.get(nodeId);
          if (!entry) continue;
          const node = entry.data;
          if (labels && labels.length > 0) {
            if (!labels.some((label) => node.labels.includes(label))) {
              continue;
            }
          }
          if (filters && !this.matchesNodeFilters(node, filters)) {
            continue;
          }
          results.push(this.deepClone(node));
        }
        this.stats.queriesExecuted++;
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Found ${results.length} nodes`);
        return results;
      }
      // Edge operations
      async addEdge(edge) {
        this.ensureConnected();
        this.validateEdge(edge);
        if (this.edges.has(edge.id)) {
          throw new GraphValidationError(`Edge with ID '${edge.id}' already exists`, "edge");
        }
        if (this.stats.edgeCount >= this.config.maxEdges) {
          throw new GraphValidationError(
            `Maximum number of edges (${this.config.maxEdges}) exceeded`,
            "edge"
          );
        }
        if (!this.nodes.has(edge.startNodeId)) {
          throw new GraphValidationError(`Start node '${edge.startNodeId}' not found`, "edge");
        }
        if (!this.nodes.has(edge.endNodeId)) {
          throw new GraphValidationError(`End node '${edge.endNodeId}' not found`, "edge");
        }
        const entry = {
          data: this.deepClone(edge),
          createdAt: Date.now(),
          updatedAt: Date.now()
        };
        this.edges.set(edge.id, entry);
        this.stats.edgeCount++;
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        this.outgoingEdges.get(edge.startNodeId).add(edge.id);
        this.incomingEdges.get(edge.endNodeId).add(edge.id);
        if (this.config.enableIndexing) {
          this.updateEdgeIndex(edge.id, edge, "add");
        }
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Added edge:`, {
          id: edge.id,
          type: edge.type,
          from: edge.startNodeId,
          to: edge.endNodeId
        });
      }
      async addEdges(edges) {
        this.ensureConnected();
        if (this.stats.edgeCount + edges.length > this.config.maxEdges) {
          throw new GraphValidationError(
            `Adding ${edges.length} edges would exceed maximum (${this.config.maxEdges})`,
            "edge"
          );
        }
        for (const edge of edges) {
          this.validateEdge(edge);
          if (this.edges.has(edge.id)) {
            throw new GraphValidationError(`Edge with ID '${edge.id}' already exists`, "edge");
          }
        }
        for (const edge of edges) {
          await this.addEdge(edge);
        }
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Added ${edges.length} edges`);
      }
      async getEdge(edgeId) {
        this.ensureConnected();
        const entry = this.edges.get(edgeId);
        if (!entry) {
          return null;
        }
        return this.deepClone(entry.data);
      }
      async updateEdge(edgeId, properties) {
        this.ensureConnected();
        const entry = this.edges.get(edgeId);
        if (!entry) {
          throw new EdgeNotFoundError(ERROR_MESSAGES5.EDGE_NOT_FOUND, edgeId);
        }
        if (this.config.enableIndexing) {
          this.updateEdgeIndex(edgeId, entry.data, "remove");
        }
        const updatedEdge = {
          ...entry.data,
          properties: { ...entry.data.properties, ...properties }
        };
        entry.data = updatedEdge;
        entry.updatedAt = Date.now();
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        if (this.config.enableIndexing) {
          this.updateEdgeIndex(edgeId, updatedEdge, "add");
        }
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Updated edge:`, { id: edgeId });
      }
      async deleteEdge(edgeId) {
        this.ensureConnected();
        const entry = this.edges.get(edgeId);
        if (!entry) {
          return;
        }
        const edge = entry.data;
        this.outgoingEdges.get(edge.startNodeId)?.delete(edgeId);
        this.incomingEdges.get(edge.endNodeId)?.delete(edgeId);
        if (this.config.enableIndexing) {
          this.updateEdgeIndex(edgeId, edge, "remove");
        }
        this.edges.delete(edgeId);
        this.stats.edgeCount--;
        this.stats.operations++;
        this.stats.lastOperation = Date.now();
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Deleted edge:`, { id: edgeId });
      }
      async findEdges(filters, edgeType, limit) {
        this.ensureConnected();
        const maxResults = limit ?? DEFAULTS3.QUERY_LIMIT;
        const results = [];
        let candidateIds = /* @__PURE__ */ new Set();
        if (edgeType && this.config.enableIndexing) {
          candidateIds = this.edgeIndex.edgeTypes.get(edgeType) || /* @__PURE__ */ new Set();
        } else {
          candidateIds = new Set(this.edges.keys());
        }
        for (const edgeId of candidateIds) {
          if (results.length >= maxResults) break;
          const entry = this.edges.get(edgeId);
          if (!entry) continue;
          const edge = entry.data;
          if (edgeType && edge.type !== edgeType) {
            continue;
          }
          if (filters && !this.matchesEdgeFilters(edge, filters)) {
            continue;
          }
          results.push(this.deepClone(edge));
        }
        this.stats.queriesExecuted++;
        this.logger.debug(`${LOG_PREFIXES5.IN_MEMORY} Found ${results.length} edges`);
        return results;
      }
      // Advanced operations
      async query(query2) {
        this.ensureConnected();
        const startTime = Date.now();
        let result;
        try {
          switch (query2.type) {
            case "node":
              result = await this.executeNodeQuery(query2);
              break;
            case "edge":
              result = await this.executeEdgeQuery(query2);
              break;
            case "path":
              result = await this.executePathQuery(query2);
              break;
            case "cypher":
              throw new InvalidQueryError("Cypher queries not supported in in-memory backend", query2);
            default:
              throw new InvalidQueryError(`Unsupported query type: ${query2.type}`, query2);
          }
          result.metadata.executionTime = Date.now() - startTime;
          this.stats.queriesExecuted++;
          return result;
        } catch (error) {
          throw new InvalidQueryError(
            `Query execution failed: ${error.message}`,
            query2,
            error
          );
        }
      }
      async getNeighbors(nodeId, direction = "both", edgeTypes, limit) {
        this.ensureConnected();
        if (!this.nodes.has(nodeId)) {
          throw new NodeNotFoundError(ERROR_MESSAGES5.NODE_NOT_FOUND, nodeId);
        }
        const maxResults = limit ?? DEFAULTS3.MAX_NEIGHBORS;
        const results = [];
        const edgeIds = /* @__PURE__ */ new Set();
        if (direction === "out" || direction === "both") {
          const outgoing = this.outgoingEdges.get(nodeId) || /* @__PURE__ */ new Set();
          outgoing.forEach((id) => edgeIds.add(id));
        }
        if (direction === "in" || direction === "both") {
          const incoming = this.incomingEdges.get(nodeId) || /* @__PURE__ */ new Set();
          incoming.forEach((id) => edgeIds.add(id));
        }
        for (const edgeId of edgeIds) {
          if (results.length >= maxResults) break;
          const edgeEntry = this.edges.get(edgeId);
          if (!edgeEntry) continue;
          const edge = edgeEntry.data;
          if (edgeTypes && edgeTypes.length > 0 && !edgeTypes.includes(edge.type)) {
            continue;
          }
          const neighborId = edge.startNodeId === nodeId ? edge.endNodeId : edge.startNodeId;
          const neighborEntry = this.nodes.get(neighborId);
          if (!neighborEntry) continue;
          results.push({
            node: this.deepClone(neighborEntry.data),
            edge: this.deepClone(edge)
          });
        }
        this.logger.debug(
          `${LOG_PREFIXES5.IN_MEMORY} Found ${results.length} neighbors for node ${nodeId}`
        );
        return results;
      }
      async findPath(startNodeId, endNodeId, maxDepth = DEFAULTS3.MAX_PATH_DEPTH, edgeTypes) {
        this.ensureConnected();
        if (!this.nodes.has(startNodeId)) {
          throw new NodeNotFoundError(`Start node not found`, startNodeId);
        }
        if (!this.nodes.has(endNodeId)) {
          throw new NodeNotFoundError(`End node not found`, endNodeId);
        }
        const queue = [
          { nodeId: startNodeId, path: [startNodeId], edgePath: [] }
        ];
        const visited = /* @__PURE__ */ new Set();
        while (queue.length > 0) {
          const current = queue.shift();
          if (current.nodeId === endNodeId) {
            const nodes = [];
            const edges = [];
            for (const nodeId of current.path) {
              const nodeEntry = this.nodes.get(nodeId);
              if (nodeEntry) {
                nodes.push(this.deepClone(nodeEntry.data));
              }
            }
            for (const edgeId of current.edgePath) {
              const edgeEntry = this.edges.get(edgeId);
              if (edgeEntry) {
                edges.push(this.deepClone(edgeEntry.data));
              }
            }
            this.logger.debug(
              `${LOG_PREFIXES5.IN_MEMORY} Found path from ${startNodeId} to ${endNodeId} with ${edges.length} edges`
            );
            return { nodes, edges };
          }
          if (current.path.length >= maxDepth || visited.has(current.nodeId)) {
            continue;
          }
          visited.add(current.nodeId);
          const outgoing = this.outgoingEdges.get(current.nodeId) || /* @__PURE__ */ new Set();
          for (const edgeId of outgoing) {
            const edgeEntry = this.edges.get(edgeId);
            if (!edgeEntry) continue;
            const edge = edgeEntry.data;
            if (edgeTypes && edgeTypes.length > 0 && !edgeTypes.includes(edge.type)) {
              continue;
            }
            const nextNodeId = edge.endNodeId;
            if (!visited.has(nextNodeId)) {
              queue.push({
                nodeId: nextNodeId,
                path: [...current.path, nextNodeId],
                edgePath: [...current.edgePath, edgeId]
              });
            }
          }
        }
        this.logger.debug(
          `${LOG_PREFIXES5.IN_MEMORY} No path found from ${startNodeId} to ${endNodeId}`
        );
        return null;
      }
      // Graph management
      async clearGraph() {
        this.ensureConnected();
        this.clear();
        this.logger.info(`${LOG_PREFIXES5.IN_MEMORY} Graph cleared`);
      }
      async getStatistics() {
        this.ensureConnected();
        const labelCounts = {};
        const edgeTypeCounts = {};
        for (const entry of this.nodes.values()) {
          for (const label of entry.data.labels) {
            labelCounts[label] = (labelCounts[label] || 0) + 1;
          }
        }
        for (const entry of this.edges.values()) {
          const type = entry.data.type;
          edgeTypeCounts[type] = (edgeTypeCounts[type] || 0) + 1;
        }
        return {
          nodeCount: this.stats.nodeCount,
          edgeCount: this.stats.edgeCount,
          labelCounts,
          edgeTypeCounts
        };
      }
      // Private helper methods
      ensureConnected() {
        if (!this.connected) {
          throw new KnowledgeGraphConnectionError(ERROR_MESSAGES5.NOT_CONNECTED, "in-memory");
        }
      }
      validateNode(node) {
        if (!node.id || typeof node.id !== "string") {
          throw new GraphValidationError("Node ID must be a non-empty string", "node");
        }
        if (!Array.isArray(node.labels) || node.labels.length === 0) {
          throw new GraphValidationError("Node must have at least one label", "node");
        }
        if (!node.properties || typeof node.properties !== "object") {
          throw new GraphValidationError("Node properties must be an object", "node");
        }
      }
      validateEdge(edge) {
        if (!edge.id || typeof edge.id !== "string") {
          throw new GraphValidationError("Edge ID must be a non-empty string", "edge");
        }
        if (!edge.type || typeof edge.type !== "string") {
          throw new GraphValidationError("Edge type must be a non-empty string", "edge");
        }
        if (!edge.startNodeId || typeof edge.startNodeId !== "string") {
          throw new GraphValidationError("Edge startNodeId must be a non-empty string", "edge");
        }
        if (!edge.endNodeId || typeof edge.endNodeId !== "string") {
          throw new GraphValidationError("Edge endNodeId must be a non-empty string", "edge");
        }
        if (!edge.properties || typeof edge.properties !== "object") {
          throw new GraphValidationError("Edge properties must be an object", "edge");
        }
      }
      deepClone(obj) {
        return JSON.parse(JSON.stringify(obj));
      }
      clear() {
        this.nodes.clear();
        this.edges.clear();
        this.outgoingEdges.clear();
        this.incomingEdges.clear();
        this.clearIndexes();
        this.stats = {
          nodeCount: 0,
          edgeCount: 0,
          operations: 0,
          queriesExecuted: 0,
          lastOperation: 0
        };
      }
      clearIndexes() {
        this.nodeIndex.labels.clear();
        this.nodeIndex.edgeTypes.clear();
        this.nodeIndex.properties.clear();
        this.edgeIndex.labels.clear();
        this.edgeIndex.edgeTypes.clear();
        this.edgeIndex.properties.clear();
      }
      updateNodeIndex(nodeId, node, operation) {
        if (!this.config.enableIndexing) return;
        const { labels, properties } = this.nodeIndex;
        for (const label of node.labels) {
          if (!labels.has(label)) {
            labels.set(label, /* @__PURE__ */ new Set());
          }
          const labelSet = labels.get(label);
          if (operation === "add") {
            labelSet.add(nodeId);
          } else {
            labelSet.delete(nodeId);
            if (labelSet.size === 0) {
              labels.delete(label);
            }
          }
        }
        for (const [key, value] of Object.entries(node.properties)) {
          if (!properties.has(key)) {
            properties.set(key, /* @__PURE__ */ new Map());
          }
          const propMap = properties.get(key);
          if (!propMap.has(value)) {
            propMap.set(value, /* @__PURE__ */ new Set());
          }
          const valueSet = propMap.get(value);
          if (operation === "add") {
            valueSet.add(nodeId);
          } else {
            valueSet.delete(nodeId);
            if (valueSet.size === 0) {
              propMap.delete(value);
              if (propMap.size === 0) {
                properties.delete(key);
              }
            }
          }
        }
      }
      updateEdgeIndex(edgeId, edge, operation) {
        if (!this.config.enableIndexing) return;
        const { edgeTypes, properties } = this.edgeIndex;
        if (!edgeTypes.has(edge.type)) {
          edgeTypes.set(edge.type, /* @__PURE__ */ new Set());
        }
        const typeSet = edgeTypes.get(edge.type);
        if (operation === "add") {
          typeSet.add(edgeId);
        } else {
          typeSet.delete(edgeId);
          if (typeSet.size === 0) {
            edgeTypes.delete(edge.type);
          }
        }
        for (const [key, value] of Object.entries(edge.properties)) {
          if (!properties.has(key)) {
            properties.set(key, /* @__PURE__ */ new Map());
          }
          const propMap = properties.get(key);
          if (!propMap.has(value)) {
            propMap.set(value, /* @__PURE__ */ new Set());
          }
          const valueSet = propMap.get(value);
          if (operation === "add") {
            valueSet.add(edgeId);
          } else {
            valueSet.delete(edgeId);
            if (valueSet.size === 0) {
              propMap.delete(value);
              if (propMap.size === 0) {
                properties.delete(key);
              }
            }
          }
        }
      }
      matchesNodeFilters(node, filters) {
        for (const [key, filter] of Object.entries(filters)) {
          const value = node.properties[key];
          if (!this.matchesFilter(value, filter)) {
            return false;
          }
        }
        return true;
      }
      matchesEdgeFilters(edge, filters) {
        for (const [key, filter] of Object.entries(filters)) {
          const value = edge.properties[key];
          if (!this.matchesFilter(value, filter)) {
            return false;
          }
        }
        return true;
      }
      matchesFilter(value, filter) {
        if (filter === null || filter === void 0) {
          return value === filter;
        }
        if (typeof filter === "object" && !Array.isArray(filter)) {
          if ("gte" in filter && value < filter.gte) return false;
          if ("gt" in filter && value <= filter.gt) return false;
          if ("lte" in filter && value > filter.lte) return false;
          if ("lt" in filter && value >= filter.lt) return false;
          if ("any" in filter) {
            return Array.isArray(filter.any) && filter.any.includes(value);
          }
          if ("all" in filter) {
            return Array.isArray(filter.all) && Array.isArray(value) && filter.all.every((item) => value.includes(item));
          }
          return true;
        }
        return value === filter;
      }
      async executeNodeQuery(query2) {
        const { pattern, limit } = query2;
        const labels = pattern?.labels;
        const properties = pattern?.properties;
        const nodes = await this.findNodes(properties, labels, limit);
        return {
          nodes,
          edges: [],
          metadata: {
            totalCount: nodes.length,
            queryType: "node"
          }
        };
      }
      async executeEdgeQuery(query2) {
        const { pattern, limit } = query2;
        const edgeType = pattern?.type;
        const properties = pattern?.properties;
        const edges = await this.findEdges(properties, edgeType, limit);
        return {
          nodes: [],
          edges,
          metadata: {
            totalCount: edges.length,
            queryType: "edge"
          }
        };
      }
      async executePathQuery(query2) {
        const { pattern, parameters } = query2;
        const startNode = pattern?.startNode;
        const endNode = pattern?.endNode;
        const maxDepth = parameters?.maxDepth ?? DEFAULTS3.MAX_PATH_DEPTH;
        if (!startNode?.id || !endNode?.id) {
          throw new InvalidQueryError("Path query requires startNode.id and endNode.id", query2);
        }
        const path12 = await this.findPath(startNode.id, endNode.id, maxDepth);
        if (path12) {
          return {
            nodes: path12.nodes,
            edges: path12.edges,
            paths: [
              {
                nodes: path12.nodes,
                edges: path12.edges,
                length: path12.edges.length
              }
            ],
            metadata: {
              totalCount: 1,
              queryType: "path"
            }
          };
        }
        return {
          nodes: [],
          edges: [],
          paths: [],
          metadata: {
            totalCount: 0,
            queryType: "path"
          }
        };
      }
      // Debug methods for testing
      getInternalStats() {
        return {
          ...this.stats,
          memoryUsage: {
            nodes: this.nodes.size,
            edges: this.edges.size,
            outgoingEdges: this.outgoingEdges.size,
            incomingEdges: this.incomingEdges.size
          },
          indexSizes: this.config.enableIndexing ? {
            nodeLabels: this.nodeIndex.labels.size,
            nodeProperties: this.nodeIndex.properties.size,
            edgeTypes: this.edgeIndex.edgeTypes.size,
            edgeProperties: this.edgeIndex.properties.size
          } : null
        };
      }
    };
  }
});

// src/core/knowledge_graph/config.ts
var config_exports = {};
__export(config_exports, {
  BackendConfigSchema: () => BackendConfigSchema3,
  InMemoryBackendSchema: () => InMemoryBackendSchema3,
  KnowledgeGraphEnvConfigSchema: () => KnowledgeGraphEnvConfigSchema,
  KnowledgeGraphSchema: () => KnowledgeGraphSchema,
  Neo4jBackendSchema: () => Neo4jBackendSchema,
  parseKnowledgeGraphConfig: () => parseKnowledgeGraphConfig,
  parseKnowledgeGraphConfigFromEnv: () => parseKnowledgeGraphConfigFromEnv,
  validateKnowledgeGraphConfig: () => validateKnowledgeGraphConfig
});
function parseKnowledgeGraphConfig(config2) {
  return KnowledgeGraphSchema.parse(config2);
}
function parseKnowledgeGraphConfigFromEnv(env3 = process.env) {
  const envConfig = KnowledgeGraphEnvConfigSchema.parse({
    KNOWLEDGE_GRAPH_ENABLED: env3.KNOWLEDGE_GRAPH_ENABLED === "true",
    KNOWLEDGE_GRAPH_TYPE: env3.KNOWLEDGE_GRAPH_TYPE || "in-memory",
    KNOWLEDGE_GRAPH_HOST: env3.KNOWLEDGE_GRAPH_HOST,
    KNOWLEDGE_GRAPH_PORT: env3.KNOWLEDGE_GRAPH_PORT ? parseInt(env3.KNOWLEDGE_GRAPH_PORT, 10) : void 0,
    KNOWLEDGE_GRAPH_URI: env3.KNOWLEDGE_GRAPH_URI,
    KNOWLEDGE_GRAPH_USERNAME: env3.KNOWLEDGE_GRAPH_USERNAME,
    KNOWLEDGE_GRAPH_PASSWORD: env3.KNOWLEDGE_GRAPH_PASSWORD,
    KNOWLEDGE_GRAPH_DATABASE: env3.KNOWLEDGE_GRAPH_DATABASE || "neo4j"
  });
  if (!envConfig.KNOWLEDGE_GRAPH_ENABLED) {
    return null;
  }
  if (envConfig.KNOWLEDGE_GRAPH_TYPE === "neo4j") {
    const neo4jConfig = {
      type: "neo4j",
      database: envConfig.KNOWLEDGE_GRAPH_DATABASE
    };
    if (envConfig.KNOWLEDGE_GRAPH_URI) {
      neo4jConfig.uri = envConfig.KNOWLEDGE_GRAPH_URI;
    } else if (envConfig.KNOWLEDGE_GRAPH_HOST && envConfig.KNOWLEDGE_GRAPH_PORT) {
      neo4jConfig.host = envConfig.KNOWLEDGE_GRAPH_HOST;
      neo4jConfig.port = envConfig.KNOWLEDGE_GRAPH_PORT;
    } else {
      throw new Error(
        "For Neo4j backend, either KNOWLEDGE_GRAPH_URI or both KNOWLEDGE_GRAPH_HOST and KNOWLEDGE_GRAPH_PORT must be provided"
      );
    }
    if (!envConfig.KNOWLEDGE_GRAPH_USERNAME || !envConfig.KNOWLEDGE_GRAPH_PASSWORD) {
      throw new Error(
        "For Neo4j backend, both KNOWLEDGE_GRAPH_USERNAME and KNOWLEDGE_GRAPH_PASSWORD must be provided"
      );
    }
    neo4jConfig.username = envConfig.KNOWLEDGE_GRAPH_USERNAME;
    neo4jConfig.password = envConfig.KNOWLEDGE_GRAPH_PASSWORD;
    if (!neo4jConfig.uri && (!neo4jConfig.host || !neo4jConfig.port)) {
      throw new Error(
        "For Neo4j backend, either KNOWLEDGE_GRAPH_URI or both KNOWLEDGE_GRAPH_HOST and KNOWLEDGE_GRAPH_PORT must be provided"
      );
    }
    return parseKnowledgeGraphConfig(neo4jConfig);
  } else {
    return parseKnowledgeGraphConfig({
      type: "in-memory"
    });
  }
}
function validateKnowledgeGraphConfig(config2) {
  try {
    const data = parseKnowledgeGraphConfig(config2);
    return { success: true, data };
  } catch (error) {
    if (error instanceof external_exports.ZodError) {
      return { success: false, errors: error };
    }
    throw error;
  }
}
var BaseBackendSchema2, InMemoryBackendSchema3, Neo4jBackendSchema, BackendConfigSchema3, KnowledgeGraphSchema, KnowledgeGraphEnvConfigSchema;
var init_config6 = __esm({
  "src/core/knowledge_graph/config.ts"() {
    init_cjs_shims();
    init_zod();
    BaseBackendSchema2 = external_exports.object({
      /** Backend type identifier */
      type: external_exports.string(),
      /** Connection timeout in milliseconds */
      timeout: external_exports.number().min(1e3).max(3e5).default(3e4),
      /** Maximum number of retry attempts for failed operations */
      maxRetries: external_exports.number().min(0).max(10).default(3),
      /** Whether to enable connection pooling (if supported by backend) */
      enablePooling: external_exports.boolean().default(true),
      /** Pool size for connections (if pooling is enabled) */
      poolSize: external_exports.number().min(1).max(100).default(10)
    });
    InMemoryBackendSchema3 = BaseBackendSchema2.extend({
      type: external_exports.literal("in-memory"),
      /** Maximum number of nodes to store in memory */
      maxNodes: external_exports.number().min(100).max(1e6).default(1e4),
      /** Maximum number of edges to store in memory */
      maxEdges: external_exports.number().min(100).max(5e6).default(5e4),
      /** Whether to enable indexing for faster lookups */
      enableIndexing: external_exports.boolean().default(true),
      /** Whether to enable automatic garbage collection of orphaned nodes */
      enableGarbageCollection: external_exports.boolean().default(false)
    });
    Neo4jBackendSchema = BaseBackendSchema2.extend({
      type: external_exports.literal("neo4j"),
      /** Neo4j server hostname (alternative to uri) */
      host: external_exports.string().optional(),
      /** Neo4j server port (alternative to uri) */
      port: external_exports.number().min(1).max(65535).optional(),
      /** Complete Neo4j connection URI (alternative to host/port) */
      uri: external_exports.string().url().optional(),
      /** Database username */
      username: external_exports.string().min(1),
      /** Database password */
      password: external_exports.string().min(1),
      /** Database name to connect to */
      database: external_exports.string().min(1).default("neo4j"),
      /** Whether to use encrypted connection */
      encrypted: external_exports.boolean().default(false),
      /** Whether to trust the server certificate (for encrypted connections) */
      trustServerCertificate: external_exports.boolean().default(false),
      /** Maximum transaction retry time in milliseconds */
      maxTransactionRetryTime: external_exports.number().min(1e3).max(3e5).default(3e4),
      /** Connection acquisition timeout in milliseconds */
      connectionAcquisitionTimeout: external_exports.number().min(1e3).max(6e4).default(6e4),
      /** Maximum connection lifetime in milliseconds */
      maxConnectionLifetime: external_exports.number().min(6e4).max(36e5).default(36e5),
      /** Connection liveness check timeout in milliseconds */
      connectionLivenessCheckTimeout: external_exports.number().min(1e3).max(3e4).default(3e4)
    });
    BackendConfigSchema3 = external_exports.discriminatedUnion("type", [
      InMemoryBackendSchema3,
      Neo4jBackendSchema
    ]);
    KnowledgeGraphSchema = external_exports.intersection(
      BackendConfigSchema3,
      external_exports.object({
        /** Whether to enable automatic indexing of node properties */
        enableAutoIndexing: external_exports.boolean().default(true),
        /** Whether to enable performance metrics collection */
        enableMetrics: external_exports.boolean().default(false),
        /** Whether to enable query caching (if supported by backend) */
        enableQueryCache: external_exports.boolean().default(true),
        /** Query cache TTL in milliseconds */
        queryCacheTTL: external_exports.number().min(1e3).max(36e5).default(3e5),
        /** Whether to enable schema validation for nodes and edges */
        enableSchemaValidation: external_exports.boolean().default(true),
        /** Default batch size for bulk operations */
        defaultBatchSize: external_exports.number().min(1).max(1e4).default(1e3)
      })
    );
    KnowledgeGraphEnvConfigSchema = external_exports.object({
      /** Whether knowledge graph is enabled */
      KNOWLEDGE_GRAPH_ENABLED: external_exports.boolean().default(false),
      /** Backend type */
      KNOWLEDGE_GRAPH_TYPE: external_exports.enum(["neo4j", "in-memory"]).default("in-memory"),
      /** Neo4j host */
      KNOWLEDGE_GRAPH_HOST: external_exports.string().optional(),
      /** Neo4j port */
      KNOWLEDGE_GRAPH_PORT: external_exports.number().optional(),
      /** Neo4j URI */
      KNOWLEDGE_GRAPH_URI: external_exports.string().optional(),
      /** Neo4j username */
      KNOWLEDGE_GRAPH_USERNAME: external_exports.string().optional(),
      /** Neo4j password */
      KNOWLEDGE_GRAPH_PASSWORD: external_exports.string().optional(),
      /** Neo4j database name */
      KNOWLEDGE_GRAPH_DATABASE: external_exports.string().default("neo4j")
    });
  }
});

// src/core/knowledge_graph/manager.ts
var KnowledgeGraphManager;
var init_manager7 = __esm({
  "src/core/knowledge_graph/manager.ts"() {
    init_cjs_shims();
    init_logger();
    init_types11();
    init_constants5();
    KnowledgeGraphManager = class _KnowledgeGraphManager {
      graph;
      connected = false;
      config;
      logger;
      // Connection tracking
      connectionAttempts = 0;
      lastConnectionError;
      connectionStartTime = 0;
      // Backend metadata
      backendMetadata = {
        type: "unknown",
        connected: false,
        fallback: false,
        lastHealthCheck: 0
      };
      // Statistics
      stats = {
        totalNodes: 0,
        totalEdges: 0,
        totalQueries: 0,
        totalOperations: 0,
        averageQueryTime: 0,
        lastOperationTime: 0,
        connectionUptime: 0
      };
      // Module loading cache
      static neo4jModule;
      static inMemoryModule;
      // Health check configuration
      healthCheckInterval = TIMEOUTS3.HEALTH_CHECK;
      healthCheckTimer;
      /**
       * Creates a new Knowledge Graph Manager
       *
       * @param config - Knowledge graph configuration
       */
      constructor(config2) {
        this.config = config2;
        this.logger = createLogger({
          level: "debug"
        });
        this.logger.debug(`${LOG_PREFIXES5.MANAGER} Initialized with backend type: ${config2.type}`);
      }
      /**
       * Get the current configuration
       */
      getConfig() {
        return this.config;
      }
      /**
       * Get manager information for monitoring
       */
      getInfo() {
        return {
          connected: this.connected,
          backend: {
            type: this.backendMetadata.type,
            connected: this.backendMetadata.connected,
            fallback: this.backendMetadata.fallback
          },
          connectionAttempts: this.connectionAttempts,
          lastError: this.lastConnectionError?.message
        };
      }
      /**
       * Get the knowledge graph instance
       */
      getGraph() {
        if (!this.connected || !this.graph) {
          return null;
        }
        return this.graph;
      }
      /**
       * Check if the manager is connected and ready
       */
      isConnected() {
        return this.connected && this.graph?.isConnected() === true;
      }
      /**
       * Connect to the knowledge graph backend
       */
      async connect() {
        if (this.connected && this.graph) {
          this.logger.debug(`${LOG_PREFIXES5.MANAGER} Already connected`);
          return this.graph;
        }
        this.connectionAttempts++;
        this.connectionStartTime = Date.now();
        try {
          this.logger.info(`${LOG_PREFIXES5.MANAGER} Connecting to ${this.config.type} backend...`);
          this.graph = await this.createBackend();
          await this.graph.connect();
          if (!this.graph.isConnected()) {
            throw new KnowledgeGraphConnectionError(
              "Backend reports as not connected after connection attempt",
              this.config.type
            );
          }
          this.backendMetadata = {
            type: this.graph.getBackendType(),
            connected: true,
            fallback: false,
            lastHealthCheck: Date.now()
          };
          this.connected = true;
          delete this.lastConnectionError;
          this.startHealthMonitoring();
          await this.updateStatistics();
          this.logger.info(
            `${LOG_PREFIXES5.MANAGER} Connected successfully to ${this.backendMetadata.type} backend`
          );
          return this.graph;
        } catch (error) {
          this.lastConnectionError = error;
          this.backendMetadata.connected = false;
          this.logger.error(`${LOG_PREFIXES5.MANAGER} Connection failed:`, error);
          if (this.config.type !== BACKEND_TYPES3.IN_MEMORY) {
            this.logger.warn(`${LOG_PREFIXES5.MANAGER} Attempting fallback to in-memory backend...`);
            try {
              this.graph = await this.createInMemoryFallback();
              await this.graph.connect();
              this.backendMetadata = {
                type: this.graph.getBackendType(),
                connected: true,
                fallback: true,
                lastHealthCheck: Date.now()
              };
              this.connected = true;
              this.startHealthMonitoring();
              this.logger.warn(`${LOG_PREFIXES5.MANAGER} Connected to fallback in-memory backend`);
              return this.graph;
            } catch (fallbackError) {
              this.logger.error(
                `${LOG_PREFIXES5.MANAGER} Fallback connection also failed:`,
                fallbackError
              );
            }
          }
          throw error;
        }
      }
      /**
       * Disconnect from the knowledge graph backend
       */
      async disconnect() {
        if (!this.connected || !this.graph) {
          this.logger.debug(`${LOG_PREFIXES5.MANAGER} Already disconnected`);
          return;
        }
        try {
          this.stopHealthMonitoring();
          if (this.connectionStartTime > 0) {
            this.stats.connectionUptime = Date.now() - this.connectionStartTime;
          }
          await this.graph.disconnect();
          this.graph = void 0;
          this.connected = false;
          this.backendMetadata.connected = false;
          this.logger.info(`${LOG_PREFIXES5.MANAGER} Disconnected successfully`);
        } catch (error) {
          this.logger.error(`${LOG_PREFIXES5.MANAGER} Error during disconnect:`, error);
          throw error;
        }
      }
      /**
       * Perform health check on the knowledge graph backend
       */
      async healthCheck() {
        const startTime = Date.now();
        let backendHealth = false;
        let backendDetails = {};
        try {
          if (this.graph && this.connected) {
            backendHealth = this.graph.isConnected();
            if (backendHealth) {
              await this.graph.getStatistics();
              backendDetails = {
                status: "healthy",
                latency: Date.now() - startTime
              };
            } else {
              backendDetails = {
                status: "disconnected",
                error: "Backend reports as not connected"
              };
            }
          } else {
            backendDetails = {
              status: "not_initialized",
              error: "Knowledge graph not initialized"
            };
          }
        } catch (error) {
          backendHealth = false;
          backendDetails = {
            status: "error",
            error: error.message,
            latency: Date.now() - startTime
          };
        }
        this.backendMetadata.lastHealthCheck = Date.now();
        const result = {
          backend: backendHealth,
          overall: backendHealth,
          details: {
            backend: backendDetails
          }
        };
        return result;
      }
      /**
       * Get current statistics
       */
      getStats() {
        return { ...this.stats };
      }
      /**
       * Reset statistics
       */
      resetStats() {
        this.stats = {
          totalNodes: 0,
          totalEdges: 0,
          totalQueries: 0,
          totalOperations: 0,
          averageQueryTime: 0,
          lastOperationTime: 0,
          connectionUptime: this.connectionStartTime > 0 ? Date.now() - this.connectionStartTime : 0
        };
        this.logger.debug(`${LOG_PREFIXES5.MANAGER} Statistics reset`);
      }
      /**
       * Execute a knowledge graph operation with statistics tracking
       */
      async executeOperation(operation, operationType = "read") {
        if (!this.graph || !this.connected) {
          throw new KnowledgeGraphConnectionError(
            ERROR_MESSAGES5.NOT_CONNECTED,
            this.backendMetadata.type
          );
        }
        const startTime = Date.now();
        try {
          const result = await operation(this.graph);
          const executionTime = Date.now() - startTime;
          this.updateOperationStats(operationType, executionTime, true);
          return result;
        } catch (error) {
          const executionTime = Date.now() - startTime;
          this.updateOperationStats(operationType, executionTime, false);
          this.logger.error(`${LOG_PREFIXES5.MANAGER} Operation failed:`, error);
          throw error;
        }
      }
      /**
       * Get backend-specific debug information
       */
      getDebugInfo() {
        return {
          config: this.config,
          backendMetadata: this.backendMetadata,
          connectionAttempts: this.connectionAttempts,
          lastConnectionError: this.lastConnectionError?.message,
          stats: this.stats,
          connected: this.connected,
          healthMonitoring: !!this.healthCheckTimer
        };
      }
      // Private methods
      async createBackend() {
        switch (this.config.type) {
          case BACKEND_TYPES3.NEO4J:
            return this.createNeo4jBackend();
          case BACKEND_TYPES3.IN_MEMORY:
            return this.createInMemoryBackend();
          default:
            throw new KnowledgeGraphConnectionError(
              `Unsupported backend type: ${this.config.type}`,
              this.config.type
            );
        }
      }
      async createNeo4jBackend() {
        try {
          if (!_KnowledgeGraphManager.neo4jModule) {
            this.logger.debug(`${LOG_PREFIXES5.MANAGER} Loading Neo4j backend module...`);
            const module2 = await Promise.resolve().then(() => (init_neo4j(), neo4j_exports));
            _KnowledgeGraphManager.neo4jModule = module2;
          }
          const { Neo4jBackend: Neo4jBackend2 } = _KnowledgeGraphManager.neo4jModule;
          return new Neo4jBackend2(this.config);
        } catch (error) {
          throw new KnowledgeGraphConnectionError(
            `Failed to create Neo4j backend: ${error.message}`,
            BACKEND_TYPES3.NEO4J,
            error
          );
        }
      }
      async createInMemoryBackend() {
        try {
          if (!_KnowledgeGraphManager.inMemoryModule) {
            this.logger.debug(`${LOG_PREFIXES5.MANAGER} Loading in-memory backend module...`);
            const module2 = await Promise.resolve().then(() => (init_in_memory3(), in_memory_exports3));
            _KnowledgeGraphManager.inMemoryModule = module2;
          }
          const { InMemoryBackend: InMemoryBackend4 } = _KnowledgeGraphManager.inMemoryModule;
          return new InMemoryBackend4(this.config);
        } catch (error) {
          throw new KnowledgeGraphConnectionError(
            `Failed to create in-memory backend: ${error.message}`,
            BACKEND_TYPES3.IN_MEMORY,
            error
          );
        }
      }
      async createInMemoryFallback() {
        const fallbackConfig = {
          ...this.config,
          type: BACKEND_TYPES3.IN_MEMORY,
          maxNodes: DEFAULTS3.MAX_NODES,
          maxEdges: DEFAULTS3.MAX_EDGES,
          enableIndexing: true,
          enableGarbageCollection: false
        };
        if (!_KnowledgeGraphManager.inMemoryModule) {
          const module2 = await Promise.resolve().then(() => (init_in_memory3(), in_memory_exports3));
          _KnowledgeGraphManager.inMemoryModule = module2;
        }
        const { InMemoryBackend: InMemoryBackend4 } = _KnowledgeGraphManager.inMemoryModule;
        return new InMemoryBackend4(fallbackConfig);
      }
      async updateStatistics() {
        if (!this.graph || !this.connected) {
          return;
        }
        try {
          const graphStats = await this.graph.getStatistics();
          this.stats.totalNodes = graphStats.nodeCount;
          this.stats.totalEdges = graphStats.edgeCount;
          if (this.connectionStartTime > 0) {
            this.stats.connectionUptime = Date.now() - this.connectionStartTime;
          }
        } catch (error) {
          this.logger.warn(`${LOG_PREFIXES5.MANAGER} Failed to update statistics:`, error);
        }
      }
      updateOperationStats(operationType, executionTime, success) {
        this.stats.totalOperations++;
        this.stats.lastOperationTime = Date.now();
        if (operationType === "query") {
          this.stats.totalQueries++;
          const totalQueryTime = this.stats.averageQueryTime * (this.stats.totalQueries - 1);
          this.stats.averageQueryTime = (totalQueryTime + executionTime) / this.stats.totalQueries;
        }
        if (success) {
          this.logger.debug(
            `${LOG_PREFIXES5.MANAGER} ${operationType} operation completed in ${executionTime}ms`
          );
        } else {
          this.logger.warn(
            `${LOG_PREFIXES5.MANAGER} ${operationType} operation failed after ${executionTime}ms`
          );
        }
      }
      startHealthMonitoring() {
        if (this.healthCheckTimer) {
          this.stopHealthMonitoring();
        }
        this.healthCheckTimer = setInterval(async () => {
          try {
            await this.healthCheck();
          } catch (error) {
            this.logger.warn(`${LOG_PREFIXES5.MANAGER} Health check failed:`, error);
          }
        }, this.healthCheckInterval);
        this.logger.debug(
          `${LOG_PREFIXES5.MANAGER} Started health monitoring (interval: ${this.healthCheckInterval}ms)`
        );
      }
      stopHealthMonitoring() {
        if (this.healthCheckTimer) {
          clearInterval(this.healthCheckTimer);
          delete this.healthCheckTimer;
          this.logger.debug(`${LOG_PREFIXES5.MANAGER} Stopped health monitoring`);
        }
      }
      /**
       * Create a new manager instance from environment configuration
       */
      static async createFromEnv() {
        const { parseKnowledgeGraphConfigFromEnv: parseKnowledgeGraphConfigFromEnv2 } = await Promise.resolve().then(() => (init_config6(), config_exports));
        const config2 = parseKnowledgeGraphConfigFromEnv2();
        if (!config2) {
          return null;
        }
        return new _KnowledgeGraphManager(config2);
      }
      /**
       * Create a default in-memory manager for testing/development
       */
      static createDefault() {
        const config2 = {
          type: BACKEND_TYPES3.IN_MEMORY,
          maxNodes: DEFAULTS3.MAX_NODES,
          maxEdges: DEFAULTS3.MAX_EDGES,
          enableIndexing: true,
          enableGarbageCollection: false,
          enableAutoIndexing: true,
          enableMetrics: false,
          enableQueryCache: false,
          queryCacheTTL: DEFAULTS3.QUERY_CACHE_TTL,
          enableSchemaValidation: true,
          defaultBatchSize: DEFAULTS3.BATCH_SIZE,
          timeout: DEFAULTS3.CONNECTION_TIMEOUT,
          maxRetries: DEFAULTS3.MAX_RETRIES,
          enablePooling: true,
          poolSize: DEFAULTS3.POOL_SIZE
        };
        return new _KnowledgeGraphManager(config2);
      }
    };
  }
});

// src/core/knowledge_graph/factory.ts
async function createKnowledgeGraph(config2) {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  logger2.debug(`${LOG_PREFIXES5.FACTORY} Creating knowledge graph system`, {
    type: config2.type,
    enableAutoIndexing: config2.enableAutoIndexing,
    enableMetrics: config2.enableMetrics
  });
  const manager = new KnowledgeGraphManager(config2);
  try {
    await manager.connect();
    const graph = manager.getGraph();
    if (!graph) {
      throw new Error("Failed to get knowledge graph instance after connection");
    }
    logger2.info(`${LOG_PREFIXES5.FACTORY} Knowledge graph system created successfully`, {
      type: manager.getInfo().backend.type,
      connected: manager.isConnected(),
      fallback: manager.getInfo().backend.fallback
    });
    return { manager, graph };
  } catch (error) {
    await manager.disconnect().catch(() => {
    });
    logger2.error(`${LOG_PREFIXES5.FACTORY} Failed to create knowledge graph system`, {
      error: error instanceof Error ? error.message : String(error)
    });
    throw error;
  }
}
async function createKnowledgeGraphFromEnv() {
  const logger2 = createLogger({ level: env.CIPHER_LOG_LEVEL });
  const config2 = parseKnowledgeGraphConfigFromEnv();
  if (!config2) {
    logger2.info(`${LOG_PREFIXES5.FACTORY} Knowledge graph is disabled in environment`);
    return null;
  }
  logger2.info(`${LOG_PREFIXES5.FACTORY} Creating knowledge graph from environment`, {
    type: config2.type,
    enableAutoIndexing: config2.enableAutoIndexing,
    enableMetrics: config2.enableMetrics
  });
  return createKnowledgeGraph(config2);
}
var init_factory8 = __esm({
  "src/core/knowledge_graph/factory.ts"() {
    init_cjs_shims();
    init_manager7();
    init_config6();
    init_logger();
    init_constants5();
    init_env();
  }
});

// src/core/events/typed-event-emitter.ts
var import_events2, TypedEventEmitter;
var init_typed_event_emitter = __esm({
  "src/core/events/typed-event-emitter.ts"() {
    init_cjs_shims();
    import_events2 = __toESM(require("events"), 1);
    init_logger();
    TypedEventEmitter = class {
      emitter = new import_events2.default();
      maxListeners;
      enableLogging;
      listenerCount = /* @__PURE__ */ new Map();
      constructor(options = {}) {
        this.maxListeners = options.maxListeners ?? 100;
        this.enableLogging = options.enableLogging ?? false;
        this.emitter.setMaxListeners(this.maxListeners);
      }
      /**
       * Emit an event with type safety
       */
      emit(event, data) {
        if (this.enableLogging) {
          logger.debug("Event emitted", { event, data });
        }
        this.emitter.emit(event, data);
      }
      /**
       * Add a typed event listener with AbortController support
       */
      on(event, listener, options = {}) {
        const wrappedListener = this.wrapListener(listener, event, options);
        if (options.once) {
          this.emitter.once(event, wrappedListener);
        } else {
          this.emitter.on(event, wrappedListener);
        }
        const currentCount = this.listenerCount.get(event) || 0;
        this.listenerCount.set(event, currentCount + 1);
        if (options.signal) {
          options.signal.addEventListener("abort", () => {
            this.off(event, listener);
          });
        }
      }
      /**
       * Add a one-time event listener
       */
      once(event, listener, options = {}) {
        this.on(event, listener, { ...options, once: true });
      }
      /**
       * Remove an event listener
       */
      off(event, listener) {
        this.emitter.off(event, listener);
        const currentCount = this.listenerCount.get(event) || 0;
        if (currentCount > 0) {
          this.listenerCount.set(event, currentCount - 1);
        }
      }
      /**
       * Remove all listeners for an event
       */
      removeAllListeners(event) {
        if (event) {
          this.emitter.removeAllListeners(event);
          this.listenerCount.set(event, 0);
        } else {
          this.emitter.removeAllListeners();
          this.listenerCount.clear();
        }
      }
      /**
       * Get the number of listeners for an event
       */
      listenerCountFor(event) {
        return this.listenerCount.get(event) || 0;
      }
      /**
       * Get all event names that have listeners
       */
      eventNames() {
        return this.emitter.eventNames();
      }
      /**
       * Wait for a specific event to be emitted
       */
      waitFor(event, options = {}) {
        return new Promise((resolve3, reject) => {
          const timeoutMs = options.timeout ?? 3e4;
          let timeoutId;
          const cleanup = () => {
            if (timeoutId) {
              clearTimeout(timeoutId);
            }
          };
          const listener = (data) => {
            cleanup();
            resolve3(data);
          };
          if (timeoutMs > 0) {
            timeoutId = setTimeout(() => {
              this.off(event, listener);
              reject(new Error(`Event '${event}' timeout after ${timeoutMs}ms`));
            }, timeoutMs);
          }
          if (options.signal) {
            options.signal.addEventListener("abort", () => {
              cleanup();
              this.off(event, listener);
              reject(new Error("Event wait aborted"));
            });
          }
          this.once(event, listener);
        });
      }
      /**
       * Dispose of the event emitter and clean up resources
       */
      dispose() {
        this.removeAllListeners();
        this.emitter.removeAllListeners();
      }
      /**
       * Wrap listener with error handling and async support
       */
      wrapListener(listener, event, _options) {
        return (data) => {
          try {
            const result = listener(data);
            if (result instanceof Promise) {
              result.catch((error) => {
                logger.error("Error in async event listener", {
                  event,
                  error: error.message,
                  stack: error.stack
                });
              });
            }
          } catch (error) {
            logger.error("Error in event listener", {
              event,
              error: error instanceof Error ? error.message : String(error),
              stack: error instanceof Error ? error.stack : void 0
            });
          }
        };
      }
    };
  }
});

// src/core/events/service-event-bus.ts
var import_uuid13, ServiceEventBus;
var init_service_event_bus = __esm({
  "src/core/events/service-event-bus.ts"() {
    init_cjs_shims();
    init_typed_event_emitter();
    init_logger();
    import_uuid13 = require("uuid");
    ServiceEventBus = class extends TypedEventEmitter {
      instanceId;
      startTime;
      eventHistory = [];
      enablePersistence;
      maxHistorySize = 1e4;
      eventPersistence;
      constructor(options = {}) {
        super({
          maxListeners: options.maxListeners ?? 200,
          enableLogging: options.enableLogging ?? true
        });
        this.instanceId = (0, import_uuid13.v4)();
        this.startTime = Date.now();
        this.enablePersistence = options.enablePersistence ?? false;
        this.eventPersistence = options.eventPersistence;
        logger.debug("ServiceEventBus initialized", {
          instanceId: this.instanceId,
          maxListeners: options.maxListeners ?? 200,
          enablePersistence: this.enablePersistence
        });
      }
      /**
       * Emit a service event with metadata and optional persistence
       */
      emitServiceEvent(event, data, metadata = {}) {
        const envelope = {
          id: (0, import_uuid13.v4)(),
          type: event,
          data,
          metadata: {
            timestamp: Date.now(),
            source: "service",
            priority: "normal",
            ...metadata
          }
        };
        if (this.enablePersistence) {
          this.addToHistory(envelope);
        }
        if (this.eventPersistence) {
          void this.eventPersistence.store(envelope);
        }
        this.emit(event, data);
        if (envelope.metadata.priority === "high") {
          logger.info("High priority service event emitted", {
            eventType: event,
            eventId: envelope.id,
            instanceId: this.instanceId
          });
        }
      }
      /**
       * Get event history (if persistence is enabled)
       */
      getEventHistory(filter) {
        if (!this.enablePersistence) {
          return [];
        }
        let history = this.eventHistory;
        if (filter) {
          if (filter.eventType) {
            history = history.filter((event) => event.type === filter.eventType);
          }
          if (filter.since) {
            history = history.filter((event) => event.metadata.timestamp >= filter.since);
          }
          if (filter.limit) {
            history = history.slice(-filter.limit);
          }
        }
        return history;
      }
      /**
       * Get service event bus statistics
       */
      getStatistics() {
        const eventTypes = {};
        const activeListeners = {};
        this.eventHistory.forEach((event) => {
          eventTypes[event.type] = (eventTypes[event.type] || 0) + 1;
        });
        this.eventNames().forEach((eventName) => {
          activeListeners[eventName] = this.listenerCountFor(eventName);
        });
        return {
          instanceId: this.instanceId,
          uptime: Date.now() - this.startTime,
          totalEvents: this.eventHistory.length,
          eventTypes,
          activeListeners
        };
      }
      /**
       * Clear event history
       */
      clearHistory() {
        this.eventHistory = [];
        logger.info("Service event history cleared", {
          instanceId: this.instanceId
        });
      }
      /**
       * Dispose of the service event bus
       */
      dispose() {
        this.clearHistory();
        super.dispose();
        logger.info("ServiceEventBus disposed", {
          instanceId: this.instanceId
        });
      }
      /**
       * Add event to history with size management
       */
      addToHistory(envelope) {
        this.eventHistory.push(envelope);
        if (this.eventHistory.length > this.maxHistorySize) {
          this.eventHistory = this.eventHistory.slice(-this.maxHistorySize);
        }
      }
      /**
       * Get instance ID
       */
      getInstanceId() {
        return this.instanceId;
      }
      /**
       * Get uptime in milliseconds
       */
      getUptime() {
        return Date.now() - this.startTime;
      }
    };
  }
});

// src/core/events/session-event-bus.ts
var import_uuid14, SessionEventBus;
var init_session_event_bus = __esm({
  "src/core/events/session-event-bus.ts"() {
    init_cjs_shims();
    init_typed_event_emitter();
    init_logger();
    import_uuid14 = require("uuid");
    SessionEventBus = class extends TypedEventEmitter {
      sessionId;
      createdAt;
      eventHistory = [];
      enablePersistence;
      maxHistorySize;
      eventPersistence;
      isDisposed = false;
      constructor(options) {
        super({
          maxListeners: options.maxListeners ?? 100,
          enableLogging: options.enableLogging ?? true
        });
        this.sessionId = options.sessionId;
        this.createdAt = Date.now();
        this.enablePersistence = options.enablePersistence ?? false;
        this.maxHistorySize = options.maxHistorySize ?? 1e3;
        this.eventPersistence = options.eventPersistence;
        logger.debug("SessionEventBus initialized", {
          sessionId: this.sessionId,
          maxListeners: options.maxListeners ?? 100,
          enablePersistence: this.enablePersistence
        });
      }
      /**
       * Emit a session event with metadata and optional persistence
       */
      emitSessionEvent(event, data, metadata = {}) {
        if (this.isDisposed) {
          logger.warn("Attempted to emit event on disposed SessionEventBus", {
            sessionId: this.sessionId,
            eventType: event
          });
          return;
        }
        const envelope = {
          id: (0, import_uuid14.v4)(),
          type: event,
          data,
          metadata: {
            timestamp: Date.now(),
            sessionId: this.sessionId,
            source: "session",
            priority: "normal",
            ...metadata
          }
        };
        if (this.enablePersistence) {
          this.addToHistory(envelope);
        }
        if (this.eventPersistence) {
          void this.eventPersistence.store(envelope);
        }
        this.emit(event, data);
        if (this.shouldLogEvent(event)) {
          logger.info("Session event emitted", {
            sessionId: this.sessionId,
            eventType: event,
            eventId: envelope.id
          });
        }
      }
      /**
       * Get session event history (if persistence is enabled)
       */
      getEventHistory(filter) {
        if (!this.enablePersistence) {
          return [];
        }
        let history = this.eventHistory;
        if (filter) {
          if (filter.eventType) {
            history = history.filter((event) => event.type === filter.eventType);
          }
          if (filter.since) {
            history = history.filter((event) => event.metadata.timestamp >= filter.since);
          }
          if (filter.limit) {
            history = history.slice(-filter.limit);
          }
        }
        return history;
      }
      /**
       * Get session event bus statistics
       */
      getStatistics() {
        const eventTypes = {};
        const activeListeners = {};
        const now = Date.now();
        const oneMinuteAgo = now - 6e4;
        const oneHourAgo = now - 36e5;
        this.eventHistory.forEach((event) => {
          eventTypes[event.type] = (eventTypes[event.type] || 0) + 1;
        });
        this.eventNames().forEach((eventName) => {
          activeListeners[eventName] = this.listenerCountFor(eventName);
        });
        const recentEvents = this.eventHistory.filter((event) => event.metadata.timestamp >= oneHourAgo);
        const eventsInLastMinute = recentEvents.filter(
          (event) => event.metadata.timestamp >= oneMinuteAgo
        ).length;
        const eventsInLastHour = recentEvents.length;
        const lastEvent = this.eventHistory.length > 0 ? this.eventHistory[this.eventHistory.length - 1] : void 0;
        const lastEventTime = lastEvent ? lastEvent.metadata.timestamp : void 0;
        return {
          sessionId: this.sessionId,
          age: now - this.createdAt,
          totalEvents: this.eventHistory.length,
          eventTypes,
          activeListeners,
          recentActivity: {
            ...lastEventTime !== void 0 ? { lastEventTime } : {},
            eventsInLastMinute,
            eventsInLastHour
          }
        };
      }
      /**
       * Clear event history
       */
      clearHistory() {
        this.eventHistory = [];
        logger.debug("Session event history cleared", {
          sessionId: this.sessionId
        });
      }
      /**
       * Get session ID
       */
      getSessionId() {
        return this.sessionId;
      }
      /**
       * Get session age in milliseconds
       */
      getAge() {
        return Date.now() - this.createdAt;
      }
      /**
       * Check if session event bus is disposed
       */
      isSessionDisposed() {
        return this.isDisposed;
      }
      /**
       * Dispose of the session event bus
       */
      dispose() {
        if (this.isDisposed) {
          return;
        }
        this.isDisposed = true;
        this.clearHistory();
        super.dispose();
        logger.debug("SessionEventBus disposed", {
          sessionId: this.sessionId,
          age: this.getAge()
        });
      }
      /**
       * Add event to history with size management
       */
      addToHistory(envelope) {
        this.eventHistory.push(envelope);
        if (this.eventHistory.length > this.maxHistorySize) {
          this.eventHistory = this.eventHistory.slice(-this.maxHistorySize);
        }
      }
      /**
       * Determine if an event should be logged based on its importance
       */
      shouldLogEvent(event) {
        const importantEvents = [
          "session:created",
          "session:expired",
          "session:deleted",
          "tool:executionFailed",
          "llm:responseError"
        ];
        return importantEvents.includes(event);
      }
      /**
       * Get events matching a pattern
       */
      getEventsByPattern(pattern) {
        if (!this.enablePersistence) {
          return [];
        }
        return this.eventHistory.filter((event) => pattern.test(event.type));
      }
      /**
       * Get the most recent event of a specific type
       */
      getLastEvent(eventType) {
        if (!this.enablePersistence) {
          return void 0;
        }
        const events = this.eventHistory.filter((event) => event.type === eventType);
        return events.length > 0 ? events[events.length - 1] : void 0;
      }
      /**
       * Count events of a specific type
       */
      countEvents(eventType) {
        if (!this.enablePersistence) {
          return 0;
        }
        return this.eventHistory.filter((event) => event.type === eventType).length;
      }
    };
  }
});

// src/core/events/filtering.ts
var EventFilterManager, CommonFilters;
var init_filtering = __esm({
  "src/core/events/filtering.ts"() {
    init_cjs_shims();
    init_logger();
    EventFilterManager = class {
      filters = /* @__PURE__ */ new Map();
      stats = {
        totalEventsProcessed: 0,
        totalEventsFiltered: 0,
        filteringRate: 0,
        averageFilteringTime: 0,
        filterStats: {}
      };
      executionTimes = [];
      /**
       * Register a new event filter
       */
      registerFilter(config2) {
        this.filters.set(config2.name, {
          priority: 0,
          ...config2
        });
        this.stats.filterStats[config2.name] = {
          eventsProcessed: 0,
          eventsFiltered: 0,
          averageExecutionTime: 0
        };
        logger.debug("Event filter registered", {
          name: config2.name,
          description: config2.description,
          enabled: config2.enabled
        });
      }
      /**
       * Unregister an event filter
       */
      unregisterFilter(name) {
        const removed = this.filters.delete(name);
        if (removed) {
          delete this.stats.filterStats[name];
          logger.debug("Event filter unregistered", { name });
        }
        return removed;
      }
      /**
       * Enable or disable a filter
       */
      setFilterEnabled(name, enabled) {
        const filter = this.filters.get(name);
        if (filter) {
          filter.enabled = enabled;
          logger.debug("Event filter state changed", { name, enabled });
        }
      }
      /**
       * Update filter priority
       */
      setFilterPriority(name, priority) {
        const filter = this.filters.get(name);
        if (filter) {
          filter.priority = priority;
          logger.debug("Event filter priority changed", { name, priority });
        }
      }
      /**
       * Apply all enabled filters to an event
       * Returns true if event should be processed, false if it should be filtered out
       */
      shouldProcessEvent(event) {
        if (this.filters.size === 0) {
          return true;
        }
        const startTime = Date.now();
        this.stats.totalEventsProcessed++;
        try {
          const enabledFilters = Array.from(this.filters.values()).filter((f) => f.enabled).sort((a, b) => (b.priority || 0) - (a.priority || 0));
          if (enabledFilters.length === 0) {
            this.updateOverallStats(Date.now() - startTime);
            return true;
          }
          for (const filterConfig of enabledFilters) {
            const filterStartTime = Date.now();
            if (this.stats.filterStats[filterConfig.name]) {
              this.stats.filterStats[filterConfig.name].eventsProcessed++;
            }
            try {
              const shouldPass = filterConfig.filter(event);
              const filterExecutionTime = Date.now() - filterStartTime;
              const filterStats = this.stats.filterStats[filterConfig.name];
              if (filterStats) {
                filterStats.averageExecutionTime = (filterStats.averageExecutionTime * (filterStats.eventsProcessed - 1) + filterExecutionTime) / filterStats.eventsProcessed;
              }
              if (!shouldPass) {
                this.stats.totalEventsFiltered++;
                if (this.stats.filterStats[filterConfig.name]) {
                  this.stats.filterStats[filterConfig.name].eventsFiltered++;
                }
                this.updateOverallStats(Date.now() - startTime);
                return false;
              }
            } catch (error) {
              logger.warn("Event filter execution failed", {
                filterName: filterConfig.name,
                eventType: event.type,
                error: error instanceof Error ? error.message : String(error)
              });
            }
          }
          this.updateOverallStats(Date.now() - startTime);
          return true;
        } catch (error) {
          logger.error("Error during event filtering", {
            eventType: event.type,
            error: error instanceof Error ? error.message : String(error)
          });
          this.updateOverallStats(Date.now() - startTime);
          return true;
        }
      }
      /**
       * Check if any filters are registered
       */
      hasFilters() {
        return this.filters.size > 0;
      }
      /**
       * Get current filtering statistics
       */
      getStats() {
        if (this.stats.totalEventsProcessed > 0) {
          this.stats.filteringRate = this.stats.totalEventsFiltered / this.stats.totalEventsProcessed * 100;
        }
        return { ...this.stats };
      }
      /**
       * Reset all statistics
       */
      resetStats() {
        this.stats = {
          totalEventsProcessed: 0,
          totalEventsFiltered: 0,
          filteringRate: 0,
          averageFilteringTime: 0,
          filterStats: {}
        };
        for (const filterName of this.filters.keys()) {
          this.stats.filterStats[filterName] = {
            eventsProcessed: 0,
            eventsFiltered: 0,
            averageExecutionTime: 0
          };
        }
        this.executionTimes = [];
        logger.debug("Event filtering statistics reset");
      }
      /**
       * Get list of registered filters
       */
      getFilters() {
        return Array.from(this.filters.values());
      }
      /**
       * Get a specific filter configuration
       */
      getFilter(name) {
        return this.filters.get(name);
      }
      updateOverallStats(executionTime) {
        this.executionTimes.push(executionTime);
        if (this.executionTimes.length > 1e3) {
          this.executionTimes = this.executionTimes.slice(-1e3);
        }
        this.stats.averageFilteringTime = this.executionTimes.reduce((sum, time) => sum + time, 0) / this.executionTimes.length;
      }
    };
    CommonFilters = class {
      /**
       * Filter events by type
       */
      static byEventType(...allowedTypes) {
        return (event) => allowedTypes.includes(event.type);
      }
      /**
       * Filter events by session ID
       */
      static bySessionId(...allowedSessionIds) {
        return (event) => {
          if (!event.metadata.sessionId) return false;
          return allowedSessionIds.includes(event.metadata.sessionId);
        };
      }
      /**
       * Filter events by source
       */
      static bySource(...allowedSources) {
        return (event) => allowedSources.includes(event.metadata.source ?? "");
      }
      /**
       * Filter events by priority
       */
      static byPriority(minPriority) {
        const priorityLevels = { low: 1, normal: 2, high: 3 };
        const minLevel = priorityLevels[minPriority];
        return (event) => {
          const eventPriority = event.metadata.priority || "normal";
          return priorityLevels[eventPriority] >= minLevel;
        };
      }
      /**
       * Filter events by time range
       */
      static byTimeRange(startTime, endTime) {
        return (event) => {
          const timestamp = event.metadata.timestamp;
          return timestamp >= startTime && timestamp <= endTime;
        };
      }
      /**
       * Filter out events older than specified age (in milliseconds)
       */
      static byMaxAge(maxAgeMs) {
        return (event) => {
          const age = Date.now() - event.metadata.timestamp;
          return age <= maxAgeMs;
        };
      }
      /**
       * Rate limiting filter - only allow N events per time window
       */
      static rateLimit(maxEvents, windowMs) {
        const eventTimes = [];
        return (_event) => {
          const now = Date.now();
          const windowStart = now - windowMs;
          while (eventTimes.length > 0 && eventTimes[0] < windowStart) {
            eventTimes.shift();
          }
          if (eventTimes.length < maxEvents) {
            eventTimes.push(now);
            return true;
          }
          return false;
        };
      }
      /**
       * Filter to reduce noise from frequent events
       */
      static deduplicateByType(timeWindowMs = 1e3) {
        const lastSeen = /* @__PURE__ */ new Map();
        return (event) => {
          const now = Date.now();
          const key = `${event.type}-${event.metadata.sessionId || "global"}`;
          const lastTime = lastSeen.get(key);
          if (!lastTime || now - lastTime >= timeWindowMs) {
            lastSeen.set(key, now);
            return true;
          }
          return false;
        };
      }
      /**
       * Filter for performance - only allow events during normal business hours
       */
      static businessHoursOnly(_timezone = "UTC") {
        return () => {
          const eventTime = /* @__PURE__ */ new Date();
          const hours = eventTime.getHours();
          return hours >= 8 && hours < 18;
        };
      }
      /**
       * Complex filter that combines multiple conditions with AND logic
       */
      static and(...filters) {
        return (event) => filters.every((filter) => filter(event));
      }
      /**
       * Complex filter that combines multiple conditions with OR logic
       */
      static or(...filters) {
        return (event) => filters.some((filter) => filter(event));
      }
      /**
       * Negate a filter
       */
      static not(filter) {
        return (event) => !filter(event);
      }
      /**
       * Filter for debugging - only allow events with error information
       */
      static errorsOnly() {
        return (event) => {
          if (event.type.includes("error") || event.type.includes("failed")) {
            return true;
          }
          if (event.data && typeof event.data === "object" && "error" in event.data) {
            return true;
          }
          return false;
        };
      }
      /**
       * Filter for performance monitoring - only high-impact events
       */
      static performanceEvents() {
        const performanceEventTypes = [
          "tool:executionStarted",
          "tool:executionCompleted",
          "tool:executionFailed",
          "llm:responseStarted",
          "llm:responseCompleted",
          "llm:responseError",
          "memory:searched",
          "memory:stored",
          "memory:retrieved"
        ];
        return (event) => performanceEventTypes.includes(event.type);
      }
      /**
       * Filter for security monitoring - only security-relevant events
       */
      static securityEvents() {
        const securityEventTypes = [
          "session:created",
          "session:expired",
          "session:deleted",
          "cipher:mcpClientConnected",
          "cipher:mcpClientDisconnected",
          "cipher:mcpClientError",
          "cipher:toolError",
          "cipher:serviceError"
        ];
        return (event) => {
          if (securityEventTypes.includes(event.type)) {
            return true;
          }
          if (event.data && typeof event.data === "object" && "error" in event.data) {
            return true;
          }
          return false;
        };
      }
    };
  }
});

// src/core/events/persistence.ts
var import_fs5, import_path5, FileEventStorage, MemoryEventStorage, EventPersistence;
var init_persistence = __esm({
  "src/core/events/persistence.ts"() {
    init_cjs_shims();
    init_logger();
    import_fs5 = require("fs");
    import_path5 = __toESM(require("path"), 1);
    FileEventStorage = class {
      config;
      currentFile = "";
      eventCount = 0;
      constructor(config2) {
        this.config = {
          maxEvents: 1e5,
          rotationSize: 10 * 1024 * 1024,
          // 10MB
          retentionDays: 30,
          filePath: "./data/events",
          ...config2,
          enabled: true,
          storageType: "file"
        };
      }
      async store(event) {
        if (!this.config.enabled) return;
        try {
          await this.ensureDirectory();
          const filename = await this.getCurrentFilename();
          const eventData = JSON.stringify(event) + "\n";
          await import_fs5.promises.appendFile(filename, eventData);
          this.eventCount++;
          if (this.eventCount % 1e3 === 0) {
            await this.checkRotation(filename);
          }
        } catch (error) {
          logger.error("Failed to store event", {
            error: error instanceof Error ? error.message : String(error),
            eventId: event.id,
            eventType: event.type
          });
        }
      }
      async query(query2) {
        if (!this.config.enabled) return [];
        try {
          const files = await this.getRelevantFiles(query2.since, query2.until);
          const events = [];
          for (const file of files) {
            const fileEvents = await this.readEventsFromFile(file, query2);
            events.push(...fileEvents);
          }
          events.sort((a, b) => a.metadata.timestamp - b.metadata.timestamp);
          const offset = query2.offset || 0;
          const limit = query2.limit || events.length;
          return events.slice(offset, offset + limit);
        } catch (error) {
          logger.error("Failed to query events", {
            error: error instanceof Error ? error.message : String(error),
            query: query2
          });
          return [];
        }
      }
      async cleanup(retentionMs) {
        if (!this.config.enabled) return 0;
        try {
          await this.ensureDirectory();
          const files = await import_fs5.promises.readdir(this.config.filePath);
          const eventFiles = files.filter((f) => f.startsWith("events-") && f.endsWith(".jsonl"));
          let deletedCount = 0;
          const cutoffTime = Date.now() - retentionMs;
          for (const file of eventFiles) {
            const filePath = import_path5.default.join(this.config.filePath, file);
            const stats = await import_fs5.promises.stat(filePath);
            if (stats.mtime.getTime() < cutoffTime) {
              await import_fs5.promises.unlink(filePath);
              deletedCount++;
            }
          }
          return deletedCount;
        } catch (error) {
          logger.error("Failed to cleanup events", {
            error: error instanceof Error ? error.message : String(error)
          });
          return 0;
        }
      }
      async getStats() {
        if (!this.config.enabled) return { totalEvents: 0, storageSize: 0 };
        try {
          await this.ensureDirectory();
          const files = await import_fs5.promises.readdir(this.config.filePath);
          const eventFiles = files.filter((f) => f.startsWith("events-") && f.endsWith(".jsonl"));
          let totalEvents = 0;
          let storageSize = 0;
          for (const file of eventFiles) {
            const filePath = import_path5.default.join(this.config.filePath, file);
            const stats = await import_fs5.promises.stat(filePath);
            storageSize += stats.size;
            const content = await import_fs5.promises.readFile(filePath, "utf-8");
            totalEvents += content.split("\n").filter((line) => line.trim()).length;
          }
          return { totalEvents, storageSize };
        } catch (error) {
          logger.error("Failed to get storage stats", {
            error: error instanceof Error ? error.message : String(error)
          });
          return { totalEvents: 0, storageSize: 0 };
        }
      }
      async ensureDirectory() {
        try {
          await import_fs5.promises.mkdir(this.config.filePath, { recursive: true });
        } catch {
        }
      }
      async getCurrentFilename() {
        if (!this.currentFile) {
          const date = (/* @__PURE__ */ new Date()).toISOString().split("T")[0];
          this.currentFile = import_path5.default.join(this.config.filePath, `events-${date}.jsonl`);
        }
        return this.currentFile;
      }
      async checkRotation(filename) {
        try {
          const stats = await import_fs5.promises.stat(filename);
          if (stats.size > this.config.rotationSize) {
            this.currentFile = "";
          }
        } catch {
        }
      }
      async getRelevantFiles(since, until) {
        try {
          await this.ensureDirectory();
          const files = await import_fs5.promises.readdir(this.config.filePath);
          const eventFiles = files.filter((f) => f.startsWith("events-") && f.endsWith(".jsonl"));
          if (!since && !until) {
            return eventFiles.map((f) => import_path5.default.join(this.config.filePath, f));
          }
          const relevantFiles = [];
          for (const file of eventFiles) {
            const filePath = import_path5.default.join(this.config.filePath, file);
            const stats = await import_fs5.promises.stat(filePath);
            if (since && stats.mtime.getTime() < since) continue;
            if (until && stats.mtime.getTime() > until) continue;
            relevantFiles.push(filePath);
          }
          return relevantFiles;
        } catch {
          return [];
        }
      }
      async readEventsFromFile(filePath, query2) {
        try {
          const content = await import_fs5.promises.readFile(filePath, "utf-8");
          const lines = content.split("\n").filter((line) => line.trim());
          const events = [];
          for (const line of lines) {
            try {
              const event = JSON.parse(line);
              if (query2.sessionId && event.metadata.sessionId !== query2.sessionId) continue;
              if (query2.eventType && event.type !== query2.eventType) continue;
              if (query2.since && event.metadata.timestamp < query2.since) continue;
              if (query2.until && event.metadata.timestamp > query2.until) continue;
              events.push(event);
            } catch {
            }
          }
          return events;
        } catch {
          return [];
        }
      }
    };
    MemoryEventStorage = class {
      events = [];
      config;
      constructor(config2) {
        this.config = {
          maxEvents: 1e4,
          retentionDays: 7,
          rotationSize: config2.rotationSize ?? 0,
          filePath: config2.filePath ?? "",
          ...config2,
          enabled: true,
          storageType: "memory"
        };
      }
      async store(event) {
        if (!this.config.enabled) return;
        this.events.push(event);
        if (this.events.length > this.config.maxEvents) {
          this.events = this.events.slice(-this.config.maxEvents);
        }
      }
      async query(query2) {
        if (!this.config.enabled) return [];
        let filteredEvents = this.events;
        if (query2.sessionId) {
          filteredEvents = filteredEvents.filter((e) => e.metadata.sessionId === query2.sessionId);
        }
        if (query2.eventType) {
          filteredEvents = filteredEvents.filter((e) => e.type === query2.eventType);
        }
        if (query2.since) {
          filteredEvents = filteredEvents.filter((e) => e.metadata.timestamp >= query2.since);
        }
        if (query2.until) {
          filteredEvents = filteredEvents.filter((e) => e.metadata.timestamp <= query2.until);
        }
        filteredEvents.sort((a, b) => a.metadata.timestamp - b.metadata.timestamp);
        const offset = query2.offset || 0;
        const limit = query2.limit || filteredEvents.length;
        return filteredEvents.slice(offset, offset + limit);
      }
      async cleanup(retentionMs) {
        if (!this.config.enabled) return 0;
        const cutoffTime = Date.now() - retentionMs;
        const originalLength = this.events.length;
        this.events = this.events.filter((e) => e.metadata.timestamp >= cutoffTime);
        return originalLength - this.events.length;
      }
      async getStats() {
        const storageSize = JSON.stringify(this.events).length;
        return {
          totalEvents: this.events.length,
          storageSize
        };
      }
    };
    EventPersistence = class {
      storage;
      cleanupInterval;
      constructor(config2) {
        if (config2.storageType === "file") {
          this.storage = new FileEventStorage(config2);
        } else {
          this.storage = new MemoryEventStorage(config2);
        }
        if (config2.retentionDays && config2.retentionDays > 0) {
          const cleanupIntervalMs = 24 * 60 * 60 * 1e3;
          const retentionMs = config2.retentionDays * 24 * 60 * 60 * 1e3;
          this.cleanupInterval = setInterval(async () => {
            try {
              const deletedCount = await this.storage.cleanup(retentionMs);
              if (deletedCount > 0) {
                logger.info("Event persistence cleanup completed", { deletedCount });
              }
            } catch (error) {
              logger.error("Event persistence cleanup failed", { error });
            }
          }, cleanupIntervalMs);
        }
      }
      async store(event) {
        return this.storage.store(event);
      }
      async query(query2) {
        return this.storage.query(query2);
      }
      async getStats() {
        return this.storage.getStats();
      }
      dispose() {
        if (this.cleanupInterval) {
          clearInterval(this.cleanupInterval);
        }
      }
    };
  }
});

// src/core/events/event-manager.ts
var import_uuid15, EventManager;
var init_event_manager = __esm({
  "src/core/events/event-manager.ts"() {
    init_cjs_shims();
    init_service_event_bus();
    init_session_event_bus();
    init_filtering();
    init_logger();
    import_uuid15 = require("uuid");
    init_persistence();
    EventManager = class {
      serviceEventBus;
      sessionEventBuses = /* @__PURE__ */ new Map();
      filterManager;
      options;
      instanceId;
      cleanupInterval;
      isDisposed = false;
      eventPersistence;
      constructor(options = {}) {
        this.instanceId = (0, import_uuid15.v4)();
        this.options = {
          enableLogging: options.enableLogging ?? true,
          enablePersistence: options.enablePersistence ?? false,
          enableFiltering: options.enableFiltering ?? false,
          maxServiceListeners: options.maxServiceListeners ?? 200,
          maxSessionListeners: options.maxSessionListeners ?? 100,
          maxSessionHistorySize: options.maxSessionHistorySize ?? 1e3,
          sessionCleanupInterval: options.sessionCleanupInterval ?? 3e5,
          // 5 minutes
          eventPersistenceConfig: options.eventPersistenceConfig ?? {}
        };
        if (this.options.enablePersistence) {
          this.eventPersistence = new EventPersistence({
            enabled: true,
            storageType: this.options.eventPersistenceConfig.storageType || "file",
            filePath: this.options.eventPersistenceConfig.filePath || "./data/events",
            retentionDays: this.options.eventPersistenceConfig.retentionDays || 7,
            ...this.options.eventPersistenceConfig.maxEvents !== void 0 ? { maxEvents: this.options.eventPersistenceConfig.maxEvents } : {},
            ...this.options.eventPersistenceConfig.rotationSize !== void 0 ? { rotationSize: this.options.eventPersistenceConfig.rotationSize } : {}
          });
        }
        this.filterManager = new EventFilterManager();
        this.serviceEventBus = new ServiceEventBus({
          enableLogging: this.options.enableLogging,
          enablePersistence: this.options.enablePersistence,
          maxListeners: this.options.maxServiceListeners,
          ...this.eventPersistence ? { eventPersistence: this.eventPersistence } : {}
        });
        if (this.options.sessionCleanupInterval > 0) {
          this.cleanupInterval = setInterval(() => {
            this.cleanupInactiveSessions();
          }, this.options.sessionCleanupInterval);
        }
        logger.debug("EventManager initialized", {
          instanceId: this.instanceId,
          options: this.options
        });
      }
      /**
       * Get or create a session event bus
       */
      getSessionEventBus(sessionId) {
        if (this.isDisposed) {
          throw new Error("EventManager is disposed");
        }
        let sessionBus = this.sessionEventBuses.get(sessionId);
        if (!sessionBus) {
          sessionBus = new SessionEventBus({
            sessionId,
            enableLogging: this.options.enableLogging,
            enablePersistence: this.options.enablePersistence,
            maxListeners: this.options.maxSessionListeners,
            maxHistorySize: this.options.maxSessionHistorySize,
            ...this.eventPersistence ? { eventPersistence: this.eventPersistence } : {}
          });
          this.sessionEventBuses.set(sessionId, sessionBus);
          logger.debug("SessionEventBus created", {
            sessionId,
            instanceId: this.instanceId,
            totalSessions: this.sessionEventBuses.size
          });
        }
        return sessionBus;
      }
      /**
       * Get the service event bus
       */
      getServiceEventBus() {
        if (this.isDisposed) {
          throw new Error("EventManager is disposed");
        }
        return this.serviceEventBus;
      }
      /**
       * Remove a session event bus
       */
      removeSessionEventBus(sessionId) {
        const sessionBus = this.sessionEventBuses.get(sessionId);
        if (sessionBus) {
          sessionBus.dispose();
          this.sessionEventBuses.delete(sessionId);
          logger.debug("SessionEventBus removed", {
            sessionId,
            instanceId: this.instanceId,
            totalSessions: this.sessionEventBuses.size
          });
        }
      }
      /**
       * Emit a service event
       */
      emitServiceEvent(event, data) {
        if (this.isDisposed) {
          logger.warn("Attempted to emit service event on disposed EventManager", {
            eventType: event,
            instanceId: this.instanceId
          });
          return;
        }
        if (this.options.enableFiltering && this.filterManager.hasFilters()) {
          const eventEnvelope = {
            id: (0, import_uuid15.v4)(),
            type: event,
            data,
            metadata: {
              timestamp: Date.now(),
              source: "service",
              eventManagerId: this.instanceId
            }
          };
          if (!this.filterManager.shouldProcessEvent(eventEnvelope)) {
            return;
          }
        }
        this.serviceEventBus.emitServiceEvent(event, data);
        if (this.eventPersistence) {
          const envelope = {
            id: (0, import_uuid15.v4)(),
            type: event,
            data,
            metadata: {
              timestamp: Date.now(),
              source: "service",
              eventManagerId: this.instanceId
            }
          };
          void this.eventPersistence.store(envelope);
        }
      }
      /**
       * Emit a session event
       */
      emitSessionEvent(sessionId, event, data) {
        if (this.isDisposed) {
          logger.warn("Attempted to emit session event on disposed EventManager", {
            sessionId,
            eventType: event,
            instanceId: this.instanceId
          });
          return;
        }
        if (this.options.enableFiltering && this.filterManager.hasFilters()) {
          const eventEnvelope = {
            id: (0, import_uuid15.v4)(),
            type: event,
            data,
            metadata: {
              timestamp: Date.now(),
              source: "session",
              sessionId,
              eventManagerId: this.instanceId
            }
          };
          if (!this.filterManager.shouldProcessEvent(eventEnvelope)) {
            return;
          }
        }
        const sessionBus = this.getSessionEventBus(sessionId);
        sessionBus.emitSessionEvent(event, data);
        if (this.eventPersistence) {
          const envelope = {
            id: (0, import_uuid15.v4)(),
            type: event,
            data,
            metadata: {
              timestamp: Date.now(),
              source: "session",
              sessionId,
              eventManagerId: this.instanceId
            }
          };
          void this.eventPersistence.store(envelope);
        }
      }
      /**
       * Get all active session IDs
       */
      getActiveSessionIds() {
        return Array.from(this.sessionEventBuses.keys());
      }
      /**
       * Get comprehensive statistics
       */
      getStatistics() {
        const serviceStats = this.serviceEventBus.getStatistics();
        const sessionStats = Array.from(this.sessionEventBuses.entries()).map(([sessionId, bus]) => {
          const stats = bus.getStatistics();
          return {
            sessionId,
            age: stats.age,
            totalEvents: stats.totalEvents,
            recentActivity: stats.recentActivity
          };
        });
        return {
          instanceId: this.instanceId,
          uptime: serviceStats.uptime,
          totalSessions: this.sessionEventBuses.size,
          activeSessions: Array.from(this.sessionEventBuses.values()).filter(
            (bus) => !bus.isSessionDisposed()
          ).length,
          serviceEvents: {
            totalEvents: serviceStats.totalEvents,
            eventTypes: serviceStats.eventTypes,
            activeListeners: serviceStats.activeListeners
          },
          sessionStats
        };
      }
      /**
       * Search for events across all session buses
       */
      searchSessionEvents(filter) {
        if (!this.options.enablePersistence) {
          return [];
        }
        let results = [];
        const sessionBuses = filter.sessionId ? [this.sessionEventBuses.get(filter.sessionId)].filter(Boolean) : Array.from(this.sessionEventBuses.values());
        for (const sessionBus of sessionBuses) {
          if (sessionBus) {
            let events = sessionBus.getEventHistory({
              ...filter.eventType !== void 0 ? { eventType: filter.eventType } : {},
              ...filter.since !== void 0 ? { since: filter.since } : {},
              ...filter.limit !== void 0 ? { limit: filter.limit } : {}
            });
            if (filter.pattern) {
              events = events.filter((event) => filter.pattern.test(event.type));
            }
            results.push(...events);
          }
        }
        results.sort((a, b) => b.metadata.timestamp - a.metadata.timestamp);
        if (filter.limit && filter.limit > 0) {
          results = results.slice(0, filter.limit);
        }
        return results;
      }
      /**
       * Clean up inactive sessions
       */
      cleanupInactiveSessions() {
        const maxAge = 24 * 60 * 60 * 1e3;
        const inactiveSessionIds = [];
        for (const [sessionId, sessionBus] of this.sessionEventBuses) {
          if (sessionBus.isSessionDisposed() || sessionBus.getAge() > maxAge) {
            inactiveSessionIds.push(sessionId);
          }
        }
        if (inactiveSessionIds.length > 0) {
          logger.info("Cleaning up inactive sessions", {
            count: inactiveSessionIds.length,
            sessionIds: inactiveSessionIds,
            instanceId: this.instanceId
          });
          inactiveSessionIds.forEach((sessionId) => {
            this.removeSessionEventBus(sessionId);
          });
        }
      }
      /**
       * Create a cross-bus event forwarding rule
       */
      createForwardingRule(sessionEventType, forwardToService = true, filter) {
        logger.debug("Event forwarding rule created", {
          sessionEventType,
          forwardToService,
          hasFilter: !!filter,
          instanceId: this.instanceId
        });
      }
      /**
       * Get event manager instance ID
       */
      getInstanceId() {
        return this.instanceId;
      }
      /**
       * Check if event manager is disposed
       */
      isEventManagerDisposed() {
        return this.isDisposed;
      }
      /**
       * Register an event filter
       */
      registerFilter(config2) {
        this.filterManager.registerFilter(config2);
      }
      /**
       * Unregister an event filter
       */
      unregisterFilter(name) {
        return this.filterManager.unregisterFilter(name);
      }
      /**
       * Enable or disable a filter
       */
      setFilterEnabled(name, enabled) {
        this.filterManager.setFilterEnabled(name, enabled);
      }
      /**
       * Get filtering statistics
       */
      getFilteringStats() {
        return this.filterManager.getStats();
      }
      /**
       * Get list of registered filters
       */
      getFilters() {
        return this.filterManager.getFilters();
      }
      /**
       * Setup common filters for typical use cases
       */
      setupCommonFilters() {
        this.registerFilter({
          name: "rate-limit-tool-events",
          description: "Limit tool execution events to prevent spam",
          enabled: true,
          priority: 100,
          filter: CommonFilters.and(
            CommonFilters.byEventType("tool:executionStarted", "tool:executionCompleted"),
            CommonFilters.rateLimit(50, 6e4)
            // Max 50 tool events per minute
          )
        });
        if (process.env.NODE_ENV === "production") {
          this.registerFilter({
            name: "production-noise-filter",
            description: "Filter out noisy events in production",
            enabled: true,
            priority: 90,
            filter: CommonFilters.not(CommonFilters.byEventType("llm:thinking", "context:updated"))
          });
        }
        this.registerFilter({
          name: "incident-mode",
          description: "Only allow error events during incident response",
          enabled: false,
          // Disabled by default, enable manually during incidents
          priority: 200,
          filter: CommonFilters.errorsOnly()
        });
        this.registerFilter({
          name: "memory-deduplication",
          description: "Reduce duplicate memory operation events",
          enabled: true,
          priority: 80,
          filter: CommonFilters.and(
            CommonFilters.byEventType("memory:stored", "memory:retrieved", "memory:searched"),
            CommonFilters.deduplicateByType(500)
            // Deduplicate within 500ms
          )
        });
        logger.info("Common event filters setup completed", {
          filtersRegistered: this.getFilters().length,
          instanceId: this.instanceId
        });
      }
      /**
       * Dispose of the event manager and all resources
       */
      dispose() {
        if (this.isDisposed) {
          return;
        }
        this.isDisposed = true;
        if (this.cleanupInterval) {
          clearInterval(this.cleanupInterval);
        }
        for (const [, sessionBus] of this.sessionEventBuses) {
          sessionBus.dispose();
        }
        this.sessionEventBuses.clear();
        this.serviceEventBus.dispose();
        if (this.eventPersistence) {
          this.eventPersistence.dispose();
        }
        logger.info("EventManager disposed", {
          instanceId: this.instanceId
        });
      }
    };
  }
});

// src/core/utils/service-initializer.ts
async function createEmbeddingFromLLMProvider(embeddingManager, llmConfig) {
  const provider = llmConfig.provider?.toLowerCase();
  try {
    switch (provider) {
      case "openai": {
        const apiKey = llmConfig.apiKey || process.env.OPENAI_API_KEY;
        if (!apiKey || apiKey.trim() === "") {
          logger.debug(
            "No OpenAI API key available for embedding fallback - switching to chat-only mode"
          );
          return null;
        }
        const embeddingConfig = {
          type: "openai",
          apiKey,
          model: "text-embedding-3-small",
          baseUrl: llmConfig.baseUrl,
          organization: llmConfig.organization,
          timeout: 3e4,
          maxRetries: 3
        };
        logger.debug("Using OpenAI embedding fallback: text-embedding-3-small");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "ollama": {
        let baseUrl = llmConfig.baseUrl || process.env.OLLAMA_BASE_URL || "http://localhost:11434";
        if (baseUrl.endsWith("/v1")) {
          baseUrl = baseUrl.replace(/\/v1$/, "");
        }
        const embeddingConfig = {
          type: "ollama",
          baseUrl,
          model: "nomic-embed-text",
          timeout: 3e4,
          maxRetries: 3
        };
        logger.debug("Using Ollama embedding fallback: nomic-embed-text");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "lmstudio": {
        const baseUrl = llmConfig.baseUrl || process.env.LMSTUDIO_BASE_URL || "http://localhost:1234/v1";
        const embeddingConfig = {
          type: "lmstudio",
          baseUrl,
          model: "nomic-embed-text-v1.5",
          timeout: 3e4,
          maxRetries: 3
        };
        logger.debug("Using LM Studio embedding fallback: nomic-embed-text-v1.5");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "gemini": {
        const apiKey = llmConfig.apiKey || process.env.GEMINI_API_KEY;
        if (!apiKey || apiKey.trim() === "") {
          logger.debug(
            "No Gemini API key available for embedding fallback - switching to chat-only mode"
          );
          return null;
        }
        const embeddingConfig = {
          type: "gemini",
          apiKey,
          model: "gemini-embedding-001",
          timeout: 3e4,
          maxRetries: 3
        };
        logger.debug("Using Gemini embedding fallback: gemini-embedding-001");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "anthropic": {
        const apiKey = llmConfig.apiKey || process.env.VOYAGE_API_KEY;
        if (!apiKey || apiKey.trim() === "") {
          logger.debug(
            "No Voyage API key available for Anthropic - switching to chat-only mode (set VOYAGE_API_KEY)"
          );
          return null;
        }
        const embeddingConfig = {
          type: "voyage",
          apiKey,
          model: "voyage-3-large",
          timeout: 3e4,
          maxRetries: 3,
          dimensions: 1024
        };
        logger.debug("Using Voyage embedding for Anthropic LLM", {
          voyageModel: "voyage-3-large",
          voyageDimensions: 1024,
          provider: "voyage"
        });
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "aws": {
        const accessKeyId = llmConfig.accessKeyId || process.env.AWS_ACCESS_KEY_ID;
        const secretAccessKey = llmConfig.secretAccessKey || process.env.AWS_SECRET_ACCESS_KEY;
        if (!accessKeyId || accessKeyId.trim() === "" || !secretAccessKey || secretAccessKey.trim() === "") {
          logger.debug(
            "No AWS credentials available for AWS Bedrock embedding - switching to chat-only mode (need AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY)"
          );
          return null;
        }
        const embeddingConfig = {
          type: "aws-bedrock",
          region: llmConfig.region || process.env.AWS_DEFAULT_REGION || "us-east-1",
          accessKeyId,
          secretAccessKey,
          sessionToken: llmConfig.sessionToken || process.env.AWS_SESSION_TOKEN,
          model: "amazon.titan-embed-text-v2:0",
          timeout: 3e4,
          maxRetries: 3,
          dimensions: 1024
        };
        logger.debug("Using AWS Bedrock native embedding: amazon.titan-embed-text-v2:0");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "azure": {
        const azureApiKey = llmConfig.apiKey || process.env.AZURE_OPENAI_API_KEY;
        if (!azureApiKey || azureApiKey.trim() === "") {
          logger.debug("No Azure OpenAI API key available for embedding fallback");
          const openaiApiKey = process.env.OPENAI_API_KEY;
          if (openaiApiKey && openaiApiKey.trim() !== "") {
            const embeddingConfig2 = {
              type: "openai",
              apiKey: openaiApiKey,
              model: "text-embedding-3-small",
              timeout: 3e4,
              maxRetries: 3
            };
            logger.debug("Using OpenAI embedding fallback for Azure LLM: text-embedding-3-small");
            return await embeddingManager.createEmbedderFromConfig(embeddingConfig2, "default");
          }
          logger.debug("No OpenAI API key available either - switching to chat-only mode");
          return null;
        }
        const embeddingConfig = {
          type: "openai",
          apiKey: azureApiKey,
          model: "text-embedding-3-small",
          baseUrl: llmConfig.azure?.endpoint || process.env.AZURE_OPENAI_ENDPOINT,
          timeout: 3e4,
          maxRetries: 3
        };
        logger.debug("Using Azure OpenAI embedding fallback: text-embedding-3-small");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "qwen": {
        const apiKey = llmConfig.apiKey || process.env.QWEN_API_KEY || process.env.DASHSCOPE_API_KEY;
        if (!apiKey || apiKey.trim() === "") {
          logger.debug(
            "No Qwen API key available for native embedding - switching to chat-only mode (need QWEN_API_KEY or DASHSCOPE_API_KEY)"
          );
          return null;
        }
        const embeddingConfig = {
          type: "qwen",
          apiKey,
          model: "text-embedding-v3",
          baseUrl: llmConfig.baseUrl,
          timeout: 3e4,
          maxRetries: 3,
          dimensions: 1024
        };
        logger.debug("Using Qwen native embedding: text-embedding-v3");
        return await embeddingManager.createEmbedderFromConfig(embeddingConfig, "default");
      }
      case "openrouter": {
        logger.debug(
          "OpenRouter does not support embedding models - embeddings disabled for this provider"
        );
        return null;
      }
      default: {
        logger.debug(`No embedding fallback available for LLM provider: ${provider}`);
        return null;
      }
    }
  } catch (error) {
    logger.warn(`Failed to create embedding from LLM provider ${provider}`, {
      error: error instanceof Error ? error.message : String(error)
    });
    return null;
  }
}
async function createAgentServices(agentConfig, appMode) {
  let contextManager = void 0;
  const config2 = agentConfig;
  logger.debug("Initializing event manager...");
  const eventPersistenceConfig = {
    ...config2.eventPersistence,
    // Support EVENT_PERSISTENCE_ENABLED env variable
    enabled: process.env.EVENT_PERSISTENCE_ENABLED === "true" || (config2.eventPersistence?.enabled ?? false),
    // Support EVENT_PERSISTENCE_PATH env variable
    filePath: process.env.EVENT_PERSISTENCE_PATH || config2.eventPersistence?.filePath
  };
  const enableFiltering = process.env.EVENT_FILTERING_ENABLED === "true";
  const filteredTypes = (process.env.EVENT_FILTERED_TYPES || "").split(",").map((s) => s.trim()).filter(Boolean);
  const eventManager = new EventManager({
    enableLogging: true,
    enablePersistence: eventPersistenceConfig.enabled,
    enableFiltering,
    maxServiceListeners: 300,
    maxSessionListeners: 150,
    maxSessionHistorySize: 1e3,
    sessionCleanupInterval: 3e5,
    // 5 minutes
    // Pass through eventPersistenceConfig for use by persistence provider
    eventPersistenceConfig
  });
  if (enableFiltering && filteredTypes.length > 0) {
    eventManager.registerFilter({
      name: "env-filtered-types",
      description: "Block event types from EVENT_FILTERED_TYPES",
      enabled: true,
      filter: (event) => !filteredTypes.includes(event.type)
    });
  }
  if (eventPersistenceConfig.enabled) {
    logger.info("Event persistence enabled", {
      storageType: eventPersistenceConfig.storageType || "file",
      filePath: eventPersistenceConfig.filePath || "./data/events",
      enabled: eventPersistenceConfig.enabled
    });
  }
  eventManager.emitServiceEvent("cipher:started", {
    timestamp: Date.now(),
    version: process.env.npm_package_version || "1.0.0"
  });
  const mcpManager = new MCPManager();
  mcpManager.setEventManager(eventManager);
  if (appMode === "cli") {
    mcpManager.setQuietMode(true);
  }
  const parsedMcpServers = ServerConfigsSchema.parse(config2.mcpServers);
  await mcpManager.initializeFromConfig(parsedMcpServers);
  const mcpServerCount = Object.keys(config2.mcpServers || {}).length;
  if (mcpServerCount === 0) {
    if (appMode !== "cli") {
      logger.debug("Agent initialized without MCP servers - only built-in capabilities available");
    }
  } else {
    if (appMode !== "cli") {
      logger.debug(`Client manager initialized with ${mcpServerCount} MCP server(s)`);
    }
  }
  eventManager.emitServiceEvent("cipher:serviceStarted", {
    serviceType: "MCPManager",
    timestamp: Date.now()
  });
  if (appMode !== "cli") {
    logger.debug("Initializing embedding manager...");
  }
  const embeddingManager = new EmbeddingManager();
  let embeddingEnabled = false;
  try {
    let embeddingResult = null;
    const explicitlyDisabled = config2.embedding && typeof config2.embedding === "object" && "disabled" in config2.embedding && config2.embedding.disabled === true || config2.embedding === null || config2.embedding === false || process.env.DISABLE_EMBEDDINGS === "true" || process.env.EMBEDDING_DISABLED === "true";
    if (explicitlyDisabled) {
      logger.warn(
        "Embeddings are explicitly disabled - all embedding-dependent tools will be unavailable (chat-only mode)"
      );
      embeddingEnabled = false;
    } else {
      if (config2.embedding && typeof config2.embedding === "object" && !("disabled" in config2.embedding)) {
        logger.debug("Found explicit embedding configuration in YAML, using it");
        const embeddingConfig = config2.embedding;
        const needsApiKey = ["openai", "gemini", "anthropic", "voyage", "qwen"].includes(
          embeddingConfig.type
        );
        const needsAwsCredentials = embeddingConfig.type === "aws-bedrock";
        if (needsApiKey) {
          const apiKey = embeddingConfig.apiKey || process.env[`${embeddingConfig.type.toUpperCase()}_API_KEY`];
          if (!apiKey || apiKey.trim() === "") {
            logger.debug(
              `No API key available for explicit ${embeddingConfig.type} embedding config - switching to chat-only mode`
            );
            embeddingResult = null;
          } else {
            const cleanEmbeddingConfig = {
              type: embeddingConfig.type,
              apiKey,
              model: embeddingConfig.model || "text-embedding-3-small",
              baseUrl: embeddingConfig.baseUrl,
              organization: embeddingConfig.organization,
              timeout: embeddingConfig.timeout || 3e4,
              maxRetries: embeddingConfig.maxRetries || 3,
              dimensions: embeddingConfig.dimensions
            };
            embeddingResult = await embeddingManager.createEmbedderFromConfig(
              cleanEmbeddingConfig,
              "default"
            );
          }
        } else if (needsAwsCredentials) {
          const accessKeyId = embeddingConfig.accessKeyId || process.env.AWS_ACCESS_KEY_ID;
          const secretAccessKey = embeddingConfig.secretAccessKey || process.env.AWS_SECRET_ACCESS_KEY;
          console.log("accessKeyId", accessKeyId);
          console.log("secretAccessKey", secretAccessKey);
          if (!accessKeyId || accessKeyId.trim() === "" || !secretAccessKey || secretAccessKey.trim() === "") {
            logger.debug(
              "No AWS credentials available for explicit aws-bedrock embedding config - switching to chat-only mode"
            );
            embeddingResult = null;
          } else {
            embeddingResult = await embeddingManager.createEmbedderFromConfig(
              embeddingConfig,
              "default"
            );
          }
        } else {
          embeddingResult = await embeddingManager.createEmbedderFromConfig(
            embeddingConfig,
            "default"
          );
        }
      }
      if (!embeddingResult) {
        if (config2.llm?.provider) {
          logger.debug(
            "No explicit embedding config found, falling back to LLM provider embedding"
          );
          embeddingResult = await createEmbeddingFromLLMProvider(embeddingManager, config2.llm);
          if (embeddingResult && embeddingResult.info) {
            const embeddingConfig = {
              type: embeddingResult.info.provider,
              model: embeddingResult.info.model,
              dimensions: embeddingResult.info.dimension
            };
            config2.embedding = embeddingConfig;
            logger.debug("Updated agent config with embedding fallback configuration", {
              provider: embeddingResult.info.provider,
              model: embeddingResult.info.model,
              dimension: embeddingResult.info.dimension
            });
          }
        } else {
          logger.debug(
            "No LLM provider available for embedding fallback, trying environment auto-detection"
          );
          embeddingResult = await embeddingManager.createEmbedderFromEnv("default");
        }
      }
      if (embeddingResult) {
        if (appMode !== "cli") {
          logger.info("Embedding manager initialized successfully", {
            provider: embeddingResult.info.provider,
            model: embeddingResult.info.model,
            dimension: embeddingResult.info.dimension
          });
        }
        eventManager.emitServiceEvent("cipher:serviceStarted", {
          serviceType: "EmbeddingManager",
          timestamp: Date.now()
        });
        embeddingEnabled = true;
      } else {
        logger.warn(
          "No embedding configuration available - embedding-dependent tools will be disabled (chat-only mode)"
        );
        embeddingEnabled = false;
      }
    }
  } catch (error) {
    logger.error("Failed to initialize embedding manager - activating fallback mode", {
      error: error instanceof Error ? error.message : String(error),
      fallbackMode: "chat-only"
    });
    embeddingEnabled = false;
    logger.warn("\u{1F504} Embedding system in fallback mode:", {
      mode: "chat-only",
      availableFeatures: ["LLM conversation", "MCP tools", "System prompts"],
      unavailableFeatures: [
        "Memory search",
        "Knowledge storage",
        "Reasoning patterns",
        "Vector operations"
      ],
      recoveryAction: "Check embedding configuration and credentials, then restart the service"
    });
  }
  if (appMode !== "cli") {
    logger.debug("Initializing vector storage manager...");
  }
  let vectorStoreManager;
  try {
    const workspaceEnabled = !!env.USE_WORKSPACE_MEMORY;
    const reflectionEnabled = !env.DISABLE_REFLECTION_MEMORY && env.REFLECTION_VECTOR_STORE_COLLECTION && env.REFLECTION_VECTOR_STORE_COLLECTION.trim() !== "";
    if (workspaceEnabled) {
      logger.debug("Workspace memory enabled, using multi collection vector manager");
      const { manager } = await createMultiCollectionVectorStoreFromEnv(config2);
      vectorStoreManager = manager;
      vectorStoreManager.setEventManager(eventManager);
      const info = vectorStoreManager.getInfo();
      logger.debug("Multi collection vector storage manager initialized successfully", {
        backend: info.knowledge.manager.getInfo().backend.type,
        knowledgeCollection: info.knowledge.collectionName,
        reflectionCollection: info.reflection.enabled ? info.reflection.collectionName : "disabled",
        workspaceCollection: info.workspace.enabled ? info.workspace.collectionName : "disabled",
        dimension: info.knowledge.manager.getInfo().backend.dimension,
        knowledgeConnected: info.knowledge.connected,
        reflectionConnected: info.reflection.connected,
        workspaceConnected: info.workspace.connected,
        reflectionEnabled: info.reflection.enabled,
        workspaceEnabled: info.workspace.enabled
      });
    } else if (reflectionEnabled) {
      logger.debug("Reflection memory enabled, using dual collection vector manager");
      const { manager } = await createDualCollectionVectorStoreFromEnv(config2);
      vectorStoreManager = manager;
      vectorStoreManager.setEventManager(eventManager);
      const info = vectorStoreManager.getInfo();
      logger.debug("Dual collection vector storage manager initialized successfully", {
        backend: info.knowledge.manager.getInfo().backend.type,
        knowledgeCollection: info.knowledge.collectionName,
        reflectionCollection: info.reflection.collectionName,
        dimension: info.knowledge.manager.getInfo().backend.dimension,
        knowledgeConnected: info.knowledge.connected,
        reflectionConnected: info.reflection.connected,
        reflectionEnabled: info.reflection.enabled
      });
    } else {
      logger.debug("Reflection memory disabled, using single collection vector manager");
      const { manager } = await createVectorStoreFromEnv(config2);
      vectorStoreManager = manager;
      vectorStoreManager.setEventManager(eventManager);
      logger.debug("Vector storage manager initialized successfully", {
        backend: vectorStoreManager.getInfo().backend.type,
        collection: vectorStoreManager.getInfo().backend.collectionName,
        dimension: vectorStoreManager.getInfo().backend.dimension,
        fallback: vectorStoreManager.getInfo().backend.fallback || false
      });
    }
  } catch (error) {
    logger.warn("Failed to initialize vector storage manager", {
      error: error instanceof Error ? error.message : String(error)
    });
    logger.warn(error instanceof Error ? error.message : String(error));
    const { manager } = await createVectorStoreFromEnv(config2);
    vectorStoreManager = manager;
  }
  if (appMode !== "cli") {
    logger.debug("Initializing knowledge graph manager...");
  }
  let knowledgeGraphManager = void 0;
  try {
    const kgFactory = await createKnowledgeGraphFromEnv();
    if (kgFactory) {
      knowledgeGraphManager = kgFactory.manager;
      logger.debug("Knowledge graph manager initialized successfully", {
        backend: knowledgeGraphManager.getInfo().backend.type,
        connected: knowledgeGraphManager.isConnected(),
        fallback: knowledgeGraphManager.getInfo().backend.fallback || false
      });
    } else {
      logger.debug("Knowledge graph is disabled in environment configuration");
    }
  } catch (error) {
    logger.warn("Failed to initialize knowledge graph manager", {
      error: error instanceof Error ? error.message : String(error)
    });
  }
  const promptManager = new EnhancedPromptManager();
  let staticProvider = null;
  if (config2.systemPrompt) {
    let enabled = true;
    let content = "";
    if (typeof config2.systemPrompt === "string") {
      content = config2.systemPrompt;
    } else if (typeof config2.systemPrompt === "object" && config2.systemPrompt !== null) {
      const promptObj = config2.systemPrompt;
      enabled = promptObj.enabled !== false && promptObj.enabled !== void 0;
      content = promptObj.content || "";
    }
    staticProvider = {
      name: "user-instruction",
      type: "static" /* STATIC */,
      priority: 100,
      enabled,
      config: { content }
    };
  }
  let advancedProviders = [];
  let advancedSettings = {};
  const advancedPromptPath = import_path6.default.resolve(process.cwd(), "memAgent/cipher-advanced-prompt.yml");
  if (import_fs6.default.existsSync(advancedPromptPath)) {
    const fileContent = import_fs6.default.readFileSync(advancedPromptPath, "utf8");
    const parsed = import_yaml.default.parse(fileContent);
    if (Array.isArray(parsed.providers)) {
      advancedProviders = parsed.providers;
    }
    if (parsed.settings) {
      advancedSettings = parsed.settings;
    }
  }
  const mergedProviders = [
    ...staticProvider ? [staticProvider] : [],
    ...advancedProviders.filter((p) => !staticProvider || p.name !== staticProvider.name)
  ];
  const mergedSettings = {
    maxGenerationTime: 1e4,
    failOnProviderError: false,
    contentSeparator: "\n\n",
    ...advancedSettings
  };
  const mergedPromptConfig = {
    providers: mergedProviders,
    settings: mergedSettings
  };
  await promptManager.initialize(mergedPromptConfig);
  const stateManager = new MemAgentStateManager(config2);
  if (appMode !== "cli") {
    logger.debug("Agent state manager initialized");
  }
  let llmService = void 0;
  try {
    if (appMode !== "cli") {
      logger.debug("Initializing LLM service...");
    }
    const llmConfig = stateManager.getLLMConfig();
    const serviceCache = getServiceCache();
    const contextManagerKey = createServiceKey("contextManager", {
      provider: llmConfig.provider,
      model: llmConfig.model,
      // Include additional config for proper cache key differentiation
      apiKey: llmConfig.apiKey ? "present" : "missing",
      baseURL: llmConfig.baseURL || "default"
    });
    contextManager = await serviceCache.getOrCreate(contextManagerKey, async () => {
      return createContextManager(llmConfig, promptManager, void 0, void 0);
    });
    llmService = createLLMService(llmConfig, mcpManager, contextManager);
    if (appMode !== "cli") {
      logger.info("LLM service initialized successfully", {
        provider: llmConfig.provider,
        model: llmConfig.model
      });
    }
    promptManager.setLLMService(llmService);
  } catch (error) {
    logger.warn("Failed to initialize LLM service", {
      error: error instanceof Error ? error.message : String(error)
    });
  }
  const sessionConfig = {};
  if (config2.sessions?.maxSessions !== void 0) {
    sessionConfig.maxSessions = config2.sessions.maxSessions;
  }
  if (config2.sessions?.sessionTTL !== void 0) {
    sessionConfig.sessionTTL = config2.sessions.sessionTTL;
  }
  const internalToolManager = new InternalToolManager({
    enabled: true,
    timeout: 3e4,
    enableCache: true,
    cacheTimeout: 3e5
  });
  await internalToolManager.initialize();
  internalToolManager.setEventManager(eventManager);
  const toolRegistrationResult = await registerAllTools(internalToolManager, { embeddingEnabled });
  if (appMode !== "cli" || toolRegistrationResult.failed.length > 0) {
    logger.info("Internal tools registration completed", {
      totalTools: toolRegistrationResult.total,
      registered: toolRegistrationResult.registered.length,
      failed: toolRegistrationResult.failed.length
    });
  }
  if (toolRegistrationResult.failed.length > 0) {
    logger.warn("Some internal tools failed to register", {
      failedTools: toolRegistrationResult.failed
    });
  }
  const services = {
    vectorStoreManager,
    llmService,
    knowledgeGraphManager
  };
  if (embeddingEnabled) {
    services.embeddingManager = embeddingManager;
  }
  internalToolManager.setServices(services);
  let unifiedToolManagerConfig;
  if (appMode === "cli") {
    unifiedToolManagerConfig = {
      enableInternalTools: true,
      enableMcpTools: true,
      conflictResolution: "prefix-internal",
      mode: "cli"
      // Special CLI mode
    };
  } else if (appMode === "mcp") {
    const mcpServerMode = process.env.MCP_SERVER_MODE || "default";
    if (mcpServerMode === "aggregator") {
      unifiedToolManagerConfig = {
        enableInternalTools: true,
        enableMcpTools: true,
        conflictResolution: "prefix-internal",
        mode: "aggregator"
        // Aggregator mode exposes all tools without filtering
      };
    } else {
      unifiedToolManagerConfig = {
        enableInternalTools: true,
        enableMcpTools: true,
        conflictResolution: "prefix-internal",
        mode: "cli"
        // Internal agent needs access to all tools in default mode
      };
    }
  } else {
    const mcpServerMode = process.env.MCP_SERVER_MODE || "default";
    if (mcpServerMode === "aggregator") {
      unifiedToolManagerConfig = {
        enableInternalTools: true,
        enableMcpTools: true,
        conflictResolution: "prefix-internal",
        mode: "aggregator"
        // Aggregator mode exposes all tools without filtering
      };
    } else {
      unifiedToolManagerConfig = {
        enableInternalTools: true,
        enableMcpTools: true,
        conflictResolution: "prefix-internal",
        mode: "api"
      };
    }
  }
  const unifiedToolManager = new UnifiedToolManager(
    mcpManager,
    internalToolManager,
    unifiedToolManagerConfig
  );
  unifiedToolManager.setEventManager(eventManager);
  if (embeddingManager) {
    unifiedToolManager.setEmbeddingManager(embeddingManager);
  }
  if (appMode !== "cli") {
    logger.debug("Unified tool manager initialized");
  }
  const sessionManager = new SessionManager(
    {
      stateManager,
      promptManager,
      contextManager,
      mcpManager,
      unifiedToolManager,
      eventManager,
      ...embeddingManager && { embeddingManager }
      // Only include if available
    },
    sessionConfig
  );
  await sessionManager.init();
  if (appMode !== "cli") {
    logger.debug("Session manager with unified tools initialized");
  }
  eventManager.emitServiceEvent("cipher:serviceStarted", {
    serviceType: "SessionManager",
    timestamp: Date.now()
  });
  const agentServices = {
    mcpManager,
    promptManager,
    stateManager,
    sessionManager,
    internalToolManager,
    unifiedToolManager,
    vectorStoreManager,
    eventManager,
    contextManager,
    llmService: llmService || {
      generate: async () => "",
      directGenerate: async () => "",
      getAllTools: async () => ({}),
      getConfig: () => ({ provider: "unknown", model: "unknown" })
    }
  };
  if (embeddingEnabled) {
    agentServices.embeddingManager = embeddingManager;
  }
  if (knowledgeGraphManager) {
    agentServices.knowledgeGraphManager = knowledgeGraphManager;
  }
  const serviceTypes = Object.keys(agentServices).filter(
    (key) => agentServices[key]
  );
  eventManager.emitServiceEvent("cipher:allServicesReady", {
    timestamp: Date.now(),
    services: serviceTypes
  });
  return agentServices;
}
var import_fs6, import_path6, import_yaml;
var init_service_initializer = __esm({
  "src/core/utils/service-initializer.ts"() {
    init_cjs_shims();
    init_enhanced_manager();
    init_state_manager();
    init_manager();
    init_session_manager();
    init_manager4();
    init_unified_tool_manager();
    init_definitions();
    init_logger2();
    init_config();
    init_embedding();
    init_factory5();
    init_factory4();
    init_service_cache();
    init_factory7();
    init_factory8();
    init_event_manager();
    init_env();
    init_interfaces();
    import_fs6 = __toESM(require("fs"), 1);
    import_path6 = __toESM(require("path"), 1);
    import_yaml = __toESM(require("yaml"), 1);
  }
});

// src/core/storage/factory.ts
var init_factory9 = __esm({
  "src/core/storage/factory.ts"() {
    init_cjs_shims();
    init_manager3();
    init_logger2();
    init_constants2();
    init_env();
  }
});

// src/app/mcp/mcp_sse_server.ts
var import_express, import_http, import_sse2, McpSseServer;
var init_mcp_sse_server = __esm({
  "src/app/mcp/mcp_sse_server.ts"() {
    init_cjs_shims();
    import_express = __toESM(require("express"), 1);
    import_http = __toESM(require("http"), 1);
    import_sse2 = require("@modelcontextprotocol/sdk/server/sse.js");
    init_logger2();
    McpSseServer = class {
      app;
      httpServer;
      mcpServer;
      activeSseTransports = /* @__PURE__ */ new Map();
      port;
      host;
      sseOptions;
      constructor(port, host = "localhost", options) {
        this.port = port;
        this.host = host;
        if (options) {
          this.sseOptions = options;
        }
        this.app = (0, import_express.default)();
        this.setupMiddleware();
      }
      /**
       * Set up minimal middleware for SSE server
       */
      setupMiddleware() {
        this.app.use(import_express.default.json({ limit: "10mb" }));
        this.app.use(import_express.default.urlencoded({ extended: true }));
        this.app.use((err, req, res, _next) => {
          logger.error("[MCP SSE Server] Request error:", {
            error: err.message,
            path: req.path,
            method: req.method
          });
          if (!res.headersSent) {
            res.status(500).json({
              error: "Internal server error",
              message: err.message
            });
          }
        });
      }
      /**
       * Start the SSE server with the given MCP server instance
       */
      async start(mcpServer) {
        this.mcpServer = mcpServer;
        this.setupSseRoutes();
        return new Promise((resolve3, reject) => {
          this.httpServer = import_http.default.createServer(this.app);
          this.httpServer.listen(this.port, this.host, () => {
            logger.info(`[MCP SSE Server] Started on ${this.host}:${this.port}`, null, "green");
            logger.info(
              `[MCP SSE Server] SSE endpoint: http://${this.host}:${this.port}/sse`,
              null,
              "cyan"
            );
            resolve3();
          });
          this.httpServer.on("error", (err) => {
            logger.error("[MCP SSE Server] Failed to start:", err.message);
            reject(err);
          });
          process.on("SIGTERM", () => this.stop());
          process.on("SIGINT", () => this.stop());
        });
      }
      /**
       * Stop the SSE server and clean up resources
       */
      async stop() {
        logger.info("[MCP SSE Server] Shutting down...");
        for (const [sessionId, transport] of this.activeSseTransports) {
          logger.debug(`[MCP SSE Server] Closing SSE transport for session: ${sessionId}`);
          try {
            transport.close();
          } catch (error) {
            logger.error(`[MCP SSE Server] Error closing transport ${sessionId}:`, error);
          }
        }
        this.activeSseTransports.clear();
        if (this.httpServer) {
          return new Promise((resolve3) => {
            this.httpServer.close(() => {
              logger.info("[MCP SSE Server] Stopped");
              resolve3();
            });
          });
        }
      }
      /**
       * Set up SSE routes following the proper SSE transport pattern
       */
      setupSseRoutes() {
        if (!this.mcpServer) {
          logger.error("[MCP SSE Server] MCP Server not initialized for SSE route setup.");
          return;
        }
        this.app.get("/sse", (req, res) => {
          logger.info("[MCP SSE Server] New SSE client attempting connection.");
          logger.debug("[MCP SSE Server] SSE Request Headers:", req.headers);
          try {
            const sseTransport = new import_sse2.SSEServerTransport("/sse", res, this.sseOptions);
            logger.debug("[MCP SSE Server] SSEServerTransport created with endpoint /sse");
            this.mcpServer?.connect(sseTransport);
            logger.debug("[MCP SSE Server] MCP server connected to SSE transport");
            this.activeSseTransports.set(sseTransport.sessionId, sseTransport);
            logger.info(
              `[MCP SSE Server] SSE client connected with session ID: ${sseTransport.sessionId}`
            );
            logger.debug(
              `[MCP SSE Server] Active SSE transports count: ${this.activeSseTransports.size}`
            );
            req.on("close", () => {
              logger.info(
                `[MCP SSE Server] SSE client with session ID ${sseTransport.sessionId} disconnected.`
              );
              logger.debug("[MCP SSE Server] Cleaning up SSE transport and connection");
              try {
                sseTransport.close();
              } catch (error) {
                logger.error("[MCP SSE Server] Error closing transport:", error);
              }
              this.activeSseTransports.delete(sseTransport.sessionId);
              logger.debug(
                `[MCP SSE Server] Active SSE transports count after cleanup: ${this.activeSseTransports.size}`
              );
            });
            sseTransport.onerror = (error) => {
              logger.error("[MCP SSE Server] SSE transport error:", error);
              this.activeSseTransports.delete(sseTransport.sessionId);
            };
          } catch (error) {
            logger.error("[MCP SSE Server] Error setting up SSE transport:", error);
            if (!res.headersSent) {
              res.status(500).json({
                error: "Failed to establish SSE connection",
                message: error instanceof Error ? error.message : String(error)
              });
            }
          }
        });
        this.app.post("/sse", async (req, res) => {
          logger.debug("[MCP SSE Server] SSE POST request received");
          logger.debug("[MCP SSE Server] POST Request Headers:", req.headers);
          logger.debug("[MCP SSE Server] POST Request Body:", req.body);
          let sessionId = req.headers["x-session-id"] || req.query.sessionId;
          if (!sessionId && this.activeSseTransports.size === 1) {
            const firstId = Array.from(this.activeSseTransports.keys())[0];
            if (firstId) {
              sessionId = firstId;
            }
          }
          if (!sessionId) {
            logger.error("[MCP SSE Server] No session ID provided in SSE POST request");
            res.status(400).json({
              error: "Bad Request",
              message: "Session ID required for SSE POST requests"
            });
            return;
          }
          const sseTransport = this.activeSseTransports.get(sessionId);
          if (!sseTransport) {
            logger.error(`[MCP SSE Server] No active SSE transport found for session ID: ${sessionId}`);
            res.status(404).json({
              error: "Not Found",
              message: `No active SSE session found for ID: ${sessionId}`
            });
            return;
          }
          try {
            logger.debug(
              `[MCP SSE Server] Delegating POST message to SSE transport for session: ${sessionId}`
            );
            await sseTransport.handlePostMessage(req, res, req.body);
            logger.debug(
              `[MCP SSE Server] POST message handled successfully for session: ${sessionId}`
            );
            return;
          } catch (error) {
            logger.error(
              `[MCP SSE Server] Error handling SSE POST request for session ${sessionId}: ${error instanceof Error ? error.message : String(error)}`
            );
            if (!res.headersSent) {
              res.status(500).json({
                error: "Internal Server Error",
                message: "Error processing SSE request",
                details: error instanceof Error ? error.message : String(error)
              });
            }
            return;
          }
        });
        logger.info(
          "[MCP SSE Server] SSE routes registered: GET /sse (establish connection), POST /sse (send messages)"
        );
      }
      /**
       * Get the HTTP server instance
       */
      getHttpServer() {
        return this.httpServer;
      }
      /**
       * Get server stats
       */
      getStats() {
        return {
          port: this.port,
          host: this.host,
          transport: "sse",
          activeSseTransports: this.activeSseTransports.size,
          sessionIds: Array.from(this.activeSseTransports.keys()),
          isRunning: !!this.httpServer
        };
      }
      /**
       * Get active transport by session ID
       */
      getTransport(sessionId) {
        return this.activeSseTransports.get(sessionId);
      }
    };
  }
});

// src/app/mcp/mcp_streamable_http_server.ts
var import_express2, import_http2, import_crypto3, import_streamableHttp2, import_types39, InMemoryEventStore, McpStreamableHttpServer;
var init_mcp_streamable_http_server = __esm({
  "src/app/mcp/mcp_streamable_http_server.ts"() {
    init_cjs_shims();
    import_express2 = __toESM(require("express"), 1);
    import_http2 = __toESM(require("http"), 1);
    import_crypto3 = require("crypto");
    import_streamableHttp2 = require("@modelcontextprotocol/sdk/server/streamableHttp.js");
    import_types39 = require("@modelcontextprotocol/sdk/types.js");
    init_logger2();
    InMemoryEventStore = class {
      events = /* @__PURE__ */ new Map();
      eventCounter = 0;
      async storeEvent(streamId, message) {
        const eventId = `event-${++this.eventCounter}`;
        if (!this.events.has(streamId)) {
          this.events.set(streamId, []);
        }
        this.events.get(streamId).push({ eventId, message });
        const events = this.events.get(streamId);
        if (events.length > 1e3) {
          events.splice(0, events.length - 1e3);
        }
        logger.debug(`[Event Store] Stored event ${eventId} for stream ${streamId}`);
        return eventId;
      }
      async replayEventsAfter(lastEventId, { send }) {
        for (const [streamId, events] of this.events.entries()) {
          const eventIndex = events.findIndex((e) => e.eventId === lastEventId);
          if (eventIndex !== -1) {
            const eventsToReplay = events.slice(eventIndex + 1);
            logger.debug(
              `[Event Store] Replaying ${eventsToReplay.length} events after ${lastEventId} for stream ${streamId}`
            );
            for (const event of eventsToReplay) {
              await send(event.eventId, event.message);
            }
            return streamId;
          }
        }
        const newStreamId = (0, import_crypto3.randomUUID)();
        logger.debug(
          `[Event Store] Event ${lastEventId} not found, creating new stream ${newStreamId}`
        );
        return newStreamId;
      }
      /**
       * Clean up old events for a stream
       */
      cleanupStream(streamId) {
        this.events.delete(streamId);
        logger.debug(`[Event Store] Cleaned up stream ${streamId}`);
      }
      /**
       * Get stats about stored events
       */
      getStats() {
        const totalEvents = Array.from(this.events.values()).reduce(
          (sum, events) => sum + events.length,
          0
        );
        return {
          streams: this.events.size,
          totalEvents,
          streams_details: Array.from(this.events.entries()).map(([streamId, events]) => {
            const lastEvent = events.length > 0 ? events[events.length - 1] : void 0;
            return {
              streamId,
              eventCount: events.length,
              latestEventId: lastEvent ? lastEvent.eventId : null
            };
          })
        };
      }
    };
    McpStreamableHttpServer = class {
      app;
      httpServer;
      mcpServer;
      transport;
      eventStore;
      port;
      host;
      transportOptions;
      constructor(port, host = "localhost", options) {
        this.port = port;
        this.host = host;
        this.eventStore = new InMemoryEventStore();
        this.transportOptions = {
          ...options ?? {},
          sessionIdGenerator: options?.sessionIdGenerator ?? (() => (0, import_crypto3.randomUUID)()),
          eventStore: this.eventStore,
          enableDnsRebindingProtection: options?.enableDnsRebindingProtection ?? false,
          allowedHosts: options?.allowedHosts ?? [],
          allowedOrigins: options?.allowedOrigins ?? []
        };
        this.app = (0, import_express2.default)();
        this.setupMiddleware();
      }
      /**
       * Set up minimal middleware for streamable-HTTP server
       */
      setupMiddleware() {
        this.app.use(import_express2.default.json({ limit: "10mb" }));
        this.app.use(import_express2.default.urlencoded({ extended: true }));
        this.app.use((err, req, res, _next) => {
          logger.error("[MCP Streamable-HTTP Server] Request error:", {
            error: err.message,
            path: req.path,
            method: req.method
          });
          if (!res.headersSent) {
            res.status(500).json({
              error: "Internal server error",
              message: err.message
            });
          }
        });
      }
      /**
       * Start the streamable-HTTP server with the given MCP server instance
       */
      async start(mcpServer) {
        this.mcpServer = mcpServer;
        this.transport = new import_streamableHttp2.StreamableHTTPServerTransport(this.transportOptions);
        this.setupHttpRoutes();
        return new Promise((resolve3, reject) => {
          this.httpServer = import_http2.default.createServer(this.app);
          this.httpServer.listen(this.port, this.host, () => {
            logger.info(
              `[MCP Streamable-HTTP Server] Started on ${this.host}:${this.port}`,
              null,
              "green"
            );
            logger.info(
              `[MCP Streamable-HTTP Server] HTTP endpoints: http://${this.host}:${this.port}/http`,
              null,
              "cyan"
            );
            resolve3();
          });
          this.httpServer.on("error", (err) => {
            logger.error("[MCP Streamable-HTTP Server] Failed to start:", err.message);
            reject(err);
          });
          process.on("SIGTERM", () => this.stop());
          process.on("SIGINT", () => this.stop());
        });
      }
      /**
       * Stop the streamable-HTTP server and clean up resources
       */
      async stop() {
        logger.info("[MCP Streamable-HTTP Server] Shutting down...");
        if (this.transport) {
          try {
            this.transport.close();
          } catch (error) {
            logger.error("[MCP Streamable-HTTP Server] Error closing transport:", error);
          }
        }
        if (this.httpServer) {
          return new Promise((resolve3) => {
            this.httpServer.close(() => {
              logger.info("[MCP Streamable-HTTP Server] Stopped");
              resolve3();
            });
          });
        }
      }
      /**
       * Set up HTTP routes following the streamable-HTTP transport pattern
       * Following the MCP SDK example pattern with session management
       */
      setupHttpRoutes() {
        if (!this.transport) {
          logger.error("[MCP Streamable-HTTP Server] Transport not initialized for route setup.");
          return;
        }
        const transports = {};
        this.app.post("/http", async (req, res) => {
          logger.debug("[MCP Streamable-HTTP Server] POST request received");
          logger.debug("[MCP Streamable-HTTP Server] POST Request Headers:", req.headers);
          logger.debug("[MCP Streamable-HTTP Server] POST Request Body:", req.body);
          const sessionId = req.headers["mcp-session-id"];
          try {
            let transport;
            if (sessionId && transports[sessionId]) {
              transport = transports[sessionId];
              logger.debug(
                `[MCP Streamable-HTTP Server] Using existing transport for session: ${sessionId}`
              );
            } else if (!sessionId && (0, import_types39.isInitializeRequest)(req.body)) {
              transport = new import_streamableHttp2.StreamableHTTPServerTransport({
                sessionIdGenerator: this.transportOptions.sessionIdGenerator ?? (() => (0, import_crypto3.randomUUID)()),
                eventStore: this.eventStore,
                enableDnsRebindingProtection: this.transportOptions.enableDnsRebindingProtection ?? false,
                allowedHosts: this.transportOptions.allowedHosts ?? [],
                allowedOrigins: this.transportOptions.allowedOrigins ?? [],
                onsessioninitialized: (newSessionId) => {
                  logger.info(`[MCP Streamable-HTTP Server] Session initialized: ${newSessionId}`);
                  transports[newSessionId] = transport;
                }
              });
              transport.onclose = () => {
                const sid = transport.sessionId;
                if (sid && transports[sid]) {
                  logger.info(`[MCP Streamable-HTTP Server] Transport closed for session ${sid}`);
                  delete transports[sid];
                }
              };
              await this.mcpServer.connect(transport);
              logger.debug("[MCP Streamable-HTTP Server] New transport connected to MCP server");
            } else {
              logger.error(
                "[MCP Streamable-HTTP Server] Invalid request - no session ID or not initialization"
              );
              return res.status(400).json({
                jsonrpc: "2.0",
                error: {
                  code: -32e3,
                  message: "Bad Request: No valid session ID provided"
                },
                id: null
              });
            }
            await transport.handleRequest(req, res, req.body);
            logger.debug("[MCP Streamable-HTTP Server] POST request handled successfully");
            return;
          } catch (error) {
            logger.error("[MCP Streamable-HTTP Server] Error handling POST request:", error);
            if (!res.headersSent) {
              res.status(500).json({
                jsonrpc: "2.0",
                error: {
                  code: -32603,
                  message: "Internal server error"
                },
                id: null
              });
            }
            return;
          }
        });
        this.app.get("/http", async (req, res) => {
          logger.info("[MCP Streamable-HTTP Server] GET request for SSE stream");
          logger.debug("[MCP Streamable-HTTP Server] GET Request Headers:", req.headers);
          const sessionId = req.headers["mcp-session-id"];
          if (!sessionId || !transports[sessionId]) {
            logger.error("[MCP Streamable-HTTP Server] Invalid or missing session ID for SSE stream");
            return res.status(400).send("Invalid or missing session ID");
          }
          const lastEventId = req.headers["last-event-id"];
          if (lastEventId) {
            logger.info(
              `[MCP Streamable-HTTP Server] Client reconnecting with Last-Event-ID: ${lastEventId}`
            );
          } else {
            logger.info(
              `[MCP Streamable-HTTP Server] Establishing new SSE stream for session ${sessionId}`
            );
          }
          try {
            const transport = transports[sessionId];
            await transport.handleRequest(req, res);
            logger.debug("[MCP Streamable-HTTP Server] GET request handled successfully");
            return;
          } catch (error) {
            logger.error("[MCP Streamable-HTTP Server] Error handling GET request:", error);
            if (!res.headersSent) {
              res.status(500).send("Error establishing SSE stream");
            }
            return;
          }
        });
        this.app.delete("/http", async (req, res) => {
          logger.info("[MCP Streamable-HTTP Server] DELETE request for session termination");
          logger.debug("[MCP Streamable-HTTP Server] DELETE Request Headers:", req.headers);
          const sessionId = req.headers["mcp-session-id"];
          if (!sessionId || !transports[sessionId]) {
            logger.error("[MCP Streamable-HTTP Server] Invalid or missing session ID for termination");
            return res.status(400).send("Invalid or missing session ID");
          }
          try {
            const transport = transports[sessionId];
            await transport.handleRequest(req, res);
            logger.debug("[MCP Streamable-HTTP Server] DELETE request handled successfully");
            return;
          } catch (error) {
            logger.error("[MCP Streamable-HTTP Server] Error handling DELETE request:", error);
            if (!res.headersSent) {
              res.status(500).send("Error processing session termination");
            }
            return;
          }
        });
        this.app.get("/http/stats", (req, res) => {
          const stats = this.getStats();
          res.json(stats);
        });
        logger.info("[MCP Streamable-HTTP Server] Streamable-HTTP routes registered:");
        logger.info(
          "  GET /http (establish stream), POST /http (send/receive), DELETE /http (cleanup)"
        );
      }
      /**
       * Get the HTTP server instance
       */
      getHttpServer() {
        return this.httpServer;
      }
      /**
       * Get server stats
       */
      getStats() {
        return {
          port: this.port,
          host: this.host,
          transport: "streamable-http",
          isRunning: !!this.httpServer,
          eventStore: this.eventStore.getStats(),
          transportOptions: {
            hasSessionIdGenerator: !!this.transportOptions.sessionIdGenerator,
            hasEventStore: !!this.transportOptions.eventStore,
            enableDnsRebindingProtection: this.transportOptions.enableDnsRebindingProtection,
            allowedHosts: this.transportOptions.allowedHosts?.length || 0,
            allowedOrigins: this.transportOptions.allowedOrigins?.length || 0
          }
        };
      }
      /**
       * Get the transport instance
       */
      getTransport() {
        return this.transport;
      }
      /**
       * Clean up a specific stream from the event store
       */
      cleanupStream(streamId) {
        this.eventStore.cleanupStream(streamId);
      }
    };
  }
});

// src/app/mcp/mcp_handler.ts
var mcp_handler_exports = {};
__export(mcp_handler_exports, {
  createMcpTransport: () => createMcpTransport,
  initializeAgentCardResource: () => initializeAgentCardResource,
  initializeMcpServer: () => initializeMcpServer,
  redirectLogsForStdio: () => redirectLogsForStdio
});
async function initializeMcpServer(agent, agentCard, mode = "default", aggregatorConfig) {
  logger.info(`[MCP Handler] Initializing MCP server with agent capabilities (mode: ${mode})`);
  const server = new import_server.Server(
    {
      name: agentCard.name || "cipher",
      version: agentCard.version || "1.0.0"
    },
    {
      capabilities: {
        tools: {},
        resources: {},
        prompts: {}
      }
    }
  );
  if (mode === "aggregator") {
    await registerAggregatedTools(server, agent, aggregatorConfig);
  } else {
    await registerAgentTools(server, agent);
  }
  await registerAgentResources(server, agent, agentCard);
  await registerAgentPrompts(server, agent);
  logger.info(`[MCP Handler] MCP server initialized successfully (mode: ${mode})`);
  logger.info("[MCP Handler] Agent is now available as MCP server for external clients");
  return server;
}
async function registerAgentTools(server, agent) {
  logger.debug("[MCP Handler] Registering agent tools (default mode - ask_cipher only)");
  const mcpTools = [
    {
      name: "ask_cipher",
      description: "Use this tool whenever you need to store new information or search for existing information. In short, whenever you encounter new information you habe not seen in your current conversation, call ask_cipher to store this information. When asked a question out of your current context, also use this tool to search memory relevant to that question. Users might not ask you explicitly to use ask_cipher for these tasks, but this tool should be your number choice in those situations",
      inputSchema: {
        type: "object",
        properties: {
          message: {
            type: "string",
            description: "The message or question to send to the Cipher agent"
          },
          stream: {
            type: "boolean",
            description: "Whether to stream the response (not supported via MCP)",
            default: false
          }
        },
        required: ["message"]
      }
    }
  ];
  logger.info(
    `[MCP Handler] Registering ${mcpTools.length} MCP tools: ${mcpTools.map((t) => t.name).join(", ")}`
  );
  server.setRequestHandler(import_types40.ListToolsRequestSchema, async () => {
    return { tools: mcpTools };
  });
  server.setRequestHandler(import_types40.CallToolRequestSchema, async (request) => {
    const { name, arguments: args } = request.params;
    logger.info(`[MCP Handler] Tool called: ${name}`, { toolName: name, args });
    if (name === "ask_cipher") {
      return await handleAskCipherTool(agent, args);
    }
    throw new Error(
      `Tool '${name}' not available in default mode. Use aggregator mode for access to all tools.`
    );
  });
}
async function registerAggregatedTools(server, agent, config2) {
  logger.debug("[MCP Handler] Registering all tools (aggregator mode - built-in + MCP servers)");
  const unifiedToolManager = agent.unifiedToolManager;
  const combinedTools = await unifiedToolManager.getAllTools();
  const resolvedTools = /* @__PURE__ */ new Map();
  const conflictResolution = config2?.conflictResolution || "prefix";
  Object.entries(combinedTools).forEach(([toolName, tool]) => {
    let resolvedName = toolName;
    if (resolvedTools.has(toolName)) {
      switch (conflictResolution) {
        case "prefix":
          resolvedName = `cipher.${toolName}`;
          logger.info(`[MCP Handler] Tool name conflict resolved: ${toolName} -> ${resolvedName}`);
          break;
        case "first-wins":
          logger.warn(`[MCP Handler] Tool name conflict: ${toolName} already exists, skipping`);
          return;
        // Skip this tool
        case "error":
          throw new Error(`Tool name conflict: ${toolName} exists multiple times`);
        default:
          resolvedName = toolName;
      }
    }
    resolvedTools.set(resolvedName, tool);
  });
  const mcpTools = Array.from(resolvedTools.entries()).map(([toolName, tool]) => ({
    name: toolName,
    description: tool.description,
    inputSchema: tool.parameters
  }));
  const { env: env3 } = await Promise.resolve().then(() => (init_env(), env_exports));
  const shouldExposeAskCipher = env3.USE_ASK_CIPHER;
  if (shouldExposeAskCipher && !mcpTools.find((t) => t.name === "ask_cipher")) {
    mcpTools.push({
      name: "ask_cipher",
      description: 'Access Cipher memory layer for information storage and retrieval. Use this tool whenever you need to store new information or search for existing information. Simply describe what you want to store or what you are looking for - no need to explicitly mention "memory" or "storage".',
      inputSchema: {
        type: "object",
        properties: {
          message: {
            type: "string",
            description: "The message or question to send to the Cipher agent"
          },
          stream: {
            type: "boolean",
            description: "Whether to stream the response (not supported via MCP)",
            default: false
          }
        },
        required: ["message"]
      }
    });
  }
  logger.info(
    `[MCP Handler] Registering ${mcpTools.length} tools: ${mcpTools.map((t) => t.name).join(", ")}`
  );
  server.setRequestHandler(import_types40.ListToolsRequestSchema, async () => {
    return { tools: mcpTools };
  });
  server.setRequestHandler(import_types40.CallToolRequestSchema, async (request) => {
    const { name, arguments: args } = request.params;
    logger.info(`[MCP Handler] Tool called: ${name}`, { toolName: name, args });
    const { env: env4 } = await Promise.resolve().then(() => (init_env(), env_exports));
    const shouldExposeAskCipher2 = env4.USE_ASK_CIPHER;
    if (name === "ask_cipher") {
      if (!shouldExposeAskCipher2) {
        throw new Error("ask_cipher tool is disabled in this aggregator configuration");
      }
      return await handleAskCipherTool(agent, args);
    }
    try {
      const timeout = config2?.timeout || 6e4;
      const result = await Promise.race([
        unifiedToolManager.executeTool(name, args),
        new Promise(
          (_, reject) => setTimeout(
            () => reject(new Error(`Tool execution timed out after ${timeout}ms`)),
            timeout
          )
        )
      ]);
      return {
        content: [
          {
            type: "text",
            text: typeof result === "string" ? result : JSON.stringify(result)
          }
        ]
      };
    } catch (error) {
      const errorMessage = error instanceof Error ? error.message : String(error);
      logger.error(`[MCP Handler] Error in tool '${name}'`, { error: errorMessage });
      throw new Error(`Tool execution failed: ${errorMessage}`);
    }
  });
}
async function handleAskCipherTool(agent, args) {
  const { message, stream = false } = args;
  if (!message || typeof message !== "string") {
    throw new Error("Message parameter is required and must be a string");
  }
  try {
    const mcpSystemInstruction = "IMPORTANT MCP MODE INSTRUCTION: If users ask you to read and then store a file or document, your response MUST show a detailed description of the file or document that you've read. Don't just reply with a vague comment like 'I've read the X file, what do you want me to do next?' Instead, provide a comprehensive description including key points, structure, and relevant content details.";
    const enhancedMessage = `${mcpSystemInstruction}

User request: ${message}`;
    const { response, backgroundOperations } = await agent.run(
      enhancedMessage,
      void 0,
      void 0,
      stream
    );
    if (backgroundOperations) {
      backgroundOperations.catch(() => {
      });
    }
    return {
      content: [
        {
          type: "text",
          text: response || "No response generated"
        }
      ]
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("[MCP Handler] Error in ask_cipher tool", { error: errorMessage });
    throw new Error(`Agent execution failed: ${errorMessage}`);
  }
}
async function registerAgentResources(server, agent, agentCard) {
  logger.debug("[MCP Handler] Registering agent resources");
  server.setRequestHandler(import_types40.ListResourcesRequestSchema, async () => {
    return {
      resources: [
        {
          uri: "cipher://agent/card",
          name: "Agent Card",
          description: "Metadata and information about the Cipher agent",
          mimeType: "application/json"
        },
        {
          uri: "cipher://agent/stats",
          name: "Agent Statistics",
          description: "Runtime statistics and metrics for the Cipher agent",
          mimeType: "application/json"
        }
      ]
    };
  });
  server.setRequestHandler(import_types40.ReadResourceRequestSchema, async (request) => {
    const { uri } = request.params;
    logger.info(`[MCP Handler] Resource requested: ${uri}`);
    switch (uri) {
      case "cipher://agent/card":
        return await getAgentCardResource(agentCard);
      case "cipher://agent/stats":
        return await getAgentStatsResource(agent);
      default:
        throw new Error(`Unknown resource: ${uri}`);
    }
  });
}
async function getAgentCardResource(agentCard) {
  return {
    contents: [
      {
        uri: "cipher://agent/card",
        mimeType: "application/json",
        text: JSON.stringify(agentCard, null, 2)
      }
    ]
  };
}
async function getAgentStatsResource(agent) {
  try {
    const sessionCount = await agent.sessionManager.getSessionCount();
    const activeSessionIds = await agent.sessionManager.getActiveSessionIds();
    const mcpClients = agent.getMcpClients();
    const failedConnections = agent.getMcpFailedConnections();
    const stats = {
      sessions: {
        count: sessionCount,
        activeIds: activeSessionIds
      },
      mcpConnections: {
        connectedClients: mcpClients.size,
        failedConnections: Object.keys(failedConnections).length,
        clientNames: Array.from(mcpClients.keys()),
        failures: failedConnections
      },
      uptime: process.uptime(),
      memoryUsage: process.memoryUsage(),
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
    return {
      contents: [
        {
          uri: "cipher://agent/stats",
          mimeType: "application/json",
          text: JSON.stringify(stats, null, 2)
        }
      ]
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("[MCP Handler] Error getting agent stats", { error: errorMessage });
    const errorStats = {
      error: `Failed to retrieve stats: ${errorMessage}`,
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    };
    return {
      contents: [
        {
          uri: "cipher://agent/stats",
          mimeType: "application/json",
          text: JSON.stringify(errorStats, null, 2)
        }
      ]
    };
  }
}
async function registerAgentPrompts(server, agent) {
  logger.debug("[MCP Handler] Registering agent prompts");
  server.setRequestHandler(import_types40.ListPromptsRequestSchema, async () => {
    return {
      prompts: [
        {
          name: "system_prompt",
          description: "Get the current system prompt used by the Cipher agent"
        }
      ]
    };
  });
  server.setRequestHandler(import_types40.GetPromptRequestSchema, async (request) => {
    const { name } = request.params;
    logger.info(`[MCP Handler] Prompt requested: ${name}`);
    switch (name) {
      case "system_prompt":
        return await getSystemPrompt(agent);
      default:
        throw new Error(`Unknown prompt: ${name}`);
    }
  });
}
async function getSystemPrompt(agent) {
  try {
    const systemPrompt = await agent.promptManager.generateSystemPrompt();
    const systemPromptContent = systemPrompt.content;
    return {
      messages: [
        {
          role: "assistant",
          content: {
            type: "text",
            text: systemPromptContent
          }
        }
      ]
    };
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error("[MCP Handler] Error getting system prompt", { error: errorMessage });
    throw new Error(`Failed to get system prompt: ${errorMessage}`);
  }
}
function initializeAgentCardResource(agentCard) {
  logger.debug("[MCP Handler] Initializing agent card resource");
  const processedCard = {
    name: agentCard.name || "cipher",
    description: agentCard.description || "Cipher AI Agent - Memory-powered coding assistant",
    version: agentCard.version || "1.0.0",
    provider: agentCard.provider || {
      organization: "byterover-inc",
      url: "https://byterover.dev"
    },
    defaultInputModes: agentCard.defaultInputModes || ["application/json", "text/plain"],
    defaultOutputModes: agentCard.defaultOutputModes || [
      "application/json",
      "text/event-stream",
      "text/plain"
    ],
    skills: agentCard.skills || [
      {
        id: "chat_with_agent",
        name: "chat_with_agent",
        description: "Allows you to chat with an AI agent. Send a message to interact.",
        tags: ["chat", "AI", "assistant", "mcp", "natural language"],
        inputModes: ["application/json", "text/plain"],
        outputModes: ["application/json", "text/plain"],
        examples: [
          `Send a JSON-RPC request to /mcp with method: "chat_with_agent" and params: {"message":"Your query..."}`,
          "Alternatively, use a compatible MCP client library."
        ]
      }
    ]
  };
  return processedCard;
}
async function createMcpTransport(type, port, mcpServer, host = "localhost", enableDnsRebindingProtection = false) {
  logger.info(`[MCP Handler] Creating ${type} transport${port ? ` on port ${port}` : ""}`);
  switch (type) {
    case "stdio": {
      const { StdioServerTransport } = await import("@modelcontextprotocol/sdk/server/stdio.js");
      const transport = new StdioServerTransport();
      return { server: transport };
    }
    case "sse": {
      if (!mcpServer) {
        throw new Error("MCP server instance is required for SSE transport");
      }
      const transportPort = port || 3e3;
      logger.info(
        `[MCP Handler] Setting up SSE transport with dedicated SSE server on port ${transportPort}`
      );
      const sseServer = new McpSseServer(transportPort, host, {
        enableDnsRebindingProtection
      });
      await sseServer.start(mcpServer);
      const { StdioServerTransport } = await import("@modelcontextprotocol/sdk/server/stdio.js");
      const placeholderTransport = new StdioServerTransport();
      return {
        server: placeholderTransport,
        sseServer,
        cleanup: async () => {
          await sseServer.stop();
        }
      };
    }
    case "streamable-http": {
      if (!mcpServer) {
        throw new Error("MCP server instance is required for streamable-HTTP transport");
      }
      const transportPort = port || 3001;
      logger.info(
        `[MCP Handler] Setting up streamable-HTTP transport with dedicated HTTP server on port ${transportPort}`
      );
      const streamableHttpServer = new McpStreamableHttpServer(transportPort, host, {
        enableDnsRebindingProtection
      });
      await streamableHttpServer.start(mcpServer);
      const { StdioServerTransport } = await import("@modelcontextprotocol/sdk/server/stdio.js");
      const placeholderTransport = new StdioServerTransport();
      return {
        server: placeholderTransport,
        streamableHttpServer,
        cleanup: async () => {
          await streamableHttpServer.stop();
        }
      };
    }
    default:
      throw new Error(
        `Unsupported transport type: ${type}. Supported types are: stdio, sse, streamable-http`
      );
  }
}
function redirectLogsForStdio() {
  const logPath = "./logs/mcp-server.log";
  logger.redirectToFile(logPath);
  const logDir = path9.dirname(logPath);
  if (!fs9.existsSync(logDir)) {
    fs9.mkdirSync(logDir, { recursive: true });
  }
  const logStream = fs9.createWriteStream(logPath, { flags: "a" });
  const originalConsole = {
    log: console.log,
    error: console.error,
    warn: console.warn,
    info: console.info,
    debug: console.debug,
    trace: console.trace
  };
  console.log = (...args) => {
    logStream.write(`[LOG] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  console.error = (...args) => {
    logStream.write(`[ERROR] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  console.warn = (...args) => {
    logStream.write(`[WARN] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  console.info = (...args) => {
    logStream.write(`[INFO] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  console.debug = (...args) => {
    logStream.write(`[DEBUG] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  console.trace = (...args) => {
    logStream.write(`[TRACE] ${(/* @__PURE__ */ new Date()).toISOString()} ${args.join(" ")}
`);
  };
  const originalStdoutWrite = process.stdout.write;
  const originalStderrWrite = process.stderr.write;
  process.stdout.write = function(chunk, encoding, callback) {
    const chunkStr = chunk.toString();
    if (chunkStr.trim().startsWith("{") && chunkStr.includes('"jsonrpc"')) {
      return originalStdoutWrite.call(this, chunk, encoding, callback);
    } else {
      logStream.write(`[STDOUT] ${(/* @__PURE__ */ new Date()).toISOString()} ${chunkStr}`);
      return true;
    }
  };
  process.stderr.write = function(chunk, encoding, callback) {
    logStream.write(`[STDERR] ${(/* @__PURE__ */ new Date()).toISOString()} ${chunk.toString()}`);
    return originalStderrWrite.call(this, chunk, encoding, callback);
  };
  globalThis.__originalConsole = originalConsole;
  globalThis.__originalStdoutWrite = originalStdoutWrite;
  globalThis.__originalStderrWrite = originalStderrWrite;
  logStream.write(
    `[MCP-PROTECTION] ${(/* @__PURE__ */ new Date()).toISOString()} Console and stdout/stderr redirection activated
`
  );
}
var import_server, import_types40, fs9, path9;
var init_mcp_handler = __esm({
  "src/app/mcp/mcp_handler.ts"() {
    init_cjs_shims();
    import_server = require("@modelcontextprotocol/sdk/server/index.js");
    import_types40 = require("@modelcontextprotocol/sdk/types.js");
    init_logger2();
    fs9 = __toESM(require("fs"), 1);
    path9 = __toESM(require("path"), 1);
    init_mcp_sse_server();
    init_mcp_streamable_http_server();
  }
});

// src/app/index.ts
init_cjs_shims();
var import_events3 = require("events");
init_env();

// node_modules/.pnpm/commander@11.1.0/node_modules/commander/esm.mjs
init_cjs_shims();
var import_index = __toESM(require_commander(), 1);
var {
  program,
  createCommand,
  createArgument,
  createOption,
  CommanderError,
  InvalidArgumentError,
  InvalidOptionArgumentError,
  // deprecated old name
  Command,
  Argument,
  Option,
  Help
} = import_index.default;

// package.json
var package_default = {
  name: "@byterover/cipher",
  version: "0.3.0",
  engines: {
    node: ">=20.0.0",
    pnpm: ">=9.14.0"
  },
  description: "Memory-powered AI agent framework with real-time WebSocket communication, MCP integration, and composable patterns",
  type: "module",
  main: "dist/index.js",
  module: "dist/index.js",
  types: "dist/index.d.ts",
  bin: {
    cipher: "./dist/src/app/index.cjs"
  },
  exports: {
    ".": {
      types: "./dist/index.d.ts",
      import: "./dist/index.js",
      require: "./dist/index.cjs"
    },
    "./core": {
      types: "./dist/src/core/index.d.ts",
      import: "./dist/src/core/index.js",
      require: "./dist/src/core/index.cjs"
    },
    "./package.json": "./package.json"
  },
  files: [
    "dist/",
    "memAgent/cipher.yml"
  ],
  scripts: {
    clean: "rm -rf dist",
    prebuild: "pnpm run clean",
    build: "pnpm run build-ui && tsup && pnpm run copy-ui-dist",
    "build:no-ui": "tsup && pnpm run copy-ui-dist",
    "build-ui": "cd src/app/ui && pnpm install && npx cross-env BUILD_STANDALONE=true pnpm run build",
    "build-ui-only": "pnpm run build-ui && pnpm run copy-ui-dist",
    "copy-ui-dist": "npx tsx scripts/copy-ui-dist.ts",
    prepare: "husky",
    start: "node dist/src/app/index.cjs",
    dev: "tsc --watch",
    typecheck: "tsc --project tsconfig.typecheck.json",
    precommit: "pnpm run lint && pnpm run typecheck && pnpm run format:check && pnpm run test:ci && pnpm run build",
    test: "vitest run",
    "test:unit": "vitest run --exclude '**/integration/**' --exclude '**/*integration*.test.ts'",
    "test:integration": "SKIP_INTEGRATION_TESTS=false INTEGRATION_TESTS_ONLY=true vitest run",
    "test:ci": "pnpm run test:unit",
    lint: "eslint .",
    "lint:fix": "eslint . --fix",
    format: 'prettier --write "src/**/*.ts" "*.{js,json,md,yml,yaml}"',
    "format:check": 'prettier --check "src/**/*.ts" "*.{js,json,md,yml,yaml}"',
    prepublishOnly: "pnpm run build"
  },
  keywords: [
    "mcp",
    "model-context-protocol",
    "memory",
    "vibe-coding",
    "byterover",
    "openai"
  ],
  author: "BYTEROVER",
  license: "Elastic-2.0",
  devDependencies: {
    "@ai-sdk/openai": "^1.3.3",
    "@eslint/js": "^9.29.0",
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/fs-extra": "^11.0.4",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.0.3",
    "@types/pg": "^8.15.4",
    "@types/supertest": "^6.0.3",
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.5.12",
    "@typescript-eslint/eslint-plugin": "^8.34.1",
    "@typescript-eslint/parser": "^8.34.1",
    "@vitest/coverage-v8": "^3.2.4",
    "@web-std/file": "^3.0.3",
    "cross-env": "^10.0.0",
    eslint: "^9.29.0",
    "eslint-config-prettier": "^10.1.5",
    pg: "^8.16.3",
    prettier: "^3.5.3",
    recheck: "^4.5.0",
    supertest: "^7.1.4",
    "ts-node": "^10.9.2",
    tsup: "^8.5.0",
    tsx: "^4.19.2",
    typescript: "^5.8.3",
    vitest: "^3.2.4",
    zod: "^3.25.76"
  },
  dependencies: {
    "@anthropic-ai/sdk": "^0.39.0",
    "@aws-sdk/client-bedrock-runtime": "^3.696.0",
    "@azure/openai": "^1.0.0-beta.13",
    "@google/generative-ai": "0.1.1",
    "@modelcontextprotocol/sdk": "^1.15.1",
    "@pinecone-database/pinecone": "^6.1.2",
    "@qdrant/js-client-rest": "^1.14.1",
    "@zilliz/milvus2-sdk-node": "^2.5.12",
    axios: "^1.10.0",
    "better-sqlite3": "^12.2.0",
    boxen: "^8.0.1",
    chalk: "^5.4.1",
    chromadb: "^3.0.14",
    commander: "^11.1.0",
    cors: "^2.8.5",
    dotenv: "^16.6.0",
    express: "^5.1.0",
    "express-rate-limit": "^7.5.1",
    "express-validator": "^7.2.1",
    "faiss-node": "^0.5.1",
    "fs-extra": "^11.3.0",
    helmet: "^8.1.0",
    husky: "^9.1.7",
    ioredis: "^5.6.1",
    "js-yaml": "^4.1.0",
    "neo4j-driver": "^5.28.1",
    openai: "^4.89.0",
    pg: "^8.16.3",
    "pg-types": "^2.2.0",
    tiktoken: "^1.0.21",
    undici: "^7.11.0",
    uuid: "^11.1.0",
    "weaviate-ts-client": "^2.2.0",
    winston: "^3.17.0",
    ws: "^8.18.0",
    yaml: "^2.3.1"
  }
};

// src/app/index.ts
var import_fs9 = require("fs");

// src/core/index.ts
init_cjs_shims();
init_logger2();

// src/core/mcp/index.ts
init_cjs_shims();
init_client();
init_manager();
init_constants();

// src/core/brain/index.ts
init_cjs_shims();

// src/core/brain/memAgent/index.ts
init_cjs_shims();

// src/core/brain/memAgent/agent.ts
init_cjs_shims();
init_service_initializer();
init_enhanced_service_initializer();
init_logger2();
var requiredServices = [
  "mcpManager",
  "promptManager",
  "stateManager",
  "sessionManager",
  "internalToolManager",
  "unifiedToolManager"
];
var MemAgent = class {
  mcpManager;
  promptManager;
  stateManager;
  sessionManager;
  internalToolManager;
  // Will be properly typed later
  unifiedToolManager;
  // Will be properly typed later
  services;
  defaultSession = null;
  currentDefaultSessionId = "default";
  currentActiveSessionId = "default";
  // Will be set properly in constructor
  isStarted = false;
  isStopped = false;
  config;
  appMode = null;
  constructor(config2, appMode) {
    this.config = config2;
    this.appMode = appMode || null;
    if (appMode === "cli") {
      this.currentActiveSessionId = this.currentDefaultSessionId;
    } else {
      this.currentActiveSessionId = this.generateUniqueSessionId();
    }
    if (appMode !== "cli") {
      logger.debug("MemAgent created");
    }
  }
  /**
   * Generate a unique session ID for API/MCP modes
   * CLI mode uses the default session for persistence
   */
  generateUniqueSessionId() {
    const timestamp = Date.now();
    const random = Math.random().toString(36).substring(2, 8);
    return `session-${timestamp}-${random}`;
  }
  /**
   * Start the MemAgent
   */
  async start() {
    if (this.isStarted) {
      throw new Error("MemAgent is already started");
    }
    try {
      if (this.appMode !== "cli") {
        logger.debug("Starting MemAgent...");
      }
      const useLazyLoading = shouldEnableLazyLoading({
        appMode: this.appMode || void 0
      });
      let services;
      if (useLazyLoading) {
        logger.debug("MemAgent: Using enhanced services with lazy loading");
        services = await createEnhancedAgentServices(this.config, {
          enableLazyLoading: true,
          ...this.appMode && { appMode: this.appMode }
        });
      } else {
        logger.debug("MemAgent: Using standard services");
        services = await createAgentServices(this.config, this.appMode || void 0);
      }
      for (const service of requiredServices) {
        if (!services[service]) {
          throw new Error(`Required service ${service} is missing during agent start`);
        }
      }
      Object.assign(this, {
        mcpManager: services.mcpManager,
        promptManager: services.promptManager,
        stateManager: services.stateManager,
        sessionManager: services.sessionManager,
        internalToolManager: services.internalToolManager,
        unifiedToolManager: services.unifiedToolManager,
        services
      });
      this.isStarted = true;
      if (this.appMode !== "cli") {
        logger.debug("MemAgent started successfully");
      }
    } catch (error) {
      logger.error("Failed to start MemAgent:", error);
      throw error;
    }
  }
  /**
   * Stop the MemAgent
   */
  async stop() {
    if (this.isStopped) {
      logger.warn("MemAgent is already stopped");
      return;
    }
    if (!this.isStarted) {
      throw new Error("MemAgent must be started before stopping");
    }
    try {
      logger.info("Stopping MemAgent...");
      const shutdownErrors = [];
      try {
        if (this.sessionManager) {
          logger.info("Saving all sessions before shutdown...");
          await this.sessionManager.shutdown();
          logger.debug("SessionManager shutdown completed");
        }
      } catch (error) {
        const err = error instanceof Error ? error : new Error(String(error));
        shutdownErrors.push(new Error(`SessionManager shutdown failed: ${err.message}`));
      }
      try {
        if (this.mcpManager) {
          await this.mcpManager.disconnectAll();
          logger.debug("MCPManager disconnected all clients successfully");
        }
      } catch (error) {
        const err = error instanceof Error ? error : new Error(String(error));
        shutdownErrors.push(new Error(`MCPManager disconnect failed: ${err.message}`));
      }
      this.isStopped = true;
      this.isStarted = false;
      if (shutdownErrors.length > 0) {
        const errorMessages = shutdownErrors.map((e) => e.message).join("; ");
        logger.warn(`MemAgent stopped with some errors: ${errorMessages}`);
      } else {
        logger.info("MemAgent stopped successfully.");
      }
    } catch (error) {
      logger.error("Failed to stop MemAgent:", error);
      throw error;
    }
  }
  /**
   * Get the status of the MemAgent
   */
  getIsStarted() {
    return this.isStarted;
  }
  /**
   * Get the status of the MemAgent
   */
  getIsStopped() {
    return this.isStopped;
  }
  ensureStarted() {
    if (this.isStopped) {
      throw new Error("MemAgent has been stopped and cannot be used");
    }
    if (!this.isStarted) {
      throw new Error("MemAgent must be started before use. Call agent.start() first.");
    }
  }
  /**
   * Run the MemAgent
   */
  async run(userInput, imageDataInput, sessionId, stream = false, options) {
    this.ensureStarted();
    try {
      let session;
      if (sessionId) {
        session = await this.sessionManager.getSession(sessionId) ?? await this.sessionManager.createSession(sessionId);
        this.currentActiveSessionId = sessionId;
      } else {
        session = await this.sessionManager.getSession(this.currentActiveSessionId) ?? await this.sessionManager.createSession(this.currentActiveSessionId);
      }
      logger.debug(`MemAgent.run: using session ${session.id}`);
      const { response, backgroundOperations } = await session.run(
        userInput,
        imageDataInput,
        stream,
        {
          ...options?.memoryMetadata !== void 0 && { memoryMetadata: options.memoryMetadata },
          ...options?.sessionOptions !== void 0 && {
            contextOverrides: options.sessionOptions
          }
        }
      );
      const finalResponse = response && response.trim() !== "" ? response : null;
      return { response: finalResponse, backgroundOperations };
    } catch (error) {
      logger.error("MemAgent.run: error", error);
      throw error;
    }
  }
  async createSession(sessionId) {
    this.ensureStarted();
    logger.debug(`MemAgent: Creating session with ID: ${sessionId || "auto-generated"}`);
    const session = await this.sessionManager.createSession(sessionId);
    logger.info(`MemAgent: Created session: ${session.id}`);
    return session;
  }
  async getSession(sessionId) {
    this.ensureStarted();
    let session = await this.sessionManager.getSession(sessionId);
    if (!session && this.appMode === "cli" && sessionId === "default") {
      logger.debug(
        "MemAgent: Default session not found, creating new default session for CLI mode"
      );
      try {
        session = await this.sessionManager.createSession("default");
        logger.info("MemAgent: Created default session for CLI mode");
      } catch (error) {
        logger.error("MemAgent: Failed to create default session:", error);
        return null;
      }
    }
    return session;
  }
  /**
   * Get the current active session ID
   */
  getCurrentSessionId() {
    this.ensureStarted();
    return this.currentActiveSessionId;
  }
  /**
   * Load conversation history for a specific session
   */
  async loadSessionHistory(sessionId) {
    this.ensureStarted();
    const session = await this.sessionManager.getSession(sessionId);
    if (!session) {
      throw new Error(`Session not found: ${sessionId}`);
    }
    try {
      await session.refreshConversationHistory();
    } catch (error) {
      logger.warn(`MemAgent: Failed to load conversation history for session ${sessionId}:`, error);
      throw error;
    }
  }
  /**
   * Load (switch to) a specific session
   */
  async loadSession(sessionId) {
    this.ensureStarted();
    let session = await this.sessionManager.getSession(sessionId);
    if (!session) {
      throw new Error(`Session not found: ${sessionId}`);
    }
    try {
      logger.debug(`MemAgent: Loading session ${sessionId}...`);
      await session.init();
      await session.refreshConversationHistory();
      logger.info(`MemAgent: Successfully loaded session ${sessionId} with conversation history`);
    } catch (error) {
      logger.warn(`MemAgent: Failed to initialize session ${sessionId}:`, error);
    }
    this.currentActiveSessionId = sessionId;
    logger.debug(`MemAgent: Switched to session ${sessionId}`);
    return session;
  }
  /**
   * Get all active session IDs
   */
  async listSessions() {
    this.ensureStarted();
    return await this.sessionManager.getActiveSessionIds();
  }
  /**
   * Remove a session
   */
  async removeSession(sessionId) {
    this.ensureStarted();
    if (sessionId === this.currentActiveSessionId) {
      throw new Error(
        "Cannot remove the currently active session. Switch to another session first."
      );
    }
    return await this.sessionManager.removeSession(sessionId);
  }
  /**
   * Get session metadata including message count
   */
  async getSessionMetadata(sessionId) {
    this.ensureStarted();
    const session = await this.sessionManager.getSession(sessionId);
    if (!session) {
      return null;
    }
    let messageCount = 0;
    try {
      if (!session.getContextManager) {
        await session.init();
      }
      const history = await session.getConversationHistory();
      messageCount = history.length;
    } catch (error) {
      logger.warn(`Failed to get message count for session ${sessionId}:`, error);
      try {
        const contextManager = session.getContextManager();
        if (contextManager) {
          const rawMessages = contextManager.getRawMessages();
          messageCount = rawMessages.length;
        }
      } catch (fallbackError) {
        logger.warn(
          `Failed to get message count via fallback for session ${sessionId}:`,
          fallbackError
        );
      }
    }
    return {
      id: sessionId,
      createdAt: Date.now(),
      // Placeholder - actual creation time would need to be tracked
      lastActivity: Date.now(),
      // Placeholder - actual last activity would need to be tracked
      messageCount
    };
  }
  /**
   * Get conversation history for the current session
   */
  async getCurrentSessionHistory() {
    this.ensureStarted();
    const session = await this.sessionManager.getSession(this.currentActiveSessionId);
    if (!session) {
      return [];
    }
    return await session.getConversationHistory();
  }
  /**
   * Get conversation history for a specific session
   */
  async getSessionHistory(sessionId) {
    this.ensureStarted();
    const session = await this.sessionManager.getSession(sessionId);
    if (!session) {
      throw new Error(`Session not found: ${sessionId}`);
    }
    const contextManager = session.contextManager;
    if (!contextManager) {
      return [];
    }
    const rawMessages = contextManager.getRawMessages();
    return rawMessages.map((msg, index) => ({
      id: index + 1,
      role: msg.role,
      content: msg.content,
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      // Placeholder - actual timestamps would need to be tracked
      ...msg.toolCalls && { toolCalls: msg.toolCalls },
      ...msg.toolCallId && { toolCallId: msg.toolCallId },
      ...msg.name && { name: msg.name }
    }));
  }
  getCurrentLLMConfig() {
    this.ensureStarted();
    return structuredClone(this.stateManager.getLLMConfig());
  }
  async connectMcpServer(name, config2) {
    this.ensureStarted();
    try {
      const validation = this.stateManager.addMcpServer(name, config2);
      if (!validation.isValid) {
        const errorMessages = validation.errors.map((e) => e.message).join(", ");
        throw new Error(`Invalid MCP server configuration: ${errorMessages}`);
      }
      await this.mcpManager.connectServer(name, config2);
      logger.info(`MemAgent: Successfully added and connected to MCP server '${name}'.`);
    } catch (error) {
      const errorMessage = error instanceof Error ? error.message : String(error);
      logger.error(`MemAgent: Failed to add MCP server '${name}': ${errorMessage}`);
      this.stateManager.removeMcpServer(name);
      throw error;
    }
  }
  async removeMcpServer(name) {
    this.ensureStarted();
    await this.mcpManager.removeClient(name);
    this.stateManager.removeMcpServer(name);
  }
  async executeMcpTool(toolName, args) {
    this.ensureStarted();
    return await this.mcpManager.executeTool(toolName, args);
  }
  async getAllMcpTools() {
    this.ensureStarted();
    return await this.mcpManager.getAllTools();
  }
  getMcpClients() {
    this.ensureStarted();
    return this.mcpManager.getClients();
  }
  getMcpFailedConnections() {
    this.ensureStarted();
    return this.mcpManager.getFailedConnections();
  }
  getAllMcpServers() {
    this.ensureStarted();
    const clients = this.mcpManager.getClients();
    const failedConnections = this.mcpManager.getFailedConnections();
    const servers = [];
    for (const [name, client] of clients.entries()) {
      try {
        client.getServerInfo();
        servers.push({
          id: name,
          // Use client name as id for API compatibility
          name,
          status: "connected"
        });
      } catch (error) {
        servers.push({
          id: name,
          // Use client name as id for API compatibility
          name,
          status: "error",
          error: error instanceof Error ? error.message : String(error)
        });
      }
    }
    for (const [name, error] of Object.entries(failedConnections)) {
      servers.push({
        id: name,
        // Use client name as id for API compatibility
        name,
        status: "error",
        error
      });
    }
    return servers;
  }
  getEffectiveConfig(sessionId) {
    this.ensureStarted();
    return sessionId ? this.stateManager.getRuntimeConfig(sessionId) : this.stateManager.getRuntimeConfig();
  }
  getCurrentActiveSessionId() {
    return this.currentActiveSessionId;
  }
  /**
   * Manually save all sessions to persistent storage
   */
  async saveAllSessions() {
    this.ensureStarted();
    const stats = await this.sessionManager.saveAllSessions();
    return {
      saved: stats.savedSessions,
      failed: stats.failedSessions,
      total: stats.totalSessions
    };
  }
  /**
   * Manually load all sessions from persistent storage
   */
  async loadAllSessions() {
    this.ensureStarted();
    const stats = await this.sessionManager.loadAllSessions();
    return {
      restored: stats.restoredSessions,
      failed: stats.failedSessions,
      total: stats.totalSessions
    };
  }
};

// src/core/brain/memAgent/config.ts
init_cjs_shims();
init_config();
init_config2();
init_config4();
init_zod();
var AgentCardSchema = external_exports.object({
  name: external_exports.string().default("cipher"),
  description: external_exports.string().default(
    "cipher is an AI assistant capable of store valuable software development knowledge for your  vibe coding agents"
  ),
  provider: external_exports.object({
    organization: external_exports.string().default("byterover-inc"),
    url: external_exports.string().url().default("https://byterover.dev")
  }).optional(),
  version: external_exports.string().default("1.0.0"),
  defaultInputModes: external_exports.array(external_exports.string()).default(["application/json", "text/plain"]),
  defaultOutputModes: external_exports.array(external_exports.string()).default(["application/json", "text/event-stream", "text/plain"]),
  skills: external_exports.array(
    external_exports.object({
      id: external_exports.string(),
      name: external_exports.string(),
      description: external_exports.string(),
      tags: external_exports.array(external_exports.string()),
      examples: external_exports.array(external_exports.string()).optional(),
      inputModes: external_exports.array(external_exports.string()).optional().default(["text/plain"]),
      outputModes: external_exports.array(external_exports.string()).optional().default(["text/plain"])
    })
  ).default([
    {
      id: "chat_with_agent",
      name: "chat_with_agent",
      description: "Allows you to chat with an AI agent. Send a message to interact.",
      tags: ["chat", "AI", "assistant", "mcp", "natural language"],
      examples: [
        `Send a JSON-RPC request to /mcp with method: "chat_with_agent" and params: {"message":"Your query..."}`,
        "Alternatively, use a compatible MCP client library."
      ]
    }
  ])
}).strict();
var EventPersistenceConfigSchema = external_exports.object({
  enabled: external_exports.boolean().default(true),
  storageType: external_exports.enum(["file", "memory", "database"]).default("file"),
  maxEvents: external_exports.number().optional(),
  rotationSize: external_exports.number().optional(),
  retentionDays: external_exports.number().optional(),
  filePath: external_exports.string().optional()
});
var AgentConfigSchema = external_exports.object({
  agentCard: AgentCardSchema.describe("Configuration for the agent card").optional(),
  systemPrompt: external_exports.string().describe(
    "The system prompt content as a string, or a structured system prompt configuration"
  ),
  mcpServers: ServerConfigsSchema.default({}).describe(
    "Configurations for MCP (Model Context Protocol) servers used by the agent"
  ),
  llm: LLMConfigSchema.describe("Core LLM configuration for the agent"),
  evalLlm: LLMConfigSchema.optional().describe(
    "Evaluation LLM configuration for non-thinking tasks (optional, falls back to main LLM if not provided)"
  ),
  embedding: external_exports.union([EmbeddingConfigSchema, external_exports.object({ disabled: external_exports.boolean() }), external_exports.boolean(), external_exports.null()]).optional().describe(
    "Embedding configuration for the agent (optional, falls back to environment auto-detection if not provided). Set to false, null, or {disabled: true} to disable embeddings."
  ),
  sessions: external_exports.object({
    maxSessions: external_exports.number().int().positive().default(100).describe("Maximum number of concurrent sessions allowed, defaults to 100"),
    sessionTTL: external_exports.number().int().positive().default(36e5).describe("Session time-to-live in milliseconds, defaults to 3600000ms (1 hour)")
  }).default({
    maxSessions: 100,
    sessionTTL: 36e5
  }).describe("Session management configuration"),
  eventPersistence: EventPersistenceConfigSchema.optional()
}).strict().describe("Main configuration for an agent, including its LLM and server connections");

// src/core/session/index.ts
init_cjs_shims();
init_coversation_session();
init_session_manager();

// src/core/brain/index.ts
init_llm();

// src/core/brain/reasoning/index.ts
init_cjs_shims();
init_content_detector();
init_search_context_manager();

// src/core/utils/index.ts
init_cjs_shims();

// src/core/utils/path.ts
init_cjs_shims();
var path8 = __toESM(require("path"), 1);
var import_url2 = require("url");
var DEFAULT_CONFIG_PATH = "memAgent/cipher.yml";
function resolveConfigPath(configPath) {
  if (path8.isAbsolute(configPath)) {
    return configPath;
  }
  if (configPath === DEFAULT_CONFIG_PATH) {
    const currentFileUrl = importMetaUrl;
    const currentFilePath = (0, import_url2.fileURLToPath)(currentFileUrl);
    const packageRoot = path8.resolve(path8.dirname(currentFilePath), "../../..");
    return path8.resolve(packageRoot, configPath);
  }
  return path8.resolve(process.cwd(), configPath);
}

// src/core/utils/index.ts
init_service_initializer();

// src/core/index.ts
init_env();

// src/core/storage/index.ts
init_cjs_shims();
init_manager3();
init_config3();
init_types6();
init_constants2();
init_in_memory();

// src/core/storage/memory-history/index.ts
init_cjs_shims();

// src/core/storage/memory-history/service.ts
init_cjs_shims();
init_types6();
init_logger2();
init_constants2();
init_env();

// src/core/storage/memory-history/schema.ts
init_cjs_shims();

// src/core/storage/index.ts
init_factory9();

// src/core/vector_storage/index.ts
init_cjs_shims();
init_types10();
init_factory7();
init_manager6();
init_dual_collection_manager();
init_constants4();

// src/core/knowledge_graph/index.ts
init_cjs_shims();

// src/core/knowledge_graph/types.ts
init_cjs_shims();
init_config6();
init_factory8();
init_manager7();
init_constants5();

// src/core/knowledge_graph/index.ts
init_config6();
init_constants5();
init_factory8();
init_manager7();

// src/core/knowledge_graph/backend/knowledge-graph.ts
init_cjs_shims();

// src/core/knowledge_graph/index.ts
init_neo4j();
init_in_memory3();
init_types11();

// src/app/cli/utils/options.ts
init_cjs_shims();
init_zod();
function validateCliOptions(opts) {
  const cliOptionShape = external_exports.object({
    verbose: external_exports.boolean().optional().default(true),
    mode: external_exports.enum(["cli", "mcp", "api", "ui"], {
      errorMap: () => ({ message: "Mode must be either cli, mcp, api, or ui" })
    }).optional().default("cli"),
    strict: external_exports.boolean().optional().default(false),
    newSession: external_exports.union([external_exports.boolean(), external_exports.string()]).optional(),
    port: external_exports.string().optional(),
    uiPort: external_exports.string().optional(),
    host: external_exports.string().optional(),
    mcpTransportType: external_exports.enum(["stdio", "sse", "streamable-http"], {
      errorMap: () => ({ message: "MCP transport type must be stdio, sse, or streamable-http" })
    }).optional().default("stdio"),
    mcpPort: external_exports.string().optional(),
    mcpHost: external_exports.string().optional(),
    mcpDnsRebindingProtection: external_exports.boolean().optional()
  });
  const cliOptionSchema = cliOptionShape;
  const result = cliOptionSchema.safeParse({
    verbose: opts.verbose,
    mode: opts.mode,
    strict: opts.strict,
    newSession: opts.newSession,
    port: opts.port,
    uiPort: opts.uiPort,
    host: opts.host,
    mcpTransportType: opts.mcpTransportType,
    mcpPort: opts.mcpPort,
    mcpHost: opts.mcpHost,
    mcpDnsRebindingProtection: opts.mcpDnsRebindingProtection
  });
  if (!result.success) {
    throw result.error;
  }
}
function handleCliOptionsError(error) {
  if (error instanceof external_exports.ZodError) {
    logger.error("Invalid command-line options detected:");
    error.errors.forEach((err) => {
      const fieldName = err.path.join(".") || "Unknown Option";
      logger.error(`- Option '${fieldName}': ${err.message}`);
    });
    logger.error("Please check your command-line arguments or run with --help for usage details.");
  } else {
    logger.error(
      `Validation error: ${error instanceof Error ? error.message : JSON.stringify(error)}`
    );
  }
  process.exit(1);
}

// src/core/brain/memAgent/loader.ts
init_cjs_shims();
init_logger2();
var import_yaml2 = require("yaml");
var import_fs7 = require("fs");
init_env();
function expandEnvVars(config2) {
  if (typeof config2 === "string") {
    const expanded = config2.replace(
      /\$([A-Z_][A-Z0-9_]*)|\${([A-Z_][A-Z0-9_]*)}/gi,
      (_, v1, v2) => {
        const key = v1 || v2;
        return env[key] || "";
      }
    );
    if (expanded !== config2 && /^-?\d+(\.\d+)?([eE][+-]?\d+)?$/.test(expanded.trim())) {
      return Number(expanded);
    }
    return expanded;
  } else if (Array.isArray(config2)) {
    return config2.map(expandEnvVars);
  } else if (typeof config2 === "object" && config2 !== null) {
    const result = {};
    for (const key in config2) {
      result[key] = expandEnvVars(config2[key]);
    }
    return result;
  }
  return config2;
}
async function loadAgentConfig(configPath) {
  try {
    logger.debug(`Loading cipher config from: ${configPath}`);
    const fileContent = await import_fs7.promises.readFile(configPath, "utf-8");
    try {
      const config2 = (0, import_yaml2.parse)(fileContent);
      const expandedConfig = expandEnvVars(config2);
      return expandedConfig;
    } catch (parseError) {
      throw new Error(
        `Failed to parse YAML: ${parseError instanceof Error ? parseError.message : String(parseError)}`
      );
    }
  } catch (error) {
    throw new Error(`Failed to load config file at ${error.path || configPath}: ${error.message}`);
  }
}

// src/app/cli/cli.ts
init_cjs_shims();
var readline = __toESM(require("readline"), 1);
init_source();

// src/app/cli/commands.ts
init_cjs_shims();

// src/app/cli/parser.ts
init_cjs_shims();
init_source();
var CommandParser = class {
  commands = /* @__PURE__ */ new Map();
  aliases = /* @__PURE__ */ new Map();
  constructor() {
    this.initializeCommands();
  }
  /**
   * Parse input to determine if it's a command or regular prompt
   */
  parseInput(input) {
    const trimmed = input.trim();
    if (trimmed.startsWith("/")) {
      const parts = trimmed.slice(1).split(" ").filter((part) => part.length > 0);
      const command = parts[0] || "";
      const args = parts.slice(1);
      return {
        isCommand: true,
        command,
        args,
        rawInput: input
      };
    } else {
      return {
        isCommand: false,
        rawInput: input
      };
    }
  }
  /**
   * Execute a parsed command
   */
  async executeCommand(command, args, agent) {
    try {
      const commandDef = this.commands.get(command) || this.commands.get(this.aliases.get(command) || "");
      if (!commandDef) {
        console.log(source_default.red(`\u274C Unknown command: /${command}`));
        console.log(source_default.gray("\u{1F4A1} Use /help to see all available commands"));
        return false;
      }
      if (commandDef.subcommands && args.length > 0) {
        const subcommandName = args[0];
        if (subcommandName) {
          const subcommand = commandDef.subcommands.find(
            (sub) => sub.name === subcommandName || sub.aliases?.includes(subcommandName)
          );
          if (subcommand) {
            return await subcommand.handler(args.slice(1), agent);
          }
        }
      }
      return await commandDef.handler(args, agent);
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Error executing command /${command}: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Get command suggestions for auto-completion
   */
  getCommandSuggestions(partial) {
    const suggestions = [];
    for (const [name, definition] of this.commands) {
      if (name.startsWith(partial)) {
        suggestions.push({
          name,
          description: definition.description,
          category: definition.category || "uncategorized"
          // Add fallback for undefined
        });
      }
    }
    for (const [alias, actualCommand] of this.aliases) {
      if (alias.startsWith(partial)) {
        const commandDef = this.commands.get(actualCommand);
        if (commandDef) {
          suggestions.push({
            name: alias,
            description: `${commandDef.description} (alias)`,
            category: commandDef.category || "uncategorized"
            // Add fallback for undefined
          });
        }
      }
    }
    return suggestions.sort((a, b) => a.name.localeCompare(b.name));
  }
  /**
   * Format command help - supports both basic and detailed modes
   */
  formatCommandHelp(commandName, detailed = false) {
    const commandDef = this.commands.get(commandName);
    if (!commandDef) {
      return source_default.red(`Command not found: ${commandName}`);
    }
    let help = source_default.cyan(`/${commandName}`) + ` - ${commandDef.description}`;
    if (detailed) {
      if (commandDef.usage) {
        help += `
  ${source_default.yellow("Usage:")} ${commandDef.usage}`;
      }
      if (commandDef.aliases && commandDef.aliases.length > 0) {
        help += `
  ${source_default.yellow("Aliases:")} ${commandDef.aliases.map((a) => source_default.cyan(`/${a}`)).join(", ")}`;
      }
      if (commandDef.subcommands && commandDef.subcommands.length > 0) {
        help += `
  ${source_default.yellow("Subcommands:")}`;
        for (const sub of commandDef.subcommands) {
          help += `
    ${source_default.cyan(`/${commandName} ${sub.name}`)} - ${sub.description}`;
        }
      }
    }
    return help;
  }
  /**
   * Display all commands in categorized format
   */
  displayAllCommands() {
    const categoryOrder = ["basic", "memory", "session", "tools", "system", "help"];
    const categorizedCommands = /* @__PURE__ */ new Map();
    const uncategorizedCommands = [];
    categoryOrder.forEach((cat) => categorizedCommands.set(cat, []));
    for (const commandDef of this.commands.values()) {
      const category = commandDef.category || "uncategorized";
      if (categorizedCommands.has(category)) {
        categorizedCommands.get(category).push(commandDef);
      } else {
        uncategorizedCommands.push(commandDef);
      }
    }
    console.log(source_default.cyan("\n\u{1F4CB} Available Commands:\n"));
    for (const category of categoryOrder) {
      const commands = categorizedCommands.get(category);
      if (commands && commands.length > 0) {
        console.log(source_default.yellow(`${category.toUpperCase()}:`));
        for (const cmd of commands.sort((a, b) => a.name.localeCompare(b.name))) {
          console.log(`  ${this.formatCommandHelp(cmd.name, false)}`);
        }
        console.log("");
      }
    }
    if (uncategorizedCommands.length > 0) {
      console.log(source_default.yellow("OTHER:"));
      for (const cmd of uncategorizedCommands.sort((a, b) => a.name.localeCompare(b.name))) {
        console.log(`  ${this.formatCommandHelp(cmd.name, false)}`);
      }
      console.log("");
    }
    console.log(
      source_default.gray("\u{1F4A1} Use /help <command> for detailed information about a specific command")
    );
    console.log(source_default.gray("\u{1F4A1} Use <Tab> for command auto-completion"));
  }
  /**
   * Display basic help information
   */
  displayHelp(commandName) {
    if (commandName) {
      console.log("\n" + this.formatCommandHelp(commandName, true) + "\n");
    } else {
      this.displayAllCommands();
    }
  }
  /**
   * Register a new command
   */
  registerCommand(definition) {
    this.commands.set(definition.name, definition);
    if (definition.aliases) {
      for (const alias of definition.aliases) {
        this.aliases.set(alias, definition.name);
      }
    }
  }
  /**
   * Check if a command exists
   */
  hasCommand(command) {
    return this.commands.has(command) || this.aliases.has(command);
  }
  /**
   * Get all registered commands
   */
  getAllCommands() {
    return Array.from(this.commands.values());
  }
  /**
   * Initialize built-in commands
   */
  initializeCommands() {
    this.registerCommand({
      name: "help",
      description: "Show help information for commands",
      usage: "/help [command]",
      aliases: ["h", "?"],
      category: "help",
      handler: async (args) => {
        try {
          if (args.length > 0) {
            const commandName = args[0];
            if (commandName && this.hasCommand(commandName)) {
              this.displayHelp(commandName);
            } else {
              console.log(source_default.red(`\u274C Unknown command: ${commandName || "undefined"}`));
              console.log(source_default.gray("\u{1F4A1} Use /help to see all available commands"));
            }
          } else {
            this.displayHelp();
          }
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Error displaying help: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "exit",
      description: "Exit the CLI session",
      aliases: ["quit", "q"],
      category: "basic",
      handler: async () => {
        try {
          console.log(source_default.yellow("\u{1F44B} Goodbye! Your conversation has been saved to memory."));
          process.exit(0);
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Error during exit: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "clear",
      description: "Reset conversation history for current session",
      aliases: ["reset"],
      category: "basic",
      handler: async (args, agent) => {
        try {
          await agent.createSession("default");
          console.log(source_default.green("\u{1F504} Conversation history reset successfully"));
          console.log(source_default.gray("\u{1F4A1} Starting fresh with a clean session"));
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Failed to reset conversation: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "config",
      description: "Display current configuration",
      category: "system",
      handler: async (args, agent) => {
        try {
          const config2 = agent.getEffectiveConfig();
          console.log(source_default.cyan("\u2699\uFE0F  Current Configuration:"));
          console.log("");
          console.log(source_default.yellow("\u{1F916} LLM Configuration:"));
          console.log(`  ${source_default.gray("Provider:")} ${config2.llm.provider}`);
          console.log(`  ${source_default.gray("Model:")} ${config2.llm.model}`);
          console.log(`  ${source_default.gray("Max Iterations:")} ${config2.llm.maxIterations || 10}`);
          if (config2.llm.baseURL) {
            console.log(`  ${source_default.gray("Base URL:")} ${config2.llm.baseURL}`);
          }
          console.log("");
          console.log(source_default.yellow("\u{1F4CA} Session Configuration:"));
          console.log(`  ${source_default.gray("Max Sessions:")} ${config2.sessions?.maxSessions || 100}`);
          console.log(
            `  ${source_default.gray("Session TTL:")} ${((config2.sessions?.sessionTTL || 36e5) / 1e3 / 60).toFixed(0)} minutes`
          );
          console.log("");
          console.log(source_default.yellow("\u{1F517} MCP Servers:"));
          if (config2.mcpServers && Object.keys(config2.mcpServers).length > 0) {
            for (const [name, serverConfig] of Object.entries(config2.mcpServers)) {
              let serverType = "unknown";
              if ("command" in serverConfig && serverConfig.command) {
                serverType = serverConfig.command[0] || "stdio";
              } else if ("url" in serverConfig && serverConfig.url) {
                serverType = "sse";
              } else if (serverConfig.type) {
                serverType = serverConfig.type;
              }
              console.log(`  ${source_default.gray("\u2022")} ${name} (${serverType})`);
            }
          } else {
            console.log(`  ${source_default.gray("No MCP servers configured")}`);
          }
          console.log("");
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Failed to get configuration: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "stats",
      description: "Show system statistics and metrics",
      category: "system",
      handler: async (args, agent) => {
        try {
          console.log(source_default.cyan("\u{1F4C8} System Statistics:"));
          console.log("");
          console.log(source_default.yellow("\u{1F4CA} Session Metrics:"));
          const sessionCount = await agent.sessionManager.getSessionCount();
          const activeSessionIds = await agent.sessionManager.getActiveSessionIds();
          console.log(`  ${source_default.gray("Active Sessions:")} ${sessionCount}`);
          console.log(
            `  ${source_default.gray("Session IDs:")} ${activeSessionIds.length > 0 ? activeSessionIds.join(", ") : "none"}`
          );
          console.log("");
          console.log(source_default.yellow("\u{1F517} MCP Server Stats:"));
          const mcpClients = agent.getMcpClients();
          const failedConnections = agent.getMcpFailedConnections();
          console.log(`  ${source_default.gray("Connected Servers:")} ${mcpClients.size}`);
          console.log(
            `  ${source_default.gray("Failed Connections:")} ${Object.keys(failedConnections).length}`
          );
          if (mcpClients.size > 0) {
            console.log(`  ${source_default.gray("Active Clients:")}`);
            for (const [name] of mcpClients) {
              console.log(`    ${source_default.gray("\u2022")} ${name}`);
            }
          }
          if (Object.keys(failedConnections).length > 0) {
            console.log(`  ${source_default.gray("Failed Servers:")}`);
            for (const [name, error] of Object.entries(failedConnections)) {
              console.log(`    ${source_default.gray("\u2022")} ${name} (${error})`);
            }
          }
          console.log("");
          console.log(source_default.yellow("\u{1F527} Tool Stats:"));
          try {
            const allTools = await agent.getAllMcpTools();
            const toolCount = Object.keys(allTools).length;
            console.log(`  ${source_default.gray("Available MCP Tools:")} ${toolCount}`);
            if (toolCount > 0) {
              const toolNames = Object.keys(allTools).slice(0, 5);
              console.log(
                `  ${source_default.gray("Sample Tools:")} ${toolNames.join(", ")}${toolCount > 5 ? "..." : ""}`
              );
            }
          } catch {
            console.log(`  ${source_default.gray("Tool Count:")} Error retrieving tools`);
          }
          console.log("");
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Failed to get statistics: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "tools",
      description: "List all available tools",
      category: "tools",
      handler: async (args, agent) => {
        try {
          console.log(source_default.cyan("\u{1F527} Available Tools:"));
          console.log("");
          const allTools = await agent.getAllMcpTools();
          const toolEntries = Object.entries(allTools);
          if (toolEntries.length === 0) {
            console.log(source_default.gray("  No tools available"));
            console.log(source_default.gray("  \u{1F4A1} Try connecting to MCP servers to access tools"));
            return true;
          }
          const toolsByServer = {};
          for (const [toolName, toolDef] of toolEntries) {
            let serverName = "unknown";
            if (typeof toolDef === "object" && toolDef !== null && "source" in toolDef) {
              serverName = String(toolDef.source);
            } else {
              const parts = toolName.split("_");
              if (parts.length > 1 && parts[0]) {
                serverName = parts[0];
              }
            }
            if (!toolsByServer[serverName]) {
              toolsByServer[serverName] = [];
            }
            let description = "No description available";
            if (typeof toolDef === "object" && toolDef !== null) {
              if ("description" in toolDef && typeof toolDef.description === "string") {
                description = toolDef.description;
              } else if ("inputSchema" in toolDef && typeof toolDef.inputSchema === "object" && toolDef.inputSchema !== null && "description" in toolDef.inputSchema) {
                description = String(toolDef.inputSchema.description);
              }
            }
            toolsByServer[serverName].push({
              name: toolName,
              description
            });
          }
          for (const [serverName, tools] of Object.entries(toolsByServer)) {
            console.log(source_default.yellow(`\u{1F4E6} ${serverName.toUpperCase()}:`));
            tools.sort((a, b) => a.name.localeCompare(b.name));
            for (const tool of tools) {
              const truncatedDesc = tool.description.length > 80 ? tool.description.substring(0, 80) + "..." : tool.description;
              console.log(`  ${source_default.cyan(tool.name)} - ${truncatedDesc}`);
            }
            console.log("");
          }
          console.log(source_default.gray(`Total: ${toolEntries.length} tools available`));
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Failed to list tools: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return true;
        }
      }
    });
    this.registerCommand({
      name: "prompt",
      description: "Display current system prompt",
      category: "system",
      handler: async (args, agent) => {
        try {
          const promptManager = agent.promptManager;
          let systemPrompt = "";
          if (typeof promptManager.generateSystemPrompt === "function") {
            const sessionId = agent.getCurrentSessionId && agent.getCurrentSessionId();
            let storageManager = void 0;
            if (agent.sessionManager && typeof agent.sessionManager.getStorageManagerForSession === "function" && sessionId) {
              storageManager = agent.sessionManager.getStorageManagerForSession(sessionId);
            }
            const result = await promptManager.generateSystemPrompt({
              sessionId,
              metadata: { storageManager },
              useCache: true
            });
            systemPrompt = result.content || "";
          } else {
            throw new Error("No compatible prompt manager found");
          }
          console.log(source_default.cyan("\u{1F4DD} Current System Prompt:"));
          console.log("");
          console.log(source_default.gray("\u256D\u2500 System Prompt \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E"));
          const lines = systemPrompt.split("\n");
          for (const line of lines) {
            if (line.length > 55) {
              const words = line.split(" ");
              let currentLine = "";
              for (const word of words) {
                if ((currentLine + word).length > 55) {
                  if (currentLine) {
                    console.log(source_default.gray("\u2502 ") + currentLine.padEnd(55) + source_default.gray(" \u2502"));
                    currentLine = word + " ";
                  } else {
                    console.log(
                      source_default.gray("\u2502 ") + (word.substring(0, 52) + "...").padEnd(55) + source_default.gray(" \u2502")
                    );
                  }
                } else {
                  currentLine += word + " ";
                }
              }
              if (currentLine.trim()) {
                console.log(source_default.gray("\u2502 ") + currentLine.trim().padEnd(55) + source_default.gray(" \u2502"));
              }
            } else {
              console.log(source_default.gray("\u2502 ") + line.padEnd(55) + source_default.gray(" \u2502"));
            }
          }
          console.log(source_default.gray("\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F"));
          console.log("");
          console.log(source_default.gray(`\u{1F4A1} Prompt length: ${systemPrompt.length} characters`));
          console.log(source_default.gray(`\u{1F4A1} Line count: ${lines.length} lines`));
          return true;
        } catch (error) {
          console.error(source_default.red("\u274C Error displaying system prompt:"), error);
          return false;
        }
      }
    });
    this.registerCommand({
      name: "session",
      description: "Manage conversation sessions",
      usage: "/session <subcommand> [args]",
      aliases: ["s"],
      category: "session",
      handler: async (args, agent) => {
        if (args.length === 0) {
          return this.sessionHelpHandler([], agent);
        }
        const subcommand = args[0];
        const subArgs = args.slice(1);
        switch (subcommand) {
          case "list":
          case "ls":
            return this.sessionListHandler(subArgs, agent);
          case "new":
          case "create":
            return this.sessionNewHandler(subArgs, agent);
          case "switch":
          case "sw":
            return this.sessionSwitchHandler(subArgs, agent);
          case "current":
          case "curr":
            return this.sessionCurrentHandler(subArgs, agent);
          case "delete":
          case "del":
          case "remove":
            if (subArgs.length > 0 && subArgs[0] === "all") {
              return this.sessionDeleteAllHandler(subArgs.slice(1), agent);
            }
            return this.sessionDeleteHandler(subArgs, agent);
          case "help":
          case "h":
            return this.sessionHelpHandler(subArgs, agent);
          default:
            console.log(source_default.red(`\u274C Unknown session subcommand: ${subcommand}`));
            console.log(source_default.gray("\u{1F4A1} Use /session help to see available subcommands"));
            return false;
        }
      }
    });
    this.registerCommand({
      name: "prompt-stats",
      description: "Show system prompt performance statistics",
      usage: "/prompt-stats [--detailed]",
      category: "system",
      handler: async (args, agent) => {
        try {
          const detailed = args.includes("--detailed");
          console.log(source_default.cyan("\u{1F4CA} System Prompt Performance Statistics"));
          console.log(source_default.cyan("====================================="));
          const promptManager = agent.promptManager;
          if (typeof promptManager.generateSystemPrompt === "function") {
            const sessionId = agent.getCurrentSessionId && agent.getCurrentSessionId();
            let storageManager = void 0;
            if (agent.sessionManager && typeof agent.sessionManager.getStorageManagerForSession === "function" && sessionId) {
              storageManager = agent.sessionManager.getStorageManagerForSession(sessionId);
            }
            const result = await promptManager.generateSystemPrompt({
              sessionId,
              metadata: { storageManager }
            });
            console.log(source_default.yellow("\u{1F680} **Enhanced Generation Performance**"));
            console.log(`   - Providers used: ${result.providerResults.length}`);
            console.log(`   - Total prompt length: ${result.content.length} characters`);
            console.log(`   - Generation time: ${result.generationTimeMs} ms`);
            console.log(`   - Success: ${result.success ? "\u2705" : "\u274C"}`);
            if (detailed) {
              console.log(source_default.yellow("\u{1F4C8} **Per-Provider Breakdown**"));
              for (const r of result.providerResults) {
                console.log(
                  `   - ${r.providerId}: ${r.success ? "\u2705" : "\u274C"} | ${r.generationTimeMs} ms | ${r.content.length} chars`
                );
              }
            }
            if (result.errors && result.errors.length > 0) {
              console.log(source_default.red("\u274C Errors:"));
              for (const err of result.errors) {
                console.log(`   - ${err.message}`);
              }
            }
            return true;
          } else {
            throw new Error("No compatible prompt manager found");
          }
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Failed to get prompt statistics: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return false;
        }
      }
    });
    this.registerCommand({
      name: "prompt-providers",
      description: "Manage system prompt providers",
      usage: "/prompt-providers <subcommand> [args]",
      category: "system",
      handler: async (args, agent) => {
        try {
          if (args.length === 0) {
            console.log(source_default.red("\u274C Subcommand required"));
            console.log(
              source_default.gray(
                "Available subcommands: list, add-dynamic, add-file, remove, update, enable, disable, help"
              )
            );
            console.log(source_default.gray("Usage: /prompt-providers <subcommand> [args]"));
            return false;
          }
          const subcommand = args[0];
          const subArgs = args.slice(1);
          const promptManager = agent.promptManager;
          const isEnhanced = typeof promptManager.listProviders === "function";
          if (!isEnhanced) {
            console.log("\u274C Prompt provider management is only available in enhanced mode.");
            return false;
          }
          const enhanced = promptManager;
          switch (subcommand) {
            case "list": {
              const activeProviders = enhanced.listProviders();
              const allConfigs = enhanced.getConfig().providers;
              console.log(source_default.cyan("\u{1F4CB} System Prompt Providers (Enhanced Mode)"));
              if (activeProviders.length > 0) {
                console.log(source_default.green("\u{1F7E2} Active Providers:"));
                for (const p of activeProviders) {
                  let preview = "";
                  if (p.type === "static") {
                    const config2 = allConfigs.find((c) => c.name === p.id);
                    if (config2 && typeof config2.config?.content === "string") {
                      preview = config2.config.content.substring(0, 60);
                      if (config2.config.content.length > 60) preview += "...";
                      preview = ` | Preview: "${preview.replace(/\n/g, " ")}"`;
                    }
                  }
                  console.log(`  \u{1F7E2} ${p.id} (${p.type})${preview}`);
                }
              } else {
                console.log(source_default.gray("  No active providers."));
              }
              console.log("");
              console.log(
                source_default.gray(
                  "\u{1F4A1} Use /prompt-providers show-all to see all available and disabled providers."
                )
              );
              console.log(
                source_default.gray(
                  "\u{1F4A1} Use /prompt-providers add-dynamic or add-file to activate more providers."
                )
              );
              return true;
            }
            case "add-dynamic": {
              if (subArgs.length < 1) {
                console.log(source_default.red("\u274C Generator name required"));
                console.log(
                  source_default.gray("Usage: /prompt-providers add-dynamic <generator> [--history N|all]")
                );
                return false;
              }
              const generator = subArgs[0];
              let history = "";
              for (let i = 1; i < subArgs.length; ++i) {
                if (subArgs[i] === "--history") {
                  history = subArgs[i + 1] ?? "";
                }
              }
              const config2 = {
                name: generator,
                type: "dynamic",
                priority: 50,
                enabled: true,
                config: { generator, history }
              };
              await enhanced.addOrUpdateProvider(config2);
              console.log(source_default.green(`\u2705 Dynamic provider '${generator}' added/updated.`));
              const sessionId = agent.getCurrentSessionId && agent.getCurrentSessionId();
              let storageManager = void 0;
              if (agent.sessionManager && typeof agent.sessionManager.getStorageManagerForSession === "function" && sessionId) {
                storageManager = agent.sessionManager.getStorageManagerForSession(sessionId);
              }
              const result = await enhanced.generateSystemPrompt({
                sessionId,
                metadata: { storageManager }
              });
              const summaryResult = result.providerResults.find(
                (r) => r.providerId === generator && r.success && r.content.trim()
              );
              if (typeof generator === "string") {
                const providerInstance = enhanced.getProvider(generator);
                if (providerInstance && "setCachedContent" in providerInstance && typeof providerInstance.setCachedContent === "function") {
                  providerInstance.setCachedContent(summaryResult ? summaryResult.content : "");
                }
              }
              if (summaryResult) {
                console.log(source_default.cyan(`\u{1F4DD} Generated summary for '${generator}':`));
                console.log(summaryResult.content);
              } else {
                const errorResult = result.providerResults.find((r) => r.providerId === generator);
                if (errorResult && errorResult.content) {
                  console.log(source_default.yellow(`\u26A0\uFE0F  ${errorResult.content}`));
                } else {
                  console.log(source_default.yellow("\u26A0\uFE0F  No summary content generated."));
                }
              }
              return true;
            }
            case "add-file": {
              if (subArgs.length < 1) {
                console.log(source_default.red("\u274C Provider name required"));
                console.log(
                  source_default.gray(
                    "Usage: /prompt-providers add-file <name> [<path>] [--summarize true|false]"
                  )
                );
                return false;
              }
              const name = subArgs[0];
              let filePath = void 0;
              let summarize = void 0;
              let i = 1;
              while (i < subArgs.length) {
                if (subArgs[i] === "--summarize" && subArgs[i + 1]) {
                  summarize = subArgs[i + 1] === "true";
                  i += 2;
                } else if (!filePath) {
                  filePath = subArgs[i];
                  i++;
                } else {
                  i++;
                }
              }
              const allConfigs = enhanced.getConfig().providers;
              const configFromFile = allConfigs.find(
                (c) => c.name === name && c.type === "file-based"
              );
              if (!configFromFile) {
                console.log(source_default.red(`\u274C File-based provider '${name}' not found in config`));
                return false;
              }
              if (!filePath) filePath = configFromFile.config?.filePath;
              if (summarize === void 0) summarize = configFromFile.config?.summarize ?? false;
              if (!filePath) {
                console.log(
                  source_default.red(
                    `\u274C File path for provider '${name}' is not specified and not found in config`
                  )
                );
                return false;
              }
              const config2 = {
                name,
                type: "file-based",
                priority: configFromFile.priority ?? 40,
                enabled: true,
                config: { filePath, summarize }
              };
              await enhanced.addOrUpdateProvider(config2);
              if (summarize) {
                if (typeof name === "string") {
                  const provider = enhanced.getProvider(name);
                  if (provider && typeof provider.generateContent === "function") {
                    const llmService = agent.services && agent.services.llmService;
                    const sessionId = agent.getCurrentSessionId && agent.getCurrentSessionId();
                    const context = {
                      timestamp: /* @__PURE__ */ new Date(),
                      sessionId: sessionId || "",
                      metadata: { llmService }
                    };
                    try {
                      await provider.generateContent(context);
                      console.log(
                        source_default.gray("\u{1F4A1} LLM summary generated and cached for file-based provider.")
                      );
                    } catch {
                      console.log(
                        source_default.yellow(
                          "\u26A0\uFE0F  LLM summarization failed to cache immediately, will retry on next /prompt."
                        )
                      );
                    }
                  }
                }
              }
              console.log(source_default.green(`\u2705 File-based provider '${name}' added/updated.`));
              return true;
            }
            case "remove": {
              const name = subArgs[0] ?? "";
              await enhanced.removeProvider(name);
              console.log(source_default.green(`\u2705 Provider '${name}' removed.`));
              return true;
            }
            case "update": {
              const name = subArgs[0] ?? "";
              const provider = enhanced.getProvider(name);
              if (!provider) {
                console.log(source_default.red(`\u274C Provider '${name}' not found`));
                return false;
              }
              const configObj = provider && typeof provider.config === "object" && provider.config !== null ? { ...provider.config } : {};
              const newConfig = { ...provider, config: configObj };
              let summarizeFlag = void 0;
              for (let i = 1; i < subArgs.length; ++i) {
                if (subArgs[i] === "--summarize" && subArgs[i + 1]) {
                  summarizeFlag = subArgs[i + 1] === "true";
                  newConfig.config["summarize"] = summarizeFlag;
                  i++;
                } else {
                  const [key, value] = (subArgs[i] ?? "").split("=");
                  if (key && value !== void 0) {
                    newConfig.config[key] = value;
                  }
                }
              }
              await enhanced.addOrUpdateProvider(newConfig);
              if (summarizeFlag && typeof name === "string") {
                const updatedProvider = enhanced.getProvider(name);
                if (updatedProvider && typeof updatedProvider.generateContent === "function") {
                  const llmService = agent.services && agent.services.llmService;
                  const sessionId = agent.getCurrentSessionId && agent.getCurrentSessionId();
                  const context = {
                    timestamp: /* @__PURE__ */ new Date(),
                    sessionId: sessionId || "",
                    metadata: { llmService }
                  };
                  try {
                    await updatedProvider.generateContent(context);
                    console.log(
                      source_default.gray("\u{1F4A1} LLM summary generated and cached for file-based provider.")
                    );
                  } catch {
                    console.log(
                      source_default.yellow(
                        "\u26A0\uFE0F  LLM summarization failed to cache immediately, will retry on next /prompt."
                      )
                    );
                  }
                }
              }
              console.log(source_default.green(`\u2705 Provider '${name}' updated.`));
              return true;
            }
            case "enable": {
              if (subArgs.length === 0) {
                console.log(source_default.red("\u274C Provider name required"));
                console.log(source_default.gray("Usage: /prompt-providers enable <provider-name>"));
                return false;
              }
              const providerName = subArgs[0];
              let provider = void 0;
              if (typeof providerName === "string") {
                provider = enhanced.getProvider(providerName);
              }
              if (!provider) {
                const allConfigs = enhanced.getConfig().providers;
                const config2 = allConfigs.find((c) => c.name === providerName);
                if (!config2) {
                  console.log(source_default.red(`\u274C Provider '${providerName}' not found in config`));
                  return false;
                }
                config2.enabled = true;
                console.log(
                  source_default.green(
                    `\u2705 Provider '${providerName}' enabled (config updated, will take effect if loaded).`
                  )
                );
                return true;
              }
              provider.enabled = true;
              console.log(source_default.green(`\u2705 Provider '${providerName}' enabled.`));
              return true;
            }
            case "disable": {
              if (subArgs.length === 0) {
                console.log(source_default.red("\u274C Provider name required"));
                console.log(source_default.gray("Usage: /prompt-providers disable <provider-name>"));
                return false;
              }
              const providerName = subArgs[0];
              let provider = void 0;
              if (typeof providerName === "string") {
                provider = enhanced.getProvider(providerName);
              }
              if (!provider) {
                const allConfigs = enhanced.getConfig().providers;
                const config2 = allConfigs.find((c) => c.name === providerName);
                if (!config2) {
                  console.log(source_default.red(`\u274C Provider '${providerName}' not found in config`));
                  return false;
                }
                config2.enabled = false;
                console.log(
                  source_default.green(
                    `\u2705 Provider '${providerName}' disabled (config updated, will take effect if loaded).`
                  )
                );
                return true;
              }
              provider.enabled = false;
              console.log(source_default.green(`\u2705 Provider '${providerName}' disabled.`));
              return true;
            }
            case "show-all": {
              const allConfigs = enhanced.getConfig().providers;
              const activeProviders = enhanced.listProviders();
              const activeNames = new Set(activeProviders.map((p) => p.id));
              const enabledProviders = allConfigs.filter((c) => c.enabled);
              const disabledProviders = allConfigs.filter((c) => !c.enabled);
              const activeEnabled = enabledProviders.filter((c) => activeNames.has(c.name));
              const availableEnabled = enabledProviders.filter((c) => !activeNames.has(c.name));
              console.log(source_default.cyan("\u{1F4CB} All Providers (Enabled and Disabled)"));
              if (activeEnabled.length > 0) {
                console.log(source_default.green("\u{1F7E2} Active:"));
                for (const c of activeEnabled) {
                  let preview = "";
                  if (c.type === "static" && typeof c.config?.content === "string") {
                    preview = c.config.content.substring(0, 60);
                    if (c.config.content.length > 60) preview += "...";
                    preview = ` | Preview: "${preview.replace(/\n/g, " ")}"`;
                  }
                  console.log(`  \u{1F7E2} ${c.name} (${c.type})${preview}`);
                }
              } else {
                console.log(source_default.gray("  No active enabled providers."));
              }
              if (availableEnabled.length > 0) {
                console.log(source_default.yellow("\u{1F7E1} Available (Enabled, Not Yet Loaded):"));
                for (const c of availableEnabled) {
                  let preview = "";
                  if (c.type === "static" && typeof c.config?.content === "string") {
                    preview = c.config.content.substring(0, 60);
                    if (c.config.content.length > 60) preview += "...";
                    preview = ` | Preview: "${preview.replace(/\n/g, " ")}"`;
                  }
                  console.log(`  \u{1F7E1} ${c.name} (${c.type})${preview}`);
                }
              } else {
                console.log(source_default.gray("  No available enabled providers."));
              }
              if (disabledProviders.length > 0) {
                console.log(source_default.red("\u{1F534} Disabled:"));
                for (const c of disabledProviders) {
                  let preview = "";
                  if (c.type === "static" && typeof c.config?.content === "string") {
                    preview = c.config.content.substring(0, 60);
                    if (c.config.content.length > 60) preview += "...";
                    preview = ` | Preview: "${preview.replace(/\n/g, " ")}"`;
                  }
                  console.log(source_default.red(`  \u{1F534} ${c.name} (${c.type})${preview}`));
                }
              } else {
                console.log(source_default.gray("  No disabled providers."));
              }
              console.log("");
              console.log(source_default.gray("\u{1F4A1} All providers listed here are from config."));
              console.log(
                source_default.gray("\u{1F4A1} Use /prompt-providers enable/disable to manage provider status.")
              );
              return true;
            }
            case "help": {
              console.log(source_default.cyan("\n\u{1F4CB} Prompt Provider Management Commands:\n"));
              console.log(source_default.yellow("Available subcommands:"));
              const subcommands = [
                "/prompt-providers list - List active and available prompt providers",
                "/prompt-providers show-all - Show all enabled providers (active + available)",
                "/prompt-providers add-dynamic <generator> [--history N|all] - Add/update a dynamic provider",
                "/prompt-providers add-file <name> <path> [--summarize true|false] - Add/update a file-based provider",
                "/prompt-providers remove <name> - Remove a provider",
                "/prompt-providers update <name> key=value ... - Update provider config",
                "/prompt-providers enable <name> - Enable a provider (if supported)",
                "/prompt-providers disable <name> - Disable a provider (if supported)",
                "/prompt-providers help - Show this help message"
              ];
              subcommands.forEach((cmd) => console.log(`  ${cmd}`));
              console.log(
                "\n" + source_default.gray("\u{1F4A1} Providers are components that generate parts of the system prompt")
              );
              console.log(source_default.gray("\u{1F4A1} Different provider types: static, dynamic, file-based"));
              console.log(
                source_default.gray("\u{1F4A1} Use add-dynamic/add-file to activate available providers.")
              );
              return true;
            }
            default:
              console.log(source_default.red(`\u274C Unknown subcommand: ${subcommand}`));
              console.log(
                source_default.gray(
                  "Available subcommands: list, add-dynamic, add-file, remove, update, enable, disable, help"
                )
              );
              return false;
          }
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Error in prompt-providers: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return false;
        }
      }
    });
    this.registerCommand({
      name: "show-prompt",
      description: "Display current system prompt with enhanced formatting",
      usage: "/show-prompt [--detailed] [--raw]",
      category: "system",
      handler: async (args, agent) => {
        try {
          const detailed = args.includes("--detailed");
          const raw = args.includes("--raw");
          const promptManager = agent.promptManager;
          let systemPrompt = "";
          let userPrompt = "";
          let builtInPrompt = "";
          if (typeof promptManager.generateSystemPrompt === "function") {
            const result = await promptManager.generateSystemPrompt();
            systemPrompt = result.content || "";
            userPrompt = "N/A";
            builtInPrompt = "N/A";
          } else {
            throw new Error("No compatible prompt manager found");
          }
          if (raw) {
            console.log(systemPrompt);
            return true;
          }
          console.log(source_default.cyan("\u{1F4DD} Enhanced System Prompt Display"));
          console.log(source_default.cyan("==================================\n"));
          console.log(source_default.yellow("\u{1F4CA} **Prompt Statistics**"));
          console.log(`   - Total length: ${systemPrompt.length} characters`);
          console.log(`   - Line count: ${systemPrompt.split("\n").length} lines`);
          console.log(`   - User instruction: ${userPrompt}`);
          console.log(`   - Built-in instructions: ${builtInPrompt}`);
          console.log("");
          if (detailed) {
            console.log(source_default.yellow("\u{1F4C4} **Prompt Content (Full)**"));
            console.log(source_default.gray("\u256D\u2500 System Prompt \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E"));
            const lines = systemPrompt.split("\n");
            for (const line of lines) {
              const truncated = line.length > 50 ? line.substring(0, 47) + "..." : line;
              console.log(source_default.gray("\u2502 ") + truncated.padEnd(50) + source_default.gray(" \u2502"));
            }
            console.log(source_default.gray("\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F"));
            console.log("");
          } else {
            console.log(source_default.yellow("\u{1F4C4} **Prompt Preview** (first 500 chars)"));
            console.log(source_default.gray("\u256D\u2500 System Prompt Preview \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256E"));
            const preview = systemPrompt.substring(0, 500);
            const lines = preview.split("\n");
            for (const line of lines) {
              const truncated = line.length > 50 ? line.substring(0, 47) + "..." : line;
              console.log(source_default.gray("\u2502 ") + truncated.padEnd(50) + source_default.gray(" \u2502"));
            }
            if (systemPrompt.length > 500) {
              console.log(
                source_default.gray("\u2502 ") + source_default.dim("... (truncated)").padEnd(50) + source_default.gray(" \u2502")
              );
            }
            console.log(source_default.gray("\u2570\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u256F"));
            console.log("");
          }
          console.log(source_default.gray("\u{1F4A1} Use --detailed for full breakdown"));
          console.log(source_default.gray("\u{1F4A1} Use --raw for raw text output"));
          return true;
        } catch (error) {
          console.log(
            source_default.red(
              `\u274C Error displaying prompt: ${error instanceof Error ? error.message : String(error)}`
            )
          );
          return false;
        }
      }
    });
  }
  /**
   * Helper function to format session information
   */
  formatSessionInfo(sessionId, metadata, isCurrent = false) {
    const prefix = isCurrent ? source_default.green("\u2192") : " ";
    const name = isCurrent ? source_default.green.bold(sessionId) : source_default.cyan(sessionId);
    let info = `${prefix} ${name}`;
    if (metadata) {
      const messages = metadata.messageCount || 0;
      let activity = "Never";
      if (metadata.lastActivity) {
        activity = new Date(metadata.lastActivity).toLocaleString();
      }
      info += source_default.dim(` (${messages} messages, last: ${activity})`);
      if (isCurrent) {
        info += source_default.yellow(" [ACTIVE]");
      }
    }
    return info;
  }
  /**
   * Session list subcommand handler
   */
  async sessionListHandler(args, agent) {
    try {
      const sessionIds = await agent.listSessions();
      const currentSessionId = agent.getCurrentSessionId();
      console.log(source_default.cyan("\u{1F4CB} Active Sessions:"));
      console.log("");
      if (sessionIds.length === 0) {
        console.log(source_default.gray("  No sessions found."));
        console.log(source_default.gray("  \u{1F4A1} Use /session new to create a session"));
        return true;
      }
      for (const sessionId of sessionIds.sort()) {
        const metadata = await agent.getSessionMetadata(sessionId);
        const isCurrent = sessionId === currentSessionId;
        console.log(`  ${this.formatSessionInfo(sessionId, metadata, isCurrent)}`);
      }
      console.log("");
      console.log(source_default.gray(`Total: ${sessionIds.length} sessions`));
      console.log(source_default.gray("\u{1F4A1} Use /session switch <id> to change sessions"));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to list sessions: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session new subcommand handler
   */
  async sessionNewHandler(args, agent) {
    try {
      const sessionId = args.length > 0 ? args[0] : void 0;
      const session = await agent.createSession(sessionId);
      console.log(source_default.green(`\u2705 Created new session: ${session.id}`));
      await agent.loadSession(session.id);
      console.log(source_default.blue("\u{1F504} Switched to new session"));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to create session: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session switch subcommand handler
   */
  async sessionSwitchHandler(args, agent) {
    try {
      if (args.length === 0) {
        console.log(source_default.red("\u274C Session ID required"));
        console.log(source_default.gray("Usage: /session switch <id>"));
        return false;
      }
      const sessionId = args[0];
      if (!sessionId) {
        console.log(source_default.red("\u274C Session ID cannot be empty"));
        return false;
      }
      await agent.loadSession(sessionId);
      const metadata = await agent.getSessionMetadata(sessionId);
      console.log(source_default.green(`\u2705 Switched to session: ${sessionId}`));
      if (metadata && metadata.messageCount && metadata.messageCount > 0) {
        console.log(source_default.gray(`   ${metadata.messageCount} messages in history`));
      } else {
        console.log(source_default.gray("   New conversation - no previous messages"));
      }
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to switch session: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session current subcommand handler
   */
  async sessionCurrentHandler(args, agent) {
    try {
      const currentSessionId = agent.getCurrentSessionId();
      if (!currentSessionId) {
        console.log(source_default.yellow("\u26A0\uFE0F No current session"));
        return true;
      }
      const metadata = await agent.getSessionMetadata(currentSessionId);
      console.log(source_default.cyan("\u{1F4CD} Current Session:"));
      console.log("");
      console.log(`  ${this.formatSessionInfo(currentSessionId, metadata, true)}`);
      console.log("");
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to get current session: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session delete subcommand handler
   */
  async sessionDeleteHandler(args, agent) {
    try {
      if (args.length === 0) {
        console.log(source_default.red("\u274C Session ID required"));
        console.log(source_default.gray("Usage: /session delete <id>"));
        return false;
      }
      const sessionId = args[0];
      if (!sessionId) {
        console.log(source_default.red("\u274C Session ID cannot be empty"));
        return false;
      }
      const currentSessionId = agent.getCurrentSessionId();
      if (sessionId === currentSessionId) {
        console.log(source_default.yellow("\u26A0\uFE0F  Cannot delete the currently active session"));
        console.log(source_default.gray("   Switch to another session first, then delete this one"));
        return false;
      }
      await agent.removeSession(sessionId);
      console.log(source_default.green(`\u2705 Deleted session: ${sessionId}`));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to delete session: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session delete all subcommand handler
   */
  async sessionDeleteAllHandler(args, agent) {
    try {
      const currentSessionId = agent.getCurrentSessionId();
      const allSessionIds = await agent.listSessions();
      if (allSessionIds.length === 0) {
        console.log(source_default.yellow("\u26A0\uFE0F  No sessions to delete"));
        return true;
      }
      const sessionsToDelete = allSessionIds.filter((sessionId) => sessionId !== currentSessionId);
      if (sessionsToDelete.length === 0) {
        console.log(source_default.yellow("\u26A0\uFE0F  Only the active session exists, nothing to delete"));
        console.log(source_default.gray("   The active session cannot be deleted"));
        return true;
      }
      console.log(
        source_default.yellow(
          `\u{1F5D1}\uFE0F  About to delete ${sessionsToDelete.length} sessions (excluding active session)`
        )
      );
      console.log(source_default.gray(`   Active session "${currentSessionId}" will be preserved`));
      console.log("");
      let deletedCount = 0;
      let failedCount = 0;
      const failedSessions = [];
      for (const sessionId of sessionsToDelete) {
        try {
          await agent.removeSession(sessionId);
          deletedCount++;
          console.log(source_default.green(`\u2705 Deleted session: ${sessionId}`));
        } catch (error) {
          failedCount++;
          failedSessions.push(sessionId);
          console.log(
            source_default.red(
              `\u274C Failed to delete session ${sessionId}: ${error instanceof Error ? error.message : String(error)}`
            )
          );
        }
      }
      console.log("");
      console.log(source_default.cyan("\u{1F4CA} Summary:"));
      console.log(`  ${source_default.green("Deleted:")} ${deletedCount} sessions`);
      console.log(`  ${source_default.red("Failed:")} ${failedCount} sessions`);
      console.log(`  ${source_default.blue("Remaining:")} 1 active session`);
      if (failedSessions.length > 0) {
        console.log("");
        console.log(source_default.yellow("\u26A0\uFE0F  Failed to delete sessions:"));
        failedSessions.forEach((sessionId) => {
          console.log(`  - ${sessionId}`);
        });
      }
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to delete sessions: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Session help subcommand handler
   */
  async sessionHelpHandler(_args, _agent) {
    console.log(source_default.cyan("\n\u{1F4CB} Session Management Commands:\n"));
    console.log(source_default.yellow("Available subcommands:"));
    const subcommands = [
      "/session list - List all sessions with status and activity",
      "/session new [name] - Create new session (optional custom name)",
      "/session switch <id> - Switch to different session",
      "/session current - Show current session info",
      "/session delete <id> - Delete session (cannot delete active)",
      "/session delete all - Delete all sessions except the active one",
      "/session help - Show this help message"
    ];
    subcommands.forEach((cmd) => console.log(`  ${cmd}`));
    console.log("\n" + source_default.gray("\uFFFD\uFFFD Sessions allow you to maintain separate conversations"));
    console.log(source_default.gray("\u{1F4A1} Use /session switch <id> to change sessions"));
    console.log(source_default.gray("\u{1F4A1} Session names can be custom or auto-generated UUIDs"));
    console.log("");
    return true;
  }
  /**
   * Prompt providers list subcommand handler
   */
  async promptProvidersListHandler(_args, agent) {
    try {
      console.log(source_default.cyan("\u{1F4CB} System Prompt Providers"));
      console.log(source_default.cyan("==========================\n"));
      const promptManager = agent.promptManager;
      console.log(source_default.yellow("Enhanced Prompt System Active"));
      console.log("");
      const providers = promptManager.listProviders();
      if (providers.length === 0) {
        console.log(source_default.gray("  No providers configured."));
        console.log(
          source_default.gray("  \u{1F4A1} Use /prompt-providers add-dynamic, add-file, or update existing ones.")
        );
      } else {
        for (const p of providers) {
          console.log(`${p.enabled ? source_default.green("\u{1F7E2}") : source_default.red("\u{1F534}")} ${p.id} (${p.type})`);
        }
      }
      console.log("");
      console.log(source_default.gray("\u{1F4A1} This is an Enhanced Prompt Manager system"));
      console.log(
        source_default.gray("\u{1F4A1} You can manage providers like user-instruction, built-in-instructions, etc.")
      );
      console.log(source_default.gray("\u{1F4A1} Use /prompt-providers enable/disable to manage provider status."));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to list providers: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Prompt providers enable subcommand handler
   */
  async promptProvidersEnableHandler(args, _agent) {
    try {
      if (args.length === 0) {
        console.log(source_default.red("\u274C Provider name required"));
        console.log(source_default.gray("Usage: /prompt-providers enable <provider-name>"));
        return false;
      }
      console.log(source_default.yellow("\u26A0\uFE0F Enhanced Prompt System Active"));
      console.log("");
      console.log("The current prompt system uses an Enhanced PromptManager that supports");
      console.log("individual provider management.");
      console.log("");
      console.log("Available providers:");
      console.log("  - user-instruction (static, priority: 100)");
      console.log("  - built-in-instructions (static, priority: 0)");
      console.log("  - dynamic-generators (dynamic, priority: 50)");
      console.log("  - file-based-providers (file-based, priority: 40)");
      console.log("");
      console.log(source_default.gray("\u{1F4A1} Use /prompt-providers disable <name> to disable a provider."));
      console.log(source_default.gray("\u{1F4A1} Providers can be re-enabled by re-adding them."));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to enable provider: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Prompt providers disable subcommand handler
   */
  async promptProvidersDisableHandler(args, _agent) {
    try {
      if (args.length === 0) {
        console.log(source_default.red("\u274C Provider name required"));
        console.log(source_default.gray("Usage: /prompt-providers disable <provider-name>"));
        return false;
      }
      console.log(source_default.yellow("\u26A0\uFE0F Enhanced Prompt System Active"));
      console.log("");
      console.log("The current prompt system uses an Enhanced PromptManager that supports");
      console.log("individual provider management.");
      console.log("");
      console.log("In enhanced mode:");
      console.log("  - You can disable a provider by removing it or setting enabled: false.");
      console.log("  - Providers can be re-enabled by re-adding them or setting enabled: true.");
      console.log("");
      console.log(source_default.gray("\u{1F4A1} Use /prompt-providers enable <name> to re-enable a provider."));
      console.log(source_default.gray("\u{1F4A1} Providers can be re-enabled by re-adding them."));
      return true;
    } catch (error) {
      console.log(
        source_default.red(
          `\u274C Failed to disable provider: ${error instanceof Error ? error.message : String(error)}`
        )
      );
      return false;
    }
  }
  /**
   * Prompt providers help subcommand handler
   */
  async promptProvidersHelpHandler(_args, _agent) {
    console.log(source_default.cyan("\n\u{1F4CB} Prompt Provider Management Commands:\n"));
    console.log(source_default.yellow("Available subcommands:"));
    const subcommands = [
      "/prompt-providers list - List all available prompt providers",
      "/prompt-providers enable <name> - Enable a specific provider",
      "/prompt-providers disable <name> - Disable a specific provider",
      "/prompt-providers help - Show this help message"
    ];
    subcommands.forEach((cmd) => console.log(`  ${cmd}`));
    console.log(
      "\n" + source_default.gray("\u{1F4A1} Providers are components that generate parts of the system prompt")
    );
    console.log(source_default.gray("\u{1F4A1} Different provider types: static, dynamic, file-based"));
    console.log(
      source_default.gray("\u{1F4A1} Current system uses Enhanced Prompt Manager for provider management")
    );
    console.log(
      source_default.gray("\u{1F4A1} You can manage providers like user-instruction, built-in-instructions, etc.")
    );
    console.log("");
    return true;
  }
};
var commandParser = new CommandParser();

// src/app/cli/commands.ts
async function executeCommand(command, args, agent) {
  return await commandParser.executeCommand(command, args, agent);
}

// src/app/cli/cli.ts
var COMPRESSION_CHECK_DELAY = 100;
var lastCompressionHistoryLength = 0;
async function startHeadlessCli(agent, input) {
  await _initCli(agent);
  if (input.trim().startsWith("!meta ")) {
    const metaAndMessage = input.trim().substring(6).split(" ");
    const metaStr = metaAndMessage[0];
    const message = metaAndMessage.slice(1).join(" ");
    let metadata = {};
    try {
      if (metaStr) {
        metadata = parseMetaString(metaStr);
      }
    } catch {
      console.log(source_default.red("\u274C Invalid metadata format. Use key=value,key2=value2 ..."));
      return;
    }
    console.log(source_default.gray("\u{1F914} Processing (with metadata)..."));
    const result = await agent.run(message, void 0, void 0, false, {
      memoryMetadata: metadata
    });
    if (result && result.backgroundOperations) {
      try {
        await result.backgroundOperations;
      } catch {
      }
    }
    if (result && result.response) {
      logger.displayAIResponse(result.response);
    } else {
      console.log(source_default.gray("No response received."));
    }
  } else {
    console.log(source_default.gray("\u{1F914} Processing..."));
    const result = await agent.run(input);
    if (result && result.backgroundOperations) {
      try {
        await result.backgroundOperations;
      } catch {
      }
    }
    if (result && result.response) {
      logger.displayAIResponse(result.response);
    } else {
      console.log(source_default.gray("No response received."));
    }
  }
}
async function startInteractiveCli(agent) {
  await _initCli(agent);
  await _initializeSessionAndCompression(agent);
  console.log(source_default.cyan("\u{1F680} Welcome to Cipher Interactive CLI!"));
  console.log(source_default.gray("Your memory-powered coding assistant is ready."));
  console.log(source_default.gray("\u2022 Type /help to see available commands"));
  console.log(source_default.gray("\u2022 Use /exit or /quit to end the session"));
  console.log(source_default.gray("\u2022 Regular messages will be sent to the AI agent\n"));
  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
    prompt: source_default.blue("cipher> ")
  });
  const handleExit = () => {
    console.log(source_default.yellow("\n\u{1F44B} Goodbye! Your conversation has been saved to memory."));
    rl.close();
    process.exit(0);
  };
  rl.on("SIGINT", handleExit);
  rl.on("SIGTERM", handleExit);
  rl.prompt();
  rl.on("line", async (input) => {
    const trimmedInput = input.trim();
    if (!trimmedInput) {
      rl.prompt();
      return;
    }
    try {
      if (trimmedInput.startsWith("!meta ")) {
        const metaAndMessage = trimmedInput.substring(6).split(" ");
        const metaStr = metaAndMessage[0];
        const message = metaAndMessage.slice(1).join(" ");
        let metadata = {};
        try {
          if (metaStr) {
            metadata = parseMetaString(metaStr);
          }
        } catch {
          console.log(source_default.red("\u274C Invalid metadata format. Use key=value,key2=value2 ..."));
          rl.prompt();
          return;
        }
        console.log(source_default.gray("\u{1F914} Thinking (with metadata)..."));
        const result = await agent.run(message, void 0, void 0, false, {
          memoryMetadata: metadata
        });
        if (result && result.backgroundOperations) {
          try {
            await result.backgroundOperations;
          } catch {
          }
        }
        if (result && result.response) {
          logger.displayAIResponse(result.response);
        } else {
          console.log(source_default.gray("No response received."));
        }
        await _showCompressionInfo(agent);
        if (result && result.backgroundOperations) {
          const currentLogLevel = process.env.CIPHER_LOG_LEVEL || "info";
          const isInfoLevelOrHigher = ["error", "warn", "info"].includes(currentLogLevel);
          if (isInfoLevelOrHigher) {
            rl.prompt();
            result.backgroundOperations.catch(() => {
            });
          } else {
            result.backgroundOperations.catch(() => {
            }).finally(() => {
              setTimeout(() => {
                rl.prompt();
              }, COMPRESSION_CHECK_DELAY);
            });
          }
        } else {
          rl.prompt();
        }
      } else {
        const parsedInput = commandParser.parseInput(trimmedInput);
        if (parsedInput.isCommand) {
          if (parsedInput.command && parsedInput.args !== void 0) {
            const commandSuccess = await executeCommand(
              parsedInput.command,
              parsedInput.args,
              agent
            );
            if (!commandSuccess) {
              console.log(source_default.gray("Command execution failed or was cancelled."));
            }
          } else {
            console.log(source_default.red("\u274C Invalid command format"));
            commandParser.displayHelp();
          }
          rl.prompt();
        } else {
          console.log(source_default.gray("\u{1F914} Thinking..."));
          const result = await agent.run(trimmedInput);
          if (result && result.response) {
            logger.displayAIResponse(result.response);
          } else {
            console.log(source_default.gray("No response received."));
          }
          await _showCompressionInfo(agent);
          if (result && result.backgroundOperations) {
            const currentLogLevel = process.env.CIPHER_LOG_LEVEL || "info";
            const isInfoLevelOrHigher = ["error", "warn", "info"].includes(currentLogLevel);
            if (isInfoLevelOrHigher) {
              rl.prompt();
              result.backgroundOperations.catch(() => {
              });
            } else {
              result.backgroundOperations.catch(() => {
              }).finally(() => {
                setTimeout(() => {
                  rl.prompt();
                }, COMPRESSION_CHECK_DELAY);
              });
            }
          } else {
            rl.prompt();
          }
        }
      }
    } catch (error) {
      logger.error(
        `Error processing input: ${error instanceof Error ? error.message : String(error)}`
      );
      rl.prompt();
    }
  });
  rl.on("close", () => {
    console.log(source_default.yellow("\n\u{1F44B} Session ended. Your conversation has been saved to memory."));
    process.exit(0);
  });
}
async function startMcpMode(agent, opts) {
  if (!agent) {
    throw new Error("Agent is not initialized");
  }
  const mcpServerMode = process.env.MCP_SERVER_MODE || "default";
  try {
    switch (mcpServerMode) {
      case "aggregator":
        await startAggregatorMode(agent, opts);
        break;
      case "default":
      default:
        await startDefaultMcpMode(agent, opts);
        break;
    }
  } catch (error) {
    const errorMessage = error instanceof Error ? error.message : String(error);
    logger.error(`[MCP Mode] Failed to start MCP server (mode: ${mcpServerMode}): ${errorMessage}`);
    process.exit(1);
  }
}
async function startDefaultMcpMode(agent, opts) {
  const { createMcpTransport: createMcpTransport2, initializeMcpServer: initializeMcpServer2, initializeAgentCardResource: initializeAgentCardResource2 } = await Promise.resolve().then(() => (init_mcp_handler(), mcp_handler_exports));
  const config2 = agent.getEffectiveConfig();
  const agentCardInput = config2.agentCard ? Object.fromEntries(
    Object.entries(config2.agentCard).filter(([, value]) => value !== void 0)
  ) : {};
  const agentCardData = initializeAgentCardResource2(agentCardInput);
  const transportType = opts?.mcpTransportType || "stdio";
  const mcpPort = opts?.mcpPort ? parseInt(opts.mcpPort, 10) : void 0;
  const mcpHost = opts?.mcpHost || "localhost";
  const mcpDnsRebindingProtection = opts?.mcpDnsRebindingProtection || false;
  logger.info("[MCP Mode] Initializing default MCP server with agent capabilities");
  const server = await initializeMcpServer2(agent, agentCardData, "default");
  logger.info(`[MCP Mode] Creating ${transportType} transport for default MCP server`);
  const mcpTransportResult = await createMcpTransport2(
    transportType,
    mcpPort,
    server,
    mcpHost,
    mcpDnsRebindingProtection
  );
  if (transportType === "stdio") {
    await server.connect(mcpTransportResult.server);
  } else {
    logger.info(`[MCP Mode] HTTP server started for ${transportType} transport`);
  }
  let isShuttingDown = false;
  const handleShutdown = async () => {
    if (isShuttingDown) return;
    isShuttingDown = true;
    logger.info("[MCP Mode] Shutting down default MCP server...");
    if (mcpTransportResult.cleanup) {
      try {
        await mcpTransportResult.cleanup();
      } catch (error) {
        logger.error(
          "[MCP Mode] Error during cleanup:",
          error instanceof Error ? error.message : String(error)
        );
      }
    }
    logger.info("[MCP Mode] Default MCP server stopped");
    process.exit(0);
  };
  process.on("SIGTERM", handleShutdown);
  process.on("SIGINT", handleShutdown);
  logger.info("[MCP Mode] Cipher agent is now running as default MCP server");
  if (transportType === "stdio") {
    process.stdin.resume();
  } else {
    setInterval(() => {
    }, 1e3);
  }
}
async function startAggregatorMode(agent, opts) {
  const { createMcpTransport: createMcpTransport2, initializeMcpServer: initializeMcpServer2, initializeAgentCardResource: initializeAgentCardResource2 } = await Promise.resolve().then(() => (init_mcp_handler(), mcp_handler_exports));
  const aggregatorConfig = await loadAggregatorConfig();
  const config2 = agent.getEffectiveConfig();
  const agentCardInput = config2.agentCard ? Object.fromEntries(
    Object.entries(config2.agentCard).filter(([, value]) => value !== void 0)
  ) : {};
  const agentCardData = initializeAgentCardResource2(agentCardInput);
  const transportType = opts?.mcpTransportType || "stdio";
  const mcpPort = opts?.mcpPort ? parseInt(opts.mcpPort, 10) : void 0;
  const mcpHost = opts?.mcpHost || "localhost";
  const mcpDnsRebindingProtection = opts?.mcpDnsRebindingProtection || false;
  logger.info("[MCP Mode] Initializing aggregator MCP server with agent capabilities");
  const server = await initializeMcpServer2(agent, agentCardData, "aggregator", aggregatorConfig);
  logger.info(`[MCP Mode] Creating ${transportType} transport for aggregator MCP server`);
  const mcpTransportResult = await createMcpTransport2(
    transportType,
    mcpPort,
    server,
    mcpHost,
    mcpDnsRebindingProtection
  );
  if (transportType === "stdio") {
    await server.connect(mcpTransportResult.server);
  } else {
    logger.info(`[MCP Mode] HTTP server started for ${transportType} transport`);
  }
  let isShuttingDown = false;
  const handleShutdown = async () => {
    if (isShuttingDown) return;
    isShuttingDown = true;
    logger.info("[MCP Mode] Shutting down aggregator MCP server...");
    if (mcpTransportResult.cleanup) {
      try {
        await mcpTransportResult.cleanup();
      } catch (error) {
        logger.error(
          "[MCP Mode] Error during cleanup:",
          error instanceof Error ? error.message : String(error)
        );
      }
    }
    logger.info("[MCP Mode] Aggregator MCP server stopped");
    process.exit(0);
  };
  process.on("SIGTERM", handleShutdown);
  process.on("SIGINT", handleShutdown);
  logger.info("[MCP Mode] Cipher is now running as aggregator MCP server");
  if (transportType === "stdio") {
    process.stdin.resume();
  } else {
    setInterval(() => {
    }, 1e3);
  }
}
async function loadAggregatorConfig() {
  const defaultConfig = {
    type: "aggregator",
    servers: {},
    // No longer needed - using unifiedToolManager
    conflictResolution: process.env.AGGREGATOR_CONFLICT_RESOLUTION || "prefix",
    autoDiscovery: false,
    timeout: parseInt(process.env.AGGREGATOR_TIMEOUT || "60000"),
    connectionMode: "lenient"
  };
  logger.info("[MCP Aggregator] Using simplified configuration with env vars", {
    conflictResolution: defaultConfig.conflictResolution,
    timeout: defaultConfig.timeout
  });
  return defaultConfig;
}
async function _initCli(agent) {
  logger.info("Initializing CLI interface...");
  if (!agent) {
    throw new Error("Agent is not initialized");
  }
  logger.info("CLI interface ready");
}
async function _initializeSessionAndCompression(agent) {
  await new Promise((resolve3) => setTimeout(resolve3, 50));
  const session = await agent.getSession(agent.getCurrentSessionId());
  if (session && typeof session.init === "function") {
    await session.init();
  }
  await new Promise((resolve3) => setTimeout(resolve3, 50));
  await _showCompressionStartup(agent);
  await new Promise((res) => process.stderr.write("", res));
}
async function _showCompressionStartup(agent) {
  try {
    const session = await agent.getSession(agent.getCurrentSessionId());
    if (!session) {
      return;
    }
    const ctx = session.getContextManager();
    if (!ctx) {
      return;
    }
    const stats = ctx.getTokenStats();
    if (stats.maxTokens > 0) {
      console.log(source_default.green("\u{1F9E0} Token-Aware Compression System is ACTIVE"));
      console.log(
        source_default.gray(
          `\u2022 Max tokens: ${stats.maxTokens}, Compression strategy: ${ctx["compressionStrategy"]?.name || "unknown"}`
        )
      );
      lastCompressionHistoryLength = ctx["compressionHistory"]?.length || 0;
    }
  } catch {
  }
}
async function _showCompressionInfo(agent) {
  try {
    const session = await agent.getSession(agent.getCurrentSessionId());
    if (!session) {
      return;
    }
    const ctx = session.getContextManager();
    const history = ctx["compressionHistory"];
    if (Array.isArray(history) && history.length > lastCompressionHistoryLength) {
      const _event = history[history.length - 1];
      _displayCompressionEvent(_event);
      lastCompressionHistoryLength = history.length;
    }
  } catch {
  }
}
function _displayCompressionEvent(_event) {
  console.log(source_default.yellowBright("\u26A1 Context has been compressed."));
}
function parseMetaString(metaStr) {
  const metadata = {};
  if (!metaStr) return metadata;
  const pairs = metaStr.split(",");
  for (const pair of pairs) {
    const [key, value] = pair.split("=");
    if (!key || value === void 0 || value === "") {
      throw new Error("Invalid metadata pair");
    }
    metadata[key.trim()] = value.trim();
  }
  return metadata;
}

// src/app/api/server.ts
init_cjs_shims();
var import_express10 = __toESM(require("express"), 1);
var import_cors = __toESM(require("cors"), 1);
var import_helmet = __toESM(require("helmet"), 1);
var import_express_rate_limit = __toESM(require("express-rate-limit"), 1);
var import_http3 = __toESM(require("http"), 1);
var import_ws3 = require("ws");
init_logger2();

// src/app/api/utils/response.ts
init_cjs_shims();
function successResponse(res, data, statusCode = 200, requestId) {
  const response = {
    success: true,
    data,
    meta: {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      ...requestId && { requestId }
    }
  };
  res.status(statusCode).json(response);
}
function errorResponse(res, code, message, statusCode = 500, details, requestId) {
  let sanitizedMessage = message;
  let sanitizedDetails = details;
  if (typeof message !== "string") {
    try {
      sanitizedMessage = message instanceof Error ? message.message : String(message);
    } catch {
      sanitizedMessage = "Unknown error occurred";
    }
  }
  if (details !== void 0 && details !== null) {
    try {
      if (details instanceof Error) {
        sanitizedDetails = {
          message: details.message,
          name: details.name,
          ...details.stack && { stack: details.stack.split("\n").slice(0, 3).join("\n") }
        };
      } else if (typeof details === "object") {
        sanitizedDetails = JSON.parse(
          JSON.stringify(details, (key, value) => {
            if (typeof value === "function") return "[Function]";
            if (typeof value === "symbol") return "[Symbol]";
            if (value instanceof Error) return { message: value.message, name: value.name };
            return value;
          })
        );
      } else {
        sanitizedDetails = String(details);
      }
    } catch {
      sanitizedDetails = { message: String(details), serializationError: true };
    }
  }
  const response = {
    success: false,
    error: {
      code,
      message: sanitizedMessage,
      ...sanitizedDetails !== void 0 && { details: sanitizedDetails }
    },
    meta: {
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      ...requestId && { requestId }
    }
  };
  res.status(statusCode).json(response);
}
var ERROR_CODES = {
  VALIDATION_ERROR: "VALIDATION_ERROR",
  NOT_FOUND: "NOT_FOUND",
  UNAUTHORIZED: "UNAUTHORIZED",
  INTERNAL_ERROR: "INTERNAL_ERROR",
  BAD_REQUEST: "BAD_REQUEST",
  SESSION_NOT_FOUND: "SESSION_NOT_FOUND",
  MCP_SERVER_ERROR: "MCP_SERVER_ERROR",
  LLM_ERROR: "LLM_ERROR",
  RATE_LIMIT_EXCEEDED: "RATE_LIMIT_EXCEEDED"
};

// src/app/api/middleware/logging.ts
init_cjs_shims();
var import_uuid16 = require("uuid");
init_logger2();
function requestIdMiddleware(req, res, next) {
  req.requestId = (0, import_uuid16.v4)();
  req.startTime = Date.now();
  res.setHeader("X-Request-ID", req.requestId);
  next();
}
function requestLoggingMiddleware(req, res, next) {
  const { method, url, ip, headers } = req;
  const userAgent = headers["user-agent"] || "unknown";
  logger.info("API Request", {
    requestId: req.requestId,
    method,
    url,
    ip,
    userAgent,
    contentType: headers["content-type"]
  });
  const originalEnd = res.end;
  res.end = function(chunk, encoding) {
    const duration = Date.now() - req.startTime;
    const { statusCode } = res;
    logger.info("API Response", {
      requestId: req.requestId,
      method,
      url,
      statusCode,
      duration: `${duration}ms`,
      responseSize: res.get("content-length") || "unknown"
    });
    return originalEnd.call(this, chunk, encoding);
  };
  next();
}
function errorLoggingMiddleware(err, req, res, next) {
  logger.error("API Error", {
    requestId: req.requestId,
    method: req.method,
    url: req.url,
    error: err.message,
    stack: err.stack,
    userAgent: req.headers["user-agent"]
  });
  next(err);
}

// src/app/api/server.ts
var import_sse3 = require("@modelcontextprotocol/sdk/server/sse.js");
init_mcp_handler();

// src/app/api/websocket/connection-manager.ts
init_cjs_shims();
var import_ws = require("ws");
var import_uuid17 = require("uuid");
init_logger2();
var WebSocketConnectionManager = class {
  // We'll set this after initialization
  constructor(maxConnections = 1e3, connectionTimeout = 3e5) {
    this.maxConnections = maxConnections;
    this.connectionTimeout = connectionTimeout;
    setInterval(() => {
      this.cleanupStaleConnections();
    }, 6e4);
  }
  connections = /* @__PURE__ */ new Map();
  sessionConnections = /* @__PURE__ */ new Map();
  connectionSessions = /* @__PURE__ */ new Map();
  stats = {
    totalMessagesReceived: 0,
    totalMessagesSent: 0,
    connectionsCreated: 0
  };
  eventSubscriber;
  /**
   * Set the event subscriber to notify when sessions are bound
   */
  setEventSubscriber(eventSubscriber) {
    this.eventSubscriber = eventSubscriber;
  }
  /**
   * Add a new WebSocket connection
   */
  addConnection(ws, sessionId) {
    if (this.connections.size >= this.maxConnections) {
      logger.warn("WebSocket connection limit reached", {
        currentConnections: this.connections.size,
        maxConnections: this.maxConnections
      });
      ws.close(1013, "Server overloaded");
      throw new Error("Maximum connections exceeded");
    }
    const connectionId = (0, import_uuid17.v4)();
    const now = Date.now();
    const connection = {
      id: connectionId,
      ws,
      sessionId: sessionId || void 0,
      subscribedEvents: /* @__PURE__ */ new Set(),
      connectedAt: now,
      lastActivity: now
    };
    this.connections.set(connectionId, connection);
    this.connectionSessions.set(connectionId, sessionId || "");
    this.stats.connectionsCreated++;
    if (sessionId) {
      this.bindToSession(connectionId, sessionId);
    }
    this.setupConnectionHandlers(connection);
    logger.info("WebSocket connection added", {
      connectionId,
      sessionId,
      totalConnections: this.connections.size
    });
    return connectionId;
  }
  /**
   * Remove a WebSocket connection
   */
  removeConnection(connectionId) {
    const connection = this.connections.get(connectionId);
    if (!connection) {
      return;
    }
    if (connection.sessionId) {
      const sessionConnections = this.sessionConnections.get(connection.sessionId);
      if (sessionConnections) {
        sessionConnections.delete(connectionId);
        if (sessionConnections.size === 0) {
          this.sessionConnections.delete(connection.sessionId);
        }
      }
    }
    this.connections.delete(connectionId);
    this.connectionSessions.delete(connectionId);
    if (connection.ws.readyState === import_ws.WebSocket.OPEN) {
      connection.ws.close();
    }
    logger.info("WebSocket connection removed", {
      connectionId,
      sessionId: connection.sessionId,
      totalConnections: this.connections.size
    });
  }
  /**
   * Bind a connection to a session
   */
  bindToSession(connectionId, sessionId) {
    const connection = this.connections.get(connectionId);
    if (!connection) {
      logger.warn("Attempted to bind non-existent connection to session", {
        connectionId,
        sessionId
      });
      return;
    }
    connection.sessionId = sessionId;
    this.connectionSessions.set(connectionId, sessionId);
    if (!this.sessionConnections.has(sessionId)) {
      this.sessionConnections.set(sessionId, /* @__PURE__ */ new Set());
    }
    this.sessionConnections.get(sessionId).add(connectionId);
    this.sendConnectionUpdate(connectionId);
    if (this.eventSubscriber) {
      this.eventSubscriber.subscribeToSession(sessionId);
    }
    logger.debug("Connection bound to session", {
      connectionId,
      sessionId
    });
  }
  /**
   * Get the session ID for a connection
   */
  getConnectionSessionId(connectionId) {
    const connection = this.connections.get(connectionId);
    return connection?.sessionId;
  }
  /**
   * Send updated connection info to a specific connection
   */
  sendConnectionUpdate(connectionId) {
    const connection = this.connections.get(connectionId);
    if (!connection || connection.ws.readyState !== import_ws.WebSocket.OPEN) {
      return;
    }
    const updateMessage = {
      event: "connectionUpdated",
      data: {
        connectionId,
        sessionId: connection.sessionId,
        timestamp: Date.now()
      },
      timestamp: Date.now()
    };
    try {
      connection.ws.send(JSON.stringify(updateMessage));
      logger.debug("Sent connection update", {
        connectionId,
        sessionId: connection.sessionId
      });
    } catch (error) {
      logger.error("Failed to send connection update", {
        connectionId,
        error: error instanceof Error ? error.message : String(error)
      });
    }
  }
  /**
   * Subscribe a connection to specific event types
   */
  subscribeToEvents(connectionId, eventTypes) {
    const connection = this.connections.get(connectionId);
    if (!connection) {
      logger.warn("Attempted to subscribe non-existent connection to events", {
        connectionId,
        eventTypes
      });
      return;
    }
    eventTypes.forEach((eventType) => {
      connection.subscribedEvents.add(eventType);
    });
    logger.debug("Connection subscribed to events", {
      connectionId,
      eventTypes,
      totalSubscriptions: connection.subscribedEvents.size
    });
  }
  /**
   * Unsubscribe a connection from specific event types
   */
  unsubscribeFromEvents(connectionId, eventTypes) {
    const connection = this.connections.get(connectionId);
    if (!connection) {
      return;
    }
    eventTypes.forEach((eventType) => {
      connection.subscribedEvents.delete(eventType);
    });
    logger.debug("Connection unsubscribed from events", {
      connectionId,
      eventTypes,
      totalSubscriptions: connection.subscribedEvents.size
    });
  }
  /**
   * Broadcast message to all connections in a session
   */
  broadcastToSession(sessionId, message) {
    const connectionIds = this.sessionConnections.get(sessionId);
    if (!connectionIds || connectionIds.size === 0) {
      return;
    }
    for (const connectionId of connectionIds) {
      this.sendToConnection(connectionId, message);
    }
  }
  /**
   * Broadcast message to all active connections
   */
  broadcastToAll(message) {
    let sentCount = 0;
    for (const connectionId of this.connections.keys()) {
      if (this.sendToConnection(connectionId, message)) {
        sentCount++;
      }
    }
    logger.debug("Message broadcast to all connections", {
      totalConnections: this.connections.size,
      sentCount,
      event: message.event
    });
  }
  /**
   * Broadcast message to connections subscribed to specific event type
   */
  broadcastToSubscribers(eventType, message) {
    let sentCount = 0;
    for (const connection of this.connections.values()) {
      const shouldSend = connection.subscribedEvents.size === 0 || connection.subscribedEvents.has(eventType);
      if (shouldSend && this.sendToConnection(connection.id, message)) {
        sentCount++;
      }
    }
    logger.debug("Message broadcast to subscribers", {
      eventType,
      totalConnections: this.connections.size,
      sentCount,
      event: message.event
    });
  }
  /**
   * Send message to a specific connection
   */
  sendToConnection(connectionId, message) {
    const connection = this.connections.get(connectionId);
    if (!connection || connection.ws.readyState !== import_ws.WebSocket.OPEN) {
      return false;
    }
    try {
      const messageStr = JSON.stringify(message);
      connection.ws.send(messageStr);
      connection.lastActivity = Date.now();
      this.stats.totalMessagesSent++;
      return true;
    } catch (error) {
      logger.error("Failed to send WebSocket message", {
        connectionId,
        error: error.message
      });
      return false;
    }
  }
  /**
   * Get connection statistics
   */
  getStats() {
    const now = Date.now();
    let totalDuration = 0;
    let activeConnections = 0;
    const activeSessions = /* @__PURE__ */ new Set();
    for (const connection of this.connections.values()) {
      if (connection.ws.readyState === import_ws.WebSocket.OPEN) {
        activeConnections++;
        totalDuration += now - connection.connectedAt;
        if (connection.sessionId) {
          activeSessions.add(connection.sessionId);
        }
      }
    }
    const averageConnectionDuration = activeConnections > 0 ? totalDuration / activeConnections : 0;
    return {
      totalConnections: this.stats.connectionsCreated,
      activeConnections,
      totalSessions: this.sessionConnections.size,
      activeSessions: activeSessions.size,
      totalMessagesReceived: this.stats.totalMessagesReceived,
      totalMessagesSent: this.stats.totalMessagesSent,
      averageConnectionDuration
    };
  }
  /**
   * Get all active session IDs
   */
  getActiveSessions() {
    return Array.from(this.sessionConnections.keys()).filter((sessionId) => {
      const connections = this.sessionConnections.get(sessionId);
      return connections && connections.size > 0;
    });
  }
  /**
   * Check if a session has active connections
   */
  hasActiveConnections(sessionId) {
    const connections = this.sessionConnections.get(sessionId);
    if (!connections) return false;
    for (const connectionId of connections) {
      const connection = this.connections.get(connectionId);
      if (connection && connection.ws.readyState === import_ws.WebSocket.OPEN) {
        return true;
      }
    }
    return false;
  }
  /**
   * Set up WebSocket event handlers for a connection
   */
  setupConnectionHandlers(connection) {
    connection.ws.on("close", () => {
      this.removeConnection(connection.id);
    });
    connection.ws.on("error", (error) => {
      logger.error("WebSocket connection error", {
        connectionId: connection.id,
        error: error.message
      });
      this.removeConnection(connection.id);
    });
    connection.ws.on("pong", () => {
      connection.lastActivity = Date.now();
    });
  }
  /**
   * Clean up stale connections
   */
  cleanupStaleConnections() {
    const now = Date.now();
    const staleConnections = [];
    for (const [connectionId, connection] of this.connections) {
      const isStale = now - connection.lastActivity > this.connectionTimeout || connection.ws.readyState !== import_ws.WebSocket.OPEN;
      if (isStale) {
        staleConnections.push(connectionId);
      }
    }
    if (staleConnections.length > 0) {
      logger.info("Cleaning up stale WebSocket connections", {
        staleCount: staleConnections.length,
        totalConnections: this.connections.size
      });
      staleConnections.forEach((connectionId) => {
        this.removeConnection(connectionId);
      });
    }
  }
  /**
   * Send heartbeat pings to all connections
   */
  sendHeartbeat() {
    for (const connection of this.connections.values()) {
      if (connection.ws.readyState === import_ws.WebSocket.OPEN) {
        try {
          connection.ws.ping();
        } catch (error) {
          logger.warn("Failed to send heartbeat ping", {
            connectionId: connection.id,
            error: error instanceof Error ? error.message : String(error)
          });
          this.removeConnection(connection.id);
        }
      }
    }
  }
  /**
   * Record incoming message for stats
   */
  recordIncomingMessage() {
    this.stats.totalMessagesReceived++;
  }
  /**
   * Dispose of the connection manager
   */
  dispose() {
    for (const connection of this.connections.values()) {
      if (connection.ws.readyState === import_ws.WebSocket.OPEN) {
        connection.ws.close();
      }
    }
    this.connections.clear();
    this.sessionConnections.clear();
    this.connectionSessions.clear();
    logger.info("WebSocket connection manager disposed");
  }
};

// src/app/api/websocket/message-router.ts
init_cjs_shims();
var import_ws2 = require("ws");
init_logger2();
var WebSocketMessageRouter = class {
  constructor(agent, connectionManager) {
    this.agent = agent;
    this.connectionManager = connectionManager;
  }
  /**
   * Route incoming WebSocket message to appropriate handler
   */
  async routeMessage(ws, connectionId, message) {
    try {
      this.connectionManager.recordIncomingMessage();
      if (!this.isValidMessage(message)) {
        this.sendError(ws, "Invalid message format", connectionId);
        return;
      }
      logger.debug("WebSocket message received", {
        connectionId,
        type: message.type,
        sessionId: message.sessionId,
        hasContent: !!message.content
      });
      switch (message.type) {
        case "message":
          await this.handleChatMessage(ws, connectionId, message);
          break;
        case "reset":
          await this.handleReset(ws, connectionId, message);
          break;
        case "subscribe":
          await this.handleSubscribe(ws, connectionId, message);
          break;
        case "unsubscribe":
          await this.handleUnsubscribe(ws, connectionId, message);
          break;
        default:
          this.sendError(ws, `Unknown message type: ${message.type}`, connectionId);
      }
    } catch (error) {
      logger.error("Error routing WebSocket message", {
        connectionId,
        error: error instanceof Error ? error.message : String(error),
        stack: error instanceof Error ? error.stack : void 0
      });
      this.sendError(
        ws,
        error instanceof Error ? error.message : "Internal server error",
        connectionId
      );
    }
  }
  /**
   * Handle chat message - process through MemAgent
   */
  async handleChatMessage(ws, connectionId, message) {
    if (!message.content && !message.imageData) {
      this.sendError(ws, "Message content or image data is required", connectionId);
      return;
    }
    let sessionId = message.sessionId;
    if (!sessionId) {
      try {
        const session = await this.agent.sessionManager.createSession();
        sessionId = session.id;
        this.connectionManager.bindToSession(connectionId, sessionId);
        this.sendResponse(ws, {
          event: "sessionCreated",
          data: { sessionId },
          sessionId
        });
        logger.info("Auto-created session for WebSocket connection", {
          connectionId,
          sessionId
        });
      } catch (error) {
        logger.error("Failed to create session for WebSocket connection", {
          connectionId,
          error: error instanceof Error ? error.message : String(error)
        });
        this.sendError(ws, "Failed to create session", connectionId);
        return;
      }
    } else {
      this.connectionManager.bindToSession(connectionId, sessionId);
    }
    try {
      const session = await this.agent.sessionManager.getSession(sessionId);
      if (!session) {
        this.sendError(ws, `Session ${sessionId} not found`, connectionId);
        return;
      }
    } catch (error) {
      logger.error("Error validating session", {
        sessionId,
        connectionId,
        error: error instanceof Error ? error.message : String(error)
      });
      this.sendError(ws, "Session validation failed", connectionId);
      return;
    }
    try {
      logger.info("Processing WebSocket chat message", {
        connectionId,
        sessionId,
        contentLength: message.content?.length || 0,
        hasImageData: !!message.imageData,
        hasFileData: !!message.fileData,
        stream: message.stream ?? true
      });
      const imageData = message.imageData ? {
        image: message.imageData.base64,
        mimeType: message.imageData.mimeType
      } : void 0;
      await this.agent.run(message.content || "", imageData, sessionId, message.stream ?? true);
      logger.debug("WebSocket chat message processed successfully", {
        connectionId,
        sessionId
      });
    } catch (error) {
      logger.error("Error processing chat message through MemAgent", {
        connectionId,
        sessionId,
        error: error instanceof Error ? error.message : String(error),
        stack: error instanceof Error ? error.stack : void 0
      });
      this.connectionManager.broadcastToSession(sessionId, {
        event: "error",
        data: {
          message: error instanceof Error ? error.message : "Processing failed",
          code: "PROCESSING_ERROR",
          sessionId
        },
        sessionId,
        error: error instanceof Error ? error.message : "Processing failed"
      });
    }
  }
  /**
   * Handle conversation reset
   */
  async handleReset(ws, connectionId, message) {
    const sessionId = message.sessionId;
    if (!sessionId) {
      this.sendError(ws, "Session ID is required for reset", connectionId);
      return;
    }
    try {
      const session = await this.agent.getSession(sessionId);
      if (!session) {
        this.sendError(ws, `Session ${sessionId} not found`, connectionId);
        return;
      }
      this.sendResponse(ws, {
        event: "conversationReset",
        data: { sessionId },
        sessionId
      });
      logger.info("Conversation reset via WebSocket", {
        connectionId,
        sessionId
      });
    } catch (error) {
      logger.error("Error resetting conversation", {
        connectionId,
        sessionId,
        error: error instanceof Error ? error.message : String(error)
      });
      this.sendError(ws, "Failed to reset conversation", connectionId);
    }
  }
  /**
   * Handle event subscription
   */
  async handleSubscribe(ws, connectionId, message) {
    if (!message.eventTypes || message.eventTypes.length === 0) {
      this.sendError(ws, "Event types are required for subscription", connectionId);
      return;
    }
    try {
      const validEventTypes = message.eventTypes.filter(
        (eventType) => this.isValidEventType(eventType)
      );
      if (validEventTypes.length === 0) {
        this.sendError(ws, "No valid event types provided", connectionId);
        return;
      }
      this.connectionManager.subscribeToEvents(connectionId, validEventTypes);
      this.sendResponse(ws, {
        event: "subscribed",
        data: {
          eventTypes: validEventTypes,
          connectionId
        }
      });
      logger.info("WebSocket connection subscribed to events", {
        connectionId,
        eventTypes: validEventTypes
      });
    } catch (error) {
      logger.error("Error handling event subscription", {
        connectionId,
        error: error instanceof Error ? error.message : String(error)
      });
      this.sendError(ws, "Failed to subscribe to events", connectionId);
    }
  }
  /**
   * Handle event unsubscription
   */
  async handleUnsubscribe(ws, connectionId, message) {
    if (!message.eventTypes || message.eventTypes.length === 0) {
      this.sendError(ws, "Event types are required for unsubscription", connectionId);
      return;
    }
    try {
      const validEventTypes = message.eventTypes.filter(
        (eventType) => this.isValidEventType(eventType)
      );
      this.connectionManager.unsubscribeFromEvents(connectionId, validEventTypes);
      this.sendResponse(ws, {
        event: "unsubscribed",
        data: {
          eventTypes: validEventTypes,
          connectionId
        }
      });
      logger.info("WebSocket connection unsubscribed from events", {
        connectionId,
        eventTypes: validEventTypes
      });
    } catch (error) {
      logger.error("Error handling event unsubscription", {
        connectionId,
        error: error instanceof Error ? error.message : String(error)
      });
      this.sendError(ws, "Failed to unsubscribe from events", connectionId);
    }
  }
  /**
   * Send error response to WebSocket client
   */
  sendError(ws, message, connectionId) {
    try {
      const errorResponse2 = {
        event: "error",
        error: message,
        data: {
          message,
          code: "WEBSOCKET_ERROR",
          timestamp: Date.now(),
          ...connectionId && { connectionId }
        },
        timestamp: Date.now()
      };
      if (ws.readyState === import_ws2.WebSocket.OPEN) {
        ws.send(JSON.stringify(errorResponse2));
      }
      logger.warn("WebSocket error sent to client", {
        connectionId,
        message
      });
    } catch (error) {
      logger.error("Failed to send error response", {
        connectionId,
        originalMessage: message,
        sendError: error instanceof Error ? error.message : String(error)
      });
    }
  }
  /**
   * Send response to WebSocket client
   */
  sendResponse(ws, response) {
    try {
      if (!response.timestamp) {
        response.timestamp = Date.now();
      }
      if (ws.readyState === import_ws2.WebSocket.OPEN) {
        ws.send(JSON.stringify(response));
      }
    } catch (error) {
      logger.error("Failed to send WebSocket response", {
        event: response.event,
        error: error instanceof Error ? error.message : String(error)
      });
    }
  }
  /**
   * Validate incoming message format
   */
  isValidMessage(message) {
    if (!message || typeof message !== "object") {
      return false;
    }
    if (!message.type || typeof message.type !== "string") {
      return false;
    }
    const validTypes = ["message", "reset", "subscribe", "unsubscribe"];
    if (!validTypes.includes(message.type)) {
      return false;
    }
    switch (message.type) {
      case "message":
        return typeof message.content === "string" && message.content.length > 0 || message.imageData && typeof message.imageData === "object" && message.imageData.base64 && message.imageData.mimeType;
      case "reset":
        return typeof message.sessionId === "string";
      case "subscribe":
      case "unsubscribe":
        return Array.isArray(message.eventTypes) && message.eventTypes.length > 0;
      default:
        return false;
    }
  }
  /**
   * Validate event type
   */
  isValidEventType(eventType) {
    const validEventTypes = [
      "thinking",
      "chunk",
      "toolCall",
      "toolResult",
      "response",
      "error",
      "conversationReset",
      "memoryOperation",
      "systemMessage",
      "sessionCreated",
      "sessionEnded",
      "mcpServerConnected",
      "mcpServerDisconnected",
      "availableToolsUpdated"
    ];
    return validEventTypes.includes(eventType);
  }
  /**
   * Get router statistics
   */
  getStats() {
    return {
      messagesProcessed: this.connectionManager.getStats().totalMessagesReceived,
      errorsHandled: 0,
      // TODO: Track errors
      activeSessions: this.connectionManager.getActiveSessions().length
    };
  }
};

// src/app/api/websocket/event-subscriber.ts
init_cjs_shims();
init_logger2();
var WebSocketEventSubscriber = class {
  constructor(connectionManager, eventManager) {
    this.connectionManager = connectionManager;
    this.eventManager = eventManager;
  }
  abortController = null;
  sessionAbortControllers = /* @__PURE__ */ new Map();
  subscribedSessions = /* @__PURE__ */ new Set();
  subscriptionStats = {
    totalEventsReceived: 0,
    totalEventsBroadcast: 0,
    eventTypeStats: /* @__PURE__ */ new Map(),
    lastEventTime: 0
  };
  /**
   * Subscribe to events from the EventManager
   */
  subscribe() {
    if (this.abortController) {
      logger.warn("WebSocket event subscriber already active");
      return;
    }
    this.abortController = new AbortController();
    const { signal } = this.abortController;
    try {
      if ("setMaxListeners" in this.abortController && typeof this.abortController.setMaxListeners === "function") {
        this.abortController.setMaxListeners(100);
      }
    } catch {
    }
    logger.info("Starting WebSocket event subscription");
    this.subscribeToServiceEvents(signal);
    this.subscribeToSessionEvents(signal);
    logger.info("WebSocket event subscriptions established");
  }
  /**
   * Subscribe to service-level events
   */
  subscribeToServiceEvents(signal) {
    const serviceBus = this.eventManager.getServiceEventBus();
    serviceBus.on(
      "cipher:mcpClientConnected",
      (data) => {
        this.handleEvent(
          "mcpServerConnected",
          {
            serverName: data.serverName,
            capabilities: []
          },
          signal
        );
      },
      { signal }
    );
    serviceBus.on(
      "cipher:mcpClientDisconnected",
      (data) => {
        this.handleEvent(
          "mcpServerDisconnected",
          {
            serverName: data.serverName,
            ...data.reason && { reason: data.reason }
          },
          signal
        );
      },
      { signal }
    );
    serviceBus.on(
      "cipher:toolRegistered",
      (data) => {
        this.handleEvent(
          "availableToolsUpdated",
          {
            tools: [data.toolName]
          },
          signal
        );
      },
      { signal }
    );
    serviceBus.on(
      "cipher:memoryOperationCompleted",
      (data) => {
        this.handleEvent(
          "memoryOperation",
          {
            operation: data.operation,
            success: true,
            sessionId: data.sessionId || "",
            details: {
              operation: data.operation,
              duration: data.duration
            }
          },
          signal
        );
      },
      { signal }
    );
    serviceBus.on(
      "cipher:memoryOperationFailed",
      (data) => {
        this.handleEvent(
          "memoryOperation",
          {
            operation: data.operation,
            success: false,
            sessionId: data.sessionId || "",
            details: {
              error: data.error
            }
          },
          signal
        );
      },
      { signal }
    );
    serviceBus.on(
      "cipher:error",
      (data) => {
        this.handleEvent(
          "error",
          {
            message: data.error || "System error",
            code: "SYSTEM_ERROR",
            ...data.stack && { stack: data.stack }
          },
          signal
        );
      },
      { signal }
    );
  }
  /**
   * Subscribe to session-specific events dynamically
   */
  subscribeToSessionEvents(signal) {
    const activeSessions = this.eventManager.getActiveSessionIds();
    activeSessions.forEach((sessionId) => {
      if (!this.subscribedSessions.has(sessionId)) {
        const sessionAbortController = new AbortController();
        this.sessionAbortControllers.set(sessionId, sessionAbortController);
        this.subscribeToSingleSessionEvents(sessionId, sessionAbortController.signal);
        this.subscribedSessions.add(sessionId);
      }
    });
    logger.debug("Session event subscriptions ready for existing sessions", {
      existingSessionCount: activeSessions.length,
      subscribedSessionCount: this.subscribedSessions.size
    });
  }
  /**
   * Subscribe to a session's events on-demand (called when session is bound to connection)
   */
  subscribeToSession(sessionId) {
    if (this.abortController && !this.subscribedSessions.has(sessionId)) {
      logger.debug("Dynamically subscribing to session events", { sessionId });
      const sessionAbortController = new AbortController();
      this.sessionAbortControllers.set(sessionId, sessionAbortController);
      this.subscribeToSingleSessionEvents(sessionId, sessionAbortController.signal);
      this.subscribedSessions.add(sessionId);
    } else if (this.subscribedSessions.has(sessionId)) {
      logger.debug("Session already subscribed, skipping", { sessionId });
    }
  }
  /**
   * Remove session from tracking when it's deleted
   */
  unsubscribeFromSession(sessionId) {
    if (this.subscribedSessions.has(sessionId)) {
      const sessionAbortController = this.sessionAbortControllers.get(sessionId);
      if (sessionAbortController) {
        sessionAbortController.abort();
        this.sessionAbortControllers.delete(sessionId);
      }
      this.subscribedSessions.delete(sessionId);
      logger.debug("Session removed from subscription tracking and listeners cleaned up", {
        sessionId
      });
    }
  }
  /**
   * Subscribe to events for a specific session
   */
  subscribeToSingleSessionEvents(sessionId, signal) {
    const sessionBus = this.eventManager.getSessionEventBus(sessionId);
    sessionBus.on(
      "llm:thinking",
      (data) => {
        this.handleEvent(
          "thinking",
          {
            sessionId: data.sessionId
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "llm:responseStarted",
      (data) => {
        logger.debug("LLM response started", { sessionId: data.sessionId });
      },
      { signal }
    );
    sessionBus.on(
      "llm:responseChunk",
      (data) => {
        this.handleEvent(
          "chunk",
          {
            text: data.chunk || data.text || "",
            isComplete: false,
            sessionId: data.sessionId,
            messageId: data.messageId
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "llm:responseCompleted",
      async (data) => {
        const content = data.response || data.content || "Response completed";
        const sessionId2 = data.sessionId;
        const messageId = data.messageId;
        if (content && typeof content === "string") {
          const chunkSize = 50;
          for (let i = 0; i < content.length; i += chunkSize) {
            const chunk = content.slice(i, i + chunkSize);
            const isComplete = i + chunkSize >= content.length;
            this.handleEvent(
              "chunk",
              {
                text: chunk,
                isComplete,
                sessionId: sessionId2,
                messageId
              },
              signal
            );
            if (!isComplete) {
              await new Promise((resolve3) => setTimeout(resolve3, 50));
            }
          }
        }
        this.handleEvent(
          "response",
          {
            content,
            sessionId: data.sessionId,
            messageId: data.messageId,
            metadata: {
              model: data.model,
              tokenCount: data.tokenCount,
              duration: data.duration
            }
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "llm:responseError",
      (data) => {
        this.handleEvent(
          "error",
          {
            message: data.error || "LLM processing error",
            code: "LLM_ERROR",
            sessionId: data.sessionId
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "tool:executionStarted",
      (data) => {
        logger.debug("WebSocket: Received tool:executionStarted event", {
          toolName: data.toolName,
          sessionId: data.sessionId,
          executionId: data.executionId,
          argsKeys: data.args ? Object.keys(data.args) : [],
          args: data.args
        });
        this.handleEvent(
          "toolExecutionStarted",
          {
            toolName: data.toolName,
            sessionId: data.sessionId,
            callId: data.executionId,
            executionId: data.executionId,
            args: data.args || {}
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "tool:executionCompleted",
      (data) => {
        this.handleEvent(
          "toolExecutionCompleted",
          {
            toolName: data.toolName,
            success: data.success,
            sessionId: data.sessionId,
            callId: data.executionId,
            executionId: data.executionId,
            result: data.result
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "tool:executionFailed",
      (data) => {
        this.handleEvent(
          "toolExecutionFailed",
          {
            toolName: data.toolName,
            error: data.error,
            sessionId: data.sessionId,
            callId: data.executionId,
            executionId: data.executionId
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "memory:stored",
      (data) => {
        this.handleEvent(
          "memoryOperation",
          {
            operation: "store",
            success: true,
            sessionId: data.sessionId,
            details: {
              type: data.type,
              size: data.size
            }
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "memory:retrieved",
      (data) => {
        this.handleEvent(
          "memoryOperation",
          {
            operation: "retrieve",
            success: true,
            sessionId: data.sessionId,
            details: {
              count: data.count,
              type: data.type
            }
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "memory:searched",
      (data) => {
        this.handleEvent(
          "memoryOperation",
          {
            operation: "search",
            success: true,
            sessionId: data.sessionId,
            details: {
              query: data.query,
              resultCount: data.resultCount,
              duration: data.duration
            }
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "session:created",
      (data) => {
        this.handleEvent(
          "sessionCreated",
          {
            sessionId: data.sessionId,
            timestamp: data.timestamp
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "session:deleted",
      (data) => {
        this.unsubscribeFromSession(data.sessionId);
        this.handleEvent(
          "sessionEnded",
          {
            sessionId: data.sessionId,
            timestamp: data.timestamp
          },
          signal
        );
      },
      { signal }
    );
    sessionBus.on(
      "conversation:cleared",
      (data) => {
        this.handleEvent(
          "conversationReset",
          {
            sessionId: data.sessionId
          },
          signal
        );
      },
      { signal }
    );
  }
  /**
   * Handle an event and broadcast to appropriate WebSocket connections
   */
  handleEvent(eventType, data, signal) {
    if (signal.aborted) {
      return;
    }
    const response = {
      event: eventType,
      data,
      timestamp: Date.now()
    };
    const sessionId = data?.sessionId;
    if (!sessionId) {
      this.broadcastMessage(response);
    } else {
      this.broadcastToSession(sessionId, response);
    }
    this.subscriptionStats.totalEventsBroadcast++;
    this.subscriptionStats.lastEventTime = Date.now();
    this.subscriptionStats.eventTypeStats.set(
      eventType,
      (this.subscriptionStats.eventTypeStats.get(eventType) || 0) + 1
    );
  }
  /**
   * Unsubscribe from events
   */
  unsubscribe() {
    if (this.abortController) {
      this.abortController.abort();
      this.abortController = null;
    }
    this.sessionAbortControllers.forEach((controller, sessionId) => {
      controller.abort();
    });
    this.sessionAbortControllers.clear();
    this.subscribedSessions.clear();
    logger.info("WebSocket event subscriptions terminated and all session controllers cleaned up");
  }
  /**
   * Get subscription statistics
   */
  getStats() {
    return {
      isActive: !!this.abortController && !this.abortController.signal.aborted,
      totalEventsReceived: this.subscriptionStats.totalEventsReceived,
      totalEventsBroadcast: this.subscriptionStats.totalEventsBroadcast,
      lastEventTime: this.subscriptionStats.lastEventTime,
      eventTypeStats: Object.fromEntries(this.subscriptionStats.eventTypeStats),
      subscribedSessionCount: this.subscribedSessions.size,
      connectionStats: this.connectionManager.getStats()
    };
  }
  /**
   * Manually broadcast a message to all connections
   */
  broadcastMessage(message) {
    this.connectionManager.broadcastToAll(message);
    this.subscriptionStats.totalEventsBroadcast++;
  }
  /**
   * Manually broadcast a message to a specific session
   */
  broadcastToSession(sessionId, message) {
    this.connectionManager.broadcastToSession(sessionId, message);
    this.subscriptionStats.totalEventsBroadcast++;
  }
  /**
   * Send a system message to all connections
   */
  sendSystemMessage(message, level = "info") {
    const response = {
      event: "systemMessage",
      data: {
        message,
        level,
        timestamp: Date.now()
      },
      timestamp: Date.now()
    };
    this.broadcastMessage(response);
    logger.info("System message broadcast via WebSocket", {
      message,
      level
    });
  }
  /**
   * Check if subscriber is active
   */
  isActive() {
    return !!this.abortController && !this.abortController.signal.aborted;
  }
  /**
   * Dispose of the event subscriber
   */
  dispose() {
    this.unsubscribe();
    this.subscriptionStats = {
      totalEventsReceived: 0,
      totalEventsBroadcast: 0,
      eventTypeStats: /* @__PURE__ */ new Map(),
      lastEventTime: 0
    };
    this.subscribedSessions.clear();
    logger.info("WebSocket event subscriber disposed");
  }
};

// src/app/api/routes/message.ts
init_cjs_shims();
var import_express3 = require("express");

// src/app/api/middleware/validation.ts
init_cjs_shims();
var import_express_validator = require("express-validator");

// src/app/api/utils/security.ts
init_cjs_shims();
var SENSITIVE_PATTERNS = [
  /api[_-]?key/i,
  /secret/i,
  /token/i,
  /password/i,
  /auth/i,
  /credential/i,
  /private[_-]?key/i
];
function redactSensitiveData2(obj) {
  if (typeof obj !== "object" || obj === null) {
    return obj;
  }
  if (Array.isArray(obj)) {
    return obj.map((item) => redactSensitiveData2(item));
  }
  const redacted = {};
  for (const [key, value] of Object.entries(obj)) {
    const isSensitive = SENSITIVE_PATTERNS.some((pattern) => pattern.test(key));
    if (isSensitive && typeof value === "string") {
      redacted[key] = maskValue(value);
    } else if (typeof value === "object") {
      redacted[key] = redactSensitiveData2(value);
    } else {
      redacted[key] = value;
    }
  }
  return redacted;
}
function maskValue(value) {
  if (!value || value.length <= 8) {
    return "***";
  }
  const start = value.slice(0, 4);
  const end = value.slice(-4);
  const middle = "*".repeat(Math.max(4, value.length - 8));
  return `${start}${middle}${end}`;
}
function sanitizeInput(input) {
  if (typeof input !== "string") {
    return input;
  }
  return input.replace(/[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g, "");
}
function isValidUUID(uuid) {
  const uuidRegex2 = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
  return uuidRegex2.test(uuid);
}
function isValidSessionId(sessionId) {
  return isValidUUID(sessionId) || /^[a-zA-Z0-9_-]{1,50}$/.test(sessionId);
}

// src/app/api/middleware/validation.ts
function handleValidationErrors(req, res, next) {
  const errors = (0, import_express_validator.validationResult)(req);
  if (!errors.isEmpty()) {
    errorResponse(
      res,
      ERROR_CODES.VALIDATION_ERROR,
      "Validation failed",
      400,
      errors.array(),
      req.requestId
    );
    return;
  }
  next();
}
function sanitizeTextInput(fields) {
  return (req, res, next) => {
    for (const field of fields) {
      if (req.body[field] && typeof req.body[field] === "string") {
        req.body[field] = sanitizeInput(req.body[field]);
      }
    }
    next();
  };
}
var validateMessageRequest = [
  (0, import_express_validator.body)("message").isString().isLength({ min: 1, max: 5e4 }).withMessage("Message must be a string between 1 and 50000 characters"),
  (0, import_express_validator.body)("sessionId").optional().custom((value) => {
    if (value && !isValidSessionId(value)) {
      throw new Error("Invalid session ID format");
    }
    return true;
  }),
  (0, import_express_validator.body)("images").optional().isArray().withMessage("Images must be an array"),
  (0, import_express_validator.body)("images.*").optional().isString().withMessage("Each image must be a base64 string"),
  sanitizeTextInput(["message"]),
  handleValidationErrors
];
var validateSessionId = [
  (0, import_express_validator.param)("sessionId").custom((value) => {
    if (!isValidSessionId(value)) {
      throw new Error("Invalid session ID format");
    }
    return true;
  }),
  handleValidationErrors
];
var validateCreateSession = [
  (0, import_express_validator.body)("sessionId").optional().custom((value) => {
    if (value && !isValidSessionId(value)) {
      throw new Error("Invalid session ID format");
    }
    return true;
  }),
  (0, import_express_validator.body)("config").optional().isObject().withMessage("Config must be an object"),
  handleValidationErrors
];
var validateMcpServerConfig2 = [
  (0, import_express_validator.body)("name").isString().isLength({ min: 1, max: 100 }).withMessage("Server name must be between 1 and 100 characters"),
  (0, import_express_validator.body)("command").optional().isString().withMessage("Command must be a string"),
  (0, import_express_validator.body)("args").optional().isArray().withMessage("Args must be an array"),
  (0, import_express_validator.body)("env").optional().isObject().withMessage("Environment must be an object"),
  (0, import_express_validator.body)("transport").optional().isIn(["stdio", "sse", "streamable-http"]).withMessage("Transport must be stdio, sse, or streamable-http"),
  (0, import_express_validator.body)("connectionMode").optional().isIn(["strict", "lenient"]).withMessage("Connection mode must be strict or lenient"),
  sanitizeTextInput(["name", "command"]),
  handleValidationErrors
];
var validateMcpServerId = [
  (0, import_express_validator.param)("serverId").isString().isLength({ min: 1, max: 100 }).withMessage("Server ID must be between 1 and 100 characters"),
  handleValidationErrors
];
var validateToolExecution = [
  (0, import_express_validator.param)("serverId").isString().isLength({ min: 1, max: 100 }).withMessage("Server ID must be between 1 and 100 characters"),
  (0, import_express_validator.param)("toolName").isString().isLength({ min: 1, max: 100 }).withMessage("Tool name must be between 1 and 100 characters"),
  (0, import_express_validator.body)("arguments").optional().isObject().withMessage("Arguments must be an object"),
  handleValidationErrors
];
var validateLlmConfig = [
  (0, import_express_validator.body)("provider").isString().isIn(["openai", "anthropic", "openrouter", "ollama", "qwen", "aws", "azure"]).withMessage(
    "Provider must be one of: openai, anthropic, openrouter, ollama, qwen, aws, azure"
  ),
  (0, import_express_validator.body)("model").isString().isLength({ min: 1, max: 100 }).withMessage("Model must be between 1 and 100 characters"),
  (0, import_express_validator.body)("config").optional().isObject().withMessage("Config must be an object"),
  // AWS-specific validations
  (0, import_express_validator.body)("config.aws.region").optional().isString().withMessage("AWS region must be a string"),
  (0, import_express_validator.body)("config.aws.accessKeyId").optional().isString().withMessage("AWS access key ID must be a string"),
  (0, import_express_validator.body)("config.aws.secretAccessKey").optional().isString().withMessage("AWS secret access key must be a string"),
  (0, import_express_validator.body)("config.aws.sessionToken").optional().isString().withMessage("AWS session token must be a string"),
  // Azure-specific validations
  (0, import_express_validator.body)("config.azure.endpoint").optional().isURL().withMessage("Azure endpoint must be a valid URL"),
  (0, import_express_validator.body)("config.azure.deploymentName").optional().isString().withMessage("Azure deployment name must be a string"),
  // Conditional validation - Azure requires endpoint
  (0, import_express_validator.body)().custom((value) => {
    if (value.provider === "azure" && !value.config?.azure?.endpoint) {
      throw new Error("Azure provider requires config.azure.endpoint to be provided");
    }
    if (value.provider === "aws" && !value.config?.aws) {
      throw new Error("AWS provider requires config.aws object to be provided");
    }
    return true;
  }),
  sanitizeTextInput(["provider", "model"]),
  handleValidationErrors
];
var validateListParams = [
  (0, import_express_validator.query)("limit").optional().isInt({ min: 1, max: 100 }).withMessage("Limit must be between 1 and 100"),
  (0, import_express_validator.query)("offset").optional().isInt({ min: 0 }).withMessage("Offset must be a non-negative integer"),
  handleValidationErrors
];

// src/app/api/routes/message.ts
init_logger2();
async function processMessageAsync(agent, message, options, requestId) {
  try {
    if (options.sessionId) {
      try {
        await agent.loadSession(options.sessionId);
      } catch {
        await agent.createSession(options.sessionId);
      }
    }
    let imageData;
    if (options.images && options.images.length > 0 && options.images[0]) {
      imageData = {
        image: options.images[0],
        mimeType: "image/jpeg"
      };
    } else if (options.imageData && typeof options.imageData === "string") {
      imageData = {
        image: options.imageData,
        mimeType: "image/jpeg"
      };
    }
    const { backgroundOperations } = await agent.run(message, imageData, options.sessionId);
    await backgroundOperations;
    logger.info("Async message processing completed", {
      requestId,
      sessionId: agent.getCurrentSessionId()
    });
  } catch (error) {
    logger.error("Async message processing failed", {
      requestId,
      error: error instanceof Error ? error.message : String(error)
    });
  }
}
function createMessageRoutes(agent) {
  const router = (0, import_express3.Router)();
  router.post("/", validateMessageRequest, async (req, res) => {
    try {
      const { message, sessionId, images, imageData, fileData, streaming = true } = req.body;
      logger.info("Processing async message request", {
        requestId: req.requestId,
        sessionId: sessionId || "default",
        hasImages: Boolean(images && images.length > 0),
        hasImageData: Boolean(imageData),
        hasFileData: Boolean(fileData),
        messageLength: message.length,
        streaming
      });
      successResponse(
        res,
        {
          message: "Message accepted for processing",
          sessionId: sessionId || agent.getCurrentSessionId(),
          requestId: req.requestId,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        202,
        req.requestId
      );
      processMessageAsync(
        agent,
        message,
        { sessionId, images, imageData, fileData },
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Async message processing setup failed", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Message processing setup failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.post("/sync", validateMessageRequest, async (req, res) => {
    try {
      const { message, sessionId, images } = req.body;
      logger.info("Processing message request", {
        requestId: req.requestId,
        sessionId: sessionId || "default",
        hasImages: Boolean(images && images.length > 0),
        messageLength: message.length
      });
      if (sessionId) {
        try {
          const session = await agent.loadSession(sessionId);
          logger.info(`Loaded session: ${session.id}`, { requestId: req.requestId });
        } catch (error) {
          const errorMsg = error instanceof Error ? error.message : String(error);
          logger.warn(`Session ${sessionId} not found, will create new one: ${errorMsg}`, {
            requestId: req.requestId
          });
          try {
            const newSession = await agent.createSession(sessionId);
            logger.info(`Created new session: ${newSession.id}`, { requestId: req.requestId });
          } catch (createError) {
            errorResponse(
              res,
              ERROR_CODES.SESSION_NOT_FOUND,
              `Failed to create session: ${createError instanceof Error ? createError.message : String(createError)}`,
              400,
              void 0,
              req.requestId
            );
            return;
          }
        }
      }
      let imageData;
      if (images && images.length > 0) {
        imageData = {
          image: images[0],
          mimeType: "image/jpeg"
          // Default, could be enhanced to detect actual type
        };
      }
      const { response, backgroundOperations } = await agent.run(message, imageData, sessionId);
      await backgroundOperations;
      successResponse(
        res,
        {
          response,
          sessionId: agent.getCurrentSessionId(),
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Message processing failed", {
        requestId: req.requestId,
        error: errorMsg,
        stack: error instanceof Error ? error.stack : void 0
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Message processing failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.post("/reset", async (req, res) => {
    try {
      const { sessionId } = req.body;
      logger.info("Processing reset request", {
        requestId: req.requestId,
        sessionId: sessionId || "current"
      });
      if (sessionId) {
        const success = await agent.removeSession(sessionId);
        if (!success) {
          errorResponse(
            res,
            ERROR_CODES.SESSION_NOT_FOUND,
            `Session ${sessionId} not found`,
            404,
            void 0,
            req.requestId
          );
          return;
        }
        const newSession = await agent.createSession(sessionId);
        successResponse(
          res,
          {
            message: `Session ${sessionId} has been reset`,
            sessionId: newSession.id,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          },
          200,
          req.requestId
        );
      } else {
        const currentSessionId = agent.getCurrentSessionId();
        if (currentSessionId) {
          await agent.removeSession(currentSessionId);
        }
        const newSession = await agent.createSession();
        successResponse(
          res,
          {
            message: "Current session has been reset",
            sessionId: newSession.id,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          },
          200,
          req.requestId
        );
      }
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Reset operation failed", {
        requestId: req.requestId,
        error: errorMsg,
        stack: error instanceof Error ? error.stack : void 0
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Reset operation failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  return router;
}

// src/app/api/routes/session.ts
init_cjs_shims();
var import_express4 = require("express");
init_logger2();
function createSessionRoutes(agent) {
  const router = (0, import_express4.Router)();
  router.get("/", validateListParams, async (req, res) => {
    const startTime = Date.now();
    try {
      logger.info("Listing sessions", { requestId: req.requestId });
      let sessionIds = [];
      try {
        sessionIds = await agent.listSessions();
        logger.debug(
          `Agent returned ${sessionIds.length} session IDs in ${Date.now() - startTime}ms`
        );
      } catch (listError) {
        logger.error("Failed to get session IDs from agent:", listError);
        sessionIds = [];
      }
      if (sessionIds.length === 0) {
        logger.debug("No sessions found, returning empty list");
        successResponse(
          res,
          {
            sessions: [],
            count: 0,
            currentSession: agent.getCurrentSessionId?.() || null,
            processingTime: Date.now() - startTime
          },
          200,
          req.requestId
        );
        return;
      }
      const sessionManager = agent.sessionManager;
      let sessionsMetadata;
      try {
        sessionsMetadata = await sessionManager.getBatchSessionMetadata(sessionIds);
        logger.debug(`Batch loaded ${sessionsMetadata.size} session metadata entries`);
      } catch (batchError) {
        logger.warn(
          "Batch metadata loading failed, falling back to individual processing:",
          batchError
        );
        sessionsMetadata = /* @__PURE__ */ new Map();
        for (const sessionId of sessionIds.slice(0, 50)) {
          try {
            const metadata = await agent.getSessionMetadata(sessionId);
            if (metadata) {
              sessionsMetadata.set(sessionId, metadata);
            }
          } catch (error) {
            logger.debug(`Failed to get metadata for session ${sessionId}:`, error);
          }
        }
      }
      const validSessions = Array.from(sessionsMetadata.values()).filter((session) => {
        return session.messageCount > 0;
      });
      const processingTime = Date.now() - startTime;
      logger.info(
        `Successfully listed ${validSessions.length}/${Array.from(sessionsMetadata.values()).length} valid sessions in ${processingTime}ms`
      );
      successResponse(
        res,
        {
          sessions: validSessions,
          count: validSessions.length,
          currentSession: agent.getCurrentSessionId?.() || null,
          processingTime
        },
        200,
        req.requestId
      );
      return;
    } catch (error) {
      let errorMsg;
      try {
        errorMsg = error instanceof Error ? error.message : String(error);
      } catch {
        errorMsg = "Unknown error occurred during session listing";
      }
      logger.error("Error listing sessions:", error);
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        "Failed to list sessions",
        500,
        { details: errorMsg, processingTime: Date.now() - startTime },
        req.requestId
      );
    }
  });
  function sanitizeSessionId(sessionId) {
    if (!sessionId) return null;
    const sanitized = sessionId.trim().replace(/[^\w-]/g, "-").replace(/^empty-?/i, "").replace(/^null-?/i, "").replace(/^undefined-?/i, "").replace(/^-+|-+$/g, "").replace(/-+/g, "-").substring(0, 64);
    if (!sanitized || sanitized.length < 3) return null;
    return sanitized;
  }
  router.post("/", validateCreateSession, async (req, res) => {
    try {
      const { sessionId: rawSessionId } = req.body;
      const sessionId = sanitizeSessionId(rawSessionId);
      logger.info("Creating new session", {
        requestId: req.requestId,
        originalSessionId: rawSessionId,
        sessionId: sessionId || "auto-generated"
      });
      const session = await agent.createSession(sessionId || void 0);
      successResponse(
        res,
        {
          session: {
            id: session.id,
            // Only include serializable session properties
            createdAt: (/* @__PURE__ */ new Date()).toISOString()
          },
          created: true,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        201,
        req.requestId
      );
    } catch (error) {
      let errorMsg;
      try {
        errorMsg = error instanceof Error ? error.message : String(error);
      } catch {
        errorMsg = "Unknown error occurred during session creation";
      }
      logger.error("Failed to create session", {
        requestId: req.requestId,
        error: errorMsg
      });
      if (errorMsg.includes("already exists") || errorMsg.includes("duplicate")) {
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          `Session creation failed: ${errorMsg}`,
          400,
          void 0,
          req.requestId
        );
      } else {
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          `Failed to create session: ${errorMsg}`,
          500,
          void 0,
          req.requestId
        );
      }
    }
  });
  router.get("/current", async (req, res) => {
    try {
      const currentSessionId = agent.getCurrentSessionId();
      const metadata = await agent.getSessionMetadata(currentSessionId);
      if (!metadata) {
        errorResponse(
          res,
          ERROR_CODES.SESSION_NOT_FOUND,
          "Current session not found",
          404,
          void 0,
          req.requestId
        );
        return;
      }
      successResponse(
        res,
        {
          sessionId: currentSessionId,
          metadata,
          isCurrent: true
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get current session", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get current session: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.get("/:sessionId", validateSessionId, async (req, res) => {
    try {
      const { sessionId } = req.params;
      if (!sessionId) {
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Session ID is required",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Getting session details", {
        requestId: req.requestId,
        sessionId
      });
      const session = await agent.getSession(sessionId);
      if (!session) {
        errorResponse(
          res,
          ERROR_CODES.SESSION_NOT_FOUND,
          `Session ${sessionId} not found`,
          404,
          void 0,
          req.requestId
        );
        return;
      }
      const metadata = await agent.getSessionMetadata(sessionId);
      successResponse(
        res,
        {
          sessionId,
          metadata,
          isCurrent: sessionId === agent.getCurrentSessionId()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get session details", {
        requestId: req.requestId,
        sessionId: req.params.sessionId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get session details: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.post("/:sessionId/load", validateSessionId, async (req, res) => {
    try {
      const { sessionId: rawSessionId } = req.params;
      const sessionId = sanitizeSessionId(rawSessionId);
      if (!sessionId) {
        logger.warn("Attempted to load session with invalid ID:", rawSessionId);
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Invalid session ID provided",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Loading session", {
        requestId: req.requestId,
        sessionId
      });
      let session;
      try {
        session = await agent.loadSession(sessionId);
      } catch (loadError) {
        const errorMsg = loadError instanceof Error ? loadError.message : String(loadError);
        logger.warn(`Session ${sessionId} not found, creating new session:`, {
          originalSessionId: rawSessionId,
          sanitizedSessionId: sessionId,
          error: errorMsg,
          requestId: req.requestId
        });
        try {
          session = await agent.createSession(sessionId);
          logger.info(`Created new session: ${sessionId}`);
        } catch (createError) {
          const createErrorMsg = createError instanceof Error ? createError.message : String(createError);
          logger.error(`Failed to create session ${sessionId}:`, createErrorMsg);
          session = await agent.createSession();
          logger.info(`Created auto-generated session: ${session.id}`);
        }
      }
      let conversationHistory = [];
      try {
        if (session && typeof session.refreshConversationHistory === "function") {
          await session.refreshConversationHistory();
          logger.debug(`Session ${sessionId}: Refreshed conversation history after loading`);
        }
        if (session && typeof session.getConversationHistory === "function") {
          conversationHistory = await session.getConversationHistory();
          logger.info(
            `Session ${sessionId}: Loaded with ${conversationHistory.length} messages in conversation history`
          );
        }
      } catch (historyError) {
        logger.warn(`Session ${sessionId}: Failed to refresh history after loading:`, historyError);
      }
      successResponse(
        res,
        {
          sessionId: session.id,
          loaded: true,
          currentSession: agent.getCurrentSessionId(),
          conversationHistory,
          // CRITICAL FIX: Return history with session load
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to load session", {
        requestId: req.requestId,
        sessionId: req.params.sessionId,
        error: errorMsg
      });
      if (errorMsg.includes("not found")) {
        errorResponse(
          res,
          ERROR_CODES.SESSION_NOT_FOUND,
          `Session ${req.params.sessionId} not found`,
          404,
          void 0,
          req.requestId
        );
      } else {
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          `Failed to load session: ${errorMsg}`,
          500,
          void 0,
          req.requestId
        );
      }
    }
  });
  router.get("/:sessionId/history", validateSessionId, async (req, res) => {
    const startTime = Date.now();
    try {
      const { sessionId } = req.params;
      if (!sessionId || sessionId === "null" || sessionId === "undefined") {
        logger.warn("Attempted to get history for session with invalid ID:", sessionId);
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Invalid session ID provided",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Getting session history", {
        requestId: req.requestId,
        sessionId
      });
      const sessionManager = agent.sessionManager;
      const cacheKey = `history_${sessionId}`;
      if (sessionManager.requestDeduplicator?.has(cacheKey)) {
        logger.debug(`Deduplicating concurrent history request for session ${sessionId}`);
        const history2 = await sessionManager.requestDeduplicator.get(cacheKey);
        successResponse(
          res,
          {
            sessionId,
            history: history2 || [],
            count: (history2 || []).length,
            timestamp: (/* @__PURE__ */ new Date()).toISOString(),
            processingTime: Date.now() - startTime,
            source: "deduplication"
          },
          200,
          req.requestId
        );
        return;
      }
      let history = [];
      let historySource = "none";
      const historyPromise = (async () => {
        try {
          const session = await agent.getSession(sessionId);
          if (session) {
            const sessionHistory = await agent.getSessionHistory(sessionId);
            if (sessionHistory && sessionHistory.length > 0) {
              return { history: sessionHistory, source: "session-manager" };
            }
          }
        } catch {
          logger.debug(`Session ${sessionId} not found in session manager`);
        }
        try {
          const storageManager = sessionManager.storageManager;
          if (storageManager?.isConnected()) {
            const backends = storageManager.getBackends();
            if (backends?.database) {
              const [messageHistory, sessionData] = await Promise.allSettled([
                backends.database.get(`messages:${sessionId}`),
                backends.database.get(`session:${sessionId}`)
              ]);
              if (messageHistory.status === "fulfilled" && messageHistory.value && Array.isArray(messageHistory.value)) {
                return { history: messageHistory.value, source: "database-messages" };
              }
              if (sessionData.status === "fulfilled" && sessionData.value?.conversationHistory && Array.isArray(sessionData.value.conversationHistory)) {
                return {
                  history: sessionData.value.conversationHistory,
                  source: "database-session"
                };
              }
            }
          }
        } catch (error) {
          logger.warn(`Failed to get history from database for session ${sessionId}:`, error);
        }
        return { history: [], source: "none" };
      })();
      if (sessionManager.requestDeduplicator) {
        sessionManager.requestDeduplicator.set(
          cacheKey,
          historyPromise.then((result) => result.history)
        );
      }
      try {
        const result = await historyPromise;
        history = result.history;
        historySource = result.source;
      } finally {
        if (sessionManager.requestDeduplicator) {
          sessionManager.requestDeduplicator.delete(cacheKey);
        }
      }
      const processingTime = Date.now() - startTime;
      logger.info(
        `Session ${sessionId} history retrieval: ${history.length} messages from ${historySource} in ${processingTime}ms`
      );
      successResponse(
        res,
        {
          sessionId,
          history,
          count: history.length,
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          processingTime,
          source: historySource
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      const processingTime = Date.now() - startTime;
      logger.error("Failed to get session history", {
        requestId: req.requestId,
        sessionId: req.params.sessionId,
        error: errorMsg,
        processingTime
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get session history: ${errorMsg}`,
        500,
        { processingTime },
        req.requestId
      );
    }
  });
  router.delete("/:sessionId", validateSessionId, async (req, res) => {
    try {
      const { sessionId } = req.params;
      if (!sessionId || sessionId === "null" || sessionId === "undefined") {
        logger.warn("Attempted to delete session with invalid ID:", sessionId);
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Invalid session ID provided",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Deleting session", {
        requestId: req.requestId,
        sessionId
      });
      const success = await agent.removeSession(sessionId);
      successResponse(
        res,
        {
          sessionId,
          deleted: true,
          successful: success,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to delete session", {
        requestId: req.requestId,
        sessionId: req.params.sessionId,
        error: errorMsg
      });
      if (errorMsg.includes("Cannot remove the currently active session")) {
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Cannot delete the currently active session. Switch to another session first.",
          400,
          void 0,
          req.requestId
        );
      } else {
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          `Failed to delete session: ${errorMsg}`,
          500,
          void 0,
          req.requestId
        );
      }
    }
  });
  router.get("/stats", async (req, res) => {
    try {
      logger.info("Getting session performance stats", { requestId: req.requestId });
      const sessionManager = agent.sessionManager;
      const stats = await sessionManager.getSessionStats();
      const runtimeStats = {
        uptime: process.uptime(),
        memoryUsage: process.memoryUsage(),
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        requestId: req.requestId
      };
      successResponse(
        res,
        {
          sessionStats: stats,
          runtimeStats,
          optimizationStatus: {
            cachingEnabled: true,
            batchProcessingEnabled: true,
            requestDeduplicationEnabled: true,
            performanceMonitoringEnabled: true
          }
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get session stats", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get session stats: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  return router;
}

// src/app/api/routes/mcp.ts
init_cjs_shims();
var import_express5 = require("express");
init_logger2();
function createMcpRoutes(agent) {
  const router = (0, import_express5.Router)();
  router.get("/tools", validateListParams, async (req, res) => {
    try {
      logger.info("Getting all MCP tools from all servers", { requestId: req.requestId });
      const mcpClients = agent.getMcpClients();
      const allTools = [];
      for (const [serverId, client] of mcpClients.entries()) {
        try {
          const tools = await client.getTools();
          if (tools && Array.isArray(tools)) {
            const serverTools = tools.map((tool) => ({
              ...tool,
              serverId,
              serverName: serverId
              // Could be enhanced with actual server names
            }));
            allTools.push(...serverTools);
          }
        } catch (error) {
          logger.warn("Failed to get tools from MCP server", {
            requestId: req.requestId,
            serverId,
            error: error instanceof Error ? error.message : String(error)
          });
        }
      }
      successResponse(
        res,
        {
          tools: allTools,
          totalTools: allTools.length,
          connectedServers: mcpClients.size,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get all MCP tools", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.MCP_SERVER_ERROR,
        `Failed to get MCP tools: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.get("/servers", validateListParams, async (req, res) => {
    try {
      logger.info("Listing MCP servers", { requestId: req.requestId });
      const allServers = agent.getAllMcpServers();
      const connectedCount = allServers.filter(
        (s) => s.status === "connected"
      ).length;
      const failedCount = allServers.filter((s) => s.status === "error").length;
      successResponse(
        res,
        {
          servers: allServers,
          totalConnected: connectedCount,
          totalFailed: failedCount,
          totalServers: allServers.length
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to list MCP servers", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.MCP_SERVER_ERROR,
        `Failed to list MCP servers: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.post("/servers", validateMcpServerConfig2, async (req, res) => {
    try {
      const { name, transport, command, args, url, env: env3, headers, timeout, connectionMode } = req.body;
      logger.info("Connecting MCP server", {
        requestId: req.requestId,
        serverName: name,
        serverType: transport
      });
      let config2 = {
        type: transport,
        // Map transport back to type for MCP config
        timeout: timeout || 3e4,
        connectionMode: connectionMode || "lenient",
        enabled: true
      };
      if (transport === "stdio") {
        config2.command = command;
        config2.args = args || [];
        config2.env = env3 || {};
      } else if (transport === "sse" || transport === "streamable-http") {
        config2.url = url;
        config2.headers = headers || {};
      }
      await agent.connectMcpServer(name, config2);
      successResponse(
        res,
        {
          serverName: name,
          connected: true,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        201,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to connect MCP server", {
        requestId: req.requestId,
        serverName: req.body.name,
        error: errorMsg
      });
      let statusCode = 500;
      if (errorMsg.includes("already exists") || errorMsg.includes("duplicate")) {
        statusCode = 409;
      } else if (errorMsg.includes("Invalid") || errorMsg.includes("validation")) {
        statusCode = 400;
      }
      errorResponse(
        res,
        ERROR_CODES.MCP_SERVER_ERROR,
        `Failed to connect MCP server: ${errorMsg}`,
        statusCode,
        void 0,
        req.requestId
      );
    }
  });
  router.delete("/servers/:serverId", validateMcpServerId, async (req, res) => {
    try {
      const { serverId } = req.params;
      if (!serverId) {
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Server ID is required",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Disconnecting MCP server", {
        requestId: req.requestId,
        serverId
      });
      await agent.removeMcpServer(serverId);
      successResponse(
        res,
        {
          serverId,
          disconnected: true,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to disconnect MCP server", {
        requestId: req.requestId,
        serverId: req.params.serverId,
        error: errorMsg
      });
      if (errorMsg.includes("not found")) {
        errorResponse(
          res,
          ERROR_CODES.NOT_FOUND,
          `MCP server ${req.params.serverId} not found`,
          404,
          void 0,
          req.requestId
        );
      } else {
        errorResponse(
          res,
          ERROR_CODES.MCP_SERVER_ERROR,
          `Failed to disconnect MCP server: ${errorMsg}`,
          500,
          void 0,
          req.requestId
        );
      }
    }
  });
  router.get(
    "/servers/:serverId/tools",
    validateMcpServerId,
    async (req, res) => {
      try {
        const { serverId } = req.params;
        if (!serverId) {
          errorResponse(
            res,
            ERROR_CODES.BAD_REQUEST,
            "Server ID is required",
            400,
            void 0,
            req.requestId
          );
          return;
        }
        logger.info("Listing tools for MCP server", {
          requestId: req.requestId,
          serverId
        });
        const clients = agent.getMcpClients();
        const client = clients.get(serverId);
        if (!client) {
          errorResponse(
            res,
            ERROR_CODES.NOT_FOUND,
            `MCP server ${serverId} not found or not connected`,
            404,
            void 0,
            req.requestId
          );
          return;
        }
        const toolSet = await client.getTools();
        const tools = Object.entries(toolSet).map(([name, tool]) => ({
          name,
          description: tool.description,
          parameters: tool.parameters
        }));
        successResponse(
          res,
          {
            serverId,
            tools,
            count: tools.length
          },
          200,
          req.requestId
        );
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : String(error);
        logger.error("Failed to list tools for MCP server", {
          requestId: req.requestId,
          serverId: req.params.serverId,
          error: errorMsg
        });
        errorResponse(
          res,
          ERROR_CODES.MCP_SERVER_ERROR,
          `Failed to list tools: ${errorMsg}`,
          500,
          void 0,
          req.requestId
        );
      }
    }
  );
  router.post(
    "/servers/:serverId/tools/:toolName/execute",
    validateToolExecution,
    async (req, res) => {
      try {
        const { serverId, toolName } = req.params;
        if (!serverId || !toolName) {
          errorResponse(
            res,
            ERROR_CODES.BAD_REQUEST,
            "Server ID and tool name are required",
            400,
            void 0,
            req.requestId
          );
          return;
        }
        const { arguments: toolArgs } = req.body;
        logger.info("Executing MCP tool", {
          requestId: req.requestId,
          serverId,
          toolName,
          hasArguments: Boolean(toolArgs)
        });
        const clients = agent.getMcpClients();
        const client = clients.get(serverId);
        if (!client) {
          errorResponse(
            res,
            ERROR_CODES.NOT_FOUND,
            `MCP server ${serverId} not found or not connected`,
            404,
            void 0,
            req.requestId
          );
          return;
        }
        const result = await agent.executeMcpTool(toolName, toolArgs || {});
        successResponse(
          res,
          {
            serverId,
            toolName,
            result,
            executed: true,
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          },
          200,
          req.requestId
        );
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : String(error);
        logger.error("Failed to execute MCP tool", {
          requestId: req.requestId,
          serverId: req.params.serverId,
          toolName: req.params.toolName,
          error: errorMsg
        });
        let statusCode = 500;
        if (errorMsg.includes("not found") || errorMsg.includes("unknown tool")) {
          statusCode = 404;
        } else if (errorMsg.includes("invalid") || errorMsg.includes("validation")) {
          statusCode = 400;
        }
        errorResponse(
          res,
          ERROR_CODES.MCP_SERVER_ERROR,
          `Failed to execute tool: ${errorMsg}`,
          statusCode,
          void 0,
          req.requestId
        );
      }
    }
  );
  return router;
}

// src/app/api/routes/config.ts
init_cjs_shims();
var import_express6 = require("express");
init_logger2();
var import_js_yaml = require("js-yaml");
function createConfigRoutes(agent) {
  const router = (0, import_express6.Router)();
  router.get("/yaml", async (req, res) => {
    try {
      logger.info("Exporting configuration as YAML", { requestId: req.requestId });
      const config2 = agent.getEffectiveConfig();
      const redactedConfig = redactSensitiveData2(config2);
      const yamlConfig = (0, import_js_yaml.dump)(redactedConfig, {
        indent: 2,
        lineWidth: 120,
        noRefs: true
      });
      res.setHeader("Content-Type", "application/x-yaml; charset=utf-8");
      res.setHeader("Content-Disposition", 'attachment; filename="cipher-config.yml"');
      res.setHeader("X-Request-ID", req.requestId);
      res.send(yamlConfig);
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to export configuration", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to export configuration: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.get("/", async (req, res) => {
    try {
      logger.info("Getting current configuration", { requestId: req.requestId });
      const config2 = agent.getEffectiveConfig();
      const redactedConfig = redactSensitiveData2(config2);
      successResponse(
        res,
        {
          config: redactedConfig,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get configuration", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get configuration: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.get("/session/:sessionId", async (req, res) => {
    try {
      const { sessionId } = req.params;
      if (!sessionId) {
        errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Session ID is required",
          400,
          void 0,
          req.requestId
        );
        return;
      }
      logger.info("Getting session-specific configuration", {
        requestId: req.requestId,
        sessionId
      });
      const session = await agent.getSession(sessionId);
      if (!session) {
        errorResponse(
          res,
          ERROR_CODES.SESSION_NOT_FOUND,
          `Session ${sessionId} not found`,
          404,
          void 0,
          req.requestId
        );
        return;
      }
      const config2 = agent.getEffectiveConfig(sessionId);
      const redactedConfig = redactSensitiveData2(config2);
      successResponse(
        res,
        {
          sessionId,
          config: redactedConfig,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get session configuration", {
        requestId: req.requestId,
        sessionId: req.params.sessionId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get session configuration: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  return router;
}

// src/app/api/routes/llm.ts
init_cjs_shims();
var import_express7 = require("express");
init_logger2();
function createLlmRoutes(agent) {
  const router = (0, import_express7.Router)();
  router.get("/config", async (req, res) => {
    return getCurrentLLMConfig(req, res, agent);
  });
  router.put("/config", validateLlmConfig, async (req, res) => {
    return switchLLMConfig(req, res, agent);
  });
  router.get("/current", async (req, res) => {
    return getCurrentLLMConfig(req, res, agent);
  });
  router.get("/providers", async (req, res) => {
    try {
      logger.info("Getting available LLM providers", { requestId: req.requestId });
      const providers = {
        openai: {
          name: "OpenAI",
          models: ["gpt-4o", "gpt-4o-mini", "gpt-4-turbo", "gpt-4", "gpt-3.5-turbo"],
          requiresApiKey: true,
          description: "OpenAI GPT models"
        },
        anthropic: {
          name: "Anthropic",
          models: [
            "claude-3-5-sonnet-20241022",
            "claude-3-5-haiku-20241022",
            "claude-3-opus-20240229",
            "claude-3-sonnet-20240229",
            "claude-3-haiku-20240307"
          ],
          requiresApiKey: true,
          description: "Anthropic Claude models"
        },
        openrouter: {
          name: "OpenRouter",
          models: [
            "openai/gpt-4o",
            "anthropic/claude-3.5-sonnet",
            "meta-llama/llama-3.1-405b-instruct",
            "google/gemini-pro-1.5"
          ],
          requiresApiKey: true,
          description: "OpenRouter unified API access"
        },
        ollama: {
          name: "Ollama",
          models: [
            "llama3.1:latest",
            "llama3.1:8b",
            "llama3.1:70b",
            "codellama:latest",
            "mistral:latest",
            "mixtral:latest"
          ],
          requiresApiKey: false,
          description: "Local Ollama models",
          note: "Requires Ollama server running locally"
        }
      };
      successResponse(
        res,
        {
          providers,
          availableProviders: Object.keys(providers),
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get LLM providers", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.LLM_ERROR,
        `Failed to get LLM providers: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  router.post("/switch", validateLlmConfig, async (req, res) => {
    return switchLLMConfig(req, res, agent);
  });
  router.get("/status", async (req, res) => {
    try {
      logger.info("Getting LLM status", { requestId: req.requestId });
      const llmConfig = agent.getCurrentLLMConfig();
      const status = {
        configured: Boolean(llmConfig.provider && llmConfig.model),
        provider: llmConfig.provider,
        model: llmConfig.model,
        healthy: true,
        // Placeholder - would need actual health check
        lastCheck: (/* @__PURE__ */ new Date()).toISOString()
      };
      successResponse(
        res,
        {
          status,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get LLM status", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.LLM_ERROR,
        `Failed to get LLM status: ${errorMsg}`,
        500,
        void 0,
        req.requestId
      );
    }
  });
  return router;
}
async function getCurrentLLMConfig(req, res, agent) {
  try {
    logger.info("Getting current LLM configuration", { requestId: req.requestId });
    const llmConfig = agent.getCurrentLLMConfig();
    const sanitizedConfig = {
      ...llmConfig,
      // Remove or mask sensitive fields
      apiKey: llmConfig.apiKey ? "***" : void 0
      // Keep other configuration details
    };
    successResponse(
      res,
      {
        llmConfig: sanitizedConfig,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      },
      200,
      req.requestId
    );
  } catch (error) {
    const errorMsg = error instanceof Error ? error.message : String(error);
    logger.error("Failed to get current LLM configuration", {
      requestId: req.requestId,
      error: errorMsg
    });
    errorResponse(
      res,
      ERROR_CODES.LLM_ERROR,
      `Failed to get LLM configuration: ${errorMsg}`,
      500,
      void 0,
      req.requestId
    );
  }
}
async function switchLLMConfig(req, res, agent) {
  try {
    const { provider, model, sessionId } = req.body;
    logger.info("Switching LLM configuration", {
      requestId: req.requestId,
      provider,
      model,
      sessionId
    });
    logger.warn("LLM switching not yet implemented", { provider, model, sessionId });
    const updatedConfig = agent.getCurrentLLMConfig();
    const sanitizedConfig = {
      ...updatedConfig,
      apiKey: updatedConfig.apiKey ? "***" : void 0
    };
    successResponse(
      res,
      {
        message: "LLM configuration updated successfully",
        llmConfig: sanitizedConfig,
        timestamp: (/* @__PURE__ */ new Date()).toISOString()
      },
      200,
      req.requestId
    );
  } catch (error) {
    const errorMsg = error instanceof Error ? error.message : String(error);
    logger.error("Failed to switch LLM configuration", {
      requestId: req.requestId,
      error: errorMsg
    });
    errorResponse(
      res,
      ERROR_CODES.LLM_ERROR,
      `Failed to switch LLM: ${errorMsg}`,
      500,
      void 0,
      req.requestId
    );
  }
}

// src/app/api/routes/search.ts
init_cjs_shims();
var import_express8 = require("express");
init_logger2();
function createSearchRoutes(_agent) {
  const router = (0, import_express8.Router)();
  router.get("/messages", async (req, res) => {
    try {
      logger.info("Message search requested but not implemented", {
        requestId: req.requestId,
        query: req.query
      });
      return errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        "Search functionality is not yet implemented. This feature will be available in a future release.",
        501,
        void 0,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Message search route error", {
        requestId: req.requestId,
        error: errorMsg,
        stack: error instanceof Error ? error.stack : void 0
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Message search failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.get("/sessions", async (req, res) => {
    try {
      logger.info("Session search requested but not implemented", {
        requestId: req.requestId,
        query: req.query
      });
      return errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        "Search functionality is not yet implemented. This feature will be available in a future release.",
        501,
        void 0,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Session search route error", {
        requestId: req.requestId,
        error: errorMsg,
        stack: error instanceof Error ? error.stack : void 0
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Session search failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  return router;
}

// src/app/api/routes/webhook.ts
init_cjs_shims();
var import_express9 = require("express");
init_logger2();
var import_crypto4 = require("crypto");
var webhooks = /* @__PURE__ */ new Map();
function createWebhookRoutes(_agent) {
  const router = (0, import_express9.Router)();
  router.post("/", async (req, res) => {
    try {
      const { url, events = ["*"] } = req.body;
      if (!url || typeof url !== "string") {
        return errorResponse(
          res,
          ERROR_CODES.VALIDATION_ERROR,
          "URL parameter is required",
          400,
          void 0,
          req.requestId
        );
      }
      try {
        new URL(url);
      } catch {
        return errorResponse(
          res,
          ERROR_CODES.VALIDATION_ERROR,
          "Invalid URL format",
          400,
          void 0,
          req.requestId
        );
      }
      const webhookId = (0, import_crypto4.randomUUID)();
      const webhookData = {
        id: webhookId,
        url,
        events: Array.isArray(events) ? events : ["*"],
        active: true,
        createdAt: (/* @__PURE__ */ new Date()).toISOString()
      };
      webhooks.set(webhookId, webhookData);
      logger.info("Webhook registered", {
        requestId: req.requestId,
        webhookId,
        url,
        events: webhookData.events
      });
      successResponse(
        res,
        {
          webhook: webhookData,
          message: "Webhook registered successfully"
        },
        201,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Webhook registration failed", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Webhook registration failed: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.get("/", async (req, res) => {
    try {
      const webhookList = Array.from(webhooks.values());
      logger.info("Listing webhooks", {
        requestId: req.requestId,
        count: webhookList.length
      });
      successResponse(
        res,
        {
          webhooks: webhookList,
          total: webhookList.length,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to list webhooks", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to list webhooks: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.get("/:webhookId", async (req, res) => {
    try {
      const { webhookId } = req.params;
      if (!webhookId) {
        return errorResponse(
          res,
          ERROR_CODES.VALIDATION_ERROR,
          "Webhook ID is required",
          400,
          void 0,
          req.requestId
        );
      }
      const webhook = webhooks.get(webhookId);
      if (!webhook) {
        return errorResponse(
          res,
          ERROR_CODES.NOT_FOUND,
          `Webhook ${webhookId} not found`,
          404,
          void 0,
          req.requestId
        );
      }
      logger.info("Retrieved webhook details", {
        requestId: req.requestId,
        webhookId
      });
      successResponse(
        res,
        {
          webhook
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to get webhook details", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to get webhook details: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.delete("/:webhookId", async (req, res) => {
    try {
      const { webhookId } = req.params;
      if (!webhookId) {
        return errorResponse(
          res,
          ERROR_CODES.VALIDATION_ERROR,
          "Webhook ID is required",
          400,
          void 0,
          req.requestId
        );
      }
      const webhook = webhooks.get(webhookId);
      if (!webhook) {
        return errorResponse(
          res,
          ERROR_CODES.NOT_FOUND,
          `Webhook ${webhookId} not found`,
          404,
          void 0,
          req.requestId
        );
      }
      webhooks.delete(webhookId);
      logger.info("Webhook removed", {
        requestId: req.requestId,
        webhookId
      });
      successResponse(
        res,
        {
          message: `Webhook ${webhookId} removed successfully`,
          webhookId
        },
        200,
        req.requestId
      );
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to remove webhook", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to remove webhook: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  router.post("/:webhookId/test", async (req, res) => {
    try {
      const { webhookId } = req.params;
      if (!webhookId) {
        return errorResponse(
          res,
          ERROR_CODES.VALIDATION_ERROR,
          "Webhook ID is required",
          400,
          void 0,
          req.requestId
        );
      }
      const webhook = webhooks.get(webhookId);
      if (!webhook) {
        return errorResponse(
          res,
          ERROR_CODES.NOT_FOUND,
          `Webhook ${webhookId} not found`,
          404,
          void 0,
          req.requestId
        );
      }
      const testPayload = {
        event: "webhook_test",
        data: {
          message: "This is a test webhook call from Cipher",
          timestamp: (/* @__PURE__ */ new Date()).toISOString(),
          webhookId
        },
        source: "cipher-agent"
      };
      try {
        const response = await fetch(webhook.url, {
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            "User-Agent": "Cipher-Agent-Webhook/1.0"
          },
          body: JSON.stringify(testPayload)
        });
        webhook.lastTriggered = (/* @__PURE__ */ new Date()).toISOString();
        webhooks.set(webhookId, webhook);
        logger.info("Webhook test completed", {
          requestId: req.requestId,
          webhookId,
          status: response.status,
          ok: response.ok
        });
        successResponse(
          res,
          {
            message: "Webhook test completed",
            webhookId,
            testResult: {
              status: response.status,
              ok: response.ok,
              url: webhook.url,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            }
          },
          200,
          req.requestId
        );
      } catch (fetchError) {
        logger.error("Webhook test failed", {
          requestId: req.requestId,
          webhookId,
          error: fetchError instanceof Error ? fetchError.message : String(fetchError)
        });
        successResponse(
          res,
          {
            message: "Webhook test completed with error",
            webhookId,
            testResult: {
              status: 0,
              ok: false,
              error: fetchError instanceof Error ? fetchError.message : String(fetchError),
              url: webhook.url,
              timestamp: (/* @__PURE__ */ new Date()).toISOString()
            }
          },
          200,
          // Still return 200 as the test was executed
          req.requestId
        );
      }
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error("Failed to test webhook", {
        requestId: req.requestId,
        error: errorMsg
      });
      errorResponse(
        res,
        ERROR_CODES.INTERNAL_ERROR,
        `Failed to test webhook: ${errorMsg}`,
        500,
        process.env.NODE_ENV === "development" ? error : void 0,
        req.requestId
      );
    }
  });
  return router;
}

// src/app/api/server.ts
var ApiServer = class {
  app;
  agent;
  config;
  apiPrefix;
  mcpServer;
  activeMcpSseTransports = /* @__PURE__ */ new Map();
  // WebSocket components
  httpServer;
  wss;
  wsConnectionManager;
  wsMessageRouter;
  wsEventSubscriber;
  heartbeatInterval;
  constructor(agent, config2) {
    this.agent = agent;
    this.config = config2;
    this.apiPrefix = this.validateAndNormalizeApiPrefix(config2.apiPrefix);
    this.app = (0, import_express10.default)();
    this.setupMiddleware();
    this.setupRoutes();
    this.setupErrorHandling();
  }
  /**
   * Validate and normalize API prefix configuration
   */
  validateAndNormalizeApiPrefix(prefix) {
    if (prefix === void 0) {
      return "/api";
    }
    if (prefix === "") {
      return "";
    }
    if (typeof prefix !== "string") {
      throw new Error("API prefix must be a string");
    }
    if (!prefix.startsWith("/")) {
      prefix = "/" + prefix;
    }
    if (prefix.endsWith("/") && prefix !== "/") {
      prefix = prefix.slice(0, -1);
    }
    logger.info(`[API Server] Using API prefix: '${prefix || "(none)"}'`);
    return prefix;
  }
  /**
   * Helper method to construct API route paths
   */
  buildApiRoute(route) {
    if (!this.apiPrefix || this.apiPrefix === "") {
      return route;
    }
    return `${this.apiPrefix}${route}`;
  }
  async setupMcpServer(transportType, _port) {
    logger.info(`[API Server] Setting up MCP server with transport type: ${transportType}`);
    try {
      const agentCard = this.agent.getEffectiveConfig().agentCard;
      const agentCardInput = agentCard ? Object.fromEntries(Object.entries(agentCard).filter(([, value]) => value !== void 0)) : {};
      const agentCardData = initializeAgentCardResource(agentCardInput);
      const mcpServerMode = process.env.MCP_SERVER_MODE || "default";
      logger.info(`[API Server] MCP server mode: ${mcpServerMode}`);
      let aggregatorConfig = void 0;
      if (mcpServerMode === "aggregator") {
        aggregatorConfig = await this.loadAggregatorConfig();
      }
      this.mcpServer = await initializeMcpServer(
        this.agent,
        agentCardData,
        mcpServerMode,
        aggregatorConfig
      );
      if (transportType === "sse") {
        this.setupMcpSseRoutes();
      } else if (transportType === "stdio") {
        logger.warn(
          `[API Server] MCP transport type 'stdio' is typically handled by the CLI directly and not integrated into the API server.`
        );
      } else {
        logger.warn(
          `[API Server] MCP transport type '${transportType}' not fully implemented for API server integration yet.`
        );
      }
    } catch (error) {
      logger.error(
        `[API Server] Failed to set up MCP server: ${error instanceof Error ? error.message : String(error)}`
      );
    }
  }
  /**
   * Load aggregator configuration from environment variables
   * Aggregator mode now uses agent's unifiedToolManager which automatically includes MCP servers from cipher.yml
   */
  async loadAggregatorConfig() {
    const defaultConfig = {
      type: "aggregator",
      servers: {},
      // No longer needed - using unifiedToolManager
      conflictResolution: process.env.AGGREGATOR_CONFLICT_RESOLUTION || "prefix",
      autoDiscovery: false,
      timeout: parseInt(process.env.AGGREGATOR_TIMEOUT || "60000"),
      connectionMode: "lenient"
    };
    logger.info("[API Server] Using aggregator configuration with env vars", {
      conflictResolution: defaultConfig.conflictResolution,
      timeout: defaultConfig.timeout
    });
    return defaultConfig;
  }
  setupMcpSseRoutes() {
    if (!this.mcpServer) {
      logger.error("[API Server] MCP Server not initialized for SSE route setup.");
      return;
    }
    this.app.get(this.buildApiRoute("/mcp/sse"), (req, res) => {
      logger.info("[API Server] New MCP SSE client attempting connection.");
      logger.debug("[API Server] SSE Request Headers:", req.headers);
      logger.debug("[API Server] SSE Request URL:", req.url);
      const sseTransport = new import_sse3.SSEServerTransport(this.buildApiRoute("/mcp"), res);
      logger.debug(
        `[API Server] SSEServerTransport created with endpoint ${this.buildApiRoute("/mcp")}`
      );
      this.mcpServer?.connect(sseTransport);
      logger.debug("[API Server] MCP server connected to SSE transport");
      this.activeMcpSseTransports.set(sseTransport.sessionId, sseTransport);
      logger.info(
        `[API Server] MCP SSE client connected with session ID: ${sseTransport.sessionId}`
      );
      logger.debug(`[API Server] Active SSE transports count: ${this.activeMcpSseTransports.size}`);
      req.on("close", () => {
        logger.info(
          `[API Server] MCP SSE client with session ID ${sseTransport.sessionId} disconnected.`
        );
        logger.debug("[API Server] Cleaning up SSE transport and connection");
        sseTransport.close();
        this.activeMcpSseTransports.delete(sseTransport.sessionId);
        logger.debug(
          `[API Server] Active SSE transports count after cleanup: ${this.activeMcpSseTransports.size}`
        );
      });
    });
    this.app.post(this.buildApiRoute("/mcp"), async (req, res) => {
      logger.debug("[API Server] MCP POST request received");
      logger.debug("[API Server] POST Request Headers:", req.headers);
      logger.debug("[API Server] POST Request Body:", req.body);
      logger.debug("[API Server] POST Request Query:", req.query);
      if (!this.mcpServer) {
        logger.error("[API Server] MCP Server not initialized for POST route.");
        return errorResponse(res, ERROR_CODES.INTERNAL_ERROR, "MCP Server not ready", 500);
      }
      const sessionId = req.query.sessionId;
      if (!sessionId) {
        logger.error("[API Server] MCP POST request received without session ID.");
        logger.debug("[API Server] Available query parameters:", Object.keys(req.query));
        return errorResponse(
          res,
          ERROR_CODES.BAD_REQUEST,
          "Missing sessionId in query parameters",
          400
        );
      }
      logger.debug(`[API Server] Looking for SSE transport with session ID: ${sessionId}`);
      logger.debug(
        `[API Server] Available session IDs: ${Array.from(this.activeMcpSseTransports.keys()).join(", ")}`
      );
      const sseTransport = this.activeMcpSseTransports.get(sessionId);
      if (!sseTransport) {
        logger.error(`[API Server] No active MCP SSE transport found for session ID: ${sessionId}`);
        return errorResponse(
          res,
          ERROR_CODES.NOT_FOUND,
          `No active session found for ID: ${sessionId}`,
          404
        );
      }
      try {
        logger.debug(
          `[API Server] Delegating POST message to SSE transport for session: ${sessionId}`
        );
        await sseTransport.handlePostMessage(req, res, req.body);
        logger.debug(`[API Server] POST message handled successfully for session: ${sessionId}`);
      } catch (error) {
        logger.error(
          `[API Server] Error handling MCP POST request for session ${sessionId}: ${error instanceof Error ? error.message : String(error)}`
        );
        logger.debug(
          `[API Server] POST error stack:`,
          error instanceof Error ? error.stack : "No stack available"
        );
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          "Error processing MCP request",
          500,
          error instanceof Error ? error.stack : void 0
        );
      }
    });
    const mcpSseRoute = this.buildApiRoute("/mcp/sse");
    const mcpPostRoute = this.buildApiRoute("/mcp");
    logger.info(
      `[API Server] MCP SSE (GET ${mcpSseRoute}) and POST (${mcpPostRoute}?sessionId=...) routes registered.`
    );
  }
  /**
   * Set up WebSocket server and event handling
   */
  setupWebSocket() {
    if (!this.config.enableWebSocket || !this.httpServer) {
      logger.debug("[API Server] WebSocket disabled or HTTP server not available");
      return;
    }
    const wsConfig = this.config.webSocketConfig || {};
    this.wss = new import_ws3.WebSocketServer({
      server: this.httpServer,
      path: wsConfig.path || "/ws",
      ...wsConfig.maxConnections && { maxClients: wsConfig.maxConnections },
      ...wsConfig.enableCompression !== void 0 && {
        perMessageDeflate: wsConfig.enableCompression
      }
    });
    this.wsConnectionManager = new WebSocketConnectionManager(
      wsConfig.maxConnections || 1e3,
      wsConfig.connectionTimeout || 3e5
    );
    this.wsMessageRouter = new WebSocketMessageRouter(this.agent, this.wsConnectionManager);
    this.wsEventSubscriber = new WebSocketEventSubscriber(
      this.wsConnectionManager,
      this.agent.services.eventManager
    );
    this.wsConnectionManager.setEventSubscriber(this.wsEventSubscriber);
    this.wss.on("connection", (ws, request) => {
      this.handleWebSocketConnection(ws, request);
    });
    this.wsEventSubscriber.subscribe();
    if (wsConfig.heartbeatInterval && wsConfig.heartbeatInterval > 0) {
      this.heartbeatInterval = setInterval(() => {
        this.wsConnectionManager?.sendHeartbeat();
      }, wsConfig.heartbeatInterval);
    }
    process.on("SIGTERM", () => {
      this.shutdownWebSocket();
    });
    process.on("SIGINT", () => {
      this.shutdownWebSocket();
    });
    logger.info("[API Server] WebSocket server initialized", {
      path: wsConfig.path || "/ws",
      maxConnections: wsConfig.maxConnections || 1e3,
      compression: wsConfig.enableCompression !== false,
      heartbeat: wsConfig.heartbeatInterval || "disabled"
    });
  }
  /**
   * Handle new WebSocket connection
   */
  handleWebSocketConnection(ws, request) {
    try {
      const url = new URL(request.url || "", `http://${request.headers.host}`);
      const sessionId = url.searchParams.get("sessionId") || void 0;
      const connectionId = this.wsConnectionManager.addConnection(ws, sessionId);
      logger.info("[API Server] New WebSocket connection established", {
        connectionId,
        sessionId,
        origin: request.headers.origin,
        userAgent: request.headers["user-agent"]
      });
      ws.on("message", async (data) => {
        try {
          const message = JSON.parse(data.toString());
          await this.wsMessageRouter.routeMessage(ws, connectionId, message);
        } catch (error) {
          logger.error("[API Server] Error parsing WebSocket message", {
            connectionId,
            error: error instanceof Error ? error.message : String(error),
            rawData: data.toString().substring(0, 200)
            // Log first 200 chars
          });
          if (ws.readyState === import_ws3.WebSocket.OPEN) {
            ws.send(
              JSON.stringify({
                event: "error",
                error: "Invalid message format",
                data: {
                  message: "Failed to parse JSON message",
                  code: "INVALID_JSON"
                },
                timestamp: Date.now()
              })
            );
          }
        }
      });
      if (ws.readyState === import_ws3.WebSocket.OPEN) {
        ws.send(
          JSON.stringify({
            event: "connected",
            data: {
              connectionId,
              sessionId,
              serverVersion: process.env.npm_package_version || "unknown",
              capabilities: ["streaming", "tools", "memory", "reset"]
            },
            timestamp: Date.now()
          })
        );
      }
    } catch (error) {
      logger.error("[API Server] Error handling WebSocket connection", {
        error: error instanceof Error ? error.message : String(error),
        origin: request.headers.origin
      });
      if (ws.readyState === import_ws3.WebSocket.OPEN) {
        ws.close(1011, "Server error during connection setup");
      }
    }
  }
  /**
   * Shutdown WebSocket server gracefully
   */
  shutdownWebSocket() {
    if (this.heartbeatInterval) {
      clearInterval(this.heartbeatInterval);
    }
    if (this.wsEventSubscriber) {
      this.wsEventSubscriber.dispose();
    }
    if (this.wsConnectionManager) {
      this.wsConnectionManager.dispose();
    }
    if (this.wss) {
      this.wss.close(() => {
        logger.info("[API Server] WebSocket server closed");
      });
    }
  }
  setupMiddleware() {
    this.app.use(
      (0, import_helmet.default)({
        contentSecurityPolicy: false,
        // Disable CSP for API
        crossOriginEmbedderPolicy: false
      })
    );
    this.app.use(
      (0, import_cors.default)({
        origin: this.config.corsOrigins || ["http://localhost:3000"],
        methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
        allowedHeaders: ["Content-Type", "Authorization", "X-Request-ID"],
        credentials: true
      })
    );
    const limiter = (0, import_express_rate_limit.default)({
      windowMs: this.config.rateLimitWindowMs || 15 * 60 * 1e3,
      // 15 minutes
      max: this.config.rateLimitMaxRequests || 100,
      // limit each IP to 100 requests per windowMs
      message: {
        success: false,
        error: {
          code: ERROR_CODES.RATE_LIMIT_EXCEEDED,
          message: "Too many requests from this IP, please try again later."
        }
      },
      standardHeaders: true,
      legacyHeaders: false
    });
    if (this.apiPrefix) {
      this.app.use(`${this.apiPrefix}/`, limiter);
    }
    this.app.use(import_express10.default.json({ limit: "10mb" }));
    this.app.use(import_express10.default.urlencoded({ extended: true }));
    this.app.use(requestIdMiddleware);
    this.app.use(requestLoggingMiddleware);
  }
  setupRoutes() {
    this.app.get("/health", (_req, res) => {
      const healthData = {
        status: "healthy",
        timestamp: (/* @__PURE__ */ new Date()).toISOString(),
        uptime: process.uptime(),
        version: process.env.npm_package_version || "unknown"
      };
      if (this.config.enableWebSocket) {
        healthData.websocket = {
          enabled: true,
          active: this.isWebSocketActive(),
          stats: this.getWebSocketStats()
        };
      }
      res.json(healthData);
    });
    this.app.get("/ws/stats", (_req, res) => {
      if (!this.config.enableWebSocket) {
        return res.status(404).json({
          success: false,
          error: {
            code: "WEBSOCKET_DISABLED",
            message: "WebSocket is not enabled"
          }
        });
      }
      const stats = this.getWebSocketStats();
      return res.json({
        success: true,
        data: {
          enabled: true,
          active: this.isWebSocketActive(),
          ...stats
        }
      });
    });
    this.app.use(this.buildApiRoute("/message"), createMessageRoutes(this.agent));
    this.app.use(this.buildApiRoute("/sessions"), createSessionRoutes(this.agent));
    this.app.use(this.buildApiRoute("/mcp"), createMcpRoutes(this.agent));
    this.app.use(this.buildApiRoute("/llm"), createLlmRoutes(this.agent));
    this.app.use(this.buildApiRoute("/config"), createConfigRoutes(this.agent));
    this.app.use(this.buildApiRoute("/search"), createSearchRoutes(this.agent));
    this.app.use(this.buildApiRoute("/webhooks"), createWebhookRoutes(this.agent));
    this.app.post(this.buildApiRoute("/connect-server"), (req, res) => {
      req.url = "/servers";
      createMcpRoutes(this.agent)(req, res, () => {
      });
    });
    this.app.get(
      "/.well-known/appspecific/com.chrome.devtools.json",
      (req, res) => {
        res.status(204).end();
      }
    );
    this.app.get("/.well-known/agent.json", (req, res) => {
      try {
        const agentCard = {
          name: "Cipher Agent",
          description: "Memory-powered AI agent framework with real-time communication",
          version: process.env.npm_package_version || "1.0.0",
          capabilities: ["conversation", "memory", "tools", "mcp", "websocket", "streaming"],
          endpoints: {
            base: `${req.protocol}://${req.get("host")}`,
            api: `${req.protocol}://${req.get("host")}${this.apiPrefix || ""}`,
            websocket: `ws://${req.get("host")}/ws`,
            health: `${req.protocol}://${req.get("host")}/health`
          },
          contact: {
            support: "https://github.com/byterover/cipher"
          },
          protocols: ["http", "websocket", "mcp"],
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        };
        res.json(agentCard);
      } catch (error) {
        logger.error("Failed to generate agent card", {
          requestId: req.requestId,
          error: error instanceof Error ? error.message : String(error)
        });
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          "Failed to generate agent discovery data",
          500,
          void 0,
          req.requestId
        );
      }
    });
    this.app.post(this.buildApiRoute("/reset"), async (req, res) => {
      try {
        const { sessionId } = req.body;
        logger.info("Processing global reset request", {
          requestId: req.requestId,
          sessionId: sessionId || "all"
        });
        if (sessionId) {
          const success = await this.agent.removeSession(sessionId);
          if (!success) {
            return errorResponse(
              res,
              ERROR_CODES.SESSION_NOT_FOUND,
              `Session ${sessionId} not found`,
              404,
              void 0,
              req.requestId
            );
          }
        } else {
          const sessionIds = await this.agent.listSessions();
          for (const id of sessionIds) {
            await this.agent.removeSession(id);
          }
        }
        successResponse(
          res,
          {
            message: sessionId ? `Session ${sessionId} reset` : "All sessions reset",
            timestamp: (/* @__PURE__ */ new Date()).toISOString()
          },
          200,
          req.requestId
        );
      } catch (error) {
        const errorMsg = error instanceof Error ? error.message : String(error);
        logger.error("Global reset failed", {
          requestId: req.requestId,
          error: errorMsg
        });
        errorResponse(
          res,
          ERROR_CODES.INTERNAL_ERROR,
          `Reset failed: ${errorMsg}`,
          500,
          process.env.NODE_ENV === "development" ? error : void 0,
          req.requestId
        );
      }
    });
  }
  setup404Handler() {
    this.app.use((req, res) => {
      errorResponse(
        res,
        ERROR_CODES.NOT_FOUND,
        `Route ${req.method} ${req.originalUrl} not found`,
        404,
        void 0,
        req.requestId
      );
    });
  }
  setupErrorHandling() {
    this.app.use(errorLoggingMiddleware);
    this.app.use((err, req, res, next) => {
      if (res.headersSent) {
        return next(err);
      }
      let statusCode = 500;
      let errorCode = ERROR_CODES.INTERNAL_ERROR;
      if (err.name === "ValidationError") {
        statusCode = 400;
        errorCode = ERROR_CODES.VALIDATION_ERROR;
      } else if (err.name === "UnauthorizedError") {
        statusCode = 401;
        errorCode = ERROR_CODES.UNAUTHORIZED;
      }
      errorResponse(
        res,
        errorCode,
        err.message || "An unexpected error occurred",
        statusCode,
        process.env.NODE_ENV === "development" ? err.stack : void 0,
        req.requestId
      );
    });
  }
  async start() {
    if (this.config.mcpTransportType) {
      try {
        await this.setupMcpServer(this.config.mcpTransportType, this.config.mcpPort);
        logger.info(`[API Server] MCP server setup completed successfully`);
      } catch (error) {
        logger.error(
          `[API Server] Failed to setup MCP server: ${error instanceof Error ? error.message : String(error)}`
        );
        throw error;
      }
    }
    this.setup404Handler();
    return new Promise((resolve3, reject) => {
      try {
        this.httpServer = import_http3.default.createServer(this.app);
        if (this.config.enableWebSocket) {
          this.setupWebSocket();
        }
        this.httpServer.listen(this.config.port, this.config.host || "localhost", () => {
          logger.info(
            `API Server started on ${this.config.host || "localhost"}:${this.config.port}`,
            null,
            "green"
          );
          if (this.config.mcpTransportType) {
            const mcpSseEndpoint = this.buildApiRoute("/mcp/sse");
            const mcpEndpoint = this.buildApiRoute("/mcp");
            logger.info(
              `[API Server] MCP SSE endpoints available at ${mcpSseEndpoint} and ${mcpEndpoint}`,
              null,
              "green"
            );
          }
          if (this.config.enableWebSocket) {
            const wsPath = this.config.webSocketConfig?.path || "/ws";
            logger.info(
              `[API Server] WebSocket server available at ws://${this.config.host || "localhost"}:${this.config.port}${wsPath}`,
              null,
              "green"
            );
          }
          resolve3();
        });
        this.httpServer.on("error", (err) => {
          const errorMessage = err.message || err.toString() || "Unknown error";
          logger.error("Failed to start API server:", errorMessage);
          logger.error("Error details:", err);
          reject(err);
        });
        process.on("SIGTERM", () => {
          logger.info("SIGTERM received, shutting down API server gracefully");
          this.shutdownWebSocket();
          this.httpServer?.close(() => {
            logger.info("API server stopped");
            process.exit(0);
          });
        });
        process.on("SIGINT", () => {
          logger.info("SIGINT received, shutting down API server gracefully");
          this.shutdownWebSocket();
          this.httpServer?.close(() => {
            logger.info("API server stopped");
            process.exit(0);
          });
        });
      } catch (error) {
        reject(error);
      }
    });
  }
  getApp() {
    return this.app;
  }
  /**
   * Get WebSocket statistics
   */
  getWebSocketStats() {
    if (!this.wsConnectionManager || !this.wsEventSubscriber) {
      return null;
    }
    return {
      connections: this.wsConnectionManager.getStats(),
      events: this.wsEventSubscriber.getStats(),
      router: this.wsMessageRouter?.getStats()
    };
  }
  /**
   * Send system message to all WebSocket connections
   */
  broadcastSystemMessage(message, level = "info") {
    if (this.wsEventSubscriber) {
      this.wsEventSubscriber.sendSystemMessage(message, level);
    }
  }
  /**
   * Check if WebSocket is enabled and active
   */
  isWebSocketActive() {
    return !!(this.config.enableWebSocket && this.wss && this.wsEventSubscriber?.isActive());
  }
};

// src/app/web/web-server.ts
init_cjs_shims();
var import_child_process2 = require("child_process");
var import_fs8 = require("fs");
var import_path7 = __toESM(require("path"), 1);
var import_url3 = require("url");
init_logger2();
var WebServerManager = class {
  config;
  process = null;
  uiPath;
  constructor(config2) {
    this.config = config2;
    const currentFileUrl = importMetaUrl;
    const currentFilePath = (0, import_url3.fileURLToPath)(currentFileUrl);
    const isCompiledVersion = currentFilePath.includes("/dist/");
    if (isCompiledVersion) {
      this.uiPath = import_path7.default.resolve(import_path7.default.dirname(currentFilePath), "ui");
    } else {
      this.uiPath = import_path7.default.resolve(import_path7.default.dirname(currentFilePath), "../ui");
    }
  }
  async start() {
    logger.info(
      `Starting Web UI server on ${this.config.host || "localhost"}:${this.config.port}`,
      null,
      "green"
    );
    if (!(0, import_fs8.existsSync)(this.uiPath)) {
      throw new Error(`UI directory not found at ${this.uiPath}`);
    }
    const packageJsonPath = import_path7.default.join(this.uiPath, "package.json");
    if (!(0, import_fs8.existsSync)(packageJsonPath)) {
      throw new Error(`UI package.json not found at ${packageJsonPath}`);
    }
    const standalonePath = import_path7.default.join(this.uiPath, ".next", "standalone");
    const standaloneServerPath = import_path7.default.join(standalonePath, "server.js");
    if ((0, import_fs8.existsSync)(standaloneServerPath)) {
      await this.startProduction();
    } else {
      await this.startDevelopment();
    }
  }
  async startProduction() {
    const standalonePath = import_path7.default.join(this.uiPath, ".next", "standalone");
    const serverPath = import_path7.default.join(standalonePath, "server.js");
    logger.info("Using production build (standalone)", null, "cyan");
    const env3 = {
      ...process.env,
      PORT: this.config.port.toString(),
      HOSTNAME: this.config.host || "localhost",
      ...this.config.apiUrl && { API_URL: this.config.apiUrl },
      ...this.config.wsUrl && { NEXT_PUBLIC_WS_URL: this.config.wsUrl },
      // Extract port from API URL for Next.js rewrite rules
      ...this.config.apiUrl && { API_PORT: new URL(this.config.apiUrl).port || "3001" }
    };
    this.process = (0, import_child_process2.spawn)("node", [serverPath], {
      cwd: standalonePath,
      env: env3,
      stdio: ["pipe", "pipe", "pipe"]
    });
    this.setupProcessHandlers();
  }
  async startDevelopment() {
    logger.info("Using development mode", null, "cyan");
    const nodeModulesPath = import_path7.default.join(this.uiPath, "node_modules");
    if (!(0, import_fs8.existsSync)(nodeModulesPath)) {
      logger.info("Installing UI dependencies...", null, "yellow");
      await this.installDependencies();
    }
    const env3 = {
      ...process.env,
      PORT: this.config.port.toString(),
      ...this.config.apiUrl && { API_URL: this.config.apiUrl },
      ...this.config.wsUrl && { NEXT_PUBLIC_WS_URL: this.config.wsUrl },
      // Extract port from API URL for Next.js rewrite rules
      ...this.config.apiUrl && { API_PORT: new URL(this.config.apiUrl).port || "3001" }
    };
    const packageManager = this.detectPackageManager();
    this.process = (0, import_child_process2.spawn)(packageManager, ["run", "dev"], {
      cwd: this.uiPath,
      env: env3,
      stdio: ["pipe", "pipe", "pipe"]
    });
    this.setupProcessHandlers();
  }
  detectPackageManager() {
    if ((0, import_fs8.existsSync)(import_path7.default.join(this.uiPath, "pnpm-lock.yaml"))) {
      return "pnpm";
    }
    if ((0, import_fs8.existsSync)(import_path7.default.join(this.uiPath, "yarn.lock"))) {
      return "yarn";
    }
    return "npm";
  }
  installDependencies() {
    const packageManager = this.detectPackageManager();
    return new Promise((resolve3, reject) => {
      const installProcess = (0, import_child_process2.spawn)(packageManager, ["install"], {
        cwd: this.uiPath,
        stdio: ["pipe", "pipe", "pipe"]
      });
      installProcess.stdout?.on("data", (data) => {
        logger.debug(`${packageManager} install: ${data}`);
      });
      installProcess.stderr?.on("data", (data) => {
        logger.debug(`${packageManager} install error: ${data}`);
      });
      installProcess.on("close", (code) => {
        if (code === 0) {
          logger.info("UI dependencies installed successfully", null, "green");
          resolve3();
        } else {
          reject(new Error(`${packageManager} install failed with code ${code}`));
        }
      });
      installProcess.on("error", (error) => {
        reject(new Error(`Failed to install dependencies: ${error.message}`));
      });
    });
  }
  setupProcessHandlers() {
    if (!this.process) return;
    this.process.stdout?.on("data", (data) => {
      const output = data.toString().trim();
      if (output) {
        logger.info(`[UI] ${output}`, null, "cyan");
      }
    });
    this.process.stderr?.on("data", (data) => {
      const output = data.toString().trim();
      if (output && !output.includes("warn")) {
        logger.warn(`[UI] ${output}`, null, "yellow");
      }
    });
    this.process.on("close", (code) => {
      if (code !== 0) {
        logger.error(`Web UI server exited with code ${code}`);
      } else {
        logger.info("Web UI server stopped", null, "gray");
      }
    });
    this.process.on("error", (error) => {
      logger.error(`Web UI server error: ${error.message}`);
    });
  }
  stop() {
    if (this.process) {
      logger.info("Stopping Web UI server...", null, "yellow");
      this.process.kill("SIGTERM");
      this.process = null;
    }
  }
  isRunning() {
    return this.process !== null && !this.process.killed;
  }
};

// src/app/index.ts
var import_path9 = __toESM(require("path"), 1);
var import_os = __toESM(require("os"), 1);
var import_url4 = require("url");
import_events3.EventEmitter.defaultMaxListeners = 20;
if (typeof globalThis !== "undefined" && globalThis.EventTarget) {
  const originalAddEventListener = globalThis.EventTarget.prototype.addEventListener;
  const listenerCounts = /* @__PURE__ */ new WeakMap();
  globalThis.EventTarget.prototype.addEventListener = function(type, listener, options) {
    if (type === "abort" && this.constructor.name === "AbortSignal") {
      const currentCount = listenerCounts.get(this) || 0;
      if (currentCount >= 15) {
        console.warn(`AbortSignal has ${currentCount} listeners, potential memory leak`);
      }
      listenerCounts.set(this, currentCount + 1);
    }
    return originalAddEventListener.call(this, type, listener, options);
  };
}
function resolveEnvPath() {
  if ((0, import_fs9.existsSync)(".env")) {
    return ".env";
  }
  const currentFileUrl = importMetaUrl;
  const currentFilePath = (0, import_url4.fileURLToPath)(currentFileUrl);
  const projectRoot = import_path9.default.resolve(import_path9.default.dirname(currentFilePath), "../..");
  const envPath = import_path9.default.resolve(projectRoot, ".env");
  return envPath;
}
var detectAndRedirectMcpLogs = () => {
  const args = process.argv;
  const isMcpMode2 = args.includes("--mode") && args[args.indexOf("--mode") + 1] === "mcp";
  if (isMcpMode2) {
    const logFile = process.env.CIPHER_MCP_LOG_FILE || import_path9.default.join(import_os.default.tmpdir(), "cipher-mcp.log");
    logger.redirectToFile(logFile);
    process.stderr.write(`[CIPHER-MCP] Log redirection activated: ${logFile}
`);
  }
};
detectAndRedirectMcpLogs();
var program2 = new Command();
program2.name("cipher").description(
  "Memory-powered AI agent framework with real-time WebSocket communication and MCP integration"
).version(package_default.version, "-v, --version", "output the current version").argument(
  "[prompt...]",
  "Natural-language prompt to run once. If not passed, cipher will start in interactive mode"
).option("--no-verbose", "Disable verbose output").option("-a, --agent <path>", "Path to agent config file", DEFAULT_CONFIG_PATH).option("-s, --strict", "Require all MCP server connections to succeed").option("--new-session [sessionId]", "Start with a new session (optionally specify session ID)").option(
  "--mode <mode>",
  "The application mode for cipher memory agent - cli | mcp | api | ui",
  "cli"
).option("--port <port>", "Port for API server (only used with --mode api or ui)", "3001").option("--ui-port <port>", "Port for UI server (only used with --mode ui)", "3000").option("--host <host>", "Host for API server (only used with --mode api or ui)", "localhost").option(
  "--api-prefix <prefix>",
  "API prefix for routes (default: /api, use empty string to disable)",
  "/api"
).option(
  "--mcp-transport-type <type>",
  "MCP transport type (stdio, sse, streamable-http)",
  "stdio"
).option("--mcp-port <port>", "Port for MCP server (only used with sse, streamable-http)", "3000").option(
  "--mcp-host <host>",
  "Host for MCP server (only used with sse, streamable-http)",
  "localhost"
).option(
  "--mcp-dns-rebinding-protection",
  "Enable DNS rebinding protection for MCP server",
  false
);
program2.description(
  "Cipher CLI allows you to interact with cipher memory agent.\nRun cipher in interactive mode with `cipher` or run a one-shot prompt with `cipher <prompt>`\n\nAvailable modes:\n  - cli: Interactive command-line interface (default)\n  - mcp: Model Context Protocol server mode\n  - api: REST API server mode with WebSocket support\n  - ui: Full-stack web application with UI and API server\n\nWebSocket Features (API mode):\n  \u2022 Real-time AI responses and streaming\n  \u2022 Live tool execution notifications\n  \u2022 Memory operation events\n  \u2022 Session management and error handling\n  \u2022 Available at ws://localhost:3001/ws\n\nOptions:\n  -s, --strict: Require all MCP server connections to succeed (overrides individual server connection modes)\n  --new-session [sessionId]: Start with a new session (optionally specify session ID)\n  --port <port>: Port for API server (default: 3001, only used with --mode api or ui)\n  --ui-port <port>: Port for UI server (default: 3000, only used with --mode ui)\n  --host <host>: Host for API server (default: localhost, only used with --mode api or ui)"
).action(async (prompt = []) => {
  const headlessInput = prompt.join(" ") || void 0;
  const opts = program2.opts();
  if (opts.mode !== "mcp") {
    const envPath = resolveEnvPath();
    if (!(0, import_fs9.existsSync)(envPath)) {
      const errorMsg = `No .env file found at ${envPath}, copy .env.example to .env and fill in the values`;
      logger.error(errorMsg);
      process.exit(1);
    }
  }
  if (!env.OPENAI_API_KEY && !env.ANTHROPIC_API_KEY && !env.OPENROUTER_API_KEY && !env.OLLAMA_BASE_URL) {
    if (!process.env.AWS_ACCESS_KEY_ID || !process.env.AWS_SECRET_ACCESS_KEY || !process.env.AWS_REGION || !process.env.AWS_INFERENCE_PROFILE_ARN) {
      const errorMsg = "No API key or Ollama configuration found, please set at least one of OPENAI_API_KEY, ANTHROPIC_API_KEY, OPENROUTER_API_KEY, OLLAMA_BASE_URL, or AWS credentials (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_REGION) in your environment variables \nAvailable providers: OpenAI, Anthropic, OpenRouter, Ollama, Qwen, Gemini, Azure, AWS, LM Studio";
      if (opts.mode === "mcp") {
        process.stderr.write(`[CIPHER-MCP] ERROR: ${errorMsg}
`);
      } else {
        logger.error(errorMsg);
      }
      process.exit(1);
    }
  }
  try {
    validateCliOptions(opts);
  } catch (err) {
    handleCliOptionsError(err);
  }
  let agent;
  try {
    const configPath = resolveConfigPath(opts.agent);
    logger.info(`Loading agent config from ${configPath}`);
    if (!(0, import_fs9.existsSync)(configPath)) {
      const configErrorMsg = `Config file not found at ${configPath}`;
      const helpMsg = opts.agent === DEFAULT_CONFIG_PATH ? "Please ensure the config file exists or create one based on memAgent/cipher.yml" : `Please ensure the specified config file exists at ${configPath}`;
      if (opts.mode === "mcp") {
        process.stderr.write(`[CIPHER-MCP] ERROR: ${configErrorMsg}
[CIPHER-MCP] ${helpMsg}
`);
      } else {
        logger.error(configErrorMsg);
        logger.error(helpMsg);
      }
      process.exit(1);
    }
    const cfg = await loadAgentConfig(configPath);
    if (opts.strict && cfg.mcpServers) {
      logger.info("Applying strict mode to all MCP server connections");
      for (const [serverName, serverConfig] of Object.entries(cfg.mcpServers)) {
        logger.debug(`Setting connection mode to strict for server: ${serverName}`);
        serverConfig.connectionMode = "strict";
      }
    }
    agent = new MemAgent(cfg, opts.mode);
    await agent.start();
    if (opts.newSession !== void 0) {
      try {
        const sessionId = typeof opts.newSession === "string" && opts.newSession ? opts.newSession : void 0;
        const session = await agent.createSession(sessionId);
        logger.info(`Created and loaded new session: ${session.id}`, null, "green");
      } catch (err) {
        logger.error(
          `Failed to create new session: ${err instanceof Error ? err.message : String(err)}`
        );
        process.exit(1);
      }
    }
    if (agent.services && agent.services.embeddingManager) {
      agent.services.embeddingManager.getEmbedder("default");
    } else {
      console.log("No embeddingManager found in agent.services");
    }
  } catch (err) {
    const errorMessage = err instanceof Error ? err.message : String(err);
    const configPath = resolveConfigPath(opts.agent);
    if (opts.mode === "mcp") {
      if (opts.strict) {
        process.stderr.write(
          `[CIPHER-MCP] ERROR: Failed to load agent config from ${configPath} (strict mode enabled): ${errorMessage}
`
        );
        process.stderr.write(
          `[CIPHER-MCP] Strict mode requires all MCP server connections to succeed. Check your MCP server configurations or run without --strict flag.
`
        );
      } else {
        process.stderr.write(
          `[CIPHER-MCP] ERROR: Failed to load agent config from ${configPath}: ${errorMessage}
`
        );
      }
    } else {
      if (opts.strict) {
        logger.error(
          `Failed to load agent config from ${configPath} (strict mode enabled):`,
          errorMessage
        );
        logger.error(
          "Strict mode requires all MCP server connections to succeed. Check your MCP server configurations or run without --strict flag to allow lenient connections."
        );
      } else {
        logger.error(`Failed to load agent config from ${configPath}:`, errorMessage);
      }
    }
    process.exit(1);
  }
  if (headlessInput) {
    try {
      await startHeadlessCli(agent, headlessInput);
      process.exit(0);
    } catch (err) {
      const errorMessage = err instanceof Error ? err.message : String(err);
      if (opts.mode === "mcp") {
        process.stderr.write(
          `[CIPHER-MCP] ERROR: Failed to execute headless command: ${errorMessage}
`
        );
      } else {
        logger.error(`Failed to execute headless command: ${errorMessage}`);
      }
      process.exit(1);
    }
  }
  async function startApiMode(agent2, options) {
    const port = parseInt(options.port) || 3001;
    const host = options.host || "localhost";
    const mcpTransportType = options.mcpTransportType || void 0;
    const mcpPort = options.mcpPort ? parseInt(options.mcpPort, 10) : void 0;
    const apiPrefix = process.env.CIPHER_API_PREFIX !== void 0 ? process.env.CIPHER_API_PREFIX === '""' ? "" : process.env.CIPHER_API_PREFIX : options.apiPrefix;
    logger.info(`Starting API server on ${host}:${port}`, null, "green");
    const apiServer = new ApiServer(agent2, {
      port,
      host,
      corsOrigins: ["http://localhost:3000", "http://localhost:3001"],
      // Default CORS origins
      rateLimitWindowMs: 15 * 60 * 1e3,
      // 15 minutes
      rateLimitMaxRequests: 100,
      // 100 requests per window
      // Enable WebSocket by default for API mode
      enableWebSocket: true,
      webSocketConfig: {
        path: "/ws",
        maxConnections: 1e3,
        connectionTimeout: 3e5,
        // 5 minutes
        heartbeatInterval: 3e4,
        // 30 seconds
        enableCompression: true
      },
      apiPrefix,
      // Add API prefix configuration
      ...mcpTransportType && { mcpTransportType },
      // Only include if defined
      ...mcpPort !== void 0 && { mcpPort }
      // Only include if defined
    });
    try {
      await apiServer.start();
      logger.info(`API server is running and ready to accept requests`, null, "green");
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      if (opts.mode === "mcp") {
        process.stderr.write(`[CIPHER-MCP] ERROR: Failed to start API server: ${errorMsg}
`);
      } else {
        logger.error(`Failed to start API server: ${errorMsg}`);
      }
      process.exit(1);
    }
  }
  async function startUiMode(agent2, options) {
    const apiPort = parseInt(options.port) || 3001;
    const uiPort = parseInt(options.uiPort) || 3e3;
    const host = options.host || "localhost";
    const mcpTransportType = options.mcpTransportType || void 0;
    const mcpPort = options.mcpPort ? parseInt(options.mcpPort, 10) : void 0;
    const apiPrefix = process.env.CIPHER_API_PREFIX !== void 0 ? process.env.CIPHER_API_PREFIX === '""' ? "" : process.env.CIPHER_API_PREFIX : options.apiPrefix;
    logger.info(
      `Starting UI mode - API server on ${host}:${apiPort}, UI server on ${host}:${uiPort}`,
      null,
      "green"
    );
    const apiServer = new ApiServer(agent2, {
      port: apiPort,
      host,
      corsOrigins: [`http://${host}:${uiPort}`, `http://localhost:${uiPort}`],
      // Allow UI to connect
      rateLimitWindowMs: 15 * 60 * 1e3,
      // 15 minutes
      rateLimitMaxRequests: 100,
      // 100 requests per window
      // Enable WebSocket by default for UI mode
      enableWebSocket: true,
      webSocketConfig: {
        path: "/ws",
        maxConnections: 1e3,
        connectionTimeout: 3e5,
        // 5 minutes
        heartbeatInterval: 3e4,
        // 30 seconds
        enableCompression: true
      },
      apiPrefix,
      // Add API prefix configuration
      ...mcpTransportType && { mcpTransportType },
      ...mcpPort !== void 0 && { mcpPort }
    });
    try {
      await apiServer.start();
      logger.info(`API server is running on ${host}:${apiPort}`, null, "green");
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error(`Failed to start API server: ${errorMsg}`);
      process.exit(1);
    }
    const webServer = new WebServerManager({
      port: uiPort,
      host,
      apiUrl: `http://${host}:${apiPort}`,
      wsUrl: `ws://${host}:${apiPort}`
    });
    try {
      await webServer.start();
      logger.info(`Web UI server is running on http://${host}:${uiPort}`, null, "green");
      logger.info(`You can now access the Cipher UI at http://${host}:${uiPort}`, null, "cyan");
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : String(error);
      logger.error(`Failed to start Web UI server: ${errorMsg}`);
      logger.info("Stopping API server due to UI startup failure...");
      process.exit(1);
    }
    const handleShutdown = async () => {
      logger.info("Shutting down UI mode...", null, "yellow");
      webServer.stop();
      await new Promise((resolve3) => setTimeout(resolve3, 1e3));
      process.exit(0);
    };
    process.on("SIGINT", handleShutdown);
    process.on("SIGTERM", handleShutdown);
  }
  if (opts.mode === "cli") {
    const eventManager = agent.services?.eventManager;
    if (eventManager) {
      const handleExit = async () => {
        try {
          const sessionId = agent.getCurrentActiveSessionId() || "default";
          eventManager.emitSessionEvent(sessionId, "session:expired", {
            sessionId,
            timestamp: Date.now()
          });
          await new Promise((res) => setTimeout(res, 200));
        } catch {
        }
        process.exit(0);
      };
      process.on("SIGINT", handleExit);
      process.on("SIGTERM", handleExit);
    }
  }
  switch (opts.mode) {
    case "cli":
      await startInteractiveCli(agent);
      break;
    case "mcp":
      await startMcpMode(agent, opts);
      break;
    case "api":
      await startApiMode(agent, opts);
      break;
    case "ui":
      await startUiMode(agent, opts);
      break;
    default: {
      const errorMsg = `Unknown mode '${opts.mode}'. Use cli, mcp, api, or ui.`;
      if (opts.mode === "mcp") {
        process.stderr.write(`[CIPHER-MCP] ERROR: ${errorMsg}
`);
      } else {
        logger.error(errorMsg);
      }
      process.exit(1);
    }
  }
});
program2.parseAsync(process.argv);
